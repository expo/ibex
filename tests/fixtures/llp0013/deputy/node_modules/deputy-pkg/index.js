// A deputy: granted fs:read, it reads whatever path a caller hands it. Under
// default attribution this acts with its own authority for anyone (the
// confused-deputy hazard). @ref LLP 0013#phase-5
var fs = require("fs");
module.exports.readFor = function readFor(path) {
  return String(fs.readFileSync(path)).trim();
};
