// Requests fs:read+fs:write and delegates fs:read down its fast-codec edge
// (package.json `ibex` field): a request, never a grant — the app's import
// site is the grant root, and the cascade intersects down this edge.
const decode = require('fast-codec');
module.exports = function processImages() {
  return "processed:" + decode();
};
