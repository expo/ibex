// @ref LLP 0013#mechanism-2 (Phase 3) — even with eval/Function endowed, code
// they produce must inherit evil-pkg's compartment (bound at the eval call
// site), so `process` stays withheld — no escape via dynamic code.
module.exports.probe = function () {
  var out = [];
  try { out.push("fn=" + (new Function("return typeof process"))()); } catch (e) { out.push("fn-threw"); }
  try { out.push("directEval=" + eval("typeof process")); } catch (e) { out.push("eval-threw"); }
  return out.join(" ");
};
