# Review: LLP 0055 S4 implementation — grok (code round 1 + delta)

**Reviewer:** grok-4.6, effort xhigh, headless (`--prompt-file`, `--output-format plain`), full repository access.
**Date:** 2026-08-25. **Requested by:** the LLP 0055 S4 lane (dual-family code round; mutually blind from the codex round).
**Round 1 target:** commit a70c8cd6a on cd9a40d5c. **Verdict:** READY — no materials; 3 minors (1 and 3 addressed by the fix round; 2 adopted as the restart-family mapping).
**Delta target:** commit eeb07d472 (fix round). **Verdict:** READY — no materials; 3 minors recorded; all six fix-round adjudications CORRECT (including adjudicating the pending identity-conflict seam FOR the cross-family content-binding reading).

---

## Round 1 (verbatim)

I'll review the S4 commit against LLP 0055 (Accepted r10 + §12 ask-2 ruling) and the listed files, without modifying anything. First I'll load the spec, ticket, and the exact HEAD diff.The ticket still lists S4 as open; I’ll read the spec sections and the HEAD diff next, then verify each load-bearing claim against the code.I'll load the spec's load-bearing sections and the implementation files in parallel so each claim can be checked against the actual code.Spec sections are in hand. Next I’ll walk the implementation and the `hot_revision.rs` delta against each load-bearing claim.I've got the admission pipeline. Next I'll read the F9 fixtures and check the `hot_revision.rs` tests for any weakened assertions.## Verdict
READY

## Material Findings
None.

## Minor Findings
1. `HotUpdateConsumerSessionV1::admit` treats “in flight” as `surface.is_in_flight()` only (`hot_update.rs:328–334`). A reserved different-`updateId` pending row is not occupancy, and `terminal_count` (`hot_update.rs:339–345`, `673`) ignores pendings. A host that retains multiple `HotUpdateAdmittedV1` tokens without `begin_admitted` can insert unbounded pending rows (the 4096 bound is terminal-only). Sequential owner-thread drive has at most one pending; `Drop` of an unconsumed handle poisons the session (`715–720`). Spec §5.2/§6 busy+capacity law still holds for the intended pipeline.

2. Check-3 algebra errors are classified by `message.contains` (`hot_update.rs:538–548`). Empty-invalidation and stale-base map to `KeepLastGood`; graph-widening maps to `FullReloadCurrentAuthority` — the F9 rows. Authority drift (`generation.rs:616`, “regenerate policy and restart”), builtin/synthetic invalidation (`generation.rs:1168–1170`, same family), and production `begin` (`generation.rs:1140–1141`) fall through to `FullReloadCurrentAuthority`. `RegeneratePolicyAndRestartRuntime` is never constructed (`hot_update.rs:187`). That contradicts LLP 0055 §10 for the restart-family rows. Check 1’s stamp comparison makes authority drift at `begin` unlikely when expectations match the armed snapshot; builtin invalidation remains a reachable host-supplied `invalidated` path.

3. Spec §5.3.7 finalizes the pending→terminal write inside the commit fence so a later exact duplicate always has the prior receipt. This slice sequences it as a host call (`settle_committed` at `hot_update.rs:573–585`) after `HotRevisionSurfaceV1::commit` returns. A successful publish followed by a dropped `HotUpdateSettlementV1` poisons the session (`739–744`) instead of minting the committed receipt. Fail-closed, and correct owner-thread glue has no JS in the window, but the “prior receipt always exists” commit rule is a host protocol, not an in-bundle write.

## Judgment Calls
**(a) AGREE.** §5.2 check-3 prose names the live v2 digest before `begin_revision`, but coordinates-first is the ask-2 law. After a commit, a stale-base envelope still carries the *old* graph digest; digest-first would answer `full-reload` for a revision race. Coordinates-first (`hot_update.rs:490–506`) yields keep-last-good with the consumer’s committed `(g, r)`; a digest mismatch after that equality (`553–560`) is graph desync at the live coordinate and correctly `FullReloadCurrentAuthority`. F9’s post-commit witness (`1291–1328`) depends on this.

**(b) AGREE.** After `execution_generation`/`base_hot_revision` equal `live` (`490–506`), passing `live` into `surface.begin` (`512`) is the claimed base by value. The algebra CAS (`generation.rs:1146–1151`) is the §5.2 item-8 backstop. Owner-thread, no concurrent mutator, so it cannot fire on the honest path.

**(c) AGREE.** `hot_update_payload_digest` is `digest_bytes(HOT_UPDATE_SIGNATURE_DOMAIN_V1, payload)` (`750–752`) — SHA-256 of `domain || 0x00 || payload` (`artifact.rs:713–716`). The Ed25519 message is `domain || '\n' || JCS body` (`754–760`). Same domain *string*, different algorithm and separator; the helper is the producer/consumer contract. §6 does not name a payload-digest domain.

## Verified Claims
1. **VERIFIED.** `authenticate` (`363–444`): signed-body ≤64 KiB and payload ≤16 MiB (`368–373`) → Ed25519 over `signing_message` (`374–380`, `754–760`) → `parse_strict` + JCS round-trip byte-equality (`382–390`) → `deny_unknown_fields` serde + I-JSON bounds (`391–393`, `71–89`) → schema (`394–396`) → session/addressing (`398–435`) → payload digest (`439–443`). Currency is explicitly not compared (`437–438`). Then rotation (`292–296`) before any `replay.get` (`306`); check 2 (`306–324`); busy (`328–334`); capacity (`339–345`); pending insert (`347–361`); check 3 in `begin_admitted` (`461–569`).
2. **VERIFIED.** Check-1 failures return `Unauthenticated` (`285–288`) with no table write. F9 helpers assert `replay.is_empty()` then a later legitimate same-`updateId` admits (`887–924`, `1045–1225`).
3. **VERIFIED.** Ordinary check-3 refusals go through `refuse_admitted` → `terminalize_matching_pending` before the `Err` is returned (`474–506`, `538–560`, `610–630`). `assert_check3_terminalized` (`936–984`) asserts pending, then exact-retry receipt equality, then different-bytes `IdentityConflict`.
4. **VERIFIED.** Check 2 precedes currency (`306–324` before `490–506`). `f9_happy_path_commits_and_post_commit_duplicate_replays_old_currency_receipt` (`1026–1042`) and the post-commit half of `1291–1301` return the committed receipt for the old-base envelope.
5. **VERIFIED.** Pending same-id → `Busy` (`308–310`); different-id in flight → `Busy` and no row (`328–334`, `1368–1395`); host-driven `surface.begin` → `Busy` and empty table (`1409–1426`); `begin_admitted` busy unreserves (`527–536`). None of these paths `terminalize_*`.
6. **VERIFIED** (terminal occupancy; pending growth is minor 1). `HOT_UPDATE_REPLAY_CAPACITY == 4096` is asserted (`36`, `1469`). New ids at capacity get the runId-rotation diagnostic, are not inserted, and do not move coordinates (`339–345`, `1496–1515`); duplicates still replay (`1511–1515`). Never evicts (`terminal_count` only increases, `673`). Replay-table memory is otherwise `BTreeMap` of at most 4096 terminals, each bounded by the 64 KiB signed body plus the receipt.
7. **VERIFIED.** Receipt union is `{Committed, CommittedDegraded, Refused}` (`191–205`) — no quarantined member. `settle_quarantined` sets the poison flag and leaves the pending row (`603–608`, `1641–1644`). Drop of unsettled admitted/settlement handles poisons (`715–720`, `739–744`, `1565–1594`). Stranded duplicates answer `RotationRequired`, not busy (`292–296`, `1537–1545`). A new `runId` session starts with an empty table; the old envelope fails check 1 (`1547–1554`).
8. **VERIFIED.** `HotUpdateSigningSessionV1` holds `Ed25519KeyPair` with no serde, a redacting `Debug`, and no private-key accessor (`93–161`, `104–112`). `verifier()` copies public bytes only (`130–134`, `163–166`). Rotation is minting a new session. Signed message is domain-prefixed (`754–760`). Matches LLP 0042’s custody paragraph.
9. **VERIFIED.** Signed body ≤64 KiB before parse (`368–370`, `1199–1213`); payload ≤16 MiB before digest (`371–373`, `1215–1224`); `invalidated.len() > 512` at check 3 (`482–488`, `35`). `issued_at_ms` is only an I-JSON bound and an identity-conflict differentiator (`83`, `972`, `1454`) — never compared to a consumer clock.
10. **VERIFIED.** Stale generation/revision → `KeepLastGood` with `committed coordinates are generation {} revision {}` (`490–506`, `1227–1241`, `1313–1315`) per the §12 ask-2 ruling. Digest mismatch at current coordinates → `FullReloadCurrentAuthority` (`553–560`, `1317–1328`).
11. **VERIFIED.** Table lives on `HotUpdateConsumerSessionV1` (`228–238`), not `HotRevisionSurfaceV1`. Key is `updateId`; value binds `envelope_digest` (`218–226`, `175–180`, `347–351`). `Rc<Cell<bool>>` rotation/in-flight flags are `!Send`/`!Sync` and match the LLP 0002/0003 owner-thread serialized drive contract (same pattern as the S3 flight guard).
12. **VERIFIED.** `begin` is `&mut self` with the same quarantined/busy/`begin_revision` body (`hot_revision.rs:90–116`). `evaluated()` additionally requires a `TopLevelAwait` shadow receipt when `has_top_level_await` (`212–231`, `generation.rs:1439–1445`); non-TLA still settles on Evaluation/CommonJsCache with the same `"staged evaluation has not settled"` string (`hot_revision.rs:717`). All eight pre-existing `hot_revision.rs` tests remain; busy/quarantine/preflight/foreign-surface strings are unchanged.
13. **VERIFIED.** F9 helpers assert table emptiness/pending/terminal, receipt `==`, class, and message (`887–984` and the `f9_*` tests). No pre-existing assertion was deleted or weakened in `hot_revision.rs` / `generation.rs`. S4 ticket scope (keypair, `ibex/hot-update-signature/1`, full tuple before staging, F9 split) is present; HTTP/WS negatives remain Exact H2 as the spec says.

---

## Delta round (verbatim)

I'll run a delta review of the LLP 0055 S4 fix against the spec: read authority, the predecessor notes, and the `eeb07d472` change, then re-check each admission-semantics item.Next I’ll pull the rest of the spec, predecessor reviews, and the fix-commit range so I can re-check admission semantics against the text.I'll load the S4 predecessor review, the spec sections in play, and the full fix-commit diff next.Predecessor S4 review artifacts aren’t under `llp/reviews/` yet; I’ll pull the implementation and the two S4 commits directly.The fix diff is truncated in the middle; I’ll read the current `hot_update.rs` and the predecessor version of the changed functions.## Verdict
READY

## Material Findings
None.

## Minor Findings
1. `ReplayEntry::Pending { envelope_digest: Option<Digest> }` (`src/module_loader/hot_update.rs:218-221`) uses `Option::take` as a move-out (`:735-738`, `:763-766`). No constructor writes `None`; a failed `take` returns false and leaves `consumed`/`settled` unset (`:645-647`, `:669-673`, `:709-711`), so Drop poisons (`:843-848`, `:867-872`) and the rotation gate (`:294-299`) runs before the `Pending { .. }` wildcard (`:316-322`). On a live session `None` is not observable. If it were, that wildcard would answer `update-identity-conflict` for an exact pending duplicate instead of busy (§5.2 check 2, §6 pending duplicate). Representation smell, not a live occupancy hole.

2. Restart-family mapping at `begin_admitted` is substring-matched (`:572-576`) and fixture-proven only for authority drift (`:1423-1454`). The other `begin_revision` restart-family refusal — builtin/synthetic invalidation (`src/module_loader/generation.rs:1168-1170`) — is classified by the same `"regenerate policy and restart"` arm and has no S4 fixture. Integrity-pinned and defining-principal fire in `validate_graph` at preflight/commit (`generation.rs:634-647`), not `begin`; `settle_refused` still takes a caller-supplied class. Not a begin-pipeline miss; the named quartet is not all executed here.

3. Foreign `commit_admitted` (`hot_update.rs:619-623`) returns `Err` before `surface.commit`, so the by-value `HotRevisionReadyToPublishV1` drops: last-good abort, `in_flight` released, origin poisoned via settlement Drop. Fail-closed and parallel to the tested `settle_refused` path (`:1907-1973`), but that drop of `ready` is untested.

## Adjudications
1. **CORRECT.** After check 2, occupancy is `pending_update.is_some() || surface.is_in_flight()` (`:341-347`). A reserved row is in flight per the r8 busy gate (§5.2: different-`updateId` answers busy with no entry before capacity/reservation; §6: one accepted-for-processing pending). Predecessor `surface.is_in_flight()`-only allowed a second reservation before `begin`. Fixture `f9_pending_reservation_occupies_admission_before_surface_begin` (`:1535-1598`) admits `pending-a` without `begin`, busy-nacks `pending-b` with the occupancy diagnostic, and asserts `pending-b` is absent from the table. Host-driven `begin` without a consumer pending still occupies via `is_in_flight()` (`:1708-1728`). Quarantine leaves `pending_update` set on purpose (`:676-688`, `:629-637`); the rotation gate (`:294-299`) runs first, so a stranded row cannot busy-nack a poisoned session. No path sets `consumed`/`settled` without either terminalizing/removing the row or poisoning; Drop covers dropped handles. A held-but-unsettled handle answering busy is occupancy, not a forever-stranded leak.

2. **CORRECT.** §6’s lead law is unqualified: one `updateId` maps to exactly one signed-envelope digest; the table is `(updateId) → (envelopeDigest, entry)` from reservation. §5.2 check 2 names three cases; only the middle one is scoped to terminal (“exact duplicate of a **terminal** entry”). The third — same-`updateId`/different-digest → `update-identity-conflict` — is not. §6’s pending bullet answers **busy** for a **duplicate**; elsewhere that word is same-id and same digest (terminal bullet; F9: “a duplicate of a PENDING update answers busy”). Binding the digest at `Pending { Some(...) }` (`:362-367`) and digest-discriminating at check 2 (`:311-322`) is the content-binding law, not a state-qualified extra. Busy for different bytes would tell the producer to retry a payload that must conflict after settle. Exact pending duplicate still busy (`:311-315`); bound digest retained (`:1621-1626`). Predecessor “all pending same-id → busy” matched clause 1 read in isolation and ignored clause 3 plus the table type. New fixture `f9_pending_update_id_is_content_bound_before_settlement` (`:1601-1665`) is an honest witness (signed remint, `issued_at_ms` only, so it still passes check 1).

3. **CORRECT.** §10 assigns authority drift, integrity-pinned change, defining-principal change, and builtin/synthetic invalidation to `regenerate-policy-and-restart-runtime` and forbids answering those rows with a plain reload. `begin_admitted` now maps `"regenerate policy and restart" | "restart required" | "changed a module defining principal"` to `RegeneratePolicyAndRestartRuntime` (`:572-576`) before the FullReload default. Authority drift is executed and asserted (`:1423-1454`, message from `validate_authority` at `generation.rs:616`). Builtin/synthetic is a real `begin_revision` error (`generation.rs:1168-1170`) and hits the same arm. Integrity-pinned / defining-principal strings are in the matcher but are not `begin()` errors (minor 2). Graph-widening invalidation stays FullReload (`:577-578` / §10). Stale-base stays KeepLastGood (`:529-533`) under the §12 ask-2-taken annotation.

4. **CORRECT.** `settle_committed` is gone. `commit_admitted` (`:612-648`) runs `surface.commit` and `terminalize_pending` under `&mut self` on the owner thread; LLP 0002/0003 gives no interleaved `admit` after a successful commit (§5.3.7 / §6: pending becomes terminal in-fence). Commit-time `Err` poisons the origin, marks `settled` so Drop is a no-op, and strands the reservation (`:631-637`) — closed receipt union, no fourth outcome. Panic in the activation flip is proven (`:1976-2027`): rotation-required, pending stranded, no receipt. `consumed`/`settled` flip only after a successful table write (`:643-647`, `:669-673`), closing the predecessor “mark settled then maybe fail to terminalize” window.

5. **CORRECT.** Foreign handles are refused by `Rc::ptr_eq` on the session’s `rotation_required` cell (`:490-496`, `:619-623`, `:658-660`, `:678-680`), not `debug_assert`. Early returns do not touch the receiving session’s `replay` / `pending_update` / poison bit. They also do not set `consumed`/`settled`, so Drop poisons **origin** (`:843-848`, `:867-872`). `f9_foreign_session_handles_mutate_only_their_origin_via_drop` (`:1907-1973`) asserts the exact begin diagnostic, empty unpoisoned foreign tables, origin still `Pending`, origin poisoned, surface not in flight; the settlement half shows `settle_refused` is a silent no-op on foreign and still Drop-poisons origin. Table flags move only after `terminalize_*` / `remove_pending` succeed. Foreign `commit_admitted` dropping `ready` is the same fail-closed shape (minor 3).

6. **CORRECT.** `RotationRequired`, `IdentityConflict`, and `CapacityRotationRequired` carry `KeepLastGood` (`:785-801`, set at `:295-297`, `:316-321`, `:329-334`, `:353-354`) matching §10 occupancy/conflict rows. Busy and Duplicate stay classless (occupancy nack / idempotent prior outcome; Duplicate is “not a refusal” in §10). `HotUpdateAdmittedV1::payload()` (`:833-840`) returns the digest-verified bytes stored at reserve (`:361-374`); `begin_admitted` docs (`:476-481`) bind H2 derivation of `invalidated` / staged records to those bytes (§6 + §13.1). `commit_update` asserts `admitted.payload() == rig.payload` (`:1123`) and signs `PAYLOAD` naming the staged id; it does not decode — that is the documented S4/H2 split, not a silent skip. `Cargo.lock` lists `ring` on the root package (`Cargo.lock:1093`; `Cargo.toml:124` plus `[patch.crates-io]` vendor pin at `:314-320`).
