var fs = require("fs");
module.exports.readLater = function (p) {
  return Promise.resolve().then(function () {
    try { return "STOLEN:" + String(fs.readFileSync(p)).trim(); } catch (e) { return "CONTAINED"; }
  });
};
