// @ref LLP 0013#mechanism-2 (Phase 3) — this package is NOT rewritten by the
// bundler (it runs unbundled). Its bare `process` reference and its sloppy-mode
// `this` UMD escape both resolve through the engine, so containment here is the
// native per-Domain compartment global, not a source transform.
module.exports.probe = function probe() {
  var bare = typeof process;
  var viaThis = (function () { return this; })();
  var thisProcess = viaThis ? typeof viaThis.process : "no-this";
  var windowProcess = typeof window.process;
  var windowSecret = typeof window.sessionSecret;
  var aliases = window === globalThis && globalThis === global && global === self;
  var registry = typeof __compartments === "object" ? __compartments : null;
  var ownKey = "evil-pkg@1.0.0";
  var ownBefore = !!registry && registry[ownKey] === globalThis;
  var victimBefore = registry ? typeof registry["victim-pkg@1.0.0"] : "no-registry";
  var bindingPreserved = true;
  if (registry) {
    var fakeRegistry = { "victim-pkg@1.0.0": globalThis };
    try { globalThis.__compartments = fakeRegistry; } catch (_) {}
    try {
      Object.defineProperty(globalThis, "__compartments", { value: fakeRegistry });
    } catch (_) {}
    try { delete globalThis.__compartments; } catch (_) {}
    bindingPreserved = __compartments === registry;
    try { registry["victim-pkg@1.0.0"] = globalThis; } catch (_) {}
    try {
      Object.defineProperty(registry, "victim-pkg@1.0.0", { value: globalThis });
    } catch (_) {}
    try { delete registry[ownKey]; } catch (_) {}
    try { Object.setPrototypeOf(registry, globalThis); } catch (_) {}
    try { Object.freeze(registry); } catch (_) {}
  }
  var ownAfter = !!registry && registry[ownKey] === globalThis;
  var victimAfter = registry ? typeof registry["victim-pkg@1.0.0"] : "no-registry";
  return "bare-process=" + bare +
    " this-process=" + thisProcess +
    " window-process=" + windowProcess +
    " window-secret=" + windowSecret +
    " self-aliases=" + aliases +
    " registry-binding=" + bindingPreserved +
    " own-registry=" + ownBefore + "/" + ownAfter +
    " victim-registry=" + victimBefore + "/" + victimAfter;
};
