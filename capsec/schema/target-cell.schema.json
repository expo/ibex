{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ibex.dev/capsec/schema/target-cell.schema.json",
  "title": "Ibex capsec target conformance cells",
  "type": "object",
  "required": ["targetCellSchema", "profile", "cells"],
  "properties": {
    "targetCellSchema": { "const": "ibex/capsec-target-cells/1" },
    "profile": { "const": "ibex/capsec/1" },
    "cells": {
      "type": "array",
      "items": { "$ref": "#/$defs/cell" }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "cell": {
      "type": "object",
      "required": ["edgeId", "target", "disposition", "implementationBranchIds", "fixtures", "rationale"],
      "properties": {
        "edgeId": {
          "$ref": "https://ibex.dev/capsec/schema/common.schema.json#/$defs/stableId"
        },
        "target": {
          "type": "object",
          "required": ["triple", "features"],
          "properties": {
            "triple": { "type": "string", "pattern": "^[a-z0-9_]+(?:-[a-z0-9_]+){2,}$" },
            "features": {
              "type": "array",
              "uniqueItems": true,
              "items": {
                "$ref": "https://ibex.dev/capsec/schema/common.schema.json#/$defs/stableId"
              }
            }
          },
          "additionalProperties": false
        },
        "disposition": {
          "enum": ["enforced", "closed", "non-capability", "absent", "unsupported"]
        },
        "implementationBranchIds": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "https://ibex.dev/capsec/schema/common.schema.json#/$defs/stableId"
          }
        },
        "fixtures": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "https://ibex.dev/capsec/schema/common.schema.json#/$defs/stableId"
          }
        },
        "rationale": { "type": "string", "minLength": 1 }
      },
      "allOf": [
        {
          "if": {
            "properties": { "disposition": { "enum": ["enforced", "closed", "non-capability"] } },
            "required": ["disposition"]
          },
          "then": {
            "properties": {
              "implementationBranchIds": { "type": "array", "minItems": 1 },
              "fixtures": { "type": "array", "minItems": 1 }
            }
          }
        },
        {
          "if": {
            "properties": { "disposition": { "const": "absent" } },
            "required": ["disposition"]
          },
          "then": {
            "properties": {
              "implementationBranchIds": { "type": "array", "maxItems": 0 },
              "fixtures": { "type": "array", "minItems": 1 }
            }
          }
        },
        {
          "if": {
            "properties": { "disposition": { "const": "unsupported" } },
            "required": ["disposition"]
          },
          "then": {
            "properties": {
              "fixtures": { "type": "array", "maxItems": 0 }
            }
          }
        }
      ],
      "additionalProperties": false
    }
  }
}
