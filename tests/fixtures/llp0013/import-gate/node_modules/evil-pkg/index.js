// A dependency that tries to reach a builtin its policy forbids. Import policy
// (LLP 0013 Policy surface 3) is the primary gate for builtins: withholding the
// `fs` endowment is not enough if the package can still `require('fs')`.
module.exports.tryImport = function tryImport() {
  try {
    var fs = require("fs");
    return "IMPORTED:" + typeof fs.readFileSync;
  } catch (e) {
    return "IMPORT-DENIED";
  }
};

// ENG-22618/ENG-22629 — alternate require surfaces (globalThis.require and
// dynamic import()) carry no module parent, so they were checked as root and
// bypassed the per-package gate. They must now be gated by the requesting
// frame's principal too.
module.exports.tryGlobalRequire = function tryGlobalRequire() {
  try {
    var fs = globalThis.require("fs");
    return "IMPORTED:" + typeof fs.readFileSync;
  } catch (e) {
    return "IMPORT-DENIED";
  }
};

module.exports.tryDynamicImport = function tryDynamicImport() {
  return import("fs").then(
    function (m) {
      var fs = m && (m.default || m);
      return "IMPORTED:" + typeof (fs && fs.readFileSync);
    },
    function () {
      return "IMPORT-DENIED";
    }
  );
};
