# LLP 0024 Hermes patch 0010 — completion-record discriminator
# Class: B (one virtual accessor, one boolean field, two one-line insertion
#         points on the existing evaluate paths)
# @ref LLP 0024#6-evaluation-outcomes-and-the-abi
#
# The VM's CallResult for a program evaluation carries a distinct Empty
# completion, but JSI collapses it into `undefined` while converting the
# result, so an embedder cannot tell a declaration/empty completion from a
# program whose value IS `undefined` without a syntactic last-expression
# heuristic. This patch records `res->isEmpty()` at both evaluation sites
# (source and prepared bytecode) and exposes it through an owner-thread
# `lastEvaluationResultWasEmpty()` accessor plus the
# `HERMES_HAS_COMPLETION_RECORD` feature-test macro, so structured evaluation
# preserves Script completion semantics.
#
diff --git a/API/hermes/hermes.cpp b/API/hermes/hermes.cpp
index d8b99e41b14672a495a22276d7b7420138b29d15..65200e0d963bfeaaa3d59f6d55f7c4fca24415d4 100644
--- a/API/hermes/hermes.cpp
+++ b/API/hermes/hermes.cpp
@@ -1262,6 +1262,7 @@ class HermesRuntimeImpl final : public HermesRuntime,
   void *getVMRuntimeUnsafe() const override;
   size_t rootsListLengthForTests() const override;
   void setJSErrorMetadataPropertyAccessEnabled(bool enabled) override;
+  bool lastEvaluationResultWasEmpty() const override;
 
   ManagedValues<vm::PinnedHermesValue> hermesValues_;
   ManagedValues<vm::WeakRoot<vm::JSObject>> weakHermesValues_;
@@ -1275,6 +1276,7 @@ class HermesRuntimeImpl final : public HermesRuntime,
   /// Compilation flags used by prepareJavaScript().
   ::hermes::hbc::CompileFlags compileFlags_{};
   bool jSErrorMetadataPropertyAccessEnabled_{true};
+  bool lastEvaluationResultWasEmpty_{false};
   /// Custom data stored by the user of the runtime.
   llvh::DenseMap<
       jsi::UUID,
@@ -1500,6 +1502,10 @@ void HermesRuntimeImpl::setJSErrorMetadataPropertyAccessEnabled(bool enabled) {
   jSErrorMetadataPropertyAccessEnabled_ = enabled;
 }
 
+bool HermesRuntimeImpl::lastEvaluationResultWasEmpty() const {
+  return lastEvaluationResultWasEmpty_;
+}
+
 jsi::ICast *HermesRuntimeImpl::castInterface(const jsi::UUID &interfaceUUID) {
   if (interfaceUUID == IHermesTestHelpers::uuid) {
     return static_cast<IHermesTestHelpers *>(this);
@@ -1855,6 +1861,7 @@ jsi::Value HermesRuntimeImpl::evaluateJavaScriptWithSourceMap(
       sourceURL,
       vm::Runtime::makeNullHandle<vm::Environment>());
   checkStatus(res.getStatus());
+  lastEvaluationResultWasEmpty_ = res->isEmpty();
   return valueFromHermesValue(*res);
 }
 
@@ -2049,6 +2056,7 @@ jsi::Value HermesRuntimeImpl::evaluatePreparedJavaScript(
         hermesPrep->sourceURL,
         vm::Runtime::makeNullHandle<vm::Environment>());
     checkStatus(res.getStatus());
+    lastEvaluationResultWasEmpty_ = res->isEmpty();
     return valueFromHermesValue(*res);
   } else {
     // Source path: delegate to evaluateJavaScript
diff --git a/API/hermes/hermes.h b/API/hermes/hermes.h
index 8f54456cb7ca1482774ec991dcc42f9cfa0a8918..0bfdd3ec5519a17ba7a65fa1530a975609cbce72 100644
--- a/API/hermes/hermes.h
+++ b/API/hermes/hermes.h
@@ -195,6 +195,7 @@ class IHermesTracingHelpers : public jsi::ICast {
 #endif
 
 #define HERMES_HAS_RAW_THROW_CAPTURE 1
+#define HERMES_HAS_COMPLETION_RECORD 1
 
 class HermesRuntime : public jsi::Runtime,
                       public IHermes,
@@ -212,5 +213,12 @@ class HermesRuntime : public jsi::Runtime,
   /// owners must restore the default after the scoped evaluation completes.
   virtual void setJSErrorMetadataPropertyAccessEnabled(bool enabled) = 0;
 
+  /// Report whether the most recent successful evaluateJavaScript call
+  /// produced the VM's Empty completion sentinel. JSI historically collapses
+  /// Empty into undefined while converting the result; structured embedders
+  /// need the original discriminator to preserve Script completion semantics.
+  /// @ref LLP 0024#6-evaluation-outcomes-and-the-abi — Empty is not undefined.
+  virtual bool lastEvaluationResultWasEmpty() const = 0;
+
   using jsi::Runtime::castInterface;
 };
