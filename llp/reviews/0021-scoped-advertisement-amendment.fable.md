# Reviews — LLP 0021 scoped-advertisement amendment (Claude/Fable family)

The LLP 0049 Phase 1 review package: the `## Amendment: scoped advertisement
(2026-08-06)` section of LLP 0021 (A1–A10, join matrix M1–M26) plus the
LLP 0032/0036 amendment deltas. Loop bound: 3 rounds (LLP 0049 §9).

## Round 1 — 2026-08-06

- **Family:** Claude (Fable 5)
- **Provider/runtime:** fresh general-purpose subagent via Claude Code Agent
  tool (independent context; the authoring session wrote neither the
  amendment nor this review — the amendment itself was authored by a
  separate Phase 1 agent)
- **Date:** 2026-08-06
- **Redacted:** no (public-repo content only)
- **Method:** package at `90aafc67`, code claims re-verified against
  `git show 6416114d:<path>` to exclude a concurrent agent's working-tree
  edits; all 26 matrix rows touched (~90 citations verified at the pin);
  independent completeness sweeps over the armed-snapshot schema string,
  both advertisement schemas, both report schemas, target-cells, the
  lineage verifier, and `new_armed_with_target_cells`; targeted questions
  Q1–Q5 per the round-1 brief
- **Verdict:** NOT READY (0 BLOCKER, 6 MATERIAL, 2 MINOR; five-item
  flip set, "with them landed, I would expect round 2 to be READY")

### Review body (verbatim)

Reviewer: independent/adversarial, per the brief. Package verified at `main` = `90aafc67` (confirmed HEAD). `90aafc67` is docs-only over the citation pin `6416114d` (`git diff --stat 6416114d 90aafc67`: 3 llp files, 868 insertions), so every code claim verified below holds identically at both revisions. Note: a concurrent agent's uncommitted edits appeared in the working tree mid-review (`packages/ibex-devtools/src/scripts/capsec-conformance-recipes.mjs` shifted `assertRecipeCatalogComplete` from :5048 to :5136); all code reads below were taken from or re-verified against `git show 6416114d:<path>`. The amendment's own "re-pin lines before implementing" instruction is already live in practice.

---

#### 1. Execution-pass results

**Mandated check 1 — M18 / open question 2 (three-way ownership conflict): PASS, and the conflict is real — but the amendment's pin enumeration is incomplete.**
Verified all three named pins: (a) `buildTargetAdvertisements` (`packages/ibex-devtools/src/scripts/generate-capsec-registry.mjs:1056-1090`, schema `ibex/capsec-target-advertisements/1` at :1058) regenerates the tracked path — :155 lists `capsec/generated/target-advertisements.json` as a generator output and :766 allow-lists it — populated from `capsec/conformance/target-attestations.json` promotions (`loadTargetPromotions`, :799+); (b) the lineage verifier requires the *artifact-source revision's* copy to be exactly empty v1 (`scripts/portable-engine-promotion-lineage.mjs:823-826`, "must remain on the closed v1 schema" / "must be empty" — the ~825 citation is exact) and exactly one advertisement blob at the exact checked path (:738, :789-790); (c) the build selector requires tracked v2 bytes at the same path (`build_support/portable_engine_promotion_report.rs:23` `ADVERTISEMENT_PATH`, :29 `ADVERTISEMENT_SCHEMA = "ibex/capsec-target-advertisements/2"`; `matching_advertisement` refuses "not exact v2 publication bytes"). The runtime refuses v1 outright (`src/host/portable_target_admission.rs:540-544`).
However, I found **four additional pins on the same path that M18 and A10 #2 do not name**:
- `src/host/portable_target_admission.rs:1866-1880` — unit test `tracked_source_a_legacy_advertisement_stays_closed` `include_str!`s the tracked file and **asserts it refuses as legacy v1**; it fails the moment v2 bytes land there.
- `scripts/hermes-artifacts-workflow-security.test.mjs:375-384` and `scripts/package-portable-hermes-macos.test.mjs:778-782` — both `assert.deepEqual(advertisements.advertisements, [])` against the tracked HEAD copy (not the artifact-source revision), so they conflict with "v2 publication owns the path" directly.
- `src/capsec_registry_generated.rs:12` — `CAPSEC_TARGET_ADVERTISEMENTS_JSON = include_str!(...)` is the delivery mechanism of the path's bytes into `authenticated_target_cells` (`src/host/mod.rs:6738-6742`); the registry generator owns both this generated .rs file and the .json bytes, so "the registry generator's v1 emission is retired" must also disposition the generated embedding it emits.
The conflict as stated is real; "v2 publication owns the path" is directionally right (the runtime already refuses v1, the build selector already demands v2, and the lineage verifier's empty-v1 pin is on the *source* revision so it stays consistent), but the ownership decision cannot be made soundly on a three-pin list when six-plus pins exist.

**Mandated check 2 — M11 / Option B blast radius: PASS on the cited pins, FAIL on completeness.**
All six cited pins verified real: `crates/sfe-format/src/lib.rs:787`, `crates/sfe-format/src/app_bound.rs:387`, `crates/sfe-catalog/src/lib.rs:708`, `src/host/abi.rs:1379+` (`install_armed_host` → `ArmedSnapshot::load`, the ingestion route), `src/host/embedder_artifacts.rs:695-712` (template loader → `ArmedSnapshot::load`), `capsec-contract.mjs:496`, plus `capsec/contract-files.json:44` (`examples/armed-snapshot.canonical.json`) and `capsec-environment-output-templates.mjs:305-313` (armed-snapshot schema-file invariants) — all as described. But my own grep for `"ibex/capsec-armed/1"` finds pins the enumeration missed: `capsec/schema/armed-snapshot.schema.json:37` (const), `capsec/registry/policy-rules.json:248` (`inputSchema`), `packages/ibex-devtools/src/scripts/generate-capsec-runtime-projection.mjs:134` + `schemas/capsec-runtime-projection-v1.schema.json:27` (the runtime-projection chain — absent entirely), `schemas/app-bound-common-v1.schema.json:232`, `schemas/stub-contract-v1.schema.json:122`, `schemas/stub-contract-v3.schema.json:124` (the SFE JSON Schemas beside the cited SFE code), `crates/sfe-format/src/lib.rs:1596,:1745` (emission constructors), `crates/capsec-semantics/src/canonical.rs:267` (contract-fixture projection table), `capsec/examples/digest-bundle.canonical.json:269005,269812` (arguably covered as "the digest vectors"). Every missed pin *increases* Option A's cost and is untouched under Option B, so the recommendation's direction survives — but "§A9 M11 enumerates these" and "the measured blast radius" are false as stated.

**Mandated check 3 — M14 / open question 1: PASS.**
`crates/capsec-semantics/src/decision.rs:610-621`: `Incomplete` refuses at a reached gate before any lifecycle result with `DecisionReason::TargetCellIncomplete`, and — load-bearing for the design — the hard decision already carries `Some(gate.coverage_edge_id)` (:618). The host constructs each `EffectGate` with `target_cell: self.target_cell(coverage_edge_id)` (`src/host/mod.rs:2844-2846`), which is exactly where the uncertified→`Incomplete` projection would happen, and the host holds the admitted scope identity under §A3. So host-layer annotation keyed by `coverage_edge_id` is mechanically achievable without touching decision.rs — strengthened by the observation that under `ScopedAdvertised`, admission (M7) refuses any in-scope defect, so a runtime `TargetCellIncomplete` can only be an out-of-scope cell or an absent-edge default (`target_cell`, `src/host/mod.rs:956-961`); the host disambiguates with a map lookup. Caveat the amendment correctly names as A10 #1: any consumer reading the raw typed decision stream (not host telemetry) sees identical refusals; whether LLP 0044's "distinguishable in refusal telemetry" is satisfied by host-emitted telemetry alone is genuinely the author's call.

**Mandated check 4 — admission and bundle behaviors: PASS.**
`src/host/portable_target_admission.rs`: exact ordered coverage inventory (:1444-1454, length + sorted + per-index `cell.edge_id == expected_edge` against `CAPSEC_COVERAGE_EDGE_IDS`); per-cell equality with checked source-derived authority (:1481-1487); Complete/Closed-only derivation with `conditional-unrefined` refusal (:1489-1505); full fixture union `required == passed == executions` plus all seven summary equalities incl. `incomplete_cells == 0` (:1508-1527). The report is also byte-bound to the advertisement (`conformanceDigest` recomputation + `reportRawContentDigest` + 15-field bindings equality, :1341-1387) — the substrate Q2 relies on. `capsec-portable-promotion-bundle.mjs`: `exactTargetCells` (:149-179) rejects any exact-target row with `disposition === "unsupported"` or malformed shape (:161-171, "promotion target cells remain unsupported or malformed") — the LLP 0044 must-amend #2 behavior, exactly as the matrix describes.

**Mandated check 5 — `scopeDigest` zero code hits: PASS.**
Case-insensitive grep for `scopedigest|scope_digest` over `src/`, `crates/`, `packages/ibex-devtools/src`, `scripts/`, `build_support/`, `tools/`, `capsec/`, `schemas/`: 0 hits. The blank-page claim holds.

**Row spot-checks (all 26 rows touched; every citation verified at the pin):** M2 (`capsec-conformance-recipes.mjs:5048`, condition :5050-5055, internally-verified proof-plan equality :5068-5078 — exact); M3 (`capsec-public-surface-evidence.mjs:9701`; `run-capsec-conformance.mjs:1881-1889` flat-maps `expectedFixtureIds` from the full catalog — exact); M4 (`capsec-conformance.mjs:393/571`; `assertReportMayAdvertise` :612-643 with the four all-or-nothing conditions — exact); M5/M23 (`capsec-portable-engine-evidence-contract.mjs:400/437/1037/1116` — exact); M6 (`:262/:866-870/:1037/:1113-1148` — exact); M7 (:529/:540-544/:568-580/:639/:1151/:1310-1318 — exact); M8 (`runtime.rs:5242` `build_default_armed_host`, :5290-5293 template `include_bytes!`, :4804-4816 `finalize_production_snapshot`, :5827+ `ExpectedArmingIdentity` build, :5882-5885 `ArmedSnapshot::load`, :3869+ session-worker path — all exact; producer provably never reads the promoted report); M9/M10 (`arming.rs:37/:345/:351`, `ExpectedArmingIdentity` :70-109 with no scope field, `digest.rs:159-163` frozen projection omitting only `armedSnapshotDigest` — exact); M12/M13/M15 (`mod.rs:723-739` constructs `CompleteAdvertised` via `authenticated_target_cells`; exhaustiveness gate :824-845 with refusal "armed target cells are incomplete" at :844-846; constructors :655/:695/:745/:885/:931 all synthesize `CompleteAdvertised` — exact); M17 (`evidence-contract.mjs:51` v2 schema const, `matchingCatalogEntry` :1027, `validatePublicationJoins` :1037, `validatePortablePromotionV2` :1116 — exact); M19 (:738 blob roles, :819-826 artifact-source state, :915-926 published-equals-bundle-graph — exact); M20 (`build.rs:864-877` embeds the selected report; `select_embedded_report` :453, `select_report_artifact` :286, `matching_advertisement` :314-360 keyed on sourceRevision/triple/artifactId, `verify_report_advertisement_join` :362 — exact); M21 (:126 generator return, `run-capsec-conformance.mjs:1581-1597` ceremony binding, bundle :540-568 — exact); M22 (`fixtureCatalogForTarget` `capsec-conformance.mjs:304`, `checked_report_authority` :1151 — exact); M24 (:269/:372 — exact); M25 (:1868-1899 four `checkPromotion` names in the stated order; `trackedAdvertisementCandidate` :1572-1578 — exact); M26 (`verifier.go:30-56` constants, Sigstore/SLSA only, no CapSec schema consumption — exact). M1/M16: correctly "no file exists" (check 5).

#### 2. Overall assessment

This is a strong, unusually honest design package: the join matrix's rows are real (every one of the ~90 file:line citations I checked was accurate at the pin), the Option B recommendation is correct and correctly argued from the actual producer/parser code, the A7 claim wording is rule-8-clean, and the amendment itself surfaced the M18 ownership conflict rather than papering over it. Where it falls short is on its own stated standard — the matrix is "the authoritative gate-code worklist" and M11 "the measured blast radius" — and my independent sweeps found real consumers and pins missing from three enumerations (the M18 path pins, the M11 armed-schema pins, and the report-schema restamp chain that M4 changes touch), plus two precision gaps in M7 that sit directly on the certification-soundness invariant. None of these makes the design unsound as written, and none undermines the Option B direction (the missed M11 pins strengthen it); they make the worklist incomplete and one A10 decision under-informed. All are cheaply fixable in a revision.

#### 3. Strengths

- **A6 is the best section: designed against the real code, and correct.** The claim that the launcher never reads the promoted report is verifiably true (`build_default_armed_host` composes the snapshot from launcher-observed facts only; target cells arrive via `Host::new_armed` → `authenticated_target_cells`, `src/host/mod.rs:729/:6726-6762`), and the Option A "derivative copy" argument follows directly. The three substitution channels in A6(ii) map onto real refusal sites I verified (:1341-1387 byte-binding, :568-580 uniqueness, lineage :915-926).
- **A7 claim wording** satisfies LLP 0049 §3 rule 8 exactly: layer-named fail-closed, explicit zero-decision carve-out, negative-controls-as-evidence, non-compositionality, and the deliberate absence of the physical-refusal layer with its reason (item 2's posture).
- **M22 is the sharpest row in the matrix**: pinning that no scoped variant of the catalog derivation is ever authored (scoping is a post-derivation partition) closes the partial-cell-credit reopening vector at its root — a row that exists purely to prevent a future mistake.
- **A8 fixture coverage is complete against its obligations**: F1–F7 are LLP 0044 §2's seven, F8–F10 are LLP 0049 §5.3's additions, and F11/F12 (constructor incapability, v1 non-carriage) are genuine code-survey products — F11 in particular closes a channel (dev/insecure/test constructors minting scope identity) that neither parent document had named.
- **M18's self-flagging** of the ownership conflict, with current-fact citations that all check out, is exactly what this review process is for — the row group did its job even though its pin list is short (Concern 1).
- **A2's "out-of-scope rows keep their honest current status" rule** protects the remainder's accounting from scoping perturbation — an easy thing to have missed.

#### 4. Concerns

**BLOCKER — none.** Nothing here admits an unsound certification or broken arming as written; every gap found fails closed (a refusing schema, a failing test, a refused admission).

**MATERIAL 1 — M18/A10 #2: the ownership decision's pin list is incomplete.** Missing: `src/host/portable_target_admission.rs:1866-1880` (unit test asserting the tracked copy refuses as legacy v1), `scripts/hermes-artifacts-workflow-security.test.mjs:375-384` and `scripts/package-portable-hermes-macos.test.mjs:778-782` (both assert the tracked copy is empty — against HEAD, not the artifact-source revision, so they break under the recommended resolution), and `src/capsec_registry_generated.rs:12` (the `include_str!` delivery the registry generator itself emits). *Resolves:* add all four to M18's current-facts list and to the A10 #2 decision inputs, each with its disposition under the recommended owner.

**MATERIAL 2 — M11's enumeration is incomplete while claiming to be the measured blast radius.** Missing pins listed in execution-pass check 2 (armed-snapshot JSON Schema const, policy-rules.json, the entire runtime-projection chain, three SFE JSON Schemas, two sfe-format emission sites, canonical.rs:267). Direction unaffected — every miss prices Option A higher. *Resolves:* complete the enumeration from a recorded grep for the schema string (state the sweep command in the row), or demote the row's claim from "enumerates" to "representative, sweep-verified with N total pins."

**MATERIAL 3 — the report-schema restamp chain has no row.** M4 + the LLP 0032 delta add a `scopeDigest` binding to both report schemas, but `bindings` is `additionalProperties: false` in both `capsec/schema/conformance-report.schema.json` and `schemas/capsec-conformance-report-v2.schema.json`, and the v1 rich-report schema is pinned across the digest contract: `capsec-contract.mjs:503`, `capsec/registry/policy-rules.json:255`, `crates/capsec-semantics/src/canonical.rs:278`, `digest.rs:166`, plus contract example fixtures. M17 names its JSON Schema file; M4 names neither schema file nor the contract chain. Unpriced restamp work on the authoritative worklist. *Resolves:* extend M4 (or add a row) enumerating the report-schema evolution surface, and decide whether the v1 rich report revs its schema id or evolves in place.

**MATERIAL 4 — M7 does not state the scoped form of the summary/fixture-union invariant.** The current soundness core is the exact equation at :1508-1527 (`required == passed == executions`, seven summary equalities). M7's amendment text says "validates the scoped required set" but never states the scoped equation — in particular that **out-of-scope cells must contribute zero required/passed fixtures and zero executions to the authoritative evidence set** (F4's "promotion evidence set unchanged" implies it; nothing states it as an admission invariant). Without that sentence, an implementation could accept a report whose out-of-scope rows carry passed executions, blurring the diagnostic/authoritative boundary M3 depends on. *Resolves:* one paragraph in M7 stating the exact scoped equalities and the zero-contribution rule for out-of-scope cells.

**MATERIAL 5 — the runtime carrier of the lineage anchor is unnamed.** §A5 and M7 say admission "resolves the tuple's currently admitted scope from the checked-in promotion lineage," but M7 is `portable_target_admission.rs` — code that runs inside the armed binary with no repository access. The natural carrier is the build-embedded checked promotion admission (`require_checked_promotion`, :639, over `EMBEDDED_PORTABLE_ENGINE_PROMOTION_ADMISSION`), with the script-time lineage verifier (M19) as the authority that stamped it — but the amendment never says which admission (script-time vs runtime) performs the predecessor comparison, or what byte-carrier delivers the lineage-resolved current scope into the binary. A10 #3 (bundle vs repo placement) is adjacent but does not decide this. *Resolves:* name the runtime-side anchor explicitly in M7/M20 (and note that rollback resistance against a whole-binary rebuild from old sources is out of scope — build provenance's job, M26's layer).

**MATERIAL 6 — the uncertified disposition's type placement is undecided and absent from A10.** Host's cell map today is `BTreeMap<String, TargetCellDisposition>` (`src/host/mod.rs:784`) — the *decision.rs* serialized enum (:395-400). A3 says the distinction "lives at the host cell-map layer," which requires either a new host-side disposition type projecting into `TargetCellDisposition` at gate construction (:2844-2846), or an `Uncertified` variant on `TargetCellDisposition` itself — which would change the typed vocabulary and falsify M14's scope-transparency at the type level even with A10 #1 resolved in favor of host annotation. Neither A3 nor A10 names this choice. *Resolves:* add it to A10 (or settle it in A3: host-side type + projection, keeping decision.rs untouched).

**MINOR 7 — unnamed consumers found in the Q1 sweep:** `scripts/portable-engine-installer-core.mjs:34,:470-476` consumes the lineage verifier's authorization result (almost certainly scope-transparent — it reads `authorized`/tuple only — but the matrix should say so, since M19's member set changes under it); `capsec/schema/target-advertisements.schema.json:13` (the v1 JSON Schema const, part of the M18 row group but unnamed).

**MINOR 8 — denominator drift across the package:** the LLP 0036 delta cites 3,922 rows/2026-08-05 while the LLP 0044 §7 record cites 3,927 rows/73 poisoned (2026-08-06) and LLP 0049 §5.2 says "the 74." Rule 5 re-derivation makes this survivable, but the three documents should agree on which measurement is current or state the supersession.

#### 5. Answers to Q1–Q5

**Q1 — matrix completeness.** Sweeps run (all at the pin, excluding `node_modules` and `llp/`): `ibex/capsec-armed/1` (21 hits), `capsec-target-advertisements/[12]` (10 hits + 3 test files), `ibex/capsec-conformance/[12]` (11 hits), `target-cells.json`/`capsec-target-cells/1` (17 hits), `portable-engine-promotion-lineage` importers, `new_armed_with_target_cells` callers (only `src/host/mod.rs`), `scopeDigest` (0 hits). Consumers touching the report↔cells↔advertisement join without a matrix row: **(a)** the three test pins + `include_str!` delivery on `capsec/generated/target-advertisements.json` (Material 1); **(b)** the v1 report-schema digest-contract chain (Material 3); **(c)** the installer's lineage consumption (Minor 7). Everything else in the sweeps maps to an existing row (M11's sweep gaps are pins within an existing row, not missing rows).

**Q2 — Option B substitution-resistance.** Attacks attempted: *(1) stale advertisement + fresh report* — refused today at :1341-1387 (raw-content + digest + 15-binding equality); the scoped re-derivation (M7) adds the scope to the same comparison; F1 pins it. *(2) two tuples' artifacts crossed* — refused by `bindings.target == advertisement.target` (:1362) + tuple-unique selection (:568-580) + `authenticate_local_engine` against the loaded engine; F1's converse arguably covers it, but no fixture explicitly crosses tuples — worth one line in F1. *(3) re-admission after lineage rollback* — closed at promotion time by §A5/M19/F6; at runtime it reduces to Material 5 (the embedded anchor) and, for whole-binary rollback, to build provenance — the amendment should say that boundary out loud. *(4) snapshot from a differently-scoped run* — under Option B the snapshot carries no scope and selects nothing but the tuple (verified: `ExpectedArmingIdentity` :70-109 has no scope field; the tuple+engine join is the only bridge), so there is no degree of freedom; F1's "no snapshot participation" note plus F11 cover it. **Judgment on the trade:** B's "digest never travels alone" argument is sound, and it does *not* relocate trust to admission — admission is already the sole cell-map authority today (`authenticated_target_cells` is the only path into `new_armed`), and B keeps the scope on exactly that channel, re-derived rather than copied. A would add a copy authenticated by the same embedded chain (no independent root), plus the M11 cascade — which my sweep shows is *larger* than priced. B is correctly recommended.

**Q3 — claim wording vs rule 8.** Compliant. Swept the full amendment for "fail-closed"/"refused"/"safe": the only fail-closed assertions are A7's, and both name their layer (startup admission, typed-gate refusal); the physical-entrypoint layer's absence is stated with its reason; the zero-decision remainder is explicitly excluded from both layers; negative controls and adversarial compositions are marked diagnostic/never-upgrading in A7, F4, and F10. "It remains callable" is an availability statement, not a safety claim. A3's uncertified-disposition wording asserts only reached-gate refusal semantics with the mechanism named (`decision.rs:609-621` projection) — no universal negative anywhere.

**Q4 — monotone lineage.** No bypass found within the chain's own threat model. *Retirement abuse:* a "retired" cell still in the live inventory fails (A1/F7); genuine inventory retirement does narrow, but that lane bottoms out in reviewed inventory code changes (the LLP 0044 §2/LLP 0040 accepted posture) — the amendment should state that the guard on that one lane is code review, not the chain. *Rename/split/merge laundering:* mappings must be generated from inventory history, total-and-exactly-once on both sides (F9), and — decisive — admission re-derives the expansion from the intensional definition against the live inventory, so mapping games cannot drop a cell the definition still selects. *Genesis re-declaration:* refused while the lineage records any scope for the tuple (F6's second half); erasing lineage means rewriting checked-in history under the lineage verifier. *Digest domains:* `ibex:capsec:scope:1` / `scope-expansion-diff:1` / `scope-cell-mapping:1` are genuinely separate, companions bound into the scope by digest — separation holds. One residual lane, for the author: a *tuple redefinition* (any feature-list change) creates a fresh tuple with a legitimate genesis scope — lineage is per-tuple, so a narrower scope can ship under a new tuple without tripping anything. The engine really is different (features are part of the authenticated identity, `mod.rs:6733-6737`), so the claim stays honest, but it is the one narrowing-shaped move the chain permits; see open question 3 below.

**Q5 — the A10 open questions.** #1 (telemetry placement): genuine author decision — both designs are sound, the trade (wire-schema restamp vs host-only distinguishability) is fairly stated, and check 3 confirms the recommended option is mechanically real. #3 (bundle vs repo placement): genuine, though it must be decided jointly with Material 5, which it currently doesn't subsume. #4 (`Copy` on `TargetArmState`): genuine implementation choice, correctly constrained ("state and identity inseparable at construction"). #2 (v1-path ownership): the decision is genuinely the author's, but the amendment should have *settled the pin inventory* before posing it — as posed, it is under-informed (Material 1). **Missing from both A6 and A10:** the uncertified-disposition type placement (Material 6) and the runtime lineage-anchor carrier (Material 5). Also arguably missing: a named schema/shape for the M16 introspection surface (it is a new certified-facing surface with no schema id reserved — minor).

#### 6. Open questions for the author beyond A10

1. Should the scoped admission invariant include an explicit **zero-authoritative-contribution rule** for out-of-scope cells (Material 4), and should F4 assert it directly (out-of-scope executions present in the diagnostic schema ⇒ byte-identical authoritative evidence set)?
2. Which artifact carries the **lineage-resolved current scope into the armed binary** — the checked promotion admission marker, the bundle-carried scope artifact via M20, or both with an equality check (Material 5)?
3. Is a **feature-list-induced fresh tuple** (new tuple ⇒ genesis scope, no predecessor) acceptable as the one chain-permitted narrowing-shaped move, or should genesis admission also require that no *prefix tuple* (same triple, different features) has a live scope? Relates to deferred register item 7.
4. When a scope eventually expands to the full inventory, does the tuple stay `ScopedAdvertised` with a total scope, or re-enter `CompleteAdvertised`? M12 says the two are mutually non-constructible per admission result; stating the end-state intent would prevent a future "graduation" shortcut.
5. Do the two workflow-security tests' empty-advertisement assertions become lineage-verifier-style *source-revision* checks under the A10 #2 resolution, or are they retired? (They currently encode "no target is ever advertised" as a permanent invariant of HEAD.)

#### 7. Verdict

**NOT READY** — by a narrow margin, and entirely on completeness-of-enumeration and two precision gaps, not on design direction. The A6/Option B recommendation, the `ScopedAdvertised` design, the A7 wording, and the A5 lineage design are sound and I found no attack the design fails to close within its stated threat model. But this matrix is, by its own §A9 declaration, the authoritative gate-code worklist, and the A10 #2 decision packet is missing four of its pins.

**Minimal flip set:**
1. Add the four missing `capsec/generated/target-advertisements.json` pins to M18 and A10 #2, each with a disposition under the recommended owner (Material 1).
2. Complete or requalify the M11 pin enumeration, recording the sweep command in the row (Material 2).
3. Add the report-schema evolution surface (both JSON Schema files + the v1 digest-contract chain) to M4 or as a new row (Material 3).
4. State M7's scoped summary/fixture-union equalities, including the zero-authoritative-contribution rule for out-of-scope cells (Material 4).
5. Name the runtime lineage-anchor carrier in M7/M20, and add the uncertified-disposition type placement to A10 (Materials 5–6).

All five are text-only amendments; none reopens a decided register item. With them landed, I would expect round 2 to be READY.

## Round 2 (delta) — 2026-08-06

- **Family:** Claude (Fable 5)
- **Provider/runtime:** fresh general-purpose subagent via Claude Code Agent
  tool (independent context; distinct from the round-1 reviewer, the
  amendment author, and the revision author)
- **Date:** 2026-08-06
- **Redacted:** no (public-repo content only)
- **Method:** delta round at commit `38c382d2`, document revision
  `9565a3cb8f30`, clean working tree; executed all mandated delta checks
  (M27's four pins + the discovery walk against `pinRunningAuthority`,
  A3's projection/chokepoint/constructibility against Rust module
  boundaries, the M11 sweep re-run verbatim, M18's eight pins + an
  independent sweep, M28's parsers, M7's scoped equalities); full
  disposition table over both families' round-1 findings with
  IN-DELTA / PRE-EXISTING labels
- **Verdict:** NOT READY (0 BLOCKER, 4 MATERIAL, 5 MINOR; four-item
  text-only flip set; both round-1 BLOCKERs dispositioned PARTIAL, and
  item 4 — genesis via truncated history — flagged as the one finding the
  reviewer would not want carried into a disagreement ledger unaddressed)

### Review body (verbatim)

# Round 2 review — LLP 0021 scoped-advertisement amendment (delta round)

**Package identity verified.** `git rev-parse HEAD` = `38c382d2eab6227c8abfc318635a61422f3a5f21`; `shasum -a 256 llp/0021-capsec-effect-model-migration.plan.md` = `9565a3cb8f30530f…`, matching the brief's `9565a3cb8f30` and identical to `git show 38c382d2:llp/…`. Working tree is clean (`git status --porcelain` empty), so no concurrent-agent edits contaminate any read below. The amendment's own citation pin `6416114d` differs from HEAD by 21 files; the amendment discloses this and instructs re-pinning, so I verified code claims at HEAD and flag drift only where it matters.

---

## 1. Delta-verification results

| # | Check | Result | Observed |
|---|---|---|---|
| 1a | M27 schema pin — `schemas/portable-engine-checked-promotion-admission-v1.schema.json:7-18` | **PASS** | Exactly the 10 named fields under `"additionalProperties": false` (:6), `"required"` block spans :7-18 verbatim. |
| 1b | M27 producer pin — `scripts/portable-engine-promotion-lineage.mjs:1185-1199` | **PASS** | `const checked = {…}` at :1185 freezes exactly the 9 non-digest fields; `Object.freeze({...checked, verificationDigest: semanticDigest(CHECKED_ADMISSION_DOMAIN, checked)})` at :1196-1199. |
| 1c | M27 build carrier — `build_support/portable_engine_build_consumption.rs:130-139` | **PASS (loose range)** | `fs::write(out_dir.join("portable_engine_promotion_admission.json"), &self.promotion_admission_bytes)` is at :135-139; the cited :130-139 also spans the adjacent build-consumption write. Carrier is real; range is one write too wide. |
| 1d | M27 Rust parser — `portable_target_admission.rs:613-628`, `require_checked_promotion` :639 | **PASS (exact)** | `exact_object_keys(&value, &[…10 keys…], "checked promotion admission")` spans :613-628 line-for-line; `pub(super) fn require_checked_promotion(` is exactly :639, and it verifies the domain digest (`compute_domain_digest(PROMOTION_ADMISSION_DOMAIN, …, &["verificationDigest"])`) before trusting fields — as M7 claims. |
| 1e | **M27 discovery walk vs. how the verifier authenticates revisions today** | **FAIL** | Step 1's "per-hop validation is unchanged in kind … re-run at R_prev" is not executable. `verifyPortableEnginePromotionAdmission` (:1034) is hard-wired to HEAD: `resolveHead` (:1041), `assertCleanWorktree` (:1043), and decisively `pinRunningAuthority` (:1047, fn :1010-1025) — which asserts the **current tree's** copies of the verifier module, contract, bundle verifier, evidence contract, schemas and catalog equal the *running* files (:1012 `fs.realpathSync(moduleFilePath) === fs.realpathSync(expectedModule)`). Re-run at R_prev this fails after any maintenance commit touching those paths. The walk necessarily uses a **reduced** per-hop check (content-hashed commit/tree objects + canonical catalog + merge shape); M27 does not say so. See New Finding 3a. |
| 1f | **M27 genesis case soundness** | **FAIL** | Genesis is admissible when the walk "exhausts **retained** history" (§A5 final bullet, M27 step 3). The verifier has no history-completeness guard: `grep -n "shallow\|grafts\|rev-list\|--depth" scripts/portable-engine-promotion-lineage.mjs` yields only `--no-replace-objects` (:144). A checkout with a **current tree but truncated history** therefore mints a legitimate genesis scope with no predecessor comparison. Not covered by the documented whole-checkout-rollback boundary, which is about a binary *rebuilt from old sources*. See New Finding 4. |
| 1g | M27 topology claim ("single active admission") | **FAIL (precision)** | `parseCatalog` asserts `catalog.admissions.length === 1` (:686) — one admission **globally**, not per tuple. M27 step 1 says "the single-active-admission rule holds **per tuple**." See New Finding 5. |
| 2a | A3 — does `Uncertified → Incomplete` at `src/host/mod.rs:2844` leave `decision.rs` untouched? | **PASS on substance, FAIL on the pin** | The projection is achievable with `decision.rs` untouched — but not at :2844. `grep -c "target_cell: self.target_cell" src/host/mod.rs` = **11** gate-construction sites (:2346, :2846, :2929, :3002, :3107, :3188, :3346, :3495, :3588, :3684, :3768). The real single funnel is `fn target_cell` (:956-961), which is private and returns `TargetCellDisposition` by `.copied()`. A3 pins 1 of 11 sites and calls it "exactly at". |
| 2b | A3 — is the "**central** host refusal path … the **single** code path" real? | **FAIL** | Three independent decision-evaluation bodies, four `evaluate_decision_set*` call sites: `evaluate_typed_decision_inner_and_then` (:3794 → :3811/:3820), `evaluate_typed_decision_with_evidence` (:3844 → :3857), `evaluate_typed_path_decision_with_evidence` (:3874 → :3888). The last two do **not** delegate to `_inner_and_then`; each opens its own context and calls the evaluator directly. No single refusal path exists. See New Finding 1. |
| 2c | A3 — can the aggregate be made M7-only-constructible as module boundaries stand? | **PASS** | `mod portable_target_admission;` (`src/host/mod.rs:23`) is a **private child** module, so a private-field struct defined there is genuinely opaque to `src/host/mod.rs`; `pub(super) fn require_checked_promotion` confirms the `pub(super)` idiom is already in use. The dev/insecure/observer/test constructors (:664, :700, :756, :889, :909, :947) live in `mod.rs` and provably cannot mint it. **Caveat:** `#[cfg(test)]` at `portable_target_admission.rs:1560` is a *descendant* module with full private access — "no test constructor" is in-file discipline, not a boundary. |
| 2d | A3 — does anything else construct the cell map? | **PASS** | `fn new_armed_with_target_cells` (:781) is private; field `target_cells: Arc<BTreeMap<…>>` (:238) is private; assignment only at :876. Seven callers: :676 dev, :712 insecure, :731 armed, :766 observer, :889/:909 test, :947 native runner. `authenticated_target_cells` (:6726) is the sole admission route and is called only from `Host::new_armed` (:729). Chokepoint claim holds. |
| 3 | **M11 re-run sweep — 20 hits in 16 files?** | **PASS (exact)** | Ran the recorded command verbatim. Result: **20 hits, 16 files**, identical with or without the quote marks and with or without `.git/` exclusion. Every one of M11's 20 line numbers is correct to the line: `arming.rs:37,:3679`; `sfe-format/lib.rs:787,:1596,:1745`; `app_bound.rs:387`; `sfe-catalog:708`; `canonical.rs:267`; `digest.rs:161`; `armed-snapshot.schema.json:37`; `stub-contract-v1:122`; `stub-contract-v3:124`; `app-bound-common-v1:232`; `capsec-runtime-projection-v1:27`; `policy-rules.json:248`; `capsec-contract.mjs:496`; `generate-capsec-runtime-projection.mjs:134`; `armed-snapshot.canonical.json:2`; `digest-bundle.canonical.json:269005,:269812`. **Adjudication: the revision (20/16) is right; round-1 Fable's "21" was an overcount.** The three transitive routes (`abi.rs`, `embedder_artifacts.rs`, `capsec-environment-output-templates.mjs`) correctly do **not** appear in the sweep, and `capsec/contract-files.json:44` is correctly cited as a path listing, not a pin. |
| 4a | M18 — are all eight pins real? | **PASS** | 1: `generate-capsec-registry.mjs:1056-1058` emits `ibex/capsec-target-advertisements/1`, path allow-listed :766. 2: lineage `:822-826` pins the *source* copy to empty v1 (:819-820 pins attestations). 3: `portable_engine_promotion_report.rs:23` `ADVERTISEMENT_PATH`, :29 `= "…/2"`. 4: `portable_target_admission.rs:1866-1880`, `include_str!` + `assert!(error…contains("legacy v1"))`. 5: `hermes-artifacts-workflow-security.test.mjs:375-384`, `assert.deepEqual(advertisements.advertisements, [])` at :383. 6: `package-portable-hermes-macos.test.mjs:778-782`, same assertion at :781. 7: `src/capsec_registry_generated.rs:11-12` `include_str!`, consumed at `mod.rs:6738-6742` (exact). 8: `capsec/schema/target-advertisements.schema.json:13` const (exact). |
| 4b | M18 — is the inventory **complete**, and does each disposition work? | **FAIL** | ≥3 further pins on the same path: `capsec-contract.mjs:3514-3517` + `:3682-3683` (validates the **HEAD** copy against `SCHEMA_IDS.targetAdvertisements` = the **v1** schema, :130-131) with v1-shape field reads at :4205/:4218/:4231/:4426/:4495; `generate-capsec-registry.mjs:61-68` + `:154-158` (the generator's declared-output registry / generated-artifact manifest); `capsec-portable-engine-evidence-contract.test.mjs:1312-1324` (a fourth HEAD-copy v1+empty assertion). **Pin 8's stated disposition — "unchanged … describes the closed v1 schema for the artifact-source foundation copy" — is falsified**: the digest-contract runner applies it to the HEAD copy. See New Finding 2. |
| 4c | M18 — do the two workflow-security assertions and the admission unit test work under the settled disposition? | **PARTIAL** | Pins 5/6 are Node tests reading `repoRoot` at run time, so "source-revision check **or** retirement" is a genuine choice. Pin 4 uses `include_str!(concat!(env!("CARGO_MANIFEST_DIR"), …))` — a compile-time embed; a lineage-verifier-style source-revision check needs Git object access a Rust unit test in that file cannot have. For pin 4 only retirement/replacement is actually open. See New Finding 6. |
| 5a | M28 — do the two Rust parsers reject unknown binding fields? | **PASS (exact)** | `capsec_portable_public_batch.rs:183-204` `assert_exact_keys` over 15 keys; `capsec_exact_fixture_evidence_batch.rs:489-508` over 14 (no `conformanceRunner`). `portableExecutionBindingDigest` (`capsec-portable-engine-evidence-contract.mjs:216-235`) hand-enumerates the same 15 — an added `scopeDigest` is silently ignored. M28's "opposite failure modes, both wrong" is accurate. |
| 5b | M28/A10 #5 — is the carriage rule a real choice or a forced one? | **PASS — real choice** | Transitivity is mechanically available: `portableRecipeCatalogDigest` (:238-241) digests the **whole** catalog object minus its self-digest, so a `scopeDigest` field added to the catalog per M2 propagates into `recipeCatalogDigest`, which is present in all three closed lists. A second independent route exists via `publicSurfaceExecutionDigest` (M3). Both options are implementable; A10 #5 is genuine. *Note:* the two parser lists already differ by one key, so "uniform across all execution-plan bindings" is aspirational, not descriptive. |
| 6 | M7 — is the scoped form equivalent-or-stronger than today's `required == passed == executions`? | **PASS** | Today's invariant (`portable_target_admission.rs:1508-1527`): non-empty `required`, `required == passed == executions`, plus 7 summary equalities. M7's scoped form is **exactly today's equation restricted to S**, and is *stronger* in one respect: it writes `required(S) == passed(S) == execution_fixtures` against the report's **whole** authoritative execution set (not `executions(S)`), so any out-of-scope execution riding in the authoritative artifact refuses — a naive restriction would have permitted it. `summary.cells == \|inventory\|` and `incomplete_cells == 0` preserved; only `conformant_cells` weakens from `\|inventory\|` to `\|S\|`, which is the intended and honestly-stated claim narrowing. One wording ambiguity in the zero-contribution sentence (New Finding 8). |

---

## 2. Disposition of every round-1 finding

### Codex family (`…codex.md`)
*(Note: the artifact header says "4 MATERIAL"; §4 actually carries **five** MATERIAL headings. All five dispositioned.)*

| # | Round-1 finding | Disposition | Resolving (or failing) section |
|---|---|---|---|
| C-B1 | **BLOCKER** — M19/M7: no authenticated predecessor carrier; no evolvable lineage algorithm | **PARTIAL** | M27 adds the missing row with all four pins verified exact (checks 1a-1d), revs the schema to `…/2` with `admittedScopeDigest`/`predecessorScopeDigest`, names the authority/anchor split, and states a 4-step algorithm. But the algorithm's step-1 premise is not executable (check 1e), its genesis termination has no history-completeness precondition (check 1f), it misstates today's cardinality rule (check 1g), and it collides with M18 pin 2 from the second promotion onward (New Finding 3b). The *carrier* half is RESOLVED; the *algorithm* half is not. |
| C-B2 | **BLOCKER** — A3/A6/M13/M14: scope identity, host disposition and enforced map are not one defined object | **PARTIAL** | `AdmittedScopedTargetCells` is fully specified in A3 (opaque, private constructor, no `Default`, atomic consumption at M12/M13, single retained source for M16 + per-gate projection), and check 2c confirms it is genuinely constructible-only-in-`portable_target_admission` as module boundaries stand; `HostCellDisposition` settles the type placement with `decision.rs` untouched (check 2a substance, 2d chokepoint). A10 #4 now says an independently mutable parallel record is unacceptable. **But** Codex's resolution text explicitly required "Specify the host telemetry schema **and central emission point**" (and Q5 #5 asked "which centralized Host path guarantees that every reached-gate refusal receives it?"). The schema is specified; the central emission point is **asserted to exist and does not** (check 2b), and the projection site is mis-pinned (check 2a). |
| C-M1 | **MATERIAL** — A9/M3/M11/M19/M25 consumer matrix incomplete | **RESOLVED** | Every named consumer now has a row or pin: `portableExecutionBindingDigest` + both strict parsers → M28 (verified 5a); generated Rust carrier → M18 pin 7 (verified); physical-promotion workflow → M29; checked-admission chain → M27; plus M30/M31. M11's literal-pin sweep is now exact (check 3). |
| C-M2 | **MATERIAL** — A5 rename/split/merge totality does not prevent semantic narrowing | **RESOLVED** | §A5 "Semantic preservation across the mapping (normative)" carries all six requested rules verbatim in substance: rename→successor in scope; split→**every** successor in scope; merge with any in-scope predecessor→successor in scope; mapped-predecessor and retirement sets disjoint; predecessor+current inventory digests bound and **independently regenerated** by the validator; genesis searches retained history. Tuple migration is additionally surfaced as A10 #7. |
| C-M3 | **MATERIAL** — A7 two sentences overstate the boundary | **RESOLVED** | "remains callable" is gone; A7 now reads "the certification does not constrain its availability and no universal physical-refusal claim is made" (:3888-3889). Startup admission is now described as "a scope/map-integrity property (metadata admission) … **never** an execution-refusal property of any surface" (:3895-3898), and zero-decision surfaces "have no typed-gate refusal and no physical-entrypoint refusal" (:3901-3903). The conflation is gone. |
| C-M4 | **MATERIAL** — A8 substitution fixtures not exhaustive | **RESOLVED** | F1a (stale advertisement × fresh report, pinned to :1341-1387), F1b (cross-tuple, incl. a foreign-tuple snapshot), F1c (direct scope-identity/map crossing at the Host constructor, unrepresentable by construction); F6 gains the documented lineage-root rollback boundary; F11 gains the positive coherence half (arm, introspect, sweep every generated edge, assert equality with the aggregate's partition). All four requested additions present. |
| C-M5 | **MATERIAL** — M18/A10 #2 ownership should be settled, not an equal alternative | **RESOLVED as to the decision; PARTIAL as to its inputs** | A10 #2 is now marked "SETTLED normatively … the v2/v3 publication owns `capsec/generated/target-advertisements.json` at promotion time," with only the diagnostic-v1 destination and the test-pin *form* left discretionary — exactly Codex's resolution. The decision packet supporting it remains incomplete (check 4b). |
| C-m1 | **MINOR** — M11 terminology (transitive routes ≠ literal pins) | **RESOLVED** | M11 now uses a three-way classification — "Direct schema pins", "Generated mirrors / emission sites / vectors", "Transitive callers (no literal pin; ingest via `ArmedSnapshot::load`)" — with `abi.rs`, `embedder_artifacts.rs` and `capsec-environment-output-templates.mjs` correctly moved to the third bucket. Verified: none appears in the literal sweep (check 3). |

### Fable family (`…fable.md`)

| # | Round-1 finding | Disposition | Resolving (or failing) section |
|---|---|---|---|
| F-M1 | **MATERIAL 1** — M18/A10 #2 pin list incomplete (4 named misses) | **PARTIAL** | All four named misses landed as pins 4, 5, 6, 7, each with a per-pin disposition, plus pin 8 from F-m1. But M18 now claims to be "**The complete pin inventory**" and is not: ≥3 further pins exist and one stated disposition is falsified (check 4b, New Finding 2). The same defect class recurs after a targeted flip-set item. |
| F-M2 | **MATERIAL 2** — M11 claims to be the measured blast radius but is incomplete | **RESOLVED** | The sweep command is now recorded in the row, and re-running it verbatim reproduces the stated result to the line: 20 hits / 16 files (check 3). Round-1's "21" was the overcount. |
| F-M3 | **MATERIAL 3** — report-schema restamp chain has no row | **RESOLVED** | M30 added. Verified: `capsec-contract.mjs:503`, `capsec/registry/policy-rules.json:255`, `crates/capsec-semantics/src/canonical.rs:278`, `crates/capsec-semantics/src/digest.rs:166` all carry `ibex/capsec-conformance/1`; both report schemas close objects with `additionalProperties: false`. The rev-vs-evolve decision is framed as A10 #6, and M4 now states it is "not implementable without M30's worklist". |
| F-M4 | **MATERIAL 4** — M7 does not state the scoped invariant or the zero-contribution rule | **RESOLVED** | M7 "The scoped forms of the :1508-1527 equalities (normative)" states all ten equalities plus the zero-authoritative-contribution rule, and F4 asserts it directly. Verified equivalent-on-S and marginally stronger on executions (check 6). One residual wording ambiguity → New Finding 8 (MINOR). |
| F-M5 | **MATERIAL 5** — runtime carrier of the lineage anchor unnamed | **RESOLVED (as to naming)** | M7 "The runtime lineage anchor" names `require_checked_promotion` (:639) over `EMBEDDED_PORTABLE_ENGINE_PROMOTION_ADMISSION` (`mod.rs:6743-6746`) — both verified — with M27 as the carrier, M19 as the script-time authority, and the authority/anchor distinction stated. M20 adds "Anchor naming". The rollback caveat Fable asked for is now explicit in M27 and F6. The *algorithm* behind the anchor is where C-B1 remains PARTIAL. |
| F-M6 | **MATERIAL 6** — uncertified-disposition type placement undecided | **RESOLVED** | Settled in A3 and recorded in A10's "Settled" preamble: host-side `HostCellDisposition` in `portable_target_admission.rs`, `Certified(TargetCellDisposition)` / `Uncertified`, projecting to `Incomplete`, `decision.rs` untouched. Check 2a confirms the substance is implementable (via `fn target_cell` :956-961); only the cited site is wrong. |
| F-m1 | **MINOR 7** — unnamed consumers (installer; v1 JSON Schema const) | **RESOLVED** | M31 added for `scripts/portable-engine-installer-core.mjs:34,:470-476` with a transparency argument and the F6 catch-fixture; `capsec/schema/target-advertisements.schema.json:13` added as M18 pin 8 (verified exact) — though its disposition is the one falsified in check 4b. |
| F-m2 | **MINOR 8** — denominator drift across the package | **RESOLVED** | The LLP 0036 delta now marks the 2026-08-05 figures "**superseded**", states the 2026-08-06 measurement in force, and states the re-derivation rule that makes all such figures snapshots. (Residual, outside the package: LLP 0049 still says "the 74" at :43/:428/:430/:477/:480 while its own evidence table at :714 says 73 poisoned. Not a package finding.) |

---

## 3. New findings

### **IN-DELTA — MATERIAL — A3/M14: the "central host refusal path" does not exist, and the projection site is mis-pinned**

A3 places the `ibex/capsec-scoped-refusal/1` envelope "at the central host refusal path — **the single code path** that receives a hard `TargetCellIncomplete` decision from a gate it constructed at `src/host/mod.rs:2844-2846`." Both halves are wrong about the code.

- `grep -c "target_cell: self.target_cell" src/host/mod.rs` → **11**. Site :2846 is one of eleven `EffectGate` constructions (:2346, :2846, :2929, :3002, :3107, :3188, :3346, :3495, :3588, :3684, :3768). The genuine single funnel is `fn target_cell` (:956-961).
- `grep -n "capsec_semantics::decision::evaluate_decision_set" src/host/mod.rs` → **four** call sites in **three** independent bodies: `evaluate_typed_decision_inner_and_then` (:3794, calls :3811/:3820), `evaluate_typed_decision_with_evidence` (:3844, call :3857), `evaluate_typed_path_decision_with_evidence` (:3874, call :3888). The latter two each open their own `decision_context` read guard and call the evaluator directly rather than delegating.

This matters because A10 #1 asks the author to choose between the host envelope and a new `DecisionReason` — and the recommended option is described as resting on a chokepoint that must first be built. It is also the precise sub-clause Codex BLOCKER 2's resolution named ("central emission point") and Q5 #5 asked for.

**Exact resolution (text-only):** in A3 and M14, repoint the projection to `src/host/mod.rs:956-961` (`fn target_cell`, noting all 11 gate sites funnel through it and `TargetCellDisposition: Copy` is preserved by a `Copy` `HostCellDisposition`); and replace "the single code path" with the honest statement that no such path exists today — the envelope requires either funnelling the three `evaluate_decision_set*` bodies through one refusal-observing helper or emitting at all three — and add that funnelling to M13/M14 as scope-validating work.

### **IN-DELTA — MATERIAL — M18: the "complete pin inventory" is still incomplete, and pin 8's disposition is falsified**

Command: `grep -rn "target-advertisements.json" scripts packages/ibex-devtools/src package.json .github | grep -v node_modules`.

1. **`packages/ibex-devtools/src/scripts/capsec-contract.mjs:3514-3517`** reads the **HEAD** copy (`readConfinedGeneratedJson`) and validates it at :3682-3683 against `SCHEMA_IDS.targetAdvertisements` = `https://ibex.dev/capsec/schema/target-advertisements.schema.json` (:130-131) — the **v1** schema of pin 8 — then consumes v1-shaped fields at :4205 (`targetCellsRawContentDigest`), :4218/:4231 (`advertisements[]`), and includes it in bound sets at :4426/:4495. Under the settled owner, v2/v3 bytes at the path fail the CapSec contract runner outright. **This directly falsifies pin 8's stated disposition** ("unchanged: it continues to describe the closed v1 schema for the artifact-source foundation copy") — the v1 schema is applied to the *current-revision* copy.
2. **`generate-capsec-registry.mjs:61-68`** (`generatedRegistryPaths.targetAdvertisements`) and **`:154-158`** (generated-artifact manifest entry `{path: "capsec/generated/target-advertisements.json", kind: "target-advertisements", digestBound: false}`) — the generator's declared-output registry, a distinct mechanism from the `buildTargetAdvertisements` emission pin 1 names. Pin 1's "retired from this path" must disposition these, or a regenerate/drift pass rewrites v1 bytes over published v2 bytes.
3. **`capsec-portable-engine-evidence-contract.test.mjs:1312-1324`** — a fourth HEAD-copy assertion (`toBe("ibex/capsec-target-advertisements/1")`, `advertisements).toEqual([])`), same class as pins 5/6, unlisted.

**Exact resolution (text-only):** add these three to M18 with dispositions (the contract-runner pin is the load-bearing one: either it moves to the artifact-source copy or `SCHEMA_IDS.targetAdvertisements` must admit v2/v3), correct pin 8's disposition accordingly, and replace "The complete pin inventory" with a recorded sweep command and its result — the same requalification M11 already received and passed.

### **IN-DELTA — MATERIAL — M27: the evolvable lineage algorithm is not executable as stated, and collides with M18 pin 2**

**(a) Step 1 is false as written.** See check 1e. `pinRunningAuthority` (`scripts/portable-engine-promotion-lineage.mjs:1010-1025`, called :1047) pins the running verifier's own module, contract, bundle verifier, evidence contract, schemas and catalog against the **current tree's** copies. Re-running "the same object-hash checks used today" at R_prev fails whenever any of those files changed between R_prev and HEAD. The walk must use a reduced per-hop set — content-hash-verified commit/tree objects (`readGitObject`, :571-577, which independently re-hashes), canonical-JSON catalog parse (`parseCatalog`, :673-689), and merge shape (:1065-1074) — evaluated by the *HEAD* verifier's code. That reduction is defensible (arguably preferable: the current, reviewed verifier judges history), but it must be stated, because the security argument depends on exactly which checks survive the hop.

**(b) The settled M18 owner and the evolvable lineage are mutually inconsistent from promotion 2 onward.** `assertSourceAuthorityClosed` requires the **artifact-source revision's** `capsec/generated/target-advertisements.json` to be exactly empty v1 (`:822-826`) and its attestations empty (`:819-820`). Once the v2/v3 publication owns that path at promotion time, the *next* artifact-source revision is a descendant of the previous promotion merge and carries v2/v3 bytes there — so :823-826 fails unless an unstated reset-to-empty-v1 step runs each cycle. M18 pin 2's disposition ("**unchanged** … stays consistent with v2/v3 bytes at the *current* revision") holds only for the first promotion. M27 makes the *catalog* topology evolvable and leaves the *advertisement foundation* precondition one-shot.

**Exact resolution (text-only):** in M27 step 1, state the reduced per-hop check set explicitly and drop "unchanged in kind"; add a bullet dispositioning `portable-engine-promotion-lineage.mjs:804-826` under repeated promotions (either a per-cycle foundation reset stated as an operational rule, or the source-foundation pin revs with M17), and reconcile M18 pin 2's disposition with it.

### **IN-DELTA — MATERIAL — A5/M27: genesis admissibility has no history-completeness precondition**

Genesis is admissible when the walk "exhausts **retained** history" (§A5 final bullet; M27 step 3). Nothing constrains what "retained" means. `grep -n "shallow\|grafts\|rev-list\|--depth" scripts/portable-engine-promotion-lineage.mjs` finds only `--no-replace-objects` (:144); there is no `--is-shallow-repository` assertion, no graft/replace-ref check, no root-commit pin. A checkout whose **tree is current** but whose history is truncated (shallow clone, re-created repository, filtered history) therefore exhausts the walk immediately and mints a genesis scope — an arbitrarily narrow one — with no predecessor comparison and no F6 trip.

This is **not** covered by the whole-checkout-rollback boundary the revision documents in M27 and F6: that boundary concerns "a binary honestly rebuilt from **old sources**", where the entire checkout is stale. Here the sources are current and only the lineage evidence is absent. It is the one lane where the newly-claimed monotonicity guarantee can be bypassed without rewriting checked-in history.

**Exact resolution (text-only):** add to §A5/M27 a genesis precondition — the verifier asserts a non-shallow object store, no grafts or replace refs, and that the first-parent walk terminates at an expected root-commit object ID (or an explicitly pinned lineage floor) — and add an F6 subcase. Alternatively, name the truncated-history lane in M27's boundary paragraph as a stated non-guarantee, so the claim is honest rather than closed.

### **IN-DELTA — MINOR — M27 step 1 misstates today's admission cardinality**

`parseCatalog` asserts `catalog.admissions.length === 1` for an enabled catalog (`:686`) — one admission **globally**, not one per tuple. M27 step 1 says "the single-active-admission rule holds **per tuple**." A4's "exactly one active scope per tuple" and today's global rule are different invariants; the walk's "carries an admission for the canonical tuple" filter happens to be safe only because at most one admission exists at all. **Resolution:** one clause distinguishing today's global rule from A4's per-tuple rule, and state which one the evolvable topology adopts.

### **IN-DELTA — MINOR — M18 pin 4's "source-revision check" alternative is unavailable at that site**

`tracked_source_a_legacy_advertisement_stays_closed` (`src/host/portable_target_admission.rs:1866-1880`) uses `include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/capsec/generated/target-advertisements.json"))` — a compile-time embed of the working-tree file. A lineage-verifier-style source-revision check needs Git object access at check time, which a Rust unit test in that file cannot have. Only retirement/replacement by the F12 fixtures is genuinely open. A10 #2's "(b) the form — source-revision check vs. retirement" is a real choice for pins 5/6 (Node tests with `repoRoot` access) and a false one for pin 4. **Resolution:** say so in pin 4's disposition.

### **IN-DELTA — MINOR — A3's "no test constructor" is discipline, not a boundary**

Check 2c confirms `AdmittedScopedTargetCells` can be made genuinely unconstructible from `src/host/mod.rs` (private child module at `mod.rs:23`), so the M15 constructors provably cannot mint it — F11's negative half is sound for those. But `#[cfg(test)]` at `portable_target_admission.rs:1560` is a **descendant** module with full access to private constructors. **Resolution:** one clause narrowing "no test constructor" to "no constructor outside `portable_target_admission`; the in-file test module is constrained by review, not by the type system," so F11's negative half is not over-claimed.

### **IN-DELTA — MINOR — M7's zero-contribution sentence admits a reading that contradicts A2/M22**

"An authoritative execution or **fixture row** attributed to an out-of-scope cell refuses admission outright" reads either as (a) exclusion from the authoritative *unions*, or (b) out-of-scope report cells must literally carry empty `required_fixtures`. Reading (b) contradicts A2 ("out-of-scope rows are never reclassified… honest current status"), M22 ("the full-inventory derivation is **unchanged**"), and today's per-cell authority equality at `portable_target_admission.rs:1481-1487`, which applies to **every** cell and derives non-empty required fixtures for out-of-scope cells. Reading (a) is the only coherent one and is almost certainly intended. **Resolution:** one clause — "out-of-scope rows retain their honest source-derived `required_fixtures`; the rule governs the authoritative unions and the execution set, not row content."

### **PRE-EXISTING — MINOR — pin staleness is already live in M2/A2**

`assertRecipeCatalogComplete` is at `:5048` at the pin `6416114d` and at **`:5248`** at HEAD (verified both). The amendment discloses "Re-pin lines before implementing", so this is disclosed drift rather than an error — but §A2 and M2 quote `:5048`, `:5050-5055`, `:5062-5081` as current facts, and they no longer are. Worth a dated re-pin note when the amendment next moves. *(Confirmed separately: A9's header claim that `90aafc67` is "docs-only over the pin" is true — `git diff --stat 6416114d 90aafc67` = 3 llp files, 868 insertions.)*

---

## 4. Verdict

### **NOT READY** for the author's LLP 0044 item-5 decision — narrowly, and on four text-only items.

I want to be precise about why, because the revision is genuinely strong and I am not manufacturing a third round.

**What is now verified sound and should not be reopened:** Option B and its threat argument; the `AdmittedScopedTargetCells` design, including the non-obvious fact that Rust module boundaries really do make it M7-only-constructible (check 2c) and that the Host cell map has exactly one private chokepoint (check 2d); the `HostCellDisposition` placement with `decision.rs` untouched; M7's scoped equalities, which are equivalent-on-S and marginally *stronger* than today on the execution set (check 6); A7's claim wording; the A5 semantic-preservation rules; the F1a/F1b/F1c/F6/F11 fixture additions; M28's parser analysis and the genuineness of the A10 #5 choice (check 5b); M30's four contract pins; and — notably — **M11, which I re-ran verbatim and which is exact to the line, adjudicating round-1 Fable's "21" as the overcount**. Nine of the eleven round-1 flip-set items are cleanly RESOLVED.

**Why not READY:** both of Codex's BLOCKERs come back **PARTIAL**, and in each case the partiality sits on the exact sub-clause the round-1 resolution text named.

- BLOCKER 2's resolution said "Specify the host telemetry schema **and central emission point**." The schema is specified; the emission point is *asserted* — and three independent decision-evaluation bodies say otherwise. The author is being asked to decide A10 #1 between two options, one of which is described as resting on a chokepoint that must first be constructed.
- BLOCKER 1's resolution said "specify… **how the verifier finds the prior admitted scope in authenticated Git history**." The carrier is now fully and correctly specified (all four pins exact). The *finding* algorithm is not executable as written, its termination condition has no history-completeness precondition, and it contradicts a disposition the same revision settled two rows earlier.

Separately, M18 — the one row both families flagged in round 1 — repeats its defect after a targeted fix: 3 pins → 8 pins → still ≥11, with one stated disposition falsified by an unlisted pin on the digest-contract surface. A document whose §A9 declares itself "the authoritative gate-code worklist" is judged against that standard.

Only one finding (genesis via truncated history) touches a security property; the other three are precision and completeness on a worklist. None makes the design unsound, and none reopens a decided register item. All four resolutions are text-only, the same character as round 1's.

### Minimal flip set

1. **Repoint the projection and stop claiming a central refusal path** (A3, M14). Cite `src/host/mod.rs:956-961` (`fn target_cell`) as the projection funnel, not :2844-2846 (1 of 11 gate sites). State that no single refusal path exists — three `evaluate_decision_set*` bodies at :3794/:3844/:3874 — and add creating one (or emitting at all three) to M13/M14 as scope-validating work.
2. **Complete and correct M18** (M18, A10 #2). Add `capsec-contract.mjs:3514-3517`+:3682-3683 (which validates the HEAD copy against the v1 schema and therefore **falsifies pin 8's disposition**), `generate-capsec-registry.mjs:61-68`/:154-158, and `capsec-portable-engine-evidence-contract.test.mjs:1312-1324`; correct pin 8; note pin 4's `include_str!` forecloses the source-revision form; replace "the complete pin inventory" with a recorded sweep, as M11 now does.
3. **Make M27's algorithm executable and self-consistent.** State the reduced per-hop validation set and drop "unchanged in kind" (`pinRunningAuthority`, :1010-1025/:1047, forbids literal re-run); disposition `assertSourceAuthorityClosed`'s empty-v1 source foundation (:804-826) under repeated promotions and reconcile it with M18 pin 2; fix "per tuple" → today's global single-admission rule (:686).
4. **Give genesis a history-completeness precondition** (§A5, M27 step 3, F6) — non-shallow store, no grafts/replace refs, walk terminates at a pinned root — or explicitly name truncated history as a stated non-guarantee in M27's boundary paragraph, so the monotonicity claim is honest rather than overclaimed.

With items 1–4 landed I would expect the package to be READY; if the author prefers to close the loop here, items 1–3 are worklist-accuracy defects that an implementer will discover and can correct in place, while **item 4 is the one I would not want carried into a disagreement ledger unaddressed**, since it is a live bypass of a guarantee the amendment newly claims.

## Round 3 (final round) — 2026-08-06

- **Family:** Claude (Fable 5)
- **Provider/runtime:** fresh general-purpose subagent via Claude Code Agent
  tool (independent context; authored neither the amendment nor any
  revision of it)
- **Date:** 2026-08-06
- **Redacted:** no (public-repo content only)
- **Method:** final delta round at commit `8def7e20`, document revision
  `a770c2f95e9b`, clean working tree; 20 mandated delta checks executed
  including an independent count of the evaluator ingresses, a git-object
  reconstruction of the pinned lineage floor `afad4af9` and its parents,
  both M11 sweep commands re-run verbatim with the causal claim tested to
  the literal `orderBy` string, and an attempted construction of the
  genesis bypass; findings sorted ledger-worthy vs implementer-correctable
  per the final-round brief
- **Verdict:** **READY** for the author's LLP 0044 item-5 decision
  (3 MATERIAL + 5 MINOR, none blocking, all resolutions text-only)

### Review body (verbatim)

# Round 3 review — LLP 0021 scoped-advertisement amendment (final round)

**Package identity: VERIFIED.** `git log --oneline -1` = `8def7e20 Merge origin/main into LLP 0049 Phase 1 round-2 revision`; `shasum -a 256 llp/0021-capsec-effect-model-migration.plan.md` = `a770c2f95e9b2360807c…`, matching the brief's `a770c2f95e9b`. `git status --porcelain` is empty — no concurrent-agent edits contaminate any read below. `git diff --name-only f154a5c5 8def7e20` touches only `issues/`, `src/bin/ibex/engine/hermes.rs`, `src/engine/hermes_runtime_ios.cc` and `llp/`, so every citation re-verified at `f154a5c5` by the round-2 revision holds unchanged at HEAD.

---

## 1. Delta-verification results

| # | Check | Result | Observed |
|---|---|---|---|
| 1 | **Ingress count — "FIVE doors"** | **PASS (exact)** | `grep -n "fn .*decision" src/host/mod.rs`: `pub` at :3776, :3844, :3942, :3963; `evaluate_typed_decision_inner` (:3784), `_inner_and_then` (:3794), `evaluate_typed_path_decision_with_evidence` (:3874) are **private**. `grep -n 'pub unsafe extern "C" fn ex_host_.*decision' src/host/abi.rs` → exactly one symbol, `:5823`. Four `pub` + one C symbol = five. The count is right. |
| 2 | **`EffectGate.target_cell` caller-constructible** | **PASS (exact)** | `crates/capsec-semantics/src/decision.rs:401-407`: `#[derive(Clone, Debug, Deserialize, …)] #[serde(rename_all="camelCase", deny_unknown_fields)] pub struct EffectGate { … pub target_cell: TargetCellDisposition, … }`. Public field on a `Deserialize` struct. Confirmed. |
| 3 | **C symbol is unconditionally exported and passes gates through unchanged** | **PASS** | `src/host/abi.rs:5822-5839`: `#[no_mangle] pub unsafe extern "C" fn ex_host_evaluate_typed_decision(…)`, no `cfg`, `gates` taken as a raw caller buffer, dispatched at `:5835` to `host.evaluate_typed_decision_json_with_evidence`. |
| 4 | **Runtime-extension path facts** | **PASS (exact)** | `src/host/mod.rs:1107` `pub(crate) fn authorize_runtime_extension_operation`, no `cfg(test)`; `effect_semantics: &str` param at `:1113`; `let coverage_edge_id = effect_semantics.clone();` at `:1266`; static-floor loop `static_authority_covers` at `:1255-1263` (inside A3's cited `:1245-1264`); gate literal `target_cell: TargetCellDisposition::Complete` at `:1306-1309`. Exported via `src/host/abi.rs:5447`; also reached from `src/host/embedder_artifacts.rs:2193`. |
| 5 | **A3 ingress rule closes the caller-supplied `targetCell` hole?** | **PASS in substance, FAIL on composition** | Discard-and-recompute at the four `pub` methods does close it: after recomputation the caller's byte has no authority, and `fn target_cell`'s `unwrap_or(Incomplete)` gives the total answer for absent ids. **But** `authorize_runtime_extension_operation` reaches the evaluator by calling `self.evaluate_typed_decision(…)` at `src/host/mod.rs:1304` — ingress 2 physically flows **through** ingress 1. A3/M32 state two mutually contradictory dispositions for the same input at that one call site (absent edge ⇒ `Incomplete`/refuse for ingress 1; absent edge ⇒ keep literal `Complete` for ingress 2) and never say how they compose. **New finding N1.** |
| 6 | **The projection funnel and eleven gate sites** | **PASS (exact)** | `grep -c "target_cell: self.target_cell" src/host/mod.rs` → **11**, at exactly :2346, :2846, :2929, :3002, :3107, :3188, :3346, :3495, :3588, :3684, :3768 — every line number in A3/M13 correct. `fn target_cell` at `:956-961` is `self.target_cells.get(edge).copied().unwrap_or(Incomplete)`; the `.copied()` makes A3's "`HostCellDisposition` must derive `Copy`" a real, load-bearing requirement, not a stylistic one. |
| 7 | **Three evaluation bodies / no central refusal path** | **PASS (exact)** | `grep -n "evaluate_decision_set" src/host/mod.rs` → four call sites at :3811, :3820, :3857, :3888, in three bodies at :3794, :3844, :3874. The round-2 correction is accurate. |
| 8 | **F3a-3 pins recomputation, not min-of-two** | **PASS** | `decision.rs:609-621` refuses on `Incomplete` before any lifecycle result, carrying `Some(gate.coverage_edge_id)` at `:618`. A "most-restrictive-wins" rule on (caller=`incomplete`, aggregate=`Complete`) yields refuse; recomputation yields `Complete`/allow. F3a-3 therefore genuinely discriminates the two rules. F3a-5's `CompleteAdvertised` no-op is consistent, because the rule is gated on the arm state. |
| 9 | **M27 (i) predicate exact against `:1064-1087`?** | **PASS in substance, FAIL on four pins** | All seven clauses correspond to real asserts in the same order: 2 parents `:1065`✓, `parents[0]===sourceRevision` `:1066`✓, topic one-parent `:1070` (M27 says :1069), `R.tree===T.tree` `:1071` (M27 says :1070), `sourceCommit.tree===sourceTreeObjectId` `:1072` (M27 says :1071), `sourceCommit.tree!==R.tree` `:1073` (M27 says :1072). The **range** `:1064-1074` is right; four individual pins are off by one in a subsection that claims re-verification. Substantively the predicate **does** exclude catalog-byte-inheriting descendants — an ordinary first-parent descendant has one parent and fails clause 2; an unrelated merge fails `parents[0]===sourceRevision`. Codex's round-2 BLOCKER-1 defect is genuinely fixed. **New finding N4.** |
| 10 | **M27 (ii) — does `admittedScopeDigest` on the tracked admission work?** | **PASS** | `validateAdmissionShape` at `:691-704` is exactly the eight keys named (`schema`…`admissionDigest`) — a one-string addition. Decisively, `:758` computes `semanticDigest(ADMISSION_DOMAIN, admission, ["admissionDigest"])` over the **whole** object minus its self-digest, so a new field is authenticated automatically. M27's "already covered by `admissionDigest`" is **true**. The companion `schemas/portable-engine-promotion-admission-catalog-v1.schema.json` does close with `"additionalProperties": false` (`:6`), and M27 names it. |
| 11 | **M27 (iii) — is the reduced per-hop set sufficient?** | **PASS on mechanism, MINOR overstatement** | Every named primitive is real and independently re-hashing: `readGitObject` `:571`, `parseCommit` `:580`, `collectTreeLeaves` `:641`, `readTrackedBlob` `:667`, `parseCatalog` `:673-689`, `validateAdmissionShape` `:691-704`; `pinRunningAuthority` `:1010-1025` genuinely forbids literal re-run. Sufficiency: the only value read at R_prev is `admittedScopeDigest`, and structure+content is enough to prove R_prev *is* a promotion merge. But M27 says the ceremony result is "attested by R_prev's own `admissionDigest`" — that digest is self-computable by anyone who can write a commit and attests only internal consistency. The load-bearing fact is solely first-parent reachability in reviewed history. **New finding N5.** |
| 12 | **M27 (iv) — does the reset lifecycle reconcile M18 pin 2 past promotion 1?** | **PASS, and stronger than stated** | `assertSourceAuthorityClosed` `:804`: catalog disabled+empty `:807`, attestations empty-v1 `:819-820`, advertisements empty-v1 `:825-826` — a reset commit restoring exactly those three satisfies all of them, so pin 2 is genuinely unchanged for every promotion. Two clauses the amendment does **not** discuss also survive, and I verified why: `assertSourceAuthorityClosed`'s trust-policy (`:812-814`) and CapSec-policy-rules (`:835-841`) clauses need no reset because `verifyChangedArtifacts` (`:930`) asserts the source→merge changed-path set equals **exactly** `[CATALOG_PATH, ...admission.artifacts]`, so a promotion never touches them; and its `conformance-evidence must be newly added` rule survives because `evidencePrefix` embeds `admission.sourceRevision`, making promotion n+1's evidence paths disjoint from n's. The lifecycle is mechanically sound. |
| 13 | **What the stated cost breaks** | **PASS — honestly priced** | "Between the reset and the merge the tree advertises nothing" means the advertised window is exactly `R_n` → reset−1, and releases must be cut there. Consequences I confirmed: in that window the M18 pin 4/5/6/12 HEAD-copy emptiness assertions are red (which is why they retire), and pin 9 fails — the amendment says so. Nothing else breaks: today's tree already advertises nothing (`git log --all -S'"enabled": true' -- <catalog>` → no commit), so the depublished state is the status quo, not a regression. |
| 14 | **M27 (v) — is `afad4af9` a sound lineage floor?** | **PASS on the floor, FAIL on the parent clause** | `afad4af9f4257eb8262cf8348e5fbb0a3c082ecf` = "Merge checked portable promotion admission"; its catalog is present, `"enabled":false`, `"admissions":[]` — verified by `git show`. It is first-parent-reachable from HEAD (`git rev-list --first-parent --count HEAD ^afad4af9` = 406). No `enabled: true` catalog has ever existed. **But `afad4af9` is itself a two-parent merge** (`3d778d22`, `00255532`), and the **second** parent contains the catalog path with byte-identical disabled/empty content. M27 (v) and F6a say "the floor's **parent**" (singular) must lack the path — false of the pinned object under any reading but "first parent." **New finding N2.** |
| 15 | **M27 (vi) — does the genesis precondition close the truncated-history bypass?** | **PARTIAL** | All three assertions are real, correctly specified, and pass here: `git rev-parse --is-shallow-repository` → `false`; `.git/shallow` absent; `.git/info/grafts` absent; `git for-each-ref refs/replace` → 0 lines; floor reachable by first-parent. This **does** close the shallow-clone / CI-misconfiguration lane round-2 Fable found. It does **not** close reconstruction: one commit whose parent is `afad4af9` and whose tree is the current tree passes all three assertions with no promotion in the walk. **New finding N3.** |
| 16 | **M11 re-adjudication — did `grep -v target/` eat a line?** | **PASS (exact, including the causal claim)** | Old pipeline re-run verbatim → **20 lines / 16 files**. New path-anchored command → **21 lines / 17 files**; with `-o` → **23 occurrences**. `diff` of the two file lists yields exactly one addition: `vendored-generated/capsec-runtime-projection.canonical.json`. That file is one line (`wc -l` = 0) and contains `"orderBy":["/edgeId","/target/triple","/target/features"]` — verified by `grep -o`. The content filter really did eat it, for the reason and with the verbatim string the row states. It carries the schema string **twice**, as does `arming.rs:3679`, reconciling 21 + 2 = 23. Fully vindicated. |
| 17 | **M18 recorded sweep** | **PASS (exact)** | Re-ran the recorded command → **19 lines / 15 files**, matching the row to the number. |
| 18 | **M18 pin 9 — release-breaking under the settled owner?** | **PASS — confirmed release-breaking** | `capsec-contract.mjs:3514-3517` reads the **HEAD** copy via `readConfinedGeneratedJson(path.join(capsecRoot,"generated/target-advertisements.json"))`; `:3680-3685` is `validateWith(ajv, SCHEMA_IDS.targetAdvertisements, targetAdvertisements, "target advertisements")`; `SCHEMA_IDS.targetAdvertisements` at `:130-131` = `https://ibex.dev/capsec/schema/target-advertisements.schema.json`, whose `:13` pins `"targetAdvertisementSchema": { "const": "ibex/capsec-target-advertisements/1" }`. v1-shaped reads confirmed at `:4205`, `:4218`, `:4231`, `:4426`, `:4495`; reachable because `capsec/contract-files.json:97` lists the path. v2/v3 bytes fail the runner outright. The row's "this pin, not pins 4–6, is the one that gates whether the settled owner can ship" is correct. Recommendation sound in direction, with one unpriced dependency — **New finding N7**. Pins 10 and 12 also verified real (`generate-capsec-registry.mjs:61-68`, `:154-158`, `:280`; `capsec-portable-engine-evidence-contract.test.mjs:1312-1314`). |
| 19 | **A3 opacity narrowing** | **PASS (exact)** | `src/host/mod.rs:23` = `mod portable_target_admission;` (private child); `src/host/portable_target_admission.rs:1560` = `#[cfg(test)]` above `mod tests` (descendant with private access). The round-2 narrowing is precisely right. |
| 20 | **Disclosed pin drift** | **PASS** | `assertRecipeCatalogComplete` is at `:5248` today, as the amendment's disclosure paragraph states; `packages/` is untouched between `f154a5c5` and HEAD. |

---

## 2. Disposition of every round-2 finding

### Codex round-2 (2 IN-DELTA BLOCKERs, 3-item flip set)

| Finding | Disposition | Exact resolving/failing location |
|---|---|---|
| **BLOCKER 1 — M27 does not identify an authentic historical promotion revision** | **RESOLVED** | M27 (i) supplies an exact predicate whose seven clauses map one-to-one onto `portable-engine-promotion-lineage.mjs:1065-1073` (check 9), and it demonstrably excludes catalog-byte-inheriting descendants. M27 (ii) resolves the "`R_prev`'s `admittedScopeDigest` does not exist" defect by moving the field into the **tracked** admission at `:691-704`, which check 10 confirms is a one-key addition automatically covered by the `:758` digest. Residuals are precision only (N4, N5). |
| **BLOCKER 1a — enabled/disabled catalog lifecycle between promotions; reconcile M18 pin 2** | **RESOLVED** | M27 (iv). Check 12 confirms the reset satisfies `:807`/`:819-820`/`:825-826`, and additionally that `verifyChangedArtifacts` (`:930`) and the `conformance-evidence` newly-added rule do not obstruct it — a compatibility the amendment does not claim but which holds. M18 pin 2's amended disposition and M27 (iv) are mutually coherent. |
| **BLOCKER 1b — pre-foundation genesis semantics** | **PARTIAL** | M27 (v) pins `afad4af9` by object id and check 14 confirms the floor's own state exactly. The "floor's parent lacks the path" clause is falsified by the floor being a two-parent merge (N2). |
| **BLOCKER 2 — scoped gate disposition remains caller-controlled through the Host ABI** | **RESOLVED** | A3's ingress rule + M32 + F1c's ABI subcase + the new F3a class. Checks 1–4 confirm the diagnosis is exactly right and the door count complete; check 8 confirms F3a-3 is a genuine discriminator against a min-of-two rule; check 6 confirms the funnel and the `Copy` requirement. Codex's resolution text asked for "discard/recompute **or** equality-check"; the revision picked the stronger of the two and argued it correctly (totality, trust-boundary removal, no comparison to invert — all three arguments check out against `:956-961`). Residual: composition with ingress 2 (N1). |
| **BLOCKER 2a — "identify one complete telemetry emission path covering all evaluator variants"** | **RESOLVED** | A3 withdraws the false "central path" claim, check 7 confirms the three-body/four-call-site fact exactly, M13 item 2 makes building the funnel explicit scope-validating work, and F3 drives the fixture through all three bodies. |
| **Flip-set item 3 — correct M11's sweep/count, include the generated mirror** | **RESOLVED (exactly)** | Check 16. Both commands re-run; 20/16 vs 21/17 vs 23 all reproduce, the seventeenth file is the one Codex named, and the causal explanation is verified verbatim down to the `orderBy` string. |

### Fable round-2 (4 MATERIAL + 5 MINOR, 4-item flip set)

| Finding | Disposition | Exact resolving/failing location |
|---|---|---|
| **MATERIAL 1 — no central refusal path; projection site mis-pinned** | **RESOLVED (exactly)** | A3's "Projection site corrected" paragraph and M13 items 1–2. Check 6 confirms all eleven line numbers and the `:956-961` funnel; check 7 confirms :3794/:3844/:3874. |
| **MATERIAL 2 — M18 inventory still incomplete; pin 8's disposition falsified** | **RESOLVED (exactly)** | Pins 9–13 added; pin 8's disposition corrected to "schema file unchanged, but its binding to the HEAD copy moves with pin 9"; "complete pin inventory" replaced by a recorded command that reproduces at 19/15 (check 17). Pin 9 verified release-breaking to the line (check 18). |
| **MATERIAL 3a — M27 step 1 not executable (`pinRunningAuthority`)** | **RESOLVED** | M27 (iii) states the reduced set explicitly, drops "unchanged in kind", and argues the posture. Check 11 confirms every primitive and the `pinRunningAuthority` obstruction. |
| **MATERIAL 3b — settled M18 owner vs. evolvable lineage inconsistent from promotion 2** | **RESOLVED** | M27 (iv), verified in check 12; the amendment explicitly rejects the alternative (revving `assertSourceAuthorityClosed`) with a stated reason. |
| **MATERIAL 4 — genesis via truncated history (flagged as the one live bypass)** | **PARTIAL** | M27 (vi) + §A5 + F6e. Check 15: the three preconditions are real, correctly specified, and close the shallow/graft/replace-ref and CI-misconfiguration lanes completely. They do not close reconstruction on top of the pinned floor, so the amendment's "**closed**, not disclosed" and "absence … never the failure of a search" are stronger than what the mechanism delivers. **N3.** |
| **MINOR — admission cardinality is global, not per tuple** | **RESOLVED** | M27's "Today's admission cardinality is GLOBAL" paragraph; `parseCatalog:686` verified (`admissions.length === 1` for any tuple), `:683` verified for the disabled case. |
| **MINOR — M18 pin 4's source-revision alternative unavailable** | **RESOLVED** | M18 pin 4 now says only retirement or replacement is open, with the `include_str!`/`CARGO_MANIFEST_DIR` reason stated; A10 #2(b) records the same. |
| **MINOR — A3's "no test constructor" is discipline, not a boundary** | **RESOLVED (exactly)** | A3's parenthetical + F11's negative half narrowing; check 19 confirms `mod.rs:23` and `portable_target_admission.rs:1560`. |
| **MINOR — M7's zero-contribution sentence admits a contradictory reading** | **RESOLVED** | M7's "Scope of that rule, disambiguated" paragraph adopts reading (a) explicitly, names the three things reading (b) would contradict, and cites `portable_target_admission.rs:1481-1487` as the falsifier. |
| **PRE-EXISTING MINOR — pin staleness in M2/A2** | **RESOLVED as disclosure** | The new "Disclosed pin drift" paragraph records `:5048` → `:5248`; check 20 confirms `:5248` today. |

**Round-1 carry-forwards still PARTIAL entering round 3** (Codex's disposition table): C-B1 and C-B2 → now RESOLVED per the above; Codex-MATERIAL "consumer matrix incomplete" → **RESOLVED** (M11's mirror added, check 16); Codex-MATERIAL "rename/split/merge narrowing" → **RESOLVED** as to rules, with tuple migration correctly parked as A10 #7 (a decision, not a defect); Fable-MINOR 8 denominator drift → resolved inside the package, with the LLP 0049 residual correctly excluded as out-of-package.

---

## 3. New findings

### N1 — IN-DELTA — MATERIAL — the ingress rule and the runtime-extension carve-out collide at one verified call site

`Host::authorize_runtime_extension_operation` does not reach the evaluator independently: it calls `self.evaluate_typed_decision(&decision_set, &[EffectGate{ coverage_edge_id, target_cell: Complete, … }])` at `src/host/mod.rs:1304`, i.e. through **ingress 1** (`:3776`). A3 gives that call site two contradictory rules:

- ingress 1: "**must discard the incoming `target_cell` and recompute it** from the aggregate" — and for an id absent from the aggregate "the honest answer there is already `target_cell`'s `unwrap_or(Incomplete)` default";
- ingress 2: "if it is **not** a generated edge, the path keeps its literal `Complete`" with `hostDisposition: "extension-declared"`.

Applied literally at `:3776`, the first rule makes the second unreachable, and **every** runtime-extension operation whose `effect_semantics` string is not a generated `edgeId` — which A3 itself says is the normal case — is refused under `ScopedAdvertised`. This fails closed (a liveness break, not a bypass) and F3a-4's second half would catch it immediately. The hazard is the *other* obvious repair: an implementer who reconciles them by making "absent from the aggregate ⇒ keep the caller's value" at ingress 1 silently reopens Codex BLOCKER 2 for every non-inventory `coverage_edge_id` presented over `ex_host_evaluate_typed_decision`.

**Exact resolution (text-only):** in A3 and M32, state that the two rules apply at *different* points — recomputation at the four `pub` entry points and the C symbol (where the gate arrives from outside), with the runtime-extension path constructing its gate through a distinct internal resolver that has already applied the conditional rule — and state explicitly that "absent from the aggregate ⇒ keep the caller's value" is forbidden at ingress 1.

### N2 — IN-DELTA — MATERIAL — M27 (v)'s floor-parent clause is false about the pinned floor object

`git log -1 --format='%H %P' afad4af9` → `afad4af9f425… 3d778d2281d6… 0025553297f4…`. The pinned floor is a **two-parent merge**, and `git show 00255532:schemas/portable-engine-promotion-admission-catalog-v1.json` returns the catalog with identical `{"enabled":false,"admissions":[]}` bytes. M27 (v)'s normative "at the floor's parent the path must be **absent**" — and F6a's "whose floor parent lacks the catalog path entirely" — are true only of the **first** parent (`3d778d22`, verified absent). An implementation reading "the parent" as "every parent" fails on this repository and can never conclude genesis. This is fail-closed and the fix is one word, but the amendment elevated exactly this clause to "a positive, checkable object-level fact," and the fact as written is not one.

**Exact resolution (text-only):** say "the floor's **first** parent," and note in M27 (v) that `afad4af9` is a merge whose second parent introduced the path, so the clause is a first-parent statement by construction.

### N3 — IN-DELTA — MATERIAL — the genesis precondition closes truncation, not reconstruction; "closed, not disclosed" overstates it

M27 (vi)'s three assertions are real, correctly specified, and all pass here (check 15). They fully close the lane round-2 Fable actually found — a shallow or graft-bearing checkout, including the accidental CI-misconfiguration form, can no longer mint genesis. But they do not make absence "a positive, checkable fact about content-hashed objects." A repository whose HEAD chain is `<one commit carrying the current tree> → afad4af9` is non-shallow, has no grafts or replace refs, and terminates first-parent at the pinned floor by object id — so all three assertions pass and the walk finds no prior admission. Constructing it costs one `git commit`; it does not require rewriting any published object, because `afad4af9` is reused verbatim. Nothing in `verifyPortableEnginePromotionAdmission` pins HEAD to an externally attested tip: `pinRunningAuthority` (`:1010-1025`) compares the current tree's verifier files against the *running* files, which such a checkout satisfies trivially.

This residual is, in my judgement, genuinely of the same character as the whole-checkout-rollback boundary the amendment already documents as out of scope — so the design is not unsound, and the security *improvement* is real and large. What is wrong is only the claim's absoluteness, and the A10 preamble's explicit rejection of naming any residual ("a newly claimed monotonicity guarantee should not ship with a named bypass") is what makes the overstatement load-bearing: the author is being told the lane is shut.

**Exact resolution (text-only, one sentence):** in M27 (vi) and A10, state that the preconditions close history *truncation* (shallow, grafts, replace refs, filtered history) and that history *reconstruction* on top of the pinned floor remains inside the documented whole-checkout boundary, where the trust assumption is "you are standing in the reviewed repository." Keep F6e as-is.

### N4 — IN-DELTA — MINOR — four M27 (i) line pins are off by one

The topic-parent clause is at `:1070` (M27 says `:1069`), `R.tree === T.tree` at `:1071` (says `:1070`), `sourceCommit.tree === A.sourceTreeObjectId` at `:1072` (says `:1071`), `sourceCommit.tree !== R.tree` at `:1073` (says `:1072`). The enclosing range `:1064-1074` is correct and the clauses are substantively exact. Notable only because this subsection states "All pins in this subsection re-verified at `f154a5c5`." **Resolution:** shift the four pins by one.

### N5 — IN-DELTA — MINOR — M27 (iii) attributes attestation value to `admissionDigest` that it does not have

M27 (iii) says the full ceremony's result "is attested by R_prev's own `admissionDigest` and by the fact that R_prev is in the first-parent chain of a reviewed history." `:758` computes `semanticDigest(ADMISSION_DOMAIN, admission, ["admissionDigest"])` — a self-digest anyone who can write a commit can compute. It proves internal consistency only. The second conjunct is the whole of the argument. Since M27 (iii) explicitly invites round-3 attack on this exact sentence, it should be precise. **Resolution:** drop the first conjunct.

### N6 — IN-DELTA — MINOR — "the only in-repo caller of the C symbol" is three callers

`grep -rn "ex_host_evaluate_typed_decision" src` returns `src/bin/ibex/engine/hermes.rs:1144`, `src/bin/ibex/engine/capsec_host_abi_output_batch.test.rs:2014` (included from `hermes.rs:6301`), and `src/host/abi.rs:13208` (below the `#[cfg(test)]` at `:10476`). All three are test-gated, so the substance — no production in-repo caller, symbol exported unconditionally — holds. **Resolution:** "the in-repo callers are all test-gated," with the three sites named.

### N7 — IN-DELTA — MINOR — M18 pin 9's preferred option carries an unpriced dependency

"The runner reads the artifact-source revision's copy instead of HEAD" is recommended, but `capsec-contract.mjs` today is a pure working-tree checker (`readConfinedGeneratedJson`, `:3514-3517`) with no Git-object access and no lineage dependency. Making it read the artifact-source blob requires it to *resolve which revision that is* — i.e. to consume the promotion catalog or the lineage verifier — a new coupling from the digest contract to the lineage chain that the row does not price. It is probably still the right call (it keeps `capsec/contract-files.json:97`'s digest over unchanged empty-v1 bytes and leaves pin 11's vectors unrestamped), but the cost belongs in the row. **Resolution:** one clause naming the new dependency.

### N8 — PRE-EXISTING — MINOR — under `CompleteAdvertised` a caller can present `complete` for a `Closed` cell

F3a-5 correctly scopes the ingress rule to `ScopedAdvertised`, so today's releases are untouched — which also means the caller-supplied `target_cell` remains authoritative on every shipping build. Beyond the out-of-scope case the amendment analyses, the same channel lets a caller submit `target_cell: "complete"` for a cell the admitted map holds as `Closed`, converting a deny (`decision.rs:672-683`) into a pass, and lets any non-inventory `coverage_edge_id` evade the `unwrap_or(Incomplete)` default. The practical reach is bounded — `ex_host_evaluate_typed_decision` returns a verdict and grants nothing, so the exposure is to the typed-evidence stream and to embedders that treat the ABI answer as their enforcement point rather than to host-mediated capability — but this is a genuine pre-existing property that M32's survey uncovered and did not name. It is outside this amendment's remit. **Resolution:** file a ticket; do not expand this amendment.

---

## 4. Verdict

# READY

for the author's LLP 0044 register-item-5 decision.

Both round-2 BLOCKERs are **substantively resolved**, and I verified the hard parts myself rather than taking the revision's word:

- The ingress diagnosis is exactly right, the door count is exactly right (four `pub` methods at :3776/:3844/:3942/:3963 plus exactly one exported C evaluator symbol at `abi.rs:5823`, plus the runtime-extension path), discard-and-recompute is the stronger of the two remedies Codex offered and its three-part justification checks out against `fn target_cell:956-961`, and F3a-3 is a real discriminator against the weaker min-of-two rule that would otherwise have passed F3a-1.
- The lineage algorithm is now implementable: the (i) predicate maps one-to-one onto `:1065-1073` and provably excludes the descendants that defeated the round-1 sketch; (ii)'s move of `admittedScopeDigest` into the tracked admission at `:691-704` works and is automatically authenticated by `:758`; (iv)'s reset lifecycle satisfies `assertSourceAuthorityClosed` at `:807`/`:819-820`/`:825-826` and — a compatibility the amendment does not even claim — also survives `verifyChangedArtifacts`'s exact changed-path-set equality and its newly-added-evidence rule; and (v)'s floor object is real, disabled, empty, and 406 first-parent commits below HEAD.
- The two enumerations both families punished are now **exact and reproducible**: M11's 20/16 → 21/17 → 23 all reproduce, including the verbatim `orderBy` string that explains the content-filter defect; M18's recorded sweep reproduces at 19/15 and pin 9 is confirmed release-breaking to the line.

My eight findings comprise three MATERIAL and five MINOR. **None is blocking**: every one fails closed, none admits an unsound certification or a broken arming, none reopens a decided register item, and none changes the answer to the question the author is being asked (Option B + `ScopedAdvertised` + the aggregate as sole introspection authority, all of which I verified are supported by the real code). All eight resolutions are text-only. Withholding READY here would buy a fourth round that the LLP 0049 §9 bound does not permit, in exchange for corrections the author or an implementer can make in place.

**For the author (must reach them, one sentence each):**
- **N3** — the genesis precondition closes truncation and misconfiguration, not reconstruction on the pinned floor. The author rejected "name it as a non-guarantee" in favour of "closed"; the honest position is that the residual falls inside the *already-documented* whole-checkout boundary. This is the only finding touching a security claim, and the author should see it before accepting.
- **N8** — a pre-existing ABI property surfaced by M32's survey, out of this amendment's remit; it wants a ticket, not an amendment change.

**For the implementer (correct in place before or during gate code):**
- **N1** — state how the ingress rule and the extension carve-out compose at `mod.rs:1304`, and forbid "absent ⇒ keep the caller's value" at ingress 1. This is the one with a trap in it: the wrong repair silently reopens BLOCKER 2.
- **N2** — "the floor's **first** parent."
- **N4, N5, N6, N7** — four pins shifted by one, one over-attributed digest, one "only caller," one unpriced dependency.

## Round 4 — 2026-08-06

- **Family:** Claude (Fable 5)
- **Provider/runtime:** fresh general-purpose subagent via Claude Code Agent
  tool (independent context; authored neither the amendment nor any
  revision)
- **Date:** 2026-08-06
- **Redacted:** no (public-repo content only)
- **Method:** delta round on the previously-unreviewed final revision at
  `86662719` (doc `1de5bb0b19aa`); **constructed a real SHA-1 repository**
  containing a correctly-shaped two-parent promotion merge with a
  canonical catalog and a `validateAdmissionShape`-clean, self-consistent
  admission, then ran the repository's own exported verifier primitives
  (`collectTreeLeaves`, `changedLeaves`, `readGitObject`, `parseCommit`,
  `rawDigest`, `semanticDigest`) under both the round-2 and round-3 clause
  sets; also ran the real `verifyChangedArtifacts` against a synthetic
  promotion carrying the new role
- **Verdict:** **READY** — dual-READY with Codex round 4. 5 MATERIAL +
  4 MINOR, none blocking, every one fails closed; eight of nine
  resolutions text-only

### Review body (verbatim)

# Round 4 (delta) review — LLP 0021 scoped-advertisement amendment

**Reviewer:** independent adversarial reviewer, Claude/Fable family. I authored neither the amendment nor any revision of it.
**Package identity: VERIFIED.** `git rev-parse HEAD` → `8666271922c3163129c05fa7900cc83042298912` (matches brief's `86662719`). `shasum -a 256 llp/0021-capsec-effect-model-migration.plan.md` → `1de5bb0b19aa0e64090a88be4bade10ca6aa7e9f4…` (matches `1de5bb0b19aa`). `git status --porcelain` empty at start and at end of the session. `git diff --stat fb343180 HEAD -- scripts/portable-engine-promotion-lineage.mjs src/host/mod.rs src/host/portable_target_admission.rs build_support/` → **empty**, so every citation the round-3 revision re-verified at `fb343180` holds unchanged at HEAD. No drift to note beyond first-parent depth below the floor (406 at `8def7e20` → **410** at HEAD, expected).

---

## 1. Delta-verification results

| # | Check | Result | Observed |
|---|---|---|---|
| 1 | **M27 (i) cited primitives are real** | **PASS (exact, 8 of 10 pins)** | `grep -n` on `scripts/portable-engine-promotion-lineage.mjs`: role list `:724` ✓; `changedLeaves` fn `:858` ✓ (`:858-865` ✓); changed-path equality `:938-943` ✓ (`expectedPaths` :938, `observedPaths` :939, assert :940-943); per-row `current.objectId === artifact.blobObjectId` **`:959`** ✓, `bytes.length === artifact.size` **`:967`** ✓, `rawDigest(bytes) === artifact.digest` **`:968`** ✓; `assertArtifactRolePath` `:784` ✓; `evidencePrefix` `:761-763` ✓; `validateAdmissionShape` `:691-759` with unkeyed self-digest at **`:758`** ✓; `verifyChangedArtifacts` `:930-1001` ✓, `verifyPromotionBundleGraph` call at **`:995`** ✓. **Two pins off by one:** roleCounts cardinality is at `:736-738` (M27 says `:735-737`; `roleCounts.set` is `:734`), and `verifyPromotionBundleGraph` is `:867-`**`928`** (M27 says `:867-929`). Both in subsections claiming re-verification at `fb343180`. **New finding N8.** |
| 2 | **The tree-unbacked-digest attack — constructed and run** | **PASS: all three F6f variants REFUSE** | I built a scratch SHA-1 repo with a real source revision, a real one-parent topic, and a real two-parent merge (`R.tree === T.tree`, `R.parents[0] === A.sourceRevision`, `sourceCommit.tree === A.sourceTreeObjectId`), 14 artifact rows satisfying `MIN_CHANGED_ARTIFACTS` (`:80`), and a self-consistent recomputed `admissionDigest`, then ran the **real** exported primitives (`collectTreeLeaves`, `changedLeaves`, `readGitObject`, `parseCommit`, `rawDigest`, `semanticDigest`) under both clause sets. Results: **(a)** no `scope-artifact` row → round-2 predicate **ACCEPTS**, round-3 joins **REFUSE** (`expected exactly one scope-artifact row, got 0`); **(b)** row declared but blob absent from the tree → round-2 **ACCEPTS**, round-3 **REFUSES** at the changed-path-set equality; **(b′)** row present but `blobObjectId`/`size`/`digest` disagree with the tree blob → round-2 **ACCEPTS**, round-3 **REFUSES** (`blobObjectId mismatch`); **(c)** byte-consistent row whose recomputed digest ≠ declared → round-2 **ACCEPTS**, round-3 **REFUSES** (`recomputed scopeDigest sha256-JXuzhQ3-… !== admittedScopeDigest sha256-AAAA…`). **F6f is a genuine regression test: it fails against the round-2 text and passes against the round-3 text**, exactly as claimed. Codex's round-3 BLOCKER is answered in substance. |
| 3 | **The attack's residual — is anything left?** | **PARTIAL (honestly stated, one cheap join omitted)** | A fourth variant I added — fully self-consistent, attacker-**authored** narrow scope artifact, blob present, digest recomputes — is **ACCEPTED** by the round-3 joins. That is inherent and the amendment states it correctly: the joins prove "a hop whose *published artifacts* are real and whose *declaration* matches them; it is not a hop whose ceremony has been re-run" (`llp/0021…:5586-5588`), with the residual carried by first-parent reachability in reviewed history. **But** the joins never cross-check the scope artifact against the **published advertisement blob in the same tree**, which is itself already declared, path-pinned (`assertArtifactRolePath` `:786-789`) and blob-verified, and which per M17 carries `scopeDigest`. **New finding N7.** |
| 4 | **Subset-vs-full-set: is the liveness argument sound?** | **PASS on the argument, FAIL on its stated reason** | `verifyChangedArtifacts` does terminate in `verifyPromotionBundleGraph` (`:995`, fn `:867-928`), which hands **published** bytes to `validatePortablePromotionBundleGraph` (`verify-capsec-portable-promotion-bundle.mjs:269`) → `validatePortablePromotionV2`, whose schema constants are **hard-pinned with no dispatch**: `PORTABLE_CONFORMANCE_SCHEMA = "ibex/capsec-conformance/2"` (`capsec-portable-engine-evidence-contract.mjs:46`), `PORTABLE_TARGET_ATTESTATIONS_SCHEMA = "…/2"` (`:48-49`), `PORTABLE_TARGET_ADVERTISEMENTS_SCHEMA = "…/2"` (`:50-51`). So after M17's v2→v3, HEAD's bundle verifier over promotion *n*'s bytes fails on a version constant. **The argument is sound.** However the justification the revision gives for the subset — "They are all pure functions of content-hashed objects … so they carry no version dependency on the HEAD verifier's schema constants" (`llp/0021…:5509-5512`) — is **false of the subset's own load-bearing clause**. **New finding N3.** |
| 5 | **Is the subset a genuine floor?** | **PASS** | Every subset clause is strictly weaker than a real check in `verifyChangedArtifacts`, none is a substitute, and the text forbids reading it as licence to omit any (i) join (`:5551-5552`). The MAY/SHOULD escalation rule is coherent. |
| 6 | **M27 (ii) tuple-identity consumer list complete?** | **FAIL** | Every cited site is exact: triple regex `:710` ✓, evidence path templates `:739-740` ✓, `evidencePrefix` `:762` ✓, trust-policy row `:814` ✓, CapSec-rules candidate `:836`/`:840` ✓, checked-admission constructor `:1192` within `:1185-1199` ✓; `expectedTarget` derivation `:842-855` with closed key set `:843` and canonical sorted features `:844-854` ✓ exact. Rust side exact: `exact_object_keys` closed list including `"targetTriple"` at `src/host/portable_target_admission.rs:613-628` ✓, `require_checked_promotion` `:639` ✓ (its current triple-only join is at `:705`, `admission.target_triple != advertisement.target.triple`); installer `:476` `lineage.targetTriple === TARGET_TRIPLE` ✓. **But the list omits the lineage verification-result surface entirely** — and that surface is the only channel by which canonical features can reach `:1192`. **New finding N2.** |
| 7 | **Does the `:758` digest projection really need no change?** | **PASS (exact)** | `semanticDigest(ADMISSION_DOMAIN, admission, ["admissionDigest"])` is computed over the **whole** object minus its self-digest, so any key added to the closed set is authenticated automatically. The claim is true, and the revision's own caveat — "What the self-digest does **not** do is prove the digest names a real artifact" (`:5473-5474`) — is correct and is what (i)'s joins now supply. |
| 8 | **Does the new `scope-artifact` role actually land?** | **FAIL** | Ran the **real** `verifyChangedArtifacts` (current-revision path) against my synthetic promotion carrying a `scope-artifact` row at the evidence prefix: **REFUSES** — `promotion artifact 0 (…/capsec-scope.json): target publication path must replace the closed source blob in place`. The role-conditional rule at `:969-974` sends everything that is not `conformance-evidence` into an else-branch requiring the path to pre-exist as a blob in the **source** tree, which an `evidencePrefix` path (embedding `admission.sourceRevision`) never can. M27 adds the role to `:724` and to `assertArtifactRolePath` `:784` but never dispositions `:969-974`. **New finding N1.** |
| 9 | **M33 gates every release path; caches correctly excluded?** | **PARTIAL** | The mechanism facts are exact: `select_embedded_report` returns `b"null\n"` on `authorized: false` at `build_support/portable_engine_promotion_report.rs:461-465` ✓ (exact); `.github/workflows/hermes-artifacts.yml:24-25` "Releases created here are ARTIFACT CACHES, not product releases" ✓, push trigger `:35-36` ✓; `require_checked_promotion` `src/host/portable_target_admission.rs:639` ✓. The diagnostic-cache carve-out is correct. **But the exclusion list is one path short**: `.github/workflows/portable-engine-physical-promotion.yml` checks out "exact source A" (`:1`, `:40`) — which under M27 (iv) **is the reset commit** — and "may upload candidate bytes" (`:3`). **New finding N4.** |
| 10 | **Pin 14 / coupled attestations correct and complete?** | **PASS (exact)** | Re-ran the recorded sweep verbatim → **9 lines / 7 files**, matching the row to the number (my own scratch file was the only extra and was removed; re-run confirms 9). Every cited join verified: `capsec-contract.mjs:3518-3520` reads the **HEAD** attestations via `readJsonStrict`; `:3686-3690` v1-validates them against `SCHEMA_IDS.targetAttestations` (`:132-133`); `capsec/schema/target-attestations.schema.json:8` pins `"const": "ibex/capsec-target-attestations/1"` ✓; the target-set equality throw "target advertisements are not the exact mechanically verified attestation set" is at `:4212-4229` ✓; promotion already emits v2 at `capsec-portable-promotion-bundle.mjs:1089-1099` ✓ (`"ibex/capsec-target-attestations/2"` at `:1090`); lineage `TARGET_ATTESTATION_PATH` `:47` ✓, source-foundation empty-v1 assertions `:816-820` ✓ exact, bundle-graph publication join `:867-928` ✓. The joint disposition is correct: **neither document's disposition is independently decidable**, and pin 14 says so. |
| 11 | **Fable N1's trap — does it compose at `mod.rs:1304`?** | **FAIL** | `src/host/mod.rs:1304` is `let decision = self.evaluate_typed_decision(` with the literal gate at `:1306-1309` ✓ exact — so the premise is right. But `evaluate_typed_decision` (`:3776`) is a single `pub fn` whose body is `self.evaluate_typed_decision_inner(set, gates, None)`; it **cannot distinguish** an internal caller from an embedder. A3's "passes through ingress 1 unmodified" (`llp/0021…:3828-3829`) is therefore incompatible with "recomputation applies at the four `pub` entry points" unless a flag is threaded — which A3 forbids two lines later. **New finding N5.** The needed structural door already exists and is unnamed: `fn evaluate_typed_decision_inner` (`:3784`) and `fn evaluate_typed_decision_inner_and_then` (`:3794`) are private. |
| 12 | **Is `hostDisposition: "extension-declared"` reachable?** | **FAIL** | A3 defines `ibex/capsec-scoped-refusal/1` as **refusal** telemetry whose emission condition is a hard `TargetCellIncomplete` (`llp/0021…:3845-3862`; `decision.rs:609-621`). An `extension-declared` gate carries `target_cell: Complete`, so it never produces `TargetCellIncomplete` and never emits the envelope. F3a-4's second half — "proceeds and its envelope records `hostDisposition: "extension-declared"`" (`:4259-4261`) — is unsatisfiable as written. **New finding N6.** |
| 13 | **Ingress/gate facts re-verified at HEAD** | **PASS (exact)** | Four `pub` ingresses at `src/host/mod.rs:3776`, `:3844`, `:3942`, `:3963` ✓; `fn target_cell` `:956-961` with `.get(edge).copied().unwrap_or(Incomplete)` ✓; three evaluation bodies / four `evaluate_decision_set*` sites at `:3811`, `:3820`, `:3857`, `:3888` in bodies `:3794`, `:3844`, `:3874` ✓; `select_v2_advertisement` tuple match `advertisement.target.triple == target && advertisement.target.features == features` at `src/host/portable_target_admission.rs:573` ✓. |
| 14 | **Floor and genesis preconditions at HEAD** | **PASS (exact)** | `git log -1 --format='%H %P' afad4af9` → `afad4af9f425… 3d778d2281d6e160… 0025553297f485f4…`; first parent `3d778d22` **lacks** the catalog path (`git cat-file -e` → fatal "exists on disk, but not in"), second parent `00255532` **has** it — the round-3 "**first** parent" correction is exactly right and load-bearing. Both whitespace forms of `git log --all -S'"enabled": true' -- <catalog>` → **0 commits**. `git rev-parse --is-shallow-repository` → `false`; no `.git/shallow`, no `.git/info/grafts`; `git for-each-ref refs/replace` → 0. Floor first-parent-reachable from HEAD at depth **410**. |
| 15 | **Package self-consistency** | **FAIL (documentary)** | The package's own status header (`llp/0021…:3485-3504`) and ledger (`:6344-6345`, `:6451-6452`) state the loop is **closed at three rounds** and "**no round-4 review exists and none may be run**" — which contradicts the author's 2026-08-06 lifting of the bound under which this round runs. **New finding N9.** |
| 16 | **Downstream deltas** | **PASS** | `llp/0032-…spec.md:803-829` and `llp/0036-…plan.md:414-450` are internally consistent with the round-3 amendment: 0032 correctly defers the restamp worklist to M30 and the rev-vs-evolve choice to A10 #6; 0036 correctly makes gate 2 scoped and marks its own row counts as snapshots. Neither claims anything the round-3 revision withdrew. |

---

## 2. Disposition of every round-3 finding

### Codex round 3 (1 IN-DELTA BLOCKER, 2 IN-DELTA MATERIAL, 1 PRE-EXISTING MATERIAL)

| Finding | Disposition | Exact section / row |
|---|---|---|
| **BLOCKER — "M27 authenticates a catalog declaration, not a historical promotion hop"** | **RESOLVED** | M27 (i)'s "Merge shape is not enough — the scope-critical artifact joins" (`llp/0021…:5313-5360`) and (iii)'s lift to every hop (`:5504-5512`). Verified by construction, not by reading: all three F6f variants that the round-2 predicate accepts are refused by the round-3 joins (check 2). The `admissionDigest`-attests conjunct is withdrawn outright (`:5578-5582`) and the residual restated as exactly two things (`:5583-5588`). Residual: N3, N7. |
| **MATERIAL — "M27 cannot identify a per-feature tuple"** | **PARTIAL** | Answered in substance — the tracked admission revs to `ibex/portable-engine-promotion-admission/2` with a new domain and a closed canonical `target {triple, features}` replacing `targetTriple` (`:5386-5435`), selection is on the full canonical tuple (`:5362-5374`), F6g fixtures it (`:4341-4347`), and *which* contract revs is stated rather than silent — which is precisely what Codex asked for. **Not fully resolved as to pricing:** the enumerated consumer list omits the lineage verification-result surface, without which the checked admission at `:1192` has no source of features (N2). |
| **MATERIAL — "reset revisions lack a mechanical non-release gate"** | **PARTIAL** | M33 exists (`:6026-6055`), asserts on the checked admission rather than the catalog for the right reason, and F6h fixtures both halves (`:4348-4360`). The per-SHA diagnostic-cache exclusion is correct and verified. **Not complete:** the exclusion list misses the physical-promotion candidate upload, which runs at the reset revision by construction (N4). |
| **PRE-EXISTING MATERIAL — "M18 pin 9 ignores the coupled target-attestation publication"** | **RESOLVED** | Pin 9's CORRECTION (`:5009-5036`) and new pin 14 (`:5097-5138`), plus A10 #2's corrected framing (`:6257-6271`). Sweep reproduces at 9/7 exactly; every join re-verified to the line (check 10). "No disposition here is independently decidable" is the correct statement of the coupling. |
| Codex's stated **preference** for the full version-dispatched check set | **Correctly recorded as a preference, with a sound reason** | `:5520-5557` and the ledger at `:6362-6379`. The liveness argument is verified sound against hard-pinned schema constants (check 4). The ledger's sentence "a reviewer who examined the code independently would have chosen the stronger option" is exactly the right disclosure. But see N3: the *reason given* over-claims. |

### Fable round 3 (3 MATERIAL + 5 MINOR)

| Finding | Disposition | Exact section / row |
|---|---|---|
| **N1 — ingress rule vs runtime-extension carve-out collide at `mod.rs:1304`** | **PARTIAL** | A3's "How the two rules compose" (`:3804-3844`) and M32's "Where each rule applies" (`:6000-6017`) do the important half: the forbidden repair ("absent ⇒ keep the caller's value") is named and prohibited in both places, so the **trap is sealed**. The remaining half is not: "passes through ingress 1 unmodified" is not implementable at a `pub fn` that cannot see its caller (N5). Fails closed either way. |
| **N2 — floor's parent clause false of the pinned object** | **RESOLVED (exactly)** | `:5704-5723` and F6a `:4296-4300`. Verified: `afad4af9` is a two-parent merge, first parent lacks the path, second parent carries byte-identical disabled/empty content. |
| **N3 — precondition closes truncation, not reconstruction** | **RESOLVED (exactly)** | `:5751-5789`, §A5 `:3975-3990`, A10 `:6114-6131`, F6e's narrowing `:4320-4324`, and the ledger's "the only round-3 finding that touches a security claim" (`:6418-6419`). The claim is now stated at its true width. This is the honesty correction the author must read, and it survives into this round unchanged. |
| **N4 — four M27 (i) pins off by one** | **RESOLVED** | `:5301-5306`. I re-verified `:1065`, `:1066`, `:1070`, `:1071`, `:1072`, `:1073` against the corrected text. Two *other* pins in the same delta are still off by one (N8). |
| **N5 — `admissionDigest` over-attributed** | **RESOLVED (exactly)** | `:5571-5588` withdraws the conjunct and names the two things that actually carry the residual. |
| **N6 — "the only in-repo caller" is three** | **RESOLVED (exactly)** | `:3722-3737` and `:5959-5966`, with all three sites and their gates named. |
| **N7 — pin 9's unpriced digest-contract → lineage dependency** | **RESOLVED (exactly)** | `:5037-5050` and A10 #2's closing paragraph `:6266-6271`: "the author is choosing that edge, not avoiding a restamp." |
| **N8 — pre-existing `CompleteAdvertised` ABI property** | **RESOLVED by filing** | `issues/20260806-abi-caller-supplied-targetcell-authoritative.md`, recorded at `:6208-6215` and `:6440-6449`. Correctly kept out of the amendment. |

---

## 3. New findings

### N1 — IN-DELTA — MATERIAL — `verifyChangedArtifacts` rejects the new `scope-artifact` role, and the obvious fix is a trap

M27 (i) adds a `scope-artifact` role to the closed role set at `scripts/portable-engine-promotion-lineage.mjs:724` and places it "at the fixed evidence-prefix path" (`llp/0021…:5344-5348`). It does not disposition the role-conditional rule at `:969-974`:

```js
if (artifact.role === "conformance-evidence") {
  assert(source === undefined, `${label}: conformance evidence must be newly added at promotion`);
} else {
  assert(source?.kind === "blob" && source.mode === "100644", `${label}: target publication path must replace the closed source blob in place`);
}
```

Anything that is not `conformance-evidence` must **pre-exist** in the source tree. An `evidencePrefix` path embeds `admission.sourceRevision` (`:762`), so it cannot. I ran the real function against a synthetic promotion carrying the row and it refuses: `promotion artifact 0 (…/capsec-scope.json): target publication path must replace the closed source blob in place`. This fires at the **current** revision, where the full `verifyChangedArtifacts` runs — so the very first scoped promotion is unverifiable.

**The trap:** the cheapest-looking repair is to give the scope artifact a fixed tracked path (like `TARGET_ATTESTATION_PATH`/`TARGET_ADVERTISEMENT_PATH`) so it satisfies the else-branch. That would make the scope artifact a third HEAD-tracked foundation document, dragging it into M18's pin-9/pin-14 coupling, `assertSourceAuthorityClosed`'s source-foundation clauses, and M27 (iv)'s reset lifecycle — a large, silent expansion of the blast radius the amendment spent two rounds measuring.

**Exact resolution (text-only):** in M27, state that `:970-974`'s newly-added predicate extends to the `scope-artifact` role (`role === "conformance-evidence" || role === SCOPE_ROLE`), and state explicitly that the scope artifact stays evidence-prefix-scoped and never becomes a tracked foundation document. Add it to M19's amendment sentence, which today says only "the scope artifact and companions join the closed member/role set" (`:5150-5152`).

### N2 — IN-DELTA — MATERIAL — the `targetTriple → target` consumer list omits the surface the features must travel through

M27 (ii) enumerates the consumer list "so the rev is priced, not discovered" (`llp/0021…:5437-5464`). `grep -n targetTriple scripts/portable-engine-promotion-lineage.mjs` returns 19 sites. The list names `:699`, `:710`, `:739-740`, `:762`, `:814`, `:836`, `:840`, `:1100`, `:1192`, and deliberately excludes the option surface `:1112-1128` and the installer `:476`. It **never mentions** `:1133-1169` — `validateLineageVerificationResult`, whose `assertExactKeys` closed list includes `"targetTriple"` at `:1152` and whose triple regex is at `:1161-1166` — nor `:1180`, `bindVerifiedPortableEnginePromotionAdmission`'s `lineage.targetTriple === selected.targetTriple` join.

This is not a cosmetic omission. The checked admission is constructed at `:1192` as `targetTriple: selected.targetTriple`, and `selected` comes from `exactCheckedSelectionOptions`, whose allowed keys are exactly `["expectedSourceRevision", "portableArtifactId", "repoRoot", "targetTriple"]` (`:1112`) — **no features** — and which M27 declares "deliberately unchanged" (`:5457-5460`). So the only path by which the canonical feature list can reach the checked admission is *tracked admission → verification result → checked admission*, and every site on that path is unenumerated. Without it, M27's runtime strengthening — `require_checked_promotion` asserting the checked admission's `target` equals the advertisement's `target` (`:5453-5455`; the current triple-only join is at `src/host/portable_target_admission.rs:705`) — is unimplementable, and M31's "this row's read becomes `lineage.target.triple` at `:476`" (`:5925-5931`) presupposes a result-surface rename the row never prices.

Two further precision defects in the same list: (a) `:1100` is labelled "the disabled-catalog result field," but the disabled branch returns `{schema, authorized:false, currentRevision, admission:null}` (`:1052-1060`) with no triple at all — `:1100` is the **authorized** result field; (b) the "Revised schema" paragraph (`:5261-5268`) says only two fields join the checked admission's closed set, keeping `targetTriple`, while the consumer list says the checked admission carries a `target` object — the two paragraphs disagree about the same artifact.

**Exact resolution (text-only):** add `:1092-1101` (result construction), `:1143-1157` (`assertExactKeys` closed list), `:1161-1166` (triple regex) and `:1180` to the consumer list; state that the canonical tuple travels tracked admission → verification result → checked admission while the *selection* option surface stays triple-only; reconcile the "Revised schema" paragraph to say the checked admission's `targetTriple` also becomes `target`; and relabel `:1100`.

### N3 — IN-DELTA — MATERIAL — the subset's version-independence claim is false of the subset's own load-bearing clause

M27 (iii) rejects the full check set because it "requires the HEAD bundle verifier to version-dispatch across **every** schema version ever published" (`llp/0021…:5530-5533`) — verified sound (check 4). It then justifies the subset: "They are all pure functions of content-hashed objects at R and at `A.sourceRevision`, so they carry **no version dependency on the HEAD verifier's schema constants** and re-run at any hop without obstruction" (`:5509-5512`).

That is false of the clause the whole BLOCKER fix rests on. "Recomputing the scope artifact's own digest from those bytes **under the `ibex:capsec:scope:1` canonical-form rule of §A1**" (`:5349-5351`) *is* a HEAD schema constant applied to a historical tree. The moment a scope artifact ever revs to `ibex/capsec-scope/2` with its own digest domain — which §A1 makes a versioned artifact precisely so it *can* rev — recomputing an old `/1` artifact requires exactly the version dispatch the paragraph rejected, on the one clause that cannot be dropped. Two lesser instances sit beside it: `assertArtifactRolePath` `:784` hard-codes `TARGET_ATTESTATION_PATH` (`:47`) and `TARGET_ADVERTISEMENT_PATH` (`:48`), and the fixed evidence-path templates at `:739-740` encode HEAD's path grammar — the same "HEAD-era constant applied to a historical tree" property the row concedes for `assertSourceAuthorityClosed` (`:5537-5541`) but not for its own clauses.

This does not make the fix wrong today (only `/1` exists) and it fails closed (a hop that cannot be recomputed is a verification failure, not a silent accept). What is wrong is the reason given for a decision the author is being asked to accept over a reviewer's stated preference for the stronger option.

**Exact resolution (text-only, plus one stated rule):** narrow the sentence to "no version dependency on the HEAD verifier's *artifact-schema validators*," and state normatively that HEAD must retain the canonical-form/digest-domain rule of **every** published `ibex/capsec-scope/N`, with the reason this dispatch is cheap where the bundle verifier's is not (one canonical-form rule vs. a whole evidence-graph validator). Say the same for the path templates.

### N4 — IN-DELTA — MATERIAL — M33's exclusion list is one path short, and the missing one makes promotion *n+1* circular

M33 gates "**Every** product release, tag, or publication path" on `authorized: true` with a non-null `admittedScopeDigest` and "**refuses** otherwise" (`llp/0021…:6042-6047`), and explicitly excludes only the per-SHA diagnostic caches, for the correct reason that "gating them would break the ceremony it is meant to protect" (`:5666-5669`).

The same reason applies, harder, to `.github/workflows/portable-engine-physical-promotion.yml`. Its header: "Trusted, same-runner macOS ceremony for **one exact portable-engine source A** … A successful run **may upload candidate bytes**, but only a separate reviewed one-commit promotion topic and ordinary PR merge can grant the checked admission" (`:1-5`). Under M27 (iv) step 3, source A **is** the reset commit — the revision at which `authorized: false` and `select_embedded_report` embeds `b"null\n"` (`build_support/portable_engine_promotion_report.rs:461-465`). So a literal reading of "every … publication path" gates the workflow that produces the candidate bundle *at exactly the revision where the gate must fail*, and promotion *n+1* becomes impossible: you cannot produce the artifacts that make the admission authorized until after the admission is authorized. F6h fixtures only one of the two halves that need excluding.

**Exact resolution (text-only):** name the gate's subject as *product releases of the binary* and extend M33's exclusion clause to cover every artifact-source-revision ceremony output — explicitly the physical-promotion candidate upload — with the same circularity reason; extend F6h (b) to assert the candidate ceremony still succeeds at the reset revision.

### N5 — IN-DELTA — MATERIAL — "passes through ingress 1 unmodified" is not implementable at a `pub fn`

A3's composition rule says recomputation fires "at the four `pub` entry points … and at the C symbol behind them," while the runtime-extension gate "is therefore Host-constructed and already resolved, and **passes through ingress 1 unmodified**," and implementations "must make this structural — a resolver function that is the only producer of a runtime-extension gate — **rather than a flag threaded into the ingress**" (`llp/0021…:3817-3832`; M32 repeats it at `:6004-6013`).

Verified at HEAD: `pub fn evaluate_typed_decision` (`src/host/mod.rs:3776`) is a three-line body — `self.evaluate_typed_decision_inner(set, gates, None)` — with no caller information available to it. A resolver being the *sole producer* of a gate does not tell `:3776` who called it. So either (a) recomputation is implemented inside `:3776` and the resolver's gate is recomputed too, in which case every non-inventory `effect_semantics` string refuses — the liveness break the paragraph exists to prevent — or (b) `:3776` must distinguish, which is the flag the paragraph forbids. "Passes through ingress 1 unmodified" cannot be true simultaneously with "recomputation applies at the four `pub` entry points."

The clean structural door exists and the amendment does not name it: `fn evaluate_typed_decision_inner` (`:3784`) and `fn evaluate_typed_decision_inner_and_then` (`:3794`) are **private**. The correct statement is that the resolver calls the private inner evaluator, i.e. `:1304` stops calling the `pub` ingress.

This fails closed, and A3's explicit prohibition on "absent ⇒ keep the caller's value" (`:3833-3844`) already seals the dangerous repair, so the round-2 BLOCKER cannot silently reopen through this route. But the composition text is the one item the ledger itself flags as "the one with a trap in it" (`:6427-6429`), and it should be exactly right.

**Exact resolution (text-only):** in A3 and M32, replace "passes through ingress 1 unmodified" with: the resolver hands its already-resolved gate to the **private** `evaluate_typed_decision_inner` (`src/host/mod.rs:3784`), not to the `pub` `evaluate_typed_decision` (`:3776`); `:1304` changes accordingly; recomputation lives in the four `pub` methods and the C symbol, which are then exactly the trust boundary and nothing else.

### N6 — IN-DELTA — MINOR — `hostDisposition: "extension-declared"` can never be emitted, and F3a-4's second half is unsatisfiable

A3 defines `ibex/capsec-scoped-refusal/1` as a **refusal** envelope whose emission condition is a hard `TargetCellIncomplete` decision (`llp/0021…:3845-3862`; `crates/capsec-semantics/src/decision.rs:609-621` refuses on `Incomplete` before any lifecycle result). An `extension-declared` call keeps `target_cell: TargetCellDisposition::Complete` (`:3796-3798`), which by construction never yields `TargetCellIncomplete`, so no envelope is emitted. F3a-4 nevertheless asserts "the same call with a non-inventory effect-semantics string **proceeds** and **its envelope** records `hostDisposition: "extension-declared"`" (`:4259-4261`). A proceeding call has no refusal envelope; the fixture cannot be written.

**Exact resolution (text-only):** either widen the envelope from refusal-only to a decision-annotation envelope emitted at the M13 funnel for every scope-governed decision (which also makes `extension-declared` observable, its actual purpose), or restate F3a-4's second half as "proceeds, emits **no** scoped-refusal envelope, and records `extension-declared` in the separate diagnostic record." Pick one; the current text implies both.

### N7 — IN-DELTA — MINOR — the joins bind the declaration to a blob but never to the hop's own published advertisement

M27 (i)'s five joins prove the declared `admittedScopeDigest` names a blob actually present in R's tree with matching bytes, and that the blob's tuple matches the admission's. They do not compare the scope artifact against the **published advertisement blob in the same tree** — which is itself in `A.artifacts`, path-pinned to `TARGET_ADVERTISEMENT_PATH` by `assertArtifactRolePath` (`:786-789`), already blob-verified by the per-row checks the subset adopts, and which per M17 carries `scopeDigest`. So a hop whose own published advertisement contradicts its admitted scope digest is accepted as the predecessor anchor.

I confirmed the residual empirically: my fourth variant — a fully self-consistent, attacker-authored narrow scope artifact — is accepted by the round-3 joins, as it must be. The advertisement cross-check does not close that (an attacker authoring the scope artifact authors the advertisement too), so this is not a security gap in the fabricated-repository threat model. It is a cheap internal-consistency join that catches partial tampering and honest divergence, in a hop set whose whole purpose is binding declarations to content. Its cost is one field read, and it carries the version dependency N3 already requires the design to accept.

**Exact resolution (text-only):** add a sixth (i) clause — the published advertisement blob's `scopeDigest` equals `A.admittedScopeDigest` — or state explicitly why the hop deliberately does not check it, so a future reader does not read the omission as an oversight.

### N8 — IN-DELTA — MINOR — two more one-off pins, in delta text claiming re-verification

M27 (i) cites "a `roleCounts` cardinality assert beside `:735-737`" (`llp/0021…:5346-5347`); the three asserts are at `:736`, `:737`, `:738` (`roleCounts.set` is `:734`). M27 (iii) cites "`verifyPromotionBundleGraph` `:995`, `fn` at `:867-929`" (`:5517`); the function is `:867-928` (`:929` is blank). The enclosing claims are substantively exact, and both subsections state re-verification at `fb343180` — the same class as Fable's round-3 N4, which the revision fixed for four other pins.

**Exact resolution:** `:736-738` and `:867-928`.

### N9 — IN-DELTA — MINOR — the package asserts that this review cannot exist

The status header (`llp/0021…:3485-3504`), the ledger header (`:6344-6345`) and its closing paragraph (`:6451-6452`) all state the loop is **closed at three rounds** with "**no round-4 review exists and none may be run**," and A10's round-3 settlement preamble (`:6150-6153`) rests on the same premise. The author lifted that bound on 2026-08-06 and authorized up to three further rounds. Leaving the assertion in place means the document that carries the split verdict to the author misdescribes its own review state, and the "UNREVIEWED final revision" framing — which is what the ledger asks the author to weigh — will be wrong the moment this round lands.

**Exact resolution (text-only):** record the lifted bound and the decision rule in the status header and the ledger's "What remains" paragraph, and replace "none may be run" with the current round budget. Keep everything else in the ledger as-is; it remains accurate.

---

## 4. Verdict

# READY

for the author's LLP 0044 §7 register-item-5 decision.

**The round-3 BLOCKER is genuinely closed, and I verified it by construction rather than by reading.** I built a real SHA-1 repository containing a correctly shaped two-parent promotion merge — right topology, canonical catalog, `validateAdmissionShape`-clean admission with a recomputed self-consistent `admissionDigest` — and ran the actual verifier primitives under both clause sets. Every one of F6f's three variants is accepted by the round-2 predicate and refused by the round-3 joins, with the refusal arriving from a different clause in each case (role cardinality, changed-path-set equality, blob-id equality, digest recomputation). F6f is a regression test, not a restatement of the fix. The residual — a hop whose scope artifact is attacker-authored — is real, is inherent, and is stated by the revision at exactly its true width: first-parent reachability in reviewed history plus the joins, and nothing else (`:5583-5588`). The withdrawal of the `admissionDigest`-attests conjunct is the right call and is complete.

**The subset-vs-full-set choice is sound and the subset is a genuine floor.** I confirmed the liveness argument against hard-pinned schema constants that do not dispatch (`capsec-portable-engine-evidence-contract.mjs:46`, `:48-49`, `:50-51`), reached through `verifyPromotionBundleGraph` (`:995`) → `validatePortablePromotionBundleGraph`. Codex's preferred stronger option really would break at every hop after any artifact schema rev. The ledger's disclosure that an independent reviewer would have chosen the stronger option is the honest way to carry a preference disagreement to an author, and it should stay.

**Nothing I found is blocking.** Five MATERIAL and four MINOR, and every one of them fails closed:

- N1 refuses the promotion (the ceremony stops), N4 refuses the ceremony (promotion *n+1* stalls), N2 refuses on `assertExactKeys` the first time an implementer adds the field, N5 refuses every non-inventory extension operation, N3 refuses a hop it cannot recompute. None admits an unsound certification, none produces a scoped advertisement that overstates its scope, none reopens a decided register item, and none changes the answer to the question the author is being asked — Option B, `ScopedAdvertised`, and the retained aggregate as sole introspection authority, all of which I re-verified are supported by the real code at HEAD.
- Eight of the nine resolutions are text-only. N1's is one boolean clause in `verifyChangedArtifacts`.
- The one item with a genuine trap in it, N5, has its trap already sealed: A3 and M32 both explicitly forbid "absent from the aggregate ⇒ keep the caller's value" at ingress 1 (`:3833-3844`, `:6014-6017`), which is the repair that would silently reopen the round-2 ABI BLOCKER. What remains is a text contradiction with an obvious, already-present structural answer.

Two of Codex's three round-3 answers are **PARTIAL on pricing completeness, not on substance** (N2 for tuple identity, N4 for the reset gate). That distinction matters for the author: the design decisions are right, the enumerations that make them implementable are one surface short each — which is the same failure mode M11 and M18 were corrected for across three rounds, now appearing in the newly written round-3 text. It is worth saying plainly that this row's enumerations have needed a correction in every round so far, and needed two more here.

**For the author (each must reach them):**
- **Fable round-3 N3 survives unchanged and is still the only finding touching a security claim.** The genesis preconditions close history *truncation*; they do not close *reconstruction* on the pinned floor. I re-ran all three assertions at HEAD and they pass; I also confirmed the reconstruction lane costs one `git commit` and rewrites no published object. The narrowed claim in `:5751-5789` is correct and should be read before accepting.
- **N3 (mine)** — the reason given for preferring the subset over the stronger option is over-stated in a way that touches the BLOCKER fix itself. The decision is still right; the justification needs one narrowing sentence and one normative rule about retaining every published scope-artifact canonical-form rule.
- **N9** — the package currently tells the author this review cannot exist.

**For the implementer (correct in place, before or during gate code):**
- **N1** — extend `verifyChangedArtifacts:970-974`'s newly-added predicate to the `scope-artifact` role, and do **not** solve it by giving the scope artifact a tracked foundation path.
- **N2** — add `:1092-1101`, `:1143-1157`, `:1161-1166`, `:1180` to M27 (ii)'s consumer list; relabel `:1100`; reconcile the checked-admission "Revised schema" paragraph with the consumer list.
- **N4** — extend M33's exclusion to the artifact-source-revision ceremony outputs, F6h (b) with them.
- **N5** — the runtime-extension resolver hands its gate to the private `evaluate_typed_decision_inner` (`:3784`), not to `pub evaluate_typed_decision` (`:3776`).
- **N6, N7, N8** — one envelope-reachability fix, one optional sixth join (or a stated reason for its absence), two pins.
