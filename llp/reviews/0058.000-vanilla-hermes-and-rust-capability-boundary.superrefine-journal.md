# Super-refine journal — LLP 0058.000

**Authorization:** On 2026-08-27, Charlie Cheever invoked
`$llp-super-refine` for the newly authored LLP 0058.000 and requested exactly
one round. The invocation is recorded as author authorization for review and
revision and as repository-disclosure authorization for the external sends
described below. The target moved `Draft` → `Review` at loop start; the skill
will not move it to `Accepted` or beyond.

**Topology:** Single-target, one normal RFC-altitude round. Fresh mutually
blind reviewers: OpenAI Codex `gpt-5.6-sol` at `ultra` in a read-only sandbox,
and xAI `grok-4.6` at `xhigh` in headless mode with a full terminal/tool/network
ban. Reviser after both reviews: fresh Claude `claude-fable-5` at `xhigh`.
Round budget: 1. Launch cap: 2 per reviewer family; one launch per family is
planned.

**Outbound inventory:** Exact unredacted target bytes; governing LLPs 0057,
0058, and 0039; `patches/hermes/README.md`; and current implementation evidence
from `crates/stdlib/src/boundary.rs`, `src/stdlib_adapter.rs`,
`src/engine/hermes_runtime_timers.cc`, and
`src/engine/hermes_runtime_stdlib.cc`. No review artifact, run journal,
or orchestrator note is present in the capsule. No secret or credential file is
included.

## Round 1 — pre-launch record

- Date: 2026-08-27
- Repository base: `4db349e6e2407cd46e41ba1f949d55351d832f93`
- Target SHA-256: `1b6d10a6d684370de08446907dd1c672e5bd9c54a78a5baf6390ed5b864de075`
- Capsule SHA-256: `83d34c96b7d332a1c01f0cd5b7dcffc0a4f3ef9373592c474e1c03ea046121aa`
- Shared instruction SHA-256: `2b09b0c52481396054e38b8e84bbbce9525b7230f8a6021e7fdc26f4a6d0d3c8`
- Reviewer-visible inlined prompt SHA-256: `c2c580e8850b2cdf389d53db2a45e290c8b6f335872610869b7632ebb95cfe20`
- Capsule size: 121,754 bytes; inlined prompt size: 124,218 bytes
- Blindness: structural. The capsule is an out-of-repository export containing
  only the disclosed files. Both reviewers receive the same inlined bytes and
  are forbidden from using tools or reading the live checkout.
- Redacted: no. Target bytes are complete.
- Codex runner: `codex-cli 0.149.1`, `codex exec --ephemeral -m gpt-5.6-sol
  -c model_reasoning_effort=ultra -s read-only`.
- Grok runner: `grok 1.0.5`, `grok --prompt-file ... -m grok-4.6
  --reasoning-effort xhigh --permission-mode dontAsk --no-plan --no-subagents
  --disable-web-search --output-format streaming-json`; full terminal ban is in
  the shared prompt.
- Reviser-never-reviews: the orchestrator authored the entering revision;
  neither reviewer may revise it. A fresh Claude Fable session will revise
  only after both verbatim artifacts have landed.

### Launch ledger

| Family | Launch | Status |
|---|---:|---|
| Codex | 1/2 | receipt stored verbatim; `VERDICT: NOT READY` |
| Grok | 1/2 | receipt stored verbatim; `VERDICT: NOT READY` |

Both launches completed successfully. No retry was used. The two verdicts bind
the entering target SHA-256 recorded above; neither reviewer saw the other
review or any later revision.

## Round 1 close-out

- Codex verdict: `NOT READY` on target SHA-256
  `1b6d10a6d684370de08446907dd1c672e5bd9c54a78a5baf6390ed5b864de075`.
- Grok verdict: `NOT READY` on the same target SHA-256.
- Review rounds run: 1 of the author-requested 1. No convergence claim.
- Each family artifact preserves the received body verbatim and appends a
  separate orchestrator disposition ledger.
- Revision input sent to the fresh Claude Fable 5 `xhigh` reviser: the exact
  entering RFC plus both stored verbatim reviews. No other repository file,
  journal, credential, or secret was sent.
- The reviser's first returned document body was 32,183 bytes despite an
  internal 27,860-byte claim. A same-role correction send contained only that
  candidate plus growth/format instructions; it returned 30,336 bytes. Neither
  result was applied as returned.
- The orchestrator removed repetition without changing the review-driven
  decisions. Applied RFC size: 27,913 bytes versus 23,262 entering bytes
  (`+19.9940%`, within the approximately 20% growth budget).
- Applied later RFC SHA-256:
  `b6922771b32692499ed37a0a29a1198b064579fc9045247908a505524ccbba52`.
  It remains `Status: Review` and is **unreviewed** because no second round was
  authorized or requested.
- A derived Draft child Spec was created at
  `llp/0058.000.000-rust-capability-context-and-engine-adapter.spec.md`,
  SHA-256
  `d763d7293ba2e092ebdd23fdbc0497c116c46a816592d5e8670eab7df2185f75`.
  It was authored after the review round and has not been reviewed by either
  family.

### Terminal disposition

The bounded run stops after one completed round as requested. LLP 0058.000
stays in `Review`; the child Spec starts in `Draft`. The proposed next step is
a delta or normal second round over the revised RFC, followed by independent
review of the child Spec, if the author wants readiness evidence.

## Lockstep invocation 2 — pre-launch record

**Authorization:** On 2026-08-27, Charlie Cheever invoked
`$llp-super-refine` on `0058*`, explicitly including the RFC and Spec, and
stated that full convergence was not required because the goal is to surface
major issues. The literal glob resolves to LLP 0058, LLP 0058.000, and LLP
0058.000.000. This invocation authorizes one bounded lockstep review round,
review-driven revisions within the growth budgets, and the disclosed external
sends below. LLP 0058 and LLP 0058.000.000 moved `Draft` → `Review`; LLP
0058.000 was already `Review`. The skill will not set `Accepted` or beyond.

**Topology:** The three targets form one tightly coupled cluster. Each fresh
reviewer receives all three exact targets and returns per-target plus set-level
verdicts. The complete cluster review is preserved once per family in LLP
0058.000's family artifact; sibling artifacts carry their own target record,
verdict, and cross-reference. Round budget: 1 for this invocation. Launch cap:
2 per family for this round; one launch per family is planned.

**Reviewers:** OpenAI Codex `gpt-5.6-sol` at `ultra` in an ephemeral read-only
sandbox, and xAI `grok-4.6` at `xhigh` in headless mode. Both receive the same
inlined prompt. The Grok prompt carries a full terminal/tool/network/subagent
ban. The orchestrator is the reviser and will not review any later revision.

**Outbound inventory:** Exact unredacted bytes for LLP 0058, LLP 0058.000,
and LLP 0058.000.000; full LLP 0057, Accepted LLP 0039, LLP 0059.000, and the
Hermes patch inventory; selected, labeled source excerpts from LLP 0013 lines
193–230, 415–508, 559–600, and 724–731, LLP 0040 lines 201–529, and LLP 0052
lines 11–70 and 558–570; plus current implementation evidence from
`crates/stdlib/src/{value,handles,boundary}.rs` and
`src/engine/hermes_runtime_{timers,stdlib}.cc`. No review artifact, journal,
orchestrator note, credential, secret, or unrelated repository file is in the
capsule.

### Lockstep round hashes and runners

- Date: 2026-08-27
- Repository base: `4db349e6e2407cd46e41ba1f949d55351d832f93`
- LLP 0058 SHA-256:
  `4fd9af71d86bd53632deb63a7bff32a3c4615d4a1ff4aa358b9ecdab5e9825b2`
- LLP 0058.000 SHA-256:
  `b6922771b32692499ed37a0a29a1198b064579fc9045247908a505524ccbba52`
- LLP 0058.000.000 SHA-256:
  `53d2092f33f42a27c089087fec398a2300adadf4a3063ec188d08443f8175605`
- Canonical target-vector SHA-256:
  `9d402471e998dbf6d76e571f88c9a61f803941569f98d6f33606c50fdee5caeb`
- Capsule-manifest SHA-256:
  `306b58d9e5434258077c49526c56d52516c8e7dcaaddc6817e4f4840c0ca656b`
- Shared instruction SHA-256:
  `d879d884f66eec5a624c68d36ee3ead7886d5b077c6f750dcd30fe42c4922ca0`
- Reviewer-visible prompt SHA-256:
  `a3275d97992923ac2f375256c437a148a51f38f913fc3d5beda35f04dc9b0abd`
- Capsule file bytes: 213,418; prompt bytes: 219,816
- Blindness: structural. The out-of-repository capsule contains only the
  disclosed files; review artifacts and this journal are absent.
- Redacted: no target bytes are redacted. Governing context is deliberately
  scoped through the disclosed excerpts.
- Codex runner: `codex-cli 0.150.1`, `codex exec --ephemeral -m gpt-5.6-sol
  -c model_reasoning_effort=ultra -s read-only`.
- Grok runner: `grok 1.0.5`, `grok --prompt-file ... -m grok-4.6
  --reasoning-effort xhigh --permission-mode dontAsk --no-plan --no-subagents
  --disable-web-search --output-format streaming-json`.

### Invocation 2 launch ledger

| Family | Launch | Status before launch |
|---|---:|---|
| Codex | 1/2 | receipt stored verbatim; set `NOT READY` |
| Grok | 1/2 | receipt stored verbatim; set `NOT READY` |

Both launches completed successfully; no retry was used. Codex returned
`NOT READY` for all three targets and the set. Grok returned `READY` for LLP
0058, `NOT READY` for LLP 0058.000 and LLP 0058.000.000, and `NOT READY` for
the set. These verdicts bind only to the target vector and prompt hashes above.
Neither reviewer saw the other review or any later revision.

## Lockstep invocation 2 — close-out

- Review rounds run: 1 of the one-round bounded major-issue pass authorized by
  the author.
- Launches: one successful launch per family; no retry or voided round.
- Codex verdicts on the entering vector: LLP 0058 `NOT READY`, LLP 0058.000
  `NOT READY`, LLP 0058.000.000 `NOT READY`, set `NOT READY`.
- Grok verdicts on the same vector: LLP 0058 `READY`, LLP 0058.000 `NOT READY`,
  LLP 0058.000.000 `NOT READY`, set `NOT READY`.
- Both family reviews are preserved verbatim in the primary family artifacts;
  sibling artifacts contain per-target verdicts and cross-references.
- Every material and non-blocking concern has an orchestrator disposition in
  its family artifact. No concern was treated as an acceptance vote.

The reviewers converged on major gaps in five areas: the engine/Rust job-queue
handoff, the binding-aware module artifact ABI, the exact vanilla threat
claim, deterministic stock-public lockdown, and cross-LLP posture/deputy/
extension/API conflicts. The Spec additionally needed linearized policy,
revocation, completion, coercion, lifetime, and inventory-completeness rules.

One bounded lockstep revision was applied after both receipts landed:

- LLP 0058 grew from 6,164 to 6,844 bytes (`+11.0318%`) and now closes the job
  queue question at RFC altitude, points to the Spec state machine, requires a
  receipt-bound factory seam, and gates stock-public feasibility.
- LLP 0058.000 grew from 27,913 to 31,328 bytes (`+12.2344%`) and now defines
  the adversary precisely, maps LLP 0039/0047 postures, assigns deputy/audit
  and extensions to tiers, records the LLP 0059.000 delta, and makes owning
  amendments plus packaging/probe evidence flip gates.
- LLP 0058.000.000 grew from 26,516 to 31,783 bytes (`+19.8635%`) and now
  defines `FactoryV1`, deterministic lockdown, conversion classes,
  exact-generation policy invalidation, a completion lease, race
  linearization, a transactional single-FIFO drive state machine, teardown,
  bounded derivation, and a closed-world inventory schema.

The later document hashes are:

- LLP 0058:
  `aec56e544997f096ebe61b94abf253fbc210c094c41e5ed4288bce4cb63bf02c`
- LLP 0058.000:
  `9080d05802e16fa84505144cb7145017921665aac35369ca0f51d0b5ee87a25b`
- LLP 0058.000.000:
  `d6a2f4fa37c3f9987ce2a2d2d5960d6af5e0c518f891603600f01fed5daeed64`

These hashes are **unreviewed**. Actual stock-Hermes feasibility evidence and
accepted amendments to LLP 0013/0039/0040/0059.000 remain graduation gates;
this revision records them but does not fabricate their completion. All three
targets remain `Status: Review`. The invocation stops here as requested,
without dual-family convergence, a delta round, or an acceptance transition.

### Validation

- `git diff --check` passed; the three targets, journal, and sibling artifacts
  have no trailing whitespace. Whitespace inside the primary verbatim review
  bodies remains receipt-owned.
- The Codex and Grok cluster bodies compare byte-for-byte with their raw
  receipts (the Codex runner omitted a terminal newline; the artifact's
  Markdown separator supplies only that delimiter).
- All three `llp/current/` links resolve to their target documents.
- `./ref-check` inspected 62 LLP documents and 2,688 refs. It reported only the
  same two pre-existing workspace errors: bare cross-repo `LLP 0514` in
  `patches/hermes/0015-empty-disabled-hermes-internal.patch`, and a malformed
  bare `LLP` target in `src/engine/hermes_runtime.cc`. No 0058-target or new
  review reference failed.
- No product tests were run; this invocation changed LLP/review documents only.

## Post-loop author-directed greenfield revision — 2026-08-27

After the bounded super-refine invocation was fully closed, Charlie Cheever
asked to specify a mostly-from-scratch Ibex 2 path and suggested superseding the
last LLP. Orientation found that LLP 0058.000.000's security protocol remains
load-bearing; only the incremental-in-place implementation strategy was stale.

Draft LLP 0058.000.001, “Greenfield Ibex 2 Runtime Kernel,” therefore owns the
new composition/dependency topology, reuse boundary, isolated upstream build,
vertical slice, work gates, no-hybrid invariant, and artifact-level cutover.
It explicitly supersedes only the old transition strategy in LLP 0057 §5.1,
LLP 0058.000 §6, and LLP 0058.000.000 §12. Those sections were revised to point
to the new Spec; the earlier documents remain current for architecture and
protocol.

Post-loop unreviewed hashes:

- LLP 0057:
  `c03bc0bab028624097bd4144adebab31fb9ac727f571fd7492c5c76816cd5436`
- LLP 0058.000:
  `3bd53ed07216a4dd60c820cf80f6fd04782fb5052a5d98d2fb5f69bc254bf6fa`
- LLP 0058.000.000:
  `9c0a5c24425470f49c617f41cd465c44e024518540e6bb218d23b0975f1445a1`
- new LLP 0058.000.001:
  `1e9703f8e73736df82cba51ddc3dd11b4788c0946c57cfbb72a0a42dfb78ab39`

This is a separate author-directed corpus revision, not another review round.
No reviewer saw these bytes, and no earlier verdict or convergence claim
applies to them. `./ref-check` found no new 0058-family error; it still reports
only the two pre-existing repository errors recorded above.
