{
  "cap": "network:fetch",
  "resource": {
    "kind": "fetch-endpoint",
    "schemes": ["https"],
    "host": { "kind": "dns-name", "name": "127.1" },
    "port": { "kind": "exact", "value": 443 },
    "peerClasses": ["loopback"],
    "route": { "kind": "direct" }
  }
}
