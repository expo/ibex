{
  "schema": "ibex/module-loader-performance-platform-matrix/1",
  "authority": "LLP 0001",
  "targets": [
    {
      "id": "macos-arm64",
      "runner": "macos-14",
      "runtimeEvidence": "tests/fixtures/module-semantics/performance-macos-arm64.json",
      "collectionWorkflow": ".github/workflows/module-loader-baselines.yml",
      "status": "provisional",
      "note": "Checked runtime samples exist, but the current report is explicitly host-contended and unusable for an accepted budget."
    },
    {
      "id": "linux-x64",
      "runner": "ubuntu-latest",
      "runtimeEvidence": null,
      "collectionWorkflow": ".github/workflows/module-loader-baselines.yml",
      "status": "pending-ci",
      "note": "The repository publishes a patched Linux Hermes bundle; the workflow collects the native row without fallback or zero-case success."
    },
    {
      "id": "windows-x64",
      "runner": "windows-2025",
      "runtimeEvidence": null,
      "collectionWorkflow": ".github/workflows/module-loader-baselines.yml",
      "status": "pending-ci",
      "note": "The hosted workflow now installs the pinned Windows Hermes package and collects both compatibility-loader and authenticated native-runner metrics; the row remains pending until that artifact is received."
    }
  ]
}
