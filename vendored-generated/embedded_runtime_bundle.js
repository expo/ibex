(function() {
	//#region \0rolldown/runtime.js
	var __defProp = Object.defineProperty;
	var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames = Object.getOwnPropertyNames;
	var __hasOwnProp = Object.prototype.hasOwnProperty;
	var __esmMin = (fn, res) => () => (fn && (res = fn(fn = 0)), res);
	var __exportAll = (all, no_symbols) => {
		let target = {};
		for (var name in all) __defProp(target, name, {
			get: all[name],
			enumerable: true
		});
		if (!no_symbols) __defProp(target, Symbol.toStringTag, { value: "Module" });
		return target;
	};
	var __copyProps = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
			key = keys[i];
			if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
				get: ((k) => from[k]).bind(null, key),
				enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
			});
		}
		return to;
	};
	var __toCommonJS = (mod) => __hasOwnProp.call(mod, "module.exports") ? mod["module.exports"] : __copyProps(__defProp({}, "__esModule", { value: true }), mod);
	//#endregion
	//#region ../ibex-runtime-js/src/native/NativeModules.ts
	let _timerModule = null;
	let _performanceModule = null;
	let _cryptoModule = null;
	let _encodingModule = null;
	let _consoleModule = null;
	let _fetchModule = null;
	let _webSocketModule = null;
	let _storageModule = null;
	let _fileSystemModule = null;
	function setNativeCryptoModule(module) {
		_cryptoModule = module;
	}
	function getNativeCryptoModule() {
		return _cryptoModule;
	}
	function setNativeWebSocketModule(module) {
		_webSocketModule = module;
	}
	function setNativeFileSystemModule(module) {
		_fileSystemModule = module;
	}
	let _schedulingModule = null;
	/**
	* Type-safe getter for native modules by name.
	* Returns null if the module is not available.
	* 
	* @example
	* const crypto = getNativeModule('crypto');
	* if (crypto) {
	*   crypto.getRandomValues(new Uint8Array(16)); // Type-safe!
	* }
	*/
	function getNativeModule(name) {
		switch (name) {
			case "timer": return _timerModule;
			case "performance": return _performanceModule;
			case "crypto": return _cryptoModule;
			case "encoding": return _encodingModule;
			case "console": return _consoleModule;
			case "fetch": return _fetchModule;
			case "websocket": return _webSocketModule;
			case "storage": return _storageModule;
			case "filesystem": return _fileSystemModule;
			case "scheduling": return _schedulingModule;
			default: return null;
		}
	}
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/typeof.js
	function _typeof(o) {
		"@babel/helpers - typeof";
		return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
			return typeof o;
		} : function(o) {
			return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
		}, _typeof(o);
	}
	var init_typeof = __esmMin((() => {}));
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/toPrimitive.js
	function toPrimitive(t, r) {
		if ("object" != _typeof(t) || !t) return t;
		var e = t[Symbol.toPrimitive];
		if (void 0 !== e) {
			var i = e.call(t, r || "default");
			if ("object" != _typeof(i)) return i;
			throw new TypeError("@@toPrimitive must return a primitive value.");
		}
		return ("string" === r ? String : Number)(t);
	}
	var init_toPrimitive = __esmMin((() => {
		init_typeof();
	}));
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/toPropertyKey.js
	function toPropertyKey(t) {
		var i = toPrimitive(t, "string");
		return "symbol" == _typeof(i) ? i : i + "";
	}
	var init_toPropertyKey = __esmMin((() => {
		init_typeof();
		init_toPrimitive();
	}));
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/defineProperty.js
	function _defineProperty(e, r, t) {
		return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
			value: t,
			enumerable: !0,
			configurable: !0,
			writable: !0
		}) : e[r] = t, e;
	}
	var init_defineProperty = __esmMin((() => {
		init_toPropertyKey();
	}));
	//#endregion
	//#region ../ibex-runtime-js/src/events/DOMException.ts
	init_defineProperty();
	/**
	* DOMException - Web Standard DOMException Implementation
	*
	* @see https://webidl.spec.whatwg.org/#dfn-DOMException
	*/
	const legacyErrorCodes = {
		IndexSizeError: 1,
		DOMStringSizeError: 2,
		HierarchyRequestError: 3,
		WrongDocumentError: 4,
		InvalidCharacterError: 5,
		NoDataAllowedError: 6,
		NoModificationAllowedError: 7,
		NotFoundError: 8,
		NotSupportedError: 9,
		InUseAttributeError: 10,
		InvalidStateError: 11,
		SyntaxError: 12,
		InvalidModificationError: 13,
		NamespaceError: 14,
		InvalidAccessError: 15,
		ValidationError: 16,
		TypeMismatchError: 17,
		SecurityError: 18,
		NetworkError: 19,
		AbortError: 20,
		URLMismatchError: 21,
		QuotaExceededError: 22,
		TimeoutError: 23,
		InvalidNodeTypeError: 24,
		DataCloneError: 25,
		NotAllowedError: 0,
		OperationError: 0,
		NotReadableError: 0,
		UnknownError: 0,
		ConstraintError: 0,
		TransactionInactiveError: 0,
		ReadOnlyError: 0,
		VersionError: 0
	};
	var DOMException$6 = class DOMException$6 {
		constructor(message, name) {
			_defineProperty(this, "name", void 0);
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "code", void 0);
			_defineProperty(this, "stack", void 0);
			this.message = message ?? "";
			this.name = name ?? "Error";
			this.code = legacyErrorCodes[this.name] ?? 0;
			if (typeof Error.captureStackTrace === "function") Error.captureStackTrace(this, DOMException$6);
			else this.stack = new Error(this.message).stack;
		}
		toString() {
			return `${this.name}: ${this.message}`;
		}
	};
	Object.setPrototypeOf(DOMException$6.prototype, Error.prototype);
	Object.setPrototypeOf(DOMException$6, Error);
	Object.defineProperties(DOMException$6, {
		INDEX_SIZE_ERR: {
			value: 1,
			enumerable: true,
			configurable: true
		},
		DOMSTRING_SIZE_ERR: {
			value: 2,
			enumerable: true,
			configurable: true
		},
		HIERARCHY_REQUEST_ERR: {
			value: 3,
			enumerable: true,
			configurable: true
		},
		WRONG_DOCUMENT_ERR: {
			value: 4,
			enumerable: true,
			configurable: true
		},
		INVALID_CHARACTER_ERR: {
			value: 5,
			enumerable: true,
			configurable: true
		},
		NO_DATA_ALLOWED_ERR: {
			value: 6,
			enumerable: true,
			configurable: true
		},
		NO_MODIFICATION_ALLOWED_ERR: {
			value: 7,
			enumerable: true,
			configurable: true
		},
		NOT_FOUND_ERR: {
			value: 8,
			enumerable: true,
			configurable: true
		},
		NOT_SUPPORTED_ERR: {
			value: 9,
			enumerable: true,
			configurable: true
		},
		INUSE_ATTRIBUTE_ERR: {
			value: 10,
			enumerable: true,
			configurable: true
		},
		INVALID_STATE_ERR: {
			value: 11,
			enumerable: true,
			configurable: true
		},
		SYNTAX_ERR: {
			value: 12,
			enumerable: true,
			configurable: true
		},
		INVALID_MODIFICATION_ERR: {
			value: 13,
			enumerable: true,
			configurable: true
		},
		NAMESPACE_ERR: {
			value: 14,
			enumerable: true,
			configurable: true
		},
		INVALID_ACCESS_ERR: {
			value: 15,
			enumerable: true,
			configurable: true
		},
		VALIDATION_ERR: {
			value: 16,
			enumerable: true,
			configurable: true
		},
		TYPE_MISMATCH_ERR: {
			value: 17,
			enumerable: true,
			configurable: true
		},
		SECURITY_ERR: {
			value: 18,
			enumerable: true,
			configurable: true
		},
		NETWORK_ERR: {
			value: 19,
			enumerable: true,
			configurable: true
		},
		ABORT_ERR: {
			value: 20,
			enumerable: true,
			configurable: true
		},
		URL_MISMATCH_ERR: {
			value: 21,
			enumerable: true,
			configurable: true
		},
		QUOTA_EXCEEDED_ERR: {
			value: 22,
			enumerable: true,
			configurable: true
		},
		TIMEOUT_ERR: {
			value: 23,
			enumerable: true,
			configurable: true
		},
		INVALID_NODE_TYPE_ERR: {
			value: 24,
			enumerable: true,
			configurable: true
		},
		DATA_CLONE_ERR: {
			value: 25,
			enumerable: true,
			configurable: true
		}
	});
	Object.defineProperty(DOMException$6, "name", {
		value: "DOMException",
		configurable: true
	});
	Object.defineProperty(DOMException$6.prototype, Symbol.toStringTag, {
		value: "DOMException",
		configurable: true
	});
	function createGlobalDOMException(message, name) {
		return new (typeof globalThis === "object" && globalThis !== null && typeof globalThis.DOMException === "function" ? globalThis.DOMException : DOMException$6)(message, name);
	}
	function createQuotaExceededError(message = "The quota has been exceeded.") {
		return createGlobalDOMException(message, "QuotaExceededError");
	}
	function createDataCloneError(message = "The object could not be cloned.") {
		return createGlobalDOMException(message, "DataCloneError");
	}
	function createInvalidStateError$1(message = "The object is in an invalid state.") {
		return createGlobalDOMException(message, "InvalidStateError");
	}
	/**
	* Error thrown when a capability check fails.
	* Extends DOMException with additional context for capability denials.
	* 
	* Designed for forward compatibility:
	* - Use `reasonCategory` for switch statements (handles unknown reasons)
	* - Use `denialReason` for logging/debugging (may contain future values)
	* - Check `canRequestAgain` to know if prompting user is possible
	* 
	* @example
	* ```ts
	* try {
	*   requireCapability('device:location');
	* } catch (e) {
	*   if (e instanceof CapabilityDeniedError) {
	*     switch (e.reasonCategory) {
	*       case 'user_action_required':
	*         showPermissionPrompt(e.settingsUrl);
	*         break;
	*       case 'permanently_unavailable':
	*         hideLocationFeature();
	*         break;
	*       default:
	*         showGenericError(e.getUserMessage());
	*     }
	*   }
	* }
	* ```
	* 
	* @see JS_CAPABILITY_SECURITY_MODEL_SPEC_AND_PLAN.md
	*/
	var CapabilityDeniedError = class CapabilityDeniedError extends DOMException$6 {
		constructor(capability, reason = "module_not_granted", canRequestAgain = false, settingsUrl, metadata) {
			super(`Capability denied: ${capability}`, "NotAllowedError");
			_defineProperty(this, "capability", void 0);
			_defineProperty(this, "denialReason", void 0);
			_defineProperty(this, "reasonCategory", void 0);
			_defineProperty(this, "canRequestAgain", void 0);
			_defineProperty(this, "settingsUrl", void 0);
			_defineProperty(this, "metadata", void 0);
			this.capability = capability;
			this.denialReason = reason;
			this.reasonCategory = this._categorizeReason(reason);
			this.canRequestAgain = canRequestAgain;
			this.settingsUrl = settingsUrl;
			this.metadata = metadata;
			if (Error.captureStackTrace) Error.captureStackTrace(this, CapabilityDeniedError);
		}
		/**
		* Categorize a denial reason for forward-compatible handling.
		*/
		_categorizeReason(reason) {
			switch (reason) {
				case "os_denied":
				case "requires_settings": return "user_action_required";
				case "os_restricted": return "system_restricted";
				case "broker_denied":
				case "module_not_granted":
				case "app_not_granted": return "not_configured";
				case "temporarily_unavailable": return "temporarily_unavailable";
				case "capability_unknown":
				case "os_version_too_low": return "permanently_unavailable";
				default: return "unknown";
			}
		}
		/**
		* Returns a user-friendly message based on the denial reason.
		* Handles unknown reasons gracefully.
		*/
		getUserMessage() {
			switch (this.denialReason) {
				case "os_denied": return `Permission for ${this.capability} was denied. ${this.canRequestAgain ? "You can request access again." : "Please enable it in system settings."}`;
				case "os_restricted": return `Access to ${this.capability} is restricted by system policy.`;
				case "broker_denied": return `The application does not have permission for ${this.capability}.`;
				case "module_not_granted": return `This module does not have the ${this.capability} capability.`;
				case "app_not_granted": return `This application does not have the ${this.capability} capability.`;
				case "requires_settings": return `Please enable ${this.capability} in Settings.`;
				case "capability_unknown": return `The capability ${this.capability} is not available on this device.`;
				case "os_version_too_low": return `The capability ${this.capability} requires a newer operating system version.`;
				case "temporarily_unavailable": return `The capability ${this.capability} is temporarily unavailable. Please try again later.`;
			}
			switch (this.reasonCategory) {
				case "user_action_required": return `Permission required for ${this.capability}. Please grant access in settings.`;
				case "system_restricted": return `Access to ${this.capability} is restricted.`;
				case "not_configured": return `The capability ${this.capability} has not been configured.`;
				case "temporarily_unavailable": return `${this.capability} is temporarily unavailable.`;
				case "permanently_unavailable": return `${this.capability} is not available on this device.`;
				default: return `Capability denied: ${this.capability}`;
			}
		}
		/**
		* Check if this error is for an unknown/future reason.
		* Useful for logging or analytics.
		*/
		isUnknownReason() {
			return this.reasonCategory === "unknown";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/security/capability-bits.generated.ts
	const CapabilityBit = {
		"network:fetch": 0,
		"network:connect": 1,
		"network:listen": 2,
		"network:local": 3,
		"fs:read": 4,
		"fs:write": 5,
		"fs:list": 6,
		"fs:watch": 7,
		"env:read": 8,
		"process:spawn": 9,
		"process:signal": 10,
		"process:cwd": 11,
		"device:location": 12,
		"device:location:whenInUse": 13,
		"device:location:always": 14,
		"device:location:precise": 15,
		"device:location:reduced": 16,
		"device:camera": 17,
		"device:microphone": 18,
		"device:photos": 19,
		"device:photos:read": 20,
		"device:photos:write": 21,
		"device:photos:limited": 22,
		"device:contacts": 23,
		"device:contacts:read": 24,
		"device:contacts:write": 25,
		"device:sensors": 26,
		"device:motion": 27,
		"device:bluetooth": 28,
		"device:bluetooth:scan": 29,
		"device:bluetooth:connect": 30,
		"device:bluetooth:advertise": 31,
		"device:biometrics": 32,
		"device:credentials": 33,
		"device:credentials:read": 34,
		"device:credentials:write": 35,
		"device:notifications": 36,
		"device:calendar": 37,
		"device:calendar:read": 38,
		"device:calendar:write": 39,
		"device:reminders": 40,
		"device:health": 41,
		"device:health:read": 42,
		"device:health:write": 43,
		"storage:local": 44,
		"storage:session": 45,
		"storage:persist": 46,
		"sqlite:read": 47,
		"sqlite:write": 48,
		"clipboard:read": 49,
		"clipboard:write": 50,
		"crypto:random": 51,
		"crypto:subtle": 52,
		"time:now": 53,
		"time:highres": 54,
		"ipc:channel": 55
	};
	let _nativeCapabilityModule = null;
	let _auditCallbacks = [];
	let _silentMode = false;
	let _strictMode = false;
	const _warnedCapabilities = /* @__PURE__ */ new Set();
	/**
	* Check if we should suppress security warnings (for tests).
	*/
	function isSilentMode() {
		return _silentMode;
	}
	/**
	* Enable strict capability enforcement.
	*
	* When strict mode is enabled, capability checks will DENY requests
	* when no native capability module is available. This is the secure
	* behavior for production apps.
	*
	* By default, the capability system is LAX - it allows all capabilities
	* when no native module is present, to reduce friction during development.
	*
	* Call this during app initialization for production builds:
	* ```ts
	* import { enableStrictMode } from '@exact/runtime/security';
	*
	* if (__PROD__) {
	*   enableStrictMode();
	* }
	* ```
	*
	* The native layer should call this automatically for release builds.
	*/
	function enableStrictMode() {
		_strictMode = true;
	}
	/**
	* Set the native capability module.
	* Called by the native layer during runtime initialization.
	*/
	function setNativeCapabilityModule(module) {
		_nativeCapabilityModule = module;
		if (module.onAuditEvent) module.onAuditEvent((event) => {
			_auditCallbacks.forEach((cb) => {
				try {
					cb(event);
				} catch (e) {
					console.error("Capability audit callback error:", e);
				}
			});
		});
	}
	/**
	* Check if a capability is granted.
	* 
	* @param capability The capability to check (e.g., "network:fetch", "device:location")
	* @param moduleId Optional module ID for per-module checks
	* @returns CapabilityCheckResult with allowed status and denial reason
	*/
	function checkCapability(capability, moduleId) {
		if (moduleId !== void 0 && true) {
			const bit = getCapabilityBit(capability);
			if (bit !== void 0) {
				const bits = _effectiveBits.get(moduleId);
				if (bits) {
					if (_cachedGeneration.get(moduleId) === _permissionGeneration) {
						if (bit < 32 ? (bits[0] & 1 << bit) !== 0 : (bits[1] & 1 << bit - 32) !== 0) return {
							allowed: true,
							capability
						};
					}
				}
			}
		}
		if (_nativeCapabilityModule) {
			const allowed = _nativeCapabilityModule.checkCapability(capability, moduleId);
			return {
				allowed,
				reason: allowed ? void 0 : "module_not_granted",
				capability
			};
		}
		if (_strictMode) {
			console.warn(`[Security] Capability denied (strict mode): ${capability}`);
			return {
				allowed: false,
				reason: "app_not_granted",
				capability
			};
		}
		if (!_warnedCapabilities.has(capability)) {
			_warnedCapabilities.add(capability);
			console.warn(`[Security] No capability module available. Allowing "${capability}" in lax mode. Call enableStrictMode() for production security.`);
		}
		return {
			allowed: true,
			capability
		};
	}
	/**
	* Require a capability, throwing CapabilityDeniedError if not granted.
	* Use this for synchronous capability checks.
	* 
	* @param capability The capability to require
	* @param moduleId Optional module ID
	* @throws CapabilityDeniedError if capability is denied
	*/
	function requireCapability(capability, moduleId) {
		const result = checkCapability(capability, moduleId);
		if (!result.allowed) throw new CapabilityDeniedError(capability, result.reason, result.canRequestAgain);
	}
	/**
	* Get the bit position for a capability.
	* For parameterized caps like "network:fetch:api.example.com",
	* extracts base "network:fetch" and looks up its bit.
	*
	* @returns Bit position (0-55) or undefined if not a well-known capability
	*/
	function getCapabilityBit(capability) {
		const direct = CapabilityBit[capability];
		if (direct !== void 0) return direct;
		const { category, action } = parseCapability(capability);
		return CapabilityBit[`${category}:${action}`];
	}
	/**
	* Module effective bitmasks: moduleId -> [lo32, hi32]
	* Precomputed intersection of all 4 security layers for each module.
	*/
	const _effectiveBits = /* @__PURE__ */ new Map();
	/** Generation counter for permission changes */
	let _permissionGeneration = 0;
	/** Per-module cached generation */
	const _cachedGeneration = /* @__PURE__ */ new Map();
	/**
	* Parse a capability string into category and action.
	* 
	* @param capability Full capability string (e.g., "network:fetch:api.example.com")
	* @returns Parsed capability parts
	*/
	function parseCapability(capability) {
		const parts = capability.split(":");
		return {
			category: parts[0] || "",
			action: parts[1] || "",
			resource: parts[2]
		};
	}
	/**
	* Well-known capability names for type safety.
	* @see OS_CAPABILITY_SECURITY_OVERVIEW.md
	* @see JS_CAPABILITY_SECURITY_MODEL_SPEC_AND_PLAN.md
	*/
	const Capabilities = {
		NETWORK_FETCH: "network:fetch",
		NETWORK_CONNECT: "network:connect",
		NETWORK_LISTEN: "network:listen",
		NETWORK_LOCAL: "network:local",
		FS_READ: "fs:read",
		FS_WRITE: "fs:write",
		FS_LIST: "fs:list",
		FS_WATCH: "fs:watch",
		ENV_READ: "env:read",
		PROCESS_SPAWN: "process:spawn",
		PROCESS_SIGNAL: "process:signal",
		PROCESS_CWD: "process:cwd",
		DEVICE_LOCATION: "device:location",
		DEVICE_LOCATION_WHEN_IN_USE: "device:location:whenInUse",
		DEVICE_LOCATION_ALWAYS: "device:location:always",
		DEVICE_LOCATION_PRECISE: "device:location:precise",
		DEVICE_LOCATION_REDUCED: "device:location:reduced",
		DEVICE_CAMERA: "device:camera",
		DEVICE_MICROPHONE: "device:microphone",
		DEVICE_PHOTOS: "device:photos",
		DEVICE_PHOTOS_READ: "device:photos:read",
		DEVICE_PHOTOS_WRITE: "device:photos:write",
		DEVICE_PHOTOS_LIMITED: "device:photos:limited",
		DEVICE_CONTACTS: "device:contacts",
		DEVICE_CONTACTS_READ: "device:contacts:read",
		DEVICE_CONTACTS_WRITE: "device:contacts:write",
		DEVICE_SENSORS: "device:sensors",
		DEVICE_MOTION: "device:motion",
		DEVICE_BLUETOOTH: "device:bluetooth",
		DEVICE_BLUETOOTH_SCAN: "device:bluetooth:scan",
		DEVICE_BLUETOOTH_CONNECT: "device:bluetooth:connect",
		DEVICE_BLUETOOTH_ADVERTISE: "device:bluetooth:advertise",
		DEVICE_BIOMETRICS: "device:biometrics",
		DEVICE_CREDENTIALS: "device:credentials",
		DEVICE_CREDENTIALS_READ: "device:credentials:read",
		DEVICE_CREDENTIALS_WRITE: "device:credentials:write",
		DEVICE_NOTIFICATIONS: "device:notifications",
		DEVICE_CALENDAR: "device:calendar",
		DEVICE_CALENDAR_READ: "device:calendar:read",
		DEVICE_CALENDAR_WRITE: "device:calendar:write",
		DEVICE_REMINDERS: "device:reminders",
		DEVICE_HEALTH: "device:health",
		DEVICE_HEALTH_READ: "device:health:read",
		DEVICE_HEALTH_WRITE: "device:health:write",
		STORAGE_LOCAL: "storage:local",
		STORAGE_SESSION: "storage:session",
		STORAGE_PERSIST: "storage:persist",
		SQLITE_READ: "sqlite:read",
		SQLITE_WRITE: "sqlite:write",
		CLIPBOARD_READ: "clipboard:read",
		CLIPBOARD_WRITE: "clipboard:write",
		CRYPTO_RANDOM: "crypto:random",
		CRYPTO_SUBTLE: "crypto:subtle",
		TIME_NOW: "time:now",
		TIME_HIGHRES: "time:highres",
		IPC_CHANNEL: "ipc:channel"
	};
	//#endregion
	//#region ../ibex-runtime-js/src/location/index.ts
	init_defineProperty();
	const FOREGROUND_PERMISSION = "device:location:whenInUse";
	const LOCATION_PERMISSIONS = Object.freeze({
		getCurrentPosition: Object.freeze([FOREGROUND_PERMISSION]),
		watchPosition: Object.freeze([FOREGROUND_PERMISSION]),
		clearWatch: Object.freeze([])
	});
	const UNSUPPORTED_MESSAGE = "Location is not supported on this platform";
	var ExactGeolocationPositionError = class ExactGeolocationPositionError {
		constructor(code, message) {
			_defineProperty(this, "PERMISSION_DENIED", ExactGeolocationPositionError.PERMISSION_DENIED);
			_defineProperty(this, "POSITION_UNAVAILABLE", ExactGeolocationPositionError.POSITION_UNAVAILABLE);
			_defineProperty(this, "TIMEOUT", ExactGeolocationPositionError.TIMEOUT);
			_defineProperty(this, "name", "GeolocationPositionError");
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "code", void 0);
			_defineProperty(this, "stack", void 0);
			this.message = message ?? defaultErrorMessage(code);
			this.code = code;
			this.stack = new Error(this.message).stack;
		}
	};
	Object.setPrototypeOf(ExactGeolocationPositionError.prototype, Error.prototype);
	Object.setPrototypeOf(ExactGeolocationPositionError, Error);
	Object.defineProperties(ExactGeolocationPositionError, {
		PERMISSION_DENIED: {
			value: 1,
			enumerable: true,
			configurable: true
		},
		POSITION_UNAVAILABLE: {
			value: 2,
			enumerable: true,
			configurable: true
		},
		TIMEOUT: {
			value: 3,
			enumerable: true,
			configurable: true
		}
	});
	var UnsupportedLocationBackend = class {
		constructor() {
			_defineProperty(this, "features", /* @__PURE__ */ new Set());
		}
		async getCurrentPosition() {
			throw new DOMException$6(UNSUPPORTED_MESSAGE, "NotSupportedError");
		}
		getCurrentPositionGlobal(_success, error) {
			queueMicrotask(() => {
				error?.(new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE));
			});
		}
		watchPosition(_success, error) {
			const watchId = nextUnsupportedWatchId++;
			queueMicrotask(() => {
				error?.(new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE));
			});
			return watchId;
		}
		clearWatch(_watchId) {}
	};
	var BrowserLocationBackend = class {
		constructor(geolocation) {
			_defineProperty(this, "features", new Set(["getCurrentPosition", "watchPosition"]));
			_defineProperty(this, "geolocation", void 0);
			this.geolocation = geolocation;
		}
		getCurrentPosition(options) {
			return new Promise((resolve, reject) => {
				this.geolocation.getCurrentPosition((position) => resolve(position), (error) => reject(mapModuleError(error)), options);
			});
		}
		getCurrentPositionGlobal(success, error, options) {
			this.geolocation.getCurrentPosition(success, error ? (positionError) => {
				error(normalizePositionError(positionError));
			} : void 0, options);
		}
		watchPosition(success, error, options) {
			return this.geolocation.watchPosition(success, error ? (positionError) => {
				error(normalizePositionError(positionError));
			} : void 0, options);
		}
		clearWatch(watchId) {
			this.geolocation.clearWatch(watchId);
		}
	};
	var NativeLocationBackend = class {
		constructor() {
			_defineProperty(this, "features", new Set([
				"getCurrentPosition",
				"watchPosition",
				"highAccuracy",
				"altitude"
			]));
			_defineProperty(this, "modulePromise", null);
			_defineProperty(this, "nextWatchId", 1);
			_defineProperty(this, "watchers", /* @__PURE__ */ new Map());
			_defineProperty(this, "unsubscribeLocationUpdates", null);
			_defineProperty(this, "unsubscribeErrors", null);
			_defineProperty(this, "activeAccuracy", null);
			_defineProperty(this, "activeDistanceFilter", null);
			_defineProperty(this, "updatesStarted", false);
		}
		async getCurrentPosition(options) {
			const module = await this.loadModule();
			await ensureNativePermission(module);
			try {
				return toGeolocationPosition(await withTimeout(module.getCurrentLocation(mapAccuracy(options)), options?.timeout, () => new ExactGeolocationPositionError(ExactGeolocationPositionError.TIMEOUT, "Location request timed out")));
			} catch (error) {
				throw mapNativeModuleError(error);
			}
		}
		getCurrentPositionGlobal(success, error, options) {
			this.getCurrentPosition(options).then((position) => {
				success(position);
			}).catch((err) => {
				error?.(toGlobalPositionError(err));
			});
		}
		watchPosition(success, error, options) {
			const watchId = this.nextWatchId++;
			this.watchers.set(watchId, {
				success,
				error,
				options
			});
			this.startWatch(watchId);
			return watchId;
		}
		clearWatch(watchId) {
			this.watchers.delete(watchId);
			this.refreshUpdates();
		}
		async startWatch(watchId) {
			const registration = this.watchers.get(watchId);
			if (!registration) return;
			try {
				const module = await this.loadModule();
				await ensureNativePermission(module);
				if (!this.watchers.has(watchId)) return;
				this.ensureSubscriptions(module);
				await this.refreshUpdates();
			} catch (error) {
				if (!this.watchers.has(watchId)) return;
				this.watchers.delete(watchId);
				registration.error?.(toGlobalPositionError(error));
				this.refreshUpdates();
			}
		}
		ensureSubscriptions(module) {
			if (!this.unsubscribeLocationUpdates) this.unsubscribeLocationUpdates = module.onLocationUpdate((location) => {
				const position = toGeolocationPosition(location);
				{
					const __exactForOfIterator2 = this.watchers.values()[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep2 = __exactForOfIterator2.next();
						if (__exactForOfStep2.done) break;
						__exactForOfStep2.value.success(position);
					}
				}
			});
			if (!this.unsubscribeErrors) this.unsubscribeErrors = module.onError((nativeError) => {
				const positionError = nativeErrorToPositionError(nativeError);
				const shouldTerminate = positionError.code === ExactGeolocationPositionError.PERMISSION_DENIED;
				{
					const __exactForOfIterator3 = this.watchers.values()[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep3 = __exactForOfIterator3.next();
						if (__exactForOfStep3.done) break;
						__exactForOfStep3.value.error?.(positionError);
					}
				}
				if (shouldTerminate) {
					this.watchers.clear();
					this.refreshUpdates();
				}
			});
		}
		async refreshUpdates() {
			const module = await this.loadModule().catch(() => null);
			if (!module) return;
			if (this.watchers.size === 0) {
				if (this.updatesStarted) {
					module.stopUpdates();
					this.updatesStarted = false;
					this.activeAccuracy = null;
					this.activeDistanceFilter = null;
				}
				return;
			}
			const desiredAccuracy = computeWatchAccuracy(this.watchers.values());
			const desiredDistanceFilter = computeDistanceFilter(this.watchers.values());
			if (this.updatesStarted && this.activeAccuracy === desiredAccuracy && this.activeDistanceFilter === desiredDistanceFilter) return;
			if (this.updatesStarted) module.stopUpdates();
			module.startUpdates(desiredAccuracy, desiredDistanceFilter);
			this.updatesStarted = true;
			this.activeAccuracy = desiredAccuracy;
			this.activeDistanceFilter = desiredDistanceFilter;
		}
		loadModule() {
			if (!this.modulePromise) this.modulePromise = loadLegacyLocationModule().catch((error) => {
				this.modulePromise = null;
				throw error;
			});
			return this.modulePromise;
		}
	};
	function defaultErrorMessage(code) {
		switch (code) {
			case ExactGeolocationPositionError.PERMISSION_DENIED: return "Location permission denied";
			case ExactGeolocationPositionError.TIMEOUT: return "Location request timed out";
			case ExactGeolocationPositionError.POSITION_UNAVAILABLE:
			default: return "Location is unavailable";
		}
	}
	function toGeolocationPosition(location) {
		return {
			coords: {
				latitude: location.latitude,
				longitude: location.longitude,
				accuracy: location.horizontalAccuracy,
				altitude: Number.isFinite(location.altitude) ? location.altitude : null,
				altitudeAccuracy: Number.isFinite(location.verticalAccuracy) ? location.verticalAccuracy : null,
				heading: null,
				speed: null,
				toJSON() {
					return {
						latitude: this.latitude,
						longitude: this.longitude,
						accuracy: this.accuracy,
						altitude: this.altitude,
						altitudeAccuracy: this.altitudeAccuracy,
						heading: this.heading,
						speed: this.speed
					};
				}
			},
			timestamp: location.timestamp,
			toJSON() {
				return {
					coords: this.coords,
					timestamp: this.timestamp
				};
			}
		};
	}
	function normalizePositionError(error) {
		if (error instanceof ExactGeolocationPositionError) return error;
		return new ExactGeolocationPositionError(normalizePositionErrorCode(error?.code), error?.message);
	}
	function normalizePositionErrorCode(code) {
		if (code === ExactGeolocationPositionError.PERMISSION_DENIED) return ExactGeolocationPositionError.PERMISSION_DENIED;
		if (code === ExactGeolocationPositionError.TIMEOUT) return ExactGeolocationPositionError.TIMEOUT;
		return ExactGeolocationPositionError.POSITION_UNAVAILABLE;
	}
	function mapModuleError(error) {
		const normalized = normalizePositionError(error);
		if (normalized.code === ExactGeolocationPositionError.PERMISSION_DENIED) return new CapabilityDeniedError(FOREGROUND_PERMISSION, "os_denied", false);
		return normalized;
	}
	function mapNativeModuleError(error) {
		if (error instanceof CapabilityDeniedError) return error;
		if (error instanceof DOMException$6 && error.name === "NotSupportedError") return error;
		if (error instanceof ExactGeolocationPositionError) return error;
		if (error instanceof Error) {
			const message = error.message.toLowerCase();
			if (message.includes("permission_denied")) return new CapabilityDeniedError(FOREGROUND_PERMISSION, "os_denied", false);
			if (message.includes("timed out")) return new ExactGeolocationPositionError(ExactGeolocationPositionError.TIMEOUT, error.message);
			return new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE, error.message);
		}
		return new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE);
	}
	function toGlobalPositionError(error) {
		if (error instanceof CapabilityDeniedError) return new ExactGeolocationPositionError(ExactGeolocationPositionError.PERMISSION_DENIED, error.message);
		if (error instanceof DOMException$6 && error.name === "NotSupportedError") return new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE, error.message);
		if (error instanceof ExactGeolocationPositionError) return error;
		if (error instanceof Error && error.message.toLowerCase().includes("timed out")) return new ExactGeolocationPositionError(ExactGeolocationPositionError.TIMEOUT, error.message);
		return new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE, error instanceof Error ? error.message : void 0);
	}
	function nativeErrorToPositionError(error) {
		const code = error.code?.toUpperCase();
		if (code === "PERMISSION_DENIED") return new ExactGeolocationPositionError(ExactGeolocationPositionError.PERMISSION_DENIED, error.message);
		if (code === "TIMEOUT") return new ExactGeolocationPositionError(ExactGeolocationPositionError.TIMEOUT, error.message);
		return new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE, error.message);
	}
	async function ensureNativePermission(module) {
		const currentStatus = module.getPermissionStatus();
		const immediateError = permissionStatusToError(currentStatus);
		if (immediateError) throw immediateError;
		if (isAuthorizedStatus(currentStatus)) return;
		const result = await module.requestPermission("whenInUse");
		const requestError = permissionStatusToError(result.status);
		if (requestError) throw requestError;
		if (!isAuthorizedStatus(result.status)) throw new CapabilityDeniedError(FOREGROUND_PERMISSION, "os_denied", false);
	}
	function permissionStatusToError(status) {
		if (status === "denied") return new CapabilityDeniedError(FOREGROUND_PERMISSION, "os_denied", false);
		if (status === "restricted") return new CapabilityDeniedError(FOREGROUND_PERMISSION, "os_restricted", false);
		return null;
	}
	function isAuthorizedStatus(status) {
		return status === "authorizedWhenInUse" || status === "authorizedAlways";
	}
	function mapAccuracy(options) {
		return options?.enableHighAccuracy ? "best" : "hundredMeters";
	}
	function computeWatchAccuracy(watchers) {
		for (const watcher of watchers) if (watcher.options?.enableHighAccuracy) return "best";
		return "hundredMeters";
	}
	function computeDistanceFilter(watchers) {
		let desired = 10;
		{
			const __exactForOfIterator4 = watchers[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep4 = __exactForOfIterator4.next();
				if (__exactForOfStep4.done) break;
				const watcher = __exactForOfStep4.value;
				if (typeof watcher.options?.maximumAge === "number" && watcher.options.maximumAge <= 1e3) desired = Math.min(desired, 1);
			}
		}
		return desired;
	}
	function getHostNavigator() {
		const globalValue = globalThis;
		const exactHostNavigator = globalThis.__exactHostNavigator;
		if (exactHostNavigator && exactHostNavigator.__exactNavigator !== true) return exactHostNavigator;
		const navigatorValue = globalValue.navigator;
		if (navigatorValue && navigatorValue.__exactNavigator !== true) return navigatorValue;
	}
	function getHostGeolocation() {
		const hostNavigator = getHostNavigator();
		if (!hostNavigator?.geolocation) return null;
		return hostNavigator.geolocation;
	}
	function hasNativeLocationBridge() {
		return false;
	}
	let legacyLocationModulePromise = null;
	function loadLegacyLocationModule() {
		legacyLocationModulePromise ?? (legacyLocationModulePromise = Promise.reject(new ExactGeolocationPositionError(ExactGeolocationPositionError.POSITION_UNAVAILABLE, UNSUPPORTED_MESSAGE)));
		return legacyLocationModulePromise;
	}
	const unsupportedBackend = new UnsupportedLocationBackend();
	let nativeBackend = null;
	let nextUnsupportedWatchId = 1;
	function getLocationBackend() {
		const hostGeolocation = getHostGeolocation();
		if (hostGeolocation) return new BrowserLocationBackend(hostGeolocation);
		if (hasNativeLocationBridge()) {
			if (!nativeBackend) nativeBackend = new NativeLocationBackend();
			return nativeBackend;
		}
		return unsupportedBackend;
	}
	function withTimeout(promise, timeoutMs, createTimeoutError) {
		if (timeoutMs == null || !Number.isFinite(timeoutMs) || timeoutMs <= 0) return promise;
		return new Promise((resolve, reject) => {
			let settled = false;
			const timeoutId = setTimeout(() => {
				if (settled) return;
				settled = true;
				reject(createTimeoutError());
			}, timeoutMs);
			promise.then((value) => {
				if (settled) return;
				settled = true;
				clearTimeout(timeoutId);
				resolve(value);
			}, (error) => {
				if (settled) return;
				settled = true;
				clearTimeout(timeoutId);
				reject(error);
			});
		});
	}
	var ExactGeolocation = class {
		clearWatch(watchId) {
			getLocationBackend().clearWatch(watchId);
		}
		getCurrentPosition(success, error, options) {
			getLocationBackend().getCurrentPositionGlobal(success, error, options);
		}
		watchPosition(success, error, options) {
			return getLocationBackend().watchPosition(success, error, options);
		}
	};
	var ExactLocationModule = class {
		constructor() {
			_defineProperty(this, "permissions", LOCATION_PERMISSIONS);
		}
		get features() {
			return new Set(getLocationBackend().features);
		}
		getCurrentPosition(options) {
			return getLocationBackend().getCurrentPosition(options);
		}
		watchPosition(success, error, options) {
			return getLocationBackend().watchPosition(success, error, options);
		}
		clearWatch(watchId) {
			getLocationBackend().clearWatch(watchId);
		}
	};
	const geolocation = new ExactGeolocation();
	function getGeolocation() {
		return geolocation;
	}
	new ExactLocationModule();
	//#endregion
	//#region ../ibex-runtime-js/src/camera/index.ts
	/**
	* Exact camera runtime core.
	*
	* This module intentionally keeps the browser-facing camera implementation out
	* of the React wrapper package so that:
	* 1. the permission/access layer can query camera state without importing React
	* 2. the agent server can inspect active camera sessions directly
	* 3. the `exact/camera` React package can stay thin and mostly declarative
	*
	* The current implementation is explicitly web-first. It provides the public
	* RFC 0044 surface area in a way that works today in browser development and
	* in the camera lab, while keeping the internal contracts shaped so that
	* native backends can replace or augment this logic later.
	*/
	init_defineProperty();
	const NATIVE_CAMERA_MODULE_NAME = "com.exact.camera";
	const MIN_NATIVE_CAMERA_MODULE_VERSION = "0.2.0";
	Object.freeze({
		takePhoto: Object.freeze(["device:camera:back", "device:camera:front"]),
		takeBurst: Object.freeze(["device:camera:back", "device:camera:front"]),
		takeSnapshot: Object.freeze([]),
		startRecording: Object.freeze([
			"device:camera:back",
			"device:camera:front",
			"device:microphone"
		])
	});
	Object.freeze([
		"preview",
		"photo",
		"burst",
		"snapshot",
		"recording",
		"intentCapture",
		"readiness",
		"frameProcessor",
		"processedPreview",
		"codeScanner",
		"virtualCamera",
		"analysis"
	]);
	const DEFAULT_WEB_FORMATS = [
		{
			photoWidth: 640,
			photoHeight: 480,
			videoWidth: 640,
			videoHeight: 480,
			minFps: 15,
			maxFps: 30,
			supportsHdr: false,
			supportsDepth: false,
			pixelFormat: "bgra",
			videoStabilization: ["off", "auto"]
		},
		{
			photoWidth: 1280,
			photoHeight: 720,
			videoWidth: 1280,
			videoHeight: 720,
			minFps: 15,
			maxFps: 30,
			supportsHdr: false,
			supportsDepth: false,
			pixelFormat: "bgra",
			videoStabilization: ["off", "auto"]
		},
		{
			photoWidth: 1920,
			photoHeight: 1080,
			videoWidth: 1920,
			videoHeight: 1080,
			minFps: 15,
			maxFps: 60,
			supportsHdr: false,
			supportsDepth: false,
			pixelFormat: "bgra",
			videoStabilization: ["off", "auto"]
		},
		{
			photoWidth: 3840,
			photoHeight: 2160,
			videoWidth: 3840,
			videoHeight: 2160,
			minFps: 24,
			maxFps: 60,
			supportsHdr: false,
			supportsDepth: false,
			pixelFormat: "bgra",
			videoStabilization: ["off", "auto"]
		}
	];
	const DEFAULT_VIRTUAL_CONFIG = {
		source: {
			type: "generated",
			pattern: "counter"
		},
		fps: 30,
		resolution: {
			width: 1280,
			height: 720
		},
		loop: true
	};
	const DEFAULT_CAMERA_CAPABILITIES = Object.freeze({
		flash: false,
		torch: false,
		depth: false,
		lidar: false,
		hdr: false,
		lowLight: false,
		manualFocus: false,
		manualExposure: false,
		manualWhiteBalance: false,
		rawCapture: false,
		processingControl: false,
		multiCamera: true,
		retroactiveRecording: true
	});
	Object.freeze({
		faces: 0,
		blur: 0,
		exposure: "good",
		hasGps: false,
		dominantSubject: "scene",
		barcodes: []
	});
	Object.freeze([
		"qr",
		"ean-13",
		"ean-8",
		"code-128",
		"code-39",
		"code-93",
		"upc-a",
		"upc-e",
		"pdf-417",
		"aztec",
		"data-matrix",
		"itf"
	]);
	Object.freeze([
		"objects",
		"text",
		"faces",
		"barcodes",
		"brightness",
		"blur",
		"dominantColors"
	]);
	const deviceSubscribers = /* @__PURE__ */ new Set();
	const virtualCameraConfigs = /* @__PURE__ */ new Map();
	let defaultVirtualCameraConfig = { ...DEFAULT_VIRTUAL_CONFIG };
	let cachedDevices = [];
	let devicesRefreshPromise = null;
	let mediaDeviceListenersInstalled = false;
	function getNavigator$1() {
		if (typeof navigator === "undefined") return null;
		return navigator;
	}
	function isNativeCameraHostEnvironment() {
		return typeof globalThis.__hostCall === "function" && typeof globalThis.__exactPlatform === "string" && globalThis.__exactPlatform.length > 0 && globalThis.__exactPlatform !== "web";
	}
	function readNativeCameraModuleMetadata() {
		const exact = globalThis.exact;
		if (!exact) return null;
		if (typeof exact.getModuleMetadata === "function") try {
			return exact.getModuleMetadata(NATIVE_CAMERA_MODULE_NAME);
		} catch {
			return null;
		}
		return exact.__moduleMetadata?.[NATIVE_CAMERA_MODULE_NAME] ?? null;
	}
	function parseModuleVersion(version) {
		const segments = version.trim().split(".");
		if (segments.length === 0) return null;
		const parsed = [];
		for (const segment of segments) {
			const match = /^\d+/.exec(segment);
			if (!match) return null;
			parsed.push(Number.parseInt(match[0], 10));
		}
		return parsed;
	}
	function isModuleVersionAtLeast(actual, minimum) {
		const actualParts = parseModuleVersion(actual);
		const minimumParts = parseModuleVersion(minimum);
		if (!actualParts || !minimumParts) return false;
		const length = Math.max(actualParts.length, minimumParts.length);
		for (let index = 0; index < length; index += 1) {
			const actualValue = actualParts[index] ?? 0;
			const minimumValue = minimumParts[index] ?? 0;
			if (actualValue > minimumValue) return true;
			if (actualValue < minimumValue) return false;
		}
		return true;
	}
	function hasNativeCameraHost() {
		if (!isNativeCameraHostEnvironment()) return false;
		const metadata = readNativeCameraModuleMetadata();
		return typeof metadata?.version === "string" && isModuleVersionAtLeast(metadata.version, MIN_NATIVE_CAMERA_MODULE_VERSION);
	}
	function invokeNativeCameraHostCall(operation, payload) {
		const hostCall = globalThis.__hostCall;
		if (typeof hostCall !== "function") return;
		return hostCall(operation, typeof payload === "undefined" ? "{}" : JSON.stringify(payload));
	}
	function normalizeFacingFromLabel(label) {
		const normalized = label.toLowerCase();
		if (normalized.includes("front") || normalized.includes("facetime") || normalized.includes("user") || normalized.includes("selfie")) return "front";
		if (normalized.includes("back") || normalized.includes("rear") || normalized.includes("environment")) return "back";
		return "external";
	}
	function cloneCameraFormats() {
		return DEFAULT_WEB_FORMATS.map((format) => ({
			...format,
			videoStabilization: [...format.videoStabilization]
		}));
	}
	function cloneCameraCapabilities() {
		return { ...DEFAULT_CAMERA_CAPABILITIES };
	}
	function makePhysicalDevice(device) {
		return {
			id: device.deviceId,
			position: normalizeFacingFromLabel(device.label),
			name: device.label || "Camera",
			isVirtual: false,
			lenses: [],
			minZoom: 1,
			maxZoom: 4,
			formats: cloneCameraFormats(),
			capabilities: cloneCameraCapabilities()
		};
	}
	function getVirtualCameraIds() {
		const ids = new Set(["virtual-back", "virtual-front"]);
		{
			const __exactForOfIterator9 = virtualCameraConfigs.keys()[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep9 = __exactForOfIterator9.next();
				if (__exactForOfStep9.done) break;
				const id = __exactForOfStep9.value;
				ids.add(id);
			}
		}
		return Array.from(ids);
	}
	function getVirtualCameraConfig(deviceId) {
		return virtualCameraConfigs.get(deviceId) ?? defaultVirtualCameraConfig;
	}
	function makeVirtualDevice(deviceId) {
		const position = deviceId.includes("front") ? "front" : deviceId.includes("back") ? "back" : "external";
		const config = getVirtualCameraConfig(deviceId);
		const resolution = config.resolution ?? DEFAULT_VIRTUAL_CONFIG.resolution;
		return {
			id: deviceId,
			position,
			name: position === "front" ? "Virtual Front Camera" : position === "back" ? "Virtual Back Camera" : "Virtual Camera",
			isVirtual: true,
			lenses: [],
			minZoom: 1,
			maxZoom: 1,
			formats: [{
				photoWidth: resolution.width,
				photoHeight: resolution.height,
				videoWidth: resolution.width,
				videoHeight: resolution.height,
				minFps: config.fps ?? 30,
				maxFps: config.fps ?? 30,
				supportsHdr: false,
				supportsDepth: false,
				pixelFormat: "bgra",
				videoStabilization: ["off"]
			}],
			capabilities: {
				...cloneCameraCapabilities(),
				multiCamera: true
			}
		};
	}
	function installMediaDeviceListeners() {
		if (mediaDeviceListenersInstalled) return;
		const nav = getNavigator$1();
		if (!nav?.mediaDevices || typeof nav.mediaDevices.addEventListener !== "function") return;
		mediaDeviceListenersInstalled = true;
		nav.mediaDevices.addEventListener("devicechange", () => {
			refreshCameraDevices();
		});
	}
	async function listCameraDevices() {
		if (hasNativeCameraHost()) {
			try {
				cachedDevices = invokeNativeCameraHostCall("camera.devices.list")?.devices ?? [];
			} catch {
				cachedDevices = [];
			}
			{
				const __exactForOfIterator10 = deviceSubscribers[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep10 = __exactForOfIterator10.next();
					if (__exactForOfStep10.done) break;
					const subscriber = __exactForOfStep10.value;
					subscriber();
				}
			}
			return cachedDevices.slice();
		}
		installMediaDeviceListeners();
		if (devicesRefreshPromise) return devicesRefreshPromise;
		devicesRefreshPromise = (async () => {
			const devices = [];
			const nav = getNavigator$1();
			if (nav?.mediaDevices?.enumerateDevices) try {
				const discovered = await nav.mediaDevices.enumerateDevices();
				for (const device of discovered) {
					if (device.kind !== "videoinput") continue;
					devices.push(makePhysicalDevice(device));
				}
			} catch {}
			{
				const __exactForOfIterator11 = getVirtualCameraIds()[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep11 = __exactForOfIterator11.next();
					if (__exactForOfStep11.done) break;
					const virtualId = __exactForOfStep11.value;
					devices.push(makeVirtualDevice(virtualId));
				}
			}
			cachedDevices = devices;
			{
				const __exactForOfIterator12 = deviceSubscribers[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep12 = __exactForOfIterator12.next();
					if (__exactForOfStep12.done) break;
					const subscriber = __exactForOfStep12.value;
					subscriber();
				}
			}
			devicesRefreshPromise = null;
			return devices;
		})();
		return devicesRefreshPromise;
	}
	async function refreshCameraDevices() {
		devicesRefreshPromise = null;
		return listCameraDevices();
	}
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/Headers.ts
	init_defineProperty();
	let _Symbol$iterator$3, _Symbol$for$1;
	/**
	* Normalize a header name to lowercase.
	* HTTP header names are case-insensitive.
	*/
	function normalizeHeaderName(name) {
		if (typeof name !== "string") name = String(name);
		if (!/^[\w!#$%&'*+.^`|~-]+$/.test(name)) throw new TypeError(isBunCompatEnv$1() ? `Invalid header name: '${name}'` : `Invalid header name: ${name}`);
		return name.toLowerCase();
	}
	function isBunCompatEnv$1() {
		if (globalThis.__exactRuntimeContext === "shell") return false;
		if (typeof process !== "object" || !process || typeof process.env !== "object") return false;
		return process.env.EXACT_COMPAT_TEST === "1" && process.env.EXACT_TEST_SECTION === "bun";
	}
	function ensureRequiredArguments(methodName, actualCount, requiredCount) {
		if (actualCount < requiredCount) throw new TypeError(`Failed to execute '${methodName}' on 'Headers': ${requiredCount} argument${requiredCount === 1 ? "" : "s"} required, but only ${actualCount} present.`);
	}
	function ensureHeadersBrand(value, methodName) {
		if (!(value instanceof Headers)) throw new TypeError(`Failed to execute '${methodName}' on 'Headers': Illegal invocation`);
		return value;
	}
	function formatHeadersForInspect(entries) {
		if (entries.length === 0) return "Headers {}";
		const lines = ["Headers {"];
		for (let i = 0; i < entries.length; i++) {
			const [name, value] = entries[i];
			lines.push(`  ${JSON.stringify(name)}: ${JSON.stringify(value)},`);
		}
		lines.push("}");
		return lines.join("\n");
	}
	/**
	* CORS-safelisted request header names.
	*/
	const CORS_SAFELISTED_HEADERS = new Set([
		"accept",
		"accept-language",
		"content-language",
		"content-type"
	]);
	/**
	* Forbidden response header names.
	*/
	const FORBIDDEN_RESPONSE_HEADERS = new Set(["set-cookie", "set-cookie2"]);
	/**
	* Normalize a header value.
	*/
	function normalizeHeaderValue(value) {
		if (typeof value === "symbol") throw new TypeError(`Invalid header value`);
		if (typeof value !== "string") value = String(value);
		const rawValue = value;
		if (value.length === 0) return value;
		if (value.includes("\0")) {
			if (isBunCompatEnv$1()) throw new TypeError(`Header 'x-test' has invalid value: '${rawValue}'`);
			throw new TypeError(`Invalid header value`);
		}
		for (let i = 0; i < value.length; i++) if (value.charCodeAt(i) > 255) {
			if (isBunCompatEnv$1()) throw new TypeError(`Header 'x-test' has invalid value: '${rawValue}'`);
			throw new TypeError(`Invalid header value`);
		}
		let start = 0;
		let end = value.length;
		while (start < end) {
			const code = value.charCodeAt(start);
			if (code !== 9 && code !== 10 && code !== 13 && code !== 32) break;
			start += 1;
		}
		while (end > start) {
			const code = value.charCodeAt(end - 1);
			if (code !== 9 && code !== 10 && code !== 13 && code !== 32) break;
			end -= 1;
		}
		value = start === 0 && end === value.length ? value : value.slice(start, end);
		if (/[\r\n]/.test(value)) {
			if (isBunCompatEnv$1()) throw new TypeError(`Header 'x-test' has invalid value: '${rawValue}'`);
			throw new TypeError(`Invalid header value`);
		}
		return value;
	}
	/**
	* Check if a header name is forbidden for modification.
	*/
	function isForbiddenHeaderName(name) {
		const lower = name.toLowerCase();
		return [
			"accept-charset",
			"accept-encoding",
			"access-control-request-headers",
			"access-control-request-method",
			"access-control-request-private-network",
			"connection",
			"content-length",
			"cookie",
			"cookie2",
			"date",
			"dnt",
			"expect",
			"host",
			"keep-alive",
			"origin",
			"referer",
			"set-cookie",
			"te",
			"trailer",
			"transfer-encoding",
			"upgrade",
			"via"
		].includes(lower) || lower.startsWith("proxy-") || lower.startsWith("sec-");
	}
	function isBunCompatAllowedRequestHeader(name) {
		if (!isBunCompatEnv$1()) return false;
		const normalized = name.toLowerCase();
		return normalized === "connection" || normalized === "accept-encoding";
	}
	/**
	* Check if a string contains CORS-unsafe request-header bytes.
	* Per Fetch spec: https://fetch.spec.whatwg.org/#cors-unsafe-request-header-byte
	*/
	function hasCORSUnsafeBytes(value) {
		for (let i = 0; i < value.length; i++) {
			const c = value.charCodeAt(i);
			if (c >= 0 && c <= 8 || c >= 10 && c <= 31 || c === 34 || c === 40 || c === 41 || c === 58 || c === 60 || c === 62 || c === 63 || c === 64 || c === 91 || c === 92 || c === 93 || c === 123 || c === 125 || c === 127) return true;
		}
		return false;
	}
	/**
	* The Headers interface of the Fetch API allows you to perform
	* various actions on HTTP request and response headers.
	*/
	/**
	* Get the %IteratorPrototype% from the runtime.
	*/
	const iteratorPrototype = Object.getPrototypeOf(Object.getPrototypeOf([][Symbol.iterator]()));
	/**
	* Create a Headers iterator prototype for the given value mapper.
	*/
	function createHeadersIteratorPrototype(mapFn) {
		const proto = Object.create(iteratorPrototype);
		Object.defineProperty(proto, "next", {
			value: function() {
				const entries = this._headers._flatSortedEntries();
				if (this._index >= entries.length) return {
					value: void 0,
					done: true
				};
				return {
					value: mapFn(entries[this._index++]),
					done: false
				};
			},
			writable: true,
			enumerable: true,
			configurable: true
		});
		Object.defineProperty(proto, Symbol.toStringTag, {
			value: "Iterator",
			configurable: true
		});
		return proto;
	}
	const entriesIteratorProto = createHeadersIteratorPrototype((e) => e);
	const keysIteratorProto = createHeadersIteratorPrototype((e) => e[0]);
	const valuesIteratorProto = createHeadersIteratorPrototype((e) => e[1]);
	_Symbol$iterator$3 = Symbol.iterator;
	_Symbol$for$1 = Symbol.for("nodejs.util.inspect.custom");
	var Headers = class Headers {
		constructor(init) {
			_defineProperty(this, "_map", /* @__PURE__ */ new Map());
			_defineProperty(
				this,
				/** @internal */
				"_guard",
				"none"
			);
			if (init !== void 0) {
				if (init === null || typeof init !== "object" && typeof init !== "function") throw new TypeError("Failed to construct 'Headers': The provided value is not of type '(sequence<sequence<ByteString>> or record<ByteString, ByteString>)'");
				this._initFromInput(init);
			}
		}
		_initFromInput(init) {
			const iteratorMethod = init[Symbol.iterator];
			if ((iteratorMethod === null || iteratorMethod === void 0) && typeof init === "object" && init !== null && Symbol.iterator in init) throw new TypeError("Failed to construct 'Headers': The provided value is not of type '(sequence<sequence<ByteString>> or record<ByteString, ByteString>)'");
			if (typeof iteratorMethod === "function") {
				const iterator = iteratorMethod.call(init);
				const entries = Array.from(iterator);
				for (let i = 0; i < entries.length; i++) {
					const entry = entries[i];
					if (typeof entry === "string") throw new TypeError("Failed to construct 'Headers': Each header pair must be a name/value pair");
					if (Array.isArray(entry)) {
						if (entry.length !== 2) throw new TypeError("Failed to construct 'Headers': Each header pair must be a name/value pair");
						this.append(entry[0], entry[1]);
					} else if (typeof entry === "object" && entry !== null && Symbol.iterator in entry) {
						const pair = [...entry];
						if (pair.length !== 2) throw new TypeError("Failed to construct 'Headers': Each header pair must be a name/value pair");
						this.append(pair[0], pair[1]);
					} else throw new TypeError("Failed to construct 'Headers': Each header pair must be an iterable");
				}
			} else if (typeof init === "object" && init !== null) {
				const keys = typeof Reflect !== "undefined" && Reflect.ownKeys ? Reflect.ownKeys(init) : Object.getOwnPropertyNames(init);
				for (const key of keys) {
					const desc = Object.getOwnPropertyDescriptor(init, key);
					if (!desc || !desc.enumerable) continue;
					if (typeof key === "symbol") throw new TypeError("Failed to construct 'Headers': Invalid header name");
					normalizeHeaderName(key);
					const value = init[key];
					this.append(key, value);
				}
			}
		}
		/**
		* Appends a new value onto an existing header, or adds the header
		* if it does not already exist.
		*/
		append(name, value) {
			const headers = ensureHeadersBrand(this, "append");
			ensureRequiredArguments("append", arguments.length, 2);
			const normalizedName = normalizeHeaderName(name);
			const normalizedValue = normalizeHeaderValue(value);
			if (!headers._guardAllows(normalizedName, normalizedValue, false)) return;
			const existing = headers._map.get(normalizedName);
			if (existing) existing.push(normalizedValue);
			else headers._map.set(normalizedName, [normalizedValue]);
		}
		/**
		* Deletes a header from the Headers object.
		*/
		delete(name) {
			const headers = ensureHeadersBrand(this, "delete");
			ensureRequiredArguments("delete", arguments.length, 1);
			const normalizedName = normalizeHeaderName(name);
			if (!headers._guardAllows(normalizedName)) return;
			headers._map.delete(normalizedName);
		}
		/**
		* Returns a String containing all the values of a header with a given name.
		*/
		get(name) {
			const headers = ensureHeadersBrand(this, "get");
			ensureRequiredArguments("get", arguments.length, 1);
			const normalizedName = normalizeHeaderName(name);
			const values = headers._map.get(normalizedName);
			if (!values || values.length === 0) return null;
			return values.join(", ");
		}
		/**
		* Returns all values of a header with a given name as an array.
		*/
		getSetCookie() {
			const values = ensureHeadersBrand(this, "getSetCookie")._map.get("set-cookie");
			return values ? [...values] : [];
		}
		/**
		* Bun compatibility extension that returns all stored values for a header.
		*/
		getAll(name) {
			const headers = ensureHeadersBrand(this, "getAll");
			ensureRequiredArguments("getAll", arguments.length, 1);
			const normalizedName = normalizeHeaderName(name);
			const values = headers._map.get(normalizedName);
			return values ? [...values] : [];
		}
		/**
		* Bun compatibility extension that reports the total number of stored header entries.
		*/
		get count() {
			const headers = ensureHeadersBrand(this, "count");
			let total = 0;
			{
				const __exactForOfIterator29 = headers._map.values()[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep29 = __exactForOfIterator29.next();
					if (__exactForOfStep29.done) break;
					const values = __exactForOfStep29.value;
					total += values.length;
				}
			}
			return total;
		}
		/**
		* Returns a boolean stating whether a Headers object contains a certain header.
		*/
		has(name) {
			const headers = ensureHeadersBrand(this, "has");
			ensureRequiredArguments("has", arguments.length, 1);
			const normalizedName = normalizeHeaderName(name);
			return headers._map.has(normalizedName);
		}
		/**
		* Sets a new value for an existing header, or adds the header if it does not exist.
		*/
		set(name, value) {
			const headers = ensureHeadersBrand(this, "set");
			ensureRequiredArguments("set", arguments.length, 2);
			const normalizedName = normalizeHeaderName(name);
			const normalizedValue = normalizeHeaderValue(value);
			if (!headers._guardAllows(normalizedName, normalizedValue, true)) return;
			headers._map.set(normalizedName, [normalizedValue]);
		}
		/**
		* Executes a provided function once for each header.
		*/
		forEach(callback, thisArg) {
			const headers = ensureHeadersBrand(this, "forEach");
			ensureRequiredArguments("forEach", arguments.length, 1);
			if (typeof callback !== "function") throw new TypeError("Failed to execute 'forEach' on 'Headers': parameter 1 is not of type 'Function'");
			const entries = headers._flatSortedEntries();
			for (let i = 0; i < entries.length; i++) callback.call(thisArg, entries[i][1], entries[i][0], headers);
		}
		/**
		* Returns a live iterator of all header entries.
		* Re-sorts on each next() call per the Fetch spec.
		*/
		entries() {
			const headers = ensureHeadersBrand(this, "entries");
			const iter = Object.create(entriesIteratorProto);
			iter._headers = headers;
			iter._index = 0;
			return iter;
		}
		/**
		* Returns a live iterator of all header names.
		*/
		keys() {
			const headers = ensureHeadersBrand(this, "keys");
			const iter = Object.create(keysIteratorProto);
			iter._headers = headers;
			iter._index = 0;
			return iter;
		}
		/**
		* Returns a live iterator of all header values.
		*/
		values() {
			const headers = ensureHeadersBrand(this, "values");
			const iter = Object.create(valuesIteratorProto);
			iter._headers = headers;
			iter._index = 0;
			return iter;
		}
		/**
		* Returns an iterator of all header entries (sorted by name).
		*/
		[_Symbol$iterator$3]() {
			return ensureHeadersBrand(this, "entries").entries();
		}
		/**
		* Bun compatibility extension that materializes the headers as a plain object.
		*/
		toJSON() {
			const headers = ensureHeadersBrand(this, "toJSON");
			const result = {};
			{
				const __exactForOfIterator30 = headers._sortedEntries()[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep30 = __exactForOfIterator30.next();
					if (__exactForOfStep30.done) break;
					const [name, values] = __exactForOfStep30.value;
					if (name === "set-cookie" && isBunCompatEnv$1()) result[name] = [...values];
					else result[name] = values.join(", ");
				}
			}
			return result;
		}
		/**
		* Check if a header modification is allowed by the current guard.
		* Returns true if allowed. Throws TypeError for immutable guard.
		* Returns false (silently fails) for other guard violations per spec.
		* @param normalizedName The lowercased header name
		* @param normalizedValue The value being set/appended (for no-cors value checks)
		* @param isSet Whether this is a set (replace) vs append operation
		*/
		_guardAllows(normalizedName, normalizedValue, isSet) {
			switch (this._guard) {
				case "immutable": throw new TypeError("Failed to execute on 'Headers': Headers are immutable");
				case "request":
					if (isForbiddenHeaderName(normalizedName) && !isBunCompatAllowedRequestHeader(normalizedName)) return false;
					break;
				case "request-no-cors":
					if (!CORS_SAFELISTED_HEADERS.has(normalizedName)) return false;
					if (normalizedValue !== void 0) {
						if (hasCORSUnsafeBytes(normalizedValue)) return false;
						let combinedValue;
						if (isSet) combinedValue = normalizedValue;
						else {
							const existing = this._map.get(normalizedName);
							combinedValue = existing ? existing.join(", ") + ", " + normalizedValue : normalizedValue;
						}
						if (combinedValue.length > 128) return false;
						if (normalizedName === "content-type") {
							const mime = combinedValue.split(";")[0].trim().toLowerCase();
							if (mime !== "application/x-www-form-urlencoded" && mime !== "multipart/form-data" && mime !== "text/plain") return false;
						}
					}
					break;
				case "response":
					if (FORBIDDEN_RESPONSE_HEADERS.has(normalizedName)) return false;
					break;
				default: break;
			}
			return true;
		}
		/**
		* Get entries sorted alphabetically by name.
		*/
		_sortedEntries() {
			const entries = [...this._map.entries()].sort((a, b) => a[0].localeCompare(b[0]));
			if (!isBunCompatEnv$1()) return entries;
			const normalEntries = [];
			const setCookieEntries = [];
			for (let i = 0; i < entries.length; i++) if (entries[i][0] === "set-cookie") setCookieEntries.push(entries[i]);
			else normalEntries.push(entries[i]);
			return normalEntries.concat(setCookieEntries);
		}
		/**
		* Get flattened sorted entries per the Fetch spec.
		* Most headers combine values with ", ". Set-Cookie headers are NOT combined —
		* each value is returned as a separate entry.
		*/
		_flatSortedEntries() {
			const result = [];
			{
				const __exactForOfIterator31 = this._sortedEntries()[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep31 = __exactForOfIterator31.next();
					if (__exactForOfStep31.done) break;
					const [name, values] = __exactForOfStep31.value;
					if (name === "set-cookie") for (const v of values) result.push([name, v]);
					else result.push([name, values.join(", ")]);
				}
			}
			return result;
		}
		/**
		* Convert headers to a tuple array for native bridge.
		*/
		toTupleArray() {
			const headers = ensureHeadersBrand(this, "toTupleArray");
			const result = [];
			{
				const __exactForOfIterator32 = headers._map[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep32 = __exactForOfIterator32.next();
					if (__exactForOfStep32.done) break;
					const [name, values] = __exactForOfStep32.value;
					if (name === "set-cookie") for (const value of values) result.push([name, value]);
					else result.push([name, values.join(", ")]);
				}
			}
			return result;
		}
		[_Symbol$for$1]() {
			return formatHeadersForInspect(ensureHeadersBrand(this, "Symbol(nodejs.util.inspect.custom)")._flatSortedEntries());
		}
		/**
		* Create Headers from a tuple array (from native bridge).
		*/
		static fromTupleArray(tuples) {
			const headers = new Headers();
			{
				const __exactForOfIterator33 = tuples[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep33 = __exactForOfIterator33.next();
					if (__exactForOfStep33.done) break;
					const [name, value] = __exactForOfStep33.value;
					headers.append(name, value);
				}
			}
			return headers;
		}
	};
	Object.defineProperty(Headers, "name", {
		value: "Headers",
		configurable: true
	});
	Object.defineProperty(Headers.prototype, Symbol.toStringTag, {
		value: "Headers",
		configurable: true
	});
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/errors.ts
	init_defineProperty();
	/**
	* Fetch API Errors
	*
	* Custom error types for the Fetch API implementation.
	*/
	/**
	* Error thrown when a fetch request fails.
	*/
	var FetchError = class FetchError {
		constructor(message, options) {
			_defineProperty(this, "name", "FetchError");
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "stack", void 0);
			_defineProperty(this, "cause", void 0);
			this.message = `fetch failed: ${message}`;
			this.cause = options?.cause;
			this.stack = options?.stack ?? new Error(this.message).stack;
		}
		/**
		* Create a FetchError from an existing Error.
		*/
		static createFromError(error) {
			return new FetchError(error.message, {
				cause: error.cause,
				stack: error.stack
			});
		}
	};
	Object.setPrototypeOf(FetchError.prototype, Error.prototype);
	Object.setPrototypeOf(FetchError, Error);
	/**
	* Error thrown when a request is aborted.
	*/
	var AbortError = class {
		constructor(message = "The operation was aborted") {
			_defineProperty(this, "name", "AbortError");
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "stack", void 0);
			this.message = message;
			this.stack = new Error(this.message).stack;
		}
	};
	Object.setPrototypeOf(AbortError.prototype, Error.prototype);
	Object.setPrototypeOf(AbortError, Error);
	/**
	* Error thrown for network failures.
	*/
	var NetworkError = class {
		constructor(message = "Network request failed") {
			_defineProperty(this, "name", "NetworkError");
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "stack", void 0);
			this.message = message;
			this.stack = new Error(this.message).stack;
		}
	};
	Object.setPrototypeOf(NetworkError.prototype, Error.prototype);
	Object.setPrototypeOf(NetworkError, Error);
	/**
	* Error thrown for invalid URLs.
	*/
	var URLError = class {
		constructor(url) {
			_defineProperty(this, "name", "URLError");
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "stack", void 0);
			this.message = `Invalid URL: ${url}`;
			this.stack = new Error(this.message).stack;
		}
	};
	Object.setPrototypeOf(URLError.prototype, Error.prototype);
	Object.setPrototypeOf(URLError, Error);
	/**
	* Error thrown when body has already been consumed.
	* Extends TypeError per the Fetch spec.
	*/
	var BodyConsumedError = class {
		constructor() {
			_defineProperty(this, "name", "TypeError");
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "stack", void 0);
			this.message = "Body already used";
			this.stack = new TypeError(this.message).stack;
		}
	};
	Object.setPrototypeOf(BodyConsumedError.prototype, TypeError.prototype);
	Object.setPrototypeOf(BodyConsumedError, TypeError);
	//#endregion
	//#region ../ibex-runtime-js/src/streams/WritableStream.ts
	init_defineProperty();
	let _Symbol$toStringTag$35, _Symbol$toStringTag2$3, _Symbol$toStringTag3$1;
	/**
	* WritableStream - WHATWG Streams API Implementation
	*
	* @see https://streams.spec.whatwg.org/#ws-class
	*
	* This implementation supports:
	* - Underlying sink with start/write/close/abort
	* - Queuing strategy with backpressure
	* - WritableStreamDefaultWriter with ready/closed promises
	* - WritableStreamDefaultController with error()
	* - Proper state machine: writable -> closing -> closed, or errored
	* - Symbol.toStringTag
	*/
	const originalPromiseThen$2 = Promise.prototype.then;
	const originalPromiseResolve$2 = Promise.resolve.bind(Promise);
	const originalPromiseReject$2 = Promise.reject.bind(Promise);
	function promiseThen$2(promise, onFulfilled, onRejected) {
		return originalPromiseThen$2.call(promise, onFulfilled, onRejected);
	}
	function markPromiseHandled$1(promise) {
		originalPromiseThen$2.call(promise, void 0, () => {});
	}
	/** Create a deferred promise with exposed resolve/reject */
	function createDeferred$1() {
		const record = {
			promise: originalPromiseResolve$2(void 0),
			state: "pending",
			resolve: function(_value) {},
			reject: function(_reason) {}
		};
		record.promise = new Promise(function(res, rej) {
			record.resolve = function(value) {
				if (record.state !== "pending") return;
				record.state = "fulfilled";
				res(value);
			};
			record.reject = function(reason) {
				if (record.state !== "pending") return;
				record.state = "rejected";
				rej(reason);
			};
		});
		return record;
	}
	function createHandledRejectedPromise(reason) {
		const p = new Promise(function(_resolve, reject) {
			queueMicrotask(function() {
				reject(reason);
			});
		});
		markPromiseHandled$1(p);
		return p;
	}
	function createResolvedPromiseRecord(value) {
		return {
			promise: originalPromiseResolve$2(value),
			state: "fulfilled",
			resolve: function(_value) {},
			reject: function(_reason) {}
		};
	}
	function createRejectedPromiseRecord(reason) {
		return {
			promise: createHandledRejectedPromise(reason),
			state: "rejected",
			resolve: function(_value) {},
			reject: function(_reason) {}
		};
	}
	function createReleasedWriterError() {
		return /* @__PURE__ */ new TypeError("Writer has been released");
	}
	function isCallable$1(value) {
		return typeof value === "function";
	}
	function toNumber$1(value) {
		return Number(value);
	}
	function validateHighWaterMark$1(highWaterMark, defaultValue) {
		if (highWaterMark === void 0) return defaultValue;
		const normalized = toNumber$1(highWaterMark);
		if (Number.isNaN(normalized) || normalized < 0) throw new RangeError("The highWaterMark option must be a non-negative number");
		return normalized;
	}
	function validateQueuingStrategySize$1(value) {
		if (value === void 0) return function() {
			return 1;
		};
		if (!isCallable$1(value)) throw new TypeError("size must be a function");
		return value;
	}
	function validateSinkMethod(sink, member) {
		const method = sink[member];
		if (method !== void 0 && !isCallable$1(method)) throw new TypeError(`WritableStream underlying sink ${member} must be a function`);
	}
	_Symbol$toStringTag$35 = Symbol.toStringTag;
	var WritableStreamDefaultController = class {
		/** @internal */
		constructor() {
			_defineProperty(
				this,
				/** @internal */
				"_stream",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_abortReason",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_abortController",
				void 0
			);
			this._abortController = new AbortController();
		}
		get signal() {
			return this._abortController.signal;
		}
		error(e) {
			const stream = this._stream;
			if (stream === void 0) throw new TypeError("Controller has no associated stream");
			if (stream._state !== "writable") return;
			stream._errorStream(e);
		}
		get [_Symbol$toStringTag$35]() {
			return "WritableStreamDefaultController";
		}
	};
	_Symbol$toStringTag2$3 = Symbol.toStringTag;
	var WritableStreamDefaultWriter = class {
		/** @internal */
		_setClosedPromiseRecord(record) {
			this._closedPromiseRecord = record;
			this._closedPromise = record.promise;
			this._closedResolve = record.resolve;
			this._closedReject = record.reject;
		}
		/** @internal */
		_setReadyPromiseRecord(record) {
			this._readyPromiseRecord = record;
			this._readyPromise = record.promise;
			this._readyResolve = record.resolve;
			this._readyReject = record.reject;
		}
		/** @internal */
		_ensureClosedPromiseRejected(reason) {
			const record = this._closedPromiseRecord;
			if (record.state === "pending") {
				markPromiseHandled$1(record.promise);
				record.reject(reason);
				return;
			}
			this._setClosedPromiseRecord(createRejectedPromiseRecord(reason));
		}
		/** @internal */
		_ensureReadyPromiseRejected(reason) {
			const record = this._readyPromiseRecord;
			if (record.state === "pending") {
				markPromiseHandled$1(record.promise);
				record.reject(reason);
				return;
			}
			this._setReadyPromiseRecord(createRejectedPromiseRecord(reason));
		}
		constructor(stream) {
			_defineProperty(
				this,
				/** @internal */
				"_stream",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_releasedError",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedPromise",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedResolve",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedReject",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedPromiseRecord",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_readyPromise",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_readyResolve",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_readyReject",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_readyPromiseRecord",
				void 0
			);
			if (!(stream instanceof WritableStream$1)) throw new TypeError("WritableStreamDefaultWriter can only be constructed with a WritableStream instance");
			if (stream._writer !== void 0) throw new TypeError("This stream already has a writer");
			stream._writer = this;
			this._stream = stream;
			const state = stream._state;
			if (state === "writable") {
				this._setClosedPromiseRecord(createDeferred$1());
				if (stream._backpressure) this._setReadyPromiseRecord(createDeferred$1());
				else this._setReadyPromiseRecord(createResolvedPromiseRecord(void 0));
			} else if (state === "closing") {
				this._setReadyPromiseRecord(createResolvedPromiseRecord(void 0));
				this._setClosedPromiseRecord(createDeferred$1());
			} else if (state === "closed") {
				this._setClosedPromiseRecord(createResolvedPromiseRecord(void 0));
				this._setReadyPromiseRecord(createResolvedPromiseRecord(void 0));
			} else {
				const storedError = stream._storedError;
				this._setClosedPromiseRecord(createRejectedPromiseRecord(storedError));
				this._setReadyPromiseRecord(createRejectedPromiseRecord(storedError));
			}
		}
		get closed() {
			markPromiseHandled$1(this._closedPromise);
			return this._closedPromise;
		}
		get ready() {
			markPromiseHandled$1(this._readyPromise);
			return this._readyPromise;
		}
		get desiredSize() {
			if (this._stream === void 0) throw this._releasedError ?? createReleasedWriterError();
			const state = this._stream._state;
			if (state === "errored") return null;
			if (state === "closed" || state === "closing") return 0;
			return this._stream._strategyHWM - (this._stream._queueTotalSize + this._stream._inFlightWriteSize);
		}
		write(chunk) {
			if (this._stream === void 0) return createHandledRejectedPromise(this._releasedError ?? createReleasedWriterError());
			if (this._stream._state !== "writable") {
				if (this._stream._state === "errored") return createHandledRejectedPromise(this._stream._storedError);
				return createHandledRejectedPromise(/* @__PURE__ */ new TypeError("The stream is not in the writable state and cannot be written to"));
			}
			return this._stream._writeChunk(chunk);
		}
		close() {
			if (this._stream === void 0) return createHandledRejectedPromise(this._releasedError ?? createReleasedWriterError());
			const stream = this._stream;
			if (stream._state !== "writable") {
				if (stream._state === "closed") return createHandledRejectedPromise(/* @__PURE__ */ new TypeError("The stream is already closed"));
				if (stream._state === "closing") return createHandledRejectedPromise(/* @__PURE__ */ new TypeError("The stream is already closing"));
				return createHandledRejectedPromise(/* @__PURE__ */ new TypeError("The stream is not writable"));
			}
			const p = stream._closeStream();
			markPromiseHandled$1(p);
			return p;
		}
		abort(reason) {
			if (this._stream === void 0) return createHandledRejectedPromise(this._releasedError ?? createReleasedWriterError());
			const p = this._stream._abortStream(reason);
			markPromiseHandled$1(p);
			return p;
		}
		releaseLock() {
			if (this._stream === void 0) return;
			const stream = this._stream;
			const releasedError = createReleasedWriterError();
			this._releasedError = releasedError;
			this._ensureReadyPromiseRejected(releasedError);
			this._ensureClosedPromiseRejected(releasedError);
			stream._writer = void 0;
			this._stream = void 0;
		}
		get [_Symbol$toStringTag2$3]() {
			return "WritableStreamDefaultWriter";
		}
	};
	_Symbol$toStringTag3$1 = Symbol.toStringTag;
	var WritableStream$1 = class {
		constructor(underlyingSink, strategy) {
			_defineProperty(
				this,
				/** @internal */
				"_state",
				"writable"
			);
			_defineProperty(
				this,
				/** @internal */
				"_storedError",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_writer",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_controller",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_writeAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closeAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_abortAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_started",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_writing",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_inFlightWriteRequest",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_inFlightWriteSize",
				0
			);
			_defineProperty(
				this,
				/** @internal */
				"_inFlightCloseRequest",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_writeRequests",
				[]
			);
			_defineProperty(
				this,
				/** @internal */
				"_pendingAbortRequest",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_backpressure",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_aborting",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_strategyHWM",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_strategySizeAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_queue",
				[]
			);
			_defineProperty(
				this,
				/** @internal */
				"_queueTotalSize",
				0
			);
			const sink = underlyingSink === void 0 ? {} : Object(underlyingSink);
			const sinkAsAny = sink;
			if (sink.type !== void 0) throw new TypeError("Unsupported underlying sink type");
			const strat = strategy === void 0 ? {} : Object(strategy);
			if (sink.start !== void 0) validateSinkMethod(sink, "start");
			if (sink.write !== void 0) validateSinkMethod(sink, "write");
			if (sink.close !== void 0) validateSinkMethod(sink, "close");
			if (sink.abort !== void 0) validateSinkMethod(sink, "abort");
			this._controller = new WritableStreamDefaultController();
			this._controller._stream = this;
			this._strategyHWM = validateHighWaterMark$1(strat.highWaterMark, 1);
			this._strategySizeAlgorithm = validateQueuingStrategySize$1(strat.size);
			this._writeAlgorithm = sink.write ? function(chunk, controller) {
				try {
					return originalPromiseResolve$2(sinkAsAny.write(chunk, controller));
				} catch (e) {
					return createHandledRejectedPromise(e);
				}
			} : void 0;
			this._closeAlgorithm = sink.close ? function() {
				try {
					return originalPromiseResolve$2(sinkAsAny.close());
				} catch (e) {
					return createHandledRejectedPromise(e);
				}
			} : void 0;
			this._abortAlgorithm = sink.abort ? function(reason) {
				try {
					return originalPromiseResolve$2(sinkAsAny.abort(reason));
				} catch (e) {
					return createHandledRejectedPromise(e);
				}
			} : void 0;
			this._updateBackpressure();
			const self = this;
			if (sink.start) {
				let startResult;
				try {
					startResult = sinkAsAny.start(this._controller);
				} catch (startError) {
					promiseThen$2(originalPromiseResolve$2(), function() {
						self._started = true;
						self._errorStream(startError);
					});
					return;
				}
				promiseThen$2(originalPromiseResolve$2(startResult), function() {
					self._started = true;
					self._advanceQueueIfNeeded();
				}, function(e) {
					self._started = true;
					self._errorStream(e);
				});
			} else promiseThen$2(originalPromiseResolve$2(), function() {
				self._started = true;
				self._advanceQueueIfNeeded();
			});
		}
		get locked() {
			return this._writer !== void 0;
		}
		async abort(reason) {
			if (this.locked) throw new TypeError("Cannot abort a locked stream");
			return await this._abortStream(reason);
		}
		async close() {
			if (this.locked) throw new TypeError("Cannot close a locked stream");
			if (this._state !== "writable") {
				if (this._state === "closed") throw new TypeError("The stream is already closed");
				throw new TypeError("The stream is not writable");
			}
			return await this._closeStream();
		}
		getWriter() {
			return new WritableStreamDefaultWriter(this);
		}
		get [_Symbol$toStringTag3$1]() {
			return "WritableStream";
		}
		/** @internal */
		_writeChunk(chunk) {
			let size;
			try {
				const sizeAlgorithm = this._strategySizeAlgorithm;
				size = toNumber$1(sizeAlgorithm(chunk));
			} catch (error) {
				return originalPromiseReject$2(error);
			}
			if (Number.isNaN(size) || !Number.isFinite(size) || size < 0) return originalPromiseReject$2(/* @__PURE__ */ new RangeError("The size returned by the size() algorithm must be a non-negative finite number"));
			const self = this;
			const p = new Promise(function(resolve, reject) {
				self._writeRequests.push({
					chunk,
					resolve,
					reject
				});
				self._queue.push({
					chunk,
					size
				});
				self._queueTotalSize += size;
				self._updateBackpressure();
				self._advanceQueueIfNeeded();
			});
			markPromiseHandled$1(p);
			return p;
		}
		/** @internal */
		_closeStream() {
			this._state = "closing";
			const self = this;
			return new Promise(function(resolve, reject) {
				self._inFlightCloseRequest = {
					resolve,
					reject
				};
				self._advanceQueueIfNeeded();
			});
		}
		/** @internal */
		async _abortStream(reason) {
			if (this._state === "closed" || this._state === "errored") return;
			if (this._aborting) return;
			this._aborting = true;
			this._controller._abortReason = reason;
			this._controller._abortController.abort(reason);
			if (this._state === "closing") {
				const inFlightCloseRequest = this._inFlightCloseRequest;
				if (inFlightCloseRequest) {
					inFlightCloseRequest.reject(reason);
					this._inFlightCloseRequest = void 0;
				}
			}
			const inFlightWriteRequest = this._inFlightWriteRequest;
			if (inFlightWriteRequest) {
				inFlightWriteRequest.reject(reason);
				this._inFlightWriteRequest = void 0;
			}
			this._inFlightWriteSize = 0;
			this._writing = false;
			for (var i = 0; i < this._writeRequests.length; i++) this._writeRequests[i].reject(reason);
			this._writeRequests = [];
			this._queue = [];
			this._queueTotalSize = 0;
			if (this._abortAlgorithm) try {
				await this._abortAlgorithm(reason);
			} catch (e) {
				this._aborting = false;
				this._state = "errored";
				this._storedError = e;
				this._notifyWriterError(e);
				throw e;
			}
			this._aborting = false;
			this._state = "errored";
			this._storedError = reason;
			this._notifyWriterError(reason);
		}
		/** @internal */
		_errorStream(e) {
			if (this._state !== "writable" && this._state !== "closing") return;
			this._state = "errored";
			this._storedError = e;
			const inFlightWriteRequest = this._inFlightWriteRequest;
			if (inFlightWriteRequest) {
				inFlightWriteRequest.reject(e);
				this._inFlightWriteRequest = void 0;
			}
			this._inFlightWriteSize = 0;
			this._writing = false;
			for (var i = 0; i < this._writeRequests.length; i++) this._writeRequests[i].reject(e);
			this._writeRequests = [];
			this._queue = [];
			this._queueTotalSize = 0;
			const inFlightClose = this._inFlightCloseRequest;
			if (inFlightClose) {
				inFlightClose.reject(e);
				this._inFlightCloseRequest = void 0;
			}
			this._notifyWriterError(e);
		}
		/** @internal */
		_notifyWriterError(e) {
			const writer = this._writer;
			if (writer) {
				writer._ensureReadyPromiseRejected(e);
				writer._ensureClosedPromiseRejected(e);
			}
		}
		/** @internal */
		_advanceQueueIfNeeded() {
			if (!this._started) return;
			if (this._writing) return;
			if (this._aborting) return;
			if (this._state === "errored") return;
			if (this._state === "closing" && this._queue.length === 0) {
				this._finishClose();
				return;
			}
			if (this._queue.length === 0) return;
			if (this._state !== "writable" && this._state !== "closing") return;
			const entry = this._queue.shift();
			this._queueTotalSize -= entry.size;
			const writeRequest = this._writeRequests.shift();
			this._inFlightWriteRequest = writeRequest;
			this._inFlightWriteSize = entry.size;
			this._writing = true;
			const self = this;
			const writeAlgorithm = this._writeAlgorithm;
			promiseThen$2(writeAlgorithm ? writeAlgorithm(entry.chunk, this._controller) : originalPromiseResolve$2(), function() {
				self._writing = false;
				self._inFlightWriteSize = 0;
				const inFlightWriteRequest = self._inFlightWriteRequest;
				self._inFlightWriteRequest = void 0;
				if (inFlightWriteRequest) inFlightWriteRequest.resolve();
				self._updateBackpressure();
				self._advanceQueueIfNeeded();
			}, function(reason) {
				self._writing = false;
				self._inFlightWriteSize = 0;
				const inFlightWriteRequest = self._inFlightWriteRequest;
				self._inFlightWriteRequest = void 0;
				if (inFlightWriteRequest) inFlightWriteRequest.reject(reason);
				self._errorStream(reason);
			});
		}
		/** @internal */
		_finishClose() {
			const closeAlgorithm = this._closeAlgorithm;
			let closePromise;
			try {
				closePromise = closeAlgorithm ? closeAlgorithm() : originalPromiseResolve$2();
			} catch (e) {
				closePromise = originalPromiseReject$2(e);
			}
			const self = this;
			promiseThen$2(closePromise, function() {
				if (self._state !== "closing") return;
				self._state = "closed";
				const closeRequest = self._inFlightCloseRequest;
				if (closeRequest) {
					closeRequest.resolve();
					self._inFlightCloseRequest = void 0;
				}
				const writer = self._writer;
				if (writer) writer._closedResolve?.(void 0);
			}, function(e) {
				if (self._state !== "closing") return;
				self._state = "errored";
				self._storedError = e;
				const closeRequest = self._inFlightCloseRequest;
				if (closeRequest) {
					closeRequest.reject(e);
					self._inFlightCloseRequest = void 0;
				}
				self._notifyWriterError(e);
			});
		}
		/** @internal */
		_updateBackpressure() {
			const backpressure = this._strategyHWM - (this._queueTotalSize + this._inFlightWriteSize) <= 0;
			if (backpressure !== this._backpressure) {
				this._backpressure = backpressure;
				const writer = this._writer;
				if (writer) {
					if (backpressure) {
						if (writer._readyPromiseRecord.state !== "pending") writer._setReadyPromiseRecord(createDeferred$1());
					} else if (writer._readyPromiseRecord.state === "pending") writer._readyResolve?.(void 0);
				}
			}
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/Event.ts
	init_defineProperty();
	const _isTrustedGetter = function() {
		return false;
	};
	var Event = class Event {
		constructor(type, eventInitDict) {
			_defineProperty(this, "type", void 0);
			_defineProperty(this, "bubbles", void 0);
			_defineProperty(this, "cancelable", void 0);
			_defineProperty(this, "composed", void 0);
			_defineProperty(this, "timeStamp", void 0);
			_defineProperty(this, "_target", null);
			_defineProperty(this, "_currentTarget", null);
			_defineProperty(this, "_eventPhase", Event.NONE);
			_defineProperty(this, "_defaultPrevented", false);
			_defineProperty(this, "_stopPropagationFlag", false);
			_defineProperty(this, "_stopImmediatePropagationFlag", false);
			_defineProperty(this, "_inPassiveListener", false);
			this.type = type;
			this.bubbles = eventInitDict?.bubbles ?? false;
			this.cancelable = eventInitDict?.cancelable ?? false;
			this.composed = eventInitDict?.composed ?? false;
			this.timeStamp = performance?.now?.() ?? Date.now();
			Object.defineProperty(this, "isTrusted", {
				get: _isTrustedGetter,
				configurable: false,
				enumerable: true
			});
		}
		get target() {
			return this._target;
		}
		get currentTarget() {
			return this._currentTarget;
		}
		get eventPhase() {
			return this._eventPhase;
		}
		get defaultPrevented() {
			return this._defaultPrevented;
		}
		preventDefault() {
			if (this.cancelable && !this._inPassiveListener) this._defaultPrevented = true;
		}
		stopPropagation() {
			this._stopPropagationFlag = true;
		}
		stopImmediatePropagation() {
			this._stopPropagationFlag = true;
			this._stopImmediatePropagationFlag = true;
		}
		composedPath() {
			if (this._target) return [this._target];
			return [];
		}
		/** @internal */
		_setTarget(target) {
			this._target = target;
		}
		/** @internal */
		_setCurrentTarget(currentTarget) {
			this._currentTarget = currentTarget;
		}
		/** @internal */
		_setEventPhase(phase) {
			this._eventPhase = phase;
		}
		/** @internal */
		_isPropagationStopped() {
			return this._stopPropagationFlag;
		}
		/** @internal */
		_isImmediatePropagationStopped() {
			return this._stopImmediatePropagationFlag;
		}
		/** @internal */
		_resetFlags() {
			this._stopPropagationFlag = false;
			this._stopImmediatePropagationFlag = false;
			this._defaultPrevented = false;
			this._eventPhase = Event.NONE;
			this._inPassiveListener = false;
		}
		/** @internal */
		_setInPassiveListener(passive) {
			this._inPassiveListener = passive;
		}
	};
	_defineProperty(Event, "NONE", 0);
	_defineProperty(Event, "CAPTURING_PHASE", 1);
	_defineProperty(Event, "AT_TARGET", 2);
	_defineProperty(Event, "BUBBLING_PHASE", 3);
	var CustomEvent = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "detail", void 0);
			this.detail = eventInitDict?.detail;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/EventTarget.ts
	/**
	* EventTarget - Web Standard EventTarget Implementation
	*
	* @see https://dom.spec.whatwg.org/#interface-eventtarget
	*/
	init_defineProperty();
	var EventTarget = class {
		constructor() {
			_defineProperty(this, "_listeners", /* @__PURE__ */ new Map());
		}
		compactListeners(type) {
			const listeners = this._listeners.get(type);
			if (!listeners) return;
			const activeListeners = listeners.filter((l) => !l.removed);
			if (activeListeners.length === 0) this._listeners.delete(type);
			else if (activeListeners.length !== listeners.length) this._listeners.set(type, activeListeners);
		}
		addEventListener(type, callback, options) {
			if (callback === null) return;
			const capture = typeof options === "boolean" ? options : options?.capture ?? false;
			const once = typeof options === "object" ? options?.once ?? false : false;
			const passive = typeof options === "object" ? options?.passive ?? false : false;
			const signal = typeof options === "object" ? options?.signal : void 0;
			if (signal?.aborted) return;
			let listeners = this._listeners.get(type);
			if (!listeners) {
				listeners = [];
				this._listeners.set(type, listeners);
			}
			if (listeners.some((l) => l.callback === callback && l.capture === capture && !l.removed)) return;
			const entry = {
				callback,
				capture,
				once,
				passive,
				removed: false,
				signal
			};
			listeners.push(entry);
			if (signal) {
				const removeOnAbort = () => {
					entry.removed = true;
					this.compactListeners(type);
				};
				signal.addEventListener("abort", removeOnAbort, { once: true });
			}
		}
		removeEventListener(type, callback, options) {
			if (callback === null) return;
			const capture = typeof options === "boolean" ? options : options?.capture ?? false;
			const listeners = this._listeners.get(type);
			if (!listeners) return;
			const index = listeners.findIndex((l) => l.callback === callback && l.capture === capture && !l.removed);
			if (index !== -1) {
				listeners[index].removed = true;
				this.compactListeners(type);
			}
		}
		dispatchEvent(event) {
			const dispatchEvent = this.normalizeDispatchEventArgument(event);
			dispatchEvent._setTarget(this);
			dispatchEvent._setCurrentTarget(this);
			dispatchEvent._setEventPhase(Event.AT_TARGET);
			const listeners = this._listeners.get(dispatchEvent.type);
			if (listeners) {
				const listenersCopy = [...listeners];
				for (const entry of listenersCopy) {
					if (entry.removed) continue;
					if (dispatchEvent._isImmediatePropagationStopped()) break;
					if (entry.once) entry.removed = true;
					dispatchEvent._setInPassiveListener(entry.passive);
					try {
						if (typeof entry.callback === "function") entry.callback.call(this, dispatchEvent);
						else entry.callback.handleEvent(dispatchEvent);
					} catch (error) {
						console.error("Error in event listener:", error);
					} finally {
						dispatchEvent._setInPassiveListener(false);
					}
				}
				this.compactListeners(dispatchEvent.type);
			}
			dispatchEvent._setCurrentTarget(null);
			dispatchEvent._setEventPhase(Event.NONE);
			const result = !dispatchEvent.defaultPrevented;
			dispatchEvent._resetFlags();
			return result;
		}
		normalizeDispatchEventArgument(event) {
			if (!event) throw new TypeError("Failed to execute 'dispatchEvent' on 'EventTarget': parameter 1 is not of type 'Event'.");
			if (typeof event === "string") event = new Event(event);
			if (typeof event._setTarget === "function" && typeof event._setCurrentTarget === "function" && typeof event._setEventPhase === "function" && typeof event._resetFlags === "function") return event;
			if (typeof event.type !== "string") throw new TypeError("Failed to execute 'dispatchEvent' on 'EventTarget': parameter 1 is not of type 'Event'.");
			const source = event;
			const wrapped = new Event(source.type, {
				bubbles: source.bubbles ?? false,
				cancelable: source.cancelable ?? false,
				composed: source.composed ?? false
			});
			for (const key of Object.keys(source)) {
				if (key === "type" || key === "bubbles" || key === "cancelable" || key === "composed" || key === "defaultPrevented" || key === "isTrusted" || key === "timeStamp" || key === "target" || key === "currentTarget" || key === "eventPhase" || key === "detail") continue;
				const wrappedDescriptor = Object.getOwnPropertyDescriptor(wrapped, key) || Object.getOwnPropertyDescriptor(Object.getPrototypeOf(wrapped), key);
				if (wrappedDescriptor && (wrappedDescriptor.writable === false && wrappedDescriptor.set === void 0 || wrappedDescriptor.get !== void 0)) continue;
				wrapped[key] = source[key];
			}
			return wrapped;
		}
		/**
		* Helper to check if there are any listeners for a type
		*/
		hasListeners(type) {
			const listeners = this._listeners.get(type);
			return listeners ? listeners.some((l) => !l.removed) : false;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/MessageEvent.ts
	/**
	* MessageEvent - Event for messaging APIs
	* 
	* Used by WebSocket, MessagePort, BroadcastChannel, and postMessage.
	* @see https://html.spec.whatwg.org/multipage/comms.html#messageevent
	*/
	init_defineProperty();
	let _Symbol$toStringTag$34;
	_Symbol$toStringTag$34 = Symbol.toStringTag;
	var MessageEvent$1 = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "data", void 0);
			_defineProperty(this, "origin", void 0);
			_defineProperty(this, "lastEventId", void 0);
			_defineProperty(this, "source", void 0);
			_defineProperty(this, "ports", void 0);
			this.data = eventInitDict?.data;
			this.origin = eventInitDict?.origin ?? "";
			this.lastEventId = eventInitDict?.lastEventId ?? "";
			this.source = eventInitDict?.source ?? null;
			this.ports = Object.freeze(eventInitDict?.ports ? [...eventInitDict.ports] : []);
		}
		/**
		* Legacy method for initializing a MessageEvent.
		* @deprecated Use the constructor instead.
		*/
		initMessageEvent(type, bubbles, cancelable, data, origin, lastEventId, source, ports) {}
		get [_Symbol$toStringTag$34]() {
			return "MessageEvent";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/CloseEvent.ts
	/**
	* CloseEvent - Event for WebSocket close events
	* 
	* @see https://html.spec.whatwg.org/multipage/web-sockets.html#closeevent
	*/
	init_defineProperty();
	let _Symbol$toStringTag$33;
	function assertCloseEventBrand(value) {
		if (!(value instanceof CloseEvent)) throw new TypeError("Illegal invocation");
		return value;
	}
	_Symbol$toStringTag$33 = Symbol.toStringTag;
	var CloseEvent = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "_wasClean", void 0);
			_defineProperty(this, "_code", void 0);
			_defineProperty(this, "_reason", void 0);
			this._wasClean = eventInitDict?.wasClean ?? false;
			this._code = eventInitDict?.code ?? 0;
			this._reason = eventInitDict?.reason ?? "";
		}
		get wasClean() {
			return assertCloseEventBrand(this)._wasClean;
		}
		get code() {
			return assertCloseEventBrand(this)._code;
		}
		get reason() {
			return assertCloseEventBrand(this)._reason;
		}
		get [_Symbol$toStringTag$33]() {
			return "CloseEvent";
		}
	};
	Object.defineProperty(CloseEvent, "length", {
		value: 1,
		configurable: true
	});
	function nameAccessor$1(descriptor, propertyName) {
		if (!descriptor) return descriptor;
		if (typeof descriptor.get === "function") Object.defineProperty(descriptor.get, "name", {
			value: `get ${propertyName}`,
			configurable: true
		});
		if (typeof descriptor.set === "function") Object.defineProperty(descriptor.set, "name", {
			value: `set ${propertyName}`,
			configurable: true
		});
		return descriptor;
	}
	const wasCleanDescriptor = nameAccessor$1(Object.getOwnPropertyDescriptor(CloseEvent.prototype, "wasClean"), "wasClean");
	const codeDescriptor = nameAccessor$1(Object.getOwnPropertyDescriptor(CloseEvent.prototype, "code"), "code");
	const reasonDescriptor = nameAccessor$1(Object.getOwnPropertyDescriptor(CloseEvent.prototype, "reason"), "reason");
	Object.defineProperties(CloseEvent.prototype, {
		wasClean: {
			get: wasCleanDescriptor.get,
			enumerable: true,
			configurable: true
		},
		code: {
			get: codeDescriptor.get,
			enumerable: true,
			configurable: true
		},
		reason: {
			get: reasonDescriptor.get,
			enumerable: true,
			configurable: true
		}
	});
	Object.setPrototypeOf(CloseEvent.prototype, Event.prototype);
	Object.setPrototypeOf(CloseEvent, Event);
	//#endregion
	//#region ../ibex-runtime-js/src/events/ErrorEvent.ts
	/**
	* ErrorEvent - Event for runtime errors
	* 
	* Used for reporting script errors and Worker errors.
	* @see https://html.spec.whatwg.org/multipage/webappapis.html#errorevent
	*/
	init_defineProperty();
	let _Symbol$toStringTag$32;
	_Symbol$toStringTag$32 = Symbol.toStringTag;
	var ErrorEvent = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "message", void 0);
			_defineProperty(this, "filename", void 0);
			_defineProperty(this, "lineno", void 0);
			_defineProperty(this, "colno", void 0);
			_defineProperty(this, "error", void 0);
			this.message = eventInitDict?.message ?? "";
			this.filename = eventInitDict?.filename ?? "";
			this.lineno = eventInitDict?.lineno ?? 0;
			this.colno = eventInitDict?.colno ?? 0;
			this.error = eventInitDict?.error ?? null;
		}
		get [_Symbol$toStringTag$32]() {
			return "ErrorEvent";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/ProgressEvent.ts
	/**
	* ProgressEvent - Event for tracking progress of operations
	* 
	* Used by XMLHttpRequest, FileReader, and fetch progress tracking.
	* @see https://xhr.spec.whatwg.org/#interface-progressevent
	*/
	init_defineProperty();
	let _Symbol$toStringTag$31;
	_Symbol$toStringTag$31 = Symbol.toStringTag;
	var ProgressEvent = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "lengthComputable", void 0);
			_defineProperty(this, "loaded", void 0);
			_defineProperty(this, "total", void 0);
			this.lengthComputable = eventInitDict?.lengthComputable ?? false;
			this.loaded = eventInitDict?.loaded ?? 0;
			this.total = eventInitDict?.total ?? 0;
		}
		get [_Symbol$toStringTag$31]() {
			return "ProgressEvent";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/KeyboardEvent.ts
	/**
	* KeyboardEvent - Minimal DOM-compatible keyboard event implementation.
	*
	* The DOM shim only needs the standard readonly keyboard fields that framework
	* event handlers commonly read (`key`, `code`, and modifier flags). The base
	* Event implementation already handles cancelation and dispatch bookkeeping.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$30;
	_Symbol$toStringTag$30 = Symbol.toStringTag;
	var KeyboardEvent = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "key", void 0);
			_defineProperty(this, "code", void 0);
			_defineProperty(this, "altKey", void 0);
			_defineProperty(this, "ctrlKey", void 0);
			_defineProperty(this, "metaKey", void 0);
			_defineProperty(this, "shiftKey", void 0);
			_defineProperty(this, "repeat", void 0);
			this.key = eventInitDict?.key ?? "";
			this.code = eventInitDict?.code ?? "";
			this.altKey = eventInitDict?.altKey === true;
			this.ctrlKey = eventInitDict?.ctrlKey === true;
			this.metaKey = eventInitDict?.metaKey === true;
			this.shiftKey = eventInitDict?.shiftKey === true;
			this.repeat = eventInitDict?.repeat === true;
		}
		get [_Symbol$toStringTag$30]() {
			return "KeyboardEvent";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/FocusEvent.ts
	/**
	* FocusEvent - Minimal DOM-compatible focus event implementation.
	*
	* The DOM shim uses this for focus/blur/focusin/focusout payload wrapping so
	* handlers can read `relatedTarget` without special Exact-specific branching.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$29;
	_Symbol$toStringTag$29 = Symbol.toStringTag;
	var FocusEvent = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "relatedTarget", void 0);
			this.relatedTarget = eventInitDict?.relatedTarget ?? null;
		}
		get [_Symbol$toStringTag$29]() {
			return "FocusEvent";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/events/PromiseRejectionEvent.ts
	/**
	* PromiseRejectionEvent - Event for unhandled promise rejections
	*
	* Fired when a Promise is rejected and no rejection handler is attached,
	* or when a previously unhandled rejection becomes handled.
	* @see https://html.spec.whatwg.org/multipage/webappapis.html#promiserejectionevent
	*/
	init_defineProperty();
	let _Symbol$toStringTag$28;
	_Symbol$toStringTag$28 = Symbol.toStringTag;
	var PromiseRejectionEvent = class extends Event {
		constructor(type, eventInitDict) {
			super(type, eventInitDict);
			_defineProperty(this, "promise", void 0);
			_defineProperty(this, "reason", void 0);
			this.promise = eventInitDict.promise;
			this.reason = "reason" in eventInitDict ? eventInitDict.reason : void 0;
		}
		get [_Symbol$toStringTag$28]() {
			return "PromiseRejectionEvent";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/abort/AbortSignal.ts
	/**
	* AbortSignal - Web Standard AbortSignal Implementation
	*
	* @see https://dom.spec.whatwg.org/#interface-abortsignal
	*/
	init_defineProperty();
	const ABORT_SIGNAL_BRAND = Symbol.for("@exact/AbortSignal");
	function isAbortSignal(value) {
		if (typeof value !== "object" || value === null) return false;
		if (value[ABORT_SIGNAL_BRAND] === true) return true;
		try {
			return typeof value.aborted === "boolean" && "reason" in value && typeof value.addEventListener === "function" && typeof value.removeEventListener === "function";
		} catch {
			return false;
		}
	}
	var AbortSignal = class AbortSignal extends EventTarget {
		constructor() {
			super();
			_defineProperty(this, ABORT_SIGNAL_BRAND, true);
			_defineProperty(this, "_aborted", false);
			_defineProperty(this, "_reason", void 0);
			_defineProperty(this, "onabort", null);
		}
		get aborted() {
			return this._aborted;
		}
		get reason() {
			return this._reason;
		}
		throwIfAborted() {
			if (this._aborted) throw this._reason;
		}
		/**
		* Abort the signal
		* @internal - Called by AbortController
		*/
		_abort(reason) {
			if (this._aborted) return;
			this._aborted = true;
			this._reason = reason === void 0 ? new DOMException$6("The operation was aborted.", "AbortError") : reason;
			const event = new Event("abort");
			if (this.onabort) try {
				this.onabort.call(this, event);
			} catch (e) {
				console.error("Error in onabort handler:", e);
			}
			this.dispatchEvent(event);
		}
		/**
		* Create an already-aborted signal
		*/
		static abort(reason) {
			const signal = new AbortSignal();
			signal._abort(reason);
			return signal;
		}
		/**
		* Create a signal that aborts after a timeout
		*/
		static timeout(milliseconds) {
			const signal = new AbortSignal();
			setTimeout(() => {
				signal._abort(new DOMException$6("The operation timed out.", "TimeoutError"));
			}, milliseconds);
			return signal;
		}
		/**
		* Create a signal that aborts when any of the given signals abort
		*/
		static any(signals) {
			const signal = new AbortSignal();
			for (const s of signals) if (s.aborted) {
				signal._abort(s.reason);
				return signal;
			}
			const listeners = /* @__PURE__ */ new Map();
			const cleanup = () => {
				{
					const __exactForOfIterator34 = listeners[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep34 = __exactForOfIterator34.next();
						if (__exactForOfStep34.done) break;
						const [source, listener] = __exactForOfStep34.value;
						source.removeEventListener("abort", listener);
					}
				}
				listeners.clear();
			};
			const onAbort = () => {
				for (const s of signals) if (s.aborted) {
					cleanup();
					signal._abort(s.reason);
					break;
				}
			};
			{
				const __exactForOfIterator35 = signals[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep35 = __exactForOfIterator35.next();
					if (__exactForOfStep35.done) break;
					const s = __exactForOfStep35.value;
					listeners.set(s, onAbort);
					s.addEventListener("abort", onAbort, { once: true });
				}
			}
			return signal;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/arraybuffer-detach.ts
	const DETACHED_ARRAY_BUFFERS = Symbol.for("exact.detachedArrayBuffers");
	const NON_TRANSFERABLE_ARRAY_BUFFERS = Symbol.for("exact.nonTransferableArrayBuffers");
	const PATCHED_ARRAY_BUFFER_BYTE_LENGTH = Symbol.for("exact.patchedArrayBufferByteLength");
	function getDetachedArrayBuffers() {
		if (typeof WeakSet !== "function") return null;
		const globalObject = globalThis;
		let detached = globalObject[DETACHED_ARRAY_BUFFERS];
		if (!detached) {
			detached = /* @__PURE__ */ new WeakSet();
			globalObject[DETACHED_ARRAY_BUFFERS] = detached;
		}
		return detached;
	}
	function getNonTransferableArrayBuffers() {
		if (typeof WeakSet !== "function") return null;
		const globalObject = globalThis;
		let buffers = globalObject[NON_TRANSFERABLE_ARRAY_BUFFERS];
		if (!buffers) {
			buffers = /* @__PURE__ */ new WeakSet();
			globalObject[NON_TRANSFERABLE_ARRAY_BUFFERS] = buffers;
		}
		return buffers;
	}
	function installDetachedArrayBufferByteLengthPatch() {
		const proto = ArrayBuffer.prototype;
		if (proto[PATCHED_ARRAY_BUFFER_BYTE_LENGTH]) return;
		const descriptor = Object.getOwnPropertyDescriptor(ArrayBuffer.prototype, "byteLength");
		if (!descriptor?.get) return;
		try {
			Object.defineProperty(ArrayBuffer.prototype, "byteLength", {
				get: function byteLength() {
					if (getDetachedArrayBuffers()?.has(this)) return 0;
					return descriptor.get.call(this);
				},
				enumerable: descriptor.enumerable ?? false,
				configurable: descriptor.configurable ?? true
			});
			Object.defineProperty(proto, PATCHED_ARRAY_BUFFER_BYTE_LENGTH, {
				value: true,
				configurable: true
			});
		} catch (_error) {}
	}
	installDetachedArrayBufferByteLengthPatch();
	function isDetachedArrayBuffer(buffer) {
		if (getDetachedArrayBuffers()?.has(buffer)) return true;
		if (buffer.byteLength !== 0) return false;
		try {
			new Uint8Array(buffer);
			return false;
		} catch (_error) {
			return true;
		}
	}
	function markDetachedArrayBuffer(buffer) {
		if (buffer.byteLength > 0) new Uint8Array(buffer).fill(0);
		getDetachedArrayBuffers()?.add(buffer);
		try {
			Object.defineProperty(buffer, "byteLength", {
				value: 0,
				writable: false,
				enumerable: false,
				configurable: true
			});
		} catch (_error) {}
	}
	function isNonTransferableArrayBuffer(buffer) {
		return getNonTransferableArrayBuffers()?.has(buffer) ?? false;
	}
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/checkPrivateRedeclaration.js
	function _checkPrivateRedeclaration(e, t) {
		if (t.has(e)) throw new TypeError("Cannot initialize the same private elements twice on an object");
	}
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/classPrivateMethodInitSpec.js
	function _classPrivateMethodInitSpec(e, a) {
		_checkPrivateRedeclaration(e, a), a.add(e);
	}
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/classPrivateFieldInitSpec.js
	function _classPrivateFieldInitSpec(e, t, a) {
		_checkPrivateRedeclaration(e, t), t.set(e, a);
	}
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/assertClassBrand.js
	function _assertClassBrand(e, t, n) {
		if ("function" == typeof e ? e === t : e.has(t)) return arguments.length < 3 ? t : n;
		throw new TypeError("Private element is not present on this object");
	}
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/classPrivateFieldSet2.js
	function _classPrivateFieldSet2(s, a, r) {
		return s.set(_assertClassBrand(s, a), r), r;
	}
	//#endregion
	//#region \0@oxc-project+runtime@0.135.0/helpers/esm/classPrivateFieldGet2.js
	function _classPrivateFieldGet2(s, a) {
		return s.get(_assertClassBrand(s, a));
	}
	//#endregion
	//#region ../ibex-runtime-js/src/blob/Blob.ts
	/**
	* Blob implementation for Exact runtime
	* 
	* Implements the WHATWG Blob API for handling binary data.
	* https://w3c.github.io/FileAPI/#blob-section
	*/
	let _Symbol$toStringTag$27;
	var _parts = /* @__PURE__ */ new WeakMap();
	var _type = /* @__PURE__ */ new WeakMap();
	var _size = /* @__PURE__ */ new WeakMap();
	var _Blob_brand = /* @__PURE__ */ new WeakSet();
	_Symbol$toStringTag$27 = Symbol.toStringTag;
	/**
	* Blob represents immutable raw binary data.
	*/
	var Blob$1 = class Blob$1 {
		constructor(blobParts, options) {
			_classPrivateMethodInitSpec(this, _Blob_brand);
			_classPrivateFieldInitSpec(this, _parts, void 0);
			_classPrivateFieldInitSpec(this, _type, void 0);
			_classPrivateFieldInitSpec(this, _size, void 0);
			_classPrivateFieldSet2(_parts, this, []);
			_classPrivateFieldSet2(_type, this, normalizeType(options?.type ?? ""));
			const endings = options?.endings ?? "transparent";
			if (blobParts) {
				const parts = Array.from(blobParts);
				for (let i = 0; i < parts.length; i++) _classPrivateFieldGet2(_parts, this).push(convertToBytes(parts[i], endings));
			}
			_classPrivateFieldSet2(_size, this, _classPrivateFieldGet2(_parts, this).reduce((acc, part) => acc + part.byteLength, 0));
		}
		/**
		* The size of the Blob in bytes.
		*/
		get size() {
			return _classPrivateFieldGet2(_size, this);
		}
		/**
		* The MIME type of the Blob.
		*/
		get type() {
			return _classPrivateFieldGet2(_type, this);
		}
		/**
		* Returns a new Blob containing a subset of this Blob's data.
		*/
		slice(start, end, contentType) {
			const size = _classPrivateFieldGet2(_size, this);
			let relativeStart;
			if (start === void 0) relativeStart = 0;
			else if (start < 0) relativeStart = Math.max(size + start, 0);
			else relativeStart = Math.min(start, size);
			let relativeEnd;
			if (end === void 0) relativeEnd = size;
			else if (end < 0) relativeEnd = Math.max(size + end, 0);
			else relativeEnd = Math.min(end, size);
			const span = Math.max(relativeEnd - relativeStart, 0);
			if (span === 0) return new Blob$1([], { type: normalizeType(contentType ?? "") });
			return new Blob$1([_assertClassBrand(_Blob_brand, this, _concatenateBytes).call(this).slice(relativeStart, relativeStart + span)], { type: normalizeType(contentType ?? "") });
		}
		/**
		* Returns a Promise that resolves with the contents as an ArrayBuffer.
		*/
		async arrayBuffer() {
			const bytes = _assertClassBrand(_Blob_brand, this, _concatenateBytes).call(this);
			return bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
		}
		/**
		* Returns a Promise that resolves with the contents as a string.
		*/
		async text() {
			const bytes = _assertClassBrand(_Blob_brand, this, _concatenateBytes).call(this);
			return new TextDecoder().decode(bytes);
		}
		/**
		* Returns a Promise that resolves with the parsed JSON contents.
		*/
		async json() {
			const text = await this.text();
			const normalized = text.charCodeAt(0) === 65279 ? text.slice(1) : text;
			return JSON.parse(normalized);
		}
		/**
		* Returns a Promise that resolves with parsed application/x-www-form-urlencoded data.
		*/
		async formData() {
			const text = await this.text();
			const normalized = text.charCodeAt(0) === 65279 ? text.slice(1) : text;
			const params = new URLSearchParams(normalized);
			const form = new FormData();
			params.forEach((value, key) => {
				form.append(key, value);
			});
			return form;
		}
		/**
		* Returns a Promise that resolves with the Blob contents as a Uint8Array.
		*/
		async bytes() {
			return _assertClassBrand(_Blob_brand, this, _concatenateBytes).call(this);
		}
		/**
		* Returns a ReadableStream that can be used to read the Blob's contents.
		*
		* Uses byte stream type (type: 'bytes') so that BYOB readers work.
		*/
		stream() {
			const bytes = _assertClassBrand(_Blob_brand, this, _concatenateBytes).call(this);
			let position = 0;
			const chunkSize = 65536;
			return new (globalThis.ReadableStream ?? ReadableStream$1)({
				type: "bytes",
				pull(controller) {
					if (position >= bytes.length) {
						controller.close();
						return;
					}
					const chunk = bytes.slice(position, position + chunkSize);
					position += chunkSize;
					controller.enqueue(chunk);
				}
			});
		}
		/**
		* Returns the raw bytes for internal use.
		* Non-standard - used by File and other internal APIs.
		*/
		_getBytes() {
			return _assertClassBrand(_Blob_brand, this, _concatenateBytes).call(this);
		}
		get [_Symbol$toStringTag$27]() {
			return "Blob";
		}
	};
	/**
	* Returns a new Blob with the given bytes appended.
	* Non-standard extension for internal use.
	*/
	function _concatenateBytes() {
		if (_classPrivateFieldGet2(_parts, this).length === 0) return new Uint8Array(0);
		if (_classPrivateFieldGet2(_parts, this).length === 1) return _classPrivateFieldGet2(_parts, this)[0];
		const result = new Uint8Array(_classPrivateFieldGet2(_size, this));
		let offset = 0;
		{
			const __exactForOfIterator36 = _classPrivateFieldGet2(_parts, this)[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep36 = __exactForOfIterator36.next();
				if (__exactForOfStep36.done) break;
				const part = __exactForOfStep36.value;
				result.set(part, offset);
				offset += part.byteLength;
			}
		}
		return result;
	}
	/**
	* Normalize a MIME type string.
	* Per spec: lowercase, only ASCII printable chars (0x20-0x7E) excluding certain chars.
	*/
	function normalizeType(type) {
		if (!type) return "";
		const lowered = type.toLowerCase();
		for (let i = 0; i < lowered.length; i++) {
			const code = lowered.charCodeAt(i);
			if (code < 32 || code > 126) return "";
		}
		return lowered;
	}
	/**
	* Convert a BlobPart to bytes.
	*/
	function convertToBytes(part, endings) {
		if (typeof part === "string") {
			let str = part;
			if (endings === "native") str = str.replace(/\r\n/g, "\n").replace(/\r/g, "\n");
			return new TextEncoder().encode(str);
		}
		if (part instanceof Blob$1) return part._getBytes();
		if (part instanceof ArrayBuffer) return new Uint8Array(part);
		if (ArrayBuffer.isView(part)) return new Uint8Array(part.buffer, part.byteOffset, part.byteLength);
		throw new TypeError("Invalid blob part type");
	}
	//#endregion
	//#region ../ibex-runtime-js/src/blob/File.ts
	/**
	* File implementation for Exact runtime
	* 
	* Implements the WHATWG File API.
	* https://w3c.github.io/FileAPI/#file-section
	*/
	let _Symbol$toStringTag$26;
	var _name = /* @__PURE__ */ new WeakMap();
	var _lastModified = /* @__PURE__ */ new WeakMap();
	_Symbol$toStringTag$26 = Symbol.toStringTag;
	/**
	* File represents a file with a name and optional last modified date.
	* Extends Blob with file-specific metadata.
	*/
	var File$1 = class extends Blob$1 {
		constructor(fileBits, fileName, options) {
			super(fileBits, options);
			_classPrivateFieldInitSpec(this, _name, void 0);
			_classPrivateFieldInitSpec(this, _lastModified, void 0);
			_classPrivateFieldSet2(_name, this, String(fileName));
			_classPrivateFieldSet2(_lastModified, this, options?.lastModified ?? Date.now());
		}
		/**
		* The name of the file.
		*/
		get name() {
			return _classPrivateFieldGet2(_name, this);
		}
		/**
		* The last modified time of the file, in milliseconds since the Unix epoch.
		*/
		get lastModified() {
			return _classPrivateFieldGet2(_lastModified, this);
		}
		/**
		* The relative path of the file (empty for files not from a directory picker).
		* This is a legacy property, always returns empty string.
		*/
		get webkitRelativePath() {
			return "";
		}
		get [_Symbol$toStringTag$26]() {
			return "File";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/clone/transferableSymbols.ts
	const structuredCloneTransferSymbol = Symbol.for("exact.structuredClone.transfer");
	const structuredCloneCloneSymbol = Symbol.for("exact.structuredClone.clone");
	//#endregion
	//#region ../ibex-runtime-js/src/clone/structuredClone.ts
	/**
	* structuredClone implementation for Exact runtime
	* 
	* Implements the HTML structured clone algorithm.
	* @see https://html.spec.whatwg.org/multipage/structured-data.html#structuredclone
	*/
	let _Blob;
	let _File;
	function getBlobClasses() {
		if (!_Blob) {
			_Blob = Blob$1;
			_File = File$1;
		}
		return {
			Blob: _Blob,
			File: _File
		};
	}
	/**
	* Creates a deep copy of a value using the structured clone algorithm.
	* 
	* Supported types:
	* - Primitives (undefined, null, boolean, number, string, bigint)
	* - Date
	* - RegExp
	* - Array
	* - Object (plain objects)
	* - Map
	* - Set
	* - ArrayBuffer
	* - TypedArrays (Uint8Array, Int32Array, etc.)
	* - DataView
	* - Blob
	* - File
	* - Error (partial - only message and name)
	* 
	* NOT supported:
	* - Functions
	* - Symbols (as values - ignored as keys)
	* - DOM nodes
	* - WeakMap, WeakSet, WeakRef
	* - Generators, Promises
	* - SharedArrayBuffer (Hermes limitation)
	*/
	function structuredClone(value, options) {
		const transfer = options?.transfer ?? [];
		const transferSet = /* @__PURE__ */ new Set();
		{
			const __exactForOfIterator37 = transfer[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep37 = __exactForOfIterator37.next();
				if (__exactForOfStep37.done) break;
				const transferable = __exactForOfStep37.value;
				if (!(transferable instanceof ArrayBuffer) && !(transferable && typeof transferable === "object" && structuredCloneTransferSymbol in transferable)) throw createDataCloneError("The object could not be cloned.");
				if (transferable && typeof transferable === "object") {
					if (transferSet.has(transferable)) throw createDataCloneError("The object could not be cloned.");
					transferSet.add(transferable);
				}
			}
		}
		const cloned = cloneInternal(value, /* @__PURE__ */ new Map(), transferSet);
		{
			const __exactForOfIterator38 = transfer[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep38 = __exactForOfIterator38.next();
				if (__exactForOfStep38.done) break;
				const transferable = __exactForOfStep38.value;
				if (transferable instanceof ArrayBuffer) markDetachedArrayBuffer(transferable);
			}
		}
		return cloned;
	}
	function getGlobalDOMExceptionConstructor() {
		return typeof globalThis === "object" && globalThis !== null && typeof globalThis.DOMException === "function" ? globalThis.DOMException : void 0;
	}
	function isDomExceptionInstance(value) {
		if (value instanceof DOMException$6) return true;
		const GlobalDOMException = getGlobalDOMExceptionConstructor();
		return typeof GlobalDOMException === "function" && value instanceof GlobalDOMException;
	}
	function getGlobalImageDataConstructor() {
		return typeof globalThis === "object" && globalThis !== null && typeof globalThis.ImageData === "function" ? globalThis.ImageData : void 0;
	}
	function isImageDataInstance(value) {
		const ImageDataCtor = getGlobalImageDataConstructor();
		return typeof ImageDataCtor === "function" && value instanceof ImageDataCtor;
	}
	function cloneInternal(value, cloneMap, transferSet) {
		if (value === null || value === void 0) return value;
		const type = typeof value;
		if (type === "boolean" || type === "number" || type === "string" || type === "bigint") return value;
		if (type === "symbol") throw createDataCloneError("Symbols cannot be cloned");
		if (type === "function") throw createDataCloneError("Functions cannot be cloned");
		const obj = value;
		if (cloneMap.has(obj)) return cloneMap.get(obj);
		if (transferSet.has(obj)) {
			if (obj instanceof ArrayBuffer) {
				const clone = obj.slice(0);
				cloneMap.set(obj, clone);
				return clone;
			}
			if (structuredCloneTransferSymbol in obj) {
				const transferClone = obj[structuredCloneTransferSymbol]();
				cloneMap.set(obj, transferClone);
				return transferClone;
			}
		}
		if (structuredCloneCloneSymbol in obj) {
			const clone = obj[structuredCloneCloneSymbol]();
			cloneMap.set(obj, clone);
			return clone;
		}
		if (structuredCloneTransferSymbol in obj) throw createDataCloneError("The object could not be cloned.");
		if (obj instanceof Date) {
			const clone = new Date(obj.getTime());
			cloneMap.set(obj, clone);
			return clone;
		}
		if (obj instanceof RegExp) {
			const clone = new RegExp(obj.source, obj.flags);
			cloneMap.set(obj, clone);
			return clone;
		}
		if (isDomExceptionInstance(obj)) {
			const clone = new ((typeof obj.constructor === "function" ? obj.constructor : void 0) ?? (getGlobalDOMExceptionConstructor()) ?? DOMException$6)(obj.message, obj.name);
			if (typeof obj.stack === "string") clone.stack = obj.stack;
			if ("cause" in obj) clone.cause = cloneInternal(obj.cause, cloneMap, transferSet);
			cloneMap.set(obj, clone);
			return clone;
		}
		if (obj instanceof Error) {
			const ErrorConstructor = obj.constructor;
			const clone = new ErrorConstructor(obj.message);
			clone.name = obj.name;
			if (typeof obj.stack === "string") clone.stack = obj.stack;
			if ("cause" in obj) clone.cause = cloneInternal(obj.cause, cloneMap, transferSet);
			cloneMap.set(obj, clone);
			return clone;
		}
		if (isImageDataInstance(obj)) {
			const ImageDataCtor = getGlobalImageDataConstructor();
			if (!ImageDataCtor) throw createDataCloneError("The object could not be cloned.");
			const clonedData = cloneInternal(obj.data, cloneMap, transferSet);
			let clone;
			try {
				clone = new ImageDataCtor(clonedData, obj.width, obj.height, { colorSpace: obj.colorSpace });
			} catch {
				clone = new ImageDataCtor(clonedData, obj.width, obj.height);
			}
			cloneMap.set(obj, clone);
			return clone;
		}
		if (obj instanceof ArrayBuffer) {
			const clone = obj.slice(0);
			cloneMap.set(obj, clone);
			return clone;
		}
		if (ArrayBuffer.isView(obj) && !(obj instanceof DataView)) {
			const TypedArrayConstructor = obj.constructor;
			const clone = new TypedArrayConstructor(obj.buffer.slice(obj.byteOffset, obj.byteOffset + obj.byteLength), 0, obj.length);
			cloneMap.set(obj, clone);
			return clone;
		}
		if (obj instanceof DataView) {
			const clonedBuffer = obj.buffer.slice(0);
			const clone = new DataView(clonedBuffer, obj.byteOffset, obj.byteLength);
			cloneMap.set(obj, clone);
			return clone;
		}
		if (obj instanceof Map) {
			const clone = /* @__PURE__ */ new Map();
			cloneMap.set(obj, clone);
			{
				const __exactForOfIterator39 = obj[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep39 = __exactForOfIterator39.next();
					if (__exactForOfStep39.done) break;
					const [key, val] = __exactForOfStep39.value;
					const clonedKey = cloneInternal(key, cloneMap, transferSet);
					const clonedVal = cloneInternal(val, cloneMap, transferSet);
					clone.set(clonedKey, clonedVal);
				}
			}
			return clone;
		}
		if (obj instanceof Set) {
			const clone = /* @__PURE__ */ new Set();
			cloneMap.set(obj, clone);
			{
				const __exactForOfIterator40 = obj[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep40 = __exactForOfIterator40.next();
					if (__exactForOfStep40.done) break;
					const item = __exactForOfStep40.value;
					clone.add(cloneInternal(item, cloneMap, transferSet));
				}
			}
			return clone;
		}
		const { Blob: BlobClass, File: FileClass } = getBlobClasses();
		if (obj instanceof FileClass) {
			const clone = new FileClass([obj._getBytes()], obj.name, {
				type: obj.type,
				lastModified: obj.lastModified
			});
			cloneMap.set(obj, clone);
			return clone;
		}
		if (obj instanceof BlobClass) {
			const clone = new BlobClass([obj._getBytes()], { type: obj.type });
			cloneMap.set(obj, clone);
			return clone;
		}
		if (Array.isArray(obj)) {
			const clone = [];
			cloneMap.set(obj, clone);
			for (let i = 0; i < obj.length; i++) clone[i] = cloneInternal(obj[i], cloneMap, transferSet);
			return clone;
		}
		const proto = Object.getPrototypeOf(obj);
		if (proto === null || proto === Object.prototype) {
			const clone = {};
			cloneMap.set(obj, clone);
			{
				const __exactForOfIterator41 = Object.keys(obj)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep41 = __exactForOfIterator41.next();
					if (__exactForOfStep41.done) break;
					const key = __exactForOfStep41.value;
					clone[key] = cloneInternal(obj[key], cloneMap, transferSet);
				}
			}
			return clone;
		}
		if (typeof obj === "object") {
			const clone = {};
			cloneMap.set(obj, clone);
			{
				const __exactForOfIterator42 = Object.keys(obj)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep42 = __exactForOfIterator42.next();
					if (__exactForOfStep42.done) break;
					const key = __exactForOfStep42.value;
					clone[key] = cloneInternal(obj[key], cloneMap, transferSet);
				}
			}
			return clone;
		}
		throw createDataCloneError(`Object of type ${obj.constructor?.name ?? "unknown"} cannot be cloned`);
	}
	//#endregion
	//#region ../ibex-runtime-js/src/promise-rejection-tracking.ts
	/**
	* Promise Rejection Tracking
	*
	* Provides `unhandledrejection` and `rejectionhandled` event dispatch on globalThis.
	*
	* Strategy:
	* 1. If the native bridge exposes `__exactOnUnhandledRejection`, use it directly.
	* 2. Otherwise, install a userland polyfill that:
	*    a. Wraps `Promise.prototype.then` to track which promises have rejection handlers.
	*    b. Uses a Map to track unhandled rejections.
	*    c. Uses `queueMicrotask` to defer detection until after synchronous handler attachment.
	*    d. Dispatches `unhandledrejection` on globalThis when a rejection is unhandled.
	*    e. Dispatches `rejectionhandled` when a previously-unhandled rejection gets a handler.
	*
	* @see https://html.spec.whatwg.org/multipage/webappapis.html#unhandled-promise-rejections
	*/
	/**
	* Map of currently-unhandled rejections: Promise -> reason.
	* Exposed for testing purposes.
	*/
	const _unhandledRejections = /* @__PURE__ */ new Map();
	/**
	* Internal EventTarget used to dispatch promise rejection events.
	* We always use our own EventTarget instance to ensure compatibility with
	* our custom Event classes (avoids issues with native EventTarget implementations
	* in environments like Bun/Node that reject non-native Event subclasses).
	*/
	let _eventTarget = null;
	/**
	* Whether globalThis.addEventListener has been wrapped to intercept
	* unhandledrejection/rejectionhandled listeners.
	*/
	let _globalListenersInstalled = false;
	/**
	* Promise rejection event types that we intercept.
	*/
	const REJECTION_EVENT_TYPES = new Set(["unhandledrejection", "rejectionhandled"]);
	const trailingDataErrorSymbol$1 = Symbol.for("__exact.decompression.trailing-data-error");
	const _synchronouslyHandledPromises = /* @__PURE__ */ new WeakSet();
	let _nativeHandledTrackingInstalled = false;
	/**
	* Get or create the internal EventTarget for promise rejection events.
	*/
	function getEventTarget() {
		if (_eventTarget) return _eventTarget;
		_eventTarget = new EventTarget();
		return _eventTarget;
	}
	/**
	* Install interception of addEventListener/removeEventListener on globalThis
	* so that listeners for 'unhandledrejection' and 'rejectionhandled' are
	* forwarded to our internal EventTarget.
	*/
	function installGlobalListenerForwarding() {
		if (_globalListenersInstalled) return;
		_globalListenersInstalled = true;
		const g = globalThis;
		const et = getEventTarget();
		const origAddEventListener = g.addEventListener;
		const origRemoveEventListener = g.removeEventListener;
		if (typeof origAddEventListener === "function") g.addEventListener = function wrappedAddEventListener(type, callback, options) {
			if (REJECTION_EVENT_TYPES.has(type)) et.addEventListener(type, callback, options);
			else origAddEventListener.call(this, type, callback, options);
		};
		else g.addEventListener = et.addEventListener.bind(et);
		if (typeof origRemoveEventListener === "function") g.removeEventListener = function wrappedRemoveEventListener(type, callback, options) {
			if (REJECTION_EVENT_TYPES.has(type)) et.removeEventListener(type, callback, options);
			else origRemoveEventListener.call(this, type, callback, options);
		};
		else g.removeEventListener = et.removeEventListener.bind(et);
		if (typeof g.dispatchEvent !== "function") g.dispatchEvent = et.dispatchEvent.bind(et);
	}
	/**
	* Track a promise rejection. Called when a promise is rejected without a handler.
	* After a microtask, if no handler has been attached, dispatches `unhandledrejection`.
	*/
	function trackPromiseRejection(promise, reason) {
		if (_synchronouslyHandledPromises.has(promise)) return;
		const suppressUnhandledRejection = globalThis.__exactShouldSuppressUnhandledRejection;
		if (typeof suppressUnhandledRejection === "function") try {
			if (suppressUnhandledRejection(reason, promise)) return;
		} catch (_) {}
		if (reason && reason[trailingDataErrorSymbol$1]) return;
		_unhandledRejections.set(promise, reason);
		queueMicrotask(() => {
			if (!_unhandledRejections.has(promise)) return;
			const target = getEventTarget();
			const event = new PromiseRejectionEvent("unhandledrejection", {
				promise,
				reason,
				cancelable: true
			});
			if (target.dispatchEvent(event)) console.error("Unhandled promise rejection:", reason);
		});
	}
	function installNativeHandledPromiseTracking(OriginalPromise) {
		if (_nativeHandledTrackingInstalled) return;
		_nativeHandledTrackingInstalled = true;
		const originalThen = OriginalPromise.prototype.then;
		const originalCatch = OriginalPromise.prototype.catch;
		const originalFinally = OriginalPromise.prototype.finally;
		function markHandled(promise) {
			_synchronouslyHandledPromises.add(promise);
			if (_unhandledRejections.has(promise)) trackPromiseRejectionHandled(promise);
		}
		OriginalPromise.prototype.then = function wrappedThen(onFulfilled, onRejected) {
			if (this && (typeof this === "object" || typeof this === "function")) markHandled(this);
			return originalThen.call(this, onFulfilled, onRejected);
		};
		OriginalPromise.prototype.catch = function wrappedCatch(onRejected) {
			if (typeof onRejected === "function" && this && (typeof this === "object" || typeof this === "function")) markHandled(this);
			return originalCatch.call(this, onRejected);
		};
		if (typeof originalFinally === "function") OriginalPromise.prototype.finally = function wrappedFinally(onFinally) {
			if (this && (typeof this === "object" || typeof this === "function")) markHandled(this);
			return originalFinally.call(this, onFinally);
		};
	}
	/**
	* Track that a previously-unhandled promise rejection has been handled.
	* Dispatches `rejectionhandled` on globalThis.
	*/
	function trackPromiseRejectionHandled(promise) {
		const reason = _unhandledRejections.get(promise);
		if (!_unhandledRejections.has(promise)) return;
		_unhandledRejections.delete(promise);
		queueMicrotask(() => {
			const target = getEventTarget();
			const event = new PromiseRejectionEvent("rejectionhandled", {
				promise,
				reason,
				cancelable: false
			});
			target.dispatchEvent(event);
		});
	}
	/**
	* Install promise rejection tracking on the global Promise.
	*
	* This wraps Promise.prototype.then to intercept rejection handler attachment
	* and hooks into the Promise constructor to detect unhandled rejections.
	*/
	function installPromiseRejectionTracking() {
		const g = globalThis;
		installGlobalListenerForwarding();
		const OriginalPromise = g.Promise;
		if (typeof g.__exactOnUnhandledRejection === "function") {
			installNativeHandledPromiseTracking(OriginalPromise);
			g.__exactOnUnhandledRejection((promise, reason) => {
				trackPromiseRejection(promise, reason);
			});
			if (typeof g.__exactOnRejectionHandled === "function") g.__exactOnRejectionHandled((promise) => {
				trackPromiseRejectionHandled(promise);
			});
			return;
		}
		const originalThen = OriginalPromise.prototype.then;
		const originalCatch = OriginalPromise.prototype.catch;
		const _handledPromises = /* @__PURE__ */ new WeakSet();
		const _rejectedPromises = /* @__PURE__ */ new WeakSet();
		const _promiseParents = /* @__PURE__ */ new WeakMap();
		/**
		* Mark a promise (and its ancestor chain) as handled.
		*/
		function markHandled(promise) {
			let current = promise;
			while (current && (typeof current === "object" || typeof current === "function")) {
				if (_handledPromises.has(current)) break;
				_handledPromises.add(current);
				if (_rejectedPromises.has(current)) {
					_rejectedPromises.delete(current);
					trackPromiseRejectionHandled(current);
				}
				current = _promiseParents.get(current);
			}
		}
		/**
		* Wrapped .then() that tracks rejection handler attachment.
		*/
		OriginalPromise.prototype.then = function wrappedThen(onFulfilled, onRejected) {
			const child = originalThen.call(this, onFulfilled, onRejected);
			if (child && (typeof child === "object" || typeof child === "function")) _promiseParents.set(child, this);
			if (typeof onRejected === "function" && this && (typeof this === "object" || typeof this === "function")) markHandled(this);
			return child;
		};
		/**
		* Wrapped .catch() that tracks rejection handler attachment.
		*/
		OriginalPromise.prototype.catch = function wrappedCatch(onRejected) {
			const child = originalCatch.call(this, onRejected);
			if (child && (typeof child === "object" || typeof child === "function")) _promiseParents.set(child, this);
			if (typeof onRejected === "function" && this && (typeof this === "object" || typeof this === "function")) markHandled(this);
			return child;
		};
		const originalFinally = OriginalPromise.prototype.finally;
		if (originalFinally) OriginalPromise.prototype.finally = function wrappedFinally(onFinally) {
			const child = originalFinally.call(this, onFinally);
			if (child && (typeof child === "object" || typeof child === "function")) _promiseParents.set(child, this);
			return child;
		};
		/**
		* Create a wrapper around Promise.reject to detect immediate rejections.
		*/
		const originalReject = OriginalPromise.reject;
		OriginalPromise.reject = function wrappedReject(reason) {
			const promise = originalReject.call(this, reason);
			if (promise && (typeof promise === "object" || typeof promise === "function")) _rejectedPromises.add(promise);
			queueMicrotask(() => {
				if (promise && (typeof promise === "object" || typeof promise === "function") && !_handledPromises.has(promise)) trackPromiseRejection(promise, reason);
			});
			return promise;
		};
		/**
		* Wrap the Promise constructor to detect rejections in executor functions.
		*/
		const ExactPromise = function ExactPromise(executor) {
			let promiseRef = null;
			let pendingReject = null;
			const wrappedExecutor = (resolve, reject) => {
				const wrappedReject = (reason) => {
					if (promiseRef && (typeof promiseRef === "object" || typeof promiseRef === "function")) {
						_rejectedPromises.add(promiseRef);
						queueMicrotask(() => {
							if (!_handledPromises.has(promiseRef)) trackPromiseRejection(promiseRef, reason);
						});
					} else pendingReject = reason;
					return reject(reason);
				};
				return executor(resolve, wrappedReject);
			};
			const promise = new OriginalPromise(wrappedExecutor);
			promiseRef = promise;
			if (pendingReject !== null) {
				const reason = pendingReject;
				pendingReject = null;
				_rejectedPromises.add(promiseRef);
				queueMicrotask(() => {
					if (!_handledPromises.has(promiseRef)) trackPromiseRejection(promiseRef, reason);
				});
			}
			return promise;
		};
		ExactPromise.resolve = OriginalPromise.resolve.bind(OriginalPromise);
		ExactPromise.reject = OriginalPromise.reject;
		ExactPromise.all = OriginalPromise.all.bind(OriginalPromise);
		ExactPromise.allSettled = OriginalPromise.allSettled.bind(OriginalPromise);
		ExactPromise.race = OriginalPromise.race.bind(OriginalPromise);
		ExactPromise.any = OriginalPromise.any.bind(OriginalPromise);
		if (OriginalPromise.withResolvers) ExactPromise.withResolvers = OriginalPromise.withResolvers.bind(OriginalPromise);
		ExactPromise.prototype = OriginalPromise.prototype;
		ExactPromise[Symbol.species] = OriginalPromise;
		Object.defineProperty(ExactPromise, Symbol.hasInstance, { value: (instance) => instance instanceof OriginalPromise });
		g.Promise = ExactPromise;
		g.__OriginalPromise = OriginalPromise;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/streams/ReadableStream.ts
	/**
	* ReadableStream - WHATWG Streams API Implementation
	*
	* @see https://streams.spec.whatwg.org/#rs-class
	*
	* This implementation supports:
	* - Underlying source with pull/start/cancel
	* - Queuing strategy
	* - ReadableStreamDefaultReader
	* - ReadableStreamBYOBReader (byte streams)
	* - ReadableByteStreamController
	* - Async iteration
	* - tee(), pipeTo(), pipeThrough()
	*/
	init_defineProperty();
	let _Symbol$toStringTag$25, _Symbol$toStringTag2$2, _Symbol$toStringTag3, _Symbol$toStringTag4, _Symbol$toStringTag5, _Symbol$asyncIterator, _Symbol$asyncIterator2, _Symbol$toStringTag6, _Symbol$toStringTag7, _Symbol$toStringTag8;
	function isBunCompatReadableStreamTest() {
		if (typeof process !== "object" || !process || typeof process.env !== "object") return false;
		return process.env.EXACT_COMPAT_TEST === "1" && process.env.EXACT_TEST_SECTION === "bun";
	}
	function getReadableStreamLockedMessage() {
		return isBunCompatReadableStreamTest() ? "ReadableStream is locked" : "This stream already has a reader";
	}
	/**
	* Create a read result object that is immune to Object.prototype.then injection.
	* Per WHATWG Streams spec, read results must not be thenables.
	*/
	function createReadResult(done, value) {
		const result = Object.create(null);
		result.done = done;
		result.value = value;
		return result;
	}
	const originalPromiseThen$1 = Promise.prototype.then;
	const originalPromiseResolve$1 = Promise.resolve.bind(Promise);
	const originalPromiseReject$1 = Promise.reject.bind(Promise);
	function hideFunctionPrototype(fn) {
		return new Proxy(fn, { has(target, property) {
			if (property === "prototype") return false;
			return property in target;
		} });
	}
	function promiseThen$1(promise, onFulfilled, onRejected) {
		if (typeof onRejected === "function" && (typeof promise === "object" && promise !== null || typeof promise === "function")) trackPromiseRejectionHandled(promise);
		return originalPromiseThen$1.call(promise, onFulfilled, onRejected);
	}
	function promiseCatch(promise, onRejected) {
		return promiseThen$1(promise, void 0, onRejected);
	}
	function markPromiseHandled(promise) {
		originalPromiseThen$1.call(promise, void 0, () => {});
		if (typeof promise === "object" && promise !== null || typeof promise === "function") trackPromiseRejectionHandled(promise);
	}
	function observeObjectPrototypeThen() {
		const descriptor = Object.getOwnPropertyDescriptor(Object.prototype, "then");
		if (!descriptor || typeof descriptor.get !== "function") return;
		try {
			({}).then;
		} catch (_error) {}
	}
	function createAsyncIteratorResult(done, value) {
		return {
			done,
			value
		};
	}
	function cloneChunkForTee(value) {
		if (!ArrayBuffer.isView(value)) try {
			return structuredClone(value);
		} catch {
			return value;
		}
		if (value instanceof Uint8Array) return new Uint8Array(value);
		try {
			return structuredClone(value);
		} catch {
			const view = value;
			return new Uint8Array(view.buffer.slice(view.byteOffset, view.byteOffset + view.byteLength));
		}
	}
	function isReadableStream(value) {
		return !!(value && typeof value === "object" && typeof value.getReader === "function" && typeof value.cancel === "function");
	}
	function isReadableStreamBrand(value) {
		return value instanceof ReadableStream$1 && typeof value._state === "string";
	}
	function isWritableStreamBrand(value) {
		return value instanceof WritableStream$1 && typeof value._state === "string";
	}
	function getPipeToAbortReason(signalReason) {
		return signalReason === void 0 ? new DOMException$6("The operation was aborted.", "AbortError") : signalReason;
	}
	const byobRequestBrand = Symbol("ReadableStreamBYOBRequestBrand");
	function isObject(value) {
		return value !== null && (typeof value === "object" || typeof value === "function");
	}
	function isCallable(value) {
		return typeof value === "function";
	}
	function toNumber(value) {
		return Number(value);
	}
	function toUnrestrictedDouble(value) {
		return toNumber(value);
	}
	function getPropertyDescriptorWithoutObjectPrototype(object, property) {
		if (!isObject(object)) return;
		let current = object;
		while (current !== null && current !== Object.prototype) {
			const descriptor = Object.getOwnPropertyDescriptor(current, property);
			if (descriptor !== void 0) return descriptor;
			current = Object.getPrototypeOf(current);
		}
	}
	function getPropertyValueWithoutObjectPrototype(object, property) {
		const descriptor = getPropertyDescriptorWithoutObjectPrototype(object, property);
		if (descriptor === void 0) return;
		if (descriptor.get !== void 0) return descriptor.get.call(object);
		return descriptor.value;
	}
	function hasPropertyWithoutObjectPrototype(object, property) {
		return getPropertyDescriptorWithoutObjectPrototype(object, property) !== void 0;
	}
	function getQueuingStrategyHighWaterMark(init, errorMessage) {
		if (init === void 0) throw new TypeError(errorMessage);
		const validated = Object(init);
		if (!hasPropertyWithoutObjectPrototype(validated, "highWaterMark")) throw new TypeError(errorMessage);
		return toUnrestrictedDouble(getPropertyValueWithoutObjectPrototype(validated, "highWaterMark"));
	}
	function validateHighWaterMark(highWaterMark, defaultValue) {
		if (highWaterMark === void 0) return defaultValue;
		const normalized = toNumber(highWaterMark);
		if (Number.isNaN(normalized) || normalized < 0) throw new RangeError("The highWaterMark option must be a non-negative number");
		return normalized;
	}
	function validateQueuingStrategySize(value) {
		if (value === void 0) return function() {
			return 1;
		};
		if (!isCallable(value)) throw new TypeError("size must be a function");
		return value;
	}
	function getUnderlyingSourceType(source) {
		if (!hasPropertyWithoutObjectPrototype(source, "type")) return;
		const streamType = getPropertyValueWithoutObjectPrototype(source, "type");
		if (streamType === void 0) return;
		const type = String(streamType);
		if (type !== "bytes" && type !== "owning" && type !== "direct") throw new TypeError("Cannot construct ReadableStream with non-\"bytes\" type");
		return type;
	}
	function createDirectReadableStreamController(getController) {
		function resolveController() {
			const controller = getController();
			if (!controller) throw new TypeError("ReadableStream direct controller is not initialized");
			return controller;
		}
		function resolveVoid() {
			return originalPromiseResolve$1(void 0);
		}
		function rejectError(error) {
			return originalPromiseReject$1(error);
		}
		return {
			get desiredSize() {
				return resolveController().desiredSize;
			},
			close() {
				try {
					resolveController().close();
					return resolveVoid();
				} catch (error) {
					return rejectError(error);
				}
			},
			enqueue(chunk, options) {
				try {
					resolveController().enqueue(chunk, options);
					return resolveVoid();
				} catch (error) {
					return rejectError(error);
				}
			},
			error(e) {
				try {
					resolveController().error(e);
					return resolveVoid();
				} catch (error) {
					return rejectError(error);
				}
			},
			write(chunk, options) {
				return this.enqueue(chunk, options);
			},
			flush() {
				return resolveVoid();
			},
			end(chunk, options) {
				try {
					const controller = resolveController();
					if (arguments.length > 0) controller.enqueue(chunk, options);
					controller.close();
					return resolveVoid();
				} catch (error) {
					return rejectError(error);
				}
			}
		};
	}
	function validateUnderlyingSourceMethod(member, method) {
		if (method !== void 0 && !isCallable(method)) throw new TypeError(`ReadableStream underlying source ${member} must be a function`);
	}
	function isSharedArrayBufferLike(buffer) {
		return Object.prototype.toString.call(buffer) === "[object SharedArrayBuffer]";
	}
	function transferArrayBuffer(buffer) {
		if (isSharedArrayBufferLike(buffer)) throw new TypeError("Cannot transfer a non-transferable ArrayBuffer");
		if (isNonTransferableArrayBuffer(buffer)) throw new TypeError("Cannot transfer a non-transferable ArrayBuffer");
		const bufferWithTransfer = buffer;
		if (typeof bufferWithTransfer.transfer === "function") {
			const transferred = bufferWithTransfer.transfer();
			if (buffer.byteLength !== 0) markDetachedArrayBuffer(buffer);
			return transferred;
		}
		const transferred = buffer.slice(0);
		markDetachedArrayBuffer(buffer);
		return transferred;
	}
	function transferArrayBufferView(view) {
		const byteOffset = view.byteOffset;
		const byteLength = view.byteLength;
		const elementSize = view.BYTES_PER_ELEMENT || 1;
		const elementLength = Math.floor(byteLength / elementSize);
		const transferredBuffer = transferArrayBuffer(view.buffer);
		if (view instanceof DataView) return new DataView(transferredBuffer, byteOffset, byteLength);
		const ctor = view.constructor;
		return new ctor(transferredBuffer, byteOffset, elementLength);
	}
	function toTransferList(value) {
		if (Array.isArray(value)) return value;
		if (value === void 0 || value === null) return [];
		return Array.from(value);
	}
	function getControllerEnqueueTransferList(options) {
		if (options === void 0 || options === null) return;
		if (!isObject(options)) return;
		const transferValue = options.transfer;
		if (transferValue === void 0) return;
		return toTransferList(transferValue);
	}
	function cloneOwningStreamChunk(chunk, transfer) {
		if (transfer === void 0 || transfer.length === 0) return chunk;
		return structuredClone(chunk, { transfer });
	}
	function getAsyncIteratorPrototype() {
		if (!isObject(Object.getPrototypeOf)) return Object.prototype;
		try {
			const nativePrototype = globalThis.AsyncIterator?.prototype;
			if (isObject(nativePrototype)) return nativePrototype;
		} catch (_error) {}
		try {
			const iteratorPrototype = Object.getPrototypeOf([][Symbol.iterator]());
			if (isObject(iteratorPrototype)) return Object.create(iteratorPrototype);
		} catch (_error) {}
		return Object.prototype;
	}
	const asyncIteratorPrototype = getAsyncIteratorPrototype();
	let originalReadableStreamDefaultReaderRead = null;
	let originalReadableStreamDefaultReaderReleaseLock = null;
	let originalReadableStreamGetReader = null;
	function isIteratorResult(value) {
		return isObject(value) && "done" in value;
	}
	const countSizeTarget = new Function("return (() => 1)")();
	const byteLengthSizeTarget = new Function("return ((chunk) => chunk.byteLength)")();
	Object.defineProperty(countSizeTarget, "name", {
		value: "size",
		configurable: true
	});
	Object.defineProperty(byteLengthSizeTarget, "name", {
		value: "size",
		configurable: true
	});
	const countSize = hideFunctionPrototype(countSizeTarget);
	const byteLengthSize = hideFunctionPrototype(byteLengthSizeTarget);
	function normalizePipeToOptions(options) {
		const pipeOptions = options === void 0 || options === null ? {} : Object(options);
		const preventAbort = Boolean(pipeOptions.preventAbort);
		const preventCancel = Boolean(pipeOptions.preventCancel);
		const preventClose = Boolean(pipeOptions.preventClose);
		const signal = pipeOptions.signal;
		if (signal !== void 0 && !isAbortSignal(signal)) throw new TypeError("Expected signal to be a valid AbortSignal");
		return {
			preventAbort,
			preventCancel,
			preventClose,
			signal
		};
	}
	function clampQueueTotalSize(size) {
		return size < 0 ? 0 : size;
	}
	function copyByteSlice(buffer, byteOffset, byteLength) {
		const bytes = new Uint8Array(byteLength);
		if (byteLength > 0) bytes.set(new Uint8Array(buffer, byteOffset, byteLength));
		return bytes;
	}
	function invalidateReadableByteStreamByobRequest(controller) {
		if (controller._byobRequest !== null) {
			controller._byobRequest._view = null;
			controller._byobRequest = null;
		}
	}
	function removePullIntoRequestFromReader(controller, descriptor) {
		const request = descriptor.pendingRequest;
		if (request === null) return null;
		const reader = controller._stream._reader;
		if (descriptor.readerType === "byob") {
			if (reader instanceof ReadableStreamBYOBReader) {
				const requestIndex = reader._readIntoRequests.indexOf(request);
				if (requestIndex !== -1) reader._readIntoRequests.splice(requestIndex, 1);
			}
		} else if (reader instanceof ReadableStreamDefaultReader) {
			const requestIndex = reader._readRequests.indexOf(request);
			if (requestIndex !== -1) reader._readRequests.splice(requestIndex, 1);
		}
		descriptor.pendingRequest = null;
		return request;
	}
	function transferPullIntoDescriptorBuffer(descriptor) {
		const transferredBuffer = transferArrayBuffer(descriptor.buffer);
		descriptor.buffer = transferredBuffer;
		descriptor.bufferByteLength = transferredBuffer.byteLength;
	}
	function deliverByteStreamChunk(controller, chunk) {
		const reader = controller._stream._reader;
		if (reader instanceof ReadableStreamDefaultReader && reader._readRequests.length > 0) {
			reader._readRequests.shift().resolve(createReadResult(false, chunk));
			return;
		}
		controller._queue.push({
			buffer: chunk.buffer,
			byteOffset: chunk.byteOffset,
			byteLength: chunk.byteLength
		});
		controller._queueTotalSize = clampQueueTotalSize(controller._queueTotalSize + chunk.byteLength);
	}
	function attachDefaultReadRequestToPendingPullInto(controller, request) {
		const firstPullInto = controller._pendingPullIntos[0];
		if (firstPullInto === void 0 || firstPullInto.readerType !== "default" || firstPullInto.pendingRequest !== null) return false;
		firstPullInto.pendingRequest = request;
		return true;
	}
	function enqueueByteStreamChunk(controller, chunk, preserveView = false) {
		if (chunk.byteLength === 0) return;
		let remainingOffset = chunk.byteOffset;
		let remainingBytes = chunk.byteLength;
		while (remainingBytes > 0 && controller._pendingPullIntos.length > 0) {
			const firstPullInto = controller._pendingPullIntos[0];
			if (isDetachedArrayBuffer(firstPullInto.buffer)) throw new TypeError("Cannot enqueue into a detached BYOB request buffer");
			const reader = controller._stream._reader;
			if (firstPullInto.readerType === "default" && firstPullInto.bytesFilled === 0 && reader instanceof ReadableStreamDefaultReader) {
				controller._pendingPullIntos.shift();
				transferPullIntoDescriptorBuffer(firstPullInto);
				invalidateReadableByteStreamByobRequest(controller);
				const request = removePullIntoRequestFromReader(controller, firstPullInto);
				const directChunk = preserveView ? new Uint8Array(chunk.buffer, remainingOffset, remainingBytes) : copyByteSlice(chunk.buffer, remainingOffset, remainingBytes);
				if (request !== null) request.resolve(createReadResult(false, directChunk));
				else deliverByteStreamChunk(controller, directChunk);
				return;
			}
			if (firstPullInto.readerType === "byob" && firstPullInto.pendingRequest === null && reader instanceof ReadableStreamDefaultReader) {
				controller._pendingPullIntos.shift();
				transferPullIntoDescriptorBuffer(firstPullInto);
				invalidateReadableByteStreamByobRequest(controller);
				if (firstPullInto.bytesFilled > 0) deliverByteStreamChunk(controller, copyByteSlice(firstPullInto.buffer, firstPullInto.byteOffset, firstPullInto.bytesFilled));
				continue;
			}
			const bytesToCopy = Math.min(remainingBytes, firstPullInto.byteLength - firstPullInto.bytesFilled);
			const destBuffer = new Uint8Array(firstPullInto.buffer, firstPullInto.byteOffset + firstPullInto.bytesFilled, bytesToCopy);
			const srcBuffer = new Uint8Array(chunk.buffer, remainingOffset, bytesToCopy);
			destBuffer.set(srcBuffer);
			firstPullInto.bytesFilled += bytesToCopy;
			remainingOffset += bytesToCopy;
			remainingBytes -= bytesToCopy;
			if (firstPullInto.bytesFilled < firstPullInto.minimumBytes && bytesToCopy > 0) transferPullIntoDescriptorBuffer(firstPullInto);
			invalidateReadableByteStreamByobRequest(controller);
			if (firstPullInto.bytesFilled >= firstPullInto.minimumBytes) {
				controller._pendingPullIntos.shift();
				resolvePullIntoDescriptor(controller, firstPullInto, false);
			}
		}
		if (remainingBytes === 0) return;
		deliverByteStreamChunk(controller, preserveView ? new Uint8Array(chunk.buffer, remainingOffset, remainingBytes) : copyByteSlice(chunk.buffer, remainingOffset, remainingBytes));
	}
	function resolvePullIntoDescriptor(controller, descriptor, done) {
		const remainderSize = descriptor.bytesFilled % descriptor.elementSize;
		const readyBytes = descriptor.bytesFilled - remainderSize;
		const remainderChunk = remainderSize > 0 ? copyByteSlice(descriptor.buffer, descriptor.byteOffset + readyBytes, remainderSize) : null;
		const request = removePullIntoRequestFromReader(controller, descriptor);
		if (request !== null) {
			const filledView = new descriptor.viewConstructor(descriptor.buffer, descriptor.byteOffset, readyBytes / descriptor.elementSize);
			request.resolve(createReadResult(done, transferArrayBufferView(filledView)));
		} else if (readyBytes > 0) enqueueByteStreamChunk(controller, copyByteSlice(descriptor.buffer, descriptor.byteOffset, readyBytes));
		if (remainderChunk !== null) enqueueByteStreamChunk(controller, remainderChunk);
	}
	function detachPendingPullIntoRequests(controller, requests) {
		if (requests.length === 0) return;
		{
			const __exactForOfIterator43 = controller._pendingPullIntos[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep43 = __exactForOfIterator43.next();
				if (__exactForOfStep43.done) break;
				const descriptor = __exactForOfStep43.value;
				if (descriptor.pendingRequest !== null && requests.includes(descriptor.pendingRequest)) descriptor.pendingRequest = null;
			}
		}
	}
	function resolvePendingPullIntosOnClose(controller) {
		if (controller._pendingPullIntos.length === 0) return;
		for (const descriptor of controller._pendingPullIntos) if (descriptor.bytesFilled % descriptor.elementSize !== 0) {
			const e = /* @__PURE__ */ new TypeError("Insufficient bytes to fill elements in the given buffer");
			controller.error(e);
			return;
		}
		const descriptors = controller._pendingPullIntos.slice();
		controller._pendingPullIntos = [];
		invalidateReadableByteStreamByobRequest(controller);
		{
			const __exactForOfIterator44 = descriptors[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep44 = __exactForOfIterator44.next();
				if (__exactForOfStep44.done) break;
				const descriptor = __exactForOfStep44.value;
				resolvePullIntoDescriptor(controller, descriptor, true);
			}
		}
	}
	function finalizeReadableByteStreamBranchClose(controller) {
		if (controller._stream._state !== "readable") return;
		if (controller._pendingPullIntos.length === 0) {
			controller.close();
			return;
		}
		resolvePendingPullIntosOnClose(controller);
		if (controller._stream._state !== "readable") return;
		controller._stream._closeStream();
	}
	function deliverReadableByteStreamBranchChunk(controller, chunk, preserveView) {
		if (!preserveView || controller._pendingPullIntos.length > 0) {
			controller.enqueue(chunk);
			return;
		}
		const reader = controller._stream._reader;
		if (reader && reader instanceof ReadableStreamDefaultReader && reader._readRequests.length > 0) {
			reader._readRequests.shift().resolve(createReadResult(false, chunk));
			controller._pullIfNeeded();
			return;
		}
		controller._queue.push({
			buffer: chunk.buffer,
			byteOffset: chunk.byteOffset,
			byteLength: chunk.byteLength
		});
		controller._queueTotalSize = clampQueueTotalSize(controller._queueTotalSize + chunk.byteLength);
		controller._pullIfNeeded();
	}
	function ensureAutoAllocatePullInto(controller) {
		if (controller._autoAllocateChunkSize === void 0 || controller._pendingPullIntos.length > 0) return;
		const reader = controller._stream._reader;
		if (!(reader instanceof ReadableStreamDefaultReader) || reader._readRequests.length === 0) return;
		let buffer;
		try {
			buffer = new ArrayBuffer(controller._autoAllocateChunkSize);
		} catch (error) {
			controller._error(error);
			return;
		}
		controller._pendingPullIntos.push({
			buffer,
			bufferByteLength: buffer.byteLength,
			byteOffset: 0,
			byteLength: buffer.byteLength,
			bytesFilled: 0,
			minimumBytes: 1,
			elementSize: 1,
			viewConstructor: Uint8Array,
			readerType: "default",
			pendingRequest: reader._readRequests[0] ?? null
		});
	}
	async function performPipeTo(source, destination, options) {
		const { preventAbort, preventCancel, preventClose, signal } = options;
		if (source.locked) throw new TypeError("Cannot pipe a locked stream");
		if (destination.locked) throw new TypeError("Cannot pipe to a locked stream");
		const reader = (originalReadableStreamGetReader ?? source.getReader).call(source);
		const writer = destination.getWriter();
		markPromiseHandled(writer.ready);
		const pendingWrites = /* @__PURE__ */ new Set();
		let pendingWriteError = void 0;
		let hasPendingWriteError = false;
		let shuttingDown = false;
		let abortError = void 0;
		let abortListener;
		let abortPromise;
		const releaseReader = () => {
			if (reader._stream === void 0) return;
			if (source._reader === reader) source._reader = void 0;
			reader._stream = void 0;
		};
		const releaseWriter = () => {
			if (writer._stream === void 0) return;
			if (destination._writer === writer) destination._writer = void 0;
			writer._stream = void 0;
		};
		if (signal !== void 0) {
			abortError = getPipeToAbortReason(signal.reason);
			abortPromise = new Promise((_, reject) => {
				const onAbort = () => {
					abortError = getPipeToAbortReason(signal.reason);
					reject(abortError);
				};
				abortListener = onAbort;
				if (signal.aborted) onAbort();
				else signal.addEventListener("abort", onAbort);
			});
			markPromiseHandled(abortPromise);
		}
		if (signal !== void 0 && signal.aborted) {
			const reason = abortError !== void 0 ? abortError : new DOMException$6("The operation was aborted.", "AbortError");
			let abortFailure = void 0;
			let cancelFailure = void 0;
			if (abortListener !== void 0) {
				signal.removeEventListener("abort", abortListener);
				abortListener = void 0;
			}
			if (!preventAbort && destination._state === "writable") try {
				await writer.abort(reason);
			} catch (error) {
				abortFailure = error;
			}
			if (!preventCancel && source._state === "readable") try {
				await reader.cancel(reason);
			} catch (error) {
				cancelFailure = error;
			}
			else if (!preventCancel && source._state === "errored" && source._controller !== void 0) try {
				await source._controller._cancelAlgorithm(reason);
			} catch (error) {
				cancelFailure = error;
			}
			releaseReader();
			releaseWriter();
			throw abortFailure ?? cancelFailure ?? reason;
		}
		const canAbortDestination = () => destination._state !== "errored" && destination._state !== "closed";
		const waitPendingWrites = async () => {
			if (pendingWrites.size === 0) return;
			for (const pendingWrite of [...pendingWrites]) try {
				await pendingWrite;
			} catch (_error) {}
		};
		const shutdown = async (error, hasError, action, preferActionError = false) => {
			if (shuttingDown) {
				if (hasError) throw error;
				return;
			}
			shuttingDown = true;
			if (abortListener !== void 0 && signal !== void 0) {
				signal.removeEventListener("abort", abortListener);
				abortListener = void 0;
			}
			await waitPendingWrites();
			let finalError = error;
			let hasFinalError = hasError;
			if (!hasFinalError && hasPendingWriteError) {
				finalError = pendingWriteError;
				hasFinalError = true;
			}
			if (action !== void 0 && (!hasFinalError || preferActionError)) try {
				await action();
			} catch (actionError) {
				finalError = actionError;
				hasFinalError = true;
			}
			releaseReader();
			releaseWriter();
			if (hasFinalError) throw finalError;
		};
		const shutdownOnAbort = async () => {
			const reason = abortError !== void 0 ? abortError : new DOMException$6("The operation was aborted.", "AbortError");
			let abortFailure = void 0;
			let cancelFailure = void 0;
			await shutdown(void 0, false, async () => {
				if (!preventAbort && canAbortDestination()) try {
					await writer.abort(reason);
				} catch (error) {
					abortFailure = error;
				}
				if (!preventCancel && source._state === "readable") try {
					await reader.cancel(reason);
				} catch (error) {
					cancelFailure = error;
				}
				else if (!preventCancel && source._state === "errored" && source._controller !== void 0) try {
					await source._controller._cancelAlgorithm(reason);
				} catch (error) {
					cancelFailure = error;
				}
			});
			throw abortFailure ?? cancelFailure ?? reason;
		};
		const shutdownOnSourceError = async (error) => {
			await shutdown(error, true, async () => {
				if (!preventAbort && canAbortDestination()) await writer.abort(error);
			}, true);
			throw error;
		};
		const shutdownOnDestinationError = async (error) => {
			await shutdown(error, true, async () => {
				if (!preventCancel) await reader.cancel(error);
			}, true);
			throw error;
		};
		const shutdownOnDestinationClosed = async () => {
			const closedError = /* @__PURE__ */ new TypeError("the destination writable stream closed before all data could be piped to it");
			let finalError = closedError;
			if (!preventCancel) try {
				await reader.cancel(closedError);
			} catch (cancelError) {
				finalError = cancelError;
			}
			await shutdown(finalError, true);
			throw finalError;
		};
		const shutdownOnClose = async () => {
			await shutdown(void 0, false, async () => {
				if (preventClose) return;
				if (destination._state === "errored") throw destination._storedError;
				if (destination._state === "closing" || destination._state === "closed") return;
				try {
					await writer.close();
				} catch (closeError) {
					if (destination._state === "errored") throw destination._storedError;
					throw closeError;
				}
			});
		};
		const WAIT_READY = 1;
		const WAIT_DESTINATION_CLOSED = 2;
		const WAIT_WRITE_COMPLETE = 3;
		const WAIT_READ_COMPLETE = 4;
		const WAIT_SOURCE_CLOSED = 5;
		let pendingReadResult;
		const waitForWriterReady = () => {
			return new Promise((resolve, reject) => {
				let settled = false;
				const resolveOnce = (value) => {
					if (settled) return;
					settled = true;
					resolve(value);
				};
				const rejectOnce = (reason) => {
					if (settled) return;
					settled = true;
					reject(reason);
				};
				promiseThen$1(writer.ready, () => resolveOnce(WAIT_READY), rejectOnce);
				promiseThen$1(reader.closed, () => resolveOnce(WAIT_SOURCE_CLOSED), rejectOnce);
				promiseThen$1(writer.closed, () => resolveOnce(WAIT_DESTINATION_CLOSED), rejectOnce);
				if (abortPromise !== void 0) promiseThen$1(abortPromise, void 0, rejectOnce);
			});
		};
		const waitForRead = () => {
			return new Promise((resolve, reject) => {
				let settled = false;
				const request = {
					resolve(result) {
						if (settled) return;
						settled = true;
						pendingReadResult = result;
						resolve(WAIT_READ_COMPLETE);
					},
					reject(reason) {
						if (settled) return;
						settled = true;
						reject(reason);
					}
				};
				const rejectOnce = (reason) => {
					if (settled) return;
					settled = true;
					const index = reader._readRequests.indexOf(request);
					if (index !== -1) reader._readRequests.splice(index, 1);
					reject(reason);
				};
				source._disturbed = true;
				reader._readRequests.push(request);
				reader._processReadRequests();
				if (reader._readRequests.includes(request)) source._controller._pullIfNeeded();
				promiseThen$1(writer.closed, () => rejectOnce(/* @__PURE__ */ new TypeError("the destination writable stream closed before all data could be piped to it")), rejectOnce);
				if (abortPromise !== void 0) promiseThen$1(abortPromise, void 0, rejectOnce);
			});
		};
		const waitForWrite = (promise) => {
			return new Promise((resolve, reject) => {
				let settled = false;
				const resolveOnce = (value) => {
					if (settled) return;
					settled = true;
					resolve(value);
				};
				const rejectOnce = (reason) => {
					if (settled) return;
					settled = true;
					reject(reason);
				};
				promiseThen$1(promise, () => resolveOnce(WAIT_WRITE_COMPLETE), rejectOnce);
				promiseThen$1(writer.closed, () => resolveOnce(WAIT_DESTINATION_CLOSED), rejectOnce);
				if (abortPromise !== void 0) promiseThen$1(abortPromise, void 0, rejectOnce);
			});
		};
		if (source._state === "errored" && destination._state === "errored") {
			const initialError = !preventAbort && destination._started === false ? destination._storedError : source._storedError;
			if (abortListener && signal !== void 0) {
				signal.removeEventListener("abort", abortListener);
				abortListener = void 0;
			}
			releaseReader();
			releaseWriter();
			throw initialError;
		}
		if (source._state === "errored") {
			const sourceError = source._storedError;
			if (abortListener && signal !== void 0) {
				signal.removeEventListener("abort", abortListener);
				abortListener = void 0;
			}
			let finalError = sourceError;
			if (!preventAbort && canAbortDestination()) try {
				await writer.abort(sourceError);
			} catch (abortFailure) {
				finalError = abortFailure;
			}
			releaseReader();
			releaseWriter();
			throw finalError;
		}
		if (destination._state === "closing" || destination._state === "closed") {
			if (source._state === "closed") {
				if (abortListener && signal !== void 0) {
					signal.removeEventListener("abort", abortListener);
					abortListener = void 0;
				}
				releaseReader();
				releaseWriter();
				return;
			}
			const closedError = /* @__PURE__ */ new TypeError("the destination writable stream closed before all data could be piped to it");
			let finalError = closedError;
			if (!preventCancel) try {
				await reader.cancel(closedError);
			} catch (cancelError) {
				finalError = cancelError;
			}
			if (abortListener && signal !== void 0) {
				signal.removeEventListener("abort", abortListener);
				abortListener = void 0;
			}
			releaseReader();
			releaseWriter();
			throw finalError;
		}
		if (destination._state === "errored") {
			const destError = destination._storedError;
			if (abortListener && signal !== void 0) {
				signal.removeEventListener("abort", abortListener);
				abortListener = void 0;
			}
			let cancelError = destError;
			if (!preventCancel) try {
				await reader.cancel(destError);
			} catch (error) {
				cancelError = error;
			}
			releaseReader();
			releaseWriter();
			throw cancelError;
		}
		try {
			while (true) {
				if (shuttingDown) break;
				if (destination._state === "errored") {
					await shutdownOnDestinationError(destination._storedError);
					return;
				}
				if (destination._state === "closing" || destination._state === "closed") {
					await shutdownOnDestinationClosed();
					return;
				}
				if (source._state === "closed") break;
				if (writer.desiredSize === null) {
					await shutdownOnDestinationError(destination._storedError);
					return;
				}
				if (writer.desiredSize <= 0) {
					try {
						const readyResult = await waitForWriterReady();
						if (readyResult === WAIT_DESTINATION_CLOSED) {
							await shutdownOnDestinationClosed();
							return;
						}
						if (readyResult === WAIT_SOURCE_CLOSED || source._state === "closed") break;
					} catch (error) {
						if (!preventAbort && destination._state === "errored") {
							await shutdownOnDestinationError(destination._storedError);
							return;
						}
						if (source._state === "errored") {
							await shutdownOnSourceError(source._storedError);
							return;
						}
						if (destination._state === "errored") {
							await shutdownOnDestinationError(destination._storedError);
							return;
						}
						if (destination._state === "closing" || destination._state === "closed") {
							await shutdownOnDestinationClosed();
							return;
						}
						if (signal !== void 0 && signal.aborted && error === abortError) {
							await shutdownOnAbort();
							return;
						}
						await shutdown(error, true);
						throw error;
					}
					continue;
				}
				let readResult;
				try {
					await waitForRead();
					readResult = pendingReadResult;
				} catch (error) {
					if (shuttingDown) throw error;
					if (!preventAbort && destination._state === "errored") {
						await shutdownOnDestinationError(destination._storedError);
						return;
					}
					if (source._state === "errored") {
						await shutdownOnSourceError(source._storedError);
						return;
					}
					if (destination._state === "errored") {
						await shutdownOnDestinationError(destination._storedError);
						return;
					}
					if (destination._state === "closing" || destination._state === "closed") {
						await shutdownOnDestinationClosed();
						return;
					}
					if (signal !== void 0 && signal.aborted && error === abortError) {
						await shutdownOnAbort();
						return;
					}
					await shutdown(error, true);
					throw error;
				}
				if (readResult.done) break;
				const chunk = readResult.value;
				const writePromise = promiseCatch(writer.write(chunk), (error) => {
					if (!hasPendingWriteError) {
						hasPendingWriteError = true;
						pendingWriteError = error;
					}
					throw error;
				});
				pendingWrites.add(writePromise);
				markPromiseHandled(promiseThen$1(writePromise, () => {
					pendingWrites.delete(writePromise);
				}, () => {
					pendingWrites.delete(writePromise);
				}));
				markPromiseHandled(writePromise);
				await originalPromiseResolve$1();
				if (!preventAbort && destination._state === "errored") {
					await shutdownOnDestinationError(destination._storedError);
					return;
				}
				if (source._state === "errored") {
					await shutdownOnSourceError(source._storedError);
					return;
				}
				if (destination._state === "errored") {
					await shutdownOnDestinationError(destination._storedError);
					return;
				}
				if (destination._state === "closing" || destination._state === "closed") {
					await shutdownOnDestinationClosed();
					return;
				}
				if (signal !== void 0 && signal.aborted) {
					await shutdownOnAbort();
					return;
				}
				if (writer.desiredSize === null) {
					await shutdownOnDestinationError(destination._storedError);
					return;
				}
				if (writer.desiredSize <= 0) try {
					if (await waitForWrite(writePromise) === WAIT_DESTINATION_CLOSED) {
						await shutdownOnDestinationClosed();
						return;
					}
				} catch (error) {
					if (!preventAbort && destination._state === "errored") {
						await shutdownOnDestinationError(destination._storedError);
						return;
					}
					if (source._state === "errored") {
						await shutdownOnSourceError(source._storedError);
						return;
					}
					if (destination._state === "errored") {
						await shutdownOnDestinationError(destination._storedError);
						return;
					}
					if (destination._state === "closing" || destination._state === "closed") {
						await shutdownOnDestinationClosed();
						return;
					}
					if (signal !== void 0 && signal.aborted && error === abortError) {
						await shutdownOnAbort();
						return;
					}
					await shutdownOnDestinationError(error);
					return;
				}
			}
			await shutdownOnClose();
		} catch (error) {
			if (shuttingDown) throw error;
			if (!preventAbort && destination._state === "errored") {
				await shutdownOnDestinationError(destination._storedError);
				return;
			}
			if (source._state === "errored") {
				await shutdownOnSourceError(source._storedError);
				return;
			}
			if (destination._state === "errored") {
				await shutdownOnDestinationError(destination._storedError);
				return;
			}
			if (destination._state === "closing" || destination._state === "closed") {
				await shutdownOnDestinationClosed();
				return;
			}
			if (signal !== void 0 && signal.aborted && error === abortError) {
				await shutdownOnAbort();
				return;
			}
			await shutdown(error, true);
			throw error;
		} finally {
			if (!shuttingDown) await shutdown(void 0, false);
		}
	}
	_Symbol$toStringTag$25 = Symbol.toStringTag;
	var ReadableStreamDefaultController = class {
		/** @internal */
		constructor(stream, startAlgorithm, pullAlgorithm, cancelAlgorithm, strategySizeAlgorithm, strategyHWM, isOwningStream) {
			_defineProperty(
				this,
				/** @internal */
				"_stream",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_queue",
				[]
			);
			_defineProperty(
				this,
				/** @internal */
				"_queueTotalSize",
				0
			);
			_defineProperty(
				this,
				/** @internal */
				"_started",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_closeRequested",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_pullAgain",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_pulling",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_strategySizeAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_strategyHWM",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_cancelAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_pullAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_canCloseOrEnqueue",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_pullIfNeeded",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_shouldPull",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_error",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_dequeue",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_isOwningStream",
				void 0
			);
			this._stream = stream;
			this._pullAlgorithm = pullAlgorithm;
			this._cancelAlgorithm = cancelAlgorithm;
			this._strategySizeAlgorithm = strategySizeAlgorithm;
			this._strategyHWM = strategyHWM;
			this._isOwningStream = isOwningStream;
			this._canCloseOrEnqueue = () => {
				return this._stream._state === "readable" && !this._closeRequested;
			};
			this._pullIfNeeded = () => {
				if (!this._shouldPull()) return;
				if (this._pulling) {
					this._pullAgain = true;
					return;
				}
				this._pulling = true;
				let pullResult;
				try {
					pullResult = this._pullAlgorithm();
				} catch (error) {
					this._pulling = false;
					this._error(error);
					return;
				}
				promiseThen$1(pullResult, () => {
					this._pulling = false;
					if (this._pullAgain) {
						this._pullAgain = false;
						this._pullIfNeeded();
					}
				}, (e) => {
					this._error(e);
				});
			};
			this._shouldPull = () => {
				if (this._stream._state !== "readable") return false;
				if (this._closeRequested) return false;
				if (!this._started) return false;
				const reader = this._stream._reader;
				if (reader && reader instanceof ReadableStreamDefaultReader && reader._readRequests.length > 0) return true;
				const desiredSize = this.desiredSize;
				if (desiredSize !== null && desiredSize > 0) return true;
				return false;
			};
			this._error = (e) => {
				if (this._stream._state !== "readable") return;
				this._queue = [];
				this._queueTotalSize = 0;
				this._stream._errorStream(e);
			};
			this._dequeue = () => {
				if (this._queue.length === 0) return void 0;
				const { value, size } = this._queue.shift();
				this._queueTotalSize = clampQueueTotalSize(this._queueTotalSize - size);
				return value;
			};
			stream._controller = this;
			let startResult;
			try {
				startResult = startAlgorithm();
			} catch (error) {
				this._started = true;
				this._error(error);
				return;
			}
			promiseThen$1(originalPromiseResolve$1(startResult), () => {
				this._started = true;
				this._pullIfNeeded();
			}, (e) => {
				this._started = true;
				this._error(e);
			});
		}
		get desiredSize() {
			const state = this._stream._state;
			if (state === "errored") return null;
			if (state === "closed") return 0;
			return this._strategyHWM - this._queueTotalSize;
		}
		close() {
			if (!this._canCloseOrEnqueue()) throw new TypeError("Cannot close a stream that is not readable");
			this._closeRequested = true;
			if (this._queue.length === 0) this._stream._closeStream();
		}
		enqueue(chunk, options) {
			if (!this._canCloseOrEnqueue()) throw new TypeError("Cannot enqueue to a stream that is not readable");
			let transfer;
			try {
				transfer = getControllerEnqueueTransferList(options);
			} catch (error) {
				if (this._isOwningStream) this._error(error);
				throw error;
			}
			if (!this._isOwningStream && transfer !== void 0 && transfer.length > 0) throw new TypeError("transfer list is not empty");
			let queuedChunk = chunk;
			if (this._isOwningStream) try {
				queuedChunk = cloneOwningStreamChunk(chunk, transfer);
			} catch (error) {
				this._error(error);
				throw error;
			}
			const reader = this._stream._reader;
			if (reader && reader instanceof ReadableStreamDefaultReader && reader._readRequests.length > 0) {
				reader._readRequests.shift().resolve(createReadResult(false, queuedChunk));
				this._pullIfNeeded();
				return;
			}
			let size;
			try {
				size = this._strategySizeAlgorithm(queuedChunk);
			} catch (error) {
				this._error(error);
				throw error;
			}
			try {
				size = toNumber(size);
			} catch (error) {
				this._error(error);
				throw error;
			}
			if (Number.isNaN(size) || !Number.isFinite(size) || size < 0) {
				const rangeError = /* @__PURE__ */ new RangeError("The size returned by the size() algorithm must be a non-negative finite number");
				this._error(rangeError);
				throw rangeError;
			}
			if (this._stream._state !== "readable") return;
			this._queue.push({
				value: queuedChunk,
				size
			});
			this._queueTotalSize = clampQueueTotalSize(this._queueTotalSize + size);
			this._pullIfNeeded();
		}
		error(e) {
			this._error(e);
		}
		get [_Symbol$toStringTag$25]() {
			return "ReadableStreamDefaultController";
		}
	};
	_Symbol$toStringTag2$2 = Symbol.toStringTag;
	var ReadableStreamBYOBRequest = class {
		/** @internal */
		constructor(controller, view, brand) {
			_defineProperty(
				this,
				/** @internal */
				"_controller",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_view",
				void 0
			);
			if (brand !== byobRequestBrand) throw new TypeError("Illegal constructor");
			if (!(controller instanceof ReadableByteStreamController)) throw new TypeError("ReadableStreamBYOBRequest must use a ReadableByteStreamController");
			if (!ArrayBuffer.isView(view)) throw new TypeError("ReadableStreamBYOBRequest requires an ArrayBufferView");
			this._controller = controller;
			this._view = view;
		}
		get view() {
			return this._view;
		}
		respond(bytesWritten) {
			if (this._view === null) throw new TypeError("This BYOB request has been invalidated");
			const view = this._view;
			if (view.byteLength < 0) throw new TypeError("Invalid BYOB view");
			if (view.byteOffset + view.byteLength > view.buffer.byteLength) throw new TypeError("Invalid BYOB view");
			const normalized = toNumber(bytesWritten);
			if (Number.isNaN(normalized) || !Number.isFinite(normalized) || normalized < 0) throw new RangeError("The view's response must be a non-negative finite number");
			if (!Number.isInteger(normalized) || normalized > view.byteLength) throw new RangeError("The view's response must be between 0 and the byteLength");
			if (isDetachedArrayBuffer(view.buffer)) throw new TypeError("Cannot read from detached ArrayBuffer");
			const controller = this._controller;
			if (controller._stream._state !== "readable") throw new TypeError("The stream is not in readable state");
			this._view = null;
			controller._respondToByobRequest(normalized);
		}
		respondWithNewView(view) {
			if (this._view === null) throw new TypeError("This BYOB request has been invalidated");
			if (!ArrayBuffer.isView(view)) throw new TypeError("view must be an ArrayBufferView");
			const controller = this._controller;
			if (controller._stream._state !== "readable") throw new TypeError("The stream is not in readable state");
			this._view = null;
			controller._respondWithNewViewToByobRequest(view);
		}
		get [_Symbol$toStringTag2$2]() {
			return "ReadableStreamBYOBRequest";
		}
	};
	_Symbol$toStringTag3 = Symbol.toStringTag;
	var ReadableByteStreamController = class {
		/** @internal */
		constructor() {
			_defineProperty(
				this,
				/** @internal */
				"_stream",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_queue",
				[]
			);
			_defineProperty(
				this,
				/** @internal */
				"_queueTotalSize",
				0
			);
			_defineProperty(
				this,
				/** @internal */
				"_started",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_closeRequested",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_pullAgain",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_pulling",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_strategyHWM",
				0
			);
			_defineProperty(
				this,
				/** @internal */
				"_cancelAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_pullAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_autoAllocateChunkSize",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_pendingPullIntos",
				[]
			);
			_defineProperty(
				this,
				/** @internal */
				"_byobRequest",
				null
			);
		}
		/** @internal */
		_setup(stream, startAlgorithm, pullAlgorithm, cancelAlgorithm, highWaterMark, autoAllocateChunkSize) {
			this._stream = stream;
			this._pullAlgorithm = pullAlgorithm;
			this._cancelAlgorithm = cancelAlgorithm;
			this._strategyHWM = highWaterMark;
			this._autoAllocateChunkSize = autoAllocateChunkSize;
			stream._controller = this;
			stream._isByteStream = true;
			let startResult;
			try {
				startResult = startAlgorithm();
			} catch (error) {
				this._started = true;
				this._error(error);
				return;
			}
			promiseThen$1(originalPromiseResolve$1(startResult), () => {
				this._started = true;
				this._pullIfNeeded();
			}, (e) => {
				this._started = true;
				this._error(e);
			});
		}
		get byobRequest() {
			if (this._byobRequest === null && this._pendingPullIntos.length > 0) {
				const firstDescriptor = this._pendingPullIntos[0];
				const view = new Uint8Array(firstDescriptor.buffer, firstDescriptor.byteOffset + firstDescriptor.bytesFilled, firstDescriptor.byteLength - firstDescriptor.bytesFilled);
				this._byobRequest = new ReadableStreamBYOBRequest(this, view, byobRequestBrand);
			}
			return this._byobRequest;
		}
		get desiredSize() {
			const state = this._stream._state;
			if (state === "errored") return null;
			if (state === "closed") return 0;
			return this._strategyHWM - this._queueTotalSize;
		}
		close() {
			if (this._closeRequested) throw new TypeError("Cannot close a stream that is already closing");
			if (this._stream._state !== "readable") throw new TypeError("Cannot close a stream that is not readable");
			this._closeRequested = true;
			if (this._queue.length === 0) {
				if (this._pendingPullIntos.length > 0) {
					const firstPullInto = this._pendingPullIntos[0];
					if (firstPullInto.bytesFilled % firstPullInto.elementSize !== 0) {
						const e = /* @__PURE__ */ new TypeError("Insufficient bytes to fill elements in the given buffer");
						this._error(e);
						throw e;
					}
					return;
				}
				this._stream._closeStream();
			}
		}
		enqueue(chunk) {
			if (!ArrayBuffer.isView(chunk)) throw new TypeError("chunk must be an ArrayBufferView");
			if (chunk.byteLength === 0) throw new TypeError("chunk must have non-zero byteLength");
			if (isDetachedArrayBuffer(chunk.buffer)) throw new TypeError("Cannot enqueue a view with a detached buffer");
			if (this._closeRequested) throw new TypeError("Cannot enqueue to a closing stream");
			if (this._stream._state !== "readable") throw new TypeError("Cannot enqueue to a stream that is not readable");
			const buffer = chunk.buffer;
			const byteOffset = chunk.byteOffset;
			const byteLength = chunk.byteLength;
			const transferredBuffer = transferArrayBuffer(buffer);
			const hadPendingPullIntos = this._pendingPullIntos.length > 0;
			enqueueByteStreamChunk(this, new Uint8Array(transferredBuffer, byteOffset, byteLength), true);
			if (hadPendingPullIntos && this._pendingPullIntos.length === 0) observeObjectPrototypeThen();
			this._pullIfNeeded();
		}
		error(e) {
			this._error(e);
		}
		/** @internal */
		_respondToByobRequest(bytesWritten) {
			if (this._pendingPullIntos.length === 0) return;
			if (!Number.isInteger(bytesWritten) || bytesWritten < 0) throw new TypeError("bytesWritten must be a non-negative integer");
			const firstPullInto = this._pendingPullIntos[0];
			if (isDetachedArrayBuffer(firstPullInto.buffer)) throw new TypeError("Cannot respond using a detached BYOB request buffer");
			if (bytesWritten > firstPullInto.byteLength - firstPullInto.bytesFilled) throw new RangeError("Too many bytes written");
			firstPullInto.bytesFilled += bytesWritten;
			if (this._closeRequested && this._queue.length === 0) {
				invalidateReadableByteStreamByobRequest(this);
				if (firstPullInto.bytesFilled % firstPullInto.elementSize !== 0) {
					const e = /* @__PURE__ */ new TypeError("Insufficient bytes to fill elements in the given buffer");
					this._error(e);
					throw e;
				}
				this._pendingPullIntos.shift();
				resolvePullIntoDescriptor(this, firstPullInto, true);
				this._stream._closeStream();
				return;
			}
			if (firstPullInto.bytesFilled < firstPullInto.minimumBytes) {
				if (bytesWritten > 0) transferPullIntoDescriptorBuffer(firstPullInto);
				invalidateReadableByteStreamByobRequest(this);
				this._pullIfNeeded();
				return;
			}
			invalidateReadableByteStreamByobRequest(this);
			this._pendingPullIntos.shift();
			resolvePullIntoDescriptor(this, firstPullInto, false);
			this._pullIfNeeded();
		}
		/** @internal */
		_respondWithNewViewToByobRequest(view) {
			if (this._pendingPullIntos.length === 0) return;
			if (!ArrayBuffer.isView(view)) throw new TypeError("View must be an ArrayBufferView");
			if (isDetachedArrayBuffer(view.buffer)) throw new TypeError("Cannot read from detached ArrayBuffer");
			if (view.buffer instanceof ArrayBuffer && isNonTransferableArrayBuffer(view.buffer)) throw new TypeError("Cannot read from non-transferable ArrayBuffer");
			const firstPullInto = this._pendingPullIntos[0];
			const expectedByteOffset = firstPullInto.byteOffset + firstPullInto.bytesFilled;
			const remainingBytes = firstPullInto.byteLength - firstPullInto.bytesFilled;
			const closeRequested = this._closeRequested && this._queue.length === 0;
			if (view.byteOffset !== expectedByteOffset) throw new RangeError("The supplied view has an unexpected byteOffset");
			if (closeRequested) {
				if (view.buffer.byteLength !== firstPullInto.bufferByteLength) throw new RangeError("The supplied view has an unexpected buffer length");
				if (view.byteLength !== 0) throw new TypeError("View must be zero-length when responding after close()");
			} else {
				if (view.byteLength === 0) throw new TypeError("View must be a non-empty ArrayBufferView");
				if (view.buffer.byteLength === 0) throw new TypeError("View buffer must have non-zero byteLength");
				if (view.buffer.byteLength !== firstPullInto.bufferByteLength) throw new RangeError("The supplied view has an unexpected buffer length");
			}
			if (view.byteLength > remainingBytes) throw new RangeError("The supplied view is larger than the pending BYOB request");
			this._pendingPullIntos.shift();
			invalidateReadableByteStreamByobRequest(this);
			const request = removePullIntoRequestFromReader(this, firstPullInto);
			if (view.buffer === firstPullInto.buffer) {
				firstPullInto.pendingRequest = request;
				firstPullInto.bytesFilled += view.byteLength;
				resolvePullIntoDescriptor(this, firstPullInto, closeRequested);
			} else if (request !== null) request.resolve(createReadResult(closeRequested, view));
			else if (view.byteLength > 0) enqueueByteStreamChunk(this, copyByteSlice(view.buffer, view.byteOffset, view.byteLength));
			if (closeRequested) {
				this._stream._closeStream();
				return;
			}
			this._pullIfNeeded();
		}
		/** @internal */
		_pullIfNeeded() {
			if (!this._shouldPull()) return;
			ensureAutoAllocatePullInto(this);
			if (this._pulling) {
				this._pullAgain = true;
				return;
			}
			this._pulling = true;
			let pullResult;
			try {
				pullResult = this._pullAlgorithm();
			} catch (error) {
				this._pulling = false;
				this._error(error);
				return;
			}
			promiseThen$1(pullResult, () => {
				this._pulling = false;
				if (this._pullAgain) {
					this._pullAgain = false;
					this._pullIfNeeded();
				}
			}, (e) => {
				this._error(e);
			});
		}
		/** @internal */
		_shouldPull() {
			if (this._stream._state !== "readable") return false;
			if (this._closeRequested) return false;
			if (!this._started) return false;
			const reader = this._stream._reader;
			if (reader) {
				if (reader instanceof ReadableStreamDefaultReader && reader._readRequests.length > 0) return true;
				if (reader instanceof ReadableStreamBYOBReader && reader._readIntoRequests.length > 0) return true;
			}
			const desiredSize = this.desiredSize;
			if (desiredSize !== null && desiredSize > 0) return true;
			return false;
		}
		/** @internal */
		_error(e) {
			if (this._stream._state !== "readable") return;
			this._queue = [];
			this._queueTotalSize = 0;
			const reader = this._stream._reader;
			if (reader && reader instanceof ReadableStreamBYOBReader) {
				{
					const __exactForOfIterator45 = reader._readIntoRequests[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep45 = __exactForOfIterator45.next();
						if (__exactForOfStep45.done) break;
						__exactForOfStep45.value.reject(e);
					}
				}
				reader._readIntoRequests = [];
			}
			this._pendingPullIntos = [];
			invalidateReadableByteStreamByobRequest(this);
			this._stream._errorStream(e);
		}
		/** @internal - used by default reader to process queued byte chunks */
		_dequeue() {
			if (this._queue.length === 0) return void 0;
			const { buffer, byteOffset, byteLength } = this._queue.shift();
			this._queueTotalSize = clampQueueTotalSize(this._queueTotalSize - byteLength);
			return new Uint8Array(buffer, byteOffset, byteLength);
		}
		/** @internal - Process a BYOB read request, called by ReadableStreamBYOBReader */
		_processReadIntoRequest(view, min, readIntoRequest) {
			const elementSize = view.BYTES_PER_ELEMENT || 1;
			const minimumBytes = min * elementSize;
			const viewConstructor = view.constructor || Uint8Array;
			const byteOffset = view.byteOffset;
			const byteLength = view.byteLength;
			const finishReadIntoRequest = () => {
				const reader = this._stream._reader;
				if (!(reader instanceof ReadableStreamBYOBReader)) return;
				const requestIndex = reader._readIntoRequests.indexOf(readIntoRequest);
				if (requestIndex !== -1) reader._readIntoRequests.splice(requestIndex, 1);
			};
			if (this._stream._state === "closed") {
				const emptyView = transferArrayBufferView(new viewConstructor(view.buffer, byteOffset, 0));
				finishReadIntoRequest();
				readIntoRequest.resolve(createReadResult(true, emptyView));
				return;
			}
			let bytesFilled = 0;
			if (this._queueTotalSize > 0) {
				const destBuffer = new Uint8Array(view.buffer, byteOffset, byteLength);
				while (bytesFilled < byteLength && this._queue.length > 0) {
					const front = this._queue[0];
					const bytesToCopy = Math.min(byteLength - bytesFilled, front.byteLength);
					const srcView = new Uint8Array(front.buffer, front.byteOffset, bytesToCopy);
					destBuffer.set(srcView, bytesFilled);
					bytesFilled += bytesToCopy;
					if (bytesToCopy === front.byteLength) this._queue.shift();
					else {
						front.byteOffset += bytesToCopy;
						front.byteLength -= bytesToCopy;
					}
					this._queueTotalSize = clampQueueTotalSize(this._queueTotalSize - bytesToCopy);
				}
				if (bytesFilled >= minimumBytes) {
					resolvePullIntoDescriptor(this, {
						buffer: view.buffer,
						bufferByteLength: view.buffer.byteLength,
						byteOffset,
						byteLength,
						minimumBytes,
						bytesFilled,
						elementSize,
						viewConstructor,
						readerType: "byob",
						pendingRequest: readIntoRequest
					}, false);
					if (this._closeRequested && this._queue.length === 0) this._stream._closeStream();
					else this._pullIfNeeded();
					return;
				}
			}
			if (this._closeRequested && this._queue.length === 0) {
				if (bytesFilled > 0) {
					if (bytesFilled < minimumBytes || bytesFilled % elementSize !== 0) {
						const e = /* @__PURE__ */ new TypeError("Insufficient bytes to fill elements in the given buffer");
						this._error(e);
						return;
					}
					resolvePullIntoDescriptor(this, {
						buffer: view.buffer,
						bufferByteLength: view.buffer.byteLength,
						byteOffset,
						byteLength,
						minimumBytes,
						bytesFilled,
						elementSize,
						viewConstructor,
						readerType: "byob",
						pendingRequest: readIntoRequest
					}, false);
				} else {
					const emptyView = transferArrayBufferView(new viewConstructor(view.buffer, byteOffset, 0));
					finishReadIntoRequest();
					readIntoRequest.resolve(createReadResult(true, emptyView));
				}
				this._stream._closeStream();
				return;
			}
			const pullIntoDescriptor = {
				buffer: view.buffer,
				bufferByteLength: view.buffer.byteLength,
				byteOffset,
				byteLength,
				minimumBytes,
				bytesFilled,
				elementSize,
				viewConstructor,
				readerType: "byob",
				pendingRequest: readIntoRequest
			};
			this._pendingPullIntos.push(pullIntoDescriptor);
			this._pullIfNeeded();
		}
		get [_Symbol$toStringTag3]() {
			return "ReadableByteStreamController";
		}
	};
	_Symbol$toStringTag4 = Symbol.toStringTag;
	var ReadableStreamDefaultReader = class {
		_initializeClosedPromise(state, value) {
			if (state === "pending") this._closedPromise = new Promise((resolve, reject) => {
				this._closedResolve = resolve;
				this._closedReject = reject;
			});
			else if (state === "resolved") this._closedPromise = originalPromiseResolve$1(value);
			else this._closedPromise = originalPromiseReject$1(value);
			markPromiseHandled(this._closedPromise);
		}
		constructor(stream) {
			_defineProperty(
				this,
				/** @internal */
				"_stream",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_readRequests",
				[]
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedPromise",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedResolve",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedReject",
				void 0
			);
			if (!(stream instanceof ReadableStream$1)) throw new TypeError("ReadableStreamDefaultReader constructor only accepts a ReadableStream");
			if (stream._reader !== void 0) throw new TypeError(getReadableStreamLockedMessage());
			stream._reader = this;
			this._stream = stream;
			if (stream._state === "closed") this._initializeClosedPromise("resolved", void 0);
			else if (stream._state === "errored") this._initializeClosedPromise("rejected", stream._storedError);
			else this._initializeClosedPromise("pending");
		}
		get closed() {
			return this._closedPromise;
		}
		read() {
			if (this._stream === void 0) return originalPromiseReject$1(/* @__PURE__ */ new TypeError("Reader has been released"));
			this._stream._disturbed = true;
			const p = new Promise((resolve, reject) => {
				const request = {
					resolve,
					reject
				};
				this._readRequests.push(request);
				this._processReadRequests();
				if (this._readRequests.includes(request)) {
					const controller = this._stream._controller;
					if (controller instanceof ReadableByteStreamController) {
						if (!attachDefaultReadRequestToPendingPullInto(controller, request)) ensureAutoAllocatePullInto(controller);
					}
					this._stream._controller._pullIfNeeded();
				}
			});
			markPromiseHandled(p);
			return p;
		}
		releaseLock() {
			if (this._stream === void 0) return;
			const releaseError = /* @__PURE__ */ new TypeError("Reader was released");
			const controller = this._stream._controller;
			if (this._readRequests.length > 0) {
				const requests = this._readRequests.slice();
				if (controller instanceof ReadableByteStreamController) detachPendingPullIntoRequests(controller, requests);
				{
					const __exactForOfIterator46 = this._readRequests[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep46 = __exactForOfIterator46.next();
						if (__exactForOfStep46.done) break;
						__exactForOfStep46.value.reject(releaseError);
					}
				}
				this._readRequests = [];
			}
			this._stream._reader = void 0;
			this._closedReject?.(releaseError);
			markPromiseHandled(this._closedPromise);
			this._stream = void 0;
		}
		cancel(reason) {
			if (this._stream === void 0) return originalPromiseReject$1(/* @__PURE__ */ new TypeError("Reader has been released"));
			this._stream._disturbed = true;
			return this._stream._cancelStream(reason, this);
		}
		/** @internal */
		_processReadRequests() {
			if (this._stream === void 0) return;
			const controller = this._stream._controller;
			if (!controller) return;
			let shouldClose = false;
			while (this._readRequests.length > 0) if (controller._queue.length > 0) {
				const chunk = controller._dequeue();
				this._readRequests.shift().resolve(createReadResult(false, chunk));
				if (controller._closeRequested && controller._queue.length === 0) {
					shouldClose = true;
					break;
				} else controller._pullIfNeeded();
			} else if (this._stream._state === "closed") this._readRequests.shift().resolve(createReadResult(true, void 0));
			else if (this._stream._state === "errored") this._readRequests.shift().reject(this._stream._storedError);
			else break;
			if (shouldClose) this._stream._closeStream();
		}
		get [_Symbol$toStringTag4]() {
			return "ReadableStreamDefaultReader";
		}
	};
	originalReadableStreamDefaultReaderRead = ReadableStreamDefaultReader.prototype.read;
	originalReadableStreamDefaultReaderReleaseLock = ReadableStreamDefaultReader.prototype.releaseLock;
	_Symbol$toStringTag5 = Symbol.toStringTag;
	var ReadableStreamBYOBReader = class {
		_initializeClosedPromise(state, value) {
			if (state === "pending") this._closedPromise = new Promise((resolve, reject) => {
				this._closedResolve = resolve;
				this._closedReject = reject;
			});
			else if (state === "resolved") this._closedPromise = originalPromiseResolve$1(value);
			else this._closedPromise = originalPromiseReject$1(value);
			markPromiseHandled(this._closedPromise);
		}
		constructor(stream) {
			_defineProperty(
				this,
				/** @internal */
				"_stream",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_readIntoRequests",
				[]
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedPromise",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedResolve",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_closedReject",
				void 0
			);
			if (stream._reader !== void 0) throw new TypeError(getReadableStreamLockedMessage());
			if (!stream._isByteStream) throw new TypeError("Cannot construct a ReadableStreamBYOBReader for a non-byte stream");
			stream._reader = this;
			this._stream = stream;
			if (stream._state === "closed") this._initializeClosedPromise("resolved", void 0);
			else if (stream._state === "errored") this._initializeClosedPromise("rejected", stream._storedError);
			else this._initializeClosedPromise("pending");
		}
		get closed() {
			return this._closedPromise;
		}
		async read(view, options) {
			if (this._stream === void 0) throw new TypeError("Reader has been released");
			if (options === null) throw new TypeError("Cannot read properties of null (reading options)");
			if (options !== void 0 && typeof options !== "object") throw new TypeError("Invalid read options");
			const normalizedMin = toNumber(options?.min ?? 1);
			if (!Number.isInteger(normalizedMin) || normalizedMin < 1) throw new TypeError("The 'min' option must be a positive integer");
			if (!ArrayBuffer.isView(view)) throw new TypeError("view must be an ArrayBufferView");
			if (isDetachedArrayBuffer(view.buffer)) throw new TypeError("Cannot read into detached ArrayBuffer");
			if (view.buffer instanceof ArrayBuffer && isNonTransferableArrayBuffer(view.buffer)) throw new TypeError("Cannot read into non-transferable ArrayBuffer");
			if (view.byteLength === 0) throw new TypeError("view must have non-zero byteLength");
			if (view.buffer.byteLength === 0) throw new TypeError("view's buffer must have non-zero byteLength");
			const elementSize = view.BYTES_PER_ELEMENT || 1;
			if (normalizedMin > (view instanceof DataView ? view.byteLength : Math.floor(view.byteLength / elementSize))) throw new RangeError("The 'min' option cannot be greater than view.byteLength");
			const stream = this._stream;
			stream._disturbed = true;
			if (normalizedMin > 0 && stream._state === "closed") return createReadResult(true, transferArrayBufferView(new view.constructor(view.buffer, view.byteOffset, 0)));
			if (stream._state === "errored") throw stream._storedError;
			const p = new Promise((resolve, reject) => {
				const readIntoRequest = {
					resolve,
					reject
				};
				this._readIntoRequests.push(readIntoRequest);
				stream._controller._processReadIntoRequest(view, normalizedMin, readIntoRequest);
			});
			markPromiseHandled(p);
			return p;
		}
		releaseLock() {
			if (this._stream === void 0) return;
			const releaseError = /* @__PURE__ */ new TypeError("Reader was released");
			const controller = this._stream._controller;
			if (this._readIntoRequests.length > 0) {
				const requests = this._readIntoRequests.slice();
				if (controller instanceof ReadableByteStreamController) detachPendingPullIntoRequests(controller, requests);
				{
					const __exactForOfIterator47 = this._readIntoRequests[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep47 = __exactForOfIterator47.next();
						if (__exactForOfStep47.done) break;
						__exactForOfStep47.value.reject(releaseError);
					}
				}
				this._readIntoRequests = [];
			}
			this._stream._reader = void 0;
			this._closedReject?.(releaseError);
			markPromiseHandled(this._closedPromise);
			this._stream = void 0;
		}
		cancel(reason) {
			if (this._stream === void 0) return originalPromiseReject$1(/* @__PURE__ */ new TypeError("Reader has been released"));
			this._stream._disturbed = true;
			return this._stream._cancelStream(reason, this);
		}
		get [_Symbol$toStringTag5]() {
			return "ReadableStreamBYOBReader";
		}
	};
	_Symbol$asyncIterator = Symbol.asyncIterator;
	var ReadableStreamAsyncIterator = class {
		constructor(reader, stream, preventCancel) {
			_defineProperty(this, "_reader", void 0);
			_defineProperty(this, "_stream", void 0);
			_defineProperty(this, "_preventCancel", void 0);
			_defineProperty(this, "_readerRead", void 0);
			_defineProperty(this, "_readerReleaseLock", void 0);
			_defineProperty(this, "_returnInProgress", null);
			_defineProperty(this, "_lockReleased", false);
			_defineProperty(this, "_finished", false);
			_defineProperty(this, "_operationQueue", []);
			_defineProperty(this, "_operationRunning", false);
			this._reader = reader;
			this._stream = stream;
			this._preventCancel = preventCancel;
			const readerRead = originalReadableStreamDefaultReaderRead ?? reader.read;
			const readerReleaseLock = originalReadableStreamDefaultReaderReleaseLock ?? reader.releaseLock;
			this._readerRead = readerRead.bind(reader);
			this._readerReleaseLock = readerReleaseLock.bind(reader);
			Object.defineProperty(this, "throw", {
				configurable: true,
				enumerable: false,
				writable: true,
				value: void 0
			});
			this._releaseLockSynchronously = this._releaseLockSynchronously.bind(this);
			this._advanceOperationQueue = this._advanceOperationQueue.bind(this);
			this._enqueueOperation = this._enqueueOperation.bind(this);
			this._nextImpl = this._nextImpl.bind(this);
			this._returnImpl = this._returnImpl.bind(this);
			Object.setPrototypeOf(this, readableStreamAsyncIteratorPrototype);
		}
		_releaseLockSynchronously() {
			if (this._lockReleased) return;
			this._lockReleased = true;
			if (this._reader._stream !== void 0) this._reader._stream = void 0;
			if (this._stream._reader === this._reader) this._stream._reader = void 0;
		}
		_advanceOperationQueue() {
			const next = this._operationQueue.shift();
			if (next) {
				next();
				return;
			}
			this._operationRunning = false;
		}
		_enqueueOperation(start) {
			return new Promise((resolve, reject) => {
				const run = () => {
					this._operationRunning = true;
					let operation;
					try {
						operation = start();
					} catch (error) {
						operation = originalPromiseReject$1(error);
					}
					promiseThen$1(operation, (value) => {
						resolve(value);
						this._advanceOperationQueue();
					}, (error) => {
						reject(error);
						this._advanceOperationQueue();
					});
				};
				if (this._operationRunning) this._operationQueue.push(run);
				else run();
			});
		}
		async _nextImpl() {
			if (this._returnInProgress !== null) {
				await this._returnInProgress;
				return createAsyncIteratorResult(true, void 0);
			}
			if (this._finished) return createAsyncIteratorResult(true, void 0);
			try {
				const result = await this._readerRead();
				if (result.done) {
					this._finished = true;
					this._releaseLockSynchronously();
				}
				return createAsyncIteratorResult(result.done, result.value);
			} catch (error) {
				this._finished = true;
				this._releaseLockSynchronously();
				throw error;
			}
		}
		next() {
			return this._enqueueOperation(() => this._nextImpl());
		}
		async _returnImpl(returnValue) {
			if (!this._lockReleased) {
				this._releaseLockSynchronously();
				this._readerReleaseLock();
			}
			if (this._returnInProgress !== null) {
				await this._returnInProgress;
				return createAsyncIteratorResult(true, returnValue);
			}
			if (this._finished) return createAsyncIteratorResult(true, returnValue);
			if (this._stream._state === "errored") throw this._stream._storedError;
			if (this._preventCancel) this._returnInProgress = originalPromiseResolve$1();
			else this._returnInProgress = this._stream._cancelStream(returnValue, this._reader);
			await this._returnInProgress;
			this._finished = true;
			return createAsyncIteratorResult(true, returnValue);
		}
		return(value) {
			const returnValue = value;
			if (!this._lockReleased && !this._operationRunning) {
				this._releaseLockSynchronously();
				this._readerReleaseLock();
			}
			return this._enqueueOperation(() => this._returnImpl(returnValue));
		}
		[_Symbol$asyncIterator]() {
			return this;
		}
	};
	const readableStreamAsyncIteratorPrototype = Object.create(asyncIteratorPrototype);
	Object.defineProperties(readableStreamAsyncIteratorPrototype, {
		next: {
			configurable: true,
			enumerable: true,
			writable: true,
			value: ReadableStreamAsyncIterator.prototype.next
		},
		return: {
			configurable: true,
			enumerable: true,
			writable: true,
			value: ReadableStreamAsyncIterator.prototype.return
		}
	});
	if (typeof Symbol === "function" && typeof Symbol.asyncIterator === "symbol") Object.defineProperty(readableStreamAsyncIteratorPrototype, Symbol.asyncIterator, {
		configurable: true,
		enumerable: false,
		writable: true,
		value: ReadableStreamAsyncIterator.prototype[Symbol.asyncIterator]
	});
	delete readableStreamAsyncIteratorPrototype.constructor;
	_Symbol$asyncIterator2 = Symbol.asyncIterator;
	_Symbol$toStringTag6 = Symbol.toStringTag;
	var ReadableStream$1 = class ReadableStream$1 {
		constructor(underlyingSource, strategy) {
			_defineProperty(
				this,
				/** @internal */
				"_state",
				"readable"
			);
			_defineProperty(
				this,
				/** @internal */
				"_storedError",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_reader",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_isByteStream",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_isOwningStream",
				false
			);
			_defineProperty(
				this,
				/** @internal - set to true when any read operation is performed on this stream */
				"_disturbed",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_controller",
				void 0
			);
			if (originalReadableStreamGetReader === null) originalReadableStreamGetReader = ReadableStream$1.prototype.getReader;
			if (underlyingSource === null) throw new TypeError("Cannot convert undefined or null to object");
			const source = underlyingSource === void 0 ? {} : Object(underlyingSource);
			const strat = strategy === void 0 ? {} : Object(strategy);
			const strategyHasSize = hasPropertyWithoutObjectPrototype(strat, "size");
			const strategyHasHighWaterMark = hasPropertyWithoutObjectPrototype(strat, "highWaterMark");
			const strategySize = strategyHasSize ? validateQueuingStrategySize(getPropertyValueWithoutObjectPrototype(strat, "size")) : (() => 1);
			let strategyHWM = validateHighWaterMark(getPropertyValueWithoutObjectPrototype(strat, "highWaterMark"), 1);
			const sourceType = getUnderlyingSourceType(source);
			const isByteStream = sourceType === "bytes";
			const isOwningStream = sourceType === "owning";
			const isDirectStream = sourceType === "direct";
			const sourceAsAny = source;
			const sourceStart = getPropertyValueWithoutObjectPrototype(sourceAsAny, "start");
			const sourcePull = getPropertyValueWithoutObjectPrototype(sourceAsAny, "pull");
			const sourceCancel = getPropertyValueWithoutObjectPrototype(sourceAsAny, "cancel");
			validateUnderlyingSourceMethod("start", sourceStart);
			validateUnderlyingSourceMethod("pull", sourcePull);
			validateUnderlyingSourceMethod("cancel", sourceCancel);
			if (isByteStream && strategyHasSize) throw new RangeError("The size option must not be specified for byte streams");
			if (isByteStream && !strategyHasHighWaterMark) strategyHWM = 0;
			if (isByteStream) {
				const byteSource = source;
				let autoAllocateChunkSize = void 0;
				if (byteSource.autoAllocateChunkSize !== void 0) {
					const converted = toNumber(byteSource.autoAllocateChunkSize);
					if (converted === 0) throw new TypeError("Invalid autoAllocateChunkSize");
					if (!Number.isFinite(converted) || !Number.isInteger(converted) || converted < 1) throw new RangeError("Invalid autoAllocateChunkSize");
					autoAllocateChunkSize = converted;
				}
				this._isByteStream = true;
				const byteController = new ReadableByteStreamController();
				const startAlgorithm = () => {
					if (sourceStart) return sourceStart.call(source, byteController);
				};
				const pullAlgorithm = () => {
					try {
						return originalPromiseResolve$1(sourcePull ? sourcePull.call(source, byteController) : void 0);
					} catch (e) {
						return originalPromiseReject$1(e);
					}
				};
				const cancelAlgorithm = (reason) => {
					try {
						return originalPromiseResolve$1(sourceCancel ? sourceCancel.call(source, reason) : void 0);
					} catch (e) {
						return originalPromiseReject$1(e);
					}
				};
				byteController._setup(this, startAlgorithm, pullAlgorithm, cancelAlgorithm, strategyHWM, autoAllocateChunkSize);
			} else {
				const sourceController = isDirectStream ? createDirectReadableStreamController(() => this._controller) : void 0;
				const startAlgorithm = () => {
					if (sourceStart) return sourceStart.call(source, sourceController ?? this._controller);
				};
				const pullAlgorithm = () => {
					try {
						return originalPromiseResolve$1(sourcePull ? sourcePull.call(source, sourceController ?? this._controller) : void 0);
					} catch (e) {
						return originalPromiseReject$1(e);
					}
				};
				const cancelAlgorithm = (reason) => {
					try {
						return originalPromiseResolve$1(sourceCancel ? sourceCancel.call(source, reason) : void 0);
					} catch (e) {
						return originalPromiseReject$1(e);
					}
				};
				this._isOwningStream = isOwningStream;
				this._controller = new ReadableStreamDefaultController(this, startAlgorithm, pullAlgorithm, cancelAlgorithm, strategySize, strategyHWM, isOwningStream);
			}
		}
		get locked() {
			return this._reader !== void 0;
		}
		cancel(reason) {
			if (this.locked) throw new TypeError("Cannot cancel a locked stream");
			this._disturbed = true;
			return this._cancelStream(reason);
		}
		getReader(options) {
			if (options === null) throw new TypeError("Cannot read properties of null (reading 'mode')");
			let mode;
			if (options !== void 0) {
				if (typeof options !== "object" && typeof options !== "function") throw new TypeError("ReadableStream getReader options must be an object");
				const modeValue = getPropertyValueWithoutObjectPrototype(options, "mode");
				if (modeValue !== void 0) mode = String(modeValue);
			}
			if (mode !== void 0 && mode !== "byob") throw new TypeError("Invalid reader options mode");
			if (mode === "byob") {
				if (!this._isByteStream) throw new TypeError("Cannot get a BYOB reader for a non-byte stream");
				return new ReadableStreamBYOBReader(this);
			}
			return new ReadableStreamDefaultReader(this);
		}
		pipeThrough(transform, options) {
			if (!isReadableStreamBrand(this)) throw new TypeError("Cannot pipe through from a non-ReadableStream");
			if (transform === null || transform === void 0) throw new TypeError("Cannot destructure property of undefined or null");
			if (this.locked) throw new TypeError("Cannot pipe a locked stream");
			const readable = transform.readable;
			if (!isReadableStreamBrand(readable)) throw new TypeError("The transform readable is not a ReadableStream");
			const writable = transform.writable;
			if (!isWritableStreamBrand(writable)) throw new TypeError("The transform writable is not a WritableStream");
			const pipeOptions = normalizePipeToOptions(options);
			if (writable.locked) throw new TypeError("Cannot pipe to a locked stream");
			markPromiseHandled(performPipeTo(this, writable, pipeOptions));
			return readable;
		}
		pipeTo(destination, options) {
			try {
				if (!isReadableStreamBrand(this)) throw new TypeError("Cannot pipe from a non-ReadableStream");
				if (!isWritableStreamBrand(destination)) throw new TypeError("Cannot pipe to a non-WritableStream");
				const pipeOptions = normalizePipeToOptions(options);
				const p = performPipeTo(this, destination, pipeOptions);
				markPromiseHandled(p);
				return p;
			} catch (error) {
				const p = originalPromiseReject$1(error);
				markPromiseHandled(p);
				return p;
			}
		}
		tee() {
			if (this.locked) throw new TypeError("Cannot tee a locked stream");
			const isByteStream = this._isByteStream;
			const isOwningStream = this._isOwningStream;
			if (isByteStream) {
				const sourceStream = this;
				const getReader = originalReadableStreamGetReader ?? sourceStream.getReader;
				let byobReader;
				let defaultReader = getReader.call(sourceStream);
				let reading = false;
				let sourceDone = false;
				let sourceError = null;
				let canceled1 = false;
				let canceled2 = false;
				let reason1;
				let reason2;
				let cancelPromise;
				let resolveCancel;
				let rejectCancel;
				let cancelStarted = false;
				let stream1Controller;
				let stream2Controller;
				let stream1;
				let stream2;
				const releaseSourceReader = (activeReader) => {
					if (activeReader === void 0 || activeReader._stream === void 0) return;
					if (sourceStream._reader === activeReader) sourceStream._reader = void 0;
					activeReader._stream = void 0;
				};
				const getBranchDemand = (controller, stream, canceled) => {
					if (canceled || controller === void 0 || stream === void 0) return 0;
					if (controller._pendingPullIntos.length > 0) {
						const descriptor = controller._pendingPullIntos[0];
						const minRemaining = descriptor.minimumBytes - descriptor.bytesFilled;
						const capacityRemaining = descriptor.byteLength - descriptor.bytesFilled;
						if (capacityRemaining <= 0) return 0;
						return Math.max(1, Math.min(capacityRemaining, Math.max(minRemaining, 1)));
					}
					const branchReader = stream._reader;
					if (branchReader instanceof ReadableStreamDefaultReader && branchReader._readRequests.length > 0) return 1;
					return 0;
				};
				const getBranchReadTarget = (controller, canceled, owner) => {
					if (canceled || controller === void 0 || controller._pendingPullIntos.length === 0) return;
					const descriptor = controller._pendingPullIntos[0];
					const capacityRemaining = descriptor.byteLength - descriptor.bytesFilled;
					if (capacityRemaining <= 0) return;
					return {
						owner,
						view: new Uint8Array(capacityRemaining),
						min: 1
					};
				};
				const hasReadDemand = () => getBranchDemand(stream1Controller, stream1, canceled1) > 0 || getBranchDemand(stream2Controller, stream2, canceled2) > 0;
				const ensureCancelPromise = () => {
					if (cancelPromise === void 0) cancelPromise = new Promise((resolve, reject) => {
						resolveCancel = resolve;
						rejectCancel = reject;
					});
					return cancelPromise;
				};
				const propagateSourceError = (error) => {
					if (sourceDone || sourceError !== null) return;
					sourceError = error;
					if (cancelPromise !== void 0 && !cancelStarted) resolveCancel?.();
					if (!canceled1) stream1Controller?.error(error);
					if (!canceled2) stream2Controller?.error(error);
				};
				const watchSourceReader = (activeReader) => {
					promiseThen$1(activeReader.closed, void 0, (error) => {
						propagateSourceError(error);
					});
				};
				const ensureByobReader = () => {
					if (byobReader !== void 0) return byobReader;
					releaseSourceReader(defaultReader);
					defaultReader = void 0;
					byobReader = getReader.call(sourceStream, { mode: "byob" });
					watchSourceReader(byobReader);
					return byobReader;
				};
				const ensureDefaultReader = () => {
					if (defaultReader !== void 0) return defaultReader;
					releaseSourceReader(byobReader);
					byobReader = void 0;
					defaultReader = getReader.call(sourceStream);
					watchSourceReader(defaultReader);
					return defaultReader;
				};
				const maybeFinalizeCancel = (alreadyCanceled) => {
					if (sourceDone) return originalPromiseResolve$1();
					if (sourceStream._state === "errored") {
						if (alreadyCanceled && cancelPromise !== void 0) return cancelPromise;
						return originalPromiseReject$1(sourceStream._storedError);
					}
					if (sourceError !== null) {
						if (alreadyCanceled && cancelPromise !== void 0) return cancelPromise;
						return originalPromiseReject$1(sourceError);
					}
					const promise = ensureCancelPromise();
					if (!canceled1 || !canceled2) return promise;
					if (cancelStarted) return promise;
					cancelStarted = true;
					const reasons = [];
					if (reason1 !== void 0) reasons.push(reason1);
					if (reason2 !== void 0) reasons.push(reason2);
					promiseThen$1((byobReader ?? ensureDefaultReader()).cancel(reasons.length > 0 ? reasons : void 0), () => resolveCancel?.(), (error) => rejectCancel?.(error));
					return promise;
				};
				const drainSource = async () => {
					if (reading || sourceDone || sourceError !== null) return;
					reading = true;
					try {
						while (true) {
							const demand1 = getBranchDemand(stream1Controller, stream1, canceled1);
							const demand2 = getBranchDemand(stream2Controller, stream2, canceled2);
							if (Math.max(demand1, demand2) === 0) break;
							const branch1Target = getBranchReadTarget(stream1Controller, canceled1, 1);
							const branch2Target = getBranchReadTarget(stream2Controller, canceled2, 2);
							const readTarget = branch1Target ?? branch2Target;
							let done = false;
							let chunk;
							try {
								if (readTarget !== void 0) {
									const result = await ensureByobReader().read(readTarget.view, { min: readTarget.min });
									done = result.done;
									chunk = result.value;
								} else {
									const result = await ensureDefaultReader().read();
									done = result.done;
									chunk = result.value;
								}
							} catch (error) {
								propagateSourceError(error);
								break;
							}
							if (done) {
								sourceDone = true;
								if (!canceled1) {
									if (stream1Controller !== void 0) finalizeReadableByteStreamBranchClose(stream1Controller);
								}
								if (!canceled2) {
									if (stream2Controller !== void 0) finalizeReadableByteStreamBranchClose(stream2Controller);
								}
								break;
							}
							const deliveredChunk = chunk;
							const deliverOriginalToBranch1 = !canceled1 && (readTarget?.owner === 1 || readTarget === void 0);
							const deliverOriginalToBranch2 = !canceled2 && (readTarget?.owner === 2 || readTarget === void 0 && canceled1);
							const chunk1 = !canceled1 ? deliverOriginalToBranch1 ? deliveredChunk : cloneChunkForTee(deliveredChunk) : void 0;
							const chunk2 = !canceled2 ? deliverOriginalToBranch2 ? deliveredChunk : cloneChunkForTee(deliveredChunk) : void 0;
							if (!canceled1) deliverReadableByteStreamBranchChunk(stream1Controller, chunk1, deliverOriginalToBranch1);
							if (!canceled2) deliverReadableByteStreamBranchChunk(stream2Controller, chunk2, deliverOriginalToBranch2);
						}
					} catch (error) {
						propagateSourceError(error);
					} finally {
						reading = false;
						if (!sourceDone && sourceError === null && hasReadDemand()) drainSource();
						if (sourceDone && !cancelStarted) resolveCancel?.();
					}
				};
				const createBranch = (branch) => new ReadableStream$1({
					type: "bytes",
					pull: () => {
						if (sourceError !== null) throw sourceError;
						if (sourceDone) return;
						drainSource();
					},
					cancel: (reason) => {
						let alreadyCanceled;
						if (branch === 1) {
							alreadyCanceled = canceled1;
							canceled1 = true;
							reason1 = reason;
						} else {
							alreadyCanceled = canceled2;
							canceled2 = true;
							reason2 = reason;
						}
						return maybeFinalizeCancel(alreadyCanceled);
					}
				});
				stream1 = createBranch(1);
				stream2 = createBranch(2);
				stream1Controller = stream1._controller;
				stream2Controller = stream2._controller;
				watchSourceReader(defaultReader);
				return [stream1, stream2];
			}
			const reader = (originalReadableStreamGetReader ?? this.getReader).call(this);
			let reading = false;
			let sourceDone = false;
			let sourceError = null;
			let canceled1 = false;
			let canceled2 = false;
			let reason1;
			let reason2;
			let pendingReads1 = 0;
			let pendingReads2 = 0;
			let stream1Controller;
			let stream2Controller;
			let stream1;
			let stream2;
			const hasReadDemand = () => {
				return !canceled1 && pendingReads1 > 0 || !canceled2 && pendingReads2 > 0;
			};
			const drainSource = async () => {
				if (reading || sourceDone || sourceError !== null) return;
				reading = true;
				try {
					while (hasReadDemand()) try {
						const { done, value } = await reader.read();
						if (done) {
							sourceDone = true;
							if (!canceled1) stream1Controller?.close();
							if (!canceled2) stream2Controller?.close();
							break;
						}
						if (!canceled1) {
							const value1 = isByteStream ? cloneChunkForTee(value) : value;
							stream1Controller?.enqueue(value1);
							if (pendingReads1 > 0) pendingReads1 -= 1;
						}
						if (!canceled2) {
							let value2 = cloneChunkForTee(value);
							if (isOwningStream) try {
								value2 = structuredClone(value);
							} catch (error) {
								canceled2 = true;
								pendingReads2 = 0;
								stream2Controller?.error(error);
								continue;
							}
							stream2Controller?.enqueue(value2);
							if (pendingReads2 > 0) pendingReads2 -= 1;
						}
					} catch (error) {
						sourceError = error;
						break;
					}
				} catch (error) {
					sourceError = error;
				} finally {
					reading = false;
					if (!sourceDone && sourceError === null && hasReadDemand()) drainSource();
					if (sourceError !== null) {
						if (!canceled1) stream1Controller?.error(sourceError);
						if (!canceled2) stream2Controller?.error(sourceError);
					}
				}
			};
			const pullAlgorithm = (branch) => {
				if (sourceError !== null) throw sourceError;
				if (sourceDone) return;
				if (branch === 1) pendingReads1 += 1;
				else pendingReads2 += 1;
				if (!canceled1 || !canceled2) drainSource();
			};
			const ensureCancel = () => {
				if (canceled1 && canceled2) {
					const reasons = [];
					if (reason1 !== void 0) reasons.push(reason1);
					if (reason2 !== void 0) reasons.push(reason2);
					reader.cancel(reasons.length > 0 ? reasons : void 0);
				}
			};
			const createBranch = (branch) => {
				const cancel = (reason) => {
					if (branch === 1) {
						canceled1 = true;
						reason1 = reason;
						pendingReads1 = 0;
						stream1Controller?.error(reason);
					} else {
						canceled2 = true;
						reason2 = reason;
						pendingReads2 = 0;
						stream2Controller?.error(reason);
					}
					ensureCancel();
				};
				if (isByteStream) return new ReadableStream$1({
					type: "bytes",
					pull: () => pullAlgorithm(branch),
					cancel
				});
				return new ReadableStream$1({
					pull: () => pullAlgorithm(branch),
					cancel
				});
			};
			stream1 = createBranch(1);
			stream2 = createBranch(2);
			stream1Controller = stream1._controller;
			stream2Controller = stream2._controller;
			return [stream1, stream2];
		}
		values(options) {
			const reader = (originalReadableStreamGetReader ?? this.getReader).call(this);
			const preventCancel = options?.preventCancel ?? false;
			return new ReadableStreamAsyncIterator(reader, this, preventCancel);
		}
		[_Symbol$asyncIterator2]() {
			return this.values();
		}
		async text() {
			const reader = this.getReader();
			const chunks = [];
			while (true) {
				const { done, value } = await reader.read();
				if (done) break;
				if (value instanceof Uint8Array) chunks.push(value);
				else if (typeof value === "string") chunks.push(new TextEncoder().encode(value));
				else if (value instanceof ArrayBuffer) chunks.push(new Uint8Array(value.slice(0)));
				else if (ArrayBuffer.isView(value)) chunks.push(new Uint8Array(value.buffer, value.byteOffset, value.byteLength));
				else throw new TypeError("ReadableStream chunk must be a string, Buffer, or ArrayBufferView.");
			}
			const totalLen = chunks.reduce((s, c) => s + c.byteLength, 0);
			const merged = new Uint8Array(totalLen);
			let offset = 0;
			{
				const __exactForOfIterator48 = chunks[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep48 = __exactForOfIterator48.next();
					if (__exactForOfStep48.done) break;
					const chunk = __exactForOfStep48.value;
					merged.set(chunk, offset);
					offset += chunk.byteLength;
				}
			}
			return new TextDecoder().decode(merged);
		}
		async arrayBuffer() {
			const reader = this.getReader();
			const chunks = [];
			while (true) {
				const { done, value } = await reader.read();
				if (done) break;
				if (value instanceof Uint8Array) chunks.push(value);
				else if (typeof value === "string") chunks.push(new TextEncoder().encode(value));
				else if (value instanceof ArrayBuffer) chunks.push(new Uint8Array(value.slice(0)));
				else if (ArrayBuffer.isView(value)) chunks.push(new Uint8Array(value.buffer, value.byteOffset, value.byteLength));
				else throw new TypeError("ReadableStream chunk must be a string, Buffer, or ArrayBufferView.");
			}
			const totalLen = chunks.reduce((s, c) => s + c.byteLength, 0);
			const merged = new Uint8Array(totalLen);
			let offset = 0;
			{
				const __exactForOfIterator49 = chunks[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep49 = __exactForOfIterator49.next();
					if (__exactForOfStep49.done) break;
					const chunk = __exactForOfStep49.value;
					merged.set(chunk, offset);
					offset += chunk.byteLength;
				}
			}
			return merged.buffer;
		}
		async bytes() {
			return new Uint8Array(await this.arrayBuffer());
		}
		async blob() {
			const bytes = await this.bytes();
			return new Blob([bytes]);
		}
		async json() {
			const text = await this.text();
			return JSON.parse(text);
		}
		/** @internal */
		_closeStream() {
			if (this._state !== "readable") return;
			if (this._controller instanceof ReadableByteStreamController) {
				resolvePendingPullIntosOnClose(this._controller);
				if (this._state !== "readable") return;
			}
			this._state = "closed";
			const reader = this._reader;
			if (reader) {
				if (reader instanceof ReadableStreamDefaultReader) reader._processReadRequests();
				else if (reader instanceof ReadableStreamBYOBReader && !(this._controller instanceof ReadableByteStreamController)) {
					{
						const __exactForOfIterator50 = reader._readIntoRequests[Symbol.iterator]();
						for (;;) {
							const __exactForOfStep50 = __exactForOfIterator50.next();
							if (__exactForOfStep50.done) break;
							__exactForOfStep50.value.resolve(createReadResult(true, new Uint8Array(0)));
						}
					}
					reader._readIntoRequests = [];
				}
				reader._closedResolve?.();
			}
		}
		/** @internal */
		_errorStream(e) {
			if (this._state !== "readable") return;
			this._state = "errored";
			this._storedError = e;
			const reader = this._reader;
			if (reader) {
				reader._closedReject?.(e);
				if (reader._closedPromise) markPromiseHandled(reader._closedPromise);
				if (reader instanceof ReadableStreamDefaultReader) {
					reader._readRequests.forEach((r) => r.reject(e));
					reader._readRequests = [];
				} else if (reader instanceof ReadableStreamBYOBReader) {
					reader._readIntoRequests.forEach((r) => r.reject(e));
					reader._readIntoRequests = [];
				}
			}
		}
		/** @internal */
		async _cancelStream(reason, reader) {
			if (this._state !== "readable") return;
			this._state = "closed";
			const targetReader = reader ?? this._reader;
			if (targetReader) {
				targetReader._closedResolve?.();
				if (targetReader instanceof ReadableStreamDefaultReader) {
					const doneResult = createReadResult(true, void 0);
					const requests = targetReader._readRequests.slice();
					targetReader._readRequests = [];
					requests.forEach((request) => request.resolve(doneResult));
				} else if (targetReader instanceof ReadableStreamBYOBReader) {
					const requests = targetReader._readIntoRequests.slice();
					targetReader._readIntoRequests = [];
					requests.forEach((request) => request.resolve(createReadResult(true, void 0)));
				}
			}
			if (this._controller instanceof ReadableByteStreamController) {
				this._controller._pendingPullIntos = [];
				invalidateReadableByteStreamByobRequest(this._controller);
			}
			if (this._controller) await this._controller._cancelAlgorithm(reason);
		}
		static from(asyncIterable) {
			if (!isObject(asyncIterable)) throw new TypeError("Cannot convert undefined or null to object");
			let iterator;
			let next;
			let returnFn;
			let done = false;
			const asyncIteratorMethod = getPropertyValueWithoutObjectPrototype(asyncIterable, Symbol.asyncIterator);
			if (asyncIteratorMethod !== void 0) if (asyncIteratorMethod === null) {} else {
				if (typeof asyncIteratorMethod !== "function") throw new TypeError("async iterator property is not callable");
				iterator = asyncIteratorMethod.call(asyncIterable);
				if (!isObject(iterator)) throw new TypeError("async iterator method did not return an object");
				next = getPropertyValueWithoutObjectPrototype(iterator, "next");
				if (typeof next !== "function") throw new TypeError("iterator.next must be callable");
				returnFn = getPropertyValueWithoutObjectPrototype(iterator, "return");
			}
			if (next === void 0) {
				const syncIteratorMethod = getPropertyValueWithoutObjectPrototype(asyncIterable, Symbol.iterator);
				if (typeof syncIteratorMethod !== "function") throw new TypeError("Cannot get iterator method");
				iterator = syncIteratorMethod.call(asyncIterable);
				if (!isObject(iterator)) throw new TypeError("iterator method did not return an object");
				next = getPropertyValueWithoutObjectPrototype(iterator, "next");
				if (typeof next !== "function") throw new TypeError("iterator.next must be callable");
				returnFn = void 0;
			}
			return new ReadableStream$1({
				async pull(controller) {
					if (done) {
						controller.close();
						return;
					}
					let nextResult;
					try {
						nextResult = await next.call(iterator);
					} catch (error) {
						controller.error(error);
						return;
					}
					if (!isObject(nextResult)) {
						controller.error(/* @__PURE__ */ new TypeError("iterator next must return an object"));
						return;
					}
					if (!isIteratorResult(nextResult)) {
						controller.error(/* @__PURE__ */ new TypeError("iterator.next result must be an object with done and value"));
						return;
					}
					if (nextResult.done) {
						done = true;
						controller.close();
					} else controller.enqueue(nextResult.value);
				},
				async cancel(reason) {
					if (returnFn === void 0) return;
					if (typeof returnFn !== "function") throw new TypeError("iterator.return must be callable");
					if (!isObject(await returnFn.call(iterator, reason))) throw new TypeError("iterator.return must return an object");
				}
			}, { highWaterMark: 0 });
		}
		get [_Symbol$toStringTag6]() {
			return "ReadableStream";
		}
	};
	originalReadableStreamGetReader = ReadableStream$1.prototype.getReader;
	_Symbol$toStringTag7 = Symbol.toStringTag;
	var ByteLengthQueuingStrategy = class {
		constructor(init) {
			_defineProperty(this, "highWaterMark", void 0);
			const highWaterMark = getQueuingStrategyHighWaterMark(init, "ByteLengthQueuingStrategy requires a highWaterMark");
			this.highWaterMark = highWaterMark;
		}
		get size() {
			return byteLengthSize;
		}
		get [_Symbol$toStringTag7]() {
			return "ByteLengthQueuingStrategy";
		}
	};
	_Symbol$toStringTag8 = Symbol.toStringTag;
	var CountQueuingStrategy = class {
		constructor(init) {
			_defineProperty(this, "highWaterMark", void 0);
			const highWaterMark = getQueuingStrategyHighWaterMark(init, "CountQueuingStrategy requires a highWaterMark");
			this.highWaterMark = highWaterMark;
		}
		get size() {
			return countSize;
		}
		get [_Symbol$toStringTag8]() {
			return "CountQueuingStrategy";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/streams/TransformStream.ts
	/**
	* TransformStream - WHATWG Streams API Implementation
	*
	* @see https://streams.spec.whatwg.org/#ts-class
	*
	* This implementation supports:
	* - Transformer with start/transform/flush
	* - TransformStreamDefaultController with enqueue/error/terminate
	* - Readable and writable sides
	* - Backpressure propagation
	* - Symbol.toStringTag
	*/
	init_defineProperty();
	let _Symbol$toStringTag$24, _Symbol$toStringTag2$1;
	const originalPromiseThen = Promise.prototype.then;
	const originalPromiseResolve = Promise.resolve.bind(Promise);
	const originalPromiseReject = Promise.reject.bind(Promise);
	function promiseThen(promise, onFulfilled, onRejected) {
		return originalPromiseThen.call(promise, onFulfilled, onRejected);
	}
	_Symbol$toStringTag$24 = Symbol.toStringTag;
	var TransformStreamDefaultController = class {
		/** @internal */
		constructor(stream) {
			_defineProperty(
				this,
				/** @internal */
				"_stream",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_transformAlgorithm",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_flushAlgorithm",
				void 0
			);
			this._stream = stream;
		}
		get desiredSize() {
			const readableController = this._stream._readable._controller;
			if (readableController === void 0) return null;
			return readableController.desiredSize;
		}
		enqueue(chunk) {
			const readableController = this._stream._readable._controller;
			if (readableController === void 0) throw new TypeError("Readable side has no controller");
			try {
				readableController.enqueue(chunk);
			} catch (e) {
				const writable = this._stream._writable;
				if (writable === void 0) this._stream._pendingWritableError = e;
				else writable._errorStream(e);
				throw this._stream._readable._storedError ?? e;
			}
			const backpressure = readableController.desiredSize !== null && readableController.desiredSize <= 0;
			if (backpressure !== this._stream._backpressure) this._stream._updateBackpressure(backpressure);
		}
		error(reason) {
			const readable = this._stream._readable;
			const writable = this._stream._writable;
			if (readable._controller) readable._controller._error(reason);
			if (writable === void 0) this._stream._pendingWritableError = reason;
			else writable._errorStream(reason);
			if (this._stream._backpressure) this._stream._updateBackpressure(false);
		}
		terminate() {
			if (this._stream._isTerminated) return;
			this._stream._isTerminated = true;
			const readable = this._stream._readable;
			const writable = this._stream._writable;
			if (readable._state === "readable" && readable._controller) readable._controller.close();
			const error = /* @__PURE__ */ new TypeError("TransformStream terminated");
			if (writable === void 0) this._stream._pendingWritableError = error;
			else writable._errorStream(error);
			if (this._stream._backpressure) this._stream._updateBackpressure(false);
		}
		get [_Symbol$toStringTag$24]() {
			return "TransformStreamDefaultController";
		}
	};
	_Symbol$toStringTag2$1 = Symbol.toStringTag;
	var TransformStream = class {
		get readable() {
			return this._readable;
		}
		get writable() {
			return this._writable;
		}
		constructor(transformer, writableStrategy, readableStrategy) {
			_defineProperty(
				this,
				/** @internal */
				"_readable",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_writable",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_controller",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_backpressure",
				false
			);
			_defineProperty(
				this,
				/** @internal */
				"_backpressureResolve",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_pendingWritableError",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_isTerminated",
				false
			);
			const trans = transformer ?? {};
			const transformAsAny = trans;
			let pendingCancel;
			const cancelAlgorithm = (reason) => {
				if (pendingCancel !== void 0) return pendingCancel;
				if (transformAsAny.cancel === void 0) {
					pendingCancel = originalPromiseResolve();
					return pendingCancel;
				}
				try {
					pendingCancel = originalPromiseResolve(transformAsAny.cancel(reason));
				} catch (e) {
					pendingCancel = originalPromiseReject(e);
				}
				return pendingCancel;
			};
			if (trans.readableType !== void 0) throw new RangeError("readableType not supported");
			if (trans.writableType !== void 0) throw new RangeError("writableType not supported");
			this._controller = new TransformStreamDefaultController(this);
			const controller = this._controller;
			const transformAlgorithm = trans.transform ? function(chunk) {
				try {
					return originalPromiseResolve(transformAsAny.transform(chunk, controller));
				} catch (e) {
					return originalPromiseReject(e);
				}
			} : function(chunk) {
				try {
					controller.enqueue(chunk);
				} catch (e) {
					return originalPromiseReject(e);
				}
				return originalPromiseResolve();
			};
			const flushAlgorithm = trans.flush ? function() {
				try {
					return originalPromiseResolve(transformAsAny.flush(controller));
				} catch (e) {
					return originalPromiseReject(e);
				}
			} : function() {
				return originalPromiseResolve();
			};
			controller._transformAlgorithm = transformAlgorithm;
			controller._flushAlgorithm = flushAlgorithm;
			const self = this;
			const effectiveReadableStrategy = readableStrategy ?? { highWaterMark: 0 };
			this._readable = new ReadableStream$1({
				pull: function() {
					if (self._backpressure) self._updateBackpressure(false);
					return originalPromiseResolve();
				},
				cancel: function(reason) {
					return promiseThen(cancelAlgorithm(reason), function() {
						self._writable._errorStream(reason);
					}, function(e) {
						self._writable._errorStream(e);
						throw e;
					});
				}
			}, effectiveReadableStrategy);
			this._writable = new WritableStream$1({
				start: function(ctrl) {
					if (transformAsAny.start) return transformAsAny.start(controller);
				},
				write: function(chunk) {
					if (self._backpressure) return promiseThen(new Promise(function(resolve) {
						const prevResolve = self._backpressureResolve;
						self._backpressureResolve = function(value) {
							if (prevResolve) prevResolve(value);
							resolve(void 0);
						};
					}), function() {
						return transformAlgorithm(chunk);
					});
					return transformAlgorithm(chunk);
				},
				close: function() {
					return promiseThen(flushAlgorithm(), function() {
						const readableController = self._readable._controller;
						if (readableController) {
							if (self._readable._state === "readable") readableController.close();
						}
					}, function(e) {
						if (self._readable._controller) self._readable._controller._error(e);
						throw e;
					});
				},
				abort: function(reason) {
					return promiseThen(cancelAlgorithm(reason), function() {
						if (self._readable._controller) self._readable._controller._error(reason);
					}, function(e) {
						if (self._readable._controller) self._readable._controller._error(e);
						throw e;
					});
				}
			}, writableStrategy);
			if (this._pendingWritableError !== void 0) {
				const pendingError = this._pendingWritableError;
				this._pendingWritableError = void 0;
				this._writable._errorStream(pendingError);
			}
			this._updateBackpressure(true);
		}
		/** @internal */
		_updateBackpressure(backpressure) {
			if (this._backpressure !== backpressure) {
				const wasBackpressured = this._backpressure;
				this._backpressure = backpressure;
				if (wasBackpressured && !backpressure) {
					if (this._backpressureResolve) {
						this._backpressureResolve(void 0);
						this._backpressureResolve = void 0;
					}
				}
			}
		}
		get [_Symbol$toStringTag2$1]() {
			return "TransformStream";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/body.ts
	init_defineProperty();
	/**
	* Text encoder/decoder - lazy initialization with fallbacks for Hermes.
	*/
	let _textEncoder = null;
	let _textDecoder = null;
	let _textDecoderNoBOM = null;
	function isBunCompatFetchTest() {
		if (globalThis.__exactRuntimeContext === "shell") return false;
		if (typeof process !== "object" || !process || typeof process.env !== "object") return false;
		return process.env.EXACT_COMPAT_TEST === "1" && process.env.EXACT_TEST_SECTION === "bun";
	}
	function normalizeBlobContentType(contentType) {
		if (!contentType) return "";
		const normalized = contentType.split(";").map((part) => part.trim()).filter(Boolean).join(";").toLowerCase();
		if (!isBunCompatFetchTest() || normalized.includes("charset=")) return normalized;
		const mediaType = normalized.split(";", 1)[0];
		return mediaType.startsWith("text/") || mediaType === "application/json" || mediaType === "application/javascript" || mediaType === "application/x-www-form-urlencoded" || mediaType === "application/xml" || mediaType.endsWith("+json") || mediaType.endsWith("+xml") ? `${normalized};charset=utf-8` : normalized;
	}
	function getTextEncoder() {
		if (_textEncoder) return _textEncoder;
		if (typeof TextEncoder !== "undefined") _textEncoder = new TextEncoder();
		else _textEncoder = { encode(str) {
			const bytes = new Uint8Array(str.length * 4);
			let offset = 0;
			for (let i = 0; i < str.length; i++) {
				const code = str.charCodeAt(i);
				if (code < 128) bytes[offset++] = code;
				else if (code < 2048) {
					bytes[offset++] = 192 | code >> 6;
					bytes[offset++] = 128 | code & 63;
				} else if (code >= 55296 && code <= 56319) {
					const low = str.charCodeAt(i + 1);
					if (low >= 56320 && low <= 57343) {
						const cp = (code - 55296) * 1024 + (low - 56320) + 65536;
						bytes[offset++] = 240 | cp >> 18;
						bytes[offset++] = 128 | cp >> 12 & 63;
						bytes[offset++] = 128 | cp >> 6 & 63;
						bytes[offset++] = 128 | cp & 63;
						i++;
					} else {
						bytes[offset++] = 239;
						bytes[offset++] = 191;
						bytes[offset++] = 189;
					}
				} else if (code >= 56320 && code <= 57343) {
					bytes[offset++] = 239;
					bytes[offset++] = 191;
					bytes[offset++] = 189;
				} else {
					bytes[offset++] = 224 | code >> 12;
					bytes[offset++] = 128 | code >> 6 & 63;
					bytes[offset++] = 128 | code & 63;
				}
			}
			return bytes.slice(0, offset);
		} };
		return _textEncoder;
	}
	function getTextDecoder() {
		if (_textDecoder) return _textDecoder;
		if (typeof TextDecoder !== "undefined") _textDecoder = new TextDecoder();
		else _textDecoder = { decode(buffer) {
			const bytes = buffer instanceof ArrayBuffer ? new Uint8Array(buffer) : arrayBufferViewToUint8Array(buffer);
			let result = "";
			let i = 0;
			while (i < bytes.length) {
				const byte = bytes[i];
				if (byte < 128) {
					result += String.fromCharCode(byte);
					i++;
				} else if ((byte & 224) === 192) {
					result += String.fromCharCode((byte & 31) << 6 | bytes[i + 1] & 63);
					i += 2;
				} else if ((byte & 240) === 224) {
					result += String.fromCharCode((byte & 15) << 12 | (bytes[i + 1] & 63) << 6 | bytes[i + 2] & 63);
					i += 3;
				} else if ((byte & 248) === 240) {
					const cp = (byte & 7) << 18 | (bytes[i + 1] & 63) << 12 | (bytes[i + 2] & 63) << 6 | bytes[i + 3] & 63;
					result += String.fromCodePoint(cp);
					i += 4;
				} else i++;
			}
			return result;
		} };
		return _textDecoder;
	}
	function getTextDecoderNoBOM() {
		if (_textDecoderNoBOM) return _textDecoderNoBOM;
		if (typeof TextDecoder !== "undefined") _textDecoderNoBOM = new TextDecoder("utf-8", {
			fatal: false,
			ignoreBOM: true
		});
		else _textDecoderNoBOM = getTextDecoder();
		return _textDecoderNoBOM;
	}
	function decodeTextForFormData(buffer) {
		const text = getTextDecoderNoBOM().decode(new Uint8Array(buffer));
		return text.startsWith("﻿") ? text.slice(1) : text;
	}
	/**
	* Check if a value is a Blob-like object.
	*/
	function isBlob(value) {
		return typeof value === "object" && value !== null && typeof value.arrayBuffer === "function" && typeof value.slice === "function" && typeof value.size === "number" && typeof value.type === "string";
	}
	/**
	* Check if a value is FormData-like.
	*/
	function isFormData(value) {
		return typeof value === "object" && value !== null && typeof value.append === "function" && typeof value.entries === "function";
	}
	/**
	* Check if a value is an ArrayBuffer.
	*/
	function isArrayBuffer(value) {
		return value instanceof ArrayBuffer;
	}
	/**
	* Check if a value is an ArrayBufferView (TypedArray or DataView).
	*/
	function isArrayBufferView(value) {
		return ArrayBuffer.isView(value);
	}
	function getArrayBufferViewByteLength(view) {
		const reportedByteLength = view.byteLength;
		const typedArrayLength = view.length;
		const bytesPerElement = view && view.constructor && typeof view.constructor.BYTES_PER_ELEMENT === "number" ? view.constructor.BYTES_PER_ELEMENT : null;
		if (typeof typedArrayLength === "number" && typeof bytesPerElement === "number") {
			const computedByteLength = typedArrayLength * bytesPerElement;
			if (computedByteLength >= 0 && computedByteLength <= reportedByteLength) return computedByteLength;
		}
		return reportedByteLength;
	}
	function arrayBufferViewToUint8Array(view) {
		return new Uint8Array(view.buffer, view.byteOffset, getArrayBufferViewByteLength(view));
	}
	function normalizeBodyChunk(chunk, source = "ReadableStream body") {
		if (chunk instanceof Uint8Array) return chunk;
		if (typeof chunk === "string") return getTextEncoder().encode(chunk);
		if (isArrayBuffer(chunk)) return new Uint8Array(chunk.slice(0));
		if (isArrayBufferView(chunk)) return arrayBufferViewToUint8Array(chunk);
		throw new TypeError(`${source} chunk must be a string, Buffer, or ArrayBufferView.`);
	}
	function normalizeReadableStreamBody(stream, source = "ReadableStream body") {
		const reader = stream.getReader();
		reader.closed.catch(function() {});
		let released = false;
		function releaseReader() {
			if (released) return;
			released = true;
			try {
				reader.releaseLock();
				reader.closed.catch(function() {});
			} catch {}
		}
		return new ReadableStream$1({
			pull(controller) {
				const readPromise = reader.read();
				readPromise.catch(function() {});
				return readPromise.then(function(result) {
					if (result.done) {
						releaseReader();
						controller.close();
						return;
					}
					try {
						controller.enqueue(normalizeBodyChunk(result.value, source));
					} catch (error) {
						try {
							reader.cancel(error).catch(function() {});
						} catch {}
						releaseReader();
						throw error;
					}
				}, function(error) {
					releaseReader();
					throw error;
				});
			},
			cancel(reason) {
				const cancelResult = reader.cancel(reason);
				if (cancelResult && typeof cancelResult.then === "function") return cancelResult.then(function() {
					releaseReader();
				}, function() {
					releaseReader();
				});
				releaseReader();
			}
		});
	}
	function isAsyncIterableBody(value) {
		return value !== null && typeof value === "object" && typeof value[Symbol.asyncIterator] === "function";
	}
	function createReadableStreamFromAsyncIterableBody(body, source = "Async iterable body") {
		const iterator = body[Symbol.asyncIterator]();
		return new ReadableStream$1({
			async pull(controller) {
				const result = await iterator.next();
				if (result.done) {
					controller.close();
					return;
				}
				controller.enqueue(normalizeBodyChunk(result.value, source));
			},
			async cancel(reason) {
				if (typeof iterator.return === "function") await iterator.return(reason);
			}
		});
	}
	async function asyncIterableToUint8Array(body, source = "Async iterable body") {
		const chunks = [];
		const iterator = body[Symbol.asyncIterator]();
		let shouldCloseIterator = false;
		try {
			while (true) {
				const result = await iterator.next();
				if (result.done) break;
				shouldCloseIterator = true;
				chunks.push(normalizeBodyChunk(result.value, source));
				shouldCloseIterator = false;
			}
		} catch (error) {
			if (shouldCloseIterator && typeof iterator.return === "function") try {
				await iterator.return();
			} catch {}
			throw error;
		}
		return concatUint8Arrays$1(chunks);
	}
	/**
	* Convert various body types to Uint8Array.
	*/
	async function bodyToUint8Array(body) {
		if (body === null || body === void 0) return null;
		if (typeof body === "string") return getTextEncoder().encode(body);
		if (isArrayBuffer(body)) return new Uint8Array(body);
		if (isArrayBufferView(body)) return arrayBufferViewToUint8Array(body);
		if (isBlob(body)) {
			const buffer = await body.arrayBuffer();
			return new Uint8Array(buffer);
		}
		if (typeof URLSearchParams !== "undefined" && body instanceof URLSearchParams) return getTextEncoder().encode(body.toString());
		if (isFormData(body)) {
			let cached = formDataEncodeCache.get(body);
			if (cached) return cached.body;
			if (typeof body._encode === "function") {
				const result = body._encode();
				formDataEncodeCache.set(body, result);
				return result.body;
			}
			const encoded = await encodeFormDataInternalAsync(body);
			formDataEncodeCache.set(body, encoded);
			return encoded.body;
		}
		if (isReadableStream(body)) return await readableStreamToUint8Array(normalizeReadableStreamBody(body));
		if (isAsyncIterableBody(body)) return await asyncIterableToUint8Array(body);
		throw new TypeError("Unsupported body type");
	}
	/**
	* Resolve a promise with a value, preventing Object.prototype.then from
	* making the value appear as a thenable (per Fetch spec, internal algorithms
	* should not be affected by prototype pollution).
	*/
	function resolveWithoutThenable(resolve, value) {
		if (value != null && typeof value === "object" && typeof value.then === "function") {
			Object.defineProperty(value, "then", {
				value: void 0,
				configurable: true,
				writable: true
			});
			resolve(value);
			delete value.then;
		} else resolve(value);
	}
	/**
	* Read a ReadableStream to Uint8Array.
	* Uses explicit promise chains (not async/await) to prevent compiled async
	* wrappers from creating intermediate promises that trigger Object.prototype.then.
	*/
	function readableStreamToUint8Array(stream, signal) {
		return new Promise(function(outerResolve, outerReject) {
			const reader = stream.getReader();
			reader.closed.catch(function() {});
			const chunks = [];
			let settled = false;
			function cleanupAbortListener() {
				if (signal) signal.removeEventListener("abort", abortReader);
			}
			function rejectWith(error) {
				if (settled) return;
				settled = true;
				cleanupAbortListener();
				try {
					reader.cancel(error).catch(function() {});
				} catch {}
				try {
					reader.releaseLock();
					reader.closed.catch(function() {});
				} catch {}
				outerReject(error);
			}
			function abortReader() {
				rejectWith(signal?.reason ?? new DOMException("The operation was aborted.", "AbortError"));
			}
			if (signal) {
				if (signal.aborted) {
					abortReader();
					return;
				}
				signal.addEventListener("abort", abortReader);
			}
			function pump() {
				if (settled) return;
				if (signal?.aborted) {
					abortReader();
					return;
				}
				const readPromise = reader.read();
				readPromise.catch(function() {});
				readPromise.then(function(result) {
					if (settled) return;
					if (result.done) {
						settled = true;
						cleanupAbortListener();
						reader.releaseLock();
						resolveWithoutThenable(outerResolve, concatUint8Arrays$1(chunks));
						return;
					}
					try {
						chunks.push(normalizeBodyChunk(result.value));
					} catch (error) {
						rejectWith(error);
						return;
					}
					if (signal?.aborted) {
						abortReader();
						return;
					}
					pump();
				}, function(err) {
					rejectWith(err);
				});
			}
			pump();
		});
	}
	/**
	* Concatenate multiple Uint8Arrays into one.
	*/
	function concatUint8Arrays$1(arrays) {
		const totalLength = arrays.reduce((acc, arr) => acc + arr.length, 0);
		const result = new Uint8Array(totalLength);
		let offset = 0;
		{
			const __exactForOfIterator51 = arrays[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep51 = __exactForOfIterator51.next();
				if (__exactForOfStep51.done) break;
				const arr = __exactForOfStep51.value;
				result.set(arr, offset);
				offset += arr.length;
			}
		}
		return result;
	}
	/**
	* Generate a boundary string for multipart/form-data.
	*/
	function generateBoundary$1() {
		return "----ExactFormBoundary" + Math.random().toString(36).slice(2);
	}
	/**
	* Cache for FormData encoding results to ensure consistent boundaries.
	* Using WeakMap so FormData objects can be garbage collected.
	*/
	const formDataEncodeCache = /* @__PURE__ */ new WeakMap();
	/**
	* Get the Content-Type header for a body type.
	* For FormData, this will cache the encoding result so that bodyToUint8Array
	* returns the same bytes with matching boundary.
	*/
	function getContentTypeForBody(body) {
		if (body === null || body === void 0) return null;
		if (typeof body === "string") return "text/plain;charset=UTF-8";
		if (typeof URLSearchParams !== "undefined" && body instanceof URLSearchParams) return "application/x-www-form-urlencoded;charset=UTF-8";
		if (isBlob(body) && body.type) return body.type;
		if (isFormData(body)) {
			let cached = formDataEncodeCache.get(body);
			if (!cached) {
				cached = encodeFormDataInternal(body);
				formDataEncodeCache.set(body, cached);
			}
			return cached.contentType;
		}
		return null;
	}
	/**
	* Internal sync encoding for FormData (uses _getBytes for Blobs).
	*/
	function encodeFormDataInternal(body) {
		if (typeof body._encode === "function") return body._encode();
		const boundary = generateBoundary$1();
		const parts = [];
		{
			const __exactForOfIterator52 = body.entries()[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep52 = __exactForOfIterator52.next();
				if (__exactForOfStep52.done) break;
				const [name, value] = __exactForOfStep52.value;
				if (typeof value === "string") parts.push(getTextEncoder().encode(`--${boundary}\r\nContent-Disposition: form-data; name="${name}"\r\n\r\n${value}\r\n`));
				else if (isBlob(value)) {
					const fileBytes = value._getBytes?.() ?? new Uint8Array(0);
					const fileName = value.name || "blob";
					const contentType = value.type || "application/octet-stream";
					parts.push(getTextEncoder().encode(`--${boundary}\r\nContent-Disposition: form-data; name="${name}"; filename="${fileName}"\r\nContent-Type: ${contentType}\r\n\r\n`));
					parts.push(fileBytes);
					parts.push(getTextEncoder().encode("\r\n"));
				}
			}
		}
		if (parts.length === 0) return {
			body: new Uint8Array(0),
			contentType: `multipart/form-data; boundary=${boundary}`
		};
		parts.push(getTextEncoder().encode(`--${boundary}--\r\n`));
		return {
			body: concatUint8Arrays$1(parts),
			contentType: `multipart/form-data; boundary=${boundary}`
		};
	}
	/**
	* Internal async encoding for FormData (uses arrayBuffer for Blobs).
	*/
	async function encodeFormDataInternalAsync(body) {
		if (typeof body._encode === "function") return body._encode();
		const boundary = generateBoundary$1();
		const parts = [];
		for (const [name, value] of body.entries()) if (typeof value === "string") parts.push(getTextEncoder().encode(`--${boundary}\r\nContent-Disposition: form-data; name="${name}"\r\n\r\n${value}\r\n`));
		else if (isBlob(value)) {
			const buffer = await value.arrayBuffer();
			const fileBytes = new Uint8Array(buffer);
			const fileName = value.name || "blob";
			const contentType = value.type || "application/octet-stream";
			parts.push(getTextEncoder().encode(`--${boundary}\r\nContent-Disposition: form-data; name="${name}"; filename="${fileName}"\r\nContent-Type: ${contentType}\r\n\r\n`));
			parts.push(fileBytes);
			parts.push(getTextEncoder().encode("\r\n"));
		}
		if (parts.length === 0) return {
			body: new Uint8Array(0),
			contentType: `multipart/form-data; boundary=${boundary}`
		};
		parts.push(getTextEncoder().encode(`--${boundary}--\r\n`));
		return {
			body: concatUint8Arrays$1(parts),
			contentType: `multipart/form-data; boundary=${boundary}`
		};
	}
	/**
	* Encode FormData body with its content type (for consistent boundary).
	* Returns both the body bytes and content-type header.
	*/
	function encodeFormData(body) {
		let cached = formDataEncodeCache.get(body);
		if (cached) return cached;
		if (typeof body._encode === "function") {
			const result = body._encode();
			formDataEncodeCache.set(body, result);
			return result;
		}
		const result = encodeFormDataInternal(body);
		formDataEncodeCache.set(body, result);
		return result;
	}
	/**
	* Parse array buffer as JSON.
	*/
	function parseJson(buffer) {
		const bytes = new Uint8Array(buffer);
		if (bytes.length >= 2) {
			if (bytes[0] === 254 && bytes[1] === 255 || bytes[0] === 255 && bytes[1] === 254) throw new SyntaxError("Invalid JSON: UTF-16 encoded content is not valid");
			if (bytes[0] === 0 || bytes[1] === 0) throw new SyntaxError("Invalid JSON: UTF-16 encoded content is not valid");
		}
		const text = getTextDecoder().decode(buffer);
		return JSON.parse(text);
	}
	/**
	* Parse array buffer as text.
	*/
	function parseText(buffer) {
		return getTextDecoder().decode(buffer);
	}
	/**
	* Create a ReadableStream from a Uint8Array.
	* Returns null if ReadableStream is not available (falls back to buffer-based reading).
	*/
	function createReadableStreamFromUint8Array(data) {
		if (data === null) return null;
		const streamData = new Uint8Array(data.byteLength);
		streamData.set(data);
		const streamCtor = globalThis.ReadableStream || ReadableStream$1;
		if (typeof streamCtor !== "function") return null;
		return new streamCtor({
			type: "bytes",
			start(controller) {
				if (streamData.byteLength > 0) controller.enqueue(streamData);
				controller.close();
			}
		});
	}
	/**
	* Extract the boundary parameter from a multipart/form-data Content-Type header.
	* @param contentType The Content-Type header value
	* @returns The boundary string, or null if not found
	*/
	function extractBoundary(contentType) {
		const match = contentType.match(/boundary=(?:"([^"]+)"|([^\s;]+))/i);
		if (!match) return null;
		return match[1] ?? match[2] ?? null;
	}
	/**
	* Find the index of a byte sequence (needle) within a Uint8Array (haystack),
	* starting from the given offset.
	*/
	function indexOfBytes(haystack, needle, offset = 0) {
		const haystackLen = haystack.length;
		const needleLen = needle.length;
		if (needleLen === 0) return offset;
		if (offset + needleLen > haystackLen) return -1;
		outer: for (let i = offset; i <= haystackLen - needleLen; i++) {
			for (let j = 0; j < needleLen; j++) if (haystack[i + j] !== needle[j]) continue outer;
			return i;
		}
		return -1;
	}
	/**
	* Parse a Content-Disposition header value to extract name and filename parameters.
	*/
	function parseContentDisposition(header) {
		let name = "";
		let filename;
		const nameMatch = header.match(/\bname=(?:"([^"]*(?:\\.[^"]*)*)"|([^\s;]+))/i);
		if (nameMatch) name = (nameMatch[1] ?? nameMatch[2] ?? "").replace(/\\"/g, "\"");
		const filenameMatch = header.match(/\bfilename=(?:"([^"]*(?:\\.[^"]*)*)"|([^\s;]+))/i);
		if (filenameMatch) filename = (filenameMatch[1] ?? filenameMatch[2] ?? "").replace(/\\"/g, "\"");
		return {
			name,
			filename
		};
	}
	/**
	* Parse the headers section of a multipart part.
	* Returns a map of lowercased header names to their values.
	*/
	function parsePartHeaders(headerBytes) {
		const headerText = getTextDecoder().decode(headerBytes);
		const headers = /* @__PURE__ */ new Map();
		const lines = headerText.split("\r\n");
		for (const line of lines) {
			if (!line) continue;
			const colonIndex = line.indexOf(":");
			if (colonIndex === -1) continue;
			const name = line.slice(0, colonIndex).trim().toLowerCase();
			const value = line.slice(colonIndex + 1).trim();
			headers.set(name, value);
		}
		return headers;
	}
	/**
	* Parse a multipart/form-data body into a FormData object.
	*
	* @param body The raw body bytes
	* @param boundary The boundary string from the Content-Type header
	* @returns A populated FormData object
	*/
	function parseMultipartFormData(body, boundary) {
		const encoder = getTextEncoder();
		const formData = new FormData();
		const dashBoundary = encoder.encode("--" + boundary);
		encoder.encode("\r\n");
		const doubleCrlf = encoder.encode("\r\n\r\n");
		let pos = indexOfBytes(body, dashBoundary, 0);
		if (pos === -1) throw new TypeError("Failed to parse multipart/form-data body: boundary not found");
		pos += dashBoundary.length;
		if (pos + 2 <= body.length && body[pos] === 45 && body[pos + 1] === 45) return formData;
		if (pos + 2 <= body.length && body[pos] === 13 && body[pos + 1] === 10) pos += 2;
		let foundClosingBoundary = false;
		while (pos < body.length) {
			const headersEnd = indexOfBytes(body, doubleCrlf, pos);
			if (headersEnd === -1) break;
			const headers = parsePartHeaders(body.slice(pos, headersEnd));
			const bodyStart = headersEnd + doubleCrlf.length;
			const nextBoundary = indexOfBytes(body, dashBoundary, bodyStart);
			if (nextBoundary === -1) break;
			let bodyEnd = nextBoundary;
			if (bodyEnd >= 2 && body[bodyEnd - 2] === 13 && body[bodyEnd - 1] === 10) bodyEnd -= 2;
			const partBody = body.slice(bodyStart, bodyEnd);
			const { name, filename } = parseContentDisposition(headers.get("content-disposition") ?? "");
			if (!name) {
				pos = nextBoundary + dashBoundary.length;
				if (pos + 2 <= body.length && body[pos] === 45 && body[pos + 1] === 45) {
					foundClosingBoundary = true;
					break;
				}
				if (pos + 2 <= body.length && body[pos] === 13 && body[pos + 1] === 10) pos += 2;
				continue;
			}
			if (filename !== void 0) {
				const contentType = headers.get("content-type") ?? "application/octet-stream";
				const file = new File([partBody], filename, { type: contentType });
				formData.append(name, file);
			} else {
				const text = getTextDecoder().decode(partBody);
				formData.append(name, text);
			}
			pos = nextBoundary + dashBoundary.length;
			if (pos + 2 <= body.length && body[pos] === 45 && body[pos + 1] === 45) {
				foundClosingBoundary = true;
				break;
			}
			if (pos + 2 <= body.length && body[pos] === 13 && body[pos + 1] === 10) pos += 2;
		}
		if (!foundClosingBoundary) throw new TypeError("Failed to parse multipart/form-data body: missing closing boundary");
		return formData;
	}
	/**
	* Body mixin base class.
	* Provides common body handling functionality for Request and Response.
	*/
	var BodyMixin = class {
		constructor() {
			_defineProperty(this, "_body", null);
			_defineProperty(this, "_bodyUsed", false);
			_defineProperty(this, "_bodyBuffer", null);
			_defineProperty(this, "_signal", null);
		}
		/**
		* A ReadableStream of the body contents.
		*/
		get body() {
			return this._body;
		}
		/**
		* A Boolean that indicates whether the body has been read.
		* Per spec: also true when the body stream has been disturbed (read from)
		* or locked (e.g., via getReader() or pipeTo()).
		*/
		get bodyUsed() {
			if (this._bodyUsed) return true;
			if (this._body !== null) {
				if (this._body._disturbed) return true;
			}
			return false;
		}
		/**
		* Whether this object has a body. Subclasses can override to check
		* additional state (e.g., Request._bodyInit).
		*/
		_hasBody() {
			return this._body !== null || this._bodyBuffer !== null;
		}
		/**
		* Determine whether a body read should fail before attempting consumption.
		* Bun compat expects a reused body to report "Body already used" even if the
		* underlying stream is already locked from the prior read.
		*/
		_getBodyReadPreconditionError() {
			if (this.bodyUsed) return new BodyConsumedError();
			if (this._body !== null && typeof this._body.locked !== "undefined" && this._body.locked) return /* @__PURE__ */ new TypeError(isBunCompatFetchTest() ? "ReadableStream is locked" : "Failed to execute: body stream is locked");
			return null;
		}
		/**
		* Mark body as used and throw if already used.
		* Per spec: consuming a null body returns empty without setting bodyUsed.
		*/
		_consumeBody() {
			if (this.bodyUsed) throw new BodyConsumedError();
			if (this._hasBody()) this._bodyUsed = true;
			if (this._body !== null && this._bodyBuffer !== null && !this._body.locked) {
				this._body._disturbed = true;
				try {
					this._body.getReader();
				} catch {}
			}
		}
		/**
		* Per Fetch spec: check if the associated abort signal is aborted, and if so
		* reject with the signal's reason.
		*/
		_checkAbortSignal() {
			if (this._signal && this._signal.aborted) return this._signal.reason ?? new DOMException("The operation was aborted.", "AbortError");
			return null;
		}
		/**
		* Returns a promise that resolves with an ArrayBuffer.
		* Note: uses explicit promise chain (not async) to avoid compiled async
		* wrapper creating intermediate promises that trigger unhandled rejection tracking.
		*/
		arrayBuffer() {
			const preconditionError = this._getBodyReadPreconditionError();
			if (preconditionError) return Promise.reject(preconditionError);
			try {
				this._consumeBody();
			} catch (e) {
				return Promise.reject(e);
			}
			return this._getBodyBuffer();
		}
		/**
		* Returns a promise that resolves with a Blob.
		*/
		blob() {
			const preconditionError = this._getBodyReadPreconditionError();
			if (preconditionError) return Promise.reject(preconditionError);
			return this.arrayBuffer().then((buffer) => {
				const type = normalizeBlobContentType(this._getContentType());
				return type ? new Blob([buffer], { type }) : new Blob([buffer]);
			});
		}
		/**
		* Returns a promise that resolves with a Uint8Array.
		*/
		bytes() {
			const preconditionError = this._getBodyReadPreconditionError();
			if (preconditionError) return Promise.reject(preconditionError);
			return this.arrayBuffer().then((buffer) => new Uint8Array(buffer));
		}
		/**
		* Returns a promise that resolves with a FormData.
		* Supports both multipart/form-data and application/x-www-form-urlencoded.
		*/
		formData() {
			const preconditionError = this._getBodyReadPreconditionError();
			if (preconditionError) return Promise.reject(preconditionError);
			try {
				this._consumeBody();
			} catch (e) {
				return Promise.reject(e);
			}
			const contentType = this._getContentType();
			const ct = contentType?.toLowerCase() ?? "";
			const isMultipart = ct.startsWith("multipart/form-data");
			const isUrlEncoded = ct.startsWith("application/x-www-form-urlencoded");
			const self = this;
			return this._getBodyBuffer().then(function(buffer) {
				if (!isMultipart && !isUrlEncoded) throw new TypeError("Failed to execute 'formData': Content-Type is not 'multipart/form-data' or 'application/x-www-form-urlencoded'");
				if (isMultipart) {
					const boundary = extractBoundary(contentType);
					if (!boundary) throw new TypeError("multipart/form-data Content-Type missing boundary");
					const bytes = new Uint8Array(buffer);
					if (bytes.length === 0) {
						if (self._hasBody()) return new FormData();
						throw new TypeError("Could not parse content as FormData.");
					}
					return parseMultipartFormData(bytes, boundary);
				}
				const text = decodeTextForFormData(buffer);
				const formData = new FormData();
				const params = new URLSearchParams(text);
				const paramEntries = Array.from(params);
				for (let i = 0; i < paramEntries.length; i++) formData.append(paramEntries[i][0], paramEntries[i][1]);
				return formData;
			});
		}
		/**
		* Returns a promise that resolves with the result of parsing the body as JSON.
		*/
		json() {
			const preconditionError = this._getBodyReadPreconditionError();
			if (preconditionError) return Promise.reject(preconditionError);
			return this.arrayBuffer().then(parseJson);
		}
		/**
		* Returns a promise that resolves with a string (decoded as UTF-8).
		*/
		text() {
			const preconditionError = this._getBodyReadPreconditionError();
			if (preconditionError) return Promise.reject(preconditionError);
			return this.arrayBuffer().then(parseText);
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/Request.ts
	/**
	* Request API Implementation
	*
	* Implements the WHATWG Fetch Standard Request interface.
	* @see https://fetch.spec.whatwg.org/#request-class
	*/
	init_defineProperty();
	/**
	* HTTP methods that cannot have a body.
	*/
	const METHODS_WITHOUT_BODY = ["GET", "HEAD"];
	/**
	* HTTP methods that are forbidden per the Fetch spec.
	* These must throw TypeError when used in a Request constructor.
	*/
	const FORBIDDEN_METHODS = [
		"CONNECT",
		"TRACE",
		"TRACK"
	];
	/**
	* Ports that are considered "bad" per the Fetch spec.
	* Fetching from these ports must be blocked.
	*/
	const BAD_PORTS = new Set([
		0,
		1,
		7,
		9,
		11,
		13,
		15,
		17,
		19,
		20,
		21,
		22,
		23,
		25,
		37,
		42,
		43,
		53,
		69,
		77,
		79,
		87,
		95,
		101,
		102,
		103,
		104,
		109,
		110,
		111,
		113,
		115,
		117,
		119,
		123,
		135,
		137,
		139,
		143,
		161,
		179,
		389,
		427,
		465,
		512,
		513,
		514,
		515,
		526,
		530,
		531,
		532,
		540,
		548,
		556,
		563,
		587,
		601,
		636,
		989,
		990,
		993,
		995,
		1719,
		1720,
		1723,
		2049,
		3659,
		4045,
		4190,
		5060,
		5061,
		6e3,
		6566,
		6665,
		6666,
		6667,
		6668,
		6669,
		6679,
		6697,
		10080
	]);
	function isBunCompatRequestTest() {
		if (globalThis.__exactRuntimeContext === "shell") return false;
		if (typeof process !== "object" || !process || typeof process.env !== "object") return false;
		return process.env.EXACT_COMPAT_TEST === "1" && process.env.EXACT_TEST_SECTION === "bun";
	}
	/**
	* The Request interface of the Fetch API represents a resource request.
	*/
	var Request = class Request extends BodyMixin {
		constructor(input, init) {
			super();
			_defineProperty(this, "_method", void 0);
			_defineProperty(this, "_url", void 0);
			_defineProperty(this, "_headers", void 0);
			_defineProperty(this, "_mode", void 0);
			_defineProperty(this, "_credentials", void 0);
			_defineProperty(this, "_cache", void 0);
			_defineProperty(this, "_redirect", void 0);
			_defineProperty(this, "_referrer", void 0);
			_defineProperty(this, "_referrerPolicy", void 0);
			_defineProperty(this, "_integrity", void 0);
			_defineProperty(this, "_priority", void 0);
			_defineProperty(this, "_keepalive", void 0);
			_defineProperty(this, "_keepaliveExplicitlySet", void 0);
			_defineProperty(this, "_bodyInit", null);
			if (!(this instanceof Request)) throw new TypeError("Failed to construct 'Request': Please use the 'new' operator, this DOM object constructor cannot be called as a function.");
			let url;
			let inputRequest = null;
			let inputRequestLike = null;
			if (typeof input === "string") url = input;
			else if (input instanceof URL) url = input.href;
			else if (input instanceof Request) {
				inputRequest = input;
				url = input.url;
			} else if (typeof input === "object" && input !== null && "url" in input) {
				inputRequestLike = input;
				url = String(inputRequestLike.url);
			} else url = String(input);
			if (typeof URL !== "undefined") try {
				const hasScheme = /^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(url);
				const locationHref = typeof globalThis.location === "object" && globalThis.location !== null && typeof globalThis.location.href === "string" ? globalThis.location.href : void 0;
				const processEnv = globalThis.__exactRuntimeContext === "shell" ? null : typeof process === "object" && process && typeof process.env === "object" ? process.env : null;
				const defaultBase = locationHref || (processEnv?.WPT_SERVER_URL ?? void 0) || (typeof globalThis.location === "string" ? globalThis.location : null);
				const baseForParse = hasScheme || !defaultBase ? void 0 : defaultBase;
				const parsedUrl = baseForParse ? new URL(url, baseForParse) : new URL(url);
				parsedUrl.hash = "";
				this._url = parsedUrl.href;
			} catch {
				throw new TypeError(`Invalid URL: ${url}`);
			}
			else {
				if (!url || typeof url !== "string") throw new TypeError(`Invalid URL: ${url}`);
				if (!/^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(url)) throw new TypeError(`Invalid URL: ${url}`);
				this._url = url;
			}
			if (init && "window" in init && init.window !== null && init.window !== void 0) throw new TypeError("Failed to construct 'Request': 'window' option must be null");
			if (init?.duplex !== void 0 && init.duplex !== "half") throw new TypeError("Failed to construct 'Request': The provided value '" + init.duplex + "' is not a valid enum value of type RequestDuplex.");
			const rawMethod = init?.method ?? inputRequest?.method ?? inputRequestLike?.method ?? "GET";
			if (typeof rawMethod === "string" && !/^[\w!#$%&'*+.^`|~-]+$/.test(rawMethod)) throw new TypeError(`Failed to construct 'Request': '${rawMethod}' is not a valid HTTP method.`);
			this._method = rawMethod.toUpperCase();
			if (FORBIDDEN_METHODS.includes(this._method)) throw new TypeError(`Failed to construct 'Request': '${this._method}' HTTP method is unsupported.`);
			if (typeof URL !== "undefined") try {
				const parsedForPort = new URL(this._url);
				if (parsedForPort.port && BAD_PORTS.has(Number(parsedForPort.port))) throw new TypeError(`Failed to construct 'Request': The port ${parsedForPort.port} is not allowed.`);
				if (parsedForPort.username || parsedForPort.password) throw new TypeError("Failed to construct 'Request': Request cannot be constructed from a URL that includes credentials.");
			} catch (e) {
				if (e instanceof TypeError) throw e;
			}
			const VALID_MODES = [
				"cors",
				"no-cors",
				"same-origin",
				"navigate"
			];
			const VALID_CREDENTIALS = [
				"omit",
				"same-origin",
				"include"
			];
			const VALID_CACHE = [
				"default",
				"no-store",
				"reload",
				"no-cache",
				"force-cache",
				"only-if-cached"
			];
			const VALID_REDIRECT = [
				"follow",
				"error",
				"manual"
			];
			const VALID_REFERRER_POLICY = [
				"",
				"no-referrer",
				"no-referrer-when-downgrade",
				"same-origin",
				"origin",
				"strict-origin",
				"origin-when-cross-origin",
				"strict-origin-when-cross-origin",
				"unsafe-url"
			];
			if (init?.mode !== void 0 && !VALID_MODES.includes(init.mode)) throw new TypeError(`Failed to construct 'Request': The provided value '${init.mode}' is not a valid enum value of type RequestMode.`);
			if (init?.credentials !== void 0 && !VALID_CREDENTIALS.includes(init.credentials)) throw new TypeError(`Failed to construct 'Request': The provided value '${init.credentials}' is not a valid enum value of type RequestCredentials.`);
			if (init?.cache !== void 0 && !VALID_CACHE.includes(init.cache)) throw new TypeError(`Failed to construct 'Request': The provided value '${init.cache}' is not a valid enum value of type RequestCache.`);
			if (init?.redirect !== void 0 && !VALID_REDIRECT.includes(init.redirect)) throw new TypeError(`Failed to construct 'Request': The provided value '${init.redirect}' is not a valid enum value of type RequestRedirect.`);
			if (init?.referrerPolicy !== void 0 && !VALID_REFERRER_POLICY.includes(init.referrerPolicy)) throw new TypeError(`Failed to construct 'Request': The provided value '${init.referrerPolicy}' is not a valid enum value of type ReferrerPolicy.`);
			const modeValue = init?.mode ?? inputRequest?.mode ?? inputRequestLike?.mode ?? "cors";
			if (init?.mode === "navigate") throw new TypeError("Failed to construct 'Request': 'navigate' mode can only be used in service workers.");
			this._mode = modeValue;
			this._credentials = init?.credentials ?? inputRequest?.credentials ?? inputRequestLike?.credentials ?? "same-origin";
			this._cache = init?.cache ?? inputRequest?.cache ?? inputRequestLike?.cache ?? "default";
			this._redirect = init?.redirect ?? inputRequest?.redirect ?? inputRequestLike?.redirect ?? "follow";
			const referrer = init?.referrer ?? inputRequest?.referrer ?? inputRequestLike?.referrer ?? "about:client";
			if (referrer !== "" && referrer !== "about:client" && referrer !== "no-referrer") {
				if (typeof URL !== "undefined") try {
					new URL(referrer);
				} catch {
					throw new TypeError(`Failed to construct 'Request': '${referrer}' is not a valid URL.`);
				}
			}
			this._referrer = referrer;
			this._referrerPolicy = init?.referrerPolicy ?? inputRequest?.referrerPolicy ?? inputRequestLike?.referrerPolicy ?? "";
			this._integrity = init?.integrity ?? inputRequest?.integrity ?? inputRequestLike?.integrity ?? "";
			const sourceSignal = init && "signal" in init ? init.signal : inputRequest?.signal ?? inputRequestLike?.signal ?? null;
			if (sourceSignal && typeof AbortController !== "undefined") {
				const ac = new AbortController();
				if (sourceSignal.aborted) ac.abort(sourceSignal.reason);
				else sourceSignal.addEventListener("abort", () => {
					ac.abort(sourceSignal.reason);
				}, { once: true });
				this._signal = ac.signal;
			} else this._signal = sourceSignal;
			if (init?.priority !== void 0 && ![
				"high",
				"low",
				"auto"
			].includes(init.priority)) throw new TypeError(`Failed to construct 'Request': The provided value '${init.priority}' is not a valid enum value of type RequestPriority.`);
			this._priority = init?.priority ?? inputRequestLike?.priority ?? "auto";
			const initHasKeepalive = init !== void 0 && init !== null && "keepalive" in init;
			const inputRequestLikeHasKeepalive = inputRequestLike !== null && inputRequestLike !== void 0 && "keepalive" in inputRequestLike;
			this._keepaliveExplicitlySet = initHasKeepalive || (inputRequest ? inputRequest._keepaliveExplicitlySet : inputRequestLikeHasKeepalive);
			this._keepalive = Boolean(init?.keepalive ?? inputRequest?.keepalive ?? inputRequestLike?.keepalive ?? false);
			if (this._cache === "only-if-cached" && this._mode !== "same-origin") throw new TypeError("Failed to construct 'Request': 'only-if-cached' cache mode requires 'same-origin' mode.");
			if (this._mode === "no-cors") {
				if (![
					"GET",
					"HEAD",
					"POST"
				].includes(this._method)) throw new TypeError(`Failed to construct 'Request': '${this._method}' is unsupported in no-cors mode.`);
			}
			this._headers = new Headers();
			this._headers._guard = this._mode === "no-cors" ? "request-no-cors" : "request";
			const headerInit = init?.headers ?? inputRequest?.headers ?? inputRequestLike?.headers;
			if (headerInit) {
				const sourceHeaders = headerInit instanceof Headers ? headerInit : new Headers(headerInit);
				const headerPairs = Array.from(sourceHeaders.entries());
				for (let i = 0; i < headerPairs.length; i++) {
					const [name, value] = headerPairs[i];
					this._headers.append(name, value);
				}
			}
			if (inputRequest && (inputRequest.bodyUsed || inputRequest.body !== null && inputRequest.body.locked) && init?.body === void 0 && inputRequest.body !== null) throw new TypeError("Failed to construct 'Request': Cannot construct a Request with a Request object that has already been used.");
			const sourceBody = inputRequest && init?.body === void 0 && !inputRequest.bodyUsed ? inputRequest._bodyInit : inputRequestLike && init?.body === void 0 ? inputRequestLike.body ?? null : null;
			const body = init?.body !== void 0 ? init.body : sourceBody;
			if (body !== null && METHODS_WITHOUT_BODY.includes(this._method)) throw new TypeError(`Request with ${this._method} method cannot have body`);
			const hasExplicitInitHeaders = init !== void 0 && init !== null && "headers" in (init || {});
			if (body !== null) {
				this._bodyInit = body;
				const contentType = getContentTypeForBody(body);
				if (contentType && !hasExplicitInitHeaders && !this._headers.has("content-type")) this._headers.set("content-type", contentType);
				if (isReadableStream(body)) {
					if (this._keepalive) throw new TypeError(isBunCompatRequestTest() ? "keepalive" : "Failed to construct 'Request': keepalive request cannot have a ReadableStream body.");
					if (body.locked || body._disturbed) throw new TypeError("Failed to construct 'Request': body ReadableStream is locked.");
					if (init?.body !== void 0 && isReadableStream(init.body) && init?.duplex !== "half" && !isBunCompatRequestTest()) throw new TypeError("Failed to construct 'Request': RequestInit must set duplex to 'half' when body is a ReadableStream.");
					const normalizedBody = normalizeReadableStreamBody(body, "Request body");
					this._bodyInit = normalizedBody;
					this._body = normalizedBody;
				} else if (isAsyncIterableBody(body)) {
					const asyncBody = createReadableStreamFromAsyncIterableBody(body, "Request body");
					this._bodyInit = asyncBody;
					this._body = asyncBody;
				} else if (typeof body === "string") {
					const bytes = getTextEncoder().encode(body);
					this._bodyBuffer = bytes.buffer;
					this._body = createReadableStreamFromUint8Array(bytes);
				} else if (body instanceof ArrayBuffer) {
					this._bodyBuffer = body.slice(0);
					this._body = createReadableStreamFromUint8Array(new Uint8Array(this._bodyBuffer));
				} else if (ArrayBuffer.isView(body)) {
					const bodyBytes = arrayBufferViewToUint8Array(body);
					this._bodyBuffer = bodyBytes.buffer.slice(bodyBytes.byteOffset, bodyBytes.byteOffset + bodyBytes.byteLength);
					this._body = createReadableStreamFromUint8Array(new Uint8Array(this._bodyBuffer));
				} else if (body instanceof URLSearchParams) {
					const bytes = getTextEncoder().encode(body.toString());
					this._bodyBuffer = bytes.buffer;
					this._body = createReadableStreamFromUint8Array(bytes);
				} else if (isFormData(body)) {
					const encoded = encodeFormData(body);
					const encodedBody = encoded.body instanceof Uint8Array ? encoded.body : new Uint8Array(encoded.body);
					this._bodyBuffer = encodedBody.buffer;
					this._body = createReadableStreamFromUint8Array(encodedBody);
					if (!this._headers.has("content-type")) this._headers.set("content-type", encoded.contentType);
				} else if (isBlob(body) && typeof body._getBytes === "function") {
					const bytes = body._getBytes();
					this._bodyBuffer = bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
					this._body = createReadableStreamFromUint8Array(bytes);
				} else if (isBlob(body) && typeof body.stream === "function") {
					const stream = body.stream();
					if (stream) this._body = stream;
				} else {
					const str = String(body);
					const bytes = getTextEncoder().encode(str);
					this._bodyBuffer = bytes.buffer;
					this._body = createReadableStreamFromUint8Array(bytes);
					this._bodyInit = str;
					if (!this._headers.has("content-type")) this._headers.set("content-type", "text/plain;charset=UTF-8");
				}
			}
			if (inputRequest && !inputRequest.bodyUsed) inputRequest.markBodyAsUsedForFetch();
		}
		/**
		* The HTTP method of the request.
		*/
		get method() {
			return this._method;
		}
		/**
		* The URL of the request.
		*/
		get url() {
			return this._url;
		}
		/**
		* The headers of the request.
		*/
		get headers() {
			return this._headers;
		}
		/**
		* The mode of the request (e.g., cors, no-cors, same-origin, navigate).
		*/
		get mode() {
			return this._mode;
		}
		/**
		* The credentials mode of the request (e.g., omit, same-origin, include).
		*/
		get credentials() {
			return this._credentials;
		}
		/**
		* The cache mode of the request.
		*/
		get cache() {
			return this._cache;
		}
		/**
		* The redirect mode of the request.
		*/
		get redirect() {
			return this._redirect;
		}
		/**
		* The referrer of the request.
		*/
		get referrer() {
			return this._referrer;
		}
		/**
		* The referrer policy of the request.
		*/
		get referrerPolicy() {
			return this._referrerPolicy;
		}
		/**
		* The subresource integrity metadata of the request.
		*/
		get integrity() {
			return this._integrity;
		}
		/**
		* The AbortSignal associated with the request.
		* Per spec: if no signal was provided, return the default signal (never aborts).
		* We cache this to ensure the same signal is returned on every access.
		*/
		get signal() {
			if (!this._signal && typeof AbortController !== "undefined") this._signal = new AbortController().signal;
			return this._signal;
		}
		/**
		* Get the body lazily as a ReadableStream.
		*/
		get body() {
			if (this._body) return this._body;
			if (this._bodyInit === null) return null;
			if (this._bodyBuffer) {
				this._body = createReadableStreamFromUint8Array(new Uint8Array(this._bodyBuffer));
				return this._body;
			}
			if (isReadableStream(this._bodyInit)) {
				this._body = this._bodyInit;
				return this._body;
			}
			if (isBlob(this._bodyInit) && typeof this._bodyInit.stream === "function") {
				const stream = this._bodyInit.stream();
				this._body = stream;
				return this._body;
			}
			return null;
		}
		/**
		* Whether this request has a body.
		*/
		_hasBody() {
			return this._body !== null || this._bodyBuffer !== null || this._bodyInit !== null;
		}
		/**
		* Get the Content-Type header value.
		*/
		_getContentType() {
			return this._headers.get("content-type");
		}
		/**
		* Get the body as ArrayBuffer.
		*/
		_getBodyBuffer() {
			if (this._bodyBuffer) {
				const buf = this._bodyBuffer;
				return new Promise(function(resolve) {
					resolveWithoutThenable(resolve, buf);
				});
			}
			const self = this;
			if (this._body) return new Promise(function(resolve, reject) {
				readableStreamToUint8Array(self._body).then(function(bytes) {
					self._bodyBuffer = bytes.buffer;
					resolveWithoutThenable(resolve, self._bodyBuffer);
				}, reject);
			});
			if (this._bodyInit === null) return Promise.resolve(/* @__PURE__ */ new ArrayBuffer(0));
			return new Promise(function(resolve, reject) {
				bodyToUint8Array(self._bodyInit).then(function(bytes) {
					self._bodyBuffer = bytes?.buffer ?? /* @__PURE__ */ new ArrayBuffer(0);
					resolveWithoutThenable(resolve, self._bodyBuffer);
				}, reject);
			});
		}
		/**
		* Get body as Uint8Array for native bridge.
		* For ReadableStream bodies, this will fully buffer the stream.
		*/
		async getBodyAsUint8Array() {
			return bodyToUint8Array(this._bodyInit);
		}
		/**
		* Check if the request body is a ReadableStream (streaming upload).
		* When true, the body should be sent incrementally rather than buffered.
		*/
		isBodyStream() {
			return this._bodyInit !== null && isReadableStream(this._bodyInit);
		}
		/**
		* Get the body as a ReadableStream for streaming upload.
		* Returns null if the body is not a ReadableStream.
		* The caller is responsible for reading and consuming the stream.
		*/
		getBodyStream() {
			if (this._bodyInit !== null && isReadableStream(this._bodyInit)) return this._bodyInit;
			return null;
		}
		/**
		* Mark the request body as consumed when the request is dispatched.
		* This mirrors fetch() behavior where request body streams become disturbed.
		*/
		markBodyAsUsedForFetch() {
			if (this._bodyInit === null && this._body === null && this._bodyBuffer === null) return;
			this._consumeBody();
		}
		/**
		* The destination of the request (not used in mobile context).
		*/
		get destination() {
			return "";
		}
		/**
		* Whether the request will be kept alive.
		*/
		get keepalive() {
			return this._keepalive;
		}
		/**
		* Whether the keepalive option was explicitly provided on this request.
		*/
		hasExplicitKeepalive() {
			return this._keepaliveExplicitlySet;
		}
		/**
		* The duplex mode of the request. Always "half" per spec.
		*/
		/**
		* The priority of the request.
		*/
		get priority() {
			return this._priority;
		}
		get duplex() {
			return "half";
		}
		/**
		* Whether this is a reload navigation. Always false in non-browser context.
		*/
		get isReloadNavigation() {
			return false;
		}
		/**
		* Whether this is a history navigation. Always false in non-browser context.
		*/
		get isHistoryNavigation() {
			return false;
		}
		/**
		* Creates a copy of the Request object.
		*/
		clone() {
			if (this._bodyUsed) throw new TypeError("Cannot clone a Request whose body has already been used");
			let clonedBody = this._bodyInit;
			let duplex;
			if (this._body !== null && this._bodyBuffer === null && this._bodyInit !== null && isReadableStream(this._bodyInit)) {
				const [sourceBody, teeBody] = this._body.tee();
				this._body = sourceBody;
				this._bodyInit = sourceBody;
				clonedBody = teeBody;
				duplex = "half";
			} else if (clonedBody !== null && isReadableStream(clonedBody)) duplex = "half";
			const cloneInit = {
				method: this._method,
				headers: new Headers(this._headers),
				body: clonedBody,
				mode: this._mode,
				credentials: this._credentials,
				cache: this._cache,
				redirect: this._redirect,
				referrer: this._referrer,
				referrerPolicy: this._referrerPolicy,
				integrity: this._integrity,
				signal: this._signal,
				duplex
			};
			if (this._keepaliveExplicitlySet) cloneInit.keepalive = this._keepalive;
			return new Request(this.url, cloneInit);
		}
	};
	Object.defineProperty(Request, "name", {
		value: "Request",
		configurable: true
	});
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/Response.ts
	/**
	* Response API Implementation
	*
	* Implements the WHATWG Fetch Standard Response interface.
	* @see https://fetch.spec.whatwg.org/#response-class
	*/
	init_defineProperty();
	/**
	* HTTP status codes that represent redirects.
	*/
	const REDIRECT_STATUS_CODES$1 = [
		301,
		302,
		303,
		307,
		308
	];
	/**
	* HTTP status codes that are considered null body statuses.
	*/
	const NULL_BODY_STATUS_CODES = [
		204,
		205,
		304
	];
	/**
	* Default HTTP status text for common status codes.
	* Used when the native bridge doesn't provide a status text
	* (e.g., NSURLSession on macOS doesn't expose HTTP reason phrases,
	* and HTTP/2 doesn't have reason phrases at all).
	*/
	const DEFAULT_STATUS_TEXT = {
		100: "Continue",
		101: "Switching Protocols",
		200: "OK",
		201: "Created",
		202: "Accepted",
		203: "Non-Authoritative Information",
		204: "No Content",
		205: "Reset Content",
		206: "Partial Content",
		300: "Multiple Choices",
		301: "Moved Permanently",
		302: "Found",
		303: "See Other",
		304: "Not Modified",
		307: "Temporary Redirect",
		308: "Permanent Redirect",
		400: "Bad Request",
		401: "Unauthorized",
		403: "Forbidden",
		404: "Not Found",
		405: "Method Not Allowed",
		406: "Not Acceptable",
		408: "Request Timeout",
		409: "Conflict",
		410: "Gone",
		411: "Length Required",
		412: "Precondition Failed",
		413: "Payload Too Large",
		414: "URI Too Long",
		415: "Unsupported Media Type",
		416: "Range Not Satisfiable",
		417: "Expectation Failed",
		422: "Unprocessable Entity",
		429: "Too Many Requests",
		500: "Internal Server Error",
		501: "Not Implemented",
		502: "Bad Gateway",
		503: "Service Unavailable",
		504: "Gateway Timeout"
	};
	function isBunCompatResponseTest() {
		if (globalThis.__exactRuntimeContext === "shell") return false;
		if (typeof process !== "object" || !process || typeof process.env !== "object") return false;
		return process.env.EXACT_COMPAT_TEST === "1" && process.env.EXACT_TEST_SECTION === "bun";
	}
	/**
	* The Response interface of the Fetch API represents the response to a request.
	*/
	var Response$1 = class Response$1 extends BodyMixin {
		constructor(body, init) {
			super();
			_defineProperty(this, "_status", void 0);
			_defineProperty(this, "_statusText", void 0);
			_defineProperty(this, "_headers", void 0);
			_defineProperty(this, "_ok", void 0);
			_defineProperty(this, "_type", void 0);
			_defineProperty(this, "_url", void 0);
			_defineProperty(this, "_redirected", void 0);
			this._status = Number(init?.status ?? 200);
			this._statusText = init?.statusText !== void 0 ? String(init.statusText) : "";
			this._ok = this._status >= 200 && this._status < 300;
			this._type = "default";
			this._url = "";
			this._redirected = false;
			if (!Number.isFinite(this._status) || !Number.isInteger(this._status) || this._status < 200 || this._status > 599) throw new RangeError(`Failed to construct 'Response': The status provided (${this._status}) is outside the range [200, 599].`);
			if (this._statusText) for (let i = 0; i < this._statusText.length; i++) {
				const c = this._statusText.charCodeAt(i);
				if (c > 255 || c < 32 && c !== 9) throw new TypeError(`Failed to construct 'Response': Invalid statusText`);
			}
			if (body !== null && body !== void 0 && NULL_BODY_STATUS_CODES.includes(this._status)) throw new TypeError("Response with null body status cannot have body");
			this._headers = new Headers(init?.headers);
			this._headers._guard = "response";
			if (body !== null && body !== void 0) {
				if (isReadableStream(body)) {
					if (body.locked || body._disturbed) throw new TypeError("Failed to construct 'Response': body ReadableStream is locked.");
					this._body = normalizeReadableStreamBody(body, "Response body");
				} else if (isAsyncIterableBody(body)) this._body = createReadableStreamFromAsyncIterableBody(body, "Response body");
				else if (typeof body === "string") {
					const bytes = getTextEncoder().encode(body);
					this._bodyBuffer = bytes.buffer;
					this._body = createReadableStreamFromUint8Array(bytes);
					if (!this._headers.has("content-type")) this._headers.set("content-type", "text/plain;charset=UTF-8");
				} else if (body instanceof ArrayBuffer) {
					this._bodyBuffer = body.slice(0);
					this._body = createReadableStreamFromUint8Array(new Uint8Array(this._bodyBuffer));
				} else if (ArrayBuffer.isView(body)) {
					this._bodyBuffer = body.buffer.slice(body.byteOffset, body.byteOffset + body.byteLength);
					this._body = createReadableStreamFromUint8Array(new Uint8Array(this._bodyBuffer));
				} else if (body instanceof Blob) {
					if (typeof body._getBytes === "function") {
						const bytes = body._getBytes();
						this._bodyBuffer = bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
						this._body = createReadableStreamFromUint8Array(bytes);
					} else if (typeof body.stream === "function") {
						const stream = body.stream();
						if (stream) this._body = stream;
						else this._body = createReadableStreamFromUint8Array(new Uint8Array(0));
					}
					if (body.type && !this._headers.has("content-type")) this._headers.set("content-type", body.type);
				} else if (body instanceof URLSearchParams) {
					const bytes = getTextEncoder().encode(body.toString());
					this._bodyBuffer = bytes.buffer;
					this._body = createReadableStreamFromUint8Array(bytes);
					if (!this._headers.has("content-type")) this._headers.set("content-type", "application/x-www-form-urlencoded;charset=UTF-8");
				} else if (isFormData(body)) {
					const encoded = encodeFormData(body);
					this._bodyBuffer = encoded.body.buffer;
					this._body = createReadableStreamFromUint8Array(encoded.body);
					if (!this._headers.has("content-type")) this._headers.set("content-type", encoded.contentType);
				}
			}
		}
		/**
		* Whether the response was successful (status 200-299).
		*/
		get ok() {
			return this._ok;
		}
		/**
		* The HTTP status code of the response.
		*/
		get status() {
			return this._status;
		}
		/**
		* The HTTP status message of the response.
		*/
		get statusText() {
			return this._statusText;
		}
		/**
		* The headers of the response.
		*/
		get headers() {
			return this._headers;
		}
		/**
		* The type of the response.
		*/
		get type() {
			return this._type;
		}
		/**
		* The URL of the response.
		*/
		get url() {
			if (this._url && this._url.includes("#")) return this._url.split("#")[0];
			return this._url;
		}
		/**
		* Whether the response is the result of a redirect.
		*/
		get redirected() {
			return this._redirected;
		}
		/**
		* Get the Content-Type header value.
		*/
		_getContentType() {
			return this._headers.get("content-type");
		}
		/**
		* Get the body as ArrayBuffer.
		* Uses explicit promise chains (not async) and resolveWithoutThenable to
		* prevent Object.prototype.then from intercepting resolution values.
		*/
		_getBodyBuffer() {
			if (this._bodyBuffer) {
				const buf = this._bodyBuffer;
				return new Promise(function(resolve) {
					resolveWithoutThenable(resolve, buf);
				});
			}
			if (this._body === null) return Promise.resolve(/* @__PURE__ */ new ArrayBuffer(0));
			const self = this;
			return new Promise(function(resolve, reject) {
				readableStreamToUint8Array(self._body).then(function(bytes) {
					self._bodyBuffer = bytes.buffer;
					resolveWithoutThenable(resolve, self._bodyBuffer);
				}, reject);
			});
		}
		/**
		* Creates a copy of the Response object.
		*/
		clone() {
			if (this._bodyUsed) throw new TypeError("Cannot clone a Response whose body has already been used");
			const cloned = new Response$1(null, {
				status: this._status,
				statusText: this._statusText,
				headers: new Headers(this._headers)
			});
			cloned._ok = this._ok;
			cloned._type = this._type;
			cloned._url = this._url;
			cloned._redirected = this._redirected;
			cloned._bodyBuffer = this._bodyBuffer;
			if (this._body) {
				const [stream1, stream2] = this._body.tee();
				this._body = stream1;
				cloned._body = stream2;
			}
			return cloned;
		}
		/**
		* Creates a new Response representing a network error.
		*/
		static error() {
			const response = new Response$1(null);
			response._status = 0;
			response._statusText = "";
			response._ok = false;
			response._type = "error";
			response._headers = new Headers();
			response._headers._guard = "immutable";
			return response;
		}
		/**
		* Creates a new Response for a redirect to the specified URL.
		*/
		static redirect(url, status = 302) {
			const bunCompat = isBunCompatResponseTest();
			const rawStatus = typeof status === "object" && status !== null && "status" in status ? status.status : status;
			const normalizedStatus = rawStatus === void 0 || rawStatus === null || bunCompat && typeof rawStatus !== "number" ? 302 : Number(rawStatus);
			if (!REDIRECT_STATUS_CODES$1.includes(normalizedStatus)) throw new RangeError("Invalid redirect status code");
			let finalUrl = String(url);
			if (finalUrl.startsWith("://")) try {
				const base = typeof globalThis.location !== "undefined" ? globalThis.location.href : void 0;
				if (base) {
					const schemeMatch = /^[a-zA-Z][a-zA-Z0-9+.-]*:/.exec(String(base));
					if (schemeMatch) finalUrl = schemeMatch[0] + finalUrl.slice(1);
				}
			} catch {}
			if (!(bunCompat && finalUrl.startsWith("://")) && typeof URL !== "undefined") try {
				const base = typeof globalThis.location !== "undefined" ? globalThis.location.href : void 0;
				finalUrl = new URL(finalUrl, base).href;
			} catch {
				if (!bunCompat) throw new TypeError("Invalid URL");
			}
			return new Response$1(null, {
				status: normalizedStatus,
				statusText: "",
				headers: { Location: finalUrl }
			});
		}
		/**
		* Creates a new Response with a JSON body.
		*/
		static json(data, init) {
			const body = JSON.stringify(data);
			if (body === void 0) throw new TypeError(isBunCompatResponseTest() ? "Value is not JSON serializable" : "Failed to execute 'json' on 'Response': The data is not JSON serializable");
			const status = init?.status ?? 200;
			if (NULL_BODY_STATUS_CODES.includes(status)) throw new TypeError("Response with null body status cannot have body");
			const response = new Response$1(body, init);
			const userHeaders = init?.headers;
			let userProvidedContentType = false;
			if (userHeaders) {
				if (userHeaders instanceof Headers) userProvidedContentType = userHeaders.has("content-type");
				else if (Array.isArray(userHeaders)) userProvidedContentType = userHeaders.some((h) => Array.isArray(h) && h[0].toLowerCase() === "content-type");
				else if (typeof userHeaders === "object") userProvidedContentType = Object.keys(userHeaders).some((k) => k.toLowerCase() === "content-type");
			}
			if (!userProvidedContentType) response._headers.set("content-type", isBunCompatResponseTest() ? "application/json;charset=utf-8" : "application/json");
			return response;
		}
		/**
		* Detect if a redirect occurred by comparing the request URL to the response URL.
		* The native bridge may not always set `redirected: true`, so we also check
		* whether the response URL differs from the original request URL.
		*/
		static _detectRedirected(nativeRedirected, responseUrl, requestUrl) {
			if (nativeRedirected) return true;
			if (!requestUrl || !responseUrl) return false;
			return responseUrl !== requestUrl;
		}
		/**
		* Create a Response from native response data.
		* @param nativeResponse The native response data
		* @param requestUrl The original request URL, used to detect redirects by URL comparison
		*/
		static fromNative(nativeResponse, requestUrl) {
			const status = Number(nativeResponse.status);
			const response = new Response$1(null);
			response._status = status;
			response._statusText = nativeResponse.statusText || DEFAULT_STATUS_TEXT[status] || "";
			response._headers = Headers.fromTupleArray(nativeResponse.headers);
			response._headers._guard = "response";
			response._url = nativeResponse.url;
			response._redirected = Response$1._detectRedirected(nativeResponse.redirected, nativeResponse.url, requestUrl);
			response._ok = status >= 200 && status < 300;
			response._type = "basic";
			if (!NULL_BODY_STATUS_CODES.includes(response._status)) {
				if (nativeResponse.body) {
					const bytes = new Uint8Array(nativeResponse.body);
					response._bodyBuffer = nativeResponse.body;
					response._body = createReadableStreamFromUint8Array(bytes);
				} else if (isBunCompatResponseTest()) {
					const bytes = new Uint8Array(0);
					response._bodyBuffer = bytes.buffer;
					response._body = createReadableStreamFromUint8Array(bytes);
				}
			}
			return response;
		}
		/**
		* Create a Response from native streaming response data with a body stream.
		* @param nativeResponse The native streaming response data
		* @param bodyStream The ReadableStream for the response body
		* @param requestUrl The original request URL, used to detect redirects by URL comparison
		*/
		static fromNativeStreaming(nativeResponse, bodyStream, requestUrl) {
			const status = Number(nativeResponse.status);
			const response = new Response$1(null);
			response._status = status;
			response._statusText = nativeResponse.statusText || DEFAULT_STATUS_TEXT[status] || "";
			response._headers = Headers.fromTupleArray(nativeResponse.headers);
			response._headers._guard = "immutable";
			if (!NULL_BODY_STATUS_CODES.includes(status)) response._body = bodyStream;
			response._url = nativeResponse.url;
			response._redirected = Response$1._detectRedirected(nativeResponse.redirected, nativeResponse.url, requestUrl);
			response._ok = status >= 200 && status < 300;
			response._type = "basic";
			return response;
		}
	};
	Object.defineProperty(Response$1, "name", {
		value: "Response",
		configurable: true
	});
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/cookie-jar.ts
	init_defineProperty();
	const MAX_COOKIES_PER_DOMAIN = 300;
	const MAX_COOKIES_TOTAL = 3e3;
	const MAX_COOKIE_SIZE = 4096;
	let _runtimeOrigin = null;
	/**
	* Get the runtime origin. Falls back to location.href, then exact://app.
	*/
	function getRuntimeOrigin() {
		if (_runtimeOrigin) return _runtimeOrigin;
		const location = globalThis.location;
		if (location && typeof location === "object") {
			const href = typeof location.href === "string" ? location.href : null;
			if (href) try {
				_runtimeOrigin = new URL(href);
				return _runtimeOrigin;
			} catch {}
		}
		_runtimeOrigin = new URL("exact://app");
		return _runtimeOrigin;
	}
	/**
	* Get the default cookie path from a request URL.
	* Per RFC 6265 Section 5.1.4.
	*/
	function defaultCookiePath(url) {
		const uriPath = url.pathname;
		if (!uriPath || uriPath[0] !== "/" || uriPath === "/") return "/";
		const lastSlash = uriPath.lastIndexOf("/");
		if (lastSlash <= 0) return "/";
		return uriPath.substring(0, lastSlash);
	}
	/**
	* Check if a cookie domain matches a request host.
	* Per RFC 6265 Section 5.1.3.
	*/
	function domainMatch(cookieDomain, requestHost) {
		if (cookieDomain === requestHost) return true;
		if (requestHost.endsWith(cookieDomain)) {
			const prefixLen = requestHost.length - cookieDomain.length;
			if (prefixLen > 0 && requestHost[prefixLen - 1] === ".") {
				if (!isIPAddress(requestHost)) return true;
			}
		}
		if (cookieDomain.startsWith(".")) {
			const withoutDot = cookieDomain.substring(1);
			if (requestHost === withoutDot || requestHost.endsWith("." + withoutDot)) return true;
		}
		return false;
	}
	/**
	* Check if a cookie path matches a request path.
	* Per RFC 6265 Section 5.1.4.
	*/
	function pathMatch(cookiePath, requestPath) {
		if (cookiePath === requestPath) return true;
		if (requestPath.startsWith(cookiePath)) {
			if (cookiePath.endsWith("/")) return true;
			if (requestPath[cookiePath.length] === "/") return true;
		}
		return false;
	}
	/**
	* Rough check for IP address (IPv4 or IPv6).
	*/
	function isIPAddress(host) {
		if (host.includes(":")) return true;
		const parts = host.split(".");
		return parts.length === 4 && parts.every((p) => /^\d+$/.test(p));
	}
	/**
	* Get the registrable domain (eTLD+1) from a hostname.
	* Simplified: returns last two labels (e.g. "example.com" from "sub.example.com").
	*/
	function getRegistrableDomain(host) {
		const parts = host.split(".");
		if (parts.length <= 2) return host;
		return parts.slice(-2).join(".");
	}
	/**
	* Check if two URLs are same-site (same registrable domain).
	*/
	function isSameSite(a, b) {
		return getRegistrableDomain(a.hostname.toLowerCase()) === getRegistrableDomain(b.hostname.toLowerCase());
	}
	/**
	* Check if two URLs are same-origin.
	*/
	function isSameOrigin(a, b) {
		return a.origin === b.origin;
	}
	/**
	* Parse a date string from Expires attribute.
	* Returns unix ms timestamp or NaN.
	*/
	function parseExpiresDate(dateStr) {
		return Date.parse(dateStr);
	}
	/**
	* Parse a single Set-Cookie header value.
	* Per RFC 6265 Section 5.2.
	*/
	function parseSetCookie(header) {
		const semiIndex = header.indexOf(";");
		const nameValuePart = semiIndex === -1 ? header : header.substring(0, semiIndex);
		const attributesPart = semiIndex === -1 ? "" : header.substring(semiIndex + 1);
		const eqIndex = nameValuePart.indexOf("=");
		if (eqIndex === -1) return null;
		const name = nameValuePart.substring(0, eqIndex).trim();
		const value = nameValuePart.substring(eqIndex + 1).trim();
		if (!name) return null;
		const result = {
			name,
			value,
			secure: false,
			httpOnly: false,
			sameSite: "lax"
		};
		if (attributesPart) {
			const attrs = attributesPart.split(";");
			for (const attr of attrs) {
				const trimmed = attr.trim();
				if (!trimmed) continue;
				const attrEq = trimmed.indexOf("=");
				const attrName = (attrEq === -1 ? trimmed : trimmed.substring(0, attrEq)).trim().toLowerCase();
				const attrValue = attrEq === -1 ? "" : trimmed.substring(attrEq + 1).trim();
				switch (attrName) {
					case "domain": {
						let d = attrValue.toLowerCase();
						if (d.startsWith(".")) d = d.substring(1);
						result.domain = d;
						break;
					}
					case "path":
						result.path = attrValue;
						break;
					case "expires": {
						const ms = parseExpiresDate(attrValue);
						if (!isNaN(ms)) result.expires = ms;
						break;
					}
					case "max-age": {
						const seconds = parseInt(attrValue, 10);
						if (!isNaN(seconds)) result.maxAge = seconds;
						break;
					}
					case "secure":
						result.secure = true;
						break;
					case "httponly":
						result.httpOnly = true;
						break;
					case "samesite": {
						const sv = attrValue.toLowerCase();
						if (sv === "strict") result.sameSite = "strict";
						else if (sv === "lax") result.sameSite = "lax";
						else if (sv === "none") result.sameSite = "none";
						break;
					}
				}
			}
		}
		return result;
	}
	var CookieJar = class {
		constructor() {
			_defineProperty(this, "_store", /* @__PURE__ */ new Map());
			_defineProperty(this, "_totalCount", 0);
		}
		/**
		* Parse a Set-Cookie header and store the cookie.
		* Per RFC 6265 Section 5.3.
		*/
		setCookie(setCookieHeader, requestUrl) {
			const parsed = parseSetCookie(setCookieHeader);
			if (!parsed) return;
			const now = Date.now();
			const requestHost = requestUrl.hostname.toLowerCase();
			let domain;
			let hostOnly;
			if (parsed.domain) {
				if (!domainMatch(parsed.domain, requestHost) && parsed.domain !== requestHost) return;
				domain = parsed.domain;
				hostOnly = false;
			} else {
				domain = requestHost;
				hostOnly = true;
			}
			const path = parsed.path || defaultCookiePath(requestUrl);
			let expires = null;
			if (parsed.maxAge !== void 0) if (parsed.maxAge <= 0) expires = 0;
			else expires = now + parsed.maxAge * 1e3;
			else if (parsed.expires !== void 0) expires = parsed.expires;
			if (parsed.name.length + parsed.value.length > MAX_COOKIE_SIZE) return;
			const cookie = {
				name: parsed.name,
				value: parsed.value,
				domain,
				path,
				expires,
				secure: parsed.secure,
				httpOnly: parsed.httpOnly,
				sameSite: parsed.sameSite,
				hostOnly,
				creationTime: now,
				lastAccessTime: now
			};
			if (expires !== null && expires <= now) {
				this._removeCookie(domain, parsed.name, path);
				return;
			}
			let domainCookies = this._store.get(domain);
			if (!domainCookies) {
				domainCookies = [];
				this._store.set(domain, domainCookies);
			}
			const existingIdx = domainCookies.findIndex((c) => c.name === cookie.name && c.path === cookie.path);
			if (existingIdx !== -1) {
				cookie.creationTime = domainCookies[existingIdx].creationTime;
				domainCookies[existingIdx] = cookie;
			} else {
				if (domainCookies.length >= MAX_COOKIES_PER_DOMAIN) this._evictFromDomain(domainCookies);
				if (this._totalCount >= MAX_COOKIES_TOTAL) this._evictGlobal();
				domainCookies.push(cookie);
				this._totalCount++;
			}
		}
		/**
		* Get the Cookie header value for a request URL + credentials mode.
		* Returns null if no cookies match.
		*/
		getCookieHeader(requestUrl, credentials, requestOrigin, requestMode, requestMethod) {
			if (credentials === "omit") return null;
			if (credentials === "same-origin" && requestMode === "cors") return null;
			if (credentials === "same-origin") {
				if (!isSameOrigin(requestUrl, requestOrigin)) return null;
			}
			const now = Date.now();
			const requestHost = requestUrl.hostname.toLowerCase();
			const requestPath = requestUrl.pathname || "/";
			const isSecure = requestUrl.protocol === "https:" || requestUrl.protocol === "wss:";
			const upperMethod = (requestMethod || "GET").toUpperCase();
			const isSafeMethod = upperMethod === "GET" || upperMethod === "HEAD";
			const matches = [];
			for (const [domain, cookies] of this._store) for (const cookie of cookies) {
				if (cookie.expires !== null && cookie.expires <= now) continue;
				if (cookie.hostOnly) {
					if (domain !== requestHost) continue;
				} else if (!domainMatch(domain, requestHost)) continue;
				if (!pathMatch(cookie.path, requestPath)) continue;
				if (cookie.secure && !isSecure) continue;
				if (cookie.sameSite === "strict") {
					if (!isSameSite(requestUrl, requestOrigin)) continue;
				} else if (cookie.sameSite === "lax") {
					if (!isSameSite(requestUrl, requestOrigin)) {
						if (!isSafeMethod) continue;
					}
				}
				cookie.lastAccessTime = now;
				matches.push(cookie);
			}
			if (matches.length === 0) return null;
			matches.sort((a, b) => {
				if (b.path.length !== a.path.length) return b.path.length - a.path.length;
				return a.creationTime - b.creationTime;
			});
			return matches.map((c) => `${c.name}=${c.value}`).join("; ");
		}
		/**
		* Clear all cookies.
		*/
		clearAll() {
			this._store.clear();
			this._totalCount = 0;
		}
		/**
		* Remove expired cookies from the store.
		*/
		pruneExpired() {
			const now = Date.now();
			{
				const __exactForOfIterator53 = this._store[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep53 = __exactForOfIterator53.next();
					if (__exactForOfStep53.done) break;
					const [domain, cookies] = __exactForOfStep53.value;
					const filtered = cookies.filter((c) => c.expires === null || c.expires > now);
					const removed = cookies.length - filtered.length;
					if (removed > 0) {
						this._totalCount -= removed;
						if (filtered.length === 0) this._store.delete(domain);
						else this._store.set(domain, filtered);
					}
				}
			}
		}
		/**
		* Get the number of stored cookies (for testing/debugging).
		*/
		get count() {
			return this._totalCount;
		}
		_removeCookie(domain, name, path) {
			const cookies = this._store.get(domain);
			if (!cookies) return;
			const idx = cookies.findIndex((c) => c.name === name && c.path === path);
			if (idx !== -1) {
				cookies.splice(idx, 1);
				this._totalCount--;
				if (cookies.length === 0) this._store.delete(domain);
			}
		}
		_evictFromDomain(domainCookies) {
			let oldestIdx = 0;
			for (let i = 1; i < domainCookies.length; i++) if (domainCookies[i].lastAccessTime < domainCookies[oldestIdx].lastAccessTime) oldestIdx = i;
			domainCookies.splice(oldestIdx, 1);
			this._totalCount--;
		}
		_evictGlobal() {
			let oldestTime = Infinity;
			let oldestDomain = "";
			let oldestIdx = -1;
			{
				const __exactForOfIterator54 = this._store[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep54 = __exactForOfIterator54.next();
					if (__exactForOfStep54.done) break;
					const [domain, cookies] = __exactForOfStep54.value;
					for (let i = 0; i < cookies.length; i++) if (cookies[i].lastAccessTime < oldestTime) {
						oldestTime = cookies[i].lastAccessTime;
						oldestDomain = domain;
						oldestIdx = i;
					}
				}
			}
			if (oldestIdx !== -1) {
				const cookies = this._store.get(oldestDomain);
				cookies.splice(oldestIdx, 1);
				this._totalCount--;
				if (cookies.length === 0) this._store.delete(oldestDomain);
			}
		}
	};
	/** Global cookie jar instance shared by the fetch pipeline */
	const cookieJar = new CookieJar();
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/fetch.ts
	/**
	* Fetch API Implementation
	*
	* Implements the WHATWG Fetch Standard fetch() function.
	* @see https://fetch.spec.whatwg.org/#fetch-method
	*/
	/**
	* Global reference to the native fetch module.
	* This is injected by the native layer at runtime.
	*/
	let nativeFetchModule = null;
	const DEFAULT_FETCH_TIMEOUT_MS = 0;
	/**
	* Set the native fetch module.
	* Called by the native layer during initialization.
	*/
	function setNativeFetchModule(module) {
		nativeFetchModule = module;
	}
	/**
	* Get the native fetch module.
	* Throws if not initialized.
	*/
	function getNativeFetchModule() {
		if (!nativeFetchModule) throw new FetchError("Native fetch module not initialized");
		return nativeFetchModule;
	}
	/**
	* Add an abort signal listener with cleanup function.
	*/
	function addAbortSignalListener(signal, listener) {
		if (!signal) return () => {};
		signal.addEventListener("abort", listener);
		return () => {
			signal.removeEventListener("abort", listener);
		};
	}
	function createDataUrlFetchError() {
		return /* @__PURE__ */ new TypeError(isBunCompatEnv() ? "failed to fetch the data URL" : "Failed to fetch: invalid data URL");
	}
	function isAsciiHexDigit(code) {
		return code >= 48 && code <= 57 || code >= 65 && code <= 70 || code >= 97 && code <= 102;
	}
	function hexDigitToValue(code) {
		if (code >= 48 && code <= 57) return code - 48;
		if (code >= 65 && code <= 70) return code - 65 + 10;
		return code - 97 + 10;
	}
	function decodePercentEscapesLooseToLatin1(input) {
		let decoded = "";
		for (let i = 0; i < input.length; i++) {
			if (input.charCodeAt(i) === 37 && i + 2 < input.length && isAsciiHexDigit(input.charCodeAt(i + 1)) && isAsciiHexDigit(input.charCodeAt(i + 2))) {
				decoded += String.fromCharCode(hexDigitToValue(input.charCodeAt(i + 1)) << 4 | hexDigitToValue(input.charCodeAt(i + 2)));
				i += 2;
				continue;
			}
			decoded += input[i];
		}
		return decoded;
	}
	function decodeDataUrlPayloadLoose(input) {
		const bytes = [];
		const encoder = getTextEncoder();
		for (let i = 0; i < input.length;) {
			if (input.charCodeAt(i) === 37 && i + 2 < input.length && isAsciiHexDigit(input.charCodeAt(i + 1)) && isAsciiHexDigit(input.charCodeAt(i + 2))) {
				bytes.push(hexDigitToValue(input.charCodeAt(i + 1)) << 4 | hexDigitToValue(input.charCodeAt(i + 2)));
				i += 3;
				continue;
			}
			const codePoint = input.codePointAt(i);
			const encoded = encoder.encode(String.fromCodePoint(codePoint));
			for (let j = 0; j < encoded.length; j++) bytes.push(encoded[j]);
			i += codePoint > 65535 ? 2 : 1;
		}
		return new Uint8Array(bytes);
	}
	function suppressInternalPromiseRejection(promise) {
		promise.catch(() => {});
		return promise;
	}
	function resolveTimeoutMs(init) {
		const timeout = init?.timeout;
		if (timeout === void 0) return DEFAULT_FETCH_TIMEOUT_MS;
		if (!Number.isFinite(timeout) || timeout < 0) throw new TypeError("Failed to fetch: timeout must be a non-negative finite number");
		return timeout;
	}
	function createEffectiveSignal(sourceSignal, timeoutMs) {
		if (timeoutMs === 0) return {
			signal: sourceSignal,
			cleanup: () => {}
		};
		const controller = new AbortController();
		let timeoutId = setTimeout(() => {
			controller.abort(new DOMException$6("The operation timed out.", "TimeoutError"));
		}, timeoutMs);
		const forwardAbort = () => {
			controller.abort(sourceSignal?.reason);
		};
		if (sourceSignal?.aborted) forwardAbort();
		else if (sourceSignal) sourceSignal.addEventListener("abort", forwardAbort);
		return {
			signal: controller.signal,
			cleanup: () => {
				if (timeoutId !== void 0) {
					clearTimeout(timeoutId);
					timeoutId = void 0;
				}
				if (sourceSignal) sourceSignal.removeEventListener("abort", forwardAbort);
			}
		};
	}
	const CERTIFICATE_PEM_PATTERN = /-----BEGIN CERTIFICATE-----[\s\S]*?-----END CERTIFICATE-----/g;
	function normalizeNativeBody(body) {
		if (body == null) return null;
		if (body instanceof ArrayBuffer) return body;
		if (ArrayBuffer.isView(body)) {
			const view = body;
			return view.buffer.slice(view.byteOffset, view.byteOffset + view.byteLength);
		}
		throw new TypeError("Failed to fetch: invalid native response body");
	}
	function validateNativeHeaders(headers) {
		if (!Array.isArray(headers)) throw new TypeError("Failed to fetch: invalid native response headers");
		return headers.map((header) => {
			if (!Array.isArray(header) || header.length !== 2) throw new TypeError("Failed to fetch: invalid native response headers");
			const [name, value] = header;
			if (typeof name !== "string" || typeof value !== "string") throw new TypeError("Failed to fetch: invalid native response headers");
			return [name, normalizeBunCompatHeaderValue(value)];
		});
	}
	function validateNativeResponseBase(nativeResponse) {
		if (typeof nativeResponse !== "object" || nativeResponse === null) throw new TypeError("Failed to fetch: invalid native response");
		const { status, statusText, headers, url, redirected } = nativeResponse;
		if (!Number.isInteger(status) || status < 0 || status > 599) throw new TypeError("Failed to fetch: invalid native response status");
		if (typeof statusText !== "string") throw new TypeError("Failed to fetch: invalid native response status text");
		if (typeof url !== "string") throw new TypeError("Failed to fetch: invalid native response url");
		if (typeof redirected !== "boolean") throw new TypeError("Failed to fetch: invalid native response redirect flag");
		return {
			status,
			statusText,
			headers: validateNativeHeaders(headers),
			url,
			redirected
		};
	}
	function validateNativeResponse(nativeResponse) {
		const base = validateNativeResponseBase(nativeResponse);
		const body = normalizeNativeBody(nativeResponse.body);
		return {
			...base,
			body
		};
	}
	function validateNativeStreamingResponse(nativeResponse) {
		return validateNativeResponseBase(nativeResponse);
	}
	function hasHeader(headers, name) {
		const target = name.toLowerCase();
		for (const [headerName] of headers) if (headerName.toLowerCase() === target) return true;
		return false;
	}
	function setHeader(headers, name, value) {
		const target = name.toLowerCase();
		for (let i = 0; i < headers.length; i++) if (headers[i][0].toLowerCase() === target) {
			headers[i] = [headers[i][0], value];
			return;
		}
		headers.push([name, value]);
	}
	function copyErrorMetadata(target, source) {
		if (typeof source !== "object" || source === null) return;
		const sourceRecord = source;
		const targetRecord = target;
		const fields = [
			"code",
			"errno",
			"syscall",
			"address",
			"port"
		];
		for (let i = 0; i < fields.length; i++) {
			const key = fields[i];
			if (sourceRecord[key] !== void 0) targetRecord[key] = sourceRecord[key];
		}
	}
	function createSocketTransportError(message, code) {
		const error = new Error(message);
		copyErrorMetadata(error, { code });
		return error;
	}
	function getSocketBufferConstructor(runtimeRequire) {
		const globalBuffer = globalThis.Buffer;
		if (globalBuffer && typeof globalBuffer.from === "function" && typeof globalBuffer.concat === "function") return globalBuffer;
		const bufferModule = runtimeRequire("node:buffer");
		if (bufferModule && bufferModule.Buffer && typeof bufferModule.Buffer.from === "function") return bufferModule.Buffer;
		throw new FetchError("Buffer constructor unavailable for socket transport");
	}
	function getRuntimeProcessEnv(runtimeRequire) {
		try {
			return runtimeRequire("node:process")?.env ?? null;
		} catch {
			return null;
		}
	}
	function mergeCertificateSources(existing, extra) {
		if (existing === void 0 || existing === null || existing === "") return extra;
		if (Array.isArray(existing)) return existing.concat(extra);
		return [existing, extra];
	}
	function loadNodeExtraCaBundle(runtimeRequire, env) {
		const extraCaPath = typeof env?.NODE_EXTRA_CA_CERTS === "string" ? env.NODE_EXTRA_CA_CERTS.trim() : "";
		if (!extraCaPath) return null;
		let contents = "";
		try {
			const fs = runtimeRequire("node:fs");
			if (!fs || typeof fs.readFileSync !== "function") return null;
			contents = fs.readFileSync(extraCaPath, "utf8");
		} catch {
			return null;
		}
		if (!contents.trim()) return null;
		const matches = contents.match(CERTIFICATE_PEM_PATTERN);
		if (!matches || matches.length === 0) return null;
		if (contents.replace(CERTIFICATE_PEM_PATTERN, "").trim()) {
			if (typeof console !== "undefined" && typeof console.error === "function") console.error("ignoring extra certs from NODE_EXTRA_CA_CERTS");
			return null;
		}
		return matches.join("\n");
	}
	function applyHttpsRequestTlsOptions(requestOptions, init, runtimeRequire) {
		const tlsOptions = init?.tls;
		if (tlsOptions && typeof tlsOptions === "object") {
			const tlsRecord = tlsOptions;
			const keys = Object.keys(tlsRecord);
			for (let i = 0; i < keys.length; i++) {
				const key = keys[i];
				requestOptions[key] = tlsRecord[key];
			}
		}
		const env = getRuntimeProcessEnv(runtimeRequire);
		if (requestOptions.rejectUnauthorized === void 0 && env?.NODE_TLS_REJECT_UNAUTHORIZED === "0") requestOptions.rejectUnauthorized = false;
		const extraCaBundle = loadNodeExtraCaBundle(runtimeRequire, env);
		if (extraCaBundle) requestOptions.ca = mergeCertificateSources(requestOptions.ca, extraCaBundle);
	}
	function getSocketResponseContentEncoding(headers, decompress) {
		if (decompress === false) return "";
		const encodingHeader = headers.find(([name]) => name.toLowerCase() === "content-encoding");
		return encodingHeader ? encodingHeader[1].split(",", 1)[0].trim().toLowerCase() : "";
	}
	function isLikelyZlibWrappedDeflatePrefix(prefix) {
		if (prefix.byteLength < 2) return true;
		const cmf = prefix[0];
		const flg = prefix[1];
		return (cmf & 15) === 8 && cmf >> 4 <= 7 && ((cmf << 8) + flg) % 31 === 0;
	}
	function normalizeSocketBodyStreamError(error, code) {
		const normalized = error instanceof Error ? error : new Error(String(error));
		const normalizedCode = normalized.code;
		if (typeof normalizedCode === "string" && normalizedCode.startsWith("HPE_")) {
			const mapped = createSocketTransportError("InvalidHTTPResponse", "InvalidHTTPResponse");
			copyErrorMetadata(mapped, normalized);
			mapped.code = "InvalidHTTPResponse";
			return mapped;
		}
		if (!code) return normalized;
		const mapped = new Error(normalized.message);
		mapped.name = "Error";
		copyErrorMetadata(mapped, normalized);
		mapped.code = code;
		return mapped;
	}
	const SOCKET_DECOMPRESSED_CHUNK_SIZE = 16 * 1024;
	function createSocketResponseBodyStream(sourceStream, headers, decompress, runtimeRequire, signal) {
		const BufferCtor = getSocketBufferConstructor(runtimeRequire);
		const contentEncoding = getSocketResponseContentEncoding(headers, decompress);
		let settled = false;
		let controller = null;
		let queuedChunks = [];
		let pendingError = null;
		let pendingClose = false;
		function flushQueuedState() {
			if (!controller || settled) return;
			while (queuedChunks.length > 0) controller.enqueue(queuedChunks.shift());
			if (pendingError) {
				const error = pendingError;
				pendingError = null;
				const activeController = controller;
				controller = null;
				activeController.error(error);
				return;
			}
			if (pendingClose) {
				pendingClose = false;
				const activeController = controller;
				controller = null;
				activeController.close();
			}
		}
		function toSocketBodyChunk(chunk) {
			return typeof chunk === "string" ? BufferCtor.from(chunk, "utf8") : BufferCtor.from(chunk);
		}
		function queueChunkCopy(chunk) {
			const copy = new Uint8Array(chunk.byteLength);
			copy.set(chunk);
			if (controller) {
				controller.enqueue(copy);
				return;
			}
			queuedChunks.push(copy);
		}
		function enqueueChunk(chunk, maxChunkSize) {
			if (settled) return;
			const bytes = toSocketBodyChunk(chunk);
			if (typeof maxChunkSize !== "number" || maxChunkSize <= 0 || bytes.byteLength <= maxChunkSize) {
				queueChunkCopy(bytes);
				return;
			}
			for (let offset = 0; offset < bytes.byteLength; offset += maxChunkSize) queueChunkCopy(bytes.subarray(offset, Math.min(offset + maxChunkSize, bytes.byteLength)));
		}
		const bodyStream = new ReadableStream({
			start(streamController) {
				controller = streamController;
				flushQueuedState();
			},
			cancel(reason) {
				cleanupAbortListener();
				settled = true;
				queuedChunks = [];
				if (typeof sourceStream?.destroy === "function") try {
					sourceStream.destroy(reason instanceof Error ? reason : void 0);
				} catch {}
			}
		});
		const abortListener = () => {
			failOutput(signal?.reason instanceof Error ? signal.reason : new DOMException$6("The operation was aborted.", "AbortError"));
		};
		if (signal) if (signal.aborted) abortListener();
		else signal.addEventListener("abort", abortListener);
		function cleanupAbortListener() {
			if (signal) signal.removeEventListener("abort", abortListener);
		}
		function finishOutput() {
			if (settled) return;
			settled = true;
			cleanupAbortListener();
			if (typeof sourceStream?.destroy === "function") try {
				sourceStream.destroy();
			} catch {}
			if (controller) {
				const activeController = controller;
				controller = null;
				activeController.close();
				return;
			}
			pendingClose = true;
		}
		function failOutput(error, code) {
			if (settled) return;
			settled = true;
			cleanupAbortListener();
			if (typeof sourceStream?.destroy === "function") try {
				sourceStream.destroy(error instanceof Error ? error : void 0);
			} catch {}
			const normalizedError = normalizeSocketBodyStreamError(error, code);
			queuedChunks = [];
			if (controller) {
				const activeController = controller;
				controller = null;
				activeController.error(normalizedError);
				return;
			}
			pendingError = normalizedError;
		}
		function forwardReadable(readable, errorCode, maxChunkSize) {
			readable.on("data", (chunk) => {
				try {
					enqueueChunk(chunk, maxChunkSize);
				} catch (error) {
					failOutput(error, errorCode);
				}
			});
			readable.on("end", finishOutput);
			readable.on("error", (error) => {
				failOutput(error, errorCode);
			});
			if (typeof readable.resume === "function") readable.resume();
		}
		sourceStream.on("aborted", () => {
			failOutput(createSocketTransportError("aborted", "ECONNRESET"));
		});
		sourceStream.on("close", () => {
			if (!settled && typeof sourceStream.complete === "boolean" && sourceStream.complete === false) failOutput(createSocketTransportError("aborted", "ECONNRESET"));
		});
		sourceStream.on("error", (error) => {
			failOutput(error);
		});
		if (!contentEncoding) {
			forwardReadable(sourceStream);
			return bodyStream;
		}
		const zlib = runtimeRequire("node:zlib");
		if (!zlib) {
			forwardReadable(sourceStream);
			return bodyStream;
		}
		if (contentEncoding === "gzip" || contentEncoding === "x-gzip") {
			if (typeof zlib.createGunzip !== "function") {
				forwardReadable(sourceStream);
				return bodyStream;
			}
			const decoder = zlib.createGunzip();
			sourceStream.pipe(decoder);
			forwardReadable(decoder, "ZlibError", SOCKET_DECOMPRESSED_CHUNK_SIZE);
			return bodyStream;
		}
		if (contentEncoding === "br") {
			if (typeof zlib.createBrotliDecompress !== "function") {
				forwardReadable(sourceStream);
				return bodyStream;
			}
			const decoder = zlib.createBrotliDecompress();
			sourceStream.pipe(decoder);
			forwardReadable(decoder, "BrotliDecompressionError", SOCKET_DECOMPRESSED_CHUNK_SIZE);
			return bodyStream;
		}
		if (contentEncoding === "zstd") {
			if (typeof zlib.createZstdDecompress !== "function") {
				forwardReadable(sourceStream);
				return bodyStream;
			}
			const decoder = zlib.createZstdDecompress();
			sourceStream.pipe(decoder);
			forwardReadable(decoder, "ZstdDecompressionError", SOCKET_DECOMPRESSED_CHUNK_SIZE);
			return bodyStream;
		}
		if (contentEncoding === "deflate" && typeof zlib.createInflate === "function" && typeof zlib.createInflateRaw === "function") {
			let decoder = null;
			const prefixChunks = [];
			let prefixLength = 0;
			function startDeflateDecoder() {
				if (decoder) return;
				const prefix = prefixLength > 0 ? concatUint8Chunks(prefixChunks, prefixLength) : new Uint8Array(0);
				decoder = isLikelyZlibWrappedDeflatePrefix(prefix) ? zlib.createInflate() : zlib.createInflateRaw();
				forwardReadable(decoder, "ZlibError", SOCKET_DECOMPRESSED_CHUNK_SIZE);
				if (prefix.byteLength > 0) decoder.write(BufferCtor.from(prefix));
			}
			sourceStream.on("data", (chunk) => {
				const bytes = toSocketBodyChunk(chunk);
				if (!decoder) {
					const copy = new Uint8Array(bytes.byteLength);
					copy.set(bytes);
					prefixChunks.push(copy);
					prefixLength += copy.byteLength;
					if (prefixLength >= 2) startDeflateDecoder();
					return;
				}
				decoder.write(bytes);
			});
			sourceStream.on("end", () => {
				startDeflateDecoder();
				decoder.end();
			});
			return bodyStream;
		}
		forwardReadable(sourceStream);
		return bodyStream;
	}
	function isSecureUrlProtocol(protocol) {
		return protocol === "https:" || protocol === "wss:";
	}
	function isLoopbackHostname(hostname) {
		const normalized = hostname.toLowerCase();
		return normalized === "localhost" || normalized === "127.0.0.1" || normalized === "::1";
	}
	function shouldUseBunCompatKeepaliveSocketTransport(url, request) {
		if (!isBunCompatEnv() || !request.hasExplicitKeepalive() || request.keepalive) return false;
		try {
			const parsedUrl = new URL(url);
			return parsedUrl.protocol === "http:" && isLoopbackHostname(parsedUrl.hostname);
		} catch {
			return false;
		}
	}
	function shouldUseBunCompatLoopbackSocketTransport(url) {
		if (!isBunCompatEnv()) return false;
		try {
			const parsedUrl = new URL(url);
			return (parsedUrl.protocol === "http:" || parsedUrl.protocol === "https:") && isLoopbackHostname(parsedUrl.hostname);
		} catch {
			return false;
		}
	}
	function shouldUseTlsSocketTransport(init) {
		if (init?.tls) return true;
		const env = globalThis.process?.env;
		if (!env) return false;
		return typeof env.NODE_EXTRA_CA_CERTS === "string" && env.NODE_EXTRA_CA_CERTS.trim().length > 0 || env.NODE_TLS_REJECT_UNAUTHORIZED === "0";
	}
	function locationForReferrer(source) {
		const location = globalThis.location;
		if (!location || typeof location !== "object") return null;
		const directHref = typeof location.href === "string" ? location.href : null;
		if (source === "request-object") {
			if (directHref) return directHref;
		}
		if (source === "input") {
			if (typeof location.toString === "function") {
				const stringValue = location.toString();
				if (stringValue) return stringValue;
			}
		}
		if (directHref) return directHref;
		return null;
	}
	function isRequestInputObject(input) {
		return typeof input === "object" && input !== null && "url" in input && "method" in input && "referrer" in input;
	}
	function buildReferrerHeader(request, source) {
		const rawReferrer = request.referrer;
		if (rawReferrer === "" || rawReferrer === "no-referrer") return null;
		const policy = request.referrerPolicy || "no-referrer-when-downgrade";
		const targetUrl = request.url;
		if (!targetUrl) return null;
		let referrerUrl;
		try {
			const resolvedReferrer = rawReferrer === "about:client" ? locationForReferrer(source) : rawReferrer;
			if (!resolvedReferrer) return null;
			referrerUrl = new URL(resolvedReferrer);
		} catch {
			return null;
		}
		let requestUrl;
		try {
			requestUrl = new URL(targetUrl);
		} catch {
			return null;
		}
		referrerUrl.username = "";
		referrerUrl.password = "";
		referrerUrl.hash = "";
		const isDownGrade = isSecureUrlProtocol(referrerUrl.protocol) && !isSecureUrlProtocol(requestUrl.protocol);
		const sameOrigin = requestUrl.origin === referrerUrl.origin;
		if (policy === "no-referrer") return null;
		if (policy === "no-referrer-when-downgrade") return isDownGrade ? null : referrerUrl.href;
		if (policy === "same-origin") return sameOrigin ? referrerUrl.href : null;
		if (policy === "origin") return referrerUrl.origin;
		if (policy === "origin-when-cross-origin") return sameOrigin ? referrerUrl.href : referrerUrl.origin;
		if (policy === "strict-origin") return isDownGrade ? null : referrerUrl.origin;
		if (policy === "strict-origin-when-cross-origin") {
			if (sameOrigin) return referrerUrl.href;
			if (isDownGrade) return null;
			return referrerUrl.origin;
		}
		if (policy === "unsafe-url") return referrerUrl.href;
		return sameOrigin ? referrerUrl.href : referrerUrl.origin;
	}
	/**
	* Check if cookies should be processed for this request.
	*/
	function shouldProcessCookies(request) {
		if (request.credentials === "omit") return false;
		if (request.credentials === "same-origin" && request.mode === "cors") return false;
		return true;
	}
	/**
	* Extract Set-Cookie headers from a native response and store them in the cookie jar.
	*/
	function storeCookiesFromHeaders(headers, url) {
		try {
			const responseUrl = new URL(url);
			{
				const __exactForOfIterator55 = headers[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep55 = __exactForOfIterator55.next();
					if (__exactForOfStep55.done) break;
					const [name, value] = __exactForOfStep55.value;
					if (name.toLowerCase() === "set-cookie") cookieJar.setCookie(value, responseUrl);
				}
			}
		} catch {}
	}
	/**
	* HTTP redirect status codes per Fetch spec.
	*/
	const REDIRECT_STATUS_CODES = [
		301,
		302,
		303,
		307,
		308
	];
	/**
	* Maximum number of redirects to follow per Fetch spec.
	*/
	const MAX_REDIRECTS = 20;
	/**
	* Headers that must be stripped on cross-origin redirects per Fetch spec.
	*/
	const SENSITIVE_HEADERS = [
		"authorization",
		"cookie",
		"proxy-authorization"
	];
	function isBunCompatEnv() {
		if (globalThis.__exactRuntimeContext === "shell") return false;
		const env = globalThis.process?.env;
		return env?.EXACT_COMPAT_TEST === "1" && env?.EXACT_TEST_SECTION === "bun";
	}
	function normalizeRedirectLocation(location, currentUrl) {
		let normalized = location;
		if (isBunCompatEnv() && /[^\u0000-\u007f]/.test(normalized)) {
			const BufferCtor = globalThis.Buffer;
			if (BufferCtor && typeof BufferCtor.from === "function") try {
				let decoded = normalized;
				for (let i = 0; i < 3; i++) {
					let isLatin1Only = true;
					for (let j = 0; j < decoded.length; j++) if (decoded.charCodeAt(j) > 255) {
						isLatin1Only = false;
						break;
					}
					if (!isLatin1Only) break;
					const next = BufferCtor.from(decoded, "latin1").toString("utf8");
					if (next.includes("�") || next === decoded) break;
					decoded = next;
				}
				if (!decoded.includes("�")) normalized = decoded;
			} catch {}
		}
		if (normalized.startsWith("://")) {
			let protocol = "http:";
			if (currentUrl) {
				const schemeMatch = /^[a-zA-Z][a-zA-Z0-9+.-]*:/.exec(currentUrl);
				if (schemeMatch) protocol = schemeMatch[0];
			}
			return protocol + normalized.slice(1);
		}
		return normalized;
	}
	function normalizeBunCompatNetworkError(message) {
		if (!isBunCompatEnv()) return null;
		if (/specified hostname could not be found/i.test(message)) return "Unable to connect. Is the computer able to access the url?";
		return null;
	}
	function normalizeBunCompatHeaderValue(value) {
		if (!isBunCompatEnv() || !/[ÃÂ]/.test(value)) return value;
		const BufferCtor = globalThis.Buffer;
		if (!BufferCtor || typeof BufferCtor.from !== "function") return value;
		try {
			const decoded = BufferCtor.from(value, "latin1").toString("utf8");
			if (!decoded.includes("�") && decoded !== value) return decoded;
		} catch {}
		return value;
	}
	/**
	* Compute the new HTTP method after a redirect per Fetch spec.
	* - 301/302 with POST → GET (body cleared)
	* - 303 → GET (body cleared) unless HEAD
	* - 307/308 → preserve method and body
	*/
	function computeRedirectMethod(status, method) {
		if (status === 301 || status === 302) {
			if (method === "POST") return "GET";
		}
		if (status === 303) {
			if (method !== "HEAD") return "GET";
		}
		return method;
	}
	/**
	* Create an opaque redirect response for redirect: 'manual'.
	* Per spec: status 0, no headers exposed, type 'opaqueredirect'.
	*/
	function createOpaqueRedirectResponse(url) {
		const response = new Response$1(null);
		response._status = 0;
		response._statusText = "";
		response._ok = false;
		response._type = "opaqueredirect";
		response._url = url;
		response._redirected = false;
		response._headers = new Headers();
		response._headers._guard = "immutable";
		return response;
	}
	function concatUint8Chunks(chunks, totalLength) {
		const merged = new Uint8Array(totalLength);
		let offset = 0;
		{
			const __exactForOfIterator56 = chunks[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep56 = __exactForOfIterator56.next();
				if (__exactForOfStep56.done) break;
				const chunk = __exactForOfStep56.value;
				merged.set(chunk, offset);
				offset += chunk.byteLength;
			}
		}
		return merged;
	}
	function buildNativeHeaders(request, source, decompress) {
		const requestHeaders = request.headers.toTupleArray();
		if (isBunCompatEnv() && !hasHeader(requestHeaders, "connection")) {
			if (request.keepalive) setHeader(requestHeaders, "connection", "keep-alive");
			else if (!request.hasExplicitKeepalive()) setHeader(requestHeaders, "connection", "keep-alive");
		}
		if (!hasHeader(requestHeaders, "accept")) requestHeaders.push(["accept", "*/*"]);
		if (!hasHeader(requestHeaders, "accept-language")) requestHeaders.push(["accept-language", "en-US,en;q=0.9"]);
		if (isBunCompatEnv() && !hasHeader(requestHeaders, "user-agent") && typeof navigator === "object" && navigator !== null && typeof navigator.userAgent === "string" && navigator.userAgent.length > 0) requestHeaders.push(["user-agent", navigator.userAgent]);
		const referrer = buildReferrerHeader(request, source);
		if (referrer !== null && !hasHeader(requestHeaders, "referer")) requestHeaders.push(["referer", referrer]);
		if (!decompress && !hasHeader(requestHeaders, "accept-encoding")) requestHeaders.push(["accept-encoding", "identity"]);
		return requestHeaders;
	}
	/**
	* The global fetch() method starts the process of fetching a resource from the network,
	* returning a promise which is fulfilled once the response is available.
	*/
	/**
	* Fetch a data: URL and return a synthetic Response.
	*/
	function fetchDataUrl(url, method) {
		const commaIndex = url.indexOf(",");
		if (commaIndex === -1) throw createDataUrlFetchError();
		const meta = url.slice(5, commaIndex);
		const dataStr = url.slice(commaIndex + 1);
		let mimeType = isBunCompatEnv() ? "text/plain;charset=utf-8" : "text/plain;charset=US-ASCII";
		let isBase64 = false;
		if (meta) if (meta.endsWith(";base64")) {
			isBase64 = true;
			mimeType = meta.slice(0, -7) || mimeType;
		} else mimeType = meta;
		mimeType = isBunCompatEnv() ? normalizeBlobContentType(mimeType) : mimeType;
		let bodyBytes;
		try {
			if (isBase64) {
				const decodedBase64 = isBunCompatEnv() ? decodePercentEscapesLooseToLatin1(dataStr) : decodeURIComponent(dataStr);
				const binaryString = atob(decodedBase64);
				bodyBytes = new Uint8Array(binaryString.length);
				for (let i = 0; i < binaryString.length; i++) bodyBytes[i] = binaryString.charCodeAt(i);
			} else if (isBunCompatEnv()) bodyBytes = decodeDataUrlPayloadLoose(dataStr);
			else {
				const decoded = decodeURIComponent(dataStr);
				bodyBytes = getTextEncoder().encode(decoded);
			}
		} catch {
			throw createDataUrlFetchError();
		}
		const response = new Response$1(method?.toUpperCase() === "HEAD" ? new Uint8Array(0) : bodyBytes, {
			status: 200,
			statusText: "OK",
			headers: { "Content-Type": mimeType }
		});
		response._type = "basic";
		return response;
	}
	async function fetch$1(input, init) {
		requireCapability(Capabilities.NETWORK_FETCH);
		const inputUrl = typeof input === "string" ? input : input instanceof URL ? input.href : input.url;
		if (typeof inputUrl === "string" && inputUrl.startsWith("data:")) try {
			return fetchDataUrl(inputUrl, typeof input === "string" ? init?.method : input instanceof Request ? input.method : init?.method);
		} catch (error) {
			if (error instanceof TypeError) throw error;
			throw new TypeError("Failed to fetch");
		}
		let request;
		try {
			request = new Request(input, init);
		} catch (error) {
			if (isBunCompatEnv() && error instanceof TypeError && init?.method === void 0 && init?.body !== void 0 && error.message.includes("cannot have body")) throw new TypeError("fetch() request with GET/HEAD/OPTIONS method cannot have body.");
			throw error;
		}
		const isRequestInput = isRequestInputObject(input);
		const decompress = init?.decompress !== false;
		const timeoutMs = resolveTimeoutMs(init);
		try {
			request.markBodyAsUsedForFetch();
		} catch (error) {
			if (error instanceof TypeError) throw error;
			throw new FetchError("Request body was already used");
		}
		const nativeModule = getNativeFetchModule();
		const signalContext = createEffectiveSignal(request.signal, timeoutMs);
		const signal = signalContext.signal;
		if (signal?.aborted) throw signal.reason ?? new DOMException$6("The operation was aborted.", "AbortError");
		let abortCleanup = () => {};
		let cancelled = false;
		const abortPromise = suppressInternalPromiseRejection(new Promise((_, reject) => {
			abortCleanup = addAbortSignalListener(signal, () => {
				cancelled = true;
				reject(signal?.reason ?? new DOMException$6("The operation was aborted.", "AbortError"));
			});
		}));
		let cancelRequest;
		try {
			const isStreamingUpload = request.isBodyStream();
			const requestBodySource = isStreamingUpload ? request.getBodyStream() : null;
			const requestSource = isRequestInput ? "request-object" : "input";
			const uploadChunks = [];
			let uploadChunkLength = 0;
			const useSocketDecompressCompat = !decompress && nativeModule.__exactNativeBridge === true;
			const unixSocketPath = typeof init?.unix === "string" && init.unix.length > 0 ? init.unix : void 0;
			const useTlsSocketTransport = shouldUseTlsSocketTransport(init);
			const preferSocketTransport = useSocketDecompressCompat || !!unixSocketPath || useTlsSocketTransport || shouldUseBunCompatLoopbackSocketTransport(request.url) || shouldUseBunCompatKeepaliveSocketTransport(request.url, request);
			let uploadReader = requestBodySource && nativeModule.fetchStreamingUpload && !preferSocketTransport ? requestBodySource.getReader() : null;
			let bufferedUploadBody = null;
			let streamUploadPending = isStreamingUpload && !!nativeModule.fetchStreamingUpload && !!uploadReader;
			let currentBody;
			if (!isStreamingUpload) currentBody = await request.getBodyAsUint8Array();
			else if (!streamUploadPending) currentBody = await readableStreamToUint8Array(requestBodySource, signal);
			else currentBody = null;
			let redirectCount = 0;
			let redirected = false;
			let currentUrl = request.url;
			let currentMethod = request.method;
			let currentUnixSocketPath = unixSocketPath;
			const redirectMode = request.redirect;
			let stripSensitiveHeaders = false;
			const getReplayableUploadBody = () => {
				if (bufferedUploadBody) return bufferedUploadBody;
				bufferedUploadBody = concatUint8Chunks(uploadChunks, uploadChunkLength);
				return bufferedUploadBody;
			};
			const finalizeResponse = (nativeResponse, bodyStream) => {
				const response = bodyStream ? Response$1.fromNativeStreaming(nativeResponse, bodyStream, currentUrl) : Response$1.fromNative(nativeResponse, currentUrl);
				response._redirected = redirected || response._redirected === true;
				response._url = redirected ? currentUrl : nativeResponse.url || currentUrl;
				try {
					if (new URL(nativeResponse.url || currentUrl).origin !== getRuntimeOrigin().origin) response._type = "cors";
				} catch {}
				if (signal) response._signal = signal;
				return response;
			};
			const executeSocketFetchHopStreaming = async (url, nativeInit, body, socketPath) => {
				const parsedUrl = new URL(url);
				const runtimeRequire = globalThis.require;
				if (typeof runtimeRequire !== "function") throw new FetchError("Module loader not available for socket transport compatibility path");
				const transport = parsedUrl.protocol === "https:" ? runtimeRequire("node:https") : runtimeRequire("node:http");
				const disableSocketDefaultHeaders = shouldUseBunCompatKeepaliveSocketTransport(url, request);
				const effectiveHeaders = nativeInit.headers.slice();
				if (!hasHeader(effectiveHeaders, "host")) effectiveHeaders.push(["host", parsedUrl.host || parsedUrl.hostname]);
				if (body !== null && !hasHeader(effectiveHeaders, "content-length") && !hasHeader(effectiveHeaders, "transfer-encoding")) effectiveHeaders.push(["content-length", String(body.byteLength)]);
				const requestHeaders = {};
				{
					const __exactForOfIterator57 = effectiveHeaders[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep57 = __exactForOfIterator57.next();
						if (__exactForOfStep57.done) break;
						const [name, value] = __exactForOfStep57.value;
						const existing = requestHeaders[name];
						if (existing === void 0) requestHeaders[name] = value;
						else if (Array.isArray(existing)) existing.push(value);
						else requestHeaders[name] = [existing, value];
					}
				}
				let activeCancelRequest;
				const requestPromise = suppressInternalPromiseRejection(new Promise((resolve, reject) => {
					const requestOptions = {
						protocol: parsedUrl.protocol,
						method: nativeInit.method,
						headers: requestHeaders,
						path: parsedUrl.pathname + (parsedUrl.search || "")
					};
					if (disableSocketDefaultHeaders) {
						requestOptions.setDefaultHeaders = false;
						requestOptions.setHost = false;
					}
					if (socketPath) {
						requestOptions.socketPath = socketPath;
						if (parsedUrl.hostname) requestOptions.hostname = parsedUrl.hostname;
						if (parsedUrl.port) requestOptions.port = Number(parsedUrl.port);
					} else {
						requestOptions.hostname = parsedUrl.hostname;
						requestOptions.port = parsedUrl.port ? Number(parsedUrl.port) : void 0;
					}
					if (parsedUrl.protocol === "https:") {
						applyHttpsRequestTlsOptions(requestOptions, init, runtimeRequire);
						if (requestOptions.agent === void 0 && transport?.globalAgent) requestOptions.agent = transport.globalAgent;
					}
					const req = transport.request(requestOptions, (res) => {
						const headers = [];
						const rawHeaders = Array.isArray(res.rawHeaders) ? res.rawHeaders : [];
						for (let i = 0; i + 1 < rawHeaders.length; i += 2) headers.push([String(rawHeaders[i]), String(rawHeaders[i + 1])]);
						const status = res.statusCode || 0;
						if (nativeInit.method === "HEAD" || status >= 100 && status < 200 || status === 204 || status === 304) {
							if (typeof res.resume === "function") res.resume();
							resolve({ nativeResponse: {
								status,
								statusText: res.statusMessage || "",
								headers,
								url,
								redirected: false
							} });
							return;
						}
						resolve({
							nativeResponse: {
								status,
								statusText: res.statusMessage || "",
								headers,
								url,
								redirected: false
							},
							bodyStream: createSocketResponseBodyStream(res, headers, nativeInit.decompress, runtimeRequire, signal)
						});
						res.on("error", reject);
					});
					activeCancelRequest = () => {
						req.destroy(signal?.reason instanceof Error ? signal.reason : /* @__PURE__ */ new Error("The operation was aborted."));
					};
					req.on("error", reject);
					if (body?.byteLength) req.write(body);
					req.end();
				}));
				if (activeCancelRequest) cancelRequest = activeCancelRequest;
				try {
					return await Promise.race([requestPromise, abortPromise]);
				} finally {
					if (cancelRequest === activeCancelRequest) cancelRequest = void 0;
				}
			};
			const executeHop = async (url, nativeInit, body, useStreamingUpload) => {
				if ((useSocketDecompressCompat || !!currentUnixSocketPath || useTlsSocketTransport || shouldUseBunCompatLoopbackSocketTransport(url) || shouldUseBunCompatKeepaliveSocketTransport(url, request)) && !useStreamingUpload) {
					const socketResponse = await executeSocketFetchHopStreaming(url, nativeInit, body, currentUnixSocketPath);
					return {
						nativeResponse: validateNativeStreamingResponse(socketResponse.nativeResponse),
						bodyStream: socketResponse.bodyStream
					};
				}
				if (useStreamingUpload && nativeModule.fetchStreamingUpload && uploadReader) {
					let resolveHeaders;
					let rejectHeaders;
					let headersSettled = false;
					let streamClosed = false;
					const headersPromise = suppressInternalPromiseRejection(new Promise((resolve, reject) => {
						resolveHeaders = resolve;
						rejectHeaders = reject;
					}));
					let activeCancelRequest;
					const responseBodyStream = new ReadableStream({
						type: "bytes",
						start(controller) {
							activeCancelRequest = nativeModule.fetchStreamingUpload(url, nativeInit, async () => {
								const result = await uploadReader.read();
								if (result.done) return null;
								const chunk = result.value;
								if (chunk?.byteLength) {
									const bufferedChunk = new Uint8Array(chunk);
									uploadChunks.push(bufferedChunk);
									uploadChunkLength += bufferedChunk.byteLength;
								}
								return chunk;
							}, (nativeResponse) => {
								if (headersSettled) return;
								headersSettled = true;
								resolveHeaders(nativeResponse);
							}, (chunk) => {
								if (streamClosed) return;
								if (chunk?.byteLength) controller.enqueue(new Uint8Array(chunk));
							}, () => {
								if (streamClosed) return;
								streamClosed = true;
								controller.close();
							}, (error) => {
								if (streamClosed) return;
								if (!headersSettled) {
									streamClosed = true;
									headersSettled = true;
									rejectHeaders(error);
									return;
								}
								streamClosed = true;
								controller.error(error);
							});
							if (!activeCancelRequest) activeCancelRequest = () => {};
							cancelRequest = activeCancelRequest;
						},
						cancel() {
							streamClosed = true;
							activeCancelRequest?.();
						}
					});
					return {
						nativeResponse: validateNativeStreamingResponse(await Promise.race([headersPromise, abortPromise])),
						bodyStream: responseBodyStream
					};
				}
				if (nativeModule.fetchStreaming) {
					let resolveHeaders;
					let rejectHeaders;
					let headersSettled = false;
					let streamClosed = false;
					let activeCancelRequest;
					const headersPromise = suppressInternalPromiseRejection(new Promise((resolve, reject) => {
						resolveHeaders = resolve;
						rejectHeaders = reject;
					}));
					const bodyStream = new ReadableStream({
						type: "bytes",
						start(controller) {
							activeCancelRequest = nativeModule.fetchStreaming(url, nativeInit, body, (nativeResponse) => {
								if (headersSettled) return;
								headersSettled = true;
								resolveHeaders(nativeResponse);
							}, (chunk) => {
								if (streamClosed) return;
								if (chunk?.byteLength) controller.enqueue(new Uint8Array(chunk));
							}, () => {
								if (streamClosed) return;
								streamClosed = true;
								controller.close();
							}, (error) => {
								if (streamClosed) return;
								if (!headersSettled) {
									streamClosed = true;
									headersSettled = true;
									rejectHeaders(error);
									return;
								}
								streamClosed = true;
								controller.error(error);
							});
							if (!activeCancelRequest) activeCancelRequest = () => {};
							cancelRequest = activeCancelRequest;
						},
						cancel() {
							streamClosed = true;
							activeCancelRequest?.();
						}
					});
					return {
						nativeResponse: validateNativeStreamingResponse(await Promise.race([headersPromise, abortPromise])),
						bodyStream
					};
				}
				const nativeFetchPromise = suppressInternalPromiseRejection(nativeModule.fetch(url, nativeInit, body));
				const nativeCancel = typeof nativeFetchPromise.__exactCancel === "function" ? nativeFetchPromise.__exactCancel : void 0;
				if (nativeCancel) cancelRequest = nativeCancel;
				const nativeResponse = validateNativeResponse(await Promise.race([nativeFetchPromise, abortPromise]));
				if (cancelRequest === nativeCancel) cancelRequest = void 0;
				return { nativeResponse };
			};
			while (true) {
				const nativeHeaders = buildNativeHeaders(request, requestSource, decompress);
				if (request.cache === "no-store" && !hasHeader(nativeHeaders, "cache-control")) nativeHeaders.push(["cache-control", "no-store"]);
				else if (request.cache === "no-cache" && !hasHeader(nativeHeaders, "cache-control")) nativeHeaders.push(["cache-control", "no-cache"]);
				else if (request.cache === "reload" && !hasHeader(nativeHeaders, "cache-control")) {
					nativeHeaders.push(["cache-control", "no-cache"]);
					if (!hasHeader(nativeHeaders, "pragma")) nativeHeaders.push(["pragma", "no-cache"]);
				}
				if (redirected) {
					const cookieIdx = nativeHeaders.findIndex(([n]) => n.toLowerCase() === "cookie");
					if (cookieIdx !== -1) nativeHeaders.splice(cookieIdx, 1);
				}
				if (stripSensitiveHeaders) {
					for (let i = nativeHeaders.length - 1; i >= 0; i--) if (SENSITIVE_HEADERS.includes(nativeHeaders[i][0].toLowerCase())) nativeHeaders.splice(i, 1);
				}
				if (shouldProcessCookies(request)) try {
					const reqUrl = new URL(currentUrl);
					const cookieHeader = cookieJar.getCookieHeader(reqUrl, request.credentials, getRuntimeOrigin(), request.mode, currentMethod);
					if (cookieHeader) {
						const existingIdx = nativeHeaders.findIndex(([n]) => n.toLowerCase() === "cookie");
						if (existingIdx !== -1) nativeHeaders.splice(existingIdx, 1);
						nativeHeaders.push(["cookie", cookieHeader]);
					}
				} catch {}
				const nativeInit = {
					method: currentMethod,
					headers: nativeHeaders,
					credentials: request.credentials,
					redirect: "follow",
					decompress,
					signal
				};
				const hop = await executeHop(currentUrl, nativeInit, currentBody, streamUploadPending && currentMethod !== "GET" && currentMethod !== "HEAD");
				const nativeResponse = hop.nativeResponse;
				if (shouldProcessCookies(request)) storeCookiesFromHeaders(nativeResponse.headers, nativeResponse.url || currentUrl);
				const status = nativeResponse.status;
				if (!REDIRECT_STATUS_CODES.includes(status)) {
					const response = finalizeResponse(nativeResponse, hop.bodyStream);
					if (hop.bodyStream) cancelRequest = void 0;
					return response;
				}
				if (redirectMode === "error") {
					cancelRequest?.();
					cancelRequest = void 0;
					const error = /* @__PURE__ */ new TypeError("Failed to fetch: unexpected redirect");
					if (isBunCompatEnv()) error.code = "UnexpectedRedirect";
					throw error;
				}
				if (redirectMode === "manual") {
					if (isBunCompatEnv()) {
						const response = finalizeResponse(nativeResponse, hop.bodyStream);
						if (hop.bodyStream) cancelRequest = void 0;
						return response;
					}
					cancelRequest?.();
					cancelRequest = void 0;
					return createOpaqueRedirectResponse(currentUrl);
				}
				redirectCount++;
				if (redirectCount > MAX_REDIRECTS) {
					cancelRequest?.();
					cancelRequest = void 0;
					throw new TypeError("Failed to fetch: redirect count exceeded");
				}
				let location = null;
				for (const [name, value] of nativeResponse.headers) if (name.toLowerCase() === "location") {
					location = value;
					break;
				}
				if (!location) {
					const response = finalizeResponse(nativeResponse, hop.bodyStream);
					if (hop.bodyStream) cancelRequest = void 0;
					return response;
				}
				let redirectUrl;
				try {
					const normalizedLocation = normalizeRedirectLocation(location, currentUrl);
					if (location.startsWith("://") || normalizedLocation.startsWith("://")) {
						const schemeMatch = /^[a-zA-Z][a-zA-Z0-9+.-]*:/.exec(currentUrl);
						const protocol = schemeMatch ? schemeMatch[0] : "http:";
						redirectUrl = new URL(protocol + normalizedLocation.slice(1));
					} else redirectUrl = new URL(normalizedLocation, currentUrl);
				} catch {
					cancelRequest?.();
					cancelRequest = void 0;
					throw new TypeError("Failed to fetch: invalid redirect URL");
				}
				const newMethod = computeRedirectMethod(status, currentMethod);
				const previousUrl = currentUrl;
				cancelRequest?.();
				cancelRequest = void 0;
				if (newMethod !== currentMethod && (newMethod === "GET" || newMethod === "HEAD")) {
					currentBody = null;
					streamUploadPending = false;
				} else if (streamUploadPending) {
					currentBody = getReplayableUploadBody();
					streamUploadPending = false;
				}
				try {
					if (new URL(previousUrl).origin !== redirectUrl.origin) stripSensitiveHeaders = true;
					if (currentUnixSocketPath && new URL(previousUrl).origin !== redirectUrl.origin) currentUnixSocketPath = void 0;
				} catch {}
				currentMethod = newMethod;
				currentUrl = redirectUrl.href;
				redirected = true;
			}
		} catch (error) {
			if (cancelled || error instanceof AbortError) throw error;
			if (error instanceof TypeError) throw error;
			if (error instanceof Error) {
				const bunCompatMessage = normalizeBunCompatNetworkError(error.message);
				if (bunCompatMessage) {
					const bunCompatError = new TypeError(bunCompatMessage);
					copyErrorMetadata(bunCompatError, error);
					throw bunCompatError;
				}
				const typeError = /* @__PURE__ */ new TypeError(`Failed to fetch: ${error.message}`);
				copyErrorMetadata(typeError, error);
				throw typeError;
			}
			throw new TypeError(`Failed to fetch: ${String(error)}`);
		} finally {
			if (cancelRequest) cancelRequest();
			abortCleanup();
			signalContext.cleanup();
		}
	}
	/**
	* A mock/polyfill fetch implementation for testing and web fallback.
	* Uses XMLHttpRequest under the hood where native module is not available.
	*/
	async function fetchPolyfill(input, init) {
		requireCapability(Capabilities.NETWORK_FETCH);
		const request = new Request(input, init);
		const isRequestInput = isRequestInputObject(input);
		const decompress = init?.decompress !== false;
		const timeoutMs = resolveTimeoutMs(init);
		request.markBodyAsUsedForFetch();
		return new Promise((resolve, reject) => {
			const signalContext = createEffectiveSignal(request.signal, timeoutMs);
			const signal = signalContext.signal;
			if (signal?.aborted) {
				signalContext.cleanup();
				reject(new AbortError(signal.reason?.message ?? "The operation was aborted"));
				return;
			}
			if (typeof XMLHttpRequest === "undefined") {
				signalContext.cleanup();
				reject(new FetchError("XMLHttpRequest not available"));
				return;
			}
			const xhr = new XMLHttpRequest();
			xhr.responseType = "arraybuffer";
			const abortHandler = () => {
				xhr.abort();
				signalContext.cleanup();
				reject(new AbortError(signal?.reason?.message ?? "The operation was aborted"));
			};
			signal?.addEventListener("abort", abortHandler);
			xhr.onload = () => {
				signal?.removeEventListener("abort", abortHandler);
				signalContext.cleanup();
				const headerLines = xhr.getAllResponseHeaders().trim().split(/[\r\n]+/);
				const headers = [];
				{
					const __exactForOfIterator58 = headerLines[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep58 = __exactForOfIterator58.next();
						if (__exactForOfStep58.done) break;
						const parts = __exactForOfStep58.value.split(": ");
						const name = parts.shift();
						const value = parts.join(": ");
						if (name) headers.push([name, value]);
					}
				}
				resolve(Response$1.fromNative({
					status: xhr.status,
					statusText: xhr.statusText,
					headers,
					url: xhr.responseURL || request.url,
					redirected: xhr.responseURL !== request.url,
					body: xhr.response
				}));
			};
			xhr.onerror = () => {
				signal?.removeEventListener("abort", abortHandler);
				signalContext.cleanup();
				reject(new FetchError("Network request failed"));
			};
			xhr.ontimeout = () => {
				signal?.removeEventListener("abort", abortHandler);
				signalContext.cleanup();
				reject(new FetchError("Request timeout"));
			};
			xhr.open(request.method, request.url, true);
			const nativeHeaders = buildNativeHeaders(request, isRequestInput ? "request-object" : "input", decompress);
			{
				const __exactForOfIterator59 = nativeHeaders[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep59 = __exactForOfIterator59.next();
					if (__exactForOfStep59.done) break;
					const [name, value] = __exactForOfStep59.value;
					try {
						xhr.setRequestHeader(name, value);
					} catch {}
				}
			}
			xhr.withCredentials = request.credentials === "include";
			request.getBodyAsUint8Array().then((body) => {
				xhr.send(body);
			}, (error) => {
				signal?.removeEventListener("abort", abortHandler);
				signalContext.cleanup();
				reject(error);
			});
		});
	}
	/**
	* Install fetch globally.
	* This sets up the global fetch, Headers, Request, and Response.
	*/
	function installFetchGlobals(globalObject = globalThis) {
		const fetchImpl = nativeFetchModule ? fetch$1 : fetchPolyfill;
		Object.defineProperties(globalObject, {
			fetch: {
				value: function fetch(input) {
					return fetchImpl(input, arguments.length > 1 ? arguments[1] : void 0);
				},
				writable: true,
				configurable: true,
				enumerable: true
			},
			Headers: {
				value: Headers,
				writable: true,
				configurable: true,
				enumerable: true
			},
			Request: {
				value: Request,
				writable: true,
				configurable: true,
				enumerable: true
			},
			Response: {
				value: Response$1,
				writable: true,
				configurable: true,
				enumerable: true
			}
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/fetch/native-bridge.ts
	/**
	* Check if the native fetch bridge is available
	*/
	function isNativeFetchAvailable() {
		return typeof __nativeFetch === "function";
	}
	/**
	* Create a NativeFetchModule that wraps the C++ __nativeFetch function
	*/
	function createNativeFetchModule() {
		if (!isNativeFetchAvailable()) return null;
		return {
			__exactNativeBridge: true,
			fetch: (url, init, body) => {
				return __nativeFetch(url, init, body);
			}
		};
	}
	/**
	* Auto-initialize the native fetch module if available.
	* Call this during runtime bootstrap.
	*/
	function initializeNativeFetch() {
		const module = createNativeFetchModule();
		if (module) {
			setNativeFetchModule(module);
			return true;
		}
		return false;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/inspect/inspect.ts
	const DEFAULT_OPTIONS = {
		colors: true,
		depth: 4,
		showHidden: false,
		maxArrayLength: 100,
		maxStringLength: 1e4,
		compact: true
	};
	const colors = {
		reset: "\x1B[0m",
		bold: "\x1B[1m",
		dim: "\x1B[2m",
		number: "\x1B[33m",
		string: "\x1B[32m",
		boolean: "\x1B[33m",
		null: "\x1B[1m",
		undefined: "\x1B[2m",
		symbol: "\x1B[32m",
		function: "\x1B[36m",
		key: "\x1B[0m",
		special: "\x1B[36m",
		bracket: "\x1B[2m"
	};
	function colorize(text, color, useColors) {
		return useColors ? color + text + colors.reset : text;
	}
	function inspectPrimitive(value, opts) {
		if (value === null) return colorize("null", colors.null, opts.colors);
		if (value === void 0) return colorize("undefined", colors.undefined, opts.colors);
		if (typeof value === "number") return colorize(String(value), colors.number, opts.colors);
		if (typeof value === "boolean") return colorize(String(value), colors.boolean, opts.colors);
		if (typeof value === "string") {
			const truncated = value.length > opts.maxStringLength ? value.slice(0, opts.maxStringLength) + "..." : value;
			return colorize(JSON.stringify(truncated), colors.string, opts.colors);
		}
		if (typeof value === "symbol") return colorize(String(value), colors.symbol, opts.colors);
		if (typeof value === "function") return colorize(`[Function: ${value.name || "anonymous"}]`, colors.function, opts.colors);
		return String(value);
	}
	function inspectArray(arr, depth, opts, seen) {
		if (depth >= opts.depth) return colorize("[Array]", colors.special, opts.colors);
		const items = [];
		const limit = Math.min(arr.length, opts.maxArrayLength);
		for (let i = 0; i < limit; i++) items.push(inspectValue(arr[i], depth + 1, opts, seen));
		if (arr.length > opts.maxArrayLength) items.push(colorize(`... ${arr.length - opts.maxArrayLength} more items`, colors.dim, opts.colors));
		const open = colorize("[", colors.bracket, opts.colors);
		const close = colorize("]", colors.bracket, opts.colors);
		if (opts.compact) return open + " " + items.join(", ") + " " + close;
		else return open + "\n  " + items.join(",\n  ") + "\n" + close;
	}
	function indentMultiline(text, prefix) {
		return text.replace(/\n/g, `\n${prefix}`);
	}
	function isURLSearchParamsLike(value) {
		return !!value && typeof value === "object" && typeof value.append === "function" && typeof value.get === "function" && typeof value.getAll === "function" && typeof value.entries === "function" && typeof value.toString === "function";
	}
	function inspectURLSearchParamsLike(value) {
		try {
			const groups = [];
			{
				const __exactForOfIterator60 = value.entries()[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep60 = __exactForOfIterator60.next();
					if (__exactForOfStep60.done) break;
					const entry = __exactForOfStep60.value;
					const key = String(entry[0]);
					const entryValue = String(entry[1]);
					const group = groups.find((item) => item.key === key);
					if (group) group.values.push(entryValue);
					else groups.push({
						key,
						values: [entryValue]
					});
				}
			}
			if (groups.length === 0) return "URLSearchParams {}";
			return `URLSearchParams {\n${groups.map((group) => {
				const renderedValue = group.values.length === 1 ? JSON.stringify(group.values[0]) : `[ ${group.values.map((item) => JSON.stringify(item)).join(", ")} ]`;
				return `  ${JSON.stringify(group.key)}: ${renderedValue},`;
			}).join("\n")}\n}`;
		} catch {
			return null;
		}
	}
	function isURLLike(value) {
		return !!value && typeof value === "object" && typeof value.href === "string" && typeof value.origin === "string" && typeof value.protocol === "string" && typeof value.username === "string" && typeof value.password === "string" && typeof value.host === "string" && typeof value.hostname === "string" && typeof value.port === "string" && typeof value.pathname === "string" && typeof value.hash === "string" && typeof value.search === "string" && value.searchParams && typeof value.searchParams === "object" && typeof value.toJSON === "function" && typeof value.toString === "function";
	}
	function inspectURLLike(obj, depth, opts, seen) {
		const searchParams = indentMultiline(inspectValue(obj.searchParams, depth + 1, opts, seen), "  ");
		return `URL {
  href: ${JSON.stringify(obj.href)},
  origin: ${JSON.stringify(obj.origin)},
  protocol: ${JSON.stringify(obj.protocol)},
  username: ${JSON.stringify(obj.username)},
  password: ${JSON.stringify(obj.password)},
  host: ${JSON.stringify(obj.host)},
  hostname: ${JSON.stringify(obj.hostname)},
  port: ${JSON.stringify(obj.port)},
  pathname: ${JSON.stringify(obj.pathname)},
  hash: ${JSON.stringify(obj.hash)},
  search: ${JSON.stringify(obj.search)},
  searchParams: ${searchParams},
  toJSON: [Function: toJSON],
  toString: [Function: toString],
}`;
	}
	function inspectObject(obj, depth, opts, seen) {
		if (depth >= opts.depth) return colorize("[Object]", colors.special, opts.colors);
		if (isURLSearchParamsLike(obj)) {
			const inspected = inspectURLSearchParamsLike(obj);
			if (inspected !== null) return inspected;
		}
		if (isURLLike(obj)) return inspectURLLike(obj, depth, opts, seen);
		if (typeof obj.toISOString === "function" && typeof obj.getMonth === "function") try {
			return colorize(obj.toISOString(), colors.special, opts.colors);
		} catch {
			return colorize(String(obj), colors.special, opts.colors);
		}
		if (typeof obj.test === "function" && typeof obj.exec === "function" && obj.source !== void 0) return colorize(String(obj), colors.special, opts.colors);
		if (obj.name && obj.message && obj.stack) return colorize(`${obj.name}: ${obj.message}`, colors.special, opts.colors);
		if (typeof obj.get === "function" && typeof obj.set === "function" && typeof obj.has === "function" && typeof obj.entries === "function" && obj.size !== void 0) try {
			const entries = [];
			let count = 0;
			for (const [key, value] of obj.entries()) {
				if (count++ >= opts.maxArrayLength) break;
				entries.push(inspectValue(key, depth + 1, opts, seen) + " => " + inspectValue(value, depth + 1, opts, seen));
			}
			if (obj.size > opts.maxArrayLength) entries.push(colorize(`... ${obj.size - opts.maxArrayLength} more entries`, colors.dim, opts.colors));
			return colorize("Map(", colors.special, opts.colors) + obj.size + ") { " + entries.join(", ") + " }";
		} catch {}
		if (typeof obj.has === "function" && typeof obj.add === "function" && typeof obj.values === "function" && obj.size !== void 0) try {
			const values = [];
			let count = 0;
			for (const value of obj.values()) {
				if (count++ >= opts.maxArrayLength) break;
				values.push(inspectValue(value, depth + 1, opts, seen));
			}
			if (obj.size > opts.maxArrayLength) values.push(colorize(`... ${obj.size - opts.maxArrayLength} more items`, colors.dim, opts.colors));
			return colorize("Set(", colors.special, opts.colors) + obj.size + ") { " + values.join(", ") + " }";
		} catch {}
		if (obj.__esModule || obj.default !== void 0 || obj.exports !== void 0) {
			const keys = Object.keys(obj).filter((k) => k !== "__esModule");
			if (keys.length === 0) return colorize("Module { }", colors.special, opts.colors);
			else if (keys.length <= 5) {
				const exports = keys.map((k) => colorize(k, colors.key, opts.colors)).join(", ");
				return colorize("Module { ", colors.special, opts.colors) + exports + colorize(" }", colors.special, opts.colors);
			} else {
				const firstFive = keys.slice(0, 5).map((k) => colorize(k, colors.key, opts.colors)).join(", ");
				const remaining = keys.length - 5;
				return colorize("Module { ", colors.special, opts.colors) + firstFive + colorize(`, ...${remaining} more`, colors.dim, opts.colors) + colorize(" }", colors.special, opts.colors);
			}
		}
		if (typeof obj.then === "function" && typeof obj.catch === "function") try {
			const state = obj._x;
			const value = obj._z;
			if (value !== void 0 && value !== null && state !== 2) return colorize("Promise { ", colors.special, opts.colors) + colorize("<fulfilled>", colors.dim, opts.colors) + ": " + inspectValue(value, depth + 1, opts, seen) + colorize(" }", colors.special, opts.colors);
			else if (state === 2) return colorize("Promise { ", colors.special, opts.colors) + colorize("<rejected>", colors.dim, opts.colors) + ": " + inspectValue(value, depth + 1, opts, seen) + colorize(" }", colors.special, opts.colors);
			else return colorize("Promise { ", colors.special, opts.colors) + colorize("<pending>", colors.dim, opts.colors) + colorize(" }", colors.special, opts.colors);
		} catch {
			return colorize("Promise { ... }", colors.special, opts.colors);
		}
		const keys = opts.showHidden ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
		if (keys.length === 0) return "{}";
		const pairs = [];
		const limit = Math.min(keys.length, opts.maxArrayLength);
		for (let i = 0; i < limit; i++) {
			const key = keys[i];
			const coloredKey = colorize(!/^[a-zA-Z_$][a-zA-Z0-9_$]*$/.test(key) ? JSON.stringify(key) : key, colors.key, opts.colors);
			const value = inspectValue(obj[key], depth + 1, opts, seen);
			pairs.push(coloredKey + ": " + value);
		}
		if (keys.length > opts.maxArrayLength) pairs.push(colorize(`... ${keys.length - opts.maxArrayLength} more keys`, colors.dim, opts.colors));
		const open = colorize("{", colors.bracket, opts.colors);
		const close = colorize("}", colors.bracket, opts.colors);
		if (opts.compact) return open + " " + pairs.join(", ") + " " + close;
		else return open + "\n  " + pairs.join(",\n  ") + "\n" + close;
	}
	function inspectTypedArray(value, depth, opts) {
		const isBuffer = value[Symbol.toStringTag] === "Buffer" || value.constructor && value.constructor.name === "Buffer";
		const typeName = isBuffer ? "Buffer" : value.constructor?.name || "TypedArray";
		if (isBuffer) {
			const maxBytes = 50;
			const hexParts = [];
			const limit = Math.min(value.length, maxBytes);
			for (let i = 0; i < limit; i++) hexParts.push(value[i].toString(16).padStart(2, "0"));
			const truncated = value.length > maxBytes ? ` ... ${value.length - maxBytes} more bytes` : "";
			return colorize(`<Buffer ${hexParts.join(" ")}${truncated}>`, colors.special, opts.colors);
		}
		const maxItems = Math.min(value.length, opts.maxArrayLength);
		const items = [];
		for (let i = 0; i < maxItems; i++) items.push(colorize(String(value[i]), colors.number, opts.colors));
		const truncated = value.length > opts.maxArrayLength ? colorize(` ... ${value.length - opts.maxArrayLength} more items`, colors.dim, opts.colors) : "";
		return `${typeName}(${value.length}) [ ${items.join(", ")}${truncated} ]`;
	}
	function inspectValue(value, depth, opts, seen) {
		if (typeof value !== "object" || value === null) return inspectPrimitive(value, opts);
		if (seen.has(value)) return colorize("[Circular]", colors.special, opts.colors);
		seen.add(value);
		try {
			if (ArrayBuffer.isView(value) && !(value instanceof DataView)) return inspectTypedArray(value, depth, opts);
			if (Array.isArray(value)) return inspectArray(value, depth, opts, seen);
			return inspectObject(value, depth, opts, seen);
		} finally {
			seen.delete(value);
		}
	}
	/**
	* Inspect a value and return a formatted string representation
	* Similar to Node's util.inspect, Bun.inspect, and Deno.inspect
	*/
	function inspect(value, options) {
		return inspectValue(value, 0, {
			...DEFAULT_OPTIONS,
			...options
		}, /* @__PURE__ */ new Set());
	}
	if (typeof globalThis.Exact !== "undefined") {
		const exactGlobal = globalThis.Exact;
		const descriptor = Object.getOwnPropertyDescriptor(exactGlobal, "inspect");
		if (!descriptor || descriptor.configurable || descriptor.writable) try {
			Object.defineProperty(exactGlobal, "inspect", {
				value: inspect,
				writable: true,
				configurable: true,
				enumerable: true
			});
		} catch {}
	}
	//#endregion
	//#region ../ibex-runtime-js/src/crypto/Crypto.ts
	/**
	* Crypto - Web Crypto API Implementation
	*
	* @see https://www.w3.org/TR/WebCryptoAPI/
	*
	* Supported algorithms:
	* - Digest: SHA-1, SHA-256, SHA-384, SHA-512
	* - Encrypt/Decrypt: AES-GCM, AES-CBC, AES-CTR, RSA-OAEP
	* - Sign/Verify: HMAC, RSASSA-PKCS1-v1_5, RSA-PSS, ECDSA, Ed25519
	* - Key generation: AES-GCM, AES-CBC, AES-CTR, HMAC, RSA-OAEP, RSASSA-PKCS1-v1_5, RSA-PSS, ECDSA, ECDH, Ed25519, X25519
	* - Key derivation: PBKDF2, HKDF, X25519
	* - Key formats: raw, jwk, spki, pkcs8
	*
	* Note: This uses native crypto when available, with JS fallbacks for testing.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$23;
	/**
	* Wrap native bridge errors as DOMException with the appropriate name.
	* Native __exact* functions throw generic Error; WPT expects DOMException.
	*/
	function wrapNativeError(e, defaultName) {
		if (e instanceof DOMException$5) return e;
		const msg = e instanceof Error ? e.message : String(e);
		if (/not supported|unsupported|not available|unrecognized/i.test(msg)) return new DOMException$5(msg, "NotSupportedError");
		if (/invalid key|bad key/i.test(msg)) return new DOMException$5(msg, "DataError");
		if (/not extractable|usage/i.test(msg)) return new DOMException$5(msg, "InvalidAccessError");
		return new DOMException$5(msg, defaultName);
	}
	var _SubtleCrypto_brand = /* @__PURE__ */ new WeakSet();
	var SubtleCrypto = class {
		constructor() {
			_classPrivateMethodInitSpec(this, _SubtleCrypto_brand);
		}
		/**
		* Generate a cryptographic digest (hash)
		*/
		async digest(algorithm, data) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			const alg = typeof algorithm === "string" ? algorithm : algorithm.name;
			const normalizedAlg = normalizeHashName(alg);
			const bytes = toUint8Array$1(data);
			const native = getNativeCryptoModule();
			if (native) try {
				switch (normalizedAlg) {
					case "SHA-1":
						if (native.digest) return await native.digest(normalizedAlg, bytes);
						return await native.sha1(bytes);
					case "SHA-256":
						if (native.digest) return await native.digest(normalizedAlg, bytes);
						return await native.sha256(bytes);
					case "SHA-384":
						if (native.digest) return await native.digest(normalizedAlg, bytes);
						return await native.sha384(bytes);
					case "SHA-512":
						if (native.digest) return await native.digest(normalizedAlg, bytes);
						return await native.sha512(bytes);
				}
			} catch (_nativeErr) {}
			const platformSubtle = getPlatformSubtle();
			if (platformSubtle) try {
				return await platformSubtle.digest(algorithm, bytes);
			} catch (_platformErr) {}
			switch (normalizedAlg) {
				case "SHA-1": return jsSHA1(bytes);
				case "SHA-256": return jsSHA256(bytes);
				case "SHA-384": return jsSHA384(bytes);
				case "SHA-512": return jsSHA512(bytes);
				default: throw new DOMException$5(`Unrecognized algorithm name: ${alg}`, "NotSupportedError");
			}
		}
		/**
		* Encrypt data using the specified algorithm and key
		*/
		async encrypt(algorithm, key, data) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			validateKey(key, "encrypt");
			const bytes = toUint8Array$1(data);
			const alg = normalizeAlgorithm(algorithm);
			switch (alg.name.toUpperCase()) {
				case "AES-GCM": {
					const params = algorithm;
					const iv = toUint8Array$1(params.iv);
					const additionalData = params.additionalData ? toUint8Array$1(params.additionalData) : void 0;
					const tagLength = params.tagLength ?? 128;
					if (typeof __exactAesGcmEncrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactAesGcmEncrypt(key._keyData, iv, bytes, additionalData, tagLength));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for AES-GCM", "NotSupportedError");
				}
				case "AES-CBC": {
					const iv = toUint8Array$1(algorithm.iv);
					if (typeof __exactAesCbcEncrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactAesCbcEncrypt(key._keyData, iv, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for AES-CBC", "NotSupportedError");
				}
				case "AES-CTR": {
					const counter = toUint8Array$1(algorithm.counter);
					if (typeof __exactAesCtrEncrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactAesCtrEncrypt(key._keyData, counter, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for AES-CTR", "NotSupportedError");
				}
				case "RSA-OAEP": {
					const params = algorithm;
					const hash = key.algorithm.hash?.name || "SHA-256";
					const label = params.label ? toUint8Array$1(params.label) : new Uint8Array(0);
					if (typeof __exactRsaOaepEncrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactRsaOaepEncrypt(key._keyData, hash, label, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for RSA-OAEP", "NotSupportedError");
				}
				default: throw new DOMException$5(`Unsupported algorithm: ${alg.name}`, "NotSupportedError");
			}
		}
		/**
		* Decrypt data using the specified algorithm and key
		*/
		async decrypt(algorithm, key, data) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			validateKey(key, "decrypt");
			const bytes = toUint8Array$1(data);
			const alg = normalizeAlgorithm(algorithm);
			switch (alg.name.toUpperCase()) {
				case "AES-GCM": {
					const params = algorithm;
					const iv = toUint8Array$1(params.iv);
					const additionalData = params.additionalData ? toUint8Array$1(params.additionalData) : void 0;
					const tagLength = params.tagLength ?? 128;
					if (typeof __exactAesGcmDecrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactAesGcmDecrypt(key._keyData, iv, bytes, additionalData, tagLength));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for AES-GCM", "NotSupportedError");
				}
				case "AES-CBC": {
					const iv = toUint8Array$1(algorithm.iv);
					if (typeof __exactAesCbcDecrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactAesCbcDecrypt(key._keyData, iv, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for AES-CBC", "NotSupportedError");
				}
				case "AES-CTR": {
					const counter = toUint8Array$1(algorithm.counter);
					if (typeof __exactAesCtrEncrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactAesCtrEncrypt(key._keyData, counter, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for AES-CTR", "NotSupportedError");
				}
				case "RSA-OAEP": {
					const params = algorithm;
					const hash = key.algorithm.hash?.name || "SHA-256";
					const label = params.label ? toUint8Array$1(params.label) : new Uint8Array(0);
					if (typeof __exactRsaOaepDecrypt === "function") try {
						return uint8ArrayToArrayBuffer(__exactRsaOaepDecrypt(key._keyData, hash, label, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for RSA-OAEP", "NotSupportedError");
				}
				default: throw new DOMException$5(`Unsupported algorithm: ${alg.name}`, "NotSupportedError");
			}
		}
		/**
		* Sign data using the specified algorithm and key
		*/
		async sign(algorithm, key, data) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			validateKey(key, "sign");
			const bytes = toUint8Array$1(data);
			const alg = normalizeAlgorithm(algorithm);
			const native = getNativeCryptoModule();
			switch (alg.name.toUpperCase()) {
				case "HMAC": {
					const hash = normalizeHashName(key.algorithm.hash.name);
					if (native?.hmacSign) return native.hmacSign(hash, key._keyData, bytes);
					if (typeof __exactHmacSync === "function") {
						const keyData = key._keyData;
						const keyStr = uint8ArrayToString(keyData);
						const dataStr = uint8ArrayToString(bytes);
						const hashAlgo = hash.toLowerCase().replace("-", "");
						return hexStringToArrayBuffer(__exactHmacSync(hashAlgo, keyStr, dataStr));
					}
					return jsHmac(key._keyData, bytes, hash);
				}
				case "RSASSA-PKCS1-V1_5": {
					const hash = key.algorithm.hash.name;
					if (typeof __exactSignSync === "function") try {
						const keyStr = uint8ArrayToString(key._keyData);
						const dataStr = uint8ArrayToString(bytes);
						return uint8ArrayToArrayBuffer(__exactSignSync(hash, dataStr, keyStr));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for RSASSA-PKCS1-v1_5", "NotSupportedError");
				}
				case "RSA-PSS": {
					const hash = key.algorithm.hash.name;
					if (typeof __exactSignSync === "function") try {
						const keyStr = uint8ArrayToString(key._keyData);
						const dataStr = uint8ArrayToString(bytes);
						return uint8ArrayToArrayBuffer(__exactSignSync(hash, dataStr, keyStr));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for RSA-PSS", "NotSupportedError");
				}
				case "ECDSA": {
					const params = algorithm;
					const hash = typeof params.hash === "string" ? params.hash : params.hash.name;
					const curve = key.algorithm.namedCurve;
					if (typeof __exactEcdsaSign === "function") try {
						return uint8ArrayToArrayBuffer(__exactEcdsaSign(curve, hash, key._keyData, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for ECDSA", "NotSupportedError");
				}
				case "ED25519":
				case "EDDSA":
					if (typeof __exactEd25519Sign === "function") try {
						const keyData = key._keyData;
						const privKeyData = keyData.length === 64 ? keyData.slice(0, 32) : keyData;
						return uint8ArrayToArrayBuffer(__exactEd25519Sign(privKeyData, bytes));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for Ed25519", "NotSupportedError");
				default: throw new DOMException$5(`Unsupported algorithm: ${alg.name}`, "NotSupportedError");
			}
		}
		/**
		* Verify a signature using the specified algorithm and key
		*/
		async verify(algorithm, key, signature, data) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			validateKey(key, "verify");
			const signatureBytes = toUint8Array$1(signature);
			const dataBytes = toUint8Array$1(data);
			const alg = normalizeAlgorithm(algorithm);
			const native = getNativeCryptoModule();
			switch (alg.name.toUpperCase()) {
				case "HMAC": {
					const hash = normalizeHashName(key.algorithm.hash.name);
					if (native?.hmacVerify) return native.hmacVerify(hash, key._keyData, signatureBytes, dataBytes);
					if (typeof __exactHmacSync === "function") {
						const keyData = key._keyData;
						const keyStr = uint8ArrayToString(keyData);
						const dataStr = uint8ArrayToString(dataBytes);
						const hashAlgo = hash.toLowerCase().replace("-", "");
						const hex = __exactHmacSync(hashAlgo, keyStr, dataStr);
						const expectedSig = new Uint8Array(hex.length / 2);
						for (let i = 0; i < hex.length; i += 2) expectedSig[i / 2] = parseInt(hex.substring(i, i + 2), 16);
						return constantTimeCompare(expectedSig, signatureBytes);
					}
					const expectedSig = await jsHmac(key._keyData, dataBytes, hash);
					return constantTimeCompare(new Uint8Array(expectedSig), signatureBytes);
				}
				case "RSASSA-PKCS1-V1_5": {
					const hash = key.algorithm.hash.name;
					if (typeof __exactVerifySync === "function") try {
						const keyStr = uint8ArrayToString(key._keyData);
						const dataStr = uint8ArrayToString(dataBytes);
						return __exactVerifySync(hash, signatureBytes, dataStr, keyStr);
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for RSASSA-PKCS1-v1_5", "NotSupportedError");
				}
				case "RSA-PSS": {
					const hash = key.algorithm.hash.name;
					if (typeof __exactVerifySync === "function") try {
						const keyStr = uint8ArrayToString(key._keyData);
						const dataStr = uint8ArrayToString(dataBytes);
						return __exactVerifySync(hash, signatureBytes, dataStr, keyStr);
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for RSA-PSS", "NotSupportedError");
				}
				case "ECDSA": {
					const params = algorithm;
					const hash = typeof params.hash === "string" ? params.hash : params.hash.name;
					const curve = key.algorithm.namedCurve;
					if (typeof __exactEcdsaVerify === "function") try {
						return __exactEcdsaVerify(curve, hash, key._keyData, signatureBytes, dataBytes);
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for ECDSA", "NotSupportedError");
				}
				case "ED25519":
				case "EDDSA":
					if (typeof __exactEd25519Verify === "function") try {
						return __exactEd25519Verify(key._keyData, signatureBytes, dataBytes);
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for Ed25519", "NotSupportedError");
				default: throw new DOMException$5(`Unsupported algorithm: ${alg.name}`, "NotSupportedError");
			}
		}
		/**
		* Generate a new key or key pair
		*/
		async generateKey(algorithm, extractable, keyUsages) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			const alg = normalizeAlgorithm(algorithm);
			if (typeof alg.name !== "string") throw new DOMException$5("Invalid algorithm", "TypeError");
			const algName = alg.name.toUpperCase();
			if (keyUsages.length === 0) throw new DOMException$5("Key usages cannot be empty", "SyntaxError");
			switch (algName) {
				case "AES-GCM":
				case "AES-CBC":
				case "AES-CTR":
				case "AES-KW": {
					const length = algorithm.length;
					if (![
						128,
						192,
						256
					].includes(length)) throw new DOMException$5("Invalid key length", "DataError");
					const keyData = new Uint8Array(length / 8);
					crypto$1.getRandomValues(keyData);
					return new ExactCryptoKey("secret", extractable, {
						name: alg.name,
						length
					}, keyUsages, keyData);
				}
				case "HMAC": {
					const params = algorithm;
					const hash = normalizeHashName(typeof params.hash === "string" ? params.hash : params.hash.name);
					const length = params.length ?? getHashLength(hash);
					const keyData = new Uint8Array(length / 8);
					crypto$1.getRandomValues(keyData);
					return new ExactCryptoKey("secret", extractable, {
						name: "HMAC",
						hash: { name: hash },
						length
					}, keyUsages, keyData);
				}
				case "RSA-OAEP":
				case "RSASSA-PKCS1-V1_5":
				case "RSA-PSS": {
					const params = algorithm;
					const hash = normalizeRequiredHash(params.hash);
					if (typeof __exactGenerateKeyPairSync === "function") try {
						const result = __exactGenerateKeyPairSync("rsa", {
							modulusLength: params.modulusLength,
							publicExponent: params.publicExponent instanceof Uint8Array ? new DataView(params.publicExponent.buffer, params.publicExponent.byteOffset, params.publicExponent.byteLength).getUint32(params.publicExponent.byteLength - 4, false) : 65537
						});
						const algorithmInfo = {
							name: alg.name,
							modulusLength: params.modulusLength,
							publicExponent: params.publicExponent,
							hash: { name: hash }
						};
						const pubKeyData = new TextEncoder().encode(result.publicKey);
						const privKeyData = new TextEncoder().encode(result.privateKey);
						return {
							publicKey: new ExactCryptoKey("public", true, algorithmInfo, keyUsages.filter((u) => [
								"encrypt",
								"verify",
								"wrapKey"
							].includes(u)), pubKeyData),
							privateKey: new ExactCryptoKey("private", extractable, algorithmInfo, keyUsages.filter((u) => [
								"decrypt",
								"sign",
								"unwrapKey"
							].includes(u)), privKeyData)
						};
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for RSA key generation", "NotSupportedError");
				}
				case "ECDSA":
				case "ECDH": {
					const params = algorithm;
					if (typeof __exactGenerateKeyPairSync === "function") {
						const result = __exactGenerateKeyPairSync("ec", { namedCurve: params.namedCurve });
						const algorithmInfo = {
							name: alg.name,
							namedCurve: params.namedCurve
						};
						const pubKeyData = new TextEncoder().encode(result.publicKey);
						const privKeyData = new TextEncoder().encode(result.privateKey);
						return {
							publicKey: new ExactCryptoKey("public", true, algorithmInfo, alg.name === "ECDSA" ? keyUsages.filter((u) => u === "verify") : keyUsages.filter((u) => u === "deriveKey" || u === "deriveBits"), pubKeyData),
							privateKey: new ExactCryptoKey("private", extractable, algorithmInfo, alg.name === "ECDSA" ? keyUsages.filter((u) => u === "sign") : keyUsages.filter((u) => u === "deriveKey" || u === "deriveBits"), privKeyData)
						};
					}
					throw new DOMException$5("Native crypto not available for EC key generation", "NotSupportedError");
				}
				case "ED25519":
				case "EDDSA":
					if (typeof __exactGenerateKeyPairSync === "function") {
						const result = __exactGenerateKeyPairSync("ed25519", {});
						const algorithmInfo = { name: "Ed25519" };
						const pubKeyData = new TextEncoder().encode(result.publicKey);
						const privKeyData = new TextEncoder().encode(result.privateKey);
						return {
							publicKey: new ExactCryptoKey("public", true, algorithmInfo, keyUsages.filter((u) => u === "verify"), pubKeyData),
							privateKey: new ExactCryptoKey("private", extractable, algorithmInfo, keyUsages.filter((u) => u === "sign"), privKeyData)
						};
					}
					throw new DOMException$5("Native crypto not available for Ed25519 key generation", "NotSupportedError");
				case "X25519":
					if (typeof __exactGenerateKeyPairSync === "function") {
						const result = __exactGenerateKeyPairSync("x25519", {});
						const algorithmInfo = { name: "X25519" };
						const pubKeyData = new TextEncoder().encode(result.publicKey);
						const privKeyData = new TextEncoder().encode(result.privateKey);
						return {
							publicKey: new ExactCryptoKey("public", true, algorithmInfo, keyUsages.filter((u) => u === "deriveBits" || u === "deriveKey"), pubKeyData),
							privateKey: new ExactCryptoKey("private", extractable, algorithmInfo, keyUsages.filter((u) => u === "deriveBits" || u === "deriveKey"), privKeyData)
						};
					}
					throw new DOMException$5("Native crypto not available for X25519 key generation", "NotSupportedError");
				case "KMAC128":
				case "KMAC256": {
					const length = algorithm.length;
					if (length !== 128 && length !== 160 && length !== 256) throw new DOMException$5("Invalid key length", "DataError");
					{
						const __exactForOfIterator61 = keyUsages[Symbol.iterator]();
						for (;;) {
							const __exactForOfStep61 = __exactForOfIterator61.next();
							if (__exactForOfStep61.done) break;
							const usage = __exactForOfStep61.value;
							if (usage !== "sign" && usage !== "verify") throw new DOMException$5(`Invalid key usage '${usage}' for ${algName}`, "SyntaxError");
						}
					}
					const keyData = new Uint8Array(length / 8);
					crypto$1.getRandomValues(keyData);
					return new ExactCryptoKey("secret", extractable, {
						name: alg.name,
						length
					}, keyUsages, keyData);
				}
				default: throw new DOMException$5(`Unsupported algorithm: ${alg.name}`, "NotSupportedError");
			}
		}
		/**
		* Import a key from external data
		*/
		async importKey(format, keyData, algorithm, extractable, keyUsages) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			if (format === "raw-secret") format = "raw";
			const alg = normalizeAlgorithm(algorithm);
			if (format === "raw") {
				const rawData = toUint8Array$1(keyData);
				switch (alg.name.toUpperCase()) {
					case "AES-GCM":
					case "AES-CBC":
					case "AES-CTR":
					case "AES-KW":
						if (![
							16,
							24,
							32
						].includes(rawData.length)) throw new DOMException$5("Invalid key length", "DataError");
						return new ExactCryptoKey("secret", extractable, {
							name: alg.name,
							length: rawData.length * 8
						}, keyUsages, rawData);
					case "HMAC": {
						const params = algorithm;
						return new ExactCryptoKey("secret", extractable, {
							name: "HMAC",
							hash: { name: normalizeHashName(typeof params.hash === "string" ? params.hash : params.hash.name) },
							length: rawData.length * 8
						}, keyUsages, rawData);
					}
					case "PBKDF2":
					case "HKDF": return new ExactCryptoKey("secret", false, { name: alg.name }, keyUsages, rawData);
					case "ED25519":
					case "EDDSA":
						if (rawData.length !== 32) throw new DOMException$5("Ed25519 key must be 32 bytes", "DataError");
						return new ExactCryptoKey("public", extractable, { name: "Ed25519" }, keyUsages, rawData);
					case "X25519":
						if (rawData.length !== 32) throw new DOMException$5("X25519 key must be 32 bytes", "DataError");
						return new ExactCryptoKey("public", extractable, { name: "X25519" }, keyUsages, rawData);
					default: throw new DOMException$5(`Unsupported algorithm for raw import: ${alg.name}`, "NotSupportedError");
				}
			}
			if (format === "jwk") {
				const jwk = keyData;
				return this._importJwk(jwk, algorithm, extractable, keyUsages);
			}
			if (format === "spki") {
				const rawData = toUint8Array$1(keyData);
				if (typeof __exactImportKeySpki === "function") try {
					const result = __exactImportKeySpki(rawData);
					const rawKeyData = result.rawKeyData instanceof Uint8Array ? result.rawKeyData : new Uint8Array(result.rawKeyData);
					const algName = alg.name.toUpperCase();
					let algorithmInfo;
					if (algName === "ED25519" || algName === "EDDSA") algorithmInfo = { name: "Ed25519" };
					else if (algName === "X25519") algorithmInfo = { name: "X25519" };
					else if (algName.startsWith("RSA")) algorithmInfo = {
						name: alg.name,
						hash: { name: normalizeHashName(typeof algorithm.hash === "string" ? algorithm.hash : algorithm.hash.name) }
					};
					else if (algName === "ECDSA" || algName === "ECDH") algorithmInfo = {
						name: alg.name,
						namedCurve: algorithm.namedCurve
					};
					else algorithmInfo = { name: alg.name };
					return new ExactCryptoKey("public", extractable, algorithmInfo, keyUsages, rawKeyData);
				} catch (e) {
					throw wrapNativeError(e, "NotSupportedError");
				}
				throw new DOMException$5("SPKI import requires native implementation", "NotSupportedError");
			}
			if (format === "pkcs8") {
				const rawData = toUint8Array$1(keyData);
				if (typeof __exactImportKeyPkcs8 === "function") try {
					const result = __exactImportKeyPkcs8(rawData);
					const rawKeyData = result.rawKeyData instanceof Uint8Array ? result.rawKeyData : new Uint8Array(result.rawKeyData);
					const algName = alg.name.toUpperCase();
					let algorithmInfo;
					if (algName === "ED25519" || algName === "EDDSA") algorithmInfo = { name: "Ed25519" };
					else if (algName === "X25519") algorithmInfo = { name: "X25519" };
					else if (algName.startsWith("RSA")) algorithmInfo = {
						name: alg.name,
						hash: { name: normalizeHashName(typeof algorithm.hash === "string" ? algorithm.hash : algorithm.hash.name) }
					};
					else if (algName === "ECDSA" || algName === "ECDH") algorithmInfo = {
						name: alg.name,
						namedCurve: algorithm.namedCurve
					};
					else algorithmInfo = { name: alg.name };
					return new ExactCryptoKey("private", extractable, algorithmInfo, keyUsages, rawKeyData);
				} catch (e) {
					throw wrapNativeError(e, "NotSupportedError");
				}
				throw new DOMException$5("PKCS8 import requires native implementation", "NotSupportedError");
			}
			throw new DOMException$5(`Unsupported format: ${format}`, "NotSupportedError");
		}
		/**
		* Import a JWK key
		*/
		async _importJwk(jwk, algorithm, extractable, keyUsages) {
			const alg = normalizeAlgorithm(algorithm);
			switch (jwk.kty) {
				case "oct": {
					if (!jwk.k) throw new DOMException$5("Missing k parameter in JWK", "DataError");
					const rawData = base64UrlDecode(jwk.k);
					if (alg.name.toUpperCase().startsWith("AES")) return new ExactCryptoKey("secret", extractable, {
						name: alg.name,
						length: rawData.length * 8
					}, keyUsages, rawData);
					if (alg.name.toUpperCase() === "HMAC") {
						const params = algorithm;
						return new ExactCryptoKey("secret", extractable, {
							name: "HMAC",
							hash: { name: normalizeHashName(typeof params.hash === "string" ? params.hash : params.hash.name) },
							length: rawData.length * 8
						}, keyUsages, rawData);
					}
					throw new DOMException$5(`Unsupported algorithm for oct key: ${alg.name}`, "NotSupportedError");
				}
				case "RSA": {
					if (!jwk.n || !jwk.e) throw new DOMException$5("Missing n or e parameter in RSA JWK", "DataError");
					const isPrivate = !!jwk.d;
					const hash = algorithm.hash;
					const hashName = normalizeHashName(typeof hash === "string" ? hash : hash.name);
					const pemStr = rsaJwkToPem(jwk, isPrivate);
					const pemBytes = new TextEncoder().encode(pemStr);
					const algorithmInfo = {
						name: alg.name,
						hash: { name: hashName }
					};
					return new ExactCryptoKey(isPrivate ? "private" : "public", extractable, algorithmInfo, keyUsages, pemBytes);
				}
				case "EC": {
					if (!jwk.x || !jwk.y) throw new DOMException$5("Missing x or y parameter in EC JWK", "DataError");
					const isPrivate = !!jwk.d;
					const curve = jwk.crv || algorithm.namedCurve;
					const x = base64UrlDecode(jwk.x);
					const y = base64UrlDecode(jwk.y);
					let keyDataBytes;
					if (isPrivate && jwk.d) {
						const d = base64UrlDecode(jwk.d);
						keyDataBytes = new Uint8Array(1 + x.length + y.length + d.length);
						keyDataBytes[0] = 4;
						keyDataBytes.set(x, 1);
						keyDataBytes.set(y, 1 + x.length);
						keyDataBytes.set(d, 1 + x.length + y.length);
					} else {
						keyDataBytes = new Uint8Array(1 + x.length + y.length);
						keyDataBytes[0] = 4;
						keyDataBytes.set(x, 1);
						keyDataBytes.set(y, 1 + x.length);
					}
					const ecAlgorithmInfo = {
						name: alg.name,
						namedCurve: curve
					};
					return new ExactCryptoKey(isPrivate ? "private" : "public", extractable, ecAlgorithmInfo, keyUsages, keyDataBytes);
				}
				case "OKP": {
					if (!jwk.x) throw new DOMException$5("Missing x parameter in OKP JWK", "DataError");
					const isPrivate = !!jwk.d;
					const curve = jwk.crv;
					let keyDataBytes;
					if (isPrivate && jwk.d) {
						const dBytes = base64UrlDecode(jwk.d);
						const xBytes = base64UrlDecode(jwk.x);
						keyDataBytes = new Uint8Array(dBytes.length + xBytes.length);
						keyDataBytes.set(dBytes, 0);
						keyDataBytes.set(xBytes, dBytes.length);
					} else keyDataBytes = base64UrlDecode(jwk.x);
					let okpAlgName;
					if (curve === "Ed25519") okpAlgName = "Ed25519";
					else if (curve === "X25519") okpAlgName = "X25519";
					else throw new DOMException$5(`Unsupported OKP curve: ${curve}`, "NotSupportedError");
					return new ExactCryptoKey(isPrivate ? "private" : "public", extractable, { name: okpAlgName }, keyUsages, keyDataBytes);
				}
				default: throw new DOMException$5(`JWK import for ${jwk.kty} keys is not supported`, "NotSupportedError");
			}
		}
		/**
		* Export a key to the specified format
		*/
		async exportKey(format, key) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			if (!key.extractable) throw new DOMException$5("Key is not extractable", "InvalidAccessError");
			const exactKey = key;
			if (format === "raw") {
				if (key.type !== "secret") throw new DOMException$5("Raw export only supported for secret keys", "InvalidAccessError");
				return exactKey._keyData.buffer.slice(exactKey._keyData.byteOffset, exactKey._keyData.byteOffset + exactKey._keyData.byteLength);
			}
			if (format === "jwk") return this._exportJwk(exactKey);
			if (format === "spki") {
				if (key.type !== "public") throw new DOMException$5("SPKI export only supported for public keys", "InvalidAccessError");
				if (typeof __exactExportKeySpki === "function") {
					const algName = key.algorithm.name.toUpperCase();
					return uint8ArrayToArrayBuffer(__exactExportKeySpki(algName, exactKey._keyData));
				}
				throw new DOMException$5("SPKI export requires native implementation", "NotSupportedError");
			}
			if (format === "pkcs8") {
				if (key.type !== "private") throw new DOMException$5("PKCS8 export only supported for private keys", "InvalidAccessError");
				if (typeof __exactExportKeyPkcs8 === "function") {
					const algName = key.algorithm.name.toUpperCase();
					return uint8ArrayToArrayBuffer(__exactExportKeyPkcs8(algName, exactKey._keyData));
				}
				throw new DOMException$5("PKCS8 export requires native implementation", "NotSupportedError");
			}
			throw new DOMException$5(`Export format ${format} is not supported`, "NotSupportedError");
		}
		/**
		* Export a key as JWK
		*/
		_exportJwk(key) {
			if (key.type === "secret") {
				const algName = key.algorithm.name.toUpperCase();
				const jwk = {
					kty: "oct",
					k: base64UrlEncode(key._keyData),
					ext: key.extractable,
					key_ops: [...key.usages]
				};
				if (algName.startsWith("AES")) jwk.alg = `A${key.algorithm.length}${algName.slice(4)}`;
				else if (algName === "HMAC") jwk.alg = `HS${key.algorithm.hash.name.replace("SHA-", "")}`;
				return jwk;
			}
			const algName = key.algorithm.name.toUpperCase();
			if (algName === "ED25519" || algName === "EDDSA") {
				const jwk = {
					kty: "OKP",
					crv: "Ed25519",
					ext: key.extractable,
					key_ops: [...key.usages]
				};
				if (key.type === "public") jwk.x = base64UrlEncode(key._keyData);
				else if (key._keyData.length === 64) {
					jwk.d = base64UrlEncode(key._keyData.slice(0, 32));
					jwk.x = base64UrlEncode(key._keyData.slice(32));
				} else jwk.d = base64UrlEncode(key._keyData);
				return jwk;
			}
			if (algName === "X25519") {
				const jwk = {
					kty: "OKP",
					crv: "X25519",
					ext: key.extractable,
					key_ops: [...key.usages]
				};
				if (key.type === "public") jwk.x = base64UrlEncode(key._keyData);
				else if (key._keyData.length === 64) {
					jwk.d = base64UrlEncode(key._keyData.slice(0, 32));
					jwk.x = base64UrlEncode(key._keyData.slice(32));
				} else jwk.d = base64UrlEncode(key._keyData);
				return jwk;
			}
			if (algName === "ECDSA" || algName === "ECDH") {
				const curve = key.algorithm.namedCurve;
				const data = key._keyData;
				const coordSize = (data.length - 1) / (key.type === "private" ? 3 : 2);
				const jwk = {
					kty: "EC",
					crv: curve,
					x: base64UrlEncode(data.slice(1, 1 + coordSize)),
					y: base64UrlEncode(data.slice(1 + coordSize, 1 + 2 * coordSize)),
					ext: key.extractable,
					key_ops: [...key.usages]
				};
				if (key.type === "private") jwk.d = base64UrlEncode(data.slice(1 + 2 * coordSize));
				return jwk;
			}
			if (algName.startsWith("RSA") || algName === "RSASSA-PKCS1-V1_5") {
				const rsaComponents = parseRsaPemToJwkComponents(uint8ArrayToString(key._keyData), key.type === "private");
				const hash = key.algorithm.hash?.name || "SHA-256";
				let jwkAlg;
				if (algName === "RSA-OAEP") jwkAlg = hash === "SHA-256" ? "RSA-OAEP-256" : hash === "SHA-384" ? "RSA-OAEP-384" : hash === "SHA-512" ? "RSA-OAEP-512" : "RSA-OAEP";
				else if (algName === "RSA-PSS") jwkAlg = hash === "SHA-256" ? "PS256" : hash === "SHA-384" ? "PS384" : "PS512";
				else jwkAlg = hash === "SHA-256" ? "RS256" : hash === "SHA-384" ? "RS384" : "RS512";
				return {
					kty: "RSA",
					alg: jwkAlg,
					ext: key.extractable,
					key_ops: [...key.usages],
					...rsaComponents
				};
			}
			throw new DOMException$5("JWK export for this key type is not supported", "NotSupportedError");
		}
		/**
		* Derive bits from a base key
		*/
		async deriveBits(algorithm, baseKey, length) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			validateKey(baseKey, "deriveBits");
			const alg = normalizeAlgorithm(algorithm);
			switch (alg.name.toUpperCase()) {
				case "PBKDF2": {
					const params = algorithm;
					const salt = toUint8Array$1(params.salt);
					const normalizedHash = normalizeHashName(typeof params.hash === "string" ? params.hash : params.hash.name);
					if (typeof __exactPbkdf2 === "function") return uint8ArrayToArrayBuffer(__exactPbkdf2(baseKey._keyData, salt, params.iterations, length / 8, normalizedHash));
					if (normalizedHash === "SHA-256") return jsPbkdf2Sha256(baseKey._keyData, salt, params.iterations, length / 8);
					throw new DOMException$5("Native crypto not available for PBKDF2", "NotSupportedError");
				}
				case "HKDF": {
					const params = algorithm;
					const salt = toUint8Array$1(params.salt);
					const info = toUint8Array$1(params.info);
					const normalizedHash = normalizeHashName(typeof params.hash === "string" ? params.hash : params.hash.name);
					if (typeof __exactHkdf === "function") return uint8ArrayToArrayBuffer(__exactHkdf(normalizedHash, baseKey._keyData, salt, info, length / 8));
					return jsHkdf(baseKey._keyData, salt, info, normalizedHash, length / 8);
				}
				case "X25519": {
					const publicKey = algorithm.public;
					if (typeof __exactX25519DeriveBits === "function") try {
						const baseKeyData = baseKey._keyData;
						const privKeyData = baseKeyData.length === 64 ? baseKeyData.slice(0, 32) : baseKeyData;
						return uint8ArrayToArrayBuffer(__exactX25519DeriveBits(privKeyData, publicKey._keyData));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for X25519", "NotSupportedError");
				}
				case "ECDH": {
					const publicKey = algorithm.public;
					if (typeof __exactEcdhDeriveBits === "function") try {
						const curve = baseKey.algorithm.namedCurve;
						return uint8ArrayToArrayBuffer(__exactEcdhDeriveBits(curve, baseKey._keyData, publicKey._keyData));
					} catch (e) {
						throw wrapNativeError(e, "OperationError");
					}
					throw new DOMException$5("Native crypto not available for ECDH", "NotSupportedError");
				}
				default: throw new DOMException$5(`Unsupported algorithm: ${alg.name}`, "NotSupportedError");
			}
		}
		/**
		* Derive a key from a base key
		*/
		async deriveKey(algorithm, baseKey, derivedKeyAlgorithm, extractable, keyUsages) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			validateKey(baseKey, "deriveKey");
			const derivedAlg = normalizeAlgorithm(derivedKeyAlgorithm);
			let length;
			if (derivedAlg.name.toUpperCase().startsWith("AES")) length = derivedKeyAlgorithm.length;
			else if (derivedAlg.name.toUpperCase() === "HMAC") {
				const params = derivedKeyAlgorithm;
				const hash = normalizeHashName(typeof params.hash === "string" ? params.hash : params.hash.name);
				length = params.length ?? getHashLength(hash);
			} else throw new DOMException$5(`Unsupported derived key algorithm: ${derivedAlg.name}`, "NotSupportedError");
			const proxyKey = new ExactCryptoKey(baseKey.type, baseKey.extractable, baseKey.algorithm, [...baseKey.usages, "deriveBits"], baseKey._keyData);
			const bits = await this.deriveBits(algorithm, proxyKey, length);
			return this.importKey("raw", bits, derivedKeyAlgorithm, extractable, keyUsages);
		}
		/**
		* Wrap a key with another key
		*/
		async wrapKey(format, key, wrappingKey, wrapAlgorithm) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			const exported = await this.exportKey(format, key);
			let bytes;
			if (exported instanceof ArrayBuffer) bytes = new Uint8Array(exported);
			else bytes = new TextEncoder().encode(JSON.stringify(exported));
			validateKey(wrappingKey, "wrapKey");
			const proxyKey = new ExactCryptoKey(wrappingKey.type, wrappingKey.extractable, wrappingKey.algorithm, [...wrappingKey.usages, "encrypt"], wrappingKey._keyData);
			return this.encrypt(wrapAlgorithm, proxyKey, bytes);
		}
		/**
		* Unwrap a key
		*/
		async unwrapKey(format, wrappedKey, unwrappingKey, unwrapAlgorithm, unwrappedKeyAlgorithm, extractable, keyUsages) {
			_assertClassBrand(_SubtleCrypto_brand, this, _checkCapability$1).call(this);
			validateKey(unwrappingKey, "unwrapKey");
			const proxyKey = new ExactCryptoKey(unwrappingKey.type, unwrappingKey.extractable, unwrappingKey.algorithm, [...unwrappingKey.usages, "decrypt"], unwrappingKey._keyData);
			const decrypted = await this.decrypt(unwrapAlgorithm, proxyKey, wrappedKey);
			if (format === "jwk") {
				const jwkString = new TextDecoder().decode(decrypted);
				const jwk = JSON.parse(jwkString);
				return this.importKey(format, jwk, unwrappedKeyAlgorithm, extractable, keyUsages);
			}
			return this.importKey(format, decrypted, unwrappedKeyAlgorithm, extractable, keyUsages);
		}
	};
	/**
	* Check capability before any subtle crypto operation.
	* @throws NotAllowedError if capability is not granted
	*/
	function _checkCapability$1() {
		requireCapability(Capabilities.CRYPTO_SUBTLE);
	}
	var Crypto = class {
		constructor() {
			_defineProperty(this, "_subtle", new SubtleCrypto());
		}
		get subtle() {
			return this._subtle;
		}
		/**
		* Fill array with cryptographically secure random bytes
		*/
		getRandomValues(array) {
			requireCapability(Capabilities.CRYPTO_RANDOM);
			if (!(array instanceof Int8Array || array instanceof Uint8Array || array instanceof Uint8ClampedArray || array instanceof Int16Array || array instanceof Uint16Array || array instanceof Int32Array || array instanceof Uint32Array || array instanceof BigInt64Array || array instanceof BigUint64Array)) throw new TypeError("Argument must be an integer-typed TypedArray");
			if (array.byteLength > 65536) throw new DOMException$5("The ArrayBufferView's byte length exceeds the limit (65536)", "QuotaExceededError");
			const native = getNativeCryptoModule();
			if (native) {
				const bytes = new Uint8Array(array.buffer, array.byteOffset, array.byteLength);
				native.getRandomValues(bytes);
				return array;
			}
			if (!(typeof process !== "undefined" && process.env?.EXACT_ALLOW_INSECURE_CRYPTO === "true" || typeof globalThis.__EXACT_TEST_MODE__ !== "undefined")) throw new DOMException$5("crypto.getRandomValues requires native crypto module. The native module is not available and insecure fallbacks are disabled in production. Set EXACT_ALLOW_INSECURE_CRYPTO=true or __EXACT_TEST_MODE__=true for testing.", "NotSupportedError");
			if (!isSilentMode()) console.warn("crypto.getRandomValues: Using INSECURE Math.random() fallback! This is only acceptable in test environments.");
			const bytes = new Uint8Array(array.buffer, array.byteOffset, array.byteLength);
			for (let i = 0; i < bytes.length; i++) bytes[i] = Math.floor(Math.random() * 256);
			return array;
		}
		/**
		* Generate a random UUID v4
		*/
		randomUUID() {
			requireCapability(Capabilities.CRYPTO_RANDOM);
			const native = getNativeCryptoModule();
			if (native) return native.randomUUID();
			const bytes = new Uint8Array(16);
			this.getRandomValues(bytes);
			bytes[6] = bytes[6] & 15 | 64;
			bytes[8] = bytes[8] & 63 | 128;
			const hex = Array.from(bytes, (b) => b.toString(16).padStart(2, "0")).join("");
			return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-${hex.slice(12, 16)}-${hex.slice(16, 20)}-${hex.slice(20)}`;
		}
	};
	function toUint8Array$1(data) {
		if (data instanceof ArrayBuffer) return new Uint8Array(data);
		if (ArrayBuffer.isView(data)) return new Uint8Array(data.buffer, data.byteOffset, data.byteLength);
		throw new TypeError("Data must be an ArrayBuffer or ArrayBufferView");
	}
	async function jsSHA256(data) {
		const K = [
			1116352408,
			1899447441,
			3049323471,
			3921009573,
			961987163,
			1508970993,
			2453635748,
			2870763221,
			3624381080,
			310598401,
			607225278,
			1426881987,
			1925078388,
			2162078206,
			2614888103,
			3248222580,
			3835390401,
			4022224774,
			264347078,
			604807628,
			770255983,
			1249150122,
			1555081692,
			1996064986,
			2554220882,
			2821834349,
			2952996808,
			3210313671,
			3336571891,
			3584528711,
			113926993,
			338241895,
			666307205,
			773529912,
			1294757372,
			1396182291,
			1695183700,
			1986661051,
			2177026350,
			2456956037,
			2730485921,
			2820302411,
			3259730800,
			3345764771,
			3516065817,
			3600352804,
			4094571909,
			275423344,
			430227734,
			506948616,
			659060556,
			883997877,
			958139571,
			1322822218,
			1537002063,
			1747873779,
			1955562222,
			2024104815,
			2227730452,
			2361852424,
			2428436474,
			2756734187,
			3204031479,
			3329325298
		];
		let h0 = 1779033703;
		let h1 = 3144134277;
		let h2 = 1013904242;
		let h3 = 2773480762;
		let h4 = 1359893119;
		let h5 = 2600822924;
		let h6 = 528734635;
		let h7 = 1541459225;
		const msgLen = data.length;
		const bitLen = msgLen * 8;
		let paddedLen = msgLen + 1 + 8;
		const remainder = paddedLen % 64;
		if (remainder > 0) paddedLen += 64 - remainder;
		if (paddedLen - msgLen - 1 < 8) paddedLen += 64;
		const padded = new Uint8Array(paddedLen);
		padded.set(data);
		padded[msgLen] = 128;
		const view = new DataView(padded.buffer);
		view.setUint32(paddedLen - 4, bitLen, false);
		for (let offset = 0; offset < paddedLen; offset += 64) {
			const W = new Uint32Array(64);
			for (let i = 0; i < 16; i++) W[i] = view.getUint32(offset + i * 4, false);
			for (let i = 16; i < 64; i++) {
				const s0 = rotr(W[i - 15], 7) ^ rotr(W[i - 15], 18) ^ W[i - 15] >>> 3;
				const s1 = rotr(W[i - 2], 17) ^ rotr(W[i - 2], 19) ^ W[i - 2] >>> 10;
				W[i] = W[i - 16] + s0 + W[i - 7] + s1 >>> 0;
			}
			let a = h0, b = h1, c = h2, d = h3, e = h4, f = h5, g = h6, h = h7;
			for (let i = 0; i < 64; i++) {
				const S1 = rotr(e, 6) ^ rotr(e, 11) ^ rotr(e, 25);
				const ch = e & f ^ ~e & g;
				const temp1 = h + S1 + ch + K[i] + W[i] >>> 0;
				const temp2 = (rotr(a, 2) ^ rotr(a, 13) ^ rotr(a, 22)) + (a & b ^ a & c ^ b & c) >>> 0;
				h = g;
				g = f;
				f = e;
				e = d + temp1 >>> 0;
				d = c;
				c = b;
				b = a;
				a = temp1 + temp2 >>> 0;
			}
			h0 = h0 + a >>> 0;
			h1 = h1 + b >>> 0;
			h2 = h2 + c >>> 0;
			h3 = h3 + d >>> 0;
			h4 = h4 + e >>> 0;
			h5 = h5 + f >>> 0;
			h6 = h6 + g >>> 0;
			h7 = h7 + h >>> 0;
		}
		const result = /* @__PURE__ */ new ArrayBuffer(32);
		const resultView = new DataView(result);
		resultView.setUint32(0, h0, false);
		resultView.setUint32(4, h1, false);
		resultView.setUint32(8, h2, false);
		resultView.setUint32(12, h3, false);
		resultView.setUint32(16, h4, false);
		resultView.setUint32(20, h5, false);
		resultView.setUint32(24, h6, false);
		resultView.setUint32(28, h7, false);
		return result;
	}
	function rotr(x, n) {
		return (x >>> n | x << 32 - n) >>> 0;
	}
	var DOMException$5 = class extends Error {
		constructor(message, name) {
			super(message);
			_defineProperty(this, "code", 0);
			this.name = name;
		}
	};
	_Symbol$toStringTag$23 = Symbol.toStringTag;
	var ExactCryptoKey = class {
		constructor(type, extractable, algorithm, usages, keyData) {
			_defineProperty(this, "type", void 0);
			_defineProperty(this, "extractable", void 0);
			_defineProperty(this, "algorithm", void 0);
			_defineProperty(this, "usages", void 0);
			_defineProperty(this, "_keyData", void 0);
			this.type = type;
			this.extractable = extractable;
			this.algorithm = algorithm;
			this.usages = usages;
			this._keyData = keyData;
		}
		get [_Symbol$toStringTag$23]() {
			return "CryptoKey";
		}
	};
	/**
	* Normalize algorithm identifier to object form
	*/
	function normalizeAlgorithm(algorithm) {
		if (typeof algorithm === "string") return { name: algorithm };
		return algorithm;
	}
	/**
	* Validate that a key can be used for the specified operation
	*/
	function validateKey(key, operation) {
		if (!key.usages.includes(operation)) throw new DOMException$5(`Key does not support the '${operation}' operation`, "InvalidAccessError");
	}
	/**
	* Normalize hash names like `sha256`, `SHA-256`, and `sha-256`.
	*/
	function normalizeHashName(hash) {
		switch (hash.replace(/[^A-Za-z0-9]/g, "").toUpperCase()) {
			case "SHA1": return "SHA-1";
			case "SHA224": return "SHA-224";
			case "SHA256": return "SHA-256";
			case "SHA384": return "SHA-384";
			case "SHA512": return "SHA-512";
			default: throw new DOMException$5(`Unrecognized hash algorithm: ${hash}`, "NotSupportedError");
		}
	}
	/**
	* Return a platform subtle implementation for digest and HMAC fallbacks.
	* This avoids recursive calls into the same Exact crypto implementation.
	*/
	function getPlatformSubtle() {
		const g = globalThis;
		const candidate = g.crypto?.subtle;
		if (candidate && candidate !== crypto$1.subtle) return candidate;
		if (g.webcrypto?.subtle) return g.webcrypto.subtle;
		return null;
	}
	/**
	* Get hash output length in bits
	*/
	function getHashLength(hash) {
		switch (normalizeHashName(hash)) {
			case "SHA-1": return 160;
			case "SHA-256": return 256;
			case "SHA-384": return 384;
			case "SHA-512": return 512;
			default: throw new DOMException$5(`Unsupported hash algorithm: ${hash}`, "NotSupportedError");
		}
	}
	function normalizeRequiredHash(hash) {
		if (typeof hash === "string") return normalizeHashName(hash);
		if (hash && typeof hash === "object" && typeof hash.name === "string") return normalizeHashName(hash.name);
		throw new DOMException$5("Invalid hash algorithm", "NotSupportedError");
	}
	/**
	* Base64URL encode
	*/
	function base64UrlEncode(data) {
		return btoa(String.fromCharCode(...data)).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
	}
	/**
	* Base64URL decode
	*/
	function base64UrlDecode(str) {
		let padded = str.replace(/-/g, "+").replace(/_/g, "/");
		while (padded.length % 4) padded += "=";
		const binary = atob(padded);
		const bytes = new Uint8Array(binary.length);
		for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
		return bytes;
	}
	/**
	* Constant-time comparison to prevent timing attacks
	*/
	function constantTimeCompare(a, b) {
		if (a.length !== b.length) return false;
		let result = 0;
		for (let i = 0; i < a.length; i++) result |= a[i] ^ b[i];
		return result === 0;
	}
	/**
	* Pure JS SHA-1 implementation
	*/
	async function jsSHA1(data) {
		let h0 = 1732584193;
		let h1 = 4023233417;
		let h2 = 2562383102;
		let h3 = 271733878;
		let h4 = 3285377520;
		const msgLen = data.length;
		const bitLen = msgLen * 8;
		let paddedLen = msgLen + 1 + 8;
		const remainder = paddedLen % 64;
		if (remainder > 0) paddedLen += 64 - remainder;
		if (paddedLen - msgLen - 1 < 8) paddedLen += 64;
		const padded = new Uint8Array(paddedLen);
		padded.set(data);
		padded[msgLen] = 128;
		const view = new DataView(padded.buffer);
		view.setUint32(paddedLen - 4, bitLen, false);
		function rotl(x, n) {
			return (x << n | x >>> 32 - n) >>> 0;
		}
		for (let offset = 0; offset < paddedLen; offset += 64) {
			const W = new Uint32Array(80);
			for (let i = 0; i < 16; i++) W[i] = view.getUint32(offset + i * 4, false);
			for (let i = 16; i < 80; i++) W[i] = rotl(W[i - 3] ^ W[i - 8] ^ W[i - 14] ^ W[i - 16], 1);
			let a = h0, b = h1, c = h2, d = h3, e = h4;
			for (let i = 0; i < 80; i++) {
				let f, k;
				if (i < 20) {
					f = b & c | ~b & d;
					k = 1518500249;
				} else if (i < 40) {
					f = b ^ c ^ d;
					k = 1859775393;
				} else if (i < 60) {
					f = b & c | b & d | c & d;
					k = 2400959708;
				} else {
					f = b ^ c ^ d;
					k = 3395469782;
				}
				const temp = rotl(a, 5) + f + e + k + W[i] >>> 0;
				e = d;
				d = c;
				c = rotl(b, 30);
				b = a;
				a = temp;
			}
			h0 = h0 + a >>> 0;
			h1 = h1 + b >>> 0;
			h2 = h2 + c >>> 0;
			h3 = h3 + d >>> 0;
			h4 = h4 + e >>> 0;
		}
		const result = /* @__PURE__ */ new ArrayBuffer(20);
		const rv = new DataView(result);
		rv.setUint32(0, h0, false);
		rv.setUint32(4, h1, false);
		rv.setUint32(8, h2, false);
		rv.setUint32(12, h3, false);
		rv.setUint32(16, h4, false);
		return result;
	}
	/**
	* Pure JS SHA-512/384 implementation using BigInt for 64-bit operations
	*/
	async function jsSHA512Core(data, is384) {
		const K = [
			4794697086780616226n,
			8158064640168781261n,
			13096744586834688815n,
			16840607885511220156n,
			4131703408338449720n,
			6480981068601479193n,
			10538285296894168987n,
			12329834152419229976n,
			15566598209576043074n,
			1334009975649890238n,
			2608012711638119052n,
			6128411473006802146n,
			8268148722764581231n,
			9286055187155687089n,
			11230858885718282805n,
			13951009754708518548n,
			16472876342353939154n,
			17275323862435702243n,
			1135362057144423861n,
			2597628984639134821n,
			3308224258029322869n,
			5365058923640841347n,
			6679025012923562964n,
			8573033837759648693n,
			10970295158949994411n,
			12119686244451234320n,
			12683024718118986047n,
			13788192230050041572n,
			14330467153632333762n,
			15395433587784984357n,
			489312712824947311n,
			1452737877330783856n,
			2861767655752347644n,
			3322285676063803686n,
			5560940570517711597n,
			5996557281743188959n,
			7280758554555802590n,
			8532644243296465576n,
			9350256976987008742n,
			10552545826968843579n,
			11727347734174303076n,
			12113106623233404929n,
			14000437183269869457n,
			14369950271660146224n,
			15101387698204529176n,
			15463397548674623760n,
			17586052441742319658n,
			1182934255886127544n,
			1847814050463011016n,
			2177327727835720531n,
			2830643537854262169n,
			3796741975233480872n,
			4115178125766777443n,
			5681478168544905931n,
			6601373596472566643n,
			7507060721942968483n,
			8399075790359081724n,
			8693463985226723168n,
			9568029438360202098n,
			10144078919501101548n,
			10430055236837252648n,
			11840083180663258601n,
			13761210420658862357n,
			14299343276471374635n,
			14566680578165727644n,
			15097957966210449927n,
			16922976911328602910n,
			17689382322260857208n,
			500013540394364858n,
			748580250866718886n,
			1242879168328830382n,
			1977374033974150939n,
			2944078676154940804n,
			3659926193048069267n,
			4368137639120453308n,
			4836135668995329356n,
			5532061633213252278n,
			6448918945643986474n,
			6902733635092675308n,
			7801388544844847127n
		];
		const MASK64 = 18446744073709551615n;
		function rotr64(x, n) {
			return (x >> BigInt(n) | x << BigInt(64 - n)) & MASK64;
		}
		let H;
		if (is384) H = [
			14680500436340154072n,
			7105036623409894663n,
			10473403895298186519n,
			1526699215303891257n,
			7436329637833083697n,
			10282925794625328401n,
			15784041429090275239n,
			5167115440072839076n
		];
		else H = [
			7640891576956012808n,
			13503953896175478587n,
			4354685564936845355n,
			11912009170470909681n,
			5840696475078001361n,
			11170449401992604703n,
			2270897969802886507n,
			6620516959819538809n
		];
		const msgLen = data.length;
		const bitLen = BigInt(msgLen) * 8n;
		let paddedLen = msgLen + 1 + 16;
		const remainder = paddedLen % 128;
		if (remainder > 0) paddedLen += 128 - remainder;
		if (paddedLen - msgLen - 1 < 16) paddedLen += 128;
		const padded = new Uint8Array(paddedLen);
		padded.set(data);
		padded[msgLen] = 128;
		const lenView = new DataView(padded.buffer);
		const lenHigh = Number(bitLen >> 32n & 4294967295n);
		const lenLow = Number(bitLen & 4294967295n);
		lenView.setUint32(paddedLen - 8, lenHigh, false);
		lenView.setUint32(paddedLen - 4, lenLow, false);
		const dv = new DataView(padded.buffer);
		for (let offset = 0; offset < paddedLen; offset += 128) {
			const W = new Array(80);
			for (let i = 0; i < 16; i++) {
				const hi = BigInt(dv.getUint32(offset + i * 8, false));
				const lo = BigInt(dv.getUint32(offset + i * 8 + 4, false));
				W[i] = (hi << 32n | lo) & MASK64;
			}
			for (let i = 16; i < 80; i++) {
				const s0 = rotr64(W[i - 15], 1) ^ rotr64(W[i - 15], 8) ^ W[i - 15] >> 7n;
				const s1 = rotr64(W[i - 2], 19) ^ rotr64(W[i - 2], 61) ^ W[i - 2] >> 6n;
				W[i] = W[i - 16] + s0 + W[i - 7] + s1 & MASK64;
			}
			let [a, b, c, d, e, f, g, h] = H;
			for (let i = 0; i < 80; i++) {
				const S1 = rotr64(e, 14) ^ rotr64(e, 18) ^ rotr64(e, 41);
				const ch = e & f ^ ~e & MASK64 & g;
				const temp1 = h + S1 + ch + K[i] + W[i] & MASK64;
				const temp2 = (rotr64(a, 28) ^ rotr64(a, 34) ^ rotr64(a, 39)) + (a & b ^ a & c ^ b & c) & MASK64;
				h = g;
				g = f;
				f = e;
				e = d + temp1 & MASK64;
				d = c;
				c = b;
				b = a;
				a = temp1 + temp2 & MASK64;
			}
			H[0] = H[0] + a & MASK64;
			H[1] = H[1] + b & MASK64;
			H[2] = H[2] + c & MASK64;
			H[3] = H[3] + d & MASK64;
			H[4] = H[4] + e & MASK64;
			H[5] = H[5] + f & MASK64;
			H[6] = H[6] + g & MASK64;
			H[7] = H[7] + h & MASK64;
		}
		const outputWords = is384 ? 6 : 8;
		const result = /* @__PURE__ */ new ArrayBuffer(outputWords * 8);
		const rv = new DataView(result);
		for (let i = 0; i < outputWords; i++) {
			rv.setUint32(i * 8, Number(H[i] >> 32n & 4294967295n), false);
			rv.setUint32(i * 8 + 4, Number(H[i] & 4294967295n), false);
		}
		return result;
	}
	/**
	* SHA-384 digest (pure JS)
	*/
	async function jsSHA384(data) {
		return jsSHA512Core(data, true);
	}
	/**
	* SHA-512 digest (pure JS)
	*/
	async function jsSHA512(data) {
		return jsSHA512Core(data, false);
	}
	/**
	* Generic HMAC fallback used when native bridge callbacks are unavailable.
	*/
	async function jsHmac(key, data, hash) {
		const normalizedHash = normalizeHashName(hash);
		if (normalizedHash === "SHA-256") return jsHmacSha256(key, data);
		const subtle = getPlatformSubtle();
		if (!subtle?.importKey || !subtle?.sign) throw new DOMException$5(`Native crypto module not available for HMAC: ${normalizedHash}`, "NotSupportedError");
		const importedKey = await subtle.importKey("raw", key, {
			name: "HMAC",
			hash: { name: normalizedHash }
		}, false, ["sign"]);
		return subtle.sign({ name: "HMAC" }, importedKey, data);
	}
	/**
	* HMAC-SHA256 implementation (for JS fallback)
	*/
	async function jsHmacSha256(key, data) {
		const blockSize = 64;
		let keyBytes = key;
		if (keyBytes.length > blockSize) keyBytes = new Uint8Array(await jsSHA256(keyBytes));
		const paddedKey = new Uint8Array(blockSize);
		paddedKey.set(keyBytes);
		const ipad = new Uint8Array(blockSize);
		const opad = new Uint8Array(blockSize);
		for (let i = 0; i < blockSize; i++) {
			ipad[i] = paddedKey[i] ^ 54;
			opad[i] = paddedKey[i] ^ 92;
		}
		const innerData = new Uint8Array(blockSize + data.length);
		innerData.set(ipad);
		innerData.set(data, blockSize);
		const innerHash = new Uint8Array(await jsSHA256(innerData));
		const outerData = new Uint8Array(96);
		outerData.set(opad);
		outerData.set(innerHash, blockSize);
		return jsSHA256(outerData);
	}
	/**
	* Convert Uint8Array to its underlying ArrayBuffer (handling offset/length)
	*/
	function uint8ArrayToArrayBuffer(data) {
		return data.buffer.slice(data.byteOffset, data.byteOffset + data.byteLength);
	}
	/**
	* Convert Uint8Array to a binary string (one char per byte)
	*/
	function uint8ArrayToString(data) {
		let str = "";
		for (let i = 0; i < data.length; i++) str += String.fromCharCode(data[i]);
		return str;
	}
	/**
	* Convert a hex string to an ArrayBuffer
	*/
	function hexStringToArrayBuffer(hex) {
		const bytes = new Uint8Array(hex.length / 2);
		for (let i = 0; i < hex.length; i += 2) bytes[i / 2] = parseInt(hex.substring(i, i + 2), 16);
		return bytes.buffer;
	}
	/**
	* HKDF implementation (JS fallback) per RFC 5869
	* HKDF-Extract: PRK = HMAC-Hash(salt, IKM)
	* HKDF-Expand: iteratively compute T(i) = HMAC-Hash(PRK, T(i-1) || info || i) until enough bytes
	*/
	async function jsHkdf(ikm, salt, info, hash, length) {
		const hashLen = getHashLength(hash) / 8;
		const actualSalt = salt.length > 0 ? salt : new Uint8Array(hashLen);
		const prk = new Uint8Array(await jsHmac(actualSalt, ikm, hash));
		const n = Math.ceil(length / hashLen);
		const okm = new Uint8Array(n * hashLen);
		let prev = new Uint8Array(0);
		for (let i = 1; i <= n; i++) {
			const input = new Uint8Array(prev.length + info.length + 1);
			input.set(prev, 0);
			input.set(info, prev.length);
			input[prev.length + info.length] = i;
			prev = new Uint8Array(await jsHmac(prk, input, hash));
			okm.set(prev, (i - 1) * hashLen);
		}
		return okm.buffer.slice(0, length);
	}
	/**
	* Parse RSA PEM key (public or private) and extract JWK components.
	* This parses the base64-encoded DER within the PEM and extracts the
	* RSA parameters (n, e, d, p, q, dp, dq, qi) from the ASN.1 structure.
	*/
	function parseRsaPemToJwkComponents(pem, isPrivate) {
		const parsed = parseAsn1(base64Decode(pem.replace(/-----[A-Z ]+-----/g, "").replace(/\s/g, "")), 0);
		if (isPrivate) {
			let seq = parsed;
			if (seq.children && seq.children.length === 3 && seq.children[2].tag === 4) seq = parseAsn1(seq.children[2].value, 0);
			if (!seq.children || seq.children.length < 9) throw new DOMException$5("Invalid RSA private key structure", "DataError");
			return {
				n: base64UrlEncode(trimLeadingZero(seq.children[1].value)),
				e: base64UrlEncode(trimLeadingZero(seq.children[2].value)),
				d: base64UrlEncode(trimLeadingZero(seq.children[3].value)),
				p: base64UrlEncode(trimLeadingZero(seq.children[4].value)),
				q: base64UrlEncode(trimLeadingZero(seq.children[5].value)),
				dp: base64UrlEncode(trimLeadingZero(seq.children[6].value)),
				dq: base64UrlEncode(trimLeadingZero(seq.children[7].value)),
				qi: base64UrlEncode(trimLeadingZero(seq.children[8].value))
			};
		} else {
			let seq = parsed;
			if (seq.children && seq.children.length === 2 && seq.children[1].tag === 3) {
				const bitStr = seq.children[1].value;
				seq = parseAsn1(bitStr.slice(1), 0);
			}
			if (!seq.children || seq.children.length < 2) throw new DOMException$5("Invalid RSA public key structure", "DataError");
			return {
				n: base64UrlEncode(trimLeadingZero(seq.children[0].value)),
				e: base64UrlEncode(trimLeadingZero(seq.children[1].value))
			};
		}
	}
	function parseAsn1(data, offset) {
		const tag = data[offset];
		let lengthOffset = offset + 1;
		let length;
		if (data[lengthOffset] & 128) {
			const numLengthBytes = data[lengthOffset] & 127;
			length = 0;
			for (let i = 0; i < numLengthBytes; i++) length = length << 8 | data[lengthOffset + 1 + i];
			lengthOffset += 1 + numLengthBytes;
		} else {
			length = data[lengthOffset];
			lengthOffset += 1;
		}
		const value = data.slice(lengthOffset, lengthOffset + length);
		const node = {
			tag,
			value
		};
		if (tag === 48 || tag === 49) {
			node.children = [];
			let childOffset = 0;
			while (childOffset < value.length) {
				const child = parseAsn1(value, childOffset);
				node.children.push(child);
				let childLength = child.value.length;
				let headerLen = 2;
				if (childLength >= 128) {
					let tmp = childLength;
					let numBytes = 0;
					while (tmp > 0) {
						numBytes++;
						tmp >>= 8;
					}
					headerLen = 2 + numBytes;
				}
				childOffset += headerLen + childLength;
			}
		}
		return node;
	}
	/**
	* Trim the leading zero byte from an ASN.1 INTEGER value
	* (ASN.1 uses a leading 0x00 to indicate a positive number when the high bit is set)
	*/
	function trimLeadingZero(data) {
		if (data.length > 1 && data[0] === 0) return data.slice(1);
		return data;
	}
	/**
	* Standard base64 decode (not base64url)
	*/
	function base64Decode(str) {
		const binary = atob(str);
		const bytes = new Uint8Array(binary.length);
		for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
		return bytes;
	}
	/**
	* Standard base64 encode
	*/
	function base64Encode(data) {
		return btoa(String.fromCharCode(...data));
	}
	/**
	* Convert RSA JWK to PEM format
	* Constructs ASN.1 DER for PKCS#1 and wraps in PEM
	*/
	function rsaJwkToPem(jwk, isPrivate) {
		if (isPrivate) {
			const seq = encodeAsn1Sequence(concatUint8Arrays([
				encodeAsn1Integer(new Uint8Array([0])),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.n))),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.e))),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.d))),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.p))),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.q))),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.dp))),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.dq))),
				encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.qi)))
			]));
			return "-----BEGIN PRIVATE KEY-----\n" + (base64Encode(encodeAsn1Sequence(concatUint8Arrays([
				encodeAsn1Integer(new Uint8Array([0])),
				encodeAsn1Sequence(concatUint8Arrays([new Uint8Array([
					6,
					9,
					42,
					134,
					72,
					134,
					247,
					13,
					1,
					1,
					1
				]), new Uint8Array([5, 0])])),
				encodeAsn1Tag(4, seq)
			]))).match(/.{1,64}/g) || []).join("\n") + "\n-----END PRIVATE KEY-----\n";
		} else {
			const rsaPubKey = encodeAsn1Sequence(concatUint8Arrays([encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.n))), encodeAsn1Integer(addLeadingZero(base64UrlDecode(jwk.e)))]));
			return "-----BEGIN PUBLIC KEY-----\n" + (base64Encode(encodeAsn1Sequence(concatUint8Arrays([encodeAsn1Sequence(concatUint8Arrays([new Uint8Array([
				6,
				9,
				42,
				134,
				72,
				134,
				247,
				13,
				1,
				1,
				1
			]), new Uint8Array([5, 0])])), encodeAsn1Tag(3, concatUint8Arrays([new Uint8Array([0]), rsaPubKey]))]))).match(/.{1,64}/g) || []).join("\n") + "\n-----END PUBLIC KEY-----\n";
		}
	}
	/** Encode ASN.1 INTEGER */
	function encodeAsn1Integer(data) {
		return encodeAsn1Tag(2, data);
	}
	/** Encode ASN.1 SEQUENCE */
	function encodeAsn1Sequence(content) {
		return encodeAsn1Tag(48, content);
	}
	/** Encode an ASN.1 TLV (tag-length-value) */
	function encodeAsn1Tag(tag, content) {
		const length = content.length;
		let header;
		if (length < 128) header = new Uint8Array([tag, length]);
		else if (length < 256) header = new Uint8Array([
			tag,
			129,
			length
		]);
		else if (length < 65536) header = new Uint8Array([
			tag,
			130,
			length >> 8 & 255,
			length & 255
		]);
		else header = new Uint8Array([
			tag,
			131,
			length >> 16 & 255,
			length >> 8 & 255,
			length & 255
		]);
		return concatUint8Arrays([header, content]);
	}
	/** Add leading zero byte if high bit is set (for ASN.1 positive INTEGER) */
	function addLeadingZero(data) {
		if (data.length > 0 && (data[0] & 128) !== 0) {
			const result = new Uint8Array(data.length + 1);
			result[0] = 0;
			result.set(data, 1);
			return result;
		}
		return data;
	}
	/** Concatenate multiple Uint8Arrays */
	function concatUint8Arrays(arrays) {
		let totalLen = 0;
		{
			const __exactForOfIterator62 = arrays[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep62 = __exactForOfIterator62.next();
				if (__exactForOfStep62.done) break;
				const arr = __exactForOfStep62.value;
				totalLen += arr.length;
			}
		}
		const result = new Uint8Array(totalLen);
		let offset = 0;
		{
			const __exactForOfIterator63 = arrays[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep63 = __exactForOfIterator63.next();
				if (__exactForOfStep63.done) break;
				const arr = __exactForOfStep63.value;
				result.set(arr, offset);
				offset += arr.length;
			}
		}
		return result;
	}
	/**
	* PBKDF2-SHA256 implementation (for JS fallback)
	*/
	async function jsPbkdf2Sha256(password, salt, iterations, keyLength) {
		const hashLength = 32;
		const numBlocks = Math.ceil(keyLength / hashLength);
		const result = new Uint8Array(numBlocks * hashLength);
		for (let block = 1; block <= numBlocks; block++) {
			const blockData = new Uint8Array(salt.length + 4);
			blockData.set(salt);
			blockData[salt.length] = block >> 24 & 255;
			blockData[salt.length + 1] = block >> 16 & 255;
			blockData[salt.length + 2] = block >> 8 & 255;
			blockData[salt.length + 3] = block & 255;
			let u = new Uint8Array(await jsHmacSha256(password, blockData));
			const f = new Uint8Array(u);
			for (let i = 1; i < iterations; i++) {
				u = new Uint8Array(await jsHmacSha256(password, u));
				for (let j = 0; j < hashLength; j++) f[j] ^= u[j];
			}
			result.set(f, (block - 1) * hashLength);
		}
		return result.buffer.slice(0, keyLength);
	}
	const crypto$1 = new Crypto();
	//#endregion
	//#region ../ibex-runtime-js/src/blob/FormData.ts
	let _Symbol$iterator$2, _Symbol$toStringTag$22;
	var _entries = /* @__PURE__ */ new WeakMap();
	_Symbol$iterator$2 = Symbol.iterator;
	_Symbol$toStringTag$22 = Symbol.toStringTag;
	/**
	* FormData provides a way to construct a set of key/value pairs
	* representing form fields and their values.
	*/
	var FormData$1 = class {
		constructor(form) {
			_classPrivateFieldInitSpec(this, _entries, []);
			if (form !== void 0 && form !== null) throw new TypeError("FormData constructor with form element is not supported");
		}
		append(name, value, filename) {
			const normalizedName = String(name);
			const entry = normalizeValue(value, filename);
			_classPrivateFieldGet2(_entries, this).push([normalizedName, entry]);
		}
		/**
		* Deletes all entries with the given name.
		*/
		delete(name) {
			const normalizedName = String(name);
			for (let i = _classPrivateFieldGet2(_entries, this).length - 1; i >= 0; i--) if (_classPrivateFieldGet2(_entries, this)[i][0] === normalizedName) _classPrivateFieldGet2(_entries, this).splice(i, 1);
		}
		/**
		* Returns the first value associated with a given key.
		*/
		get(name) {
			const normalizedName = String(name);
			for (const [entryName, value] of _classPrivateFieldGet2(_entries, this)) if (entryName === normalizedName) return value;
			return null;
		}
		/**
		* Returns all values associated with a given key.
		*/
		getAll(name) {
			const normalizedName = String(name);
			const result = [];
			{
				const __exactForOfIterator64 = _classPrivateFieldGet2(_entries, this)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep64 = __exactForOfIterator64.next();
					if (__exactForOfStep64.done) break;
					const [entryName, value] = __exactForOfStep64.value;
					if (entryName === normalizedName) result.push(value);
				}
			}
			return result;
		}
		/**
		* Returns whether a key exists in the FormData.
		*/
		has(name) {
			const normalizedName = String(name);
			for (const [entryName] of _classPrivateFieldGet2(_entries, this)) if (entryName === normalizedName) return true;
			return false;
		}
		set(name, value, filename) {
			const normalizedName = String(name);
			const entry = normalizeValue(value, filename);
			let found = false;
			for (let i = 0; i < _classPrivateFieldGet2(_entries, this).length; i++) if (_classPrivateFieldGet2(_entries, this)[i][0] === normalizedName) if (!found) {
				_classPrivateFieldGet2(_entries, this)[i] = [normalizedName, entry];
				found = true;
			} else {
				_classPrivateFieldGet2(_entries, this).splice(i, 1);
				i--;
			}
			if (!found) _classPrivateFieldGet2(_entries, this).push([normalizedName, entry]);
		}
		/**
		* Returns an iterator of all keys.
		*/
		*keys() {
			for (const [name] of _classPrivateFieldGet2(_entries, this)) yield name;
		}
		/**
		* Returns an iterator of all values.
		*/
		*values() {
			for (const [, value] of _classPrivateFieldGet2(_entries, this)) yield value;
		}
		/**
		* Returns an iterator of all key/value pairs.
		*/
		*entries() {
			for (const entry of _classPrivateFieldGet2(_entries, this)) yield entry;
		}
		/**
		* Calls a function for each key/value pair.
		*/
		forEach(callback, thisArg) {
			const entries = _classPrivateFieldGet2(_entries, this);
			for (let i = 0; i < entries.length; i++) callback.call(thisArg, entries[i][1], entries[i][0], this);
		}
		[_Symbol$iterator$2]() {
			return this.entries();
		}
		get [_Symbol$toStringTag$22]() {
			return "FormData";
		}
		/**
		* Internal method to get all entries for serialization.
		* Used by fetch() to build multipart/form-data body.
		*/
		_getEntries() {
			return [..._classPrivateFieldGet2(_entries, this)];
		}
		/**
		* Encode FormData as multipart/form-data.
		* 
		* This method is used by fetch() when sending FormData as request body.
		* 
		* @returns Object containing body bytes, content-type header, and boundary
		* 
		* @example
		* const formData = new FormData();
		* formData.append('name', 'John');
		* formData.append('file', new File(['content'], 'test.txt'));
		* const { body, contentType } = formData._encode();
		* // contentType = 'multipart/form-data; boundary=----ExactFormBoundary...'
		*/
		_encode() {
			const boundary = generateBoundary();
			const encoder = new TextEncoder();
			const parts = [];
			const CRLF = "\r\n";
			{
				const __exactForOfIterator65 = _classPrivateFieldGet2(_entries, this)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep65 = __exactForOfIterator65.next();
					if (__exactForOfStep65.done) break;
					const [name, value] = __exactForOfStep65.value;
					parts.push(encoder.encode(`--${boundary}${CRLF}`));
					if (typeof value === "string") parts.push(encoder.encode(`Content-Disposition: form-data; name="${escapeQuotes(name)}"${CRLF}${CRLF}${value}${CRLF}`));
					else {
						const filename = value.name || "blob";
						const contentType = value.type || "application/octet-stream";
						parts.push(encoder.encode(`Content-Disposition: form-data; name="${escapeQuotes(name)}"; filename="${escapeQuotes(filename)}"${CRLF}Content-Type: ${contentType}${CRLF}${CRLF}`));
						parts.push(value._getBytes());
						parts.push(encoder.encode(CRLF));
					}
				}
			}
			parts.push(encoder.encode(`--${boundary}--${CRLF}`));
			const totalLength = parts.reduce((acc, part) => acc + part.byteLength, 0);
			const body = new Uint8Array(totalLength);
			let offset = 0;
			{
				const __exactForOfIterator66 = parts[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep66 = __exactForOfIterator66.next();
					if (__exactForOfStep66.done) break;
					const part = __exactForOfStep66.value;
					body.set(part, offset);
					offset += part.byteLength;
				}
			}
			return {
				body,
				contentType: `multipart/form-data; boundary=${boundary}`,
				boundary
			};
		}
	};
	/**
	* Generate a unique boundary string for multipart encoding.
	*/
	function generateBoundary() {
		return `----ExactFormBoundary${Date.now().toString(36)}${Math.random().toString(36).slice(2, 10)}${Math.random().toString(36).slice(2, 10)}`;
	}
	/**
	* Escape quotes in a string for use in Content-Disposition header.
	*/
	function escapeQuotes(str) {
		return str.replace(/"/g, "\\\"").replace(/\r/g, "%0D").replace(/\n/g, "%0A");
	}
	function isBlobLikeValue(value) {
		return typeof value === "object" && value !== null && typeof value._getBytes === "function";
	}
	/**
	* Normalize a value for storage in FormData.
	* Strings are stored as-is, Blobs are converted to Files.
	*/
	function normalizeValue(value, filename) {
		if (typeof value === "string") return value;
		if (value instanceof File$1 && filename === void 0) return value;
		if (!isBlobLikeValue(value)) throw new TypeError("FormData value must be a string, Blob, or File");
		const name = filename ?? (typeof value.name === "string" ? value.name : "blob");
		const options = {
			type: value.type,
			lastModified: typeof value.lastModified === "number" ? value.lastModified : Date.now()
		};
		return new File$1([value._getBytes()], name, options);
	}
	//#endregion
	//#region ../ibex-runtime-js/src/abort/AbortController.ts
	/**
	* AbortController - Web Standard AbortController Implementation
	*
	* @see https://dom.spec.whatwg.org/#interface-abortcontroller
	*/
	init_defineProperty();
	var AbortController$1 = class {
		constructor() {
			_defineProperty(this, "_signal", void 0);
			this._signal = new AbortSignal();
		}
		get signal() {
			return this._signal;
		}
		abort(reason) {
			this._signal._abort(reason);
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/encoding/TextEncoder.ts
	/**
	* TextEncoder - Web Standard TextEncoder Implementation
	*
	* @see https://encoding.spec.whatwg.org/#textencoder
	*/
	init_defineProperty();
	var TextEncoder$1 = class {
		constructor() {
			_defineProperty(this, "encoding", "utf-8");
		}
		/**
		* Encode a string to UTF-8 bytes
		*
		* @param input String to encode
		* @returns Uint8Array of UTF-8 bytes
		*/
		encode(input) {
			if (input === void 0) return new Uint8Array(0);
			const str = String(input);
			const nativeEncoding = getNativeModule("encoding");
			if (nativeEncoding) return nativeEncoding.encodeUTF8(str);
			const bytes = [];
			for (let i = 0; i < str.length; i++) {
				let codePoint = str.charCodeAt(i);
				if (codePoint >= 55296 && codePoint <= 56319) {
					const high = codePoint;
					const low = str.charCodeAt(++i);
					if (low >= 56320 && low <= 57343) codePoint = (high - 55296) * 1024 + (low - 56320) + 65536;
					else {
						codePoint = 65533;
						i--;
					}
				} else if (codePoint >= 56320 && codePoint <= 57343) codePoint = 65533;
				if (codePoint <= 127) bytes.push(codePoint);
				else if (codePoint <= 2047) bytes.push(192 | codePoint >> 6, 128 | codePoint & 63);
				else if (codePoint <= 65535) bytes.push(224 | codePoint >> 12, 128 | codePoint >> 6 & 63, 128 | codePoint & 63);
				else bytes.push(240 | codePoint >> 18, 128 | codePoint >> 12 & 63, 128 | codePoint >> 6 & 63, 128 | codePoint & 63);
			}
			return new Uint8Array(bytes);
		}
		/**
		* Encode a string into an existing Uint8Array
		*
		* @param source String to encode
		* @param destination Uint8Array to write into
		* @returns Object with read (chars) and written (bytes) counts
		*/
		encodeInto(source, destination) {
			if (!(destination instanceof Uint8Array)) throw new TypeError("encodeInto requires a Uint8Array destination");
			if (destination.buffer.byteLength === 0 && destination.byteLength === 0 && destination.length === 0) return {
				read: 0,
				written: 0
			};
			if (!source) return {
				read: 0,
				written: 0
			};
			let read = 0;
			let written = 0;
			for (let i = 0; i < source.length; i++) {
				let codePoint = source.charCodeAt(i);
				let bytesNeeded;
				let isSurrogatePair = false;
				if (codePoint >= 55296 && codePoint <= 56319) {
					const high = codePoint;
					const low = source.charCodeAt(i + 1);
					if (low >= 56320 && low <= 57343) {
						codePoint = (high - 55296) * 1024 + (low - 56320) + 65536;
						isSurrogatePair = true;
					} else codePoint = 65533;
				} else if (codePoint >= 56320 && codePoint <= 57343) codePoint = 65533;
				if (codePoint <= 127) bytesNeeded = 1;
				else if (codePoint <= 2047) bytesNeeded = 2;
				else if (codePoint <= 65535) bytesNeeded = 3;
				else bytesNeeded = 4;
				if (written + bytesNeeded > destination.length) if (isSurrogatePair && written + 3 <= destination.length) {
					codePoint = 65533;
					bytesNeeded = 3;
					isSurrogatePair = false;
				} else break;
				if (codePoint <= 127) destination[written++] = codePoint;
				else if (codePoint <= 2047) {
					destination[written++] = 192 | codePoint >> 6;
					destination[written++] = 128 | codePoint & 63;
				} else if (codePoint <= 65535) {
					destination[written++] = 224 | codePoint >> 12;
					destination[written++] = 128 | codePoint >> 6 & 63;
					destination[written++] = 128 | codePoint & 63;
				} else {
					destination[written++] = 240 | codePoint >> 18;
					destination[written++] = 128 | codePoint >> 12 & 63;
					destination[written++] = 128 | codePoint >> 6 & 63;
					destination[written++] = 128 | codePoint & 63;
				}
				if (isSurrogatePair) {
					read += 2;
					i++;
				} else read += 1;
			}
			return {
				read,
				written
			};
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/encoding/TextDecoder.ts
	/**
	* TextDecoder - Web Standard TextDecoder Implementation
	*
	* @see https://encoding.spec.whatwg.org/#textdecoder
	*
	* Supported encodings: All WHATWG single-byte encodings, UTF-8, UTF-16LE/BE,
	* replacement, x-user-defined.
	*/
	init_defineProperty();
	const NativeTextDecoderImpl = typeof globalThis.TextDecoder === "function" ? globalThis.TextDecoder : null;
	function createEncodingError(message) {
		const error = new TypeError(message);
		error.code = "ERR_ENCODING_INVALID_ENCODED_DATA";
		return error;
	}
	function isValidUTF8Continuation(startByte, continuationByte, position) {
		if ((continuationByte & 192) !== 128) return false;
		if (position !== 1) return true;
		if ((startByte & 240) === 224) {
			if (startByte === 224 && continuationByte < 160) return false;
			if (startByte === 237 && continuationByte > 159) return false;
		}
		if ((startByte & 248) === 240) {
			if (startByte === 240 && continuationByte < 144) return false;
			if (startByte === 244 && continuationByte > 143) return false;
		}
		return true;
	}
	const encodingLabels = {
		"unicode-1-1-utf-8": "utf-8",
		"unicode11utf8": "utf-8",
		"unicode20utf8": "utf-8",
		"utf-8": "utf-8",
		"utf8": "utf-8",
		"x-unicode20utf8": "utf-8",
		"866": "ibm866",
		"cp866": "ibm866",
		"csibm866": "ibm866",
		"ibm866": "ibm866",
		"csisolatin2": "iso-8859-2",
		"iso-8859-2": "iso-8859-2",
		"iso-ir-101": "iso-8859-2",
		"iso8859-2": "iso-8859-2",
		"iso88592": "iso-8859-2",
		"iso_8859-2": "iso-8859-2",
		"iso_8859-2:1987": "iso-8859-2",
		"l2": "iso-8859-2",
		"latin2": "iso-8859-2",
		"csisolatin3": "iso-8859-3",
		"iso-8859-3": "iso-8859-3",
		"iso-ir-109": "iso-8859-3",
		"iso8859-3": "iso-8859-3",
		"iso88593": "iso-8859-3",
		"iso_8859-3": "iso-8859-3",
		"iso_8859-3:1988": "iso-8859-3",
		"l3": "iso-8859-3",
		"latin3": "iso-8859-3",
		"csisolatin4": "iso-8859-4",
		"iso-8859-4": "iso-8859-4",
		"iso-ir-110": "iso-8859-4",
		"iso8859-4": "iso-8859-4",
		"iso88594": "iso-8859-4",
		"iso_8859-4": "iso-8859-4",
		"iso_8859-4:1988": "iso-8859-4",
		"l4": "iso-8859-4",
		"latin4": "iso-8859-4",
		"csisolatincyrillic": "iso-8859-5",
		"cyrillic": "iso-8859-5",
		"iso-8859-5": "iso-8859-5",
		"iso-ir-144": "iso-8859-5",
		"iso8859-5": "iso-8859-5",
		"iso88595": "iso-8859-5",
		"iso_8859-5": "iso-8859-5",
		"iso_8859-5:1988": "iso-8859-5",
		"arabic": "iso-8859-6",
		"asmo-708": "iso-8859-6",
		"csiso88596e": "iso-8859-6",
		"csiso88596i": "iso-8859-6",
		"csisolatinarabic": "iso-8859-6",
		"ecma-114": "iso-8859-6",
		"iso-8859-6": "iso-8859-6",
		"iso-8859-6-e": "iso-8859-6",
		"iso-8859-6-i": "iso-8859-6",
		"iso-ir-127": "iso-8859-6",
		"iso8859-6": "iso-8859-6",
		"iso88596": "iso-8859-6",
		"iso_8859-6": "iso-8859-6",
		"iso_8859-6:1987": "iso-8859-6",
		"csisolatingreek": "iso-8859-7",
		"ecma-118": "iso-8859-7",
		"elot_928": "iso-8859-7",
		"greek": "iso-8859-7",
		"greek8": "iso-8859-7",
		"iso-8859-7": "iso-8859-7",
		"iso-ir-126": "iso-8859-7",
		"iso8859-7": "iso-8859-7",
		"iso88597": "iso-8859-7",
		"iso_8859-7": "iso-8859-7",
		"iso_8859-7:1987": "iso-8859-7",
		"sun_eu_greek": "iso-8859-7",
		"csiso88598e": "iso-8859-8",
		"csisolatinhebrew": "iso-8859-8",
		"hebrew": "iso-8859-8",
		"iso-8859-8": "iso-8859-8",
		"iso-8859-8-e": "iso-8859-8",
		"iso-ir-138": "iso-8859-8",
		"iso8859-8": "iso-8859-8",
		"iso88598": "iso-8859-8",
		"iso_8859-8": "iso-8859-8",
		"iso_8859-8:1988": "iso-8859-8",
		"visual": "iso-8859-8",
		"csiso88598i": "iso-8859-8-i",
		"iso-8859-8-i": "iso-8859-8-i",
		"logical": "iso-8859-8-i",
		"csisolatin6": "iso-8859-10",
		"iso-8859-10": "iso-8859-10",
		"iso-ir-157": "iso-8859-10",
		"iso8859-10": "iso-8859-10",
		"iso885910": "iso-8859-10",
		"l6": "iso-8859-10",
		"latin6": "iso-8859-10",
		"iso-8859-13": "iso-8859-13",
		"iso8859-13": "iso-8859-13",
		"iso885913": "iso-8859-13",
		"iso-8859-14": "iso-8859-14",
		"iso8859-14": "iso-8859-14",
		"iso885914": "iso-8859-14",
		"csisolatin9": "iso-8859-15",
		"iso-8859-15": "iso-8859-15",
		"iso8859-15": "iso-8859-15",
		"iso885915": "iso-8859-15",
		"iso_8859-15": "iso-8859-15",
		"l9": "iso-8859-15",
		"iso-8859-16": "iso-8859-16",
		"cskoi8r": "koi8-r",
		"koi": "koi8-r",
		"koi8": "koi8-r",
		"koi8-r": "koi8-r",
		"koi8_r": "koi8-r",
		"koi8-ru": "koi8-u",
		"koi8-u": "koi8-u",
		"csmacintosh": "macintosh",
		"mac": "macintosh",
		"macintosh": "macintosh",
		"x-mac-roman": "macintosh",
		"dos-874": "windows-874",
		"iso-8859-11": "windows-874",
		"iso8859-11": "windows-874",
		"iso885911": "windows-874",
		"tis-620": "windows-874",
		"windows-874": "windows-874",
		"cp1250": "windows-1250",
		"windows-1250": "windows-1250",
		"x-cp1250": "windows-1250",
		"cp1251": "windows-1251",
		"windows-1251": "windows-1251",
		"x-cp1251": "windows-1251",
		"ansi_x3.4-1968": "windows-1252",
		"ascii": "windows-1252",
		"cp1252": "windows-1252",
		"cp819": "windows-1252",
		"csisolatin1": "windows-1252",
		"ibm819": "windows-1252",
		"iso-8859-1": "windows-1252",
		"iso-ir-100": "windows-1252",
		"iso8859-1": "windows-1252",
		"iso88591": "windows-1252",
		"iso_8859-1": "windows-1252",
		"iso_8859-1:1987": "windows-1252",
		"l1": "windows-1252",
		"latin1": "windows-1252",
		"us-ascii": "windows-1252",
		"windows-1252": "windows-1252",
		"x-cp1252": "windows-1252",
		"cp1253": "windows-1253",
		"windows-1253": "windows-1253",
		"x-cp1253": "windows-1253",
		"cp1254": "windows-1254",
		"csisolatin5": "windows-1254",
		"iso-8859-9": "windows-1254",
		"iso-ir-148": "windows-1254",
		"iso8859-9": "windows-1254",
		"iso88599": "windows-1254",
		"iso_8859-9": "windows-1254",
		"iso_8859-9:1989": "windows-1254",
		"l5": "windows-1254",
		"latin5": "windows-1254",
		"windows-1254": "windows-1254",
		"x-cp1254": "windows-1254",
		"cp1255": "windows-1255",
		"windows-1255": "windows-1255",
		"x-cp1255": "windows-1255",
		"cp1256": "windows-1256",
		"windows-1256": "windows-1256",
		"x-cp1256": "windows-1256",
		"cp1257": "windows-1257",
		"windows-1257": "windows-1257",
		"x-cp1257": "windows-1257",
		"cp1258": "windows-1258",
		"windows-1258": "windows-1258",
		"x-cp1258": "windows-1258",
		"x-mac-cyrillic": "x-mac-cyrillic",
		"x-mac-ukrainian": "x-mac-cyrillic",
		"chinese": "gbk",
		"csgb2312": "gbk",
		"csiso58gb231280": "gbk",
		"gb2312": "gbk",
		"gb_2312": "gbk",
		"gb_2312-80": "gbk",
		"gbk": "gbk",
		"iso-ir-58": "gbk",
		"x-gbk": "gbk",
		"gb18030": "gb18030",
		"big5": "big5",
		"big5-hkscs": "big5",
		"cn-big5": "big5",
		"csbig5": "big5",
		"x-x-big5": "big5",
		"cseucpkdfmtjapanese": "euc-jp",
		"euc-jp": "euc-jp",
		"x-euc-jp": "euc-jp",
		"csiso2022jp": "iso-2022-jp",
		"iso-2022-jp": "iso-2022-jp",
		"csshiftjis": "shift_jis",
		"ms932": "shift_jis",
		"ms_kanji": "shift_jis",
		"shift-jis": "shift_jis",
		"shift_jis": "shift_jis",
		"sjis": "shift_jis",
		"windows-31j": "shift_jis",
		"x-sjis": "shift_jis",
		"cseuckr": "euc-kr",
		"csksc56011987": "euc-kr",
		"euc-kr": "euc-kr",
		"iso-ir-149": "euc-kr",
		"korean": "euc-kr",
		"ks_c_5601-1987": "euc-kr",
		"ks_c_5601-1989": "euc-kr",
		"ksc5601": "euc-kr",
		"ksc_5601": "euc-kr",
		"windows-949": "euc-kr",
		"csiso2022kr": "replacement",
		"hz-gb-2312": "replacement",
		"iso-2022-cn": "replacement",
		"iso-2022-cn-ext": "replacement",
		"iso-2022-kr": "replacement",
		"replacement": "replacement",
		"unicodefffe": "utf-16be",
		"utf-16be": "utf-16be",
		"csunicode": "utf-16le",
		"iso-10646-ucs-2": "utf-16le",
		"ucs-2": "utf-16le",
		"unicode": "utf-16le",
		"unicodefeff": "utf-16le",
		"utf-16": "utf-16le",
		"utf-16le": "utf-16le",
		"x-user-defined": "x-user-defined"
	};
	const WINDOWS_1252 = [
		8364,
		null,
		8218,
		402,
		8222,
		8230,
		8224,
		8225,
		710,
		8240,
		352,
		8249,
		338,
		null,
		381,
		null,
		null,
		8216,
		8217,
		8220,
		8221,
		8226,
		8211,
		8212,
		732,
		8482,
		353,
		8250,
		339,
		null,
		382,
		376
	];
	const IBM866 = [
		1040,
		1041,
		1042,
		1043,
		1044,
		1045,
		1046,
		1047,
		1048,
		1049,
		1050,
		1051,
		1052,
		1053,
		1054,
		1055,
		1056,
		1057,
		1058,
		1059,
		1060,
		1061,
		1062,
		1063,
		1064,
		1065,
		1066,
		1067,
		1068,
		1069,
		1070,
		1071,
		1072,
		1073,
		1074,
		1075,
		1076,
		1077,
		1078,
		1079,
		1080,
		1081,
		1082,
		1083,
		1084,
		1085,
		1086,
		1087,
		9617,
		9618,
		9619,
		9474,
		9508,
		9569,
		9570,
		9558,
		9557,
		9571,
		9553,
		9559,
		9565,
		9564,
		9563,
		9488,
		9492,
		9524,
		9516,
		9500,
		9472,
		9532,
		9566,
		9567,
		9562,
		9556,
		9577,
		9574,
		9568,
		9552,
		9580,
		9575,
		9576,
		9572,
		9573,
		9561,
		9560,
		9554,
		9555,
		9579,
		9578,
		9496,
		9484,
		9608,
		9604,
		9612,
		9616,
		9600,
		1088,
		1089,
		1090,
		1091,
		1092,
		1093,
		1094,
		1095,
		1096,
		1097,
		1098,
		1099,
		1100,
		1101,
		1102,
		1103,
		1025,
		1105,
		1028,
		1108,
		1031,
		1111,
		1038,
		1118,
		176,
		8729,
		183,
		8730,
		8470,
		164,
		9632,
		160
	];
	const ISO_8859_2 = [
		128,
		129,
		130,
		131,
		132,
		133,
		134,
		135,
		136,
		137,
		138,
		139,
		140,
		141,
		142,
		143,
		144,
		145,
		146,
		147,
		148,
		149,
		150,
		151,
		152,
		153,
		154,
		155,
		156,
		157,
		158,
		159,
		160,
		260,
		728,
		321,
		164,
		317,
		346,
		167,
		168,
		352,
		350,
		356,
		377,
		173,
		381,
		379,
		176,
		261,
		731,
		322,
		180,
		318,
		347,
		711,
		184,
		353,
		351,
		357,
		378,
		733,
		382,
		380,
		340,
		193,
		194,
		258,
		196,
		313,
		262,
		199,
		268,
		201,
		280,
		203,
		282,
		205,
		206,
		270,
		272,
		323,
		327,
		211,
		212,
		336,
		214,
		215,
		344,
		366,
		218,
		368,
		220,
		221,
		354,
		223,
		341,
		225,
		226,
		259,
		228,
		314,
		263,
		231,
		269,
		233,
		281,
		235,
		283,
		237,
		238,
		271,
		273,
		324,
		328,
		243,
		244,
		337,
		246,
		247,
		345,
		367,
		250,
		369,
		252,
		253,
		355,
		729
	];
	const ISO_8859_3 = [
		128,
		129,
		130,
		131,
		132,
		133,
		134,
		135,
		136,
		137,
		138,
		139,
		140,
		141,
		142,
		143,
		144,
		145,
		146,
		147,
		148,
		149,
		150,
		151,
		152,
		153,
		154,
		155,
		156,
		157,
		158,
		159,
		160,
		294,
		728,
		163,
		164,
		null,
		292,
		167,
		168,
		304,
		350,
		286,
		308,
		173,
		null,
		379,
		176,
		295,
		178,
		179,
		180,
		181,
		293,
		183,
		184,
		305,
		351,
		287,
		309,
		189,
		null,
		380,
		192,
		193,
		194,
		null,
		196,
		266,
		264,
		199,
		200,
		201,
		202,
		203,
		204,
		205,
		206,
		207,
		null,
		209,
		210,
		211,
		212,
		288,
		214,
		215,
		284,
		217,
		218,
		219,
		220,
		364,
		348,
		223,
		224,
		225,
		226,
		null,
		228,
		267,
		265,
		231,
		232,
		233,
		234,
		235,
		236,
		237,
		238,
		239,
		null,
		241,
		242,
		243,
		244,
		289,
		246,
		247,
		285,
		249,
		250,
		251,
		252,
		365,
		349,
		729
	];
	const ISO_8859_4 = [
		128,
		129,
		130,
		131,
		132,
		133,
		134,
		135,
		136,
		137,
		138,
		139,
		140,
		141,
		142,
		143,
		144,
		145,
		146,
		147,
		148,
		149,
		150,
		151,
		152,
		153,
		154,
		155,
		156,
		157,
		158,
		159,
		160,
		260,
		312,
		342,
		164,
		296,
		315,
		167,
		168,
		352,
		274,
		290,
		358,
		173,
		381,
		175,
		176,
		261,
		731,
		343,
		180,
		297,
		316,
		711,
		184,
		353,
		275,
		291,
		359,
		330,
		382,
		331,
		256,
		193,
		194,
		195,
		196,
		197,
		198,
		302,
		268,
		201,
		280,
		203,
		278,
		205,
		206,
		298,
		272,
		325,
		332,
		310,
		212,
		213,
		214,
		215,
		216,
		370,
		218,
		219,
		220,
		360,
		362,
		223,
		257,
		225,
		226,
		227,
		228,
		229,
		230,
		303,
		269,
		233,
		281,
		235,
		279,
		237,
		238,
		299,
		273,
		326,
		333,
		311,
		244,
		245,
		246,
		247,
		248,
		371,
		250,
		251,
		252,
		361,
		363,
		729
	];
	const ISO_8859_5 = [
		128,
		129,
		130,
		131,
		132,
		133,
		134,
		135,
		136,
		137,
		138,
		139,
		140,
		141,
		142,
		143,
		144,
		145,
		146,
		147,
		148,
		149,
		150,
		151,
		152,
		153,
		154,
		155,
		156,
		157,
		158,
		159,
		160,
		1025,
		1026,
		1027,
		1028,
		1029,
		1030,
		1031,
		1032,
		1033,
		1034,
		1035,
		1036,
		173,
		1038,
		1039,
		1040,
		1041,
		1042,
		1043,
		1044,
		1045,
		1046,
		1047,
		1048,
		1049,
		1050,
		1051,
		1052,
		1053,
		1054,
		1055,
		1056,
		1057,
		1058,
		1059,
		1060,
		1061,
		1062,
		1063,
		1064,
		1065,
		1066,
		1067,
		1068,
		1069,
		1070,
		1071,
		1072,
		1073,
		1074,
		1075,
		1076,
		1077,
		1078,
		1079,
		1080,
		1081,
		1082,
		1083,
		1084,
		1085,
		1086,
		1087,
		1088,
		1089,
		1090,
		1091,
		1092,
		1093,
		1094,
		1095,
		1096,
		1097,
		1098,
		1099,
		1100,
		1101,
		1102,
		1103,
		8470,
		1105,
		1106,
		1107,
		1108,
		1109,
		1110,
		1111,
		1112,
		1113,
		1114,
		1115,
		1116,
		167,
		1118,
		1119
	];
	const ISO_8859_6 = [
		128,
		129,
		130,
		131,
		132,
		133,
		134,
		135,
		136,
		137,
		138,
		139,
		140,
		141,
		142,
		143,
		144,
		145,
		146,
		147,
		148,
		149,
		150,
		151,
		152,
		153,
		154,
		155,
		156,
		157,
		158,
		159,
		160,
		null,
		null,
		null,
		164,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		1548,
		173,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		1563,
		null,
		null,
		null,
		1567,
		null,
		1569,
		1570,
		1571,
		1572,
		1573,
		1574,
		1575,
		1576,
		1577,
		1578,
		1579,
		1580,
		1581,
		1582,
		1583,
		1584,
		1585,
		1586,
		1587,
		1588,
		1589,
		1590,
		1591,
		1592,
		1593,
		1594,
		null,
		null,
		null,
		null,
		null,
		1600,
		1601,
		1602,
		1603,
		1604,
		1605,
		1606,
		1607,
		1608,
		1609,
		1610,
		1611,
		1612,
		1613,
		1614,
		1615,
		1616,
		1617,
		1618,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null
	];
	const ISO_8859_7 = [
		128,
		129,
		130,
		131,
		132,
		133,
		134,
		135,
		136,
		137,
		138,
		139,
		140,
		141,
		142,
		143,
		144,
		145,
		146,
		147,
		148,
		149,
		150,
		151,
		152,
		153,
		154,
		155,
		156,
		157,
		158,
		159,
		160,
		8216,
		8217,
		163,
		8364,
		8367,
		166,
		167,
		168,
		169,
		890,
		171,
		172,
		173,
		null,
		8213,
		176,
		177,
		178,
		179,
		900,
		901,
		902,
		183,
		904,
		905,
		906,
		187,
		908,
		189,
		910,
		911,
		912,
		913,
		914,
		915,
		916,
		917,
		918,
		919,
		920,
		921,
		922,
		923,
		924,
		925,
		926,
		927,
		928,
		929,
		null,
		931,
		932,
		933,
		934,
		935,
		936,
		937,
		938,
		939,
		940,
		941,
		942,
		943,
		944,
		945,
		946,
		947,
		948,
		949,
		950,
		951,
		952,
		953,
		954,
		955,
		956,
		957,
		958,
		959,
		960,
		961,
		962,
		963,
		964,
		965,
		966,
		967,
		968,
		969,
		970,
		971,
		972,
		973,
		974,
		null
	];
	const ISO_8859_8 = [
		128,
		129,
		130,
		131,
		132,
		133,
		134,
		135,
		136,
		137,
		138,
		139,
		140,
		141,
		142,
		143,
		144,
		145,
		146,
		147,
		148,
		149,
		150,
		151,
		152,
		153,
		154,
		155,
		156,
		157,
		158,
		159,
		160,
		null,
		162,
		163,
		164,
		165,
		166,
		167,
		168,
		169,
		215,
		171,
		172,
		173,
		174,
		175,
		176,
		177,
		178,
		179,
		180,
		181,
		182,
		183,
		184,
		185,
		247,
		187,
		188,
		189,
		190,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		null,
		1488,
		1489,
		1490,
		1491,
		1492,
		1493,
		1494,
		1495,
		1496,
		1497,
		1498,
		1499,
		1500,
		1501,
		1502,
		1503,
		1504,
		1505,
		1506,
		1507,
		1508,
		1509,
		1510,
		1511,
		1512,
		1513,
		1514,
		null,
		null,
		8206,
		8207,
		null
	];
	const SINGLE_BYTE_TABLES = {
		"ibm866": IBM866,
		"iso-8859-2": ISO_8859_2,
		"iso-8859-3": ISO_8859_3,
		"iso-8859-4": ISO_8859_4,
		"iso-8859-5": ISO_8859_5,
		"iso-8859-6": ISO_8859_6,
		"iso-8859-7": ISO_8859_7,
		"iso-8859-8": ISO_8859_8,
		"iso-8859-8-i": ISO_8859_8,
		"iso-8859-10": [
			128,
			129,
			130,
			131,
			132,
			133,
			134,
			135,
			136,
			137,
			138,
			139,
			140,
			141,
			142,
			143,
			144,
			145,
			146,
			147,
			148,
			149,
			150,
			151,
			152,
			153,
			154,
			155,
			156,
			157,
			158,
			159,
			160,
			260,
			274,
			290,
			298,
			296,
			310,
			167,
			315,
			272,
			352,
			358,
			381,
			173,
			362,
			330,
			176,
			261,
			275,
			291,
			299,
			297,
			311,
			183,
			316,
			273,
			353,
			359,
			382,
			8213,
			363,
			331,
			256,
			193,
			194,
			195,
			196,
			197,
			198,
			302,
			268,
			201,
			280,
			203,
			278,
			205,
			206,
			207,
			208,
			325,
			332,
			211,
			212,
			213,
			214,
			360,
			216,
			370,
			218,
			219,
			220,
			221,
			222,
			223,
			257,
			225,
			226,
			227,
			228,
			229,
			230,
			303,
			269,
			233,
			281,
			235,
			279,
			237,
			238,
			239,
			240,
			326,
			333,
			243,
			244,
			245,
			246,
			361,
			248,
			371,
			250,
			251,
			252,
			253,
			254,
			312
		],
		"iso-8859-13": [
			128,
			129,
			130,
			131,
			132,
			133,
			134,
			135,
			136,
			137,
			138,
			139,
			140,
			141,
			142,
			143,
			144,
			145,
			146,
			147,
			148,
			149,
			150,
			151,
			152,
			153,
			154,
			155,
			156,
			157,
			158,
			159,
			160,
			8221,
			162,
			163,
			164,
			8222,
			166,
			167,
			216,
			169,
			342,
			171,
			172,
			173,
			174,
			198,
			176,
			177,
			178,
			179,
			8220,
			181,
			182,
			183,
			248,
			185,
			343,
			187,
			188,
			189,
			190,
			230,
			260,
			302,
			256,
			262,
			196,
			197,
			280,
			274,
			268,
			201,
			377,
			278,
			290,
			310,
			298,
			315,
			352,
			323,
			325,
			211,
			332,
			213,
			214,
			215,
			370,
			321,
			346,
			362,
			220,
			379,
			381,
			223,
			261,
			303,
			257,
			263,
			228,
			229,
			281,
			275,
			269,
			233,
			378,
			279,
			291,
			311,
			299,
			316,
			353,
			324,
			326,
			243,
			333,
			245,
			246,
			247,
			371,
			322,
			347,
			363,
			252,
			380,
			382,
			8217
		],
		"iso-8859-14": [
			128,
			129,
			130,
			131,
			132,
			133,
			134,
			135,
			136,
			137,
			138,
			139,
			140,
			141,
			142,
			143,
			144,
			145,
			146,
			147,
			148,
			149,
			150,
			151,
			152,
			153,
			154,
			155,
			156,
			157,
			158,
			159,
			160,
			7682,
			7683,
			163,
			266,
			267,
			7690,
			167,
			7808,
			169,
			7810,
			7691,
			7922,
			173,
			174,
			376,
			7710,
			7711,
			288,
			289,
			7744,
			7745,
			182,
			7766,
			7809,
			7767,
			7811,
			7776,
			7923,
			7812,
			7813,
			7777,
			192,
			193,
			194,
			195,
			196,
			197,
			198,
			199,
			200,
			201,
			202,
			203,
			204,
			205,
			206,
			207,
			372,
			209,
			210,
			211,
			212,
			213,
			214,
			7786,
			216,
			217,
			218,
			219,
			220,
			221,
			374,
			223,
			224,
			225,
			226,
			227,
			228,
			229,
			230,
			231,
			232,
			233,
			234,
			235,
			236,
			237,
			238,
			239,
			373,
			241,
			242,
			243,
			244,
			245,
			246,
			7787,
			248,
			249,
			250,
			251,
			252,
			253,
			375,
			255
		],
		"iso-8859-15": [
			128,
			129,
			130,
			131,
			132,
			133,
			134,
			135,
			136,
			137,
			138,
			139,
			140,
			141,
			142,
			143,
			144,
			145,
			146,
			147,
			148,
			149,
			150,
			151,
			152,
			153,
			154,
			155,
			156,
			157,
			158,
			159,
			160,
			161,
			162,
			163,
			8364,
			165,
			352,
			167,
			353,
			169,
			170,
			171,
			172,
			173,
			174,
			175,
			176,
			177,
			178,
			179,
			381,
			181,
			182,
			183,
			382,
			185,
			186,
			187,
			338,
			339,
			376,
			191,
			192,
			193,
			194,
			195,
			196,
			197,
			198,
			199,
			200,
			201,
			202,
			203,
			204,
			205,
			206,
			207,
			208,
			209,
			210,
			211,
			212,
			213,
			214,
			215,
			216,
			217,
			218,
			219,
			220,
			221,
			222,
			223,
			224,
			225,
			226,
			227,
			228,
			229,
			230,
			231,
			232,
			233,
			234,
			235,
			236,
			237,
			238,
			239,
			240,
			241,
			242,
			243,
			244,
			245,
			246,
			247,
			248,
			249,
			250,
			251,
			252,
			253,
			254,
			255
		],
		"iso-8859-16": [
			128,
			129,
			130,
			131,
			132,
			133,
			134,
			135,
			136,
			137,
			138,
			139,
			140,
			141,
			142,
			143,
			144,
			145,
			146,
			147,
			148,
			149,
			150,
			151,
			152,
			153,
			154,
			155,
			156,
			157,
			158,
			159,
			160,
			260,
			261,
			321,
			8364,
			8222,
			352,
			167,
			353,
			169,
			536,
			171,
			377,
			173,
			378,
			379,
			176,
			177,
			268,
			322,
			381,
			8221,
			182,
			183,
			382,
			269,
			537,
			187,
			338,
			339,
			376,
			380,
			192,
			193,
			194,
			258,
			196,
			262,
			198,
			199,
			200,
			201,
			202,
			203,
			204,
			205,
			206,
			207,
			272,
			323,
			210,
			211,
			212,
			336,
			214,
			346,
			368,
			217,
			218,
			219,
			220,
			280,
			538,
			223,
			224,
			225,
			226,
			259,
			228,
			263,
			230,
			231,
			232,
			233,
			234,
			235,
			236,
			237,
			238,
			239,
			273,
			324,
			242,
			243,
			244,
			337,
			246,
			347,
			369,
			249,
			250,
			251,
			252,
			281,
			539,
			255
		],
		"koi8-r": [
			9472,
			9474,
			9484,
			9488,
			9492,
			9496,
			9500,
			9508,
			9516,
			9524,
			9532,
			9600,
			9604,
			9608,
			9612,
			9616,
			9617,
			9618,
			9619,
			8992,
			9632,
			8729,
			8730,
			8776,
			8804,
			8805,
			160,
			8993,
			176,
			178,
			183,
			247,
			9552,
			9553,
			9554,
			1105,
			9555,
			9556,
			9557,
			9558,
			9559,
			9560,
			9561,
			9562,
			9563,
			9564,
			9565,
			9566,
			9567,
			9568,
			9569,
			1025,
			9570,
			9571,
			9572,
			9573,
			9574,
			9575,
			9576,
			9577,
			9578,
			9579,
			9580,
			169,
			1102,
			1072,
			1073,
			1094,
			1076,
			1077,
			1092,
			1075,
			1093,
			1080,
			1081,
			1082,
			1083,
			1084,
			1085,
			1086,
			1087,
			1103,
			1088,
			1089,
			1090,
			1091,
			1078,
			1074,
			1100,
			1099,
			1079,
			1096,
			1101,
			1097,
			1095,
			1098,
			1070,
			1040,
			1041,
			1062,
			1044,
			1045,
			1060,
			1043,
			1061,
			1048,
			1049,
			1050,
			1051,
			1052,
			1053,
			1054,
			1055,
			1071,
			1056,
			1057,
			1058,
			1059,
			1046,
			1042,
			1068,
			1067,
			1047,
			1064,
			1069,
			1065,
			1063,
			1066
		],
		"koi8-u": [
			9472,
			9474,
			9484,
			9488,
			9492,
			9496,
			9500,
			9508,
			9516,
			9524,
			9532,
			9600,
			9604,
			9608,
			9612,
			9616,
			9617,
			9618,
			9619,
			8992,
			9632,
			8729,
			8730,
			8776,
			8804,
			8805,
			160,
			8993,
			176,
			178,
			183,
			247,
			9552,
			9553,
			9554,
			1105,
			1108,
			9556,
			1110,
			1111,
			9559,
			9560,
			9561,
			9562,
			9563,
			1169,
			9565,
			9566,
			9567,
			9568,
			9569,
			1025,
			1028,
			9571,
			1030,
			1031,
			9574,
			9575,
			9576,
			9577,
			9578,
			1168,
			9580,
			169,
			1102,
			1072,
			1073,
			1094,
			1076,
			1077,
			1092,
			1075,
			1093,
			1080,
			1081,
			1082,
			1083,
			1084,
			1085,
			1086,
			1087,
			1103,
			1088,
			1089,
			1090,
			1091,
			1078,
			1074,
			1100,
			1099,
			1079,
			1096,
			1101,
			1097,
			1095,
			1098,
			1070,
			1040,
			1041,
			1062,
			1044,
			1045,
			1060,
			1043,
			1061,
			1048,
			1049,
			1050,
			1051,
			1052,
			1053,
			1054,
			1055,
			1071,
			1056,
			1057,
			1058,
			1059,
			1046,
			1042,
			1068,
			1067,
			1047,
			1064,
			1069,
			1065,
			1063,
			1066
		],
		"macintosh": [
			196,
			197,
			199,
			201,
			209,
			214,
			220,
			225,
			224,
			226,
			228,
			227,
			229,
			231,
			233,
			232,
			234,
			235,
			237,
			236,
			238,
			239,
			241,
			243,
			242,
			244,
			246,
			245,
			250,
			249,
			251,
			252,
			8224,
			176,
			162,
			163,
			167,
			8226,
			182,
			223,
			174,
			169,
			8482,
			180,
			168,
			8800,
			198,
			216,
			8734,
			177,
			8804,
			8805,
			165,
			181,
			8706,
			8721,
			8719,
			960,
			8747,
			170,
			186,
			8486,
			230,
			248,
			191,
			161,
			172,
			8730,
			402,
			8776,
			8710,
			171,
			187,
			8230,
			160,
			192,
			195,
			213,
			338,
			339,
			8211,
			8212,
			8220,
			8221,
			8216,
			8217,
			247,
			9674,
			255,
			376,
			8260,
			8364,
			8249,
			8250,
			64257,
			64258,
			8225,
			183,
			8218,
			8222,
			8240,
			194,
			202,
			193,
			203,
			200,
			205,
			206,
			207,
			204,
			211,
			212,
			63743,
			210,
			218,
			219,
			217,
			305,
			710,
			732,
			175,
			728,
			729,
			730,
			184,
			733,
			731,
			711
		],
		"windows-874": [
			8364,
			129,
			130,
			131,
			132,
			8230,
			134,
			135,
			136,
			137,
			138,
			139,
			140,
			141,
			142,
			143,
			144,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			152,
			153,
			154,
			155,
			156,
			157,
			158,
			159,
			160,
			3585,
			3586,
			3587,
			3588,
			3589,
			3590,
			3591,
			3592,
			3593,
			3594,
			3595,
			3596,
			3597,
			3598,
			3599,
			3600,
			3601,
			3602,
			3603,
			3604,
			3605,
			3606,
			3607,
			3608,
			3609,
			3610,
			3611,
			3612,
			3613,
			3614,
			3615,
			3616,
			3617,
			3618,
			3619,
			3620,
			3621,
			3622,
			3623,
			3624,
			3625,
			3626,
			3627,
			3628,
			3629,
			3630,
			3631,
			3632,
			3633,
			3634,
			3635,
			3636,
			3637,
			3638,
			3639,
			3640,
			3641,
			3642,
			null,
			null,
			null,
			null,
			3647,
			3648,
			3649,
			3650,
			3651,
			3652,
			3653,
			3654,
			3655,
			3656,
			3657,
			3658,
			3659,
			3660,
			3661,
			3662,
			3663,
			3664,
			3665,
			3666,
			3667,
			3668,
			3669,
			3670,
			3671,
			3672,
			3673,
			3674,
			3675,
			null,
			null,
			null,
			null
		],
		"windows-1250": [
			8364,
			null,
			8218,
			null,
			8222,
			8230,
			8224,
			8225,
			null,
			8240,
			352,
			8249,
			346,
			356,
			381,
			377,
			null,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			null,
			8482,
			353,
			8250,
			347,
			357,
			382,
			378,
			160,
			711,
			728,
			321,
			164,
			260,
			166,
			167,
			168,
			169,
			350,
			171,
			172,
			173,
			174,
			379,
			176,
			177,
			731,
			322,
			180,
			181,
			182,
			183,
			184,
			261,
			351,
			187,
			317,
			733,
			318,
			380,
			340,
			193,
			194,
			258,
			196,
			313,
			262,
			199,
			268,
			201,
			280,
			203,
			282,
			205,
			206,
			270,
			272,
			323,
			327,
			211,
			212,
			336,
			214,
			215,
			344,
			366,
			218,
			368,
			220,
			221,
			354,
			223,
			341,
			225,
			226,
			259,
			228,
			314,
			263,
			231,
			269,
			233,
			281,
			235,
			283,
			237,
			238,
			271,
			273,
			324,
			328,
			243,
			244,
			337,
			246,
			247,
			345,
			367,
			250,
			369,
			252,
			253,
			355,
			729
		],
		"windows-1251": [
			1026,
			1027,
			8218,
			1107,
			8222,
			8230,
			8224,
			8225,
			8364,
			8240,
			1033,
			8249,
			1034,
			1036,
			1035,
			1039,
			1106,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			null,
			8482,
			1113,
			8250,
			1114,
			1116,
			1115,
			1119,
			160,
			1038,
			1118,
			1032,
			164,
			1168,
			166,
			167,
			1025,
			169,
			1028,
			171,
			172,
			173,
			174,
			1031,
			176,
			177,
			1030,
			1110,
			1169,
			181,
			182,
			183,
			1105,
			8470,
			1108,
			187,
			1112,
			1029,
			1109,
			1111,
			1040,
			1041,
			1042,
			1043,
			1044,
			1045,
			1046,
			1047,
			1048,
			1049,
			1050,
			1051,
			1052,
			1053,
			1054,
			1055,
			1056,
			1057,
			1058,
			1059,
			1060,
			1061,
			1062,
			1063,
			1064,
			1065,
			1066,
			1067,
			1068,
			1069,
			1070,
			1071,
			1072,
			1073,
			1074,
			1075,
			1076,
			1077,
			1078,
			1079,
			1080,
			1081,
			1082,
			1083,
			1084,
			1085,
			1086,
			1087,
			1088,
			1089,
			1090,
			1091,
			1092,
			1093,
			1094,
			1095,
			1096,
			1097,
			1098,
			1099,
			1100,
			1101,
			1102,
			1103
		],
		"windows-1253": [
			8364,
			null,
			8218,
			402,
			8222,
			8230,
			8224,
			8225,
			null,
			8240,
			null,
			8249,
			null,
			null,
			null,
			null,
			null,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			null,
			8482,
			null,
			8250,
			null,
			null,
			null,
			null,
			160,
			901,
			902,
			163,
			164,
			165,
			166,
			167,
			168,
			169,
			null,
			171,
			172,
			173,
			174,
			8213,
			176,
			177,
			178,
			179,
			900,
			181,
			182,
			183,
			904,
			905,
			906,
			187,
			908,
			189,
			910,
			911,
			912,
			913,
			914,
			915,
			916,
			917,
			918,
			919,
			920,
			921,
			922,
			923,
			924,
			925,
			926,
			927,
			928,
			929,
			null,
			931,
			932,
			933,
			934,
			935,
			936,
			937,
			938,
			939,
			940,
			941,
			942,
			943,
			944,
			945,
			946,
			947,
			948,
			949,
			950,
			951,
			952,
			953,
			954,
			955,
			956,
			957,
			958,
			959,
			960,
			961,
			962,
			963,
			964,
			965,
			966,
			967,
			968,
			969,
			970,
			971,
			972,
			973,
			974,
			null
		],
		"windows-1254": [
			8364,
			null,
			8218,
			402,
			8222,
			8230,
			8224,
			8225,
			710,
			8240,
			352,
			8249,
			338,
			null,
			null,
			null,
			null,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			732,
			8482,
			353,
			8250,
			339,
			null,
			null,
			376,
			160,
			161,
			162,
			163,
			164,
			165,
			166,
			167,
			168,
			169,
			170,
			171,
			172,
			173,
			174,
			175,
			176,
			177,
			178,
			179,
			180,
			181,
			182,
			183,
			184,
			185,
			186,
			187,
			188,
			189,
			190,
			191,
			192,
			193,
			194,
			195,
			196,
			197,
			198,
			199,
			200,
			201,
			202,
			203,
			204,
			205,
			206,
			207,
			286,
			209,
			210,
			211,
			212,
			213,
			214,
			215,
			216,
			217,
			218,
			219,
			220,
			304,
			350,
			223,
			224,
			225,
			226,
			227,
			228,
			229,
			230,
			231,
			232,
			233,
			234,
			235,
			236,
			237,
			238,
			239,
			287,
			241,
			242,
			243,
			244,
			245,
			246,
			247,
			248,
			249,
			250,
			251,
			252,
			305,
			351,
			255
		],
		"windows-1255": [
			8364,
			null,
			8218,
			402,
			8222,
			8230,
			8224,
			8225,
			710,
			8240,
			null,
			8249,
			null,
			null,
			null,
			null,
			null,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			732,
			8482,
			null,
			8250,
			null,
			null,
			null,
			null,
			160,
			161,
			162,
			163,
			8362,
			165,
			166,
			167,
			168,
			169,
			215,
			171,
			172,
			173,
			174,
			175,
			176,
			177,
			178,
			179,
			180,
			181,
			182,
			183,
			184,
			185,
			247,
			187,
			188,
			189,
			190,
			191,
			1456,
			1457,
			1458,
			1459,
			1460,
			1461,
			1462,
			1463,
			1464,
			1465,
			1466,
			1467,
			1468,
			1469,
			1470,
			1471,
			1472,
			1473,
			1474,
			1475,
			1520,
			1521,
			1522,
			1523,
			1524,
			null,
			null,
			null,
			null,
			null,
			null,
			null,
			1488,
			1489,
			1490,
			1491,
			1492,
			1493,
			1494,
			1495,
			1496,
			1497,
			1498,
			1499,
			1500,
			1501,
			1502,
			1503,
			1504,
			1505,
			1506,
			1507,
			1508,
			1509,
			1510,
			1511,
			1512,
			1513,
			1514,
			null,
			null,
			8206,
			8207,
			null
		],
		"windows-1256": [
			8364,
			1662,
			8218,
			402,
			8222,
			8230,
			8224,
			8225,
			710,
			8240,
			1657,
			8249,
			338,
			1670,
			1688,
			1672,
			1711,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			1705,
			8482,
			1681,
			8250,
			339,
			8204,
			8205,
			1722,
			160,
			1548,
			162,
			163,
			164,
			165,
			166,
			167,
			168,
			169,
			1726,
			171,
			172,
			173,
			174,
			175,
			176,
			177,
			178,
			179,
			180,
			181,
			182,
			183,
			184,
			185,
			1563,
			187,
			188,
			189,
			190,
			1567,
			1729,
			1569,
			1570,
			1571,
			1572,
			1573,
			1574,
			1575,
			1576,
			1577,
			1578,
			1579,
			1580,
			1581,
			1582,
			1583,
			1584,
			1585,
			1586,
			1587,
			1588,
			1589,
			1590,
			215,
			1591,
			1592,
			1593,
			1594,
			1600,
			1601,
			1602,
			1603,
			224,
			1604,
			226,
			1605,
			1606,
			1607,
			1608,
			231,
			232,
			233,
			234,
			235,
			1609,
			1610,
			238,
			239,
			1611,
			1612,
			1613,
			1614,
			244,
			1615,
			1616,
			247,
			1617,
			249,
			1618,
			251,
			252,
			8206,
			8207,
			1746
		],
		"windows-1257": [
			8364,
			null,
			8218,
			null,
			8222,
			8230,
			8224,
			8225,
			null,
			8240,
			null,
			8249,
			null,
			168,
			711,
			184,
			null,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			null,
			8482,
			null,
			8250,
			null,
			175,
			731,
			null,
			160,
			null,
			162,
			163,
			164,
			null,
			166,
			167,
			216,
			169,
			342,
			171,
			172,
			173,
			174,
			198,
			176,
			177,
			178,
			179,
			180,
			181,
			182,
			183,
			248,
			185,
			343,
			187,
			188,
			189,
			190,
			230,
			260,
			302,
			256,
			262,
			196,
			197,
			280,
			274,
			268,
			201,
			377,
			278,
			290,
			310,
			298,
			315,
			352,
			323,
			325,
			211,
			332,
			213,
			214,
			215,
			370,
			321,
			346,
			362,
			220,
			379,
			381,
			223,
			261,
			303,
			257,
			263,
			228,
			229,
			281,
			275,
			269,
			233,
			378,
			279,
			291,
			311,
			299,
			316,
			353,
			324,
			326,
			243,
			333,
			245,
			246,
			247,
			371,
			322,
			347,
			363,
			252,
			380,
			382,
			729
		],
		"windows-1258": [
			8364,
			null,
			8218,
			402,
			8222,
			8230,
			8224,
			8225,
			710,
			8240,
			null,
			8249,
			338,
			null,
			null,
			null,
			null,
			8216,
			8217,
			8220,
			8221,
			8226,
			8211,
			8212,
			732,
			8482,
			null,
			8250,
			339,
			null,
			null,
			376,
			160,
			161,
			162,
			163,
			164,
			165,
			166,
			167,
			168,
			169,
			170,
			171,
			172,
			173,
			174,
			175,
			176,
			177,
			178,
			179,
			180,
			181,
			182,
			183,
			184,
			185,
			186,
			187,
			188,
			189,
			190,
			191,
			192,
			193,
			194,
			258,
			196,
			197,
			198,
			199,
			200,
			201,
			202,
			203,
			768,
			205,
			206,
			207,
			272,
			209,
			777,
			211,
			212,
			416,
			214,
			215,
			216,
			217,
			218,
			219,
			220,
			431,
			771,
			223,
			224,
			225,
			226,
			259,
			228,
			229,
			230,
			231,
			232,
			233,
			234,
			235,
			769,
			237,
			238,
			239,
			273,
			241,
			803,
			243,
			244,
			417,
			246,
			247,
			248,
			249,
			250,
			251,
			252,
			432,
			8363,
			255
		],
		"x-mac-cyrillic": [
			1040,
			1041,
			1042,
			1043,
			1044,
			1045,
			1046,
			1047,
			1048,
			1049,
			1050,
			1051,
			1052,
			1053,
			1054,
			1055,
			1056,
			1057,
			1058,
			1059,
			1060,
			1061,
			1062,
			1063,
			1064,
			1065,
			1066,
			1067,
			1068,
			1069,
			1070,
			1071,
			8224,
			176,
			1168,
			163,
			167,
			8226,
			182,
			1030,
			174,
			169,
			8482,
			1026,
			1106,
			8800,
			1027,
			1107,
			8734,
			177,
			8804,
			8805,
			1110,
			181,
			1169,
			1032,
			1028,
			1108,
			1031,
			1111,
			1033,
			1113,
			1034,
			1114,
			1112,
			1029,
			172,
			8730,
			402,
			8776,
			8710,
			171,
			187,
			8230,
			160,
			1035,
			1115,
			1036,
			1116,
			1109,
			8211,
			8212,
			8220,
			8221,
			8216,
			8217,
			247,
			8222,
			1038,
			1118,
			1039,
			1119,
			8470,
			1025,
			1105,
			1103,
			1072,
			1073,
			1074,
			1075,
			1076,
			1077,
			1078,
			1079,
			1080,
			1081,
			1082,
			1083,
			1084,
			1085,
			1086,
			1087,
			1088,
			1089,
			1090,
			1091,
			1092,
			1093,
			1094,
			1095,
			1096,
			1097,
			1098,
			1099,
			1100,
			1101,
			1102,
			8364
		]
	};
	var TextDecoder$1 = class TextDecoder$1 {
		constructor(label, options) {
			_defineProperty(this, "encoding", void 0);
			_defineProperty(this, "fatal", void 0);
			_defineProperty(this, "ignoreBOM", void 0);
			_defineProperty(this, "_pendingBytes", []);
			_defineProperty(this, "_bomSeen", false);
			let rawLabel = label ?? "utf-8";
			if (typeof rawLabel !== "string") rawLabel = String(rawLabel);
			let start = 0;
			let end = rawLabel.length;
			while (start < end) {
				const code = rawLabel.charCodeAt(start);
				if (code === 9 || code === 10 || code === 12 || code === 13 || code === 32) start++;
				else break;
			}
			while (end > start) {
				const code = rawLabel.charCodeAt(end - 1);
				if (code === 9 || code === 10 || code === 12 || code === 13 || code === 32) end--;
				else break;
			}
			const encoding = encodingLabels[rawLabel.slice(start, end).toLowerCase()];
			if (!encoding) throw new RangeError(`The encoding label provided ('${label}') is not supported.`);
			this.encoding = encoding;
			this.fatal = Boolean(options?.fatal);
			this.ignoreBOM = Boolean(options?.ignoreBOM);
		}
		/**
		* Decode bytes to a string
		*
		* @param input ArrayBuffer, TypedArray, or DataView
		* @param options Decode options (stream mode)
		* @returns Decoded string
		*/
		decode(input, options) {
			const stream = options?.stream ?? false;
			const hadPendingBytes = this._pendingBytes.length > 0;
			let bytes;
			if (input === void 0 || input === null) bytes = new Uint8Array(0);
			else if (input instanceof ArrayBuffer) bytes = isDetachedArrayBuffer(input) ? new Uint8Array(0) : new Uint8Array(input);
			else if (ArrayBuffer.isView(input)) {
				const sourceBuffer = input.buffer;
				if (sourceBuffer instanceof ArrayBuffer && isDetachedArrayBuffer(sourceBuffer)) bytes = new Uint8Array(0);
				else bytes = new Uint8Array(sourceBuffer, input.byteOffset, input.byteLength);
			} else throw new TypeError("Input must be an ArrayBuffer or ArrayBufferView");
			if (this._pendingBytes.length > 0) {
				const combined = new Uint8Array(this._pendingBytes.length + bytes.length);
				combined.set(this._pendingBytes);
				combined.set(bytes, this._pendingBytes.length);
				bytes = combined;
				this._pendingBytes = [];
			}
			let result;
			const enc = this.encoding;
			if (enc === "utf-8") if (!stream && !hadPendingBytes) {
				const nativeEncoding = getNativeModule("encoding");
				if (nativeEncoding) try {
					result = nativeEncoding.decodeUTF8(bytes, this.fatal);
					if (!this._bomSeen && !this.ignoreBOM && result.charCodeAt(0) === 65279) result = result.slice(1);
					this._bomSeen = true;
				} catch (e) {
					if (this.fatal) {
						if (e && typeof e === "object" && "code" in e) throw e;
						throw createEncodingError(e instanceof Error && e.message ? e.message : "Invalid encoded data");
					}
					result = this._decodeUTF8(bytes, stream);
				}
				else result = this._decodeUTF8(bytes, stream);
			} else result = this._decodeUTF8(bytes, stream);
			else if (enc === "utf-16le") result = this._decodeUTF16(bytes, false, stream);
			else if (enc === "utf-16be") result = this._decodeUTF16(bytes, true, stream);
			else if (enc === "replacement") result = this._decodeReplacement(bytes);
			else if (enc === "x-user-defined") result = this._decodeXUserDefined(bytes);
			else if (enc === "windows-1252") result = this._decodeSingleByte(bytes, WINDOWS_1252, true);
			else if (SINGLE_BYTE_TABLES[enc]) result = this._decodeSingleByte(bytes, SINGLE_BYTE_TABLES[enc], false);
			else if (NativeTextDecoderImpl && NativeTextDecoderImpl !== TextDecoder$1) try {
				result = new NativeTextDecoderImpl(enc, {
					fatal: this.fatal,
					ignoreBOM: this.ignoreBOM
				}).decode(bytes, options);
			} catch (error) {
				if (this.fatal) throw error;
				result = this._decodeUTF8(bytes, stream);
			}
			else result = this._decodeUTF8(bytes, stream);
			if (!stream) {
				this._pendingBytes = [];
				this._bomSeen = false;
			}
			return result;
		}
		_decodeSingleByte(bytes, table, isWindows1252) {
			const result = [];
			for (let i = 0; i < bytes.length; i++) {
				const byte = bytes[i];
				if (byte <= 127) result.push(String.fromCharCode(byte));
				else if (isWindows1252) if (byte >= 128 && byte <= 159) {
					const cp = table[byte - 128];
					if (cp === null) {
						if (this.fatal) throw createEncodingError("Invalid byte in encoding");
						result.push("�");
					} else result.push(String.fromCharCode(cp));
				} else result.push(String.fromCharCode(byte));
				else {
					const cp = table[byte - 128];
					if (cp === null) {
						if (this.fatal) throw createEncodingError("Invalid byte in encoding");
						result.push("�");
					} else result.push(String.fromCharCode(cp));
				}
			}
			return result.join("");
		}
		_decodeReplacement(bytes) {
			if (bytes.length === 0) return "";
			if (this.fatal) throw createEncodingError("Replacement encoding always errors on non-empty input");
			return "�";
		}
		_decodeXUserDefined(bytes) {
			const result = [];
			for (let i = 0; i < bytes.length; i++) {
				const byte = bytes[i];
				if (byte <= 127) result.push(String.fromCharCode(byte));
				else result.push(String.fromCharCode(63360 + byte - 128));
			}
			return result.join("");
		}
		_decodeUTF8(bytes, stream) {
			const result = [];
			let i = 0;
			if (!this._bomSeen && bytes.length >= 3) {
				if (bytes[0] === 239 && bytes[1] === 187 && bytes[2] === 191 && !this.ignoreBOM) i = 3;
				this._bomSeen = true;
			}
			while (i < bytes.length) {
				const byte1 = bytes[i];
				let codePoint;
				let bytesNeeded;
				if ((byte1 & 128) === 0) {
					codePoint = byte1;
					bytesNeeded = 1;
				} else if ((byte1 & 224) === 192 && byte1 >= 194) {
					codePoint = byte1 & 31;
					bytesNeeded = 2;
				} else if ((byte1 & 240) === 224) {
					codePoint = byte1 & 15;
					bytesNeeded = 3;
				} else if ((byte1 & 248) === 240 && byte1 <= 244) {
					codePoint = byte1 & 7;
					bytesNeeded = 4;
				} else {
					if (this.fatal) throw createEncodingError("Invalid UTF-8 byte sequence");
					result.push("�");
					i++;
					continue;
				}
				if (i + bytesNeeded > bytes.length) if (stream) {
					let consumed = 1;
					let malformed = false;
					while (i + consumed < bytes.length) {
						const continuation = bytes[i + consumed];
						if (!isValidUTF8Continuation(byte1, continuation, consumed)) {
							malformed = true;
							break;
						}
						consumed++;
					}
					if (malformed) {
						if (this.fatal) throw createEncodingError("Invalid UTF-8 continuation byte");
						result.push("�");
						i += consumed;
						continue;
					}
					this._pendingBytes = Array.from(bytes.slice(i));
					break;
				} else if (this.fatal) throw createEncodingError("Incomplete UTF-8 byte sequence");
				else {
					let consumed = 1;
					while (i + consumed < bytes.length) {
						const continuation = bytes[i + consumed];
						if (!isValidUTF8Continuation(byte1, continuation, consumed)) break;
						consumed++;
					}
					result.push("�");
					if (i + consumed >= bytes.length) break;
					i += consumed;
					continue;
				}
				let valid = true;
				for (let j = 1; j < bytesNeeded; j++) {
					const contByte = bytes[i + j];
					if ((contByte & 192) !== 128) {
						valid = false;
						break;
					}
					codePoint = codePoint << 6 | contByte & 63;
				}
				if (!valid) {
					if (this.fatal) throw createEncodingError("Invalid UTF-8 continuation byte");
					result.push("�");
					i++;
					continue;
				}
				if (codePoint > 1114111 || codePoint >= 55296 && codePoint <= 57343 || bytesNeeded === 2 && codePoint < 128 || bytesNeeded === 3 && codePoint < 2048 || bytesNeeded === 4 && codePoint < 65536) {
					if (this.fatal) throw createEncodingError("Invalid UTF-8 code point");
					result.push("�");
					i++;
					continue;
				}
				if (codePoint <= 65535) result.push(String.fromCharCode(codePoint));
				else {
					const offset = codePoint - 65536;
					result.push(String.fromCharCode(55296 + (offset >> 10)), String.fromCharCode(56320 + (offset & 1023)));
				}
				i += bytesNeeded;
			}
			return result.join("");
		}
		_decodeUTF16(bytes, bigEndian, stream) {
			const result = [];
			let i = 0;
			if (!this._bomSeen && bytes.length >= 2) {
				if (bytes[0] === 254 && bytes[1] === 255 && !this.ignoreBOM) {
					bigEndian = true;
					i = 2;
				} else if (bytes[0] === 255 && bytes[1] === 254 && !this.ignoreBOM) {
					bigEndian = false;
					i = 2;
				}
				this._bomSeen = true;
			}
			while (i < bytes.length) {
				if (i + 1 >= bytes.length) {
					if (stream) {
						this._pendingBytes = Array.from(bytes.slice(i));
						break;
					}
					if (this.fatal) throw createEncodingError("Invalid UTF-16 byte sequence");
					result.push("�");
					break;
				}
				const unit = bigEndian ? bytes[i] << 8 | bytes[i + 1] : bytes[i] | bytes[i + 1] << 8;
				if (unit >= 55296 && unit <= 56319) {
					if (i + 3 >= bytes.length) {
						if (stream) {
							this._pendingBytes = Array.from(bytes.slice(i));
							break;
						}
						if (this.fatal) throw createEncodingError("Invalid UTF-16 surrogate pair");
						result.push("�");
						i += 2;
						continue;
					}
					const low = bigEndian ? bytes[i + 2] << 8 | bytes[i + 3] : bytes[i + 2] | bytes[i + 3] << 8;
					if (low >= 56320 && low <= 57343) {
						result.push(String.fromCharCode(unit, low));
						i += 4;
						continue;
					}
					if (this.fatal) throw createEncodingError("Invalid UTF-16 surrogate pair");
					result.push("�");
				} else if (unit >= 56320 && unit <= 57343) {
					if (this.fatal) throw createEncodingError("Invalid UTF-16 surrogate pair");
					result.push("�");
				} else result.push(String.fromCharCode(unit));
				i += 2;
			}
			return result.join("");
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/encoding/TextEncoderStream.ts
	/**
	* TextEncoderStream - Web Standard TextEncoderStream Implementation
	*
	* @see https://encoding.spec.whatwg.org/#interface-textencoderstream
	*
	* Wraps a TransformStream that encodes string chunks into UTF-8 Uint8Array chunks.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$21;
	_Symbol$toStringTag$21 = Symbol.toStringTag;
	var TextEncoderStream = class {
		constructor() {
			_defineProperty(this, "encoding", "utf-8");
			_defineProperty(this, "readable", void 0);
			_defineProperty(this, "writable", void 0);
			_defineProperty(this, "_transform", void 0);
			const encoder = new TextEncoder$1();
			this._transform = new TransformStream({ transform(chunk, controller) {
				const encoded = encoder.encode(String(chunk));
				if (encoded.length > 0) controller.enqueue(encoded);
			} });
			this.readable = this._transform.readable;
			this.writable = this._transform.writable;
		}
		get [_Symbol$toStringTag$21]() {
			return "TextEncoderStream";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/encoding/TextDecoderStream.ts
	/**
	* TextDecoderStream - Web Standard TextDecoderStream Implementation
	*
	* @see https://encoding.spec.whatwg.org/#interface-textdecoderstream
	*
	* Wraps a TransformStream that decodes Uint8Array chunks into string chunks
	* using TextDecoder in streaming mode.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$20;
	_Symbol$toStringTag$20 = Symbol.toStringTag;
	var TextDecoderStream = class {
		constructor(label, options) {
			_defineProperty(this, "encoding", void 0);
			_defineProperty(this, "fatal", void 0);
			_defineProperty(this, "ignoreBOM", void 0);
			_defineProperty(this, "readable", void 0);
			_defineProperty(this, "writable", void 0);
			_defineProperty(this, "_transform", void 0);
			const decoder = new TextDecoder$1(label, options);
			this.encoding = decoder.encoding;
			this.fatal = decoder.fatal;
			this.ignoreBOM = decoder.ignoreBOM;
			this._transform = new TransformStream({
				transform(chunk, controller) {
					const decoded = decoder.decode(chunk, { stream: true });
					if (decoded.length > 0) controller.enqueue(decoded);
				},
				flush(controller) {
					const final = decoder.decode(void 0, { stream: false });
					if (final.length > 0) controller.enqueue(final);
				}
			});
			this.readable = this._transform.readable;
			this.writable = this._transform.writable;
		}
		get [_Symbol$toStringTag$20]() {
			return "TextDecoderStream";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/url/URLSearchParams.ts
	init_defineProperty();
	let _Symbol$iterator$1;
	_Symbol$iterator$1 = Symbol.iterator;
	var URLSearchParams$1 = class URLSearchParams$1 {
		constructor(init) {
			_defineProperty(this, "_params", []);
			_defineProperty(this, "_url", null);
			if (init === void 0 || init === null) return;
			if (typeof init === "string") this._parseString(init);
			else if (init instanceof URLSearchParams$1) this._params = [...init._params];
			else if (typeof init === "object") if (Symbol.iterator in init) {
				const entries = Array.from(init);
				for (let i = 0; i < entries.length; i++) {
					const [key, value] = entries[i];
					this._params.push([String(key), String(value)]);
				}
			} else {
				const keys = Object.keys(init);
				for (let i = 0; i < keys.length; i++) {
					const key = keys[i];
					this._params.push([key, String(init[key])]);
				}
			}
		}
		_parseString(input) {
			let str = input;
			if (str.startsWith("?")) str = str.slice(1);
			if (!str) return;
			const pairs = str.split("&");
			for (const pair of pairs) {
				if (!pair) continue;
				const eqIndex = pair.indexOf("=");
				if (eqIndex === -1) this._params.push([this._decode(pair), ""]);
				else {
					const key = this._decode(pair.slice(0, eqIndex));
					const value = this._decode(pair.slice(eqIndex + 1));
					this._params.push([key, value]);
				}
			}
		}
		_decode(str) {
			try {
				return decodeURIComponent(str.replace(/\+/g, " "));
			} catch {
				return str;
			}
		}
		_encode(str) {
			return encodeURIComponent(str).replace(/%20/g, "+");
		}
		_update() {
			if (this._url) this._url._updateSearch(this.toString());
		}
		/** @internal */
		_setURL(url) {
			this._url = url;
		}
		append(name, value) {
			this._params.push([String(name), String(value)]);
			this._update();
		}
		delete(name, value) {
			const key = String(name);
			if (value === void 0) this._params = this._params.filter(([k]) => k !== key);
			else {
				const val = String(value);
				this._params = this._params.filter(([k, v]) => !(k === key && v === val));
			}
			this._update();
		}
		get(name) {
			const key = String(name);
			const entry = this._params.find(([k]) => k === key);
			return entry ? entry[1] : null;
		}
		getAll(name) {
			const key = String(name);
			return this._params.filter(([k]) => k === key).map(([, v]) => v);
		}
		has(name, value) {
			const key = String(name);
			if (value === void 0) return this._params.some(([k]) => k === key);
			const val = String(value);
			return this._params.some(([k, v]) => k === key && v === val);
		}
		set(name, value) {
			const key = String(name);
			const val = String(value);
			let found = false;
			this._params = this._params.filter(([k]) => {
				if (k === key) {
					if (!found) {
						found = true;
						return true;
					}
					return false;
				}
				return true;
			});
			if (found) {
				const index = this._params.findIndex(([k]) => k === key);
				this._params[index] = [key, val];
			} else this._params.push([key, val]);
			this._update();
		}
		sort() {
			this._params.sort((a, b) => {
				if (a[0] < b[0]) return -1;
				if (a[0] > b[0]) return 1;
				return 0;
			});
			this._update();
		}
		get size() {
			return this._params.length;
		}
		get length() {
			return this._params.length;
		}
		toString() {
			return this._params.map(([k, v]) => `${this._encode(k)}=${this._encode(v)}`).join("&");
		}
		toJSON() {
			const result = {};
			for (let i = 0; i < this._params.length; i++) {
				const [key, value] = this._params[i];
				const existing = result[key];
				if (existing === void 0) result[key] = value;
				else if (Array.isArray(existing)) existing.push(value);
				else result[key] = [existing, value];
			}
			return result;
		}
		forEach(callback, thisArg) {
			const params = this._params;
			for (let i = 0; i < params.length; i++) callback.call(thisArg, params[i][1], params[i][0], this);
		}
		*entries() {
			for (const entry of this._params) yield entry;
		}
		*keys() {
			for (const [key] of this._params) yield key;
		}
		*values() {
			for (const [, value] of this._params) yield value;
		}
		[_Symbol$iterator$1]() {
			return this.entries();
		}
	};
	{
		const __exactForOfIterator67 = ["size", "length"][Symbol.iterator]();
		for (;;) {
			const __exactForOfStep67 = __exactForOfIterator67.next();
			if (__exactForOfStep67.done) break;
			const key = __exactForOfStep67.value;
			const descriptor = Object.getOwnPropertyDescriptor(URLSearchParams$1.prototype, key);
			if (descriptor) Object.defineProperty(URLSearchParams$1.prototype, key, {
				...descriptor,
				enumerable: true
			});
		}
	}
	//#endregion
	//#region ../ibex-runtime-js/src/url/URL.ts
	/**
	* URL - WHATWG URL Standard Implementation
	*
	* @see https://url.spec.whatwg.org/
	*/
	init_defineProperty();
	const DEFAULT_PORTS = {
		ftp: "21",
		http: "80",
		https: "443",
		ws: "80",
		wss: "443"
	};
	const SPECIAL_SCHEMES = new Set([
		"ftp",
		"file",
		"http",
		"https",
		"ws",
		"wss"
	]);
	const OBJECT_URL_PREFIX = "blob:exact:";
	const objectURLRegistry = /* @__PURE__ */ new Map();
	let objectURLCounter = 0;
	function makeMissingArgsError(argumentName) {
		const error = /* @__PURE__ */ new TypeError(`The "${argumentName}" argument must be specified`);
		error.code = "ERR_MISSING_ARGS";
		return error;
	}
	function makeInvalidBlobError(object) {
		let received = `type ${typeof object}`;
		if (object === null) received = "null";
		else if (object !== void 0 && typeof object === "object") {
			const ctorName = object.constructor?.name;
			received = ctorName ? `an instance of ${ctorName}` : "an instance of Object";
		}
		const error = /* @__PURE__ */ new TypeError(`The "obj" argument must be an instance of Blob. Received ${received}`);
		error.code = "ERR_INVALID_ARG_TYPE";
		return error;
	}
	function resolveHostURLConstructor() {
		const globalURL = globalThis.URL;
		if (typeof globalURL === "function" && globalURL !== URL$1) return globalURL;
		const globalRef = globalThis;
		for (const loaderName of ["__exactRequire", "require"]) {
			const loader = globalRef[loaderName];
			if (typeof loader !== "function") continue;
			try {
				const moduleURL = loader("url")?.URL;
				if (typeof moduleURL === "function" && moduleURL !== URL$1) return moduleURL;
			} catch {}
		}
	}
	function canonicalizeSpecialHost(value) {
		const hostURL = resolveHostURLConstructor();
		if (typeof hostURL !== "function") return value;
		return new hostURL(`https://${value}`).hostname;
	}
	function toHex(code) {
		const hex = code.toString(16).toUpperCase();
		return hex.length === 1 ? `0${hex}` : hex;
	}
	function canonicalizeHost(value, protocol) {
		if (protocol && SPECIAL_SCHEMES.has(protocol.slice(0, -1))) try {
			return canonicalizeSpecialHost(value);
		} catch {
			return value.toLowerCase();
		}
		let out = "";
		for (let i = 0; i < value.length; i++) {
			const char = value.charAt(i);
			if (char.charCodeAt(0) >= 128) {
				out += encodeURIComponent(char);
				continue;
			}
			if (char === "%" && i + 2 < value.length && /[0-9A-Fa-f]/.test(value.charAt(i + 1)) && /[0-9A-Fa-f]/.test(value.charAt(i + 2))) {
				out += `%${value.charAt(i + 1).toUpperCase()}${value.charAt(i + 2).toUpperCase()}`;
				i += 2;
				continue;
			}
			out += char.toLowerCase();
		}
		return out;
	}
	function normalizePort(value) {
		if (!value) return "";
		const match = value.match(/^[0-9]+/);
		if (!match) return "";
		value = match[0];
		return value.replace(/^0+(?=\d)/, "");
	}
	function sanitizeUserinfo(value) {
		value = String(value);
		let out = "";
		for (let i = 0; i < value.length; i++) {
			const c = value.charAt(i);
			if (c === "%" && __needsHexDigit(value.charAt(i + 1)) && __needsHexDigit(value.charAt(i + 2))) {
				out += value.slice(i, i + 3);
				i += 2;
				continue;
			}
			if (__isUserinfoUnescaped(c)) {
				out += c;
				continue;
			}
			out += encodeURIComponent(c);
		}
		return out;
	}
	function __needsHexDigit(char) {
		return !!char && /^[0-9A-Fa-f]$/.test(char);
	}
	function __isUserinfoUnescaped(c) {
		const code = c.charCodeAt(0);
		if (code >= 48 && code <= 57) return true;
		if (code >= 65 && code <= 90) return true;
		if (code >= 97 && code <= 122) return true;
		return code === 33 || code === 36 || code === 38 || code === 39 || code === 40 || code === 41 || code === 42 || code === 43 || code === 44 || code === 45 || code === 46 || code === 37;
	}
	function sanitizeHost(value, protocol) {
		value = String(value);
		let out = "";
		for (let i = 0; i < value.length; i++) {
			const code = value.charCodeAt(i);
			if (code === 0) return null;
			if (protocol === "https:" && code === 31) return null;
			if (code >= 0 && code < 32) {
				if (code === 9 || code === 10 || code === 13) continue;
				out += `%${toHex(code)}`;
				continue;
			}
			out += value.charAt(i);
		}
		return out;
	}
	function parseHostInput(value, isSpecial = false) {
		for (let i = 0; i < value.length; i++) {
			const c = value.charCodeAt(i);
			if (c === 47 || c === 63 || c === 35 || isSpecial && c === 92) return value.slice(0, i);
		}
		return value;
	}
	function stripProtocolControlChars(value) {
		value = String(value);
		let out = "";
		for (let i = 0; i < value.length; i++) {
			const code = value.charCodeAt(i);
			if (code === 9 || code === 10 || code === 13) continue;
			out += value.charAt(i);
		}
		return out;
	}
	/**
	* Create a TypeError matching the WHATWG URL spec error format.
	* Redacts base URLs that contain passwords.
	*/
	function _makeURLError(input, baseInput) {
		let msg;
		if (baseInput !== void 0) {
			const displayBase = /:[^/].*@/.test(baseInput) ? "<redacted>" : JSON.stringify(baseInput);
			msg = `${JSON.stringify(input)} cannot be parsed as a URL against ${displayBase}`;
		} else msg = `${JSON.stringify(input)} cannot be parsed as a URL`;
		const err = new TypeError(msg);
		err.code = "ERR_INVALID_URL";
		return err;
	}
	var URL$1 = class URL$1 {
		constructor(url, base) {
			_defineProperty(this, "_protocol", "");
			_defineProperty(this, "_username", "");
			_defineProperty(this, "_password", "");
			_defineProperty(this, "_hostname", "");
			_defineProperty(this, "_port", "");
			_defineProperty(this, "_pathname", "");
			_defineProperty(this, "_search", "");
			_defineProperty(this, "_hash", "");
			_defineProperty(this, "_searchParams", void 0);
			if (arguments.length === 0) {
				const error = /* @__PURE__ */ new TypeError("The \"url\" argument must be specified");
				error.code = "ERR_MISSING_ARGS";
				throw error;
			}
			const urlStr = String(url);
			let baseURL = null;
			const baseStr = base !== void 0 ? base instanceof URL$1 ? base.href : String(base) : void 0;
			if (base !== void 0) if (base instanceof URL$1) baseURL = base;
			else try {
				baseURL = new URL$1(String(base));
			} catch {
				throw _makeURLError(urlStr, baseStr);
			}
			this._parse(urlStr, baseURL, baseStr);
			this._searchParams = new URLSearchParams$1(this._search);
			this._searchParams._setURL(this);
		}
		_parse(input, base, baseStr) {
			const hostURL = resolveHostURLConstructor();
			if (typeof hostURL !== "function") throw _makeURLError(input, baseStr);
			try {
				const parsed = base ? new hostURL(input, base.href) : new hostURL(input);
				this._protocol = parsed.protocol;
				this._username = parsed.username;
				this._password = parsed.password;
				this._hostname = parsed.hostname;
				this._port = parsed.port;
				this._pathname = parsed.pathname;
				this._search = parsed.search;
				this._hash = parsed.hash;
			} catch {
				throw _makeURLError(input, baseStr);
			}
		}
		_normalizePath(path) {
			if (!path) return "/";
			const segments = path.split("/");
			const normalized = [];
			for (const segment of segments) if (segment === ".") continue;
			else if (segment === "..") normalized.pop();
			else normalized.push(segment);
			let result = normalized.join("/");
			if (!result.startsWith("/")) result = "/" + result;
			return result;
		}
		get href() {
			let result = this._protocol;
			if (this._hostname) {
				result += "//";
				if (this._username) {
					result += this._username;
					if (this._password) result += ":" + this._password;
					result += "@";
				}
				result += this._hostname;
				if (this._port) result += ":" + this._port;
			}
			result += this._pathname;
			result += this._search;
			result += this._hash;
			return result;
		}
		set href(value) {
			const newURL = new URL$1(value);
			this._protocol = newURL._protocol;
			this._username = newURL._username;
			this._password = newURL._password;
			this._hostname = newURL._hostname;
			this._port = newURL._port;
			this._pathname = newURL._pathname;
			this._search = newURL._search;
			this._hash = newURL._hash;
			this._searchParams = new URLSearchParams$1(this._search);
			this._searchParams._setURL(this);
		}
		get origin() {
			const scheme = this._protocol.slice(0, -1);
			if (scheme === "blob") try {
				let innerHref = this._pathname + this._search + this._hash;
				if (innerHref.startsWith("/") && /^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(innerHref.slice(1))) innerHref = innerHref.slice(1);
				const innerURL = new URL$1(innerHref);
				if (innerURL.protocol === "file:") return innerURL.host ? `file://${innerURL.host}` : "file://";
				return innerURL.origin;
			} catch {
				return "null";
			}
			if (scheme === "file") return "null";
			if (scheme === "http" || scheme === "https" || scheme === "ftp" || scheme === "ws" || scheme === "wss") return `${this._protocol}//${this.host}`;
			return "null";
		}
		get protocol() {
			return this._protocol;
		}
		set protocol(value) {
			let proto = stripProtocolControlChars(String(value)).toLowerCase();
			if (!proto.endsWith(":")) proto += ":";
			if (!/^[a-zA-Z][a-zA-Z0-9+.-]*:$/.test(proto)) return;
			if (proto === "file:" && (this._username || this._password || this._port)) return;
			this._protocol = proto;
		}
		get username() {
			return this._username;
		}
		set username(value) {
			if (this._protocol === "file:" || this._protocol === "unix:") return;
			this._username = sanitizeUserinfo(String(value));
		}
		get password() {
			return this._password;
		}
		set password(value) {
			if (this._protocol === "file:" || this._protocol === "unix:") return;
			this._password = sanitizeUserinfo(String(value));
		}
		get host() {
			if (this._port) return `${this._hostname}:${this._port}`;
			return this._hostname;
		}
		set host(value) {
			const input = sanitizeHost(value, this._protocol);
			if (input === null) return;
			const isSpecial = SPECIAL_SCHEMES.has(this._protocol.slice(0, -1));
			if (!this._hostname && !isSpecial) return;
			const hostInput = parseHostInput(input, isSpecial);
			if (hostInput === "") {
				if (!isSpecial && this._hostname) {
					this._hostname = "";
					this._port = "";
				}
				return;
			}
			if (hostInput.indexOf(" ") !== -1) return;
			if (hostInput.indexOf("@") !== -1) return;
			if (hostInput.indexOf("\\") !== -1 && !isSpecial) return;
			const colonIndex = hostInput.lastIndexOf(":");
			if (colonIndex !== -1 && !input.includes("[")) {
				this._hostname = canonicalizeHost(hostInput.slice(0, colonIndex), this._protocol);
				if (colonIndex < hostInput.length - 1) {
					const port = hostInput.slice(colonIndex + 1);
					const parsedPort = normalizePort(port);
					if (parsedPort) {
						const portNumber = Number(parsedPort);
						if (!Number.isNaN(portNumber) && portNumber <= 65535) this._port = parsedPort;
					} else if (port === "") this._port = "";
					if (this._port && this._protocol.slice(0, -1) && this._port === DEFAULT_PORTS[this._protocol.slice(0, -1)]) this._port = "";
				}
			} else if (hostInput !== "") this._hostname = canonicalizeHost(hostInput, this._protocol);
			else {
				this._hostname = "";
				this._port = "";
			}
		}
		get hostname() {
			return this._hostname;
		}
		set hostname(value) {
			const input = sanitizeHost(value, this._protocol);
			if (input === null) return;
			const isSpecial = SPECIAL_SCHEMES.has(this._protocol.slice(0, -1));
			if (!this._hostname && !isSpecial) return;
			const hostInput = parseHostInput(input, isSpecial);
			if (hostInput === "" || hostInput.indexOf(" ") !== -1) return;
			if (hostInput.indexOf("@") !== -1) return;
			if (hostInput.indexOf("\\") !== -1 && !isSpecial) return;
			this._hostname = canonicalizeHost(hostInput, this._protocol);
		}
		get port() {
			return this._port;
		}
		set port(value) {
			const port = String(value);
			if (port === "" || port === DEFAULT_PORTS[this._protocol.slice(0, -1)]) this._port = "";
			else this._port = port;
		}
		get pathname() {
			return this._pathname;
		}
		set pathname(value) {
			this._pathname = this._normalizePath(String(value));
		}
		get search() {
			return this._search;
		}
		set search(value) {
			let search = String(value);
			if (search && !search.startsWith("?")) search = "?" + search;
			this._search = search;
			this._searchParams = new URLSearchParams$1(search);
			this._searchParams._setURL(this);
		}
		get searchParams() {
			return this._searchParams;
		}
		get hash() {
			return this._hash;
		}
		set hash(value) {
			let hash = String(value);
			if (hash && !hash.startsWith("#")) hash = "#" + hash;
			this._hash = hash;
		}
		toString() {
			return this.href;
		}
		toJSON() {
			return this.href;
		}
		/**
		* Update search string from URLSearchParams
		* @internal
		*/
		_updateSearch(search) {
			this._search = search ? "?" + search : "";
		}
		/**
		* Check if a string can be parsed as a URL
		*
		* Per Node.js spec, throws ERR_MISSING_ARGS if called with no arguments.
		*/
		static canParse(url, base) {
			if (arguments.length === 0) throw makeMissingArgsError("url");
			try {
				new URL$1(url, base);
				return true;
			} catch {
				return false;
			}
		}
		/**
		* Parse a URL string, returning null instead of throwing on invalid input.
		*
		* @see https://url.spec.whatwg.org/#dom-url-parse
		*/
		static parse(url, base) {
			try {
				return new URL$1(url, base);
			} catch {
				return null;
			}
		}
		static createObjectURL(object) {
			if (arguments.length === 0) throw makeMissingArgsError("obj");
			if (typeof Blob !== "function" || !(object instanceof Blob)) throw makeInvalidBlobError(object);
			const url = `${OBJECT_URL_PREFIX}${++objectURLCounter}`;
			objectURLRegistry.set(url, object);
			return url;
		}
		static revokeObjectURL(url) {
			if (arguments.length === 0) throw makeMissingArgsError("url");
			objectURLRegistry.delete(String(url));
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/url/URLPattern.ts
	init_defineProperty();
	/** The set of URL component names we track. */
	const COMPONENTS = [
		"protocol",
		"username",
		"password",
		"hostname",
		"port",
		"pathname",
		"search",
		"hash"
	];
	/**
	* Default patterns for each component when not specified.
	* These match "anything" in a way appropriate for that component.
	*/
	const DEFAULT_PATTERNS = {
		protocol: "*",
		username: "*",
		password: "*",
		hostname: "*",
		port: "*",
		pathname: "*",
		search: "*",
		hash: "*"
	};
	/**
	* Characters that separate segments in each component.
	* Used to determine what a bare `:name` group should NOT match.
	*/
	function getSegmentWildcard(component) {
		switch (component) {
			case "pathname": return "[^/]+";
			case "hostname": return "[^.]+";
			default: return "[^]+";
		}
	}
	/**
	* For a full wildcard `*`, determine what regex to use.
	*/
	function getFullWildcard(component) {
		return ".*";
	}
	/**
	* Escape a string for use in a regular expression.
	*/
	function escapeRegExp(str) {
		return str.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
	}
	/**
	* Parse and compile a pattern string for a given component into a RegExp.
	*
	* Supported syntax:
	*   :name       - Named parameter (matches segment chars)
	*   *           - Wildcard (matches everything, captured as group 0, 1, ...)
	*   (regex)     - Custom regex group
	*   :name(regex)- Named parameter with custom regex
	*   {pattern}?  - Optional group (the ? makes the entire group optional)
	*   literal     - Literal text (escaped)
	*
	* Returns a CompiledComponent with the original pattern, compiled regex,
	* and an ordered list of group names.
	*/
	function compileComponent(pattern, component) {
		const groupNames = [];
		let regexStr = "^";
		let i = 0;
		let unnamedIndex = 0;
		while (i < pattern.length) {
			const ch = pattern[i];
			if (ch === "{") {
				const closeBrace = findMatchingBrace(pattern, i);
				if (closeBrace === -1) {
					regexStr += escapeRegExp(ch);
					i++;
					continue;
				}
				const innerPattern = pattern.slice(i + 1, closeBrace);
				const isOptional = closeBrace + 1 < pattern.length && pattern[closeBrace + 1] === "?";
				const inner = compileComponent(innerPattern, component);
				const innerRegex = inner.regex.source.slice(1, -1);
				if (isOptional) {
					regexStr += `(?:${innerRegex})?`;
					i = closeBrace + 2;
				} else {
					regexStr += `(?:${innerRegex})`;
					i = closeBrace + 1;
				}
				{
					const __exactForOfIterator68 = inner.groupNames[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep68 = __exactForOfIterator68.next();
						if (__exactForOfStep68.done) break;
						const name = __exactForOfStep68.value;
						groupNames.push(name);
					}
				}
				continue;
			}
			if (ch === ":") {
				i++;
				let name = "";
				while (i < pattern.length && /[\w]/.test(pattern[i])) {
					name += pattern[i];
					i++;
				}
				if (name === "") {
					regexStr += escapeRegExp(":");
					continue;
				}
				groupNames.push(name);
				if (i < pattern.length && pattern[i] === "(") {
					const closeParen = findMatchingParen(pattern, i);
					if (closeParen !== -1) {
						const customRegex = pattern.slice(i + 1, closeParen);
						regexStr += `(${customRegex})`;
						i = closeParen + 1;
					} else regexStr += `(${getSegmentWildcard(component)})`;
				} else regexStr += `(${getSegmentWildcard(component)})`;
				continue;
			}
			if (ch === "*") {
				groupNames.push(String(unnamedIndex));
				unnamedIndex++;
				regexStr += `(${getFullWildcard(component)})`;
				i++;
				continue;
			}
			if (ch === "(") {
				const closeParen = findMatchingParen(pattern, i);
				if (closeParen !== -1) {
					const customRegex = pattern.slice(i + 1, closeParen);
					groupNames.push(String(unnamedIndex));
					unnamedIndex++;
					regexStr += `(${customRegex})`;
					i = closeParen + 1;
					continue;
				}
				regexStr += escapeRegExp(ch);
				i++;
				continue;
			}
			regexStr += escapeRegExp(ch);
			i++;
		}
		regexStr += "$";
		return {
			pattern,
			regex: new RegExp(regexStr),
			groupNames
		};
	}
	/**
	* Find the matching closing brace for an opening brace at position `start`.
	* Handles nested braces.
	*/
	function findMatchingBrace(str, start) {
		let depth = 1;
		let i = start + 1;
		while (i < str.length && depth > 0) {
			if (str[i] === "{") depth++;
			else if (str[i] === "}") depth--;
			if (depth === 0) return i;
			i++;
		}
		return -1;
	}
	/**
	* Find the matching closing parenthesis for an opening paren at position `start`.
	* Handles nested parens and escaped chars.
	*/
	function findMatchingParen(str, start) {
		let depth = 1;
		let i = start + 1;
		while (i < str.length && depth > 0) {
			if (str[i] === "\\") {
				i += 2;
				continue;
			}
			if (str[i] === "(") depth++;
			else if (str[i] === ")") depth--;
			if (depth === 0) return i;
			i++;
		}
		return -1;
	}
	/**
	* Parse a URL string (or URLPatternInit) into its components.
	* Returns an object with each component as a string.
	*/
	function parseURLInput(input, baseURL) {
		if (typeof input === "object") {
			const result = {
				protocol: "",
				username: "",
				password: "",
				hostname: "",
				port: "",
				pathname: "",
				search: "",
				hash: ""
			};
			if (input.baseURL) {
				const base = parseURLString(input.baseURL);
				{
					const __exactForOfIterator69 = COMPONENTS[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep69 = __exactForOfIterator69.next();
						if (__exactForOfStep69.done) break;
						const comp = __exactForOfStep69.value;
						result[comp] = base[comp];
					}
				}
			}
			{
				const __exactForOfIterator70 = COMPONENTS[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep70 = __exactForOfIterator70.next();
					if (__exactForOfStep70.done) break;
					const comp = __exactForOfStep70.value;
					if (input[comp] !== void 0) result[comp] = input[comp];
				}
			}
			return result;
		}
		return parseURLString(baseURL ? resolveURL(input, baseURL) : input);
	}
	/**
	* Simple URL resolution: resolve `url` against `base`.
	*/
	function resolveURL(url, base) {
		if (/^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(url)) return url;
		const baseParts = parseURLString(base);
		if (url.startsWith("//")) return baseParts.protocol + ":" + url;
		if (url.startsWith("/")) {
			const authority = buildAuthority(baseParts);
			return baseParts.protocol + "://" + authority + url;
		}
		const authority = buildAuthority(baseParts);
		const basePath = baseParts.pathname.slice(0, baseParts.pathname.lastIndexOf("/") + 1);
		return baseParts.protocol + "://" + authority + basePath + url;
	}
	/**
	* Build the authority section (user:pass@host:port) from parsed components.
	*/
	function buildAuthority(parts) {
		let authority = "";
		if (parts.username) {
			authority += parts.username;
			if (parts.password) authority += ":" + parts.password;
			authority += "@";
		}
		authority += parts.hostname;
		if (parts.port) authority += ":" + parts.port;
		return authority;
	}
	/**
	* Parse a URL string into its component parts.
	* This is a pattern-aware parser that handles both actual URLs and patterns.
	*/
	function parseURLString(url) {
		const result = {
			protocol: "",
			username: "",
			password: "",
			hostname: "",
			port: "",
			pathname: "",
			search: "",
			hash: ""
		};
		let remaining = url;
		const hashIdx = remaining.indexOf("#");
		if (hashIdx !== -1) {
			result.hash = remaining.slice(hashIdx + 1);
			remaining = remaining.slice(0, hashIdx);
		}
		const searchIdx = remaining.indexOf("?");
		if (searchIdx !== -1) {
			result.search = remaining.slice(searchIdx + 1);
			remaining = remaining.slice(0, searchIdx);
		}
		const protoMatch = remaining.match(/^([a-zA-Z][a-zA-Z0-9+.-]*):(.*)$/);
		if (protoMatch) {
			result.protocol = protoMatch[1].toLowerCase();
			remaining = protoMatch[2];
		}
		if (remaining.startsWith("//")) {
			remaining = remaining.slice(2);
			let authEnd = remaining.length;
			const slashIdx = remaining.indexOf("/");
			if (slashIdx !== -1) authEnd = slashIdx;
			const authority = remaining.slice(0, authEnd);
			remaining = remaining.slice(authEnd);
			const atIdx = authority.lastIndexOf("@");
			let hostPart = authority;
			if (atIdx !== -1) {
				const userinfo = authority.slice(0, atIdx);
				hostPart = authority.slice(atIdx + 1);
				const colonIdx = userinfo.indexOf(":");
				if (colonIdx !== -1) {
					result.username = userinfo.slice(0, colonIdx);
					result.password = userinfo.slice(colonIdx + 1);
				} else result.username = userinfo;
			}
			if (hostPart.startsWith("[")) {
				const bracketEnd = hostPart.indexOf("]");
				if (bracketEnd !== -1) {
					result.hostname = hostPart.slice(0, bracketEnd + 1);
					if (bracketEnd + 1 < hostPart.length && hostPart[bracketEnd + 1] === ":") result.port = hostPart.slice(bracketEnd + 2);
				} else result.hostname = hostPart;
			} else {
				const colonIdx = hostPart.lastIndexOf(":");
				if (colonIdx !== -1) {
					result.hostname = hostPart.slice(0, colonIdx);
					result.port = hostPart.slice(colonIdx + 1);
				} else result.hostname = hostPart;
			}
		}
		result.pathname = remaining || "/";
		return result;
	}
	/**
	* Parse a pattern string that may contain URL components.
	* Similar to parseURLString but designed for patterns where components
	* may contain wildcard and pattern syntax.
	*/
	function parsePatternString(pattern) {
		const result = {
			protocol: "*",
			username: "*",
			password: "*",
			hostname: "*",
			port: "*",
			pathname: "*",
			search: "*",
			hash: "*"
		};
		let remaining = pattern;
		const hashIdx = findUnescapedChar(remaining, "#");
		if (hashIdx !== -1) {
			result.hash = remaining.slice(hashIdx + 1);
			remaining = remaining.slice(0, hashIdx);
		}
		const searchIdx = findUnescapedChar(remaining, "?", true);
		if (searchIdx !== -1) {
			result.search = remaining.slice(searchIdx + 1);
			remaining = remaining.slice(0, searchIdx);
		}
		const protoEndIdx = remaining.indexOf("://");
		if (protoEndIdx !== -1) {
			result.protocol = remaining.slice(0, protoEndIdx);
			remaining = remaining.slice(protoEndIdx + 3);
			const slashIdx = remaining.indexOf("/");
			let authority;
			if (slashIdx !== -1) {
				authority = remaining.slice(0, slashIdx);
				remaining = remaining.slice(slashIdx);
			} else {
				authority = remaining;
				remaining = "";
			}
			const atIdx = authority.lastIndexOf("@");
			let hostPart = authority;
			if (atIdx !== -1) {
				const userinfo = authority.slice(0, atIdx);
				hostPart = authority.slice(atIdx + 1);
				const colonIdx = userinfo.indexOf(":");
				if (colonIdx !== -1) {
					result.username = userinfo.slice(0, colonIdx);
					result.password = userinfo.slice(colonIdx + 1);
				} else result.username = userinfo;
			}
			const portSep = findPortSeparator(hostPart);
			if (portSep !== -1) {
				result.hostname = hostPart.slice(0, portSep);
				result.port = hostPart.slice(portSep + 1);
			} else {
				result.hostname = hostPart;
				result.port = "*";
			}
		} else if (remaining.startsWith("/") || remaining.startsWith(":") || remaining.startsWith("*")) {
			result.protocol = "*";
			result.hostname = "*";
		}
		if (remaining) result.pathname = remaining;
		return result;
	}
	/**
	* Find a character in a string, skipping characters inside parentheses and braces.
	* For '?', `topLevelOnly` skips '?' that are part of {pattern}? syntax.
	*/
	function findUnescapedChar(str, ch, topLevelOnly = false) {
		let depth = 0;
		for (let i = 0; i < str.length; i++) {
			const c = str[i];
			if (c === "(" || c === "{") depth++;
			else if (c === ")" || c === "}") depth--;
			else if (c === ch && depth === 0) {
				if (topLevelOnly && ch === "?") {
					if (i > 0 && str[i - 1] === "}") continue;
				}
				return i;
			}
		}
		return -1;
	}
	/**
	* Find the colon that separates hostname from port in a host pattern.
	* Must distinguish between `host:port` and `host:name` patterns.
	* A port separator colon is one where the part after it looks like
	* a port (digits, *, or a named param).
	* We scan from the right side.
	*/
	function findPortSeparator(hostPart) {
		if (hostPart === "*" || hostPart.startsWith(":") && !hostPart.includes(".")) return -1;
		for (let i = hostPart.length - 1; i >= 0; i--) if (hostPart[i] === ":") {
			const after = hostPart.slice(i + 1);
			if (!hostPart.slice(0, i)) continue;
			if (/^\d+$/.test(after) || after === "*" || after.startsWith(":")) return i;
			if (after.startsWith("(")) return i;
		}
		return -1;
	}
	var URLPattern = class {
		/**
		* Create a new URLPattern.
		*
		* @param input - A pattern string (like "https://example.com/:path") or
		*   an object with component patterns ({pathname: "/users/:id"})
		* @param baseURL - Optional base URL string (only when input is a string)
		*/
		constructor(input, baseURL) {
			_defineProperty(this, "_components", void 0);
			let patterns;
			if (typeof input === "string") if (baseURL) {
				const baseParts = parsePatternString(baseURL);
				const inputParts = parsePatternString(input);
				patterns = { ...baseParts };
				{
					const __exactForOfIterator71 = COMPONENTS[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep71 = __exactForOfIterator71.next();
						if (__exactForOfStep71.done) break;
						const comp = __exactForOfStep71.value;
						if (inputParts[comp] !== "*" || comp === "pathname") patterns[comp] = inputParts[comp];
					}
				}
			} else patterns = parsePatternString(input);
			else {
				patterns = { ...DEFAULT_PATTERNS };
				if (input.baseURL) {
					const baseParts = parsePatternString(input.baseURL);
					for (let i = 0; i < COMPONENTS.length; i++) {
						const comp = COMPONENTS[i];
						patterns[comp] = baseParts[comp];
					}
				}
				for (let i = 0; i < COMPONENTS.length; i++) {
					const comp = COMPONENTS[i];
					if (input[comp] !== void 0) patterns[comp] = input[comp];
				}
			}
			this._components = {};
			for (let i = 0; i < COMPONENTS.length; i++) {
				const comp = COMPONENTS[i];
				this._components[comp] = compileComponent(patterns[comp], comp);
			}
		}
		/**
		* The compiled pattern string for the protocol component.
		*/
		get protocol() {
			return this._components.protocol.pattern;
		}
		/**
		* The compiled pattern string for the username component.
		*/
		get username() {
			return this._components.username.pattern;
		}
		/**
		* The compiled pattern string for the password component.
		*/
		get password() {
			return this._components.password.pattern;
		}
		/**
		* The compiled pattern string for the hostname component.
		*/
		get hostname() {
			return this._components.hostname.pattern;
		}
		/**
		* The compiled pattern string for the port component.
		*/
		get port() {
			return this._components.port.pattern;
		}
		/**
		* The compiled pattern string for the pathname component.
		*/
		get pathname() {
			return this._components.pathname.pattern;
		}
		/**
		* The compiled pattern string for the search component.
		*/
		get search() {
			return this._components.search.pattern;
		}
		/**
		* The compiled pattern string for the hash component.
		*/
		get hash() {
			return this._components.hash.pattern;
		}
		/**
		* Test whether a URL matches this pattern.
		*
		* @param input - A URL string or URLPatternInit to test
		* @param baseURL - Optional base URL (only when input is a string)
		* @returns true if the URL matches the pattern
		*/
		test(input, baseURL) {
			return this.exec(input, baseURL) !== null;
		}
		/**
		* Execute this pattern against a URL, returning match details or null.
		*
		* @param input - A URL string or URLPatternInit to match
		* @param baseURL - Optional base URL (only when input is a string)
		* @returns URLPatternResult with match details, or null if no match
		*/
		exec(input, baseURL) {
			let urlComponents;
			try {
				urlComponents = parseURLInput(input, baseURL);
			} catch {
				return null;
			}
			const result = {};
			if (baseURL) result.inputs = [input, baseURL];
			else result.inputs = [input];
			for (const comp of COMPONENTS) {
				const compiled = this._components[comp];
				const value = urlComponents[comp];
				const match = compiled.regex.exec(value);
				if (!match) return null;
				const groups = {};
				for (let i = 0; i < compiled.groupNames.length; i++) groups[compiled.groupNames[i]] = match[i + 1];
				result[comp] = {
					input: value,
					groups
				};
			}
			return result;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/messaging.ts
	/**
	* MessagePort and MessageChannel - Web Standard Messaging API
	*
	* Provides in-process message passing between ports via the structured clone
	* algorithm.  Messages are always delivered asynchronously (via queueMicrotask)
	* to match the HTML specification behaviour.
	*
	* @see https://html.spec.whatwg.org/multipage/web-messaging.html#message-channels
	* @see https://html.spec.whatwg.org/multipage/web-messaging.html#message-ports
	*/
	init_defineProperty();
	let _Symbol$toStringTag$19, _Symbol$toStringTag2;
	var _remotePort = /* @__PURE__ */ new WeakMap();
	var _started = /* @__PURE__ */ new WeakMap();
	var _closed$1 = /* @__PURE__ */ new WeakMap();
	var _queue = /* @__PURE__ */ new WeakMap();
	var _onmessage = /* @__PURE__ */ new WeakMap();
	var _onmessageerror = /* @__PURE__ */ new WeakMap();
	var _MessagePort_brand = /* @__PURE__ */ new WeakSet();
	_Symbol$toStringTag$19 = Symbol.toStringTag;
	/**
	* A MessagePort represents one end of a two-port message channel.
	*
	* The port must be *started* before it will dispatch queued messages.
	* Setting the `onmessage` property implicitly calls `start()` (per spec).
	* Using `addEventListener('message', ...)` does NOT auto-start; the caller
	* must invoke `start()` explicitly.
	*/
	var MessagePort = class MessagePort extends EventTarget {
		constructor(..._args) {
			super(..._args);
			_classPrivateMethodInitSpec(this, _MessagePort_brand);
			_classPrivateFieldInitSpec(this, _remotePort, null);
			_classPrivateFieldInitSpec(this, _started, false);
			_classPrivateFieldInitSpec(this, _closed$1, false);
			_classPrivateFieldInitSpec(this, _queue, []);
			_classPrivateFieldInitSpec(this, _onmessage, null);
			_classPrivateFieldInitSpec(this, _onmessageerror, null);
		}
		/**
		* Setting `onmessage` implicitly calls `start()` per the HTML spec.
		*/
		get onmessage() {
			return _classPrivateFieldGet2(_onmessage, this);
		}
		set onmessage(handler) {
			_classPrivateFieldSet2(_onmessage, this, handler);
			this.start();
		}
		get onmessageerror() {
			return _classPrivateFieldGet2(_onmessageerror, this);
		}
		set onmessageerror(handler) {
			_classPrivateFieldSet2(_onmessageerror, this, handler);
		}
		/**
		* Post a message through this port to the entangled remote port.
		*
		* The `value` is cloned via `structuredClone` before delivery so that the
		* sender and receiver hold independent copies.
		*
		* @param value  The message payload (must be structured-cloneable).
		* @param transferList  Optional transfer list for ArrayBuffer ownership move.
		*/
		postMessage(value, transferList) {
			if (_classPrivateFieldGet2(_closed$1, this)) return;
			const remote = _classPrivateFieldGet2(_remotePort, this);
			if (!remote || _classPrivateFieldGet2(_closed$1, remote)) return;
			let clonedData;
			try {
				clonedData = structuredClone(value, { transfer: transferList });
			} catch (err) {
				queueMicrotask(() => {
					if (!_classPrivateFieldGet2(_closed$1, remote)) _assertClassBrand(_MessagePort_brand, remote, _dispatchMessageError$1).call(remote, err);
				});
				return;
			}
			if (_classPrivateFieldGet2(_started, remote)) queueMicrotask(() => {
				if (!_classPrivateFieldGet2(_closed$1, remote)) _assertClassBrand(_MessagePort_brand, remote, _dispatchMessage).call(remote, clonedData);
			});
			else _classPrivateFieldGet2(_queue, remote).push(clonedData);
		}
		/**
		* Begin dispatching messages.  Messages posted before `start()` is called
		* are buffered and flushed when start is invoked.
		*/
		start() {
			if (_classPrivateFieldGet2(_started, this)) return;
			_classPrivateFieldSet2(_started, this, true);
			if (_classPrivateFieldGet2(_queue, this).length > 0) {
				const pending = _classPrivateFieldGet2(_queue, this).splice(0);
				{
					const __exactForOfIterator72 = pending[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep72 = __exactForOfIterator72.next();
						if (__exactForOfStep72.done) break;
						const data = __exactForOfStep72.value;
						queueMicrotask(() => {
							if (!_classPrivateFieldGet2(_closed$1, this)) _assertClassBrand(_MessagePort_brand, this, _dispatchMessage).call(this, data);
						});
					}
				}
			}
		}
		/**
		* Disentangle this port.  After calling `close()`, no more messages will
		* be sent or received through this port.
		*/
		close() {
			_classPrivateFieldSet2(_closed$1, this, true);
			_classPrivateFieldGet2(_queue, this).length = 0;
			_classPrivateFieldSet2(_remotePort, this, null);
			_classPrivateFieldSet2(_onmessage, this, null);
			_classPrivateFieldSet2(_onmessageerror, this, null);
		}
		/** @internal Wire this port to its remote partner. */
		_setRemotePort(remote) {
			_classPrivateFieldSet2(_remotePort, this, remote);
		}
		[structuredCloneTransferSymbol]() {
			const transferredPort = new MessagePort();
			const remote = _classPrivateFieldGet2(_remotePort, this);
			_classPrivateFieldSet2(_queue, transferredPort, _classPrivateFieldGet2(_queue, this));
			_classPrivateFieldSet2(_started, transferredPort, _classPrivateFieldGet2(_started, this));
			if (remote && !_classPrivateFieldGet2(_closed$1, remote)) {
				transferredPort._setRemotePort(remote);
				remote._setRemotePort(transferredPort);
			}
			_classPrivateFieldSet2(_queue, this, []);
			_classPrivateFieldSet2(_started, this, false);
			_classPrivateFieldSet2(_closed$1, this, true);
			_classPrivateFieldSet2(_remotePort, this, null);
			_classPrivateFieldSet2(_onmessage, this, null);
			_classPrivateFieldSet2(_onmessageerror, this, null);
			return transferredPort;
		}
		get [_Symbol$toStringTag$19]() {
			return "MessagePort";
		}
	};
	function _dispatchMessage(data) {
		const event = new MessageEvent$1("message", { data });
		if (_classPrivateFieldGet2(_onmessage, this)) try {
			_classPrivateFieldGet2(_onmessage, this).call(this, event);
		} catch (error) {
			console.error("[MessagePort] onmessage handler threw:", error);
		}
		this.dispatchEvent(event);
	}
	function _dispatchMessageError$1(error) {
		const event = new MessageEvent$1("messageerror", { data: error });
		if (_classPrivateFieldGet2(_onmessageerror, this)) try {
			_classPrivateFieldGet2(_onmessageerror, this).call(this, event);
		} catch (e) {
			console.error("[MessagePort] onmessageerror handler threw:", e);
		}
		this.dispatchEvent(event);
	}
	_Symbol$toStringTag2 = Symbol.toStringTag;
	/**
	* A MessageChannel creates a pair of entangled MessagePort objects.  Messages
	* posted on one port are delivered to the other and vice versa.
	*/
	var MessageChannel = class {
		constructor() {
			_defineProperty(this, "port1", void 0);
			_defineProperty(this, "port2", void 0);
			this.port1 = new MessagePort();
			this.port2 = new MessagePort();
			this.port1._setRemotePort(this.port2);
			this.port2._setRemotePort(this.port1);
		}
		get [_Symbol$toStringTag2]() {
			return "MessageChannel";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/media/VideoFrame.ts
	init_defineProperty();
	let _Symbol$toStringTag$18;
	_Symbol$toStringTag$18 = Symbol.toStringTag;
	var VideoFrame = class VideoFrame {
		constructor(data, init = {}) {
			_defineProperty(this, "_exactData", void 0);
			_defineProperty(this, "_exactClosed", false);
			_defineProperty(this, "format", void 0);
			_defineProperty(this, "timestamp", void 0);
			_defineProperty(this, "codedWidth", void 0);
			_defineProperty(this, "codedHeight", void 0);
			this._exactData = data;
			this.format = init.format ?? null;
			this.timestamp = init.timestamp;
			this.codedWidth = init.codedWidth;
			this.codedHeight = init.codedHeight;
		}
		close() {
			this._exactClosed = true;
			this.format = null;
		}
		[structuredCloneCloneSymbol]() {
			if (this._exactClosed || this.format === null) throw createDataCloneError();
			return new VideoFrame(this._exactData, {
				format: this.format,
				timestamp: this.timestamp,
				codedWidth: this.codedWidth,
				codedHeight: this.codedHeight
			});
		}
		[structuredCloneTransferSymbol]() {
			const clone = this[structuredCloneCloneSymbol]();
			this.close();
			return clone;
		}
		get [_Symbol$toStringTag$18]() {
			return "VideoFrame";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/broadcast/BroadcastChannel.ts
	/**
	* BroadcastChannel - Cross-context messaging API
	* 
	* Allows communication between different browsing contexts (views, windows)
	* with the same origin. In Exact, this enables communication between
	* different views within the same app.
	* 
	* @see https://html.spec.whatwg.org/multipage/web-messaging.html#broadcasting-to-other-browsing-contexts
	*/
	init_defineProperty();
	let _Symbol$toStringTag$17;
	/**
	* Registry of all active BroadcastChannel instances by channel name.
	* This enables message delivery within the same runtime.
	* 
	* For cross-view messaging, the native layer should call
	* `BroadcastChannel._deliverMessage(name, data)` when a message arrives
	* from another view.
	*/
	const channelRegistry = /* @__PURE__ */ new Map();
	var _closed = /* @__PURE__ */ new WeakMap();
	var _BroadcastChannel_brand = /* @__PURE__ */ new WeakSet();
	_Symbol$toStringTag$17 = Symbol.toStringTag;
	/**
	* BroadcastChannel enables simple communication between browsing contexts
	* (views, windows, tabs) with the same origin.
	* 
	* Messages sent via postMessage() are delivered to all other BroadcastChannel
	* objects with the same name, but NOT to the sender.
	* 
	* @example
	* ```ts
	* // In View A
	* const channel = new BroadcastChannel('my-channel');
	* channel.postMessage({ type: 'update', data: 'hello' });
	* 
	* // In View B
	* const channel = new BroadcastChannel('my-channel');
	* channel.onmessage = (event) => {
	*   console.log('Received:', event.data);
	* };
	* ```
	*/
	var BroadcastChannel = class extends EventTarget {
		constructor(name) {
			super();
			_classPrivateMethodInitSpec(this, _BroadcastChannel_brand);
			_defineProperty(this, "name", void 0);
			_classPrivateFieldInitSpec(this, _closed, false);
			_defineProperty(this, "onmessage", null);
			_defineProperty(this, "onmessageerror", null);
			this.name = String(name);
			let channels = channelRegistry.get(this.name);
			if (!channels) {
				channels = /* @__PURE__ */ new Set();
				channelRegistry.set(this.name, channels);
			}
			channels.add(this);
		}
		/**
		* Send a message to all other BroadcastChannel objects with the same name.
		* The message is NOT delivered to this channel instance.
		* 
		* @param message - The message to send (will be structured cloned)
		* @throws DOMException if the channel is closed
		*/
		postMessage(message) {
			if (_classPrivateFieldGet2(_closed, this)) throw new DOMException("BroadcastChannel is closed", "InvalidStateError");
			let clonedMessage;
			try {
				clonedMessage = structuredClone(message);
			} catch (error) {
				_assertClassBrand(_BroadcastChannel_brand, this, _dispatchMessageError).call(this, error);
				return;
			}
			const channels = channelRegistry.get(this.name);
			if (channels) {
				const __exactForOfIterator73 = channels[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep73 = __exactForOfIterator73.next();
					if (__exactForOfStep73.done) break;
					const channel = __exactForOfStep73.value;
					if (channel !== this && !_classPrivateFieldGet2(_closed, channel)) queueMicrotask(() => {
						if (!_classPrivateFieldGet2(_closed, channel)) _assertClassBrand(_BroadcastChannel_brand, channel, _receiveMessage).call(channel, clonedMessage);
					});
				}
			}
		}
		/**
		* Close this BroadcastChannel. After closing, postMessage() will throw
		* and no more messages will be received.
		*/
		close() {
			if (_classPrivateFieldGet2(_closed, this)) return;
			_classPrivateFieldSet2(_closed, this, true);
			const channels = channelRegistry.get(this.name);
			if (channels) {
				channels.delete(this);
				if (channels.size === 0) channelRegistry.delete(this.name);
			}
		}
		/**
		* Static method for native layer to deliver messages from other contexts.
		* This should be called when a message arrives from another view.
		* 
		* @param channelName - The channel name
		* @param data - The message data (already cloned by sender)
		*/
		static _deliverMessage(channelName, data) {
			const channels = channelRegistry.get(channelName);
			if (channels) {
				const __exactForOfIterator74 = channels[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep74 = __exactForOfIterator74.next();
					if (__exactForOfStep74.done) break;
					const channel = __exactForOfStep74.value;
					if (!_classPrivateFieldGet2(_closed, channel)) queueMicrotask(() => {
						if (!_classPrivateFieldGet2(_closed, channel)) _assertClassBrand(_BroadcastChannel_brand, channel, _receiveMessage).call(channel, data);
					});
				}
			}
		}
		/**
		* Static method to get the count of active channels for a name.
		* Useful for debugging and testing.
		*/
		static _getChannelCount(channelName) {
			return channelRegistry.get(channelName)?.size ?? 0;
		}
		/**
		* Static method to get all active channel names.
		* Useful for debugging.
		*/
		static _getChannelNames() {
			return Array.from(channelRegistry.keys());
		}
		get [_Symbol$toStringTag$17]() {
			return "BroadcastChannel";
		}
	};
	/**
	* Internal method to receive a message.
	* Called when a message arrives from another channel.
	*/
	function _receiveMessage(data) {
		if (_classPrivateFieldGet2(_closed, this)) return;
		const event = new MessageEvent$1("message", {
			data,
			origin: ""
		});
		if (this.onmessage) try {
			this.onmessage.call(this, event);
		} catch (error) {
			console.error("[BroadcastChannel] onmessage handler threw:", error);
		}
		this.dispatchEvent(event);
	}
	/**
	* Dispatch a messageerror event when message cloning fails.
	*/
	function _dispatchMessageError(error) {
		const channels = channelRegistry.get(this.name);
		if (channels) {
			const __exactForOfIterator75 = channels[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep75 = __exactForOfIterator75.next();
				if (__exactForOfStep75.done) break;
				const channel = __exactForOfStep75.value;
				if (channel !== this && !_classPrivateFieldGet2(_closed, channel)) queueMicrotask(() => {
					if (!_classPrivateFieldGet2(_closed, channel)) {
						const event = new MessageEvent$1("messageerror", { data: error });
						if (channel.onmessageerror) try {
							channel.onmessageerror.call(channel, event);
						} catch (e) {
							console.error("[BroadcastChannel] onmessageerror handler threw:", e);
						}
						channel.dispatchEvent(event);
					}
				});
			}
		}
	}
	//#endregion
	//#region ../ibex-runtime-js/src/websocket/WebSocket.ts
	/**
	* WebSocket implementation for Exact runtime
	*
	* Implements the WHATWG WebSocket API.
	* @see https://websockets.spec.whatwg.org/
	*
	* NOTE: Uses _prefix private state for compatibility with the embedded runtimes
	* we support, including Hermes-based builds.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$16;
	const CONNECTING$1 = 0;
	const OPEN$1 = 1;
	const CLOSING = 2;
	const CLOSED$1 = 3;
	const CLOSE_NO_STATUS = 1005;
	const CLOSE_ABNORMAL = 1006;
	function getBaseUrl() {
		const currentLocation = globalThis.location;
		if (currentLocation && typeof currentLocation.href === "string") return currentLocation.href;
	}
	function isSecureContextForWebSocket() {
		const currentLocation = globalThis.location;
		return !!currentLocation && currentLocation.protocol === "https:";
	}
	function isBlobLike(value) {
		return !!value && typeof value === "object" && typeof value.arrayBuffer === "function" && typeof value.size === "number";
	}
	function isValidWebSocketSubprotocol(value) {
		return /^[!#$%&'*+\-.^_`|~0-9A-Za-z]+$/.test(value);
	}
	function assertWebSocketBrand(value) {
		if (!(value instanceof WebSocket)) throw new TypeError("Illegal invocation");
		return value;
	}
	_Symbol$toStringTag$16 = Symbol.toStringTag;
	/**
	* WebSocket provides full-duplex communication over a single TCP connection.
	*/
	var WebSocket = class extends EventTarget {
		constructor(url, protocols) {
			super();
			_defineProperty(this, "_url", "");
			_defineProperty(this, "_protocol", "");
			_defineProperty(this, "_extensions", "");
			_defineProperty(this, "_readyState", CONNECTING$1);
			_defineProperty(this, "_bufferedAmount", 0);
			_defineProperty(this, "_binaryType", "blob");
			_defineProperty(this, "_socketId", -1);
			_defineProperty(this, "_closeEventPending", false);
			_defineProperty(this, "_incomingPaused", false);
			_defineProperty(this, "_incomingFlowControlled", false);
			_defineProperty(this, "_sendQueue", []);
			_defineProperty(this, "_isSendingQueue", false);
			_defineProperty(this, "_pendingSendAcks", []);
			_defineProperty(this, "_eventQueue", []);
			_defineProperty(this, "_eventQueueScheduled", false);
			_defineProperty(this, "_onopen", null);
			_defineProperty(this, "_onmessage", null);
			_defineProperty(this, "_onerror", null);
			_defineProperty(this, "_onclose", null);
			requireCapability(Capabilities.NETWORK_CONNECT);
			const urlString = url instanceof URL ? url.href : String(url);
			if (urlString.includes("#")) throw new DOMException("The URL contains a fragment identifier, which is not allowed in WebSocket URLs.", "SyntaxError");
			let parsedUrl;
			try {
				parsedUrl = new URL(urlString, getBaseUrl());
			} catch {
				throw new DOMException("Invalid URL", "SyntaxError");
			}
			if (parsedUrl.protocol === "http:") parsedUrl.protocol = "ws:";
			else if (parsedUrl.protocol === "https:") parsedUrl.protocol = "wss:";
			else if (parsedUrl.protocol !== "ws:" && parsedUrl.protocol !== "wss:") throw new DOMException(`The URL's scheme must be either 'ws' or 'wss'. '${parsedUrl.protocol}' is not allowed.`, "SyntaxError");
			if (isSecureContextForWebSocket() && parsedUrl.protocol === "ws:") throw new DOMException("Cannot construct an insecure WebSocket from a secure context.", "SecurityError");
			const protocolList = protocols !== void 0 ? Array.isArray(protocols) ? protocols : [protocols] : [];
			const seen = /* @__PURE__ */ new Set();
			{
				const __exactForOfIterator76 = protocolList[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep76 = __exactForOfIterator76.next();
					if (__exactForOfStep76.done) break;
					const protocol = __exactForOfStep76.value;
					if (protocol === "") throw new DOMException("Invalid protocol: empty string", "SyntaxError");
					const normalizedProtocol = String(protocol);
					const seenKey = normalizedProtocol.toLowerCase();
					if (seen.has(seenKey)) throw new DOMException(`Invalid protocol: duplicate '${protocol}'`, "SyntaxError");
					if (!isValidWebSocketSubprotocol(normalizedProtocol)) throw new DOMException(`Invalid protocol: '${protocol}'`, "SyntaxError");
					seen.add(seenKey);
				}
			}
			this._url = parsedUrl.href;
			const nativeWebSocket = getNativeModule("websocket");
			if (nativeWebSocket) this._connectNative(nativeWebSocket, protocolList);
			else this._simulateConnection();
		}
		get readyState() {
			return assertWebSocketBrand(this)._readyState;
		}
		get url() {
			return assertWebSocketBrand(this)._url;
		}
		get protocol() {
			return assertWebSocketBrand(this)._protocol;
		}
		get extensions() {
			return assertWebSocketBrand(this)._extensions;
		}
		get bufferedAmount() {
			return assertWebSocketBrand(this)._bufferedAmount;
		}
		get binaryType() {
			return assertWebSocketBrand(this)._binaryType;
		}
		set binaryType(value) {
			const socket = assertWebSocketBrand(this);
			if (value !== "blob" && value !== "arraybuffer") return;
			socket._binaryType = value;
		}
		get onopen() {
			return assertWebSocketBrand(this)._onopen;
		}
		set onopen(value) {
			assertWebSocketBrand(this)._onopen = typeof value === "function" ? value : null;
		}
		get onmessage() {
			return assertWebSocketBrand(this)._onmessage;
		}
		set onmessage(value) {
			assertWebSocketBrand(this)._onmessage = typeof value === "function" ? value : null;
		}
		get onerror() {
			return assertWebSocketBrand(this)._onerror;
		}
		set onerror(value) {
			assertWebSocketBrand(this)._onerror = typeof value === "function" ? value : null;
		}
		get onclose() {
			return assertWebSocketBrand(this)._onclose;
		}
		set onclose(value) {
			assertWebSocketBrand(this)._onclose = typeof value === "function" ? value : null;
		}
		_enqueueEventTask(task) {
			this._eventQueue.push(task);
			if (this._eventQueueScheduled) return;
			this._eventQueueScheduled = true;
			setTimeout(() => {
				this._eventQueueScheduled = false;
				while (this._eventQueue.length > 0) {
					const next = this._eventQueue.shift();
					if (next) next();
				}
			}, 0);
		}
		/**
		* Send data through the WebSocket connection.
		*/
		send(data) {
			const socket = assertWebSocketBrand(this);
			if (arguments.length === 0) throw new TypeError("Failed to execute 'send' on 'WebSocket': 1 argument required, but only 0 present.");
			if (socket._readyState === CONNECTING$1) throw new DOMException("WebSocket is still in CONNECTING state", "InvalidStateError");
			if (socket._readyState !== OPEN$1) return;
			if (!getNativeModule("websocket") || socket._socketId === -1) return;
			if (isBlobLike(data)) {
				const byteSize = data.size;
				socket._bufferedAmount += byteSize;
				socket._queueSend(async () => {
					if (socket._readyState !== OPEN$1) return;
					const buffer = await data.arrayBuffer();
					if (socket._readyState !== OPEN$1) return;
					await socket._sendNative(new Uint8Array(buffer), byteSize);
				});
				return;
			}
			let sendData;
			let byteSize;
			if (typeof data === "string") {
				sendData = data;
				byteSize = new TextEncoder().encode(data).byteLength;
				socket._bufferedAmount += byteSize;
			} else if (data instanceof ArrayBuffer) {
				sendData = new Uint8Array(data);
				byteSize = data.byteLength;
				socket._bufferedAmount += byteSize;
			} else if (ArrayBuffer.isView(data)) {
				sendData = new Uint8Array(data.buffer, data.byteOffset, data.byteLength);
				byteSize = data.byteLength;
				socket._bufferedAmount += byteSize;
			} else {
				sendData = String(data);
				byteSize = new TextEncoder().encode(sendData).byteLength;
				socket._bufferedAmount += byteSize;
			}
			socket._queueSend(async () => {
				if (socket._readyState !== OPEN$1) return;
				await socket._sendNative(sendData, byteSize);
			});
		}
		/**
		* Queue a send operation to maintain ordering.
		*/
		async _queueSend(sendFn) {
			this._sendQueue.push(sendFn);
			if (this._isSendingQueue) return;
			this._isSendingQueue = true;
			while (this._sendQueue.length > 0) {
				const fn = this._sendQueue.shift();
				try {
					await fn();
				} catch (e) {}
			}
			this._isSendingQueue = false;
		}
		_sendNative(data, byteSize) {
			const nativeWebSocket = getNativeModule("websocket");
			if (!nativeWebSocket || this._socketId === -1 || this._readyState !== OPEN$1) return Promise.resolve();
			return new Promise((resolve) => {
				if (byteSize === 0) {
					try {
						nativeWebSocket.send(this._socketId, data);
					} catch (_sendZeroErr) {}
					resolve();
					return;
				}
				this._pendingSendAcks.push({
					remaining: byteSize,
					resolve
				});
				try {
					nativeWebSocket.send(this._socketId, data);
				} catch (_sendErr) {
					this._bufferedAmount = Math.max(0, this._bufferedAmount - byteSize);
					this._pendingSendAcks.pop()?.resolve();
				}
			});
		}
		_resolvePendingSendAcks() {
			while (this._pendingSendAcks.length > 0) this._pendingSendAcks.shift()?.resolve();
		}
		_pauseIncoming() {
			if (this._incomingPaused || this._socketId === -1) return;
			getNativeModule("websocket")?.pause?.(this._socketId);
			this._incomingPaused = true;
		}
		_resumeIncoming() {
			if (!this._incomingPaused || this._socketId === -1) return;
			getNativeModule("websocket")?.resume?.(this._socketId);
			this._incomingPaused = false;
		}
		_setIncomingFlowControl(enabled) {
			const nextEnabled = !!enabled;
			this._incomingFlowControlled = nextEnabled;
			if (this._socketId === -1) return;
			getNativeModule("websocket")?.setFlowControlled?.(this._socketId, nextEnabled);
		}
		/**
		* Close the WebSocket connection.
		*/
		close(code, reason) {
			const socket = assertWebSocketBrand(this);
			if (reason !== void 0 && code === void 0) throw new DOMException("Close reason must not be provided without a close code.", "InvalidAccessError");
			if (code !== void 0) {
				if (typeof code !== "number" || !Number.isInteger(code)) throw new DOMException(`Invalid close code: ${String(code)}. Must be an integer.`, "InvalidAccessError");
				if (code !== 1e3 && (code < 3e3 || code > 4999)) throw new DOMException(`Invalid close code: ${code}. Must be 1000 or in range 3000-4999.`, "InvalidAccessError");
			}
			if (reason !== void 0) {
				if (new TextEncoder().encode(reason).byteLength > 123) throw new DOMException("Close reason is too long (max 123 bytes)", "SyntaxError");
			}
			if (socket._readyState === CLOSING || socket._readyState === CLOSED$1) return;
			socket._readyState = CLOSING;
			const nativeWebSocket = getNativeModule("websocket");
			if (nativeWebSocket && socket._socketId !== -1) nativeWebSocket.close(socket._socketId, code ?? CLOSE_NO_STATUS, reason ?? "");
			else queueMicrotask(() => {
				socket._handleCloseInternal(code ?? CLOSE_NO_STATUS, reason ?? "", true);
			});
		}
		/**
		* Connect using native WebSocket module.
		*/
		_connectNative(nativeWebSocket, protocols) {
			const self = this;
			const bridge = {
				_handleOpen: (protocol, extensions) => self._handleOpen(protocol, extensions),
				_handleMessage: (data) => self._handleMessage(data),
				_handleClose: (code, reason, wasClean) => self._handleClose(code, reason, wasClean),
				_handleError: (message) => self._handleError(message),
				_handleBytesSent: (bytesSent) => self._handleBytesSent(bytesSent)
			};
			this._socketId = nativeWebSocket.create(this.url, protocols, bridge);
		}
		/**
		* Simulate connection for testing without native module.
		*/
		_simulateConnection() {
			queueMicrotask(() => {
				setTimeout(() => {
					this._handleErrorInternal("WebSocket connection failed: no native module");
					this._handleCloseInternal(CLOSE_ABNORMAL, "", false);
				}, 100);
			});
		}
		/**
		* Handle incoming message (called by native bridge).
		*/
		_handleMessage(data) {
			if (this._readyState === CLOSED$1) return;
			if (this._incomingFlowControlled) this._incomingPaused = true;
			this._enqueueEventTask(() => {
				if (this._readyState === CLOSED$1) return;
				let messageData;
				if (typeof data === "string") messageData = data;
				else if (this._binaryType === "arraybuffer") messageData = data;
				else messageData = new (globalThis.Blob || Blob$1)([data]);
				const event = new MessageEvent$1("message", {
					data: messageData,
					origin: new URL(this.url).origin
				});
				if (this._onmessage) this._onmessage.call(this, event);
				this.dispatchEvent(event);
			});
		}
		/**
		* Handle connection open (called by native bridge).
		*/
		_handleOpen(protocol, extensions) {
			if (this._readyState !== CONNECTING$1) return;
			this._readyState = OPEN$1;
			this._protocol = protocol || "";
			this._extensions = extensions || "";
			this._enqueueEventTask(() => {
				const event = new Event("open");
				if (this._onopen) this._onopen.call(this, event);
				this.dispatchEvent(event);
			});
		}
		/**
		* Handle error (internal).
		*/
		_handleErrorInternal(message) {
			this._enqueueEventTask(() => {
				const event = new ErrorEvent("error", { message });
				if (this._onerror) this._onerror.call(this, event);
				this.dispatchEvent(event);
			});
		}
		/**
		* Handle connection close (internal).
		*/
		_handleCloseInternal(code, reason, wasClean) {
			if (this._readyState === CLOSED$1 || this._closeEventPending) return;
			this._readyState = CLOSING;
			this._closeEventPending = true;
			setTimeout(() => {
				this._enqueueEventTask(() => {
					if (this._readyState === CLOSED$1) return;
					this._readyState = CLOSED$1;
					this._closeEventPending = false;
					this._bufferedAmount = 0;
					this._incomingPaused = false;
					this._incomingFlowControlled = false;
					this._resolvePendingSendAcks();
					const event = new CloseEvent("close", {
						code,
						reason,
						wasClean
					});
					if (this._onclose) this._onclose.call(this, event);
					this.dispatchEvent(event);
				});
			}, 0);
		}
		/**
		* Public method for native bridge to call on close.
		*/
		_handleClose(code, reason, wasClean) {
			this._handleCloseInternal(code, reason, wasClean);
		}
		/**
		* Public method for native bridge to call on error.
		*/
		_handleError(message) {
			this._handleErrorInternal(message);
		}
		/**
		* Public method for native bridge to call when data has been sent.
		* This allows bufferedAmount to decrease as data is transmitted.
		* @param bytesSent Number of bytes that have been sent to the network
		*/
		_handleBytesSent(bytesSent) {
			this._bufferedAmount = Math.max(0, this._bufferedAmount - bytesSent);
			let remainingBytes = bytesSent;
			while (remainingBytes > 0 && this._pendingSendAcks.length > 0) {
				const current = this._pendingSendAcks[0];
				if (remainingBytes >= current.remaining) {
					remainingBytes -= current.remaining;
					this._pendingSendAcks.shift();
					current.resolve();
				} else {
					current.remaining -= remainingBytes;
					remainingBytes = 0;
				}
			}
		}
		get [_Symbol$toStringTag$16]() {
			return "WebSocket";
		}
	};
	_defineProperty(WebSocket, "CONNECTING", CONNECTING$1);
	_defineProperty(WebSocket, "OPEN", OPEN$1);
	_defineProperty(WebSocket, "CLOSING", CLOSING);
	_defineProperty(WebSocket, "CLOSED", CLOSED$1);
	Object.defineProperty(WebSocket, "length", {
		value: 1,
		configurable: true
	});
	Object.defineProperty(WebSocket.prototype.close, "length", {
		value: 0,
		configurable: true
	});
	function nameAccessor(descriptor, propertyName) {
		if (!descriptor) return descriptor;
		if (typeof descriptor.get === "function") Object.defineProperty(descriptor.get, "name", {
			value: `get ${propertyName}`,
			configurable: true
		});
		if (typeof descriptor.set === "function") Object.defineProperty(descriptor.set, "name", {
			value: `set ${propertyName}`,
			configurable: true
		});
		return descriptor;
	}
	const urlDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "url"), "url");
	const readyStateDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "readyState"), "readyState");
	const bufferedAmountDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "bufferedAmount"), "bufferedAmount");
	const onopenDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "onopen"), "onopen");
	const onerrorDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "onerror"), "onerror");
	const oncloseDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "onclose"), "onclose");
	const extensionsDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "extensions"), "extensions");
	const protocolDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "protocol"), "protocol");
	const onmessageDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "onmessage"), "onmessage");
	const binaryTypeDescriptor = nameAccessor(Object.getOwnPropertyDescriptor(WebSocket.prototype, "binaryType"), "binaryType");
	Object.defineProperties(WebSocket, {
		CONNECTING: {
			value: CONNECTING$1,
			writable: false,
			enumerable: true,
			configurable: false
		},
		OPEN: {
			value: OPEN$1,
			writable: false,
			enumerable: true,
			configurable: false
		},
		CLOSING: {
			value: CLOSING,
			writable: false,
			enumerable: true,
			configurable: false
		},
		CLOSED: {
			value: CLOSED$1,
			writable: false,
			enumerable: true,
			configurable: false
		}
	});
	Object.defineProperties(WebSocket.prototype, {
		CONNECTING: {
			value: CONNECTING$1,
			writable: false,
			enumerable: true,
			configurable: false
		},
		OPEN: {
			value: OPEN$1,
			writable: false,
			enumerable: true,
			configurable: false
		},
		CLOSING: {
			value: CLOSING,
			writable: false,
			enumerable: true,
			configurable: false
		},
		CLOSED: {
			value: CLOSED$1,
			writable: false,
			enumerable: true,
			configurable: false
		},
		url: {
			get: urlDescriptor.get,
			enumerable: true,
			configurable: true
		},
		readyState: {
			get: readyStateDescriptor.get,
			enumerable: true,
			configurable: true
		},
		bufferedAmount: {
			get: bufferedAmountDescriptor.get,
			enumerable: true,
			configurable: true
		},
		onopen: {
			get: onopenDescriptor.get,
			set: onopenDescriptor.set,
			enumerable: true,
			configurable: true
		},
		onerror: {
			get: onerrorDescriptor.get,
			set: onerrorDescriptor.set,
			enumerable: true,
			configurable: true
		},
		onclose: {
			get: oncloseDescriptor.get,
			set: oncloseDescriptor.set,
			enumerable: true,
			configurable: true
		},
		extensions: {
			get: extensionsDescriptor.get,
			enumerable: true,
			configurable: true
		},
		protocol: {
			get: protocolDescriptor.get,
			enumerable: true,
			configurable: true
		},
		close: {
			value: WebSocket.prototype.close,
			writable: true,
			enumerable: true,
			configurable: true
		},
		onmessage: {
			get: onmessageDescriptor.get,
			set: onmessageDescriptor.set,
			enumerable: true,
			configurable: true
		},
		binaryType: {
			get: binaryTypeDescriptor.get,
			set: binaryTypeDescriptor.set,
			enumerable: true,
			configurable: true
		},
		send: {
			value: WebSocket.prototype.send,
			writable: true,
			enumerable: true,
			configurable: true
		}
	});
	Object.setPrototypeOf(WebSocket.prototype, EventTarget.prototype);
	Object.setPrototypeOf(WebSocket, EventTarget);
	//#endregion
	//#region ../ibex-runtime-js/src/websocket/WebSocketError.ts
	init_defineProperty();
	let _Symbol$toStringTag$15;
	function isValidCloseCode(code) {
		return code === 1e3 || code === 1005 || code === 1006 || code >= 3e3 && code <= 4999;
	}
	function createRealmDomException$1(message, name) {
		return new (globalThis.DOMException || DOMException$6)(message, name);
	}
	_Symbol$toStringTag$15 = Symbol.toStringTag;
	var WebSocketError = class extends DOMException$6 {
		constructor(message = "", init = {}) {
			const reason = typeof init.reason === "string" ? init.reason : "";
			let closeCode = init.closeCode ?? null;
			if (reason !== "" && closeCode === null) closeCode = 1e3;
			if (closeCode !== null) {
				if (typeof closeCode !== "number" || !Number.isInteger(closeCode) || !isValidCloseCode(closeCode)) throw createRealmDomException$1("Invalid close code", "InvalidAccessError");
			}
			if (reason !== "") {
				if (new TextEncoder().encode(reason).byteLength > 123) throw createRealmDomException$1("Close reason is too long", "SyntaxError");
			}
			super(message, "WebSocketError");
			_defineProperty(this, "closeCode", void 0);
			_defineProperty(this, "reason", void 0);
			this.closeCode = closeCode;
			this.reason = reason;
		}
		get [_Symbol$toStringTag$15]() {
			return "WebSocketError";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/websocket/WebSocketStream.ts
	init_defineProperty();
	let _Symbol$toStringTag$14;
	const STREAM_WRITE_IMMEDIATE_WINDOW_BYTES = 256 * 1024;
	const STREAM_WRITE_DRAIN_BYTES_PER_MS = 4096;
	function createDeferred() {
		let resolve;
		let reject;
		return {
			promise: new Promise((res, rej) => {
				resolve = res;
				reject = rej;
			}),
			resolve,
			reject
		};
	}
	function createRealmDomException(message, name) {
		return new (globalThis.DOMException || DOMException$6)(message, name);
	}
	function createAbortError() {
		return createRealmDomException("The operation was aborted.", "AbortError");
	}
	function createInvalidStateError() {
		return createRealmDomException("The stream is no longer writable.", "InvalidStateError");
	}
	function isObjectLike(value) {
		return typeof value === "object" && value !== null || typeof value === "function";
	}
	function isSharedArrayBufferInstance(buffer) {
		return typeof SharedArrayBuffer === "function" && buffer instanceof SharedArrayBuffer;
	}
	function isResizableArrayBuffer(buffer) {
		return !!buffer && typeof buffer === "object" && typeof buffer.byteLength === "number" && buffer.resizable === true;
	}
	function normalizeCloseArguments(value) {
		if (value === void 0) return {};
		if (!isObjectLike(value)) throw new TypeError("close() requires an options object");
		const raw = value;
		const reason = raw.reason === void 0 ? void 0 : String(raw.reason);
		let code = raw.closeCode;
		if (reason !== void 0 && reason !== "" && code === void 0) code = 1e3;
		if (code !== void 0) {
			if (typeof code !== "number" || !Number.isInteger(code) || code !== 1e3 && (code < 3e3 || code > 4999)) throw createRealmDomException("Invalid close code", "InvalidAccessError");
		}
		if (reason !== void 0) {
			if (new TextEncoder().encode(reason).byteLength > 123) throw createRealmDomException("Close reason is too long", "SyntaxError");
		}
		if (code === void 0 && reason === "") return {};
		return {
			code,
			reason
		};
	}
	function getAbortCloseArguments(reason) {
		if (reason instanceof WebSocketError) {
			const closeCode = reason.closeCode === null ? void 0 : reason.closeCode;
			return {
				code: closeCode === void 0 ? void 0 : closeCode,
				reason: reason.reason || void 0
			};
		}
		return {};
	}
	function closeSocket(socket, code, reason) {
		if (code === void 0) {
			socket.close();
			return;
		}
		if (reason === void 0) {
			socket.close(code);
			return;
		}
		socket.close(code, reason);
	}
	function normalizeClosedInfo(code, reason) {
		return {
			closeCode: code || 1005,
			reason: reason || ""
		};
	}
	function createWebSocketCloseError(message, code, reason) {
		const closeCode = code === 1005 ? null : code;
		return new WebSocketError(message, {
			closeCode: closeCode === void 0 ? null : closeCode,
			reason
		});
	}
	function getMonotonicNow() {
		if (typeof performance !== "undefined" && typeof performance.now === "function") return performance.now();
		return Date.now();
	}
	function estimateWriteResolveDelay(byteLength) {
		if (byteLength <= STREAM_WRITE_IMMEDIATE_WINDOW_BYTES) return 0;
		return Math.ceil((byteLength - STREAM_WRITE_IMMEDIATE_WINDOW_BYTES) / STREAM_WRITE_DRAIN_BYTES_PER_MS);
	}
	function normalizeWritableChunk(chunk) {
		if (typeof chunk === "string") return {
			sendValue: chunk,
			byteLength: new TextEncoder().encode(chunk).byteLength
		};
		if (chunk instanceof ArrayBuffer) {
			if (isResizableArrayBuffer(chunk)) throw new TypeError("Resizable ArrayBuffer is not supported");
			return {
				sendValue: chunk,
				byteLength: chunk.byteLength
			};
		}
		if (ArrayBuffer.isView(chunk)) {
			if (isSharedArrayBufferInstance(chunk.buffer)) throw new TypeError("SharedArrayBuffer-backed views are not supported");
			if (isResizableArrayBuffer(chunk.buffer)) throw new TypeError("Resizable ArrayBuffer is not supported");
			return {
				sendValue: chunk,
				byteLength: chunk.byteLength
			};
		}
		if (chunk && typeof chunk === "object" && typeof chunk.arrayBuffer === "function" && typeof chunk.size === "number") return {
			sendValue: chunk,
			byteLength: chunk.size
		};
		const stringified = String(chunk);
		return {
			sendValue: stringified,
			byteLength: new TextEncoder().encode(stringified).byteLength
		};
	}
	_Symbol$toStringTag$14 = Symbol.toStringTag;
	var WebSocketStream = class {
		constructor(url, options) {
			_defineProperty(this, "url", void 0);
			_defineProperty(this, "opened", void 0);
			_defineProperty(this, "closed", void 0);
			_defineProperty(this, "_socket", null);
			_defineProperty(this, "_readableController", null);
			_defineProperty(this, "_readable", null);
			_defineProperty(this, "_writable", null);
			_defineProperty(this, "_openedDeferred", createDeferred());
			_defineProperty(this, "_closedDeferred", createDeferred());
			_defineProperty(this, "_openedSettled", false);
			_defineProperty(this, "_closedSettled", false);
			_defineProperty(this, "_connected", false);
			_defineProperty(this, "_localCloseInitiated", false);
			_defineProperty(this, "_closedDuringHandshake", false);
			_defineProperty(this, "_ignoredTerminal", false);
			_defineProperty(this, "_pendingWriteRequests", []);
			_defineProperty(this, "_writableInvalidStateError", null);
			_defineProperty(this, "_pendingWriteResolveTimer", null);
			const urlString = url instanceof URL ? url.href : String(url);
			this.url = urlString;
			this.opened = this._openedDeferred.promise;
			this.closed = this._closedDeferred.promise;
			if (arguments.length === 0) throw new TypeError("WebSocketStream constructor requires a URL");
			if (options !== void 0 && !isObjectLike(options)) throw new TypeError("WebSocketStream options must be an object");
			if (options?.protocols !== void 0 && !Array.isArray(options.protocols)) throw new TypeError("WebSocketStream protocols option must be an array");
			const signal = options?.signal;
			if (signal?.aborted) {
				const abortError = createAbortError();
				this._rejectOpened(abortError);
				this._rejectClosed(abortError);
				return;
			}
			this._readable = new ReadableStream$1({
				start: (controller) => {
					this._readableController = controller;
				},
				pull: () => {
					this._syncReadableBackpressure();
				},
				cancel: async (reason) => {
					const closeArgs = getAbortCloseArguments(reason);
					await this._initiateClose(closeArgs.code, closeArgs.reason);
				}
			});
			this._writable = new WritableStream$1({
				write: async (chunk) => {
					if (!this._socket || !this._connected) throw createInvalidStateError();
					const normalized = normalizeWritableChunk(chunk);
					return new Promise((resolve, reject) => {
						const resolveDelay = estimateWriteResolveDelay(normalized.byteLength);
						this._pendingWriteRequests.push({
							remaining: normalized.byteLength,
							readyAt: getMonotonicNow() + resolveDelay,
							resolve,
							reject
						});
						try {
							this._socket.send(normalized.sendValue);
							if (normalized.byteLength === 0) this._pendingWriteRequests.shift()?.resolve();
						} catch (error) {
							this._pendingWriteRequests.pop()?.reject(error);
						}
					});
				},
				close: async () => {
					await this._initiateClose(void 0, void 0);
				},
				abort: async (reason) => {
					const closeArgs = getAbortCloseArguments(reason);
					await this._initiateClose(closeArgs.code, closeArgs.reason);
				}
			});
			const socket = new WebSocket(urlString, options?.protocols);
			socket.binaryType = "arraybuffer";
			socket._setIncomingFlowControl(true);
			this._socket = socket;
			const originalHandleBytesSent = socket._handleBytesSent.bind(socket);
			socket._handleBytesSent = (bytesSent) => {
				originalHandleBytesSent(bytesSent);
				this._handleBytesSent(bytesSent);
			};
			socket.addEventListener("open", () => {
				this._connected = true;
				if (signal) signal.removeEventListener("abort", abortListener);
				this._resolveOpened({
					readable: this._readable,
					writable: this._writable,
					protocol: socket.protocol,
					extensions: socket.extensions
				});
			});
			socket.addEventListener("message", (event) => {
				if (!this._readableController) return;
				if (typeof event.data === "string") this._readableController.enqueue(event.data);
				else if (event.data instanceof ArrayBuffer) this._readableController.enqueue(new Uint8Array(event.data));
				else if (ArrayBuffer.isView(event.data)) this._readableController.enqueue(new Uint8Array(event.data.buffer, event.data.byteOffset, event.data.byteLength));
				this._syncReadableBackpressure();
			});
			socket.addEventListener("error", () => {});
			socket.addEventListener("close", (event) => {
				if (signal) signal.removeEventListener("abort", abortListener);
				this._handleSocketClose(event.code, event.reason || "", !!event.wasClean);
			});
			const abortListener = () => {
				if (this._connected) return;
				const abortError = createAbortError();
				this._ignoredTerminal = true;
				this._rejectOpened(abortError);
				this._rejectClosed(abortError);
				try {
					socket.close();
				} catch (_abortCloseErr) {}
			};
			if (signal) signal.addEventListener("abort", abortListener, { once: true });
		}
		close(info) {
			const normalized = normalizeCloseArguments(info);
			this._initiateClose(normalized.code, normalized.reason);
		}
		_handleBytesSent(bytesSent) {
			let remainingBytes = bytesSent;
			for (const request of this._pendingWriteRequests) {
				if (remainingBytes <= 0) break;
				if (request.remaining <= 0) continue;
				if (remainingBytes >= request.remaining) {
					remainingBytes -= request.remaining;
					request.remaining = 0;
				} else {
					request.remaining -= remainingBytes;
					remainingBytes = 0;
				}
			}
			this._drainResolvedWrites();
		}
		_syncReadableBackpressure() {
			if (!this._socket || !this._connected || !this._readableController) return;
			const desiredSize = this._readableController.desiredSize;
			if (desiredSize !== null && desiredSize <= 0) {
				this._socket._pauseIncoming();
				return;
			}
			this._socket._resumeIncoming();
		}
		async _initiateClose(code, reason) {
			if (this._closedSettled) return;
			if (!this._socket) {
				if (!this._closedSettled) {
					const abortError = createAbortError();
					this._rejectOpened(abortError);
					this._rejectClosed(abortError);
				}
				return;
			}
			if (!this._connected && !this._openedSettled) {
				this._closedDuringHandshake = true;
				this._localCloseInitiated = true;
				const error = createWebSocketCloseError("WebSocketStream closed during handshake", 1005, "");
				this._ignoredTerminal = true;
				this._rejectOpened(error);
				this._rejectClosed(error);
				try {
					closeSocket(this._socket, code, reason);
				} catch (_closeDuringHandshakeErr) {}
				return;
			}
			if (this._localCloseInitiated) return this.closed.then(() => void 0);
			this._localCloseInitiated = true;
			closeSocket(this._socket, code, reason);
			return this.closed.then(() => void 0);
		}
		_handleSocketClose(code, reason, wasClean) {
			if (this._ignoredTerminal) return;
			this._connected = false;
			const closeInfo = normalizeClosedInfo(code, reason);
			const closeError = createWebSocketCloseError(reason || "WebSocket closed", closeInfo.closeCode, closeInfo.reason);
			const hadPendingWrites = this._pendingWriteRequests.length > 0;
			if (!this._openedSettled) {
				this._rejectOpened(closeError);
				this._rejectClosed(closeError);
				this._rejectPendingWrites(createInvalidStateError());
				this._errorReadable(closeError);
				this._errorWritable(closeError);
				return;
			}
			if (!wasClean || closeInfo.closeCode === 1006) {
				this._rejectClosed(closeError);
				this._errorReadable(closeError);
				if (hadPendingWrites) {
					const invalidStateError = this._getWritableInvalidStateError();
					this._rejectPendingWrites(invalidStateError);
					this._errorWritable(invalidStateError);
				} else this._errorWritable(closeError);
				return;
			}
			if (hadPendingWrites) {
				const invalidStateError = this._getWritableInvalidStateError();
				this._rejectClosed(closeError);
				this._closeReadable();
				this._rejectPendingWrites(invalidStateError);
				this._errorWritable(invalidStateError);
				return;
			}
			this._resolveClosed(closeInfo);
			this._closeReadable();
			if (this._localCloseInitiated) this._finishWritableClose();
			else {
				const invalidStateError = this._getWritableInvalidStateError();
				this._rejectPendingWrites(invalidStateError);
				this._errorWritable(invalidStateError);
			}
		}
		_getWritableInvalidStateError() {
			if (!this._writableInvalidStateError) this._writableInvalidStateError = createInvalidStateError();
			return this._writableInvalidStateError;
		}
		_rejectPendingWrites(reason) {
			this._clearPendingWriteResolveTimer();
			while (this._pendingWriteRequests.length > 0) this._pendingWriteRequests.shift()?.reject(reason);
		}
		_drainResolvedWrites() {
			this._clearPendingWriteResolveTimer();
			while (this._pendingWriteRequests.length > 0) {
				const current = this._pendingWriteRequests[0];
				if (current.remaining > 0) return;
				const delay = current.readyAt - getMonotonicNow();
				if (delay > 0) {
					this._pendingWriteResolveTimer = setTimeout(() => {
						this._pendingWriteResolveTimer = null;
						this._drainResolvedWrites();
					}, delay);
					return;
				}
				this._pendingWriteRequests.shift();
				current.resolve();
			}
		}
		_clearPendingWriteResolveTimer() {
			if (this._pendingWriteResolveTimer === null) return;
			clearTimeout(this._pendingWriteResolveTimer);
			this._pendingWriteResolveTimer = null;
		}
		_resolveOpened(value) {
			if (this._openedSettled) return;
			this._openedSettled = true;
			this._openedDeferred.resolve(value);
		}
		_rejectOpened(reason) {
			if (this._openedSettled) return;
			this._openedSettled = true;
			this._openedDeferred.reject(reason);
		}
		_resolveClosed(value) {
			if (this._closedSettled) return;
			this._closedSettled = true;
			this._closedDeferred.resolve(value);
		}
		_rejectClosed(reason) {
			if (this._closedSettled) return;
			this._closedSettled = true;
			this._closedDeferred.reject(reason);
		}
		_closeReadable() {
			if (this._readableController) try {
				this._readableController.close();
				return;
			} catch (_closeReadableErr) {}
			if (!this._readable) return;
			this._readable._closeStream();
		}
		_errorReadable(reason) {
			if (!this._readable) return;
			this._readable._errorStream(reason);
		}
		_finishWritableClose() {
			if (!this._writable) return;
			const writable = this._writable;
			if (writable._state === "closed" || writable._state === "errored") return;
			writable._state = "closed";
			writable._queue = [];
			writable._queueTotalSize = 0;
			const closeRequest = writable._inFlightCloseRequest;
			if (closeRequest) {
				closeRequest.resolve();
				writable._inFlightCloseRequest = void 0;
			}
			const writer = writable._writer;
			if (writer) {
				writer._readyResolve?.(void 0);
				writer._closedResolve?.(void 0);
			}
		}
		_errorWritable(reason) {
			if (!this._writable) return;
			this._writable._errorStream(reason);
		}
		get [_Symbol$toStringTag$14]() {
			return "WebSocketStream";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/eventsource/EventSource.ts
	/**
	* EventSource implementation for Exact runtime
	*
	* Implements the WHATWG EventSource (Server-Sent Events) API.
	* @see https://html.spec.whatwg.org/multipage/server-sent-events.html
	*/
	init_defineProperty();
	let _Symbol$toStringTag$13;
	const CONNECTING = 0;
	const OPEN = 1;
	const CLOSED = 2;
	const DEFAULT_RECONNECT_TIME = 3e3;
	const MAX_RECONNECT_TIME = 6e4;
	var _readyState$1 = /* @__PURE__ */ new WeakMap();
	var _reconnectionTime = /* @__PURE__ */ new WeakMap();
	var _lastEventId = /* @__PURE__ */ new WeakMap();
	var _abortController = /* @__PURE__ */ new WeakMap();
	var _reconnectTimer = /* @__PURE__ */ new WeakMap();
	var _consecutiveFailures = /* @__PURE__ */ new WeakMap();
	var _EventSource_brand = /* @__PURE__ */ new WeakSet();
	_Symbol$toStringTag$13 = Symbol.toStringTag;
	/**
	* EventSource provides a persistent connection to a server that sends events
	* in the text/event-stream format. Unlike WebSocket, EventSource is a
	* unidirectional channel (server to client only).
	*/
	var EventSource = class extends EventTarget {
		constructor(url, eventSourceInitDict) {
			super();
			_classPrivateMethodInitSpec(this, _EventSource_brand);
			_defineProperty(this, "CONNECTING", CONNECTING);
			_defineProperty(this, "OPEN", OPEN);
			_defineProperty(this, "CLOSED", CLOSED);
			_defineProperty(this, "url", void 0);
			_defineProperty(this, "withCredentials", void 0);
			_classPrivateFieldInitSpec(this, _readyState$1, CONNECTING);
			_classPrivateFieldInitSpec(this, _reconnectionTime, DEFAULT_RECONNECT_TIME);
			_classPrivateFieldInitSpec(this, _lastEventId, "");
			_classPrivateFieldInitSpec(this, _abortController, null);
			_classPrivateFieldInitSpec(this, _reconnectTimer, null);
			_classPrivateFieldInitSpec(this, _consecutiveFailures, 0);
			_defineProperty(this, "onopen", null);
			_defineProperty(this, "onmessage", null);
			_defineProperty(this, "onerror", null);
			requireCapability(Capabilities.NETWORK_CONNECT);
			const urlString = url instanceof URL ? url.href : url;
			let parsedUrl;
			try {
				parsedUrl = new URL(urlString);
			} catch {
				throw new DOMException("Invalid URL", "SyntaxError");
			}
			this.url = parsedUrl.href;
			this.withCredentials = eventSourceInitDict?.withCredentials ?? false;
			_assertClassBrand(_EventSource_brand, this, _connect).call(this);
		}
		get readyState() {
			return _classPrivateFieldGet2(_readyState$1, this);
		}
		/**
		* Close the EventSource connection.
		*/
		close() {
			if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) return;
			_classPrivateFieldSet2(_readyState$1, this, CLOSED);
			if (_classPrivateFieldGet2(_abortController, this)) {
				_classPrivateFieldGet2(_abortController, this).abort();
				_classPrivateFieldSet2(_abortController, this, null);
			}
			if (_classPrivateFieldGet2(_reconnectTimer, this) !== null) {
				clearTimeout(_classPrivateFieldGet2(_reconnectTimer, this));
				_classPrivateFieldSet2(_reconnectTimer, this, null);
			}
		}
		get [_Symbol$toStringTag$13]() {
			return "EventSource";
		}
	};
	/**
	* Initiate the fetch connection to the SSE endpoint.
	*/
	function _connect() {
		if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) return;
		_classPrivateFieldSet2(_readyState$1, this, CONNECTING);
		_classPrivateFieldSet2(_abortController, this, new AbortController());
		const headers = {
			"Accept": "text/event-stream",
			"Cache-Control": "no-cache"
		};
		if (_classPrivateFieldGet2(_lastEventId, this)) headers["Last-Event-ID"] = _classPrivateFieldGet2(_lastEventId, this);
		fetch(this.url, {
			headers,
			signal: _classPrivateFieldGet2(_abortController, this).signal,
			cache: "no-store"
		}).then((response) => {
			if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) return;
			if (!response.ok) {
				_assertClassBrand(_EventSource_brand, this, _failConnection).call(this, "HTTP error: " + response.status);
				return;
			}
			const contentType = response.headers.get("content-type") || "";
			if (!contentType.includes("text/event-stream")) {
				_assertClassBrand(_EventSource_brand, this, _failConnection).call(this, "Invalid content type: " + contentType);
				return;
			}
			_classPrivateFieldSet2(_readyState$1, this, OPEN);
			_classPrivateFieldSet2(_consecutiveFailures, this, 0);
			const openEvent = new Event("open");
			if (this.onopen) this.onopen.call(this, openEvent);
			this.dispatchEvent(openEvent);
			_assertClassBrand(_EventSource_brand, this, _readStream).call(this, response);
		}).catch((error) => {
			if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) return;
			if (error && error.name === "AbortError") return;
			_assertClassBrand(_EventSource_brand, this, _failConnection).call(this, error?.message || "Connection failed");
		});
	}
	/**
	* Read the response body as a stream and parse SSE events.
	*/
	async function _readStream(response) {
		const body = response.body;
		if (!body) {
			_assertClassBrand(_EventSource_brand, this, _failConnection).call(this, "Response body is null");
			return;
		}
		const reader = body.getReader();
		const decoder = new TextDecoder();
		let buffer = "";
		let eventType = "";
		let dataBuffer = "";
		let lastEventId;
		try {
			while (true) {
				if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) {
					reader.cancel();
					return;
				}
				const { done, value } = await reader.read();
				if (done) {
					if (_classPrivateFieldGet2(_readyState$1, this) !== CLOSED) _assertClassBrand(_EventSource_brand, this, _scheduleReconnect).call(this);
					return;
				}
				buffer += decoder.decode(value, { stream: true });
				let lineEnd;
				while ((lineEnd = buffer.indexOf("\n")) !== -1) {
					const line = buffer.substring(0, lineEnd);
					buffer = buffer.substring(lineEnd + 1);
					const cleanLine = line.endsWith("\r") ? line.slice(0, -1) : line;
					if (cleanLine === "") {
						if (dataBuffer !== "") {
							if (dataBuffer.endsWith("\n")) dataBuffer = dataBuffer.slice(0, -1);
							const eventName = eventType || "message";
							if (lastEventId !== void 0) _classPrivateFieldSet2(_lastEventId, this, lastEventId);
							const messageEvent = new MessageEvent$1(eventName, {
								data: dataBuffer,
								origin: new URL(this.url).origin,
								lastEventId: _classPrivateFieldGet2(_lastEventId, this)
							});
							if (eventName === "message" && this.onmessage) this.onmessage.call(this, messageEvent);
							this.dispatchEvent(messageEvent);
						}
						eventType = "";
						dataBuffer = "";
						lastEventId = void 0;
					} else if (cleanLine.startsWith(":")) {} else {
						const colonIndex = cleanLine.indexOf(":");
						let field;
						let fieldValue;
						if (colonIndex === -1) {
							field = cleanLine;
							fieldValue = "";
						} else {
							field = cleanLine.substring(0, colonIndex);
							fieldValue = cleanLine.substring(colonIndex + 1);
							if (fieldValue.startsWith(" ")) fieldValue = fieldValue.substring(1);
						}
						switch (field) {
							case "data":
								dataBuffer += fieldValue + "\n";
								break;
							case "event":
								eventType = fieldValue;
								break;
							case "id":
								if (!fieldValue.includes("\0")) lastEventId = fieldValue;
								break;
							case "retry":
								if (/^\d+$/.test(fieldValue)) _classPrivateFieldSet2(_reconnectionTime, this, parseInt(fieldValue, 10));
								break;
						}
					}
				}
			}
		} catch (error) {
			if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) return;
			if (error && error.name === "AbortError") return;
			_assertClassBrand(_EventSource_brand, this, _failConnection).call(this, error?.message || "Stream read error");
		}
	}
	/**
	* Handle a connection failure: fire error event and schedule reconnect.
	*/
	function _failConnection(message) {
		if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) return;
		const errorEvent = new Event("error");
		if (this.onerror) this.onerror.call(this, errorEvent);
		this.dispatchEvent(errorEvent);
		_assertClassBrand(_EventSource_brand, this, _scheduleReconnect).call(this);
	}
	/**
	* Schedule a reconnection attempt with exponential backoff.
	*/
	function _scheduleReconnect() {
		var _this$consecutiveFail;
		if (_classPrivateFieldGet2(_readyState$1, this) === CLOSED) return;
		_classPrivateFieldSet2(_readyState$1, this, CONNECTING);
		_classPrivateFieldSet2(_consecutiveFailures, this, (_this$consecutiveFail = _classPrivateFieldGet2(_consecutiveFailures, this), _this$consecutiveFail++, _this$consecutiveFail));
		const backoff = Math.min(_classPrivateFieldGet2(_reconnectionTime, this) * Math.pow(2, _classPrivateFieldGet2(_consecutiveFailures, this) - 1), MAX_RECONNECT_TIME);
		_classPrivateFieldSet2(_reconnectTimer, this, setTimeout(() => {
			_classPrivateFieldSet2(_reconnectTimer, this, null);
			if (_classPrivateFieldGet2(_readyState$1, this) !== CLOSED) _assertClassBrand(_EventSource_brand, this, _connect).call(this);
		}, backoff));
	}
	_defineProperty(EventSource, "CONNECTING", CONNECTING);
	_defineProperty(EventSource, "OPEN", OPEN);
	_defineProperty(EventSource, "CLOSED", CLOSED);
	//#endregion
	//#region ../ibex-runtime-js/src/filereader/FileReader.ts
	/**
	* FileReader implementation for Exact runtime (Legacy API)
	* 
	* @see https://w3c.github.io/FileAPI/#dfn-filereader
	* 
	* Note: Modern code should use Blob.arrayBuffer(), Blob.text(), etc.
	* This is provided for compatibility with legacy libraries.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$12;
	const EMPTY = 0;
	const LOADING = 1;
	const DONE = 2;
	var _readyState = /* @__PURE__ */ new WeakMap();
	var _result = /* @__PURE__ */ new WeakMap();
	var _error = /* @__PURE__ */ new WeakMap();
	var _aborted = /* @__PURE__ */ new WeakMap();
	var _FileReader_brand = /* @__PURE__ */ new WeakSet();
	_Symbol$toStringTag$12 = Symbol.toStringTag;
	/**
	* FileReader enables asynchronous reading of file or blob contents.
	*/
	var FileReader = class extends EventTarget {
		constructor(..._args) {
			super(..._args);
			_classPrivateMethodInitSpec(this, _FileReader_brand);
			_defineProperty(this, "EMPTY", EMPTY);
			_defineProperty(this, "LOADING", LOADING);
			_defineProperty(this, "DONE", DONE);
			_classPrivateFieldInitSpec(this, _readyState, EMPTY);
			_classPrivateFieldInitSpec(this, _result, null);
			_classPrivateFieldInitSpec(this, _error, null);
			_classPrivateFieldInitSpec(this, _aborted, false);
			_defineProperty(this, "onloadstart", null);
			_defineProperty(this, "onprogress", null);
			_defineProperty(this, "onload", null);
			_defineProperty(this, "onabort", null);
			_defineProperty(this, "onerror", null);
			_defineProperty(this, "onloadend", null);
		}
		get readyState() {
			return _classPrivateFieldGet2(_readyState, this);
		}
		get result() {
			return _classPrivateFieldGet2(_result, this);
		}
		get error() {
			return _classPrivateFieldGet2(_error, this);
		}
		/**
		* Read blob as ArrayBuffer.
		*/
		readAsArrayBuffer(blob) {
			_assertClassBrand(_FileReader_brand, this, _checkReadyState).call(this);
			_assertClassBrand(_FileReader_brand, this, _read).call(this, blob, "arraybuffer");
		}
		/**
		* Read blob as binary string.
		* @deprecated Use readAsArrayBuffer instead.
		*/
		readAsBinaryString(blob) {
			_assertClassBrand(_FileReader_brand, this, _checkReadyState).call(this);
			_assertClassBrand(_FileReader_brand, this, _read).call(this, blob, "binarystring");
		}
		/**
		* Read blob as text.
		*/
		readAsText(blob, encoding) {
			_assertClassBrand(_FileReader_brand, this, _checkReadyState).call(this);
			_assertClassBrand(_FileReader_brand, this, _read).call(this, blob, "text", encoding);
		}
		/**
		* Read blob as data URL.
		*/
		readAsDataURL(blob) {
			_assertClassBrand(_FileReader_brand, this, _checkReadyState).call(this);
			_assertClassBrand(_FileReader_brand, this, _read).call(this, blob, "dataurl");
		}
		/**
		* Abort the read operation.
		*/
		abort() {
			if (_classPrivateFieldGet2(_readyState, this) === EMPTY || _classPrivateFieldGet2(_readyState, this) === DONE) {
				_classPrivateFieldSet2(_result, this, null);
				return;
			}
			if (_classPrivateFieldGet2(_readyState, this) === LOADING) {
				_classPrivateFieldSet2(_readyState, this, DONE);
				_classPrivateFieldSet2(_result, this, null);
				_classPrivateFieldSet2(_aborted, this, true);
				_assertClassBrand(_FileReader_brand, this, _fireEvent).call(this, "abort");
				_assertClassBrand(_FileReader_brand, this, _fireEvent).call(this, "loadend");
			}
		}
		get [_Symbol$toStringTag$12]() {
			return "FileReader";
		}
	};
	/**
	* Check if reader is ready to start a new read operation.
	* Throws InvalidStateError if already loading.
	*/
	function _checkReadyState() {
		if (_classPrivateFieldGet2(_readyState, this) === LOADING) throw createInvalidStateError$1("FileReader is already loading");
	}
	/**
	* Internal read implementation.
	*/
	async function _read(blob, format, encoding) {
		_classPrivateFieldSet2(_readyState, this, LOADING);
		_classPrivateFieldSet2(_result, this, null);
		_classPrivateFieldSet2(_error, this, null);
		_classPrivateFieldSet2(_aborted, this, false);
		_assertClassBrand(_FileReader_brand, this, _fireProgressEvent).call(this, "loadstart", 0, blob.size);
		try {
			const buffer = await blob.arrayBuffer();
			if (_classPrivateFieldGet2(_aborted, this)) return;
			_assertClassBrand(_FileReader_brand, this, _fireProgressEvent).call(this, "progress", buffer.byteLength, blob.size);
			switch (format) {
				case "arraybuffer":
					_classPrivateFieldSet2(_result, this, buffer);
					break;
				case "binarystring": {
					const bytes = new Uint8Array(buffer);
					let result = "";
					for (let i = 0; i < bytes.length; i++) result += String.fromCharCode(bytes[i]);
					_classPrivateFieldSet2(_result, this, result);
					break;
				}
				case "text": {
					const decoder = new TextDecoder(encoding || "utf-8");
					_classPrivateFieldSet2(_result, this, decoder.decode(buffer));
					break;
				}
				case "dataurl": {
					const bytes = new Uint8Array(buffer);
					let binary = "";
					for (let i = 0; i < bytes.length; i++) binary += String.fromCharCode(bytes[i]);
					const base64 = btoa(binary);
					const mimeType = blob.type || "application/octet-stream";
					_classPrivateFieldSet2(_result, this, `data:${mimeType};base64,${base64}`);
					break;
				}
			}
			_classPrivateFieldSet2(_readyState, this, DONE);
			_assertClassBrand(_FileReader_brand, this, _fireProgressEvent).call(this, "load", buffer.byteLength, blob.size);
			_assertClassBrand(_FileReader_brand, this, _fireProgressEvent).call(this, "loadend", buffer.byteLength, blob.size);
		} catch (err) {
			if (_classPrivateFieldGet2(_aborted, this)) return;
			_classPrivateFieldSet2(_readyState, this, DONE);
			_classPrivateFieldSet2(_error, this, new DOMException$6(err instanceof Error ? err.message : "Read failed", "NotReadableError"));
			_assertClassBrand(_FileReader_brand, this, _fireProgressEvent).call(this, "error", 0, blob.size);
			_assertClassBrand(_FileReader_brand, this, _fireProgressEvent).call(this, "loadend", 0, blob.size);
		}
	}
	/**
	* Fire a simple event.
	*/
	function _fireEvent(type) {
		const event = new Event(type);
		const handler = this[`on${type}`];
		if (handler) handler.call(this, event);
		this.dispatchEvent(event);
	}
	/**
	* Fire a progress event.
	*/
	function _fireProgressEvent(type, loaded, total) {
		const event = new ProgressEvent(type, {
			lengthComputable: total > 0,
			loaded,
			total
		});
		const handler = this[`on${type}`];
		if (handler) handler.call(this, event);
		this.dispatchEvent(event);
	}
	_defineProperty(FileReader, "EMPTY", EMPTY);
	_defineProperty(FileReader, "LOADING", LOADING);
	_defineProperty(FileReader, "DONE", DONE);
	const USER_AGENT = "Ibex/0.1.0 (Hermes)";
	const RELEASE_NAME = "node";
	const RUNTIME_VERSIONS = [
		["ibex", "0.1.0"],
		["node", "24.13.1"],
		["hermes", "1.0.0"]
	];
	const BUN_COMPAT_VERSION = "1.2.0";
	//#endregion
	//#region ../ibex-runtime-js/src/locks/LockManager.ts
	init_defineProperty();
	let _Symbol$toStringTag$11;
	var _held = /* @__PURE__ */ new WeakMap();
	var _pending = /* @__PURE__ */ new WeakMap();
	var _LockManager_brand = /* @__PURE__ */ new WeakSet();
	_Symbol$toStringTag$11 = Symbol.toStringTag;
	/**
	* LockManager coordinates exclusive and shared access to named resources.
	*
	* Exclusive locks block all other lock requests for the same resource.
	* Shared locks allow other shared locks but block exclusive requests.
	* Pending requests are processed in FIFO order.
	*/
	var LockManager = class {
		constructor() {
			_classPrivateMethodInitSpec(this, _LockManager_brand);
			_classPrivateFieldInitSpec(this, _held, /* @__PURE__ */ new Map());
			_classPrivateFieldInitSpec(this, _pending, /* @__PURE__ */ new Map());
		}
		request(name, optionsOrCallback, maybeCallback) {
			let options;
			let callback;
			if (typeof optionsOrCallback === "function") {
				options = {};
				callback = optionsOrCallback;
			} else {
				options = optionsOrCallback;
				callback = maybeCallback;
			}
			if (typeof callback !== "function") throw new TypeError("LockManager.request: callback must be a function");
			if (typeof name !== "string") throw new TypeError("LockManager.request: name must be a string");
			if (name.startsWith("-")) throw new DOMException$4(`LockManager.request: names starting with '-' are reserved`, "NotSupportedError");
			const mode = options.mode ?? "exclusive";
			if (mode !== "exclusive" && mode !== "shared") throw new TypeError(`LockManager.request: invalid mode '${mode}'`);
			if (options.steal && options.ifAvailable) throw new DOMException$4("LockManager.request: 'steal' and 'ifAvailable' cannot be used together", "NotSupportedError");
			if (options.steal && mode !== "exclusive") throw new DOMException$4("LockManager.request: 'steal' is only supported for exclusive locks", "NotSupportedError");
			if (options.signal && options.steal) throw new DOMException$4("LockManager.request: 'signal' and 'steal' cannot be used together", "NotSupportedError");
			if (options.signal?.aborted) return Promise.reject(options.signal.reason);
			if (options.steal) return _assertClassBrand(_LockManager_brand, this, _stealLock).call(this, name, callback);
			if (options.ifAvailable) return _assertClassBrand(_LockManager_brand, this, _requestIfAvailable).call(this, name, mode, callback);
			return _assertClassBrand(_LockManager_brand, this, _requestLock).call(this, name, mode, callback, options.signal);
		}
		/**
		* Returns a snapshot of the currently held and pending locks.
		*/
		async query() {
			const held = [];
			const pending = [];
			{
				const __exactForOfIterator77 = _classPrivateFieldGet2(_held, this)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep77 = __exactForOfIterator77.next();
					if (__exactForOfStep77.done) break;
					const [, locks] = __exactForOfStep77.value;
					for (const entry of locks) held.push({
						name: entry.name,
						mode: entry.mode
					});
				}
			}
			{
				const __exactForOfIterator78 = _classPrivateFieldGet2(_pending, this)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep78 = __exactForOfIterator78.next();
					if (__exactForOfStep78.done) break;
					const [, requests] = __exactForOfStep78.value;
					for (const req of requests) pending.push({
						name: req.name,
						mode: req.mode
					});
				}
			}
			return {
				held,
				pending
			};
		}
		get [_Symbol$toStringTag$11]() {
			return "LockManager";
		}
	};
	/**
	* Steal a lock: abort all existing holders and pending requests, then grant.
	*/
	function _stealLock(name, callback) {
		_classPrivateFieldGet2(_held, this).delete(name);
		const existingPending = _classPrivateFieldGet2(_pending, this).get(name);
		if (existingPending) {
			{
				const __exactForOfIterator79 = existingPending[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep79 = __exactForOfIterator79.next();
					if (__exactForOfStep79.done) break;
					const req = __exactForOfStep79.value;
					_assertClassBrand(_LockManager_brand, this, _cleanupAbortHandler).call(this, req);
					req.reject(new DOMException$4("Lock broken by steal request", "AbortError"));
				}
			}
			_classPrivateFieldGet2(_pending, this).delete(name);
		}
		return _assertClassBrand(_LockManager_brand, this, _grantAndRun).call(this, name, "exclusive", callback);
	}
	/**
	* Try to acquire a lock without waiting. Returns null to callback if unavailable.
	*/
	function _requestIfAvailable(name, mode, callback) {
		if (_assertClassBrand(_LockManager_brand, this, _canAcquire).call(this, name, mode)) return _assertClassBrand(_LockManager_brand, this, _grantAndRun).call(this, name, mode, callback);
		return _assertClassBrand(_LockManager_brand, this, _invokeCallback).call(this, callback, null);
	}
	/**
	* Normal lock request: grant immediately if possible, otherwise queue.
	*/
	function _requestLock(name, mode, callback, signal) {
		if (_assertClassBrand(_LockManager_brand, this, _canAcquire).call(this, name, mode)) return _assertClassBrand(_LockManager_brand, this, _grantAndRun).call(this, name, mode, callback);
		return new Promise((resolve, reject) => {
			const request = {
				name,
				mode,
				callback,
				resolve,
				reject,
				signal
			};
			if (signal) {
				const abortHandler = () => {
					_assertClassBrand(_LockManager_brand, this, _removePendingRequest).call(this, name, request);
					request.reject(signal.reason);
				};
				request.abortHandler = abortHandler;
				signal.addEventListener("abort", abortHandler, { once: true });
			}
			let queue = _classPrivateFieldGet2(_pending, this).get(name);
			if (!queue) {
				queue = [];
				_classPrivateFieldGet2(_pending, this).set(name, queue);
			}
			queue.push(request);
		});
	}
	/**
	* Check if a lock can be granted immediately for the given name and mode.
	* Considers the pending queue to prevent writer starvation.
	*/
	function _canAcquire(name, mode) {
		const held = _classPrivateFieldGet2(_held, this).get(name);
		if (!held || held.length === 0) {
			const pendingQueue = _classPrivateFieldGet2(_pending, this).get(name);
			if (pendingQueue && pendingQueue.length > 0) return false;
			return true;
		}
		if (mode === "shared") {
			if (!held.every((h) => h.mode === "shared")) return false;
			const pendingQueue = _classPrivateFieldGet2(_pending, this).get(name);
			if (pendingQueue && pendingQueue.some((p) => p.mode === "exclusive")) return false;
			return true;
		}
		return false;
	}
	/**
	* Check if a lock can be granted, used when draining the queue.
	* Does not consider pending queue for starvation (we process FIFO).
	*/
	function _canAcquireDirect(name, mode) {
		const held = _classPrivateFieldGet2(_held, this).get(name);
		if (!held || held.length === 0) return true;
		if (mode === "shared") return held.every((h) => h.mode === "shared");
		return false;
	}
	/**
	* Grant a lock, run the callback, and release the lock when the callback finishes.
	* Returns the promise that resolves to the callback's return value.
	*/
	function _grantAndRun(name, mode, callback) {
		const lock = Object.freeze({
			name,
			mode
		});
		const entry = {
			name,
			mode
		};
		let heldList = _classPrivateFieldGet2(_held, this).get(name);
		if (!heldList) {
			heldList = [];
			_classPrivateFieldGet2(_held, this).set(name, heldList);
		}
		heldList.push(entry);
		return _assertClassBrand(_LockManager_brand, this, _invokeCallback).call(this, callback, lock).then((value) => {
			_assertClassBrand(_LockManager_brand, this, _releaseLock).call(this, name, entry);
			return value;
		}, (err) => {
			_assertClassBrand(_LockManager_brand, this, _releaseLock).call(this, name, entry);
			throw err;
		});
	}
	/**
	* Invoke the user callback safely, wrapping the result in a promise.
	*/
	function _invokeCallback(callback, lock) {
		try {
			const result = callback(lock);
			return Promise.resolve(result);
		} catch (err) {
			return Promise.reject(err);
		}
	}
	/**
	* Release a held lock and process the pending queue.
	*/
	function _releaseLock(name, entry) {
		const heldList = _classPrivateFieldGet2(_held, this).get(name);
		if (heldList) {
			const idx = heldList.indexOf(entry);
			if (idx !== -1) heldList.splice(idx, 1);
			if (heldList.length === 0) _classPrivateFieldGet2(_held, this).delete(name);
		}
		_assertClassBrand(_LockManager_brand, this, _processQueue).call(this, name);
	}
	/**
	* Remove a pending request from the queue (e.g., on abort).
	*/
	function _removePendingRequest(name, request) {
		const queue = _classPrivateFieldGet2(_pending, this).get(name);
		if (!queue) return;
		const idx = queue.indexOf(request);
		if (idx !== -1) queue.splice(idx, 1);
		if (queue.length === 0) _classPrivateFieldGet2(_pending, this).delete(name);
	}
	/**
	* Clean up abort event listener for a request.
	*/
	function _cleanupAbortHandler(request) {
		if (request.abortHandler && request.signal) request.signal.removeEventListener("abort", request.abortHandler);
	}
	/**
	* Process the pending queue for a given lock name, granting as many as possible.
	*/
	function _processQueue(name) {
		const queue = _classPrivateFieldGet2(_pending, this).get(name);
		if (!queue || queue.length === 0) return;
		while (queue.length > 0) {
			const next = queue[0];
			if (!_assertClassBrand(_LockManager_brand, this, _canAcquireDirect).call(this, name, next.mode)) break;
			queue.shift();
			_assertClassBrand(_LockManager_brand, this, _cleanupAbortHandler).call(this, next);
			if (queue.length === 0) _classPrivateFieldGet2(_pending, this).delete(name);
			_assertClassBrand(_LockManager_brand, this, _grantAndRun).call(this, name, next.mode, next.callback).then(next.resolve, next.reject);
			if (next.mode === "exclusive") break;
		}
	}
	var DOMException$4 = class extends Error {
		constructor(message, name) {
			super(message);
			_defineProperty(this, "code", 0);
			this.name = name;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/clipboard/ClipboardItem.ts
	init_defineProperty();
	let _Symbol$toStringTag$10;
	var _items$1 = /* @__PURE__ */ new WeakMap();
	var _types = /* @__PURE__ */ new WeakMap();
	_Symbol$toStringTag$10 = Symbol.toStringTag;
	/**
	* ClipboardItem - Web Clipboard API ClipboardItem implementation
	*
	* Represents a single item on the clipboard, potentially with multiple
	* representations (e.g., text/plain and text/html for the same content).
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/ClipboardItem
	* @see https://w3c.github.io/clipboard-apis/#clipboarditem
	*/
	/**
	* A ClipboardItem represents data that can be placed on or read from
	* the system clipboard. Each item can have multiple MIME-typed representations.
	*/
	var ClipboardItem = class {
		/**
		* Create a new ClipboardItem.
		*
		* @param items - A record mapping MIME types to their data. Values can be
		*   Blob, string, or Promise resolving to either.
		* @param options - Optional settings (presentationStyle, etc.) - reserved for future use.
		*/
		constructor(items, _options) {
			_classPrivateFieldInitSpec(this, _items$1, void 0);
			_classPrivateFieldInitSpec(this, _types, void 0);
			if (!items || typeof items !== "object") throw new TypeError("ClipboardItem constructor: items must be an object");
			const keys = Object.keys(items);
			if (keys.length === 0) throw new TypeError("ClipboardItem constructor: items must not be empty");
			_classPrivateFieldSet2(_items$1, this, /* @__PURE__ */ new Map());
			for (let i = 0; i < keys.length; i++) {
				const type = keys[i];
				_classPrivateFieldGet2(_items$1, this).set(type, items[type]);
			}
			_classPrivateFieldSet2(_types, this, Object.freeze([...keys]));
		}
		/**
		* The MIME types available for this clipboard item.
		*/
		get types() {
			return _classPrivateFieldGet2(_types, this);
		}
		/**
		* Retrieve the data for a specific MIME type as a Blob.
		*
		* @param type - The MIME type to retrieve.
		* @returns A Promise resolving to a Blob containing the data.
		* @throws DOMException with name "NotFoundError" if the type is not available.
		*/
		async getType(type) {
			const data = _classPrivateFieldGet2(_items$1, this).get(type);
			if (data === void 0) throw new DOMException$3(`ClipboardItem.getType: type '${type}' not found`, "NotFoundError");
			const resolved = await data;
			if (typeof resolved === "string") return new Blob([resolved], { type });
			if (resolved instanceof Blob) return resolved;
			throw new TypeError(`ClipboardItem.getType: unexpected data type for '${type}'`);
		}
		get [_Symbol$toStringTag$10]() {
			return "ClipboardItem";
		}
	};
	var DOMException$3 = class extends Error {
		constructor(message, name) {
			super(message);
			_defineProperty(this, "code", 0);
			this.name = name;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/clipboard/Clipboard.ts
	/**
	* Clipboard - Web Clipboard API implementation for Exact runtime
	*
	* Provides async read/write access to the system clipboard.
	* By default, stores data in memory. When native bridge functions
	* (__exactClipboardRead / __exactClipboardWrite) are available,
	* they are used for actual system clipboard access.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/Clipboard
	* @see https://w3c.github.io/clipboard-apis/#clipboard-interface
	*/
	let _Symbol$toStringTag$9;
	var _text = /* @__PURE__ */ new WeakMap();
	var _items = /* @__PURE__ */ new WeakMap();
	_Symbol$toStringTag$9 = Symbol.toStringTag;
	/**
	* The Clipboard interface provides read and write access to the contents
	* of the system clipboard, allowing cut, copy, and paste operations in
	* web applications.
	*
	* This implementation falls back to in-memory storage when native clipboard
	* access is not available. Native bridge functions can be plugged in via:
	* - globalThis.__exactClipboardRead: () => string
	* - globalThis.__exactClipboardWrite: (text: string) => void
	*/
	var Clipboard = class {
		constructor() {
			_classPrivateFieldInitSpec(this, _text, "");
			_classPrivateFieldInitSpec(this, _items, []);
		}
		/**
		* Read text from the clipboard.
		*
		* Uses the native bridge if available (__exactClipboardRead),
		* otherwise returns from in-memory storage.
		*
		* @returns A Promise resolving to the clipboard text content.
		*/
		async readText() {
			const g = globalThis;
			if (typeof g.__exactClipboardRead === "function") return String(g.__exactClipboardRead());
			return _classPrivateFieldGet2(_text, this);
		}
		/**
		* Write text to the clipboard.
		*
		* Uses the native bridge if available (__exactClipboardWrite),
		* otherwise stores in memory.
		*
		* @param text - The text to write to the clipboard.
		*/
		async writeText(text) {
			const g = globalThis;
			if (typeof g.__exactClipboardWrite === "function") g.__exactClipboardWrite(String(text));
			_classPrivateFieldSet2(_text, this, String(text));
			_classPrivateFieldSet2(_items, this, [new ClipboardItem({ "text/plain": text })]);
		}
		/**
		* Read structured data from the clipboard as ClipboardItem[].
		*
		* For now, returns items from the in-memory store. If only text was
		* written, wraps it as a ClipboardItem with 'text/plain' type.
		*
		* @returns A Promise resolving to an array of ClipboardItems.
		*/
		async read() {
			const g = globalThis;
			if (typeof g.__exactClipboardRead === "function") {
				const text = String(g.__exactClipboardRead());
				if (text) return [new ClipboardItem({ "text/plain": text })];
				return [];
			}
			return [..._classPrivateFieldGet2(_items, this)];
		}
		/**
		* Write structured data to the clipboard.
		*
		* Stores the items in memory. If any item contains 'text/plain',
		* also updates the text representation for readText() compatibility.
		*
		* @param items - An array of ClipboardItems to write.
		*/
		async write(items) {
			if (!Array.isArray(items)) throw new TypeError("Clipboard.write: argument must be an array of ClipboardItems");
			_classPrivateFieldSet2(_items, this, [...items]);
			for (const item of items) if (item.types.includes("text/plain")) {
				try {
					const blob = await item.getType("text/plain");
					_classPrivateFieldSet2(_text, this, await blob.text());
					const g = globalThis;
					if (typeof g.__exactClipboardWrite === "function") g.__exactClipboardWrite(_classPrivateFieldGet2(_text, this));
				} catch {}
				break;
			}
		}
		get [_Symbol$toStringTag$9]() {
			return "Clipboard";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/storage/StorageManager.ts
	/**
	* StorageManager - navigator.storage API
	*
	* Provides methods for estimating storage usage/quota and
	* requesting persistent storage.
	* @see https://storage.spec.whatwg.org/#storagemanager
	*/
	init_defineProperty();
	let _Symbol$toStringTag$8;
	const DEFAULT_QUOTA$1 = 100 * 1024 * 1024;
	_Symbol$toStringTag$8 = Symbol.toStringTag;
	var StorageManager = class {
		constructor() {
			_defineProperty(this, "_nativeStorage", void 0);
			this._nativeStorage = getNativeModule("storage");
		}
		/**
		* Returns an estimate of the total storage used and the available quota.
		*
		* @returns A promise that resolves to a StorageEstimate with usage and quota.
		*/
		async estimate() {
			if (this._nativeStorage?.estimate) return this._nativeStorage.estimate();
			return {
				usage: 0,
				quota: DEFAULT_QUOTA$1
			};
		}
		/**
		* Requests permission to use persistent storage.
		*
		* In the Exact runtime, persistent storage is granted based on the
		* STORAGE_PERSIST capability.
		*
		* @returns A promise that resolves to true if persistent storage is granted.
		*/
		async persist() {
			try {
				requireCapability(Capabilities.STORAGE_PERSIST);
			} catch {
				return false;
			}
			if (this._nativeStorage?.persist) return this._nativeStorage.persist();
			return true;
		}
		/**
		* Checks whether persistent storage has been granted.
		*
		* @returns A promise that resolves to true if storage is persistent.
		*/
		async persisted() {
			if (this._nativeStorage?.persisted) return this._nativeStorage.persisted();
			try {
				requireCapability(Capabilities.STORAGE_PERSIST);
				return true;
			} catch {
				return false;
			}
		}
		get [_Symbol$toStringTag$8]() {
			return "StorageManager";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/core/i18n-helpers.ts
	const RTL_SCRIPT_SUBTAGS = new Set([
		"adlm",
		"arab",
		"hebr",
		"mand",
		"nkoo",
		"rohg",
		"samr",
		"syrc",
		"thaa"
	]);
	const RTL_LANGUAGE_SUBTAGS = new Set([
		"ae",
		"aeb",
		"ar",
		"arc",
		"arq",
		"ars",
		"ary",
		"arz",
		"azb",
		"bal",
		"bcc",
		"bqi",
		"ckb",
		"dv",
		"fa",
		"glk",
		"he",
		"iw",
		"ks",
		"khw",
		"lrc",
		"mzn",
		"nqo",
		"pnb",
		"ps",
		"sd",
		"syr",
		"ug",
		"ur",
		"yi"
	]);
	function canonicalizeLocaleTag(tag) {
		const input = tag.trim();
		if (input.length === 0) return input;
		if (typeof Intl === "object" && Intl && typeof Intl.getCanonicalLocales === "function") try {
			const [canonical] = Intl.getCanonicalLocales(input);
			if (typeof canonical === "string" && canonical.length > 0) return canonical;
		} catch {}
		const parts = input.split("-");
		if (parts.length > 0) parts[0] = parts[0].toLowerCase();
		for (let index = 1; index < parts.length; index += 1) {
			const part = parts[index];
			if (!part) continue;
			if (part.length === 4) {
				parts[index] = part[0].toUpperCase() + part.slice(1).toLowerCase();
				continue;
			}
			if (part.length === 2 || part.length === 3 && /^[0-9A-Za-z]{3}$/.test(part)) {
				parts[index] = part.toUpperCase();
				continue;
			}
			parts[index] = part.toLowerCase();
		}
		return parts.join("-");
	}
	function parseLocaleTag(tag) {
		const parts = canonicalizeLocaleTag(tag).split("-").filter(Boolean);
		const parsed = { language: parts[0]?.toLowerCase() || "en" };
		for (const part of parts.slice(1)) {
			if (part.length === 4 && parsed.script === void 0) {
				parsed.script = part;
				continue;
			}
			if ((part.length === 2 || part.length === 3) && parsed.region === void 0) {
				parsed.region = part;
				continue;
			}
			if (part.length === 1) break;
		}
		return parsed;
	}
	function directionForLocaleTag(tag) {
		const parsed = parseLocaleTag(tag);
		if (parsed.script && RTL_SCRIPT_SUBTAGS.has(parsed.script.toLowerCase())) return "rtl";
		return RTL_LANGUAGE_SUBTAGS.has(parsed.language) ? "rtl" : "ltr";
	}
	//#endregion
	//#region ../ibex-runtime-js/src/core/locale-state.ts
	function freezeSnapshot$1(snapshot) {
		return Object.freeze({
			...snapshot,
			tags: Object.freeze([...snapshot.tags])
		});
	}
	function readNativeLocaleSnapshot() {
		const nativeSnapshot = globalThis.__exactLocaleSnapshot;
		if (nativeSnapshot && typeof nativeSnapshot === "object") return nativeSnapshot;
		const g = globalThis;
		const hostNavigator = g.__exactHostNavigator && typeof g.__exactHostNavigator === "object" ? g.__exactHostNavigator : void 0;
		const hostLanguages = Array.isArray(hostNavigator?.languages) ? hostNavigator.languages.filter((entry) => typeof entry === "string" && entry.length > 0) : [];
		const hostLanguage = typeof hostNavigator?.language === "string" && hostNavigator.language.length > 0 ? hostNavigator.language : void 0;
		const fallbackTag = typeof g.__exactLocale === "string" && g.__exactLocale.length > 0 ? g.__exactLocale : typeof g.__exactLanguage === "string" && g.__exactLanguage.length > 0 ? g.__exactLanguage : hostLanguage;
		if (!fallbackTag && hostLanguages.length === 0) return null;
		return {
			tag: fallbackTag,
			tags: hostLanguages
		};
	}
	function detectUses24Hour(localeTag) {
		if (typeof Intl !== "object" || !Intl || typeof Intl.DateTimeFormat !== "function") return false;
		try {
			const resolved = new Intl.DateTimeFormat(localeTag, { hour: "numeric" }).resolvedOptions();
			if (typeof resolved.hour12 === "boolean") return resolved.hour12 === false;
			if (typeof resolved.hourCycle === "string") return resolved.hourCycle === "h23" || resolved.hourCycle === "h24";
		} catch {}
		return false;
	}
	function buildLocaleSnapshot(nativeSnapshot, override) {
		const rawTags = Array.isArray(nativeSnapshot?.tags) ? nativeSnapshot.tags.filter((entry) => typeof entry === "string" && entry.length > 0) : [];
		const primaryTag = canonicalizeLocaleTag(override?.locale ?? nativeSnapshot?.tag ?? rawTags[0] ?? "en-US");
		const tags = override?.locale != null ? [primaryTag] : rawTags.length > 0 ? rawTags.map((tag) => canonicalizeLocaleTag(tag)) : [primaryTag];
		const parsed = parseLocaleTag(primaryTag);
		const region = parsed.region ?? "";
		const uses24Hour = typeof nativeSnapshot?.uses24Hour === "boolean" ? nativeSnapshot.uses24Hour : detectUses24Hour(primaryTag);
		return freezeSnapshot$1({
			language: parsed.language,
			region,
			tag: primaryTag,
			tags: Object.freeze(tags),
			direction: override?.direction ?? directionForLocaleTag(primaryTag),
			uses24Hour
		});
	}
	function createDefaultState$1() {
		return {
			snapshot: buildLocaleSnapshot(readNativeLocaleSnapshot(), null),
			override: null,
			listeners: /* @__PURE__ */ new Set(),
			changeTimer: null
		};
	}
	function getLocaleState() {
		if (!globalThis.__exactLocaleState) globalThis.__exactLocaleState = createDefaultState$1();
		return globalThis.__exactLocaleState;
	}
	function dispatchLocaleChange(state) {
		const snapshot = state.snapshot;
		{
			const __exactForOfIterator80 = state.listeners[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep80 = __exactForOfIterator80.next();
				if (__exactForOfStep80.done) break;
				const listener = __exactForOfStep80.value;
				try {
					listener(snapshot);
				} catch {}
			}
		}
	}
	function scheduleLocaleChange(state) {
		if (state.changeTimer != null) clearTimeout(state.changeTimer);
		state.changeTimer = setTimeout(() => {
			state.changeTimer = null;
			dispatchLocaleChange(state);
		}, 100);
	}
	function updateLocaleSnapshot(nativeSnapshot, override, notify) {
		const state = getLocaleState();
		const nextSnapshot = buildLocaleSnapshot(nativeSnapshot, override);
		const prevSnapshot = state.snapshot;
		state.snapshot = nextSnapshot;
		state.override = override;
		const changed = prevSnapshot.tag !== nextSnapshot.tag || prevSnapshot.direction !== nextSnapshot.direction || prevSnapshot.uses24Hour !== nextSnapshot.uses24Hour || prevSnapshot.tags.length !== nextSnapshot.tags.length || prevSnapshot.tags.some((tag, index) => tag !== nextSnapshot.tags[index]);
		if (notify && changed) scheduleLocaleChange(state);
		return nextSnapshot;
	}
	function subscribeExactLocaleChanges(listener) {
		const state = getLocaleState();
		state.listeners.add(listener);
		return () => {
			state.listeners.delete(listener);
		};
	}
	function getExactLocaleSnapshot() {
		return getLocaleState().snapshot;
	}
	function getExactLocaleOverride() {
		return getLocaleState().override;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/core/locale.ts
	function createLocaleNamespace() {
		return {
			get language() {
				return getExactLocaleSnapshot().language;
			},
			get region() {
				return getExactLocaleSnapshot().region;
			},
			get tag() {
				return getExactLocaleSnapshot().tag;
			},
			get tags() {
				return getExactLocaleSnapshot().tags;
			},
			get direction() {
				return getExactLocaleSnapshot().direction;
			},
			get uses24Hour() {
				return getExactLocaleSnapshot().uses24Hour;
			},
			addListener(_event, listener) {
				return subscribeExactLocaleChanges(listener);
			}
		};
	}
	function installExactLocaleGlobal() {
		const g = globalThis;
		if (typeof g.Exact !== "object" || g.Exact === null) g.Exact = {};
		const exact = g.Exact;
		const existing = exact.locale;
		if (existing && typeof existing === "object" && typeof existing.addListener === "function") return existing;
		updateLocaleSnapshot(readNativeLocaleSnapshot(), getExactLocaleOverride(), false);
		const locale = createLocaleNamespace();
		Object.defineProperty(exact, "locale", {
			value: locale,
			writable: true,
			configurable: true,
			enumerable: true
		});
		globalThis.__exactLocaleChanged = (snapshot) => {
			if (snapshot && typeof snapshot === "object") globalThis.__exactLocaleSnapshot = snapshot;
			updateLocaleSnapshot(snapshot ?? readNativeLocaleSnapshot(), getExactLocaleOverride(), true);
		};
		return locale;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/navigator/Navigator.ts
	/**
	* Navigator implementation for Exact runtime
	* 
	* Provides browser-like navigator object with device/platform info.
	* @see https://html.spec.whatwg.org/multipage/system-state.html#the-navigator-object
	*/
	init_defineProperty();
	let _Symbol$toStringTag$7;
	_Symbol$toStringTag$7 = Symbol.toStringTag;
	/**
	* Navigator provides information about the runtime environment.
	*/
	var Navigator = class {
		constructor() {
			_defineProperty(this, "__exactNavigator", true);
			_defineProperty(this, "_locks", null);
			_defineProperty(this, "_storage", null);
			_defineProperty(this, "_clipboard", null);
			_defineProperty(this, "_geolocation", null);
		}
		/**
		* User agent string identifying the runtime.
		* Format: "Exact/{version} ({platform}; {engine})"
		*/
		get userAgent() {
			return USER_AGENT;
		}
		/**
		* Vendor string (always "Exact" for this runtime).
		*/
		get vendor() {
			return "Exact";
		}
		/**
		* Vendor sub-version (empty string).
		*/
		get vendorSub() {
			return "";
		}
		/**
		* Product name (always "Exact").
		*/
		get product() {
			return "Exact";
		}
		/**
		* Product sub-version (empty string).
		*/
		get productSub() {
			return "";
		}
		/**
		* App name for compatibility (always "Netscape" per spec).
		*/
		get appName() {
			return "Netscape";
		}
		/**
		* App code name for compatibility.
		*/
		get appCodeName() {
			return "Mozilla";
		}
		/**
		* App version string.
		*/
		get appVersion() {
			return `5.0 (iOS)`;
		}
		/**
		* Platform identifier.
		* Returns "iPhone", "Android", "MacIntel", etc. depending on the runtime platform.
		*/
		get platform() {
			const g = globalThis;
			if (typeof g.__exactPlatform === "string") {
				const p = g.__exactPlatform.toLowerCase();
				if (p === "android") return "Android";
				if (p === "web" || p === "browser") return "MacIntel";
				if (p === "macos" || p === "mac") return "MacIntel";
				if (p === "windows" || p === "win32") return "Win32";
				if (p === "linux") return "Linux x86_64";
				return "iPhone";
			}
			return "iPhone";
		}
		/**
		* Primary language preference.
		* Returns the device's preferred language via native bridge if available.
		*/
		get language() {
			const exactLocale = getExactLocaleSnapshot();
			if (exactLocale.tag.length > 0) return exactLocale.tag;
			const g = globalThis;
			if (typeof g.__exactLocale === "string" && g.__exactLocale) return g.__exactLocale;
			if (typeof g.__exactLanguage === "string" && g.__exactLanguage) return g.__exactLanguage;
			return "en-US";
		}
		/**
		* Array of preferred languages.
		*/
		get languages() {
			const exactLocale = getExactLocaleSnapshot();
			if (exactLocale.tags.length > 0) return exactLocale.tags;
			const lang = this.language;
			const base = lang.includes("-") ? lang.split("-")[0] : lang;
			if (base !== lang) return Object.freeze([lang, base]);
			return Object.freeze([lang]);
		}
		/**
		* Whether the device is online.
		* Note: Requires native bridge for accurate status.
		*/
		get onLine() {
			return true;
		}
		/**
		* Number of logical processors available.
		*/
		get hardwareConcurrency() {
			return 4;
		}
		/**
		* Maximum touch points supported by the device.
		*/
		get maxTouchPoints() {
			return 5;
		}
		/**
		* Whether cookies are enabled.
		* Always returns true in Exact (we support cookies in fetch).
		*/
		get cookieEnabled() {
			return true;
		}
		/**
		* Whether Java is enabled (always false).
		*/
		javaEnabled() {
			return false;
		}
		/**
		* Device memory in GB (approximate).
		* Note: This is a hint, not exact value.
		*/
		get deviceMemory() {
			return 4;
		}
		/**
		* PDF viewer support (always false in native app).
		*/
		get pdfViewerEnabled() {
			return false;
		}
		/**
		* Web driver status (always false).
		*/
		get webdriver() {
			return false;
		}
		/**
		* Clipboard API for reading and writing clipboard data.
		* @see https://developer.mozilla.org/en-US/docs/Web/API/Navigator/clipboard
		*/
		get clipboard() {
			if (!this._clipboard) this._clipboard = new Clipboard();
			return this._clipboard;
		}
		/**
		* Geolocation API installed from the runtime location backend.
		*/
		get geolocation() {
			if (!this._geolocation) this._geolocation = getGeolocation();
			return this._geolocation;
		}
		/**
		* Web Locks API for coordinating concurrent access.
		* @see https://developer.mozilla.org/en-US/docs/Web/API/Navigator/locks
		*/
		get locks() {
			if (!this._locks) this._locks = new LockManager();
			return this._locks;
		}
		/**
		* Storage manager for quota and persistence info.
		* @see https://developer.mozilla.org/en-US/docs/Web/API/Navigator/storage
		*/
		get storage() {
			if (!this._storage) this._storage = new StorageManager();
			return this._storage;
		}
		get [_Symbol$toStringTag$7]() {
			return "Navigator";
		}
	};
	let _navigator = null;
	function getNavigator() {
		if (!_navigator) _navigator = new Navigator();
		return _navigator;
	}
	const navigator$1 = getNavigator();
	//#endregion
	//#region ../ibex-runtime-js/src/storage/Storage.ts
	/**
	* Storage implementation for Exact runtime (localStorage/sessionStorage)
	* 
	* Implements the Web Storage API.
	* @see https://html.spec.whatwg.org/multipage/webstorage.html
	*/
	let _Symbol$toStringTag$6;
	const DEFAULT_QUOTA = 10 * 1024 * 1024;
	var _data = /* @__PURE__ */ new WeakMap();
	var _quota = /* @__PURE__ */ new WeakMap();
	var _persistent = /* @__PURE__ */ new WeakMap();
	var _nativeStorage = /* @__PURE__ */ new WeakMap();
	var _capability = /* @__PURE__ */ new WeakMap();
	var _Storage_brand = /* @__PURE__ */ new WeakSet();
	_Symbol$toStringTag$6 = Symbol.toStringTag;
	/**
	* Storage provides synchronous key-value storage.
	* 
	* Note: In Exact, localStorage is per-app, not per-origin (since there's no
	* concept of origins in a native app context).
	*/
	var Storage = class {
		constructor(persistent = true, quota = DEFAULT_QUOTA, capability) {
			_classPrivateMethodInitSpec(this, _Storage_brand);
			_classPrivateFieldInitSpec(this, _data, void 0);
			_classPrivateFieldInitSpec(this, _quota, void 0);
			_classPrivateFieldInitSpec(this, _persistent, void 0);
			_classPrivateFieldInitSpec(this, _nativeStorage, void 0);
			_classPrivateFieldInitSpec(this, _capability, void 0);
			_classPrivateFieldSet2(_data, this, /* @__PURE__ */ new Map());
			_classPrivateFieldSet2(_quota, this, quota);
			_classPrivateFieldSet2(_persistent, this, persistent);
			_classPrivateFieldSet2(_capability, this, capability ?? (persistent ? Capabilities.STORAGE_LOCAL : Capabilities.STORAGE_SESSION));
			_classPrivateFieldSet2(_nativeStorage, this, getNativeModule("storage"));
			if (_classPrivateFieldGet2(_nativeStorage, this) && persistent) _assertClassBrand(_Storage_brand, this, _loadFromNative).call(this);
		}
		/**
		* Returns the number of key/value pairs.
		*/
		get length() {
			_assertClassBrand(_Storage_brand, this, _checkCapability).call(this);
			if (_classPrivateFieldGet2(_nativeStorage, this) && _classPrivateFieldGet2(_persistent, this)) return _classPrivateFieldGet2(_nativeStorage, this).length();
			return _classPrivateFieldGet2(_data, this).size;
		}
		/**
		* Returns the name of the nth key, or null if n >= length.
		*/
		key(index) {
			_assertClassBrand(_Storage_brand, this, _checkCapability).call(this);
			if (_classPrivateFieldGet2(_nativeStorage, this) && _classPrivateFieldGet2(_persistent, this)) return _classPrivateFieldGet2(_nativeStorage, this).key(index);
			if (index < 0 || index >= _classPrivateFieldGet2(_data, this).size) return null;
			return [..._classPrivateFieldGet2(_data, this).keys()][index] ?? null;
		}
		/**
		* Returns the current value associated with the given key.
		*/
		getItem(key) {
			_assertClassBrand(_Storage_brand, this, _checkCapability).call(this);
			if (_classPrivateFieldGet2(_nativeStorage, this) && _classPrivateFieldGet2(_persistent, this)) return _classPrivateFieldGet2(_nativeStorage, this).getItem(String(key));
			return _classPrivateFieldGet2(_data, this).get(String(key)) ?? null;
		}
		/**
		* Sets the value of the pair identified by key.
		* Throws QuotaExceededError if the quota is exceeded.
		*/
		setItem(key, value) {
			_assertClassBrand(_Storage_brand, this, _checkCapability).call(this);
			const keyStr = String(key);
			const valueStr = String(value);
			if (_classPrivateFieldGet2(_nativeStorage, this) && _classPrivateFieldGet2(_persistent, this)) {
				try {
					_classPrivateFieldGet2(_nativeStorage, this).setItem(keyStr, valueStr);
				} catch (e) {
					if (e.name === "QuotaExceededError") throw e;
					throw createQuotaExceededError("Storage quota exceeded");
				}
				return;
			}
			const currentSize = _assertClassBrand(_Storage_brand, this, _calculateSize).call(this);
			const oldValue = _classPrivateFieldGet2(_data, this).get(keyStr);
			const oldSize = oldValue ? keyStr.length + oldValue.length : 0;
			const newSize = keyStr.length + valueStr.length;
			if (currentSize - oldSize + newSize > _classPrivateFieldGet2(_quota, this)) throw createQuotaExceededError("Storage quota exceeded");
			_classPrivateFieldGet2(_data, this).set(keyStr, valueStr);
		}
		/**
		* Removes the key/value pair with the given key.
		*/
		removeItem(key) {
			_assertClassBrand(_Storage_brand, this, _checkCapability).call(this);
			const keyStr = String(key);
			if (_classPrivateFieldGet2(_nativeStorage, this) && _classPrivateFieldGet2(_persistent, this)) {
				_classPrivateFieldGet2(_nativeStorage, this).removeItem(keyStr);
				return;
			}
			_classPrivateFieldGet2(_data, this).delete(keyStr);
		}
		/**
		* Removes all key/value pairs.
		*/
		clear() {
			_assertClassBrand(_Storage_brand, this, _checkCapability).call(this);
			if (_classPrivateFieldGet2(_nativeStorage, this) && _classPrivateFieldGet2(_persistent, this)) {
				_classPrivateFieldGet2(_nativeStorage, this).clear();
				return;
			}
			_classPrivateFieldGet2(_data, this).clear();
		}
		get [_Symbol$toStringTag$6]() {
			return "Storage";
		}
	};
	/**
	* Check capability before any storage operation.
	* @throws NotAllowedError if capability is not granted
	*/
	function _checkCapability() {
		requireCapability(_classPrivateFieldGet2(_capability, this));
	}
	/**
	* Calculate total size of stored data.
	*/
	function _calculateSize() {
		let size = 0;
		{
			const __exactForOfIterator81 = _classPrivateFieldGet2(_data, this)[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep81 = __exactForOfIterator81.next();
				if (__exactForOfStep81.done) break;
				const [key, value] = __exactForOfStep81.value;
				size += key.length + value.length;
			}
		}
		return size;
	}
	/**
	* Load data from native storage.
	*/
	function _loadFromNative() {
		if (!_classPrivateFieldGet2(_nativeStorage, this)) return;
		const length = _classPrivateFieldGet2(_nativeStorage, this).length();
		for (let i = 0; i < length; i++) {
			const key = _classPrivateFieldGet2(_nativeStorage, this).key(i);
			if (key !== null) {
				const value = _classPrivateFieldGet2(_nativeStorage, this).getItem(key);
				if (value !== null) _classPrivateFieldGet2(_data, this).set(key, value);
			}
		}
	}
	let _localStorage = null;
	let _sessionStorage = null;
	/**
	* Get the localStorage instance.
	* localStorage persists across sessions.
	*/
	function getLocalStorage() {
		if (!_localStorage) _localStorage = new Storage(true, DEFAULT_QUOTA, Capabilities.STORAGE_LOCAL);
		return _localStorage;
	}
	/**
	* Get the sessionStorage instance.
	* sessionStorage is cleared when the app is closed.
	*/
	function getSessionStorage() {
		if (!_sessionStorage) _sessionStorage = new Storage(false, DEFAULT_QUOTA, Capabilities.STORAGE_SESSION);
		return _sessionStorage;
	}
	function createLazyStorageProxy(factory) {
		let storage = null;
		const getStorage = () => {
			if (!storage) storage = factory();
			return storage;
		};
		return new Proxy({}, {
			get(target, prop, receiver) {
				target = getStorage();
				if (typeof prop === "string") {
					if (prop in target || prop === "length") {
						const value = Reflect.get(target, prop, target);
						return typeof value === "function" ? value.bind(target) : value;
					}
					return target.getItem(prop);
				}
				const value = Reflect.get(target, prop, target);
				return typeof value === "function" ? value.bind(target) : value;
			},
			set(target, prop, value) {
				target = getStorage();
				if (typeof prop === "string") {
					target.setItem(prop, String(value));
					return true;
				}
				return false;
			},
			deleteProperty(target, prop) {
				target = getStorage();
				if (typeof prop === "string") {
					target.removeItem(prop);
					return true;
				}
				return false;
			},
			has(target, prop) {
				target = getStorage();
				if (typeof prop === "string") return target.getItem(prop) !== null;
				return prop in target;
			},
			ownKeys(target) {
				target = getStorage();
				const keys = [];
				for (let i = 0; i < target.length; i++) {
					const key = target.key(i);
					if (key !== null) keys.push(key);
				}
				return keys;
			},
			getOwnPropertyDescriptor(target, prop) {
				target = getStorage();
				if (typeof prop === "string") {
					const value = target.getItem(prop);
					if (value !== null) return {
						value,
						writable: true,
						enumerable: true,
						configurable: true
					};
				}
			}
		});
	}
	const localStorage = createLazyStorageProxy(getLocalStorage);
	const sessionStorage = createLazyStorageProxy(getSessionStorage);
	//#endregion
	//#region ../ibex-runtime-js/src/cache/Cache.ts
	/**
	* Cache API Implementation (In-Memory)
	*
	* Implements the WHATWG Cache API interface for the Exact runtime.
	* This is a purely in-memory cache with no persistence.
	*
	* @see https://w3c.github.io/ServiceWorker/#cache-interface
	*/
	init_defineProperty();
	let _Symbol$toStringTag$5;
	/**
	* Strip the query string (search) from a URL string.
	*/
	function stripSearch(url) {
		const idx = url.indexOf("?");
		if (idx === -1) return url;
		const hashIdx = url.indexOf("#", idx);
		if (hashIdx === -1) return url.substring(0, idx);
		return url.substring(0, idx) + url.substring(hashIdx);
	}
	/**
	* Normalize input into a Request object.
	*/
	function toRequest(input) {
		if (input instanceof Request) return input;
		return new Request(input);
	}
	function toFetchHeaders(headers) {
		const result = {};
		headers.forEach((value, name) => {
			result[name] = value;
		});
		return result;
	}
	_Symbol$toStringTag$5 = Symbol.toStringTag;
	/**
	* The Cache interface provides a persistent storage mechanism for
	* Request/Response object pairs. In this implementation, storage
	* is purely in-memory with no disk persistence.
	*/
	var Cache = class {
		constructor() {
			_defineProperty(this, "_entries", []);
		}
		/**
		* Check if a request URL matches a cached entry URL.
		*/
		_urlMatches(cachedUrl, requestUrl, options) {
			if (options?.ignoreSearch) return stripSearch(cachedUrl) === stripSearch(requestUrl);
			return cachedUrl === requestUrl;
		}
		/**
		* Check if a request matches a cached entry considering all options.
		*/
		_requestMatches(cachedRequest, request, options) {
			if (!options?.ignoreMethod && cachedRequest.method !== request.method) return false;
			if (!this._urlMatches(cachedRequest.url, request.url, options)) return false;
			if (!options?.ignoreVary) {
				const varyHeader = this._getVaryHeaderForEntry(cachedRequest);
				if (varyHeader) {
					const varyFields = varyHeader.split(",").map((f) => f.trim().toLowerCase());
					if (varyFields.includes("*")) return false;
					for (const field of varyFields) if (cachedRequest.headers.get(field) !== request.headers.get(field)) return false;
				}
			}
			return true;
		}
		/**
		* Get the Vary header value for a cached entry's response.
		*/
		_getVaryHeaderForEntry(cachedRequest) {
			return this._entries.find((e) => e.request === cachedRequest)?.response.headers.get("vary") ?? null;
		}
		/**
		* Returns the first matching Response for a given request, or undefined.
		*
		* @param request - The request to match against cached entries.
		* @param options - Options to control matching behavior.
		* @returns A Promise resolving to the matching Response, or undefined.
		*/
		async match(request, options) {
			const req = toRequest(request);
			if (!options?.ignoreMethod && req.method !== "GET") return;
			for (const entry of this._entries) if (this._requestMatches(entry.request, req, options)) return entry.response.clone();
		}
		/**
		* Returns all matching Responses for a given request.
		* If no request is provided, returns all cached responses.
		*
		* @param request - The request to match, or undefined to return all.
		* @param options - Options to control matching behavior.
		* @returns A Promise resolving to an array of matching Responses.
		*/
		async matchAll(request, options) {
			if (request === void 0) return this._entries.map((entry) => entry.response.clone());
			const req = toRequest(request);
			if (!options?.ignoreMethod && req.method !== "GET") return [];
			const results = [];
			{
				const __exactForOfIterator82 = this._entries[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep82 = __exactForOfIterator82.next();
					if (__exactForOfStep82.done) break;
					const entry = __exactForOfStep82.value;
					if (this._requestMatches(entry.request, req, options)) results.push(entry.response.clone());
				}
			}
			return results;
		}
		/**
		* Fetches a URL and adds the response to the cache.
		* Throws if the response is not ok (status 200-299).
		*
		* @param request - The request or URL to fetch and cache.
		*/
		async add(request) {
			await this.addAll([request]);
		}
		/**
		* Fetches multiple URLs and adds all responses to the cache.
		* Throws if any response is not ok (status 200-299).
		*
		* @param requests - The requests or URLs to fetch and cache.
		*/
		async addAll(requests) {
			const reqs = requests.map((r) => toRequest(r));
			{
				const __exactForOfIterator83 = reqs[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep83 = __exactForOfIterator83.next();
					if (__exactForOfStep83.done) break;
					if (__exactForOfStep83.value.method !== "GET") throw new TypeError("Request method must be GET for Cache.add/addAll");
				}
			}
			const fetchFn = typeof globalThis.fetch === "function" ? globalThis.fetch : null;
			if (!fetchFn) throw new TypeError("Cache.add/addAll requires a global fetch function");
			const responses = await Promise.all(reqs.map((req) => fetchFn(req.url, {
				method: req.method,
				headers: toFetchHeaders(req.headers)
			})));
			for (let i = 0; i < responses.length; i++) {
				const response = responses[i];
				if (!response.ok) throw new TypeError(`Request failed with status ${response.status}: ${reqs[i].url}`);
			}
			for (let i = 0; i < reqs.length; i++) await this.put(reqs[i], responses[i]);
		}
		/**
		* Stores a request/response pair in the cache.
		* The response is cloned before storing so the original can still be used.
		* Any existing entry with the same URL is replaced.
		*
		* @param request - The request to use as the cache key.
		* @param response - The response to store.
		*/
		async put(request, response) {
			const req = toRequest(request);
			if (req.method !== "GET") throw new TypeError("Request method must be GET for Cache.put");
			if (response.status === 206) throw new TypeError("Cannot cache partial (206) responses");
			this._entries = this._entries.filter((entry) => entry.request.url !== req.url);
			this._entries.push({
				request: new Request(req.url, {
					method: req.method,
					headers: toFetchHeaders(req.headers),
					mode: req.mode,
					credentials: req.credentials,
					cache: req.cache,
					redirect: req.redirect,
					referrer: req.referrer,
					referrerPolicy: req.referrerPolicy,
					integrity: req.integrity,
					signal: null
				}),
				response: response.clone()
			});
		}
		/**
		* Deletes all cached entries matching the given request.
		*
		* @param request - The request to match for deletion.
		* @param options - Options to control matching behavior.
		* @returns A Promise resolving to true if any entry was deleted, false otherwise.
		*/
		async delete(request, options) {
			const req = toRequest(request);
			const initialLength = this._entries.length;
			this._entries = this._entries.filter((entry) => !this._requestMatches(entry.request, req, options));
			return this._entries.length < initialLength;
		}
		/**
		* Returns all cached request keys.
		* If a request is provided, only matching keys are returned.
		*
		* @param request - Optional request to filter keys.
		* @param options - Options to control matching behavior.
		* @returns A Promise resolving to an array of cached Request objects.
		*/
		async keys(request, options) {
			if (request === void 0) return this._entries.map((entry) => entry.request.clone());
			const req = toRequest(request);
			const results = [];
			{
				const __exactForOfIterator84 = this._entries[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep84 = __exactForOfIterator84.next();
					if (__exactForOfStep84.done) break;
					const entry = __exactForOfStep84.value;
					if (this._requestMatches(entry.request, req, options)) results.push(entry.request.clone());
				}
			}
			return results;
		}
		get [_Symbol$toStringTag$5]() {
			return "Cache";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/cache/CacheStorage.ts
	/**
	* CacheStorage API Implementation (In-Memory)
	*
	* Implements the WHATWG CacheStorage interface for the Exact runtime.
	* Manages multiple named Cache instances, all purely in-memory.
	*
	* @see https://w3c.github.io/ServiceWorker/#cachestorage-interface
	*/
	init_defineProperty();
	let _Symbol$toStringTag$4;
	_Symbol$toStringTag$4 = Symbol.toStringTag;
	/**
	* The CacheStorage interface represents the storage for Cache objects.
	* It provides a master directory of all named caches accessible to the runtime.
	*/
	var CacheStorage = class {
		constructor() {
			_defineProperty(this, "_caches", /* @__PURE__ */ new Map());
		}
		/**
		* Opens and returns the named cache. If the cache does not exist,
		* a new empty cache is created with that name.
		*
		* @param cacheName - The name of the cache to open.
		* @returns A Promise resolving to the Cache object.
		*/
		async open(cacheName) {
			let cache = this._caches.get(cacheName);
			if (!cache) {
				cache = new Cache();
				this._caches.set(cacheName, cache);
			}
			return cache;
		}
		/**
		* Checks if a cache with the given name exists.
		*
		* @param cacheName - The name of the cache to check.
		* @returns A Promise resolving to true if the cache exists, false otherwise.
		*/
		async has(cacheName) {
			return this._caches.has(cacheName);
		}
		/**
		* Deletes the named cache.
		*
		* @param cacheName - The name of the cache to delete.
		* @returns A Promise resolving to true if the cache was deleted, false if it didn't exist.
		*/
		async delete(cacheName) {
			return this._caches.delete(cacheName);
		}
		/**
		* Returns the names of all caches managed by this CacheStorage.
		*
		* @returns A Promise resolving to an array of cache names.
		*/
		async keys() {
			return [...this._caches.keys()];
		}
		/**
		* Searches all caches (in creation order) for the first matching response.
		*
		* @param request - The request to match.
		* @param options - Options to control matching behavior.
		* @returns A Promise resolving to the first matching Response, or undefined.
		*/
		async match(request, options) {
			for (const cache of this._caches.values()) {
				const response = await cache.match(request, options);
				if (response !== void 0) return response;
			}
		}
		get [_Symbol$toStringTag$4]() {
			return "CacheStorage";
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/performance/Performance.ts
	/**
	* Performance - Web Standard Performance API Implementation
	*
	* @see https://www.w3.org/TR/hr-time/
	* @see https://www.w3.org/TR/user-timing/
	*/
	init_defineProperty();
	var PerformanceEntry = class {
		constructor(init) {
			_defineProperty(this, "name", void 0);
			_defineProperty(this, "entryType", void 0);
			_defineProperty(this, "startTime", void 0);
			_defineProperty(this, "duration", void 0);
			this.name = init.name;
			this.entryType = init.entryType;
			this.startTime = init.startTime;
			this.duration = init.duration;
		}
		toJSON() {
			return {
				name: this.name,
				entryType: this.entryType,
				startTime: this.startTime,
				duration: this.duration
			};
		}
	};
	var PerformanceMark = class extends PerformanceEntry {
		constructor(name, options) {
			super({
				name,
				entryType: "mark",
				startTime: options?.startTime ?? performance$1?.now?.() ?? Date.now(),
				duration: 0
			});
			_defineProperty(this, "detail", void 0);
			this.detail = options?.detail;
		}
	};
	var PerformanceMeasure = class extends PerformanceEntry {
		constructor(name, startTime, duration, detail) {
			super({
				name,
				entryType: "measure",
				startTime,
				duration
			});
			_defineProperty(this, "detail", void 0);
			this.detail = detail;
		}
	};
	const fallbackTimeOrigin = Date.now();
	const fallbackModule$1 = {
		now: () => Date.now() - fallbackTimeOrigin,
		timeOrigin: fallbackTimeOrigin
	};
	function getModule$1() {
		return fallbackModule$1;
	}
	var Performance = class {
		constructor() {
			_defineProperty(this, "_marks", /* @__PURE__ */ new Map());
			_defineProperty(this, "_measures", /* @__PURE__ */ new Map());
			_defineProperty(this, "_entries", []);
		}
		/**
		* Time origin (when the runtime started)
		*/
		get timeOrigin() {
			return getModule$1().timeOrigin;
		}
		/**
		* Current high-resolution timestamp in milliseconds
		*/
		now() {
			return getModule$1().now();
		}
		/**
		* Create a performance mark
		*/
		mark(markName, markOptions) {
			const mark = new PerformanceMark(markName, {
				...markOptions,
				startTime: markOptions?.startTime ?? this.now()
			});
			let marks = this._marks.get(markName);
			if (!marks) {
				marks = [];
				this._marks.set(markName, marks);
			}
			marks.push(mark);
			this._entries.push(mark);
			return mark;
		}
		/**
		* Create a performance measure between two marks or times
		*/
		measure(measureName, startOrMeasureOptions, endMark) {
			let startTime;
			let endTime;
			let detail;
			if (typeof startOrMeasureOptions === "string" || startOrMeasureOptions === void 0) {
				const startMarkName = startOrMeasureOptions;
				if (startMarkName) {
					const startMarks = this._marks.get(startMarkName);
					if (!startMarks || startMarks.length === 0) throw new DOMException(`Failed to execute 'measure': The mark '${startMarkName}' does not exist.`, "SyntaxError");
					startTime = startMarks[startMarks.length - 1].startTime;
				} else startTime = 0;
				if (endMark) {
					const endMarks = this._marks.get(endMark);
					if (!endMarks || endMarks.length === 0) throw new DOMException(`Failed to execute 'measure': The mark '${endMark}' does not exist.`, "SyntaxError");
					endTime = endMarks[endMarks.length - 1].startTime;
				} else endTime = this.now();
			} else {
				const options = startOrMeasureOptions;
				detail = options.detail;
				if (options.start !== void 0) if (typeof options.start === "string") {
					const startMarks = this._marks.get(options.start);
					if (!startMarks || startMarks.length === 0) throw new DOMException(`Failed to execute 'measure': The mark '${options.start}' does not exist.`, "SyntaxError");
					startTime = startMarks[startMarks.length - 1].startTime;
				} else startTime = options.start;
				else startTime = 0;
				if (options.end !== void 0) if (typeof options.end === "string") {
					const endMarks = this._marks.get(options.end);
					if (!endMarks || endMarks.length === 0) throw new DOMException(`Failed to execute 'measure': The mark '${options.end}' does not exist.`, "SyntaxError");
					endTime = endMarks[endMarks.length - 1].startTime;
				} else endTime = options.end;
				else if (options.duration !== void 0) endTime = startTime + options.duration;
				else endTime = this.now();
			}
			const duration = endTime - startTime;
			const measure = new PerformanceMeasure(measureName, startTime, duration, detail);
			let measures = this._measures.get(measureName);
			if (!measures) {
				measures = [];
				this._measures.set(measureName, measures);
			}
			measures.push(measure);
			this._entries.push(measure);
			return measure;
		}
		/**
		* Clear marks by name (or all marks if no name given)
		*/
		clearMarks(markName) {
			if (markName) {
				this._marks.delete(markName);
				this._entries = this._entries.filter((e) => !(e.entryType === "mark" && e.name === markName));
			} else {
				this._marks.clear();
				this._entries = this._entries.filter((e) => e.entryType !== "mark");
			}
		}
		/**
		* Clear measures by name (or all measures if no name given)
		*/
		clearMeasures(measureName) {
			if (measureName) {
				this._measures.delete(measureName);
				this._entries = this._entries.filter((e) => !(e.entryType === "measure" && e.name === measureName));
			} else {
				this._measures.clear();
				this._entries = this._entries.filter((e) => e.entryType !== "measure");
			}
		}
		/**
		* Get all entries
		*/
		getEntries() {
			return [...this._entries].sort((a, b) => a.startTime - b.startTime);
		}
		/**
		* Get entries by name
		*/
		getEntriesByName(name, type) {
			return this._entries.filter((e) => e.name === name && (!type || e.entryType === type)).sort((a, b) => a.startTime - b.startTime);
		}
		/**
		* Get entries by type
		*/
		getEntriesByType(type) {
			return this._entries.filter((e) => e.entryType === type).sort((a, b) => a.startTime - b.startTime);
		}
		clearResourceTimings() {}
		setResourceTimingBufferSize(_maxSize) {}
		toJSON() {
			return { timeOrigin: this.timeOrigin };
		}
	};
	Object.setPrototypeOf(Performance.prototype, EventTarget.prototype);
	const performance$1 = new Performance();
	performance$1._listeners = /* @__PURE__ */ new Map();
	var PerformanceObserverEntryList = class {
		constructor(entries) {
			_defineProperty(this, "_entries", void 0);
			this._entries = entries;
		}
		getEntries() {
			return [...this._entries].sort((a, b) => a.startTime - b.startTime);
		}
		getEntriesByName(name, type) {
			return this._entries.filter((e) => e.name === name && (!type || e.entryType === type)).sort((a, b) => a.startTime - b.startTime);
		}
		getEntriesByType(type) {
			return this._entries.filter((e) => e.entryType === type).sort((a, b) => a.startTime - b.startTime);
		}
	};
	const observers = /* @__PURE__ */ new Set();
	/**
	* PerformanceObserver enables observing performance timeline entries.
	*/
	var PerformanceObserver = class PerformanceObserver {
		/**
		* Supported entry types.
		*/
		static get supportedEntryTypes() {
			return Object.freeze(["mark", "measure"]);
		}
		constructor(callback) {
			_defineProperty(this, "_callback", void 0);
			_defineProperty(this, "_entryTypes", /* @__PURE__ */ new Set());
			_defineProperty(this, "_buffer", []);
			_defineProperty(this, "_isObserving", false);
			_defineProperty(this, "_scheduled", false);
			if (typeof callback !== "function") throw new TypeError("Callback must be a function");
			this._callback = callback;
		}
		/**
		* Start observing performance entries.
		*/
		observe(options) {
			if (!options.entryTypes && !options.type) throw new TypeError("Either entryTypes or type must be specified");
			if (options.entryTypes && options.type) throw new TypeError("Cannot specify both entryTypes and type");
			this._entryTypes.clear();
			if (options.entryTypes) for (let i = 0; i < options.entryTypes.length; i++) {
				const type = options.entryTypes[i];
				if (PerformanceObserver.supportedEntryTypes.includes(type)) this._entryTypes.add(type);
			}
			else if (options.type) {
				if (PerformanceObserver.supportedEntryTypes.includes(options.type)) this._entryTypes.add(options.type);
			}
			if (this._entryTypes.size === 0) return;
			if (options.buffered && options.type) {
				const existingEntries = [];
				const timelineEntries = performance$1.getEntries();
				for (let i = 0; i < timelineEntries.length; i++) {
					const entry = timelineEntries[i];
					if (this._entryTypes.has(entry.entryType)) existingEntries.push(entry);
				}
				if (existingEntries.length > 0) {
					this._buffer.push(...existingEntries);
					this._scheduleCallback();
				}
			}
			this._isObserving = true;
			observers.add(this);
		}
		/**
		* Stop observing.
		*/
		disconnect() {
			this._isObserving = false;
			this._buffer = [];
			observers.delete(this);
		}
		/**
		* Get buffered entries and clear the buffer.
		*/
		takeRecords() {
			const entries = [...this._buffer];
			this._buffer = [];
			return entries;
		}
		/**
		* Internal: Called when a new entry is added.
		*/
		_notify(entry) {
			if (!this._isObserving) return;
			if (!this._entryTypes.has(entry.entryType)) return;
			this._buffer.push(entry);
			this._scheduleCallback();
		}
		_scheduleCallback() {
			if (this._scheduled) return;
			this._scheduled = true;
			const observer = this;
			queueMicrotask(function() {
				observer._scheduled = false;
				if (observer._buffer.length === 0) return;
				const list = new PerformanceObserverEntryList(observer.takeRecords());
				try {
					observer._callback(list, observer);
				} catch (e) {
					console.error("PerformanceObserver callback error:", e);
				}
			});
		}
	};
	/**
	* Notify all observers of a new entry.
	* Called internally when marks/measures are created.
	*/
	function notifyObservers(entry) {
		{
			const __exactForOfIterator85 = observers[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep85 = __exactForOfIterator85.next();
				if (__exactForOfStep85.done) break;
				__exactForOfStep85.value._notify(entry);
			}
		}
	}
	const originalMark = Performance.prototype.mark;
	Performance.prototype.mark = function(markName, markOptions) {
		const mark = originalMark.call(this, markName, markOptions);
		notifyObservers(mark);
		return mark;
	};
	const originalMeasure = Performance.prototype.measure;
	Performance.prototype.measure = function(measureName, startOrMeasureOptions, endMark) {
		const measure = originalMeasure.call(this, measureName, startOrMeasureOptions, endMark);
		notifyObservers(measure);
		return measure;
	};
	//#endregion
	//#region ../ibex-runtime-js/src/base64/base64.ts
	init_defineProperty();
	/**
	* Base64 Encoding - atob/btoa
	*
	* @see https://html.spec.whatwg.org/multipage/webappapis.html#atob
	*
	* Note: These only handle Latin-1 (bytes 0-255).
	* For Unicode, use TextEncoder first.
	*/
	const BASE64_CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
	const BASE64_LOOKUP = new Uint8Array(256);
	for (let i = 0; i < 64; i++) BASE64_LOOKUP[BASE64_CHARS.charCodeAt(i)] = i;
	BASE64_LOOKUP["=".charCodeAt(0)] = 0;
	/**
	* btoa - Encode a binary string to Base64
	*
	* @param data Binary string (each char code must be 0-255)
	* @returns Base64 encoded string
	* @throws DOMException if string contains characters outside Latin-1
	*/
	function btoa$1(data) {
		if (data === void 0 || data === null) data = "undefined";
		const str = String(data);
		const bytes = [];
		for (let i = 0; i < str.length; i++) {
			const charCode = str.charCodeAt(i);
			if (charCode > 255) throw new DOMException$2("The string to be encoded contains characters outside of the Latin1 range.", "InvalidCharacterError");
			bytes.push(charCode);
		}
		let result = "";
		let i = 0;
		while (i < bytes.length) {
			const b1 = bytes[i++] ?? 0;
			const b2 = bytes[i++];
			const b3 = bytes[i++];
			result += BASE64_CHARS[b1 >> 2];
			result += BASE64_CHARS[(b1 & 3) << 4 | (b2 ?? 0) >> 4];
			if (b2 !== void 0) result += BASE64_CHARS[(b2 & 15) << 2 | (b3 ?? 0) >> 6];
			else result += "=";
			if (b3 !== void 0) result += BASE64_CHARS[b3 & 63];
			else result += "=";
		}
		return result;
	}
	/**
	* atob - Decode a Base64 string to binary
	*
	* @param data Base64 encoded string
	* @returns Decoded binary string
	* @throws DOMException if string is not valid Base64
	*/
	function atob$1(data) {
		if (data === void 0 || data === null) throw new DOMException$2("The string to be decoded is not correctly encoded.", "InvalidCharacterError");
		let str = "";
		const raw = String(data);
		for (let i = 0; i < raw.length; i++) {
			const c = raw.charCodeAt(i);
			if (c !== 9 && c !== 10 && c !== 12 && c !== 13 && c !== 32) str += raw[i];
		}
		if (str.length === 0) return "";
		let len = str.length;
		if (len % 4 === 0) {
			if (str[len - 1] === "=") len--;
			if (str[len - 1] === "=") len--;
			str = str.slice(0, len);
		}
		if (str.length % 4 === 1) throw new DOMException$2("The string to be decoded is not correctly encoded.", "InvalidCharacterError");
		for (let i = 0; i < str.length; i++) if (!BASE64_CHARS.includes(str[i])) throw new DOMException$2("The string to be decoded is not correctly encoded.", "InvalidCharacterError");
		const remainder = str.length % 4;
		if (remainder === 2) str += "==";
		else if (remainder === 3) str += "=";
		const result = [];
		let i = 0;
		while (i < str.length) {
			const c1 = BASE64_LOOKUP[str.charCodeAt(i++)];
			const c2 = BASE64_LOOKUP[str.charCodeAt(i++)];
			const c3 = BASE64_LOOKUP[str.charCodeAt(i++)];
			const c4 = BASE64_LOOKUP[str.charCodeAt(i++)];
			result.push(c1 << 2 | c2 >> 4);
			if (str[i - 2] !== "=") result.push((c2 & 15) << 4 | c3 >> 2);
			if (str[i - 1] !== "=") result.push((c3 & 3) << 6 | c4);
		}
		return String.fromCharCode(...result);
	}
	var DOMException$2 = class extends Error {
		constructor(message, name) {
			super(message);
			_defineProperty(this, "code", 0);
			this.name = name;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/core/agent-state.ts
	const activeAnimationFrames = /* @__PURE__ */ new Set();
	function trackAnimationFrameRequested(id) {
		activeAnimationFrames.add(id);
	}
	function trackAnimationFrameCancelled(id) {
		if (activeAnimationFrames.delete(id));
	}
	function trackAnimationFrameExecuted(ids) {
		let changed = false;
		{
			const __exactForOfIterator86 = ids[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep86 = __exactForOfIterator86.next();
				if (__exactForOfStep86.done) break;
				const id = __exactForOfStep86.value;
				changed = activeAnimationFrames.delete(id) || changed;
			}
		}
		if (changed);
	}
	//#endregion
	//#region ../ibex-runtime-js/src/scheduling/AnimationFrame.ts
	/**
	* requestAnimationFrame / cancelAnimationFrame implementation for Exact runtime
	* 
	* Schedules callbacks to run before the next repaint.
	* @see https://html.spec.whatwg.org/multipage/imagebitmap-and-animations.html#dom-animationframeprovider-requestanimationframe
	*/
	const callbacks$1 = /* @__PURE__ */ new Map();
	let nextId$2 = 1;
	let isScheduled$1 = false;
	const FRAME_TIME = 16;
	/**
	* Requests that a callback be called before the next repaint.
	* Returns an ID that can be used to cancel the request.
	*/
	function requestAnimationFrame$1(callback) {
		if (typeof callback !== "function") throw new TypeError("Failed to execute 'requestAnimationFrame': parameter 1 is not of type 'Function'.");
		const id = nextId$2++;
		callbacks$1.set(id, callback);
		trackAnimationFrameRequested(id);
		const nativeScheduling = getNativeModule("scheduling");
		if (nativeScheduling?.requestAnimationFrame) {
			nativeScheduling.requestAnimationFrame(() => {
				runCallbacks();
			});
			return id;
		}
		if (!isScheduled$1) {
			isScheduled$1 = true;
			setTimeout(() => {
				isScheduled$1 = false;
				runCallbacks();
			}, fallbackFrameDelayMs());
		}
		return id;
	}
	/**
	* Cancels a previously scheduled animation frame request.
	*/
	function cancelAnimationFrame$1(id) {
		if (callbacks$1.delete(id)) trackAnimationFrameCancelled(id);
	}
	/**
	* Run all pending callbacks with the current timestamp.
	*/
	function runCallbacks() {
		const timestamp = typeof performance !== "undefined" && performance.now ? performance.now() : Date.now();
		const pending = new Map(callbacks$1);
		callbacks$1.clear();
		trackAnimationFrameExecuted(Array.from(pending.keys()));
		{
			const __exactForOfIterator87 = pending[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep87 = __exactForOfIterator87.next();
				if (__exactForOfStep87.done) break;
				const [, callback] = __exactForOfStep87.value;
				try {
					callback(timestamp);
				} catch (error) {
					console.error("Error in requestAnimationFrame callback:", error);
				}
			}
		}
	}
	function fallbackFrameDelayMs() {
		return globalThis.__exactDisplayLinkedEventLoop === true ? 0 : FRAME_TIME;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/scheduling/IdleCallback.ts
	/**
	* requestIdleCallback / cancelIdleCallback implementation for Exact runtime
	* 
	* Schedules callbacks to run during idle periods.
	* @see https://w3c.github.io/requestidlecallback/
	*/
	const callbacks = /* @__PURE__ */ new Map();
	let nextId$1 = 1;
	let isScheduled = false;
	const MAX_IDLE_PERIOD = 50;
	const IDLE_CHECK_INTERVAL = 100;
	/**
	* Requests that a callback be called during an idle period.
	* Returns an ID that can be used to cancel the request.
	*/
	function requestIdleCallback(callback, options) {
		if (typeof callback !== "function") throw new TypeError("Failed to execute 'requestIdleCallback': parameter 1 is not of type 'Function'.");
		const id = nextId$1++;
		const timeout = options?.timeout;
		const scheduledTime = performance?.now?.() ?? Date.now();
		callbacks.set(id, {
			callback,
			timeout,
			scheduledTime
		});
		const nativeScheduling = getNativeModule("scheduling");
		if (nativeScheduling?.requestIdleCallback) {
			nativeScheduling.requestIdleCallback((deadline) => {
				runCallback(id, deadline);
			}, options);
			return id;
		}
		scheduleIdleCheck();
		if (timeout !== void 0 && timeout > 0) setTimeout(() => {
			if (callbacks.get(id)) runCallback(id, createDeadline(true, 0));
		}, timeout);
		return id;
	}
	/**
	* Cancels a previously scheduled idle callback.
	*/
	function cancelIdleCallback(id) {
		callbacks.delete(id);
	}
	/**
	* Schedule a check for idle time.
	*/
	function scheduleIdleCheck() {
		if (isScheduled || callbacks.size === 0) return;
		isScheduled = true;
		setTimeout(() => {
			isScheduled = false;
			runIdleCallbacks();
		}, IDLE_CHECK_INTERVAL);
	}
	/**
	* Run callbacks that can fit in the current idle period.
	*/
	function runIdleCallbacks() {
		const now = performance?.now?.() ?? Date.now();
		const idleDeadline = now + MAX_IDLE_PERIOD;
		for (const [id, pending] of callbacks) {
			const remaining = idleDeadline - (performance?.now?.() ?? Date.now());
			if (remaining <= 0) {
				scheduleIdleCheck();
				break;
			}
			const elapsed = now - pending.scheduledTime;
			runCallback(id, createDeadline(pending.timeout !== void 0 && elapsed >= pending.timeout, remaining));
		}
	}
	/**
	* Run a specific callback and remove it from the pending set.
	*/
	function runCallback(id, deadline) {
		const pending = callbacks.get(id);
		if (!pending) return;
		callbacks.delete(id);
		try {
			pending.callback(deadline);
		} catch (error) {
			console.error("Error in requestIdleCallback:", error);
		}
	}
	/**
	* Create an IdleDeadline object.
	*/
	function createDeadline(didTimeout, remainingTime) {
		const deadlineEnd = (performance?.now?.() ?? Date.now()) + remainingTime;
		return {
			didTimeout,
			timeRemaining() {
				const now = performance?.now?.() ?? Date.now();
				return Math.max(0, deadlineEnd - now);
			}
		};
	}
	//#endregion
	//#region ../ibex-runtime-js/src/compression/CompressionStream.ts
	init_defineProperty();
	/**
	* CompressionStream - Web Compression API
	*
	* Implements the CompressionStream class that compresses data using gzip, deflate, deflate-raw, or brotli.
	* Uses the native __exactDeflateSync bridge for actual compression.
	*/
	const g$4 = globalThis;
	function toTypeError$1(error) {
		if (error instanceof TypeError) return error;
		if (error instanceof Error) {
			let message = "TypeError";
			try {
				message = String(error.message);
			} catch (_ignored) {
				message = "TypeError";
			}
			return new TypeError(message);
		}
		try {
			return new TypeError(String(error));
		} catch (_ignored) {
			return /* @__PURE__ */ new TypeError("TypeError");
		}
	}
	var CompressionStream = class {
		constructor(format) {
			_defineProperty(this, "_format", void 0);
			_defineProperty(this, "_readable", void 0);
			_defineProperty(this, "_writable", void 0);
			if (format !== "gzip" && format !== "deflate" && format !== "deflate-raw" && format !== "brotli") throw new TypeError(`Unsupported compression format: '${format}'`);
			this._format = format;
			const mode = format === "gzip" ? 1 : format === "deflate-raw" ? 2 : 0;
			const compressChunk = (chunk) => {
				const input = chunk instanceof Uint8Array ? chunk : new Uint8Array(chunk);
				if (format === "brotli") {
					if (typeof g$4.__exactBrotliCompressSync !== "function") throw new Error("Native compression bridge (__exactBrotliCompressSync) not available");
					return g$4.__exactBrotliCompressSync(input);
				}
				if (typeof g$4.__exactDeflateSync !== "function") throw new Error("Native compression bridge (__exactDeflateSync) not available");
				return g$4.__exactDeflateSync(input, -1, mode);
			};
			let streamInput = new Uint8Array(0);
			let streamError = null;
			const appendInput = (chunk) => {
				const next = new Uint8Array(streamInput.length + chunk.length);
				next.set(streamInput, 0);
				next.set(chunk, streamInput.length);
				streamInput = next;
			};
			let readableController;
			const readable = new ReadableStream({
				start(controller) {
					readableController = controller;
				},
				cancel() {
					readableController = void 0;
				}
			});
			const writable = new WritableStream({
				write(chunk) {
					if (streamError) throw streamError;
					if (chunk && chunk.length > 0) appendInput(chunk);
				},
				close() {
					if (streamError) {
						if (readableController) readableController.error(streamError);
						return;
					}
					try {
						const compressed = compressChunk(streamInput);
						if (readableController && compressed.length > 0) readableController.enqueue(compressed);
						if (readableController) readableController.close();
					} catch (e) {
						streamError = e instanceof TypeError ? e : toTypeError$1(e);
						if (readableController) readableController.error(streamError);
						throw streamError;
					}
				},
				abort(reason) {
					if (reason instanceof Error) streamError = toTypeError$1(reason);
					else streamError = new TypeError(String(reason));
					if (readableController) readableController.error(streamError);
				}
			});
			this._readable = readable;
			this._writable = writable;
		}
		get readable() {
			return this._readable;
		}
		get writable() {
			return this._writable;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/compression/DecompressionStream.ts
	init_defineProperty();
	/**
	* DecompressionStream - Web Compression API
	*
	* Implements the DecompressionStream class that decompresses data using gzip, deflate, deflate-raw, or brotli.
	* Uses the native __exactInflateSync bridge for actual decompression.
	*/
	const g$3 = globalThis;
	const trailingDataErrorSymbol = Symbol.for("__exact.decompression.trailing-data-error");
	function installTrailingDataUnhandledFilter() {
		if (g$3.__exactHasDecompressionUnhandledFilter) return;
		if (typeof g$3.addEventListener !== "function") return;
		g$3.__exactHasDecompressionUnhandledFilter = true;
		g$3.addEventListener("unhandledrejection", (event) => {
			if (event && event.reason && event.reason[trailingDataErrorSymbol]) {
				if (typeof event.preventDefault === "function") event.preventDefault();
				if (typeof event.stopImmediatePropagation === "function") event.stopImmediatePropagation();
				if (typeof event.stopPropagation === "function") event.stopPropagation();
			}
		});
	}
	installTrailingDataUnhandledFilter();
	if (!g$3.__exactHasDecompressionUnhandledFilter) {
		if (typeof g$3.queueMicrotask === "function") g$3.queueMicrotask(installTrailingDataUnhandledFilter);
		else if (typeof g$3.setTimeout === "function") {
			const _h = g$3.setTimeout(installTrailingDataUnhandledFilter, 0);
			if (_h && typeof _h === "object" && typeof _h.unref === "function") _h.unref();
		}
	}
	function toTypeError(error) {
		if (error instanceof TypeError) return error;
		if (error instanceof Error) {
			let message = "TypeError";
			try {
				message = String(error.message);
			} catch (_ignored) {
				message = "TypeError";
			}
			return new TypeError(message);
		}
		try {
			return new TypeError(String(error));
		} catch (_ignored) {
			return /* @__PURE__ */ new TypeError("TypeError");
		}
	}
	function isRecoverableIncompleteError(error) {
		return error.message.toLowerCase().includes("incomplete data");
	}
	function isTrailingDataError(error) {
		return error.message.toLowerCase().includes("trailing data");
	}
	function markTrailingDataError(error) {
		const flagged = error;
		flagged[trailingDataErrorSymbol] = true;
		return error;
	}
	var DecompressionStream = class {
		constructor(format) {
			_defineProperty(this, "_format", void 0);
			_defineProperty(this, "_readable", void 0);
			_defineProperty(this, "_writable", void 0);
			if (format !== "gzip" && format !== "deflate" && format !== "deflate-raw" && format !== "brotli") throw new TypeError(`Unsupported decompression format: '${format}'`);
			this._format = format;
			const mode = format === "gzip" ? 1 : format === "deflate-raw" ? 2 : 0;
			const decompressChunk = (chunk, strict = false) => {
				const input = chunk instanceof Uint8Array ? chunk : new Uint8Array(chunk);
				if (format === "brotli") {
					if (typeof g$3.__exactBrotliDecompressSync !== "function") throw new Error("Native decompression bridge (__exactBrotliDecompressSync) not available");
					try {
						return g$3.__exactBrotliDecompressSync(input, strict);
					} catch (error) {
						throw toTypeError(error);
					}
				}
				if (typeof g$3.__exactInflateSync !== "function") throw new Error("Native decompression bridge (__exactInflateSync) not available");
				try {
					return g$3.__exactInflateSync(input, mode, strict);
				} catch (error) {
					throw toTypeError(error);
				}
			};
			let streamError = null;
			let streamFinished = false;
			let trailingDataError = false;
			let writableClosed = false;
			let readableController;
			const isBrotliFormat = format === "brotli";
			let streamInput = new Uint8Array(0);
			const appendInput = (chunk) => {
				if (chunk.length === 0) return;
				const combined = new Uint8Array(streamInput.length + chunk.length);
				combined.set(streamInput, 0);
				combined.set(chunk, streamInput.length);
				streamInput = combined;
			};
			const validateChunkCompletion = () => {
				if (streamError || streamFinished) return;
				try {
					decompressChunk(streamInput, false);
					streamFinished = true;
				} catch (error) {
					const decodedError = toTypeError(error);
					if (isRecoverableIncompleteError(decodedError)) return;
					if (isTrailingDataError(decodedError)) {
						markTrailingDataError(decodedError);
						streamError = decodedError;
						trailingDataError = true;
						streamFinished = true;
						return;
					}
					streamError = decodedError;
					if (readableController) readableController.error(streamError);
				}
			};
			const readable = new ReadableStream({
				start(controller) {
					readableController = controller;
					if (streamError) {
						controller.error(streamError);
						return;
					}
				},
				pull(controller) {
					if (!streamFinished && streamInput.length > 0 && !streamError) validateChunkCompletion();
					if (streamError && trailingDataError) {
						controller.error(streamError);
						return Promise.resolve();
					}
					if (streamError) {
						controller.error(streamError);
						return Promise.resolve();
					}
					if (writableClosed && streamError === null && !streamFinished) {
						streamError = /* @__PURE__ */ new TypeError("Decompression stream failed: incomplete data");
						controller.error(streamError);
						return Promise.resolve();
					}
					if (writableClosed && readableController && streamError === null) {
						controller.close();
						return Promise.resolve();
					}
					return Promise.resolve();
				},
				cancel() {
					writableClosed = true;
					return Promise.resolve();
				}
			});
			const writable = new WritableStream({
				write(chunk) {
					try {
						if (streamError) throw streamError;
						if (isBrotliFormat) {
							if (chunk.length === 0) return;
							const output = decompressChunk(chunk);
							streamFinished = true;
							if (output.byteLength > 0 && readableController) readableController.enqueue(output);
							return;
						}
						appendInput(chunk);
						let output;
						try {
							output = decompressChunk(chunk);
							streamFinished = true;
						} catch (error) {
							streamError = toTypeError(error);
							if (readableController) readableController.error(streamError);
							return;
						}
						if (output.byteLength > 0 && readableController) readableController.enqueue(output);
					} catch (error) {
						streamError = toTypeError(error);
						if (readableController) readableController.error(streamError);
						return;
					}
				},
				async close() {
					try {
						writableClosed = true;
						if (streamError) {
							if (readableController) readableController.error(streamError);
							return;
						}
						if (!streamFinished && streamInput.length > 0 && !streamError) validateChunkCompletion();
						if (streamError === null && !streamFinished) {
							streamError = /* @__PURE__ */ new TypeError("Decompression stream failed: incomplete data");
							if (readableController) readableController.error(streamError);
							return;
						}
						if (streamError === null && readableController) readableController.close();
					} catch (error) {
						streamError = streamError || toTypeError(error);
						if (readableController) readableController.error(streamError);
					}
				},
				async abort(reason) {
					if (streamError === null) streamError = reason === void 0 ? /* @__PURE__ */ new TypeError("The operation was aborted.") : toTypeError(reason);
					writableClosed = true;
					if (readableController) readableController.error(streamError);
				}
			});
			this._readable = readable;
			this._writable = writable;
		}
		get readable() {
			return this._readable;
		}
		get writable() {
			return this._writable;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/utils.ts
	init_defineProperty();
	/**
	* Shared utilities for the IndexedDB implementation.
	*
	* Consolidates DOMException and helper functions to avoid
	* duplicate definitions across modules and enable cross-module
	* instanceof checks.
	*/
	/**
	* DOMException polyfill.
	*
	* Uses globalThis.DOMException when available (e.g. if the runtime bootstrap
	* has already installed one) and falls back to a simple Error subclass.
	*/
	const DOMException$1 = (() => {
		if (typeof globalThis !== "undefined" && globalThis.DOMException) return globalThis.DOMException;
		class DOMExceptionPolyfill extends Error {
			constructor(message, name) {
				super(message);
				_defineProperty(this, "code", void 0);
				this.name = name;
				this.code = 0;
			}
		}
		return DOMExceptionPolyfill;
	})();
	/**
	* Create a DOMStringList-like object from a string array.
	*
	* The returned value extends Array<string> with `.contains()` and `.item()`
	* methods, matching the DOMStringList interface that IndexedDB consumers
	* expect.
	*/
	function makeDOMStringList(names) {
		const list = [...names];
		list.contains = (name) => list.includes(name);
		list.item = (index) => list[index] ?? null;
		return list;
	}
	/**
	* Sanitize a name for use as a SQL table/column identifier.
	*/
	function sanitizeName(name) {
		return name.replace(/[^a-zA-Z0-9_]/g, "_");
	}
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBTransaction.ts
	init_defineProperty();
	var IDBTransaction = class {
		constructor(db, storeNames, mode) {
			_defineProperty(this, "_db", void 0);
			_defineProperty(this, "_mode", void 0);
			_defineProperty(this, "_storeNames", void 0);
			_defineProperty(this, "_stores", /* @__PURE__ */ new Map());
			_defineProperty(this, "_committed", false);
			_defineProperty(this, "_aborted", false);
			_defineProperty(this, "_error", null);
			_defineProperty(
				this,
				/** @internal */
				"_state",
				"active"
			);
			_defineProperty(this, "_sqliteBegan", false);
			_defineProperty(this, "_listeners", {});
			_defineProperty(this, "oncomplete", null);
			_defineProperty(this, "onerror", null);
			_defineProperty(this, "onabort", null);
			this._db = db;
			this._storeNames = storeNames;
			this._mode = mode;
			if (mode === "readwrite") this._beginSqlite();
		}
		get db() {
			return this._db;
		}
		get mode() {
			return this._mode;
		}
		get objectStoreNames() {
			return [...this._storeNames].sort();
		}
		get error() {
			return this._error;
		}
		addEventListener(type, fn) {
			if (!this._listeners[type]) this._listeners[type] = [];
			this._listeners[type].push(fn);
		}
		removeEventListener(type, fn) {
			const list = this._listeners[type];
			if (list) this._listeners[type] = list.filter((f) => f !== fn);
		}
		/** @internal */
		_fireListeners(type, event) {
			const list = this._listeners[type];
			if (list) {
				const __exactForOfIterator88 = list[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep88 = __exactForOfIterator88.next();
					if (__exactForOfStep88.done) break;
					const fn = __exactForOfStep88.value;
					try {
						fn(event);
					} catch (_) {}
				}
			}
		}
		/**
		* Get an object store from this transaction.
		*/
		objectStore(name) {
			if (!this._storeNames.includes(name)) throw new DOMException$1(`Object store "${name}" is not in this transaction's scope`, "NotFoundError");
			if (this._aborted) throw new DOMException$1("Transaction has been aborted", "InvalidStateError");
			let store = this._stores.get(name);
			if (!store) {
				store = this._db._getObjectStore(name, this);
				if (!store) throw new DOMException$1(`Object store "${name}" does not exist`, "NotFoundError");
				this._stores.set(name, store);
			}
			return store;
		}
		/**
		* Commit the transaction.
		*/
		commit() {
			if (this._committed || this._aborted) return;
			this._committed = true;
			this._state = "finished";
			this._commitSqlite();
			const event = {
				type: "complete",
				target: this
			};
			queueMicrotask(() => {
				if (this.oncomplete) this.oncomplete(event);
				this._fireListeners("complete", event);
			});
		}
		/**
		* Abort the transaction.
		*/
		abort() {
			if (this._committed || this._aborted) return;
			this._aborted = true;
			this._state = "finished";
			this._error = new DOMException$1("Transaction was aborted", "AbortError");
			this._rollbackSqlite();
			const event = {
				type: "abort",
				target: this
			};
			queueMicrotask(() => {
				if (this.onabort) this.onabort(event);
				this._fireListeners("abort", event);
			});
		}
		/** @internal - Handle an error from a request */
		_handleError(error) {
			this._error = error instanceof DOMException$1 ? error : new DOMException$1(error?.message ?? "Unknown error", "UnknownError");
			const event = {
				type: "error",
				target: this
			};
			queueMicrotask(() => {
				if (this.onerror) this.onerror(event);
				this._fireListeners("error", event);
			});
		}
		/** @internal - Auto-commit if no abort was called (called after all microtasks drain) */
		_autoCommit() {
			if (!this._committed && !this._aborted) this.commit();
		}
		/** @internal - Begin a SQLite transaction */
		_beginSqlite() {
			if (this._sqliteBegan) return;
			try {
				this._db._exec("BEGIN TRANSACTION");
				this._sqliteBegan = true;
			} catch (_) {}
		}
		/** @internal - Commit the SQLite transaction */
		_commitSqlite() {
			if (!this._sqliteBegan) return;
			try {
				this._db._exec("COMMIT");
			} catch (_) {}
			this._sqliteBegan = false;
		}
		/** @internal - Rollback the SQLite transaction */
		_rollbackSqlite() {
			if (!this._sqliteBegan) return;
			try {
				this._db._exec("ROLLBACK");
			} catch (_) {}
			this._sqliteBegan = false;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBRequest.ts
	/**
	* IDBRequest - IndexedDB Request
	*
	* Represents an asynchronous request to the database. IndexedDB's async model
	* uses onsuccess/onerror callbacks instead of Promises.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/IDBRequest
	*/
	init_defineProperty();
	var IDBRequest = class {
		constructor() {
			_defineProperty(this, "_result", void 0);
			_defineProperty(this, "_error", null);
			_defineProperty(this, "_readyState", "pending");
			_defineProperty(this, "_source", null);
			_defineProperty(this, "_transaction", null);
			_defineProperty(this, "_listeners", {});
			_defineProperty(this, "onsuccess", null);
			_defineProperty(this, "onerror", null);
		}
		get result() {
			if (this._readyState === "pending") throw new DOMException$1("The request has not finished.", "InvalidStateError");
			return this._result;
		}
		get error() {
			if (this._readyState === "pending") throw new DOMException$1("The request has not finished.", "InvalidStateError");
			return this._error;
		}
		get readyState() {
			return this._readyState;
		}
		get source() {
			return this._source;
		}
		set source(s) {
			this._source = s;
		}
		get transaction() {
			return this._transaction;
		}
		set transaction(t) {
			this._transaction = t;
		}
		addEventListener(type, fn) {
			if (!this._listeners[type]) this._listeners[type] = [];
			this._listeners[type].push(fn);
		}
		removeEventListener(type, fn) {
			const list = this._listeners[type];
			if (list) this._listeners[type] = list.filter((f) => f !== fn);
		}
		/** @internal - Fire listeners for a given event type */
		_fireListeners(type, event) {
			const list = this._listeners[type];
			if (list) {
				const __exactForOfIterator89 = list[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep89 = __exactForOfIterator89.next();
					if (__exactForOfStep89.done) break;
					const fn = __exactForOfStep89.value;
					try {
						fn(event);
					} catch (e) {
						if (this._transaction && this._transaction._handleError) this._transaction._handleError(e);
					}
				}
			}
		}
		/** @internal - Resolve the request with a result */
		_resolve(value) {
			this._readyState = "done";
			this._result = value;
			this._error = null;
			const event = {
				type: "success",
				target: this
			};
			queueMicrotask(() => {
				if (this.onsuccess) try {
					this.onsuccess(event);
				} catch (e) {
					if (this._transaction && this._transaction._handleError) this._transaction._handleError(e);
				}
				this._fireListeners("success", event);
			});
		}
		/** @internal - Reject the request with an error */
		_reject(error) {
			this._readyState = "done";
			this._error = error instanceof DOMException$1 ? error : new DOMException$1(error.message, "UnknownError");
			this._result = void 0;
			const event = {
				type: "error",
				target: this
			};
			queueMicrotask(() => {
				if (this.onerror) this.onerror(event);
				this._fireListeners("error", event);
			});
		}
		/** @internal - Resolve synchronously (used during upgradeneeded) */
		_resolveSync(value) {
			this._readyState = "done";
			this._result = value;
			this._error = null;
			const event = {
				type: "success",
				target: this
			};
			if (this.onsuccess) this.onsuccess(event);
			this._fireListeners("success", event);
		}
		/**
		* @internal - Set the result value without changing readyState or firing events.
		* Used by IDBFactory.open() to make the result accessible to onupgradeneeded
		* handlers before the request formally completes.
		*/
		_setResult(value) {
			this._readyState = "done";
			this._result = value;
			this._error = null;
		}
	};
	/**
	* IDBOpenDBRequest extends IDBRequest with onupgradeneeded and onblocked.
	*/
	var IDBOpenDBRequest = class extends IDBRequest {
		constructor(..._args) {
			super(..._args);
			_defineProperty(this, "onupgradeneeded", null);
			_defineProperty(this, "onblocked", null);
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBKeyRange.ts
	/**
	* IDBKeyRange - IndexedDB Key Range
	*
	* Represents a continuous interval over keys used for selecting records.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/IDBKeyRange
	*/
	init_defineProperty();
	var IDBKeyRange = class IDBKeyRange {
		constructor(lower, upper, lowerOpen, upperOpen) {
			_defineProperty(this, "lower", void 0);
			_defineProperty(this, "upper", void 0);
			_defineProperty(this, "lowerOpen", void 0);
			_defineProperty(this, "upperOpen", void 0);
			this.lower = lower;
			this.upper = upper;
			this.lowerOpen = lowerOpen;
			this.upperOpen = upperOpen;
		}
		/**
		* Check whether a key is within this range.
		*/
		includes(key) {
			const cmp = compareKeys;
			if (this.lower !== void 0) {
				const lowerCmp = cmp(key, this.lower);
				if (this.lowerOpen ? lowerCmp <= 0 : lowerCmp < 0) return false;
			}
			if (this.upper !== void 0) {
				const upperCmp = cmp(key, this.upper);
				if (this.upperOpen ? upperCmp >= 0 : upperCmp > 0) return false;
			}
			return true;
		}
		/**
		* Creates a key range with only a lower bound.
		*/
		static lowerBound(lower, open = false) {
			return new IDBKeyRange(lower, void 0, open, true);
		}
		/**
		* Creates a key range with only an upper bound.
		*/
		static upperBound(upper, open = false) {
			return new IDBKeyRange(void 0, upper, true, open);
		}
		/**
		* Creates a key range with both lower and upper bounds.
		*/
		static bound(lower, upper, lowerOpen = false, upperOpen = false) {
			if (compareKeys(lower, upper) > 0) throw new DOMException$1("lower is greater than upper", "DataError");
			return new IDBKeyRange(lower, upper, lowerOpen, upperOpen);
		}
		/**
		* Creates a key range containing a single key value.
		*/
		static only(value) {
			return new IDBKeyRange(value, value, false, false);
		}
	};
	/**
	* Compare two IndexedDB keys per the spec ordering.
	* Returns negative if a < b, positive if a > b, 0 if equal.
	*/
	function compareKeys(a, b) {
		const typeOrder = (v) => {
			if (Array.isArray(v)) return 4;
			if (typeof v === "string") return 3;
			if (v instanceof Date) return 2;
			if (typeof v === "number") return 1;
			return 0;
		};
		const ta = typeOrder(a);
		const tb = typeOrder(b);
		if (ta !== tb) return ta - tb;
		if (typeof a === "number" && typeof b === "number") return a - b;
		if (typeof a === "string" && typeof b === "string") return a < b ? -1 : a > b ? 1 : 0;
		if (a instanceof Date && b instanceof Date) return a.getTime() - b.getTime();
		if (Array.isArray(a) && Array.isArray(b)) {
			const len = Math.min(a.length, b.length);
			for (let i = 0; i < len; i++) {
				const c = compareKeys(a[i], b[i]);
				if (c !== 0) return c;
			}
			return a.length - b.length;
		}
		return 0;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBCursor.ts
	/**
	* IDBCursor - IndexedDB Cursor
	*
	* Iterates over records in an object store or index.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/IDBCursor
	*/
	init_defineProperty();
	var IDBCursor = class {
		constructor(source, direction, records, request) {
			_defineProperty(this, "_source", void 0);
			_defineProperty(this, "_direction", void 0);
			_defineProperty(this, "_records", void 0);
			_defineProperty(this, "_position", void 0);
			_defineProperty(this, "_request", void 0);
			_defineProperty(this, "_gotValue", false);
			this._source = source;
			this._direction = direction;
			this._request = request;
			const sorted = [...records];
			if (direction === "prev" || direction === "prevunique") sorted.sort((a, b) => compareKeys(b.key, a.key));
			else sorted.sort((a, b) => compareKeys(a.key, b.key));
			if (direction === "nextunique" || direction === "prevunique") {
				const seen = /* @__PURE__ */ new Set();
				this._records = sorted.filter((r) => {
					const keyStr = JSON.stringify(r.key);
					if (seen.has(keyStr)) return false;
					seen.add(keyStr);
					return true;
				});
			} else this._records = sorted;
			this._position = 0;
			this._gotValue = this._records.length > 0;
		}
		get source() {
			return this._source;
		}
		get direction() {
			return this._direction;
		}
		get key() {
			if (!this._gotValue || this._position >= this._records.length) return void 0;
			return this._records[this._position].key;
		}
		get primaryKey() {
			if (!this._gotValue || this._position >= this._records.length) return void 0;
			return this._records[this._position].primaryKey;
		}
		get request() {
			return this._request;
		}
		/**
		* Advances the cursor to the next position.
		*/
		continue(key) {
			this._position++;
			if (key !== void 0) while (this._position < this._records.length) {
				const cmp = compareKeys(this._records[this._position].key, key);
				if (this._direction === "prev" || this._direction === "prevunique") {
					if (cmp <= 0) break;
				} else if (cmp >= 0) break;
				this._position++;
			}
			if (this._position < this._records.length) {
				this._gotValue = true;
				this._request._resolve(this);
			} else {
				this._gotValue = false;
				this._request._resolve(null);
			}
		}
		/**
		* Advances the cursor by a given number of positions.
		*/
		advance(count) {
			if (count <= 0) throw new TypeError("count must be greater than 0");
			this._position += count;
			if (this._position < this._records.length) {
				this._gotValue = true;
				this._request._resolve(this);
			} else {
				this._gotValue = false;
				this._request._resolve(null);
			}
		}
		/**
		* Updates the value at the current cursor position.
		*/
		update(value) {
			const request = new IDBRequest();
			if (!this._gotValue || this._position >= this._records.length) {
				request._reject(/* @__PURE__ */ new Error("Cursor is not pointing to a record"));
				return request;
			}
			const record = this._records[this._position];
			try {
				(this._source._objectStore || this._source)._putRecord(record.primaryKey, value);
				record.value = value;
				request._resolve(record.primaryKey);
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Deletes the record at the current cursor position.
		*/
		delete() {
			const request = new IDBRequest();
			if (!this._gotValue || this._position >= this._records.length) {
				request._reject(/* @__PURE__ */ new Error("Cursor is not pointing to a record"));
				return request;
			}
			const record = this._records[this._position];
			try {
				(this._source._objectStore || this._source)._deleteRecord(record.primaryKey);
				request._resolve(void 0);
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
	};
	/**
	* IDBCursorWithValue extends IDBCursor with a value property.
	*/
	var IDBCursorWithValue = class extends IDBCursor {
		constructor(source, direction, records, request) {
			super(source, direction, records, request);
			_defineProperty(this, "_valueRecords", void 0);
			this._valueRecords = this._records;
		}
		get value() {
			const pos = this._position;
			const records = this._records;
			if (pos >= records.length) return void 0;
			return records[pos].value;
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBIndex.ts
	/**
	* IDBIndex - IndexedDB Index
	*
	* Provides access to a subset of data in an object store,
	* with records sorted by the index key rather than the primary key.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/IDBIndex
	*/
	init_defineProperty();
	var IDBIndex = class {
		constructor(name, keyPath, options, objectStore) {
			_defineProperty(this, "name", void 0);
			_defineProperty(this, "keyPath", void 0);
			_defineProperty(this, "unique", void 0);
			_defineProperty(this, "multiEntry", void 0);
			_defineProperty(
				this,
				/** @internal */
				"_objectStore",
				void 0
			);
			this.name = name;
			this.keyPath = keyPath;
			this.unique = options.unique ?? false;
			this.multiEntry = options.multiEntry ?? false;
			this._objectStore = objectStore;
		}
		get objectStore() {
			return this._objectStore;
		}
		/**
		* Retrieve the first record matching a key or key range from this index.
		*/
		get(query) {
			const request = new IDBRequest();
			request.source = this;
			try {
				const records = this._getMatchingRecords(query);
				request._resolve(records.length > 0 ? records[0].value : void 0);
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Retrieve the key of the first record matching from this index.
		*/
		getKey(query) {
			const request = new IDBRequest();
			request.source = this;
			try {
				const records = this._getMatchingRecords(query);
				request._resolve(records.length > 0 ? records[0].primaryKey : void 0);
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Retrieve all records matching from this index.
		*/
		getAll(query, count) {
			const request = new IDBRequest();
			request.source = this;
			try {
				let records = this._getMatchingRecords(query);
				if (count !== void 0 && count >= 0) records = records.slice(0, count);
				request._resolve(records.map((r) => r.value));
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Retrieve all keys matching from this index.
		*/
		getAllKeys(query, count) {
			const request = new IDBRequest();
			request.source = this;
			try {
				let records = this._getMatchingRecords(query);
				if (count !== void 0 && count >= 0) records = records.slice(0, count);
				request._resolve(records.map((r) => r.primaryKey));
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Count records matching a key or range in this index.
		*/
		count(query) {
			const request = new IDBRequest();
			request.source = this;
			try {
				const records = this._getMatchingRecords(query);
				request._resolve(records.length);
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Open a cursor over the index's records.
		*/
		openCursor(query, direction) {
			const request = new IDBRequest();
			request.source = this;
			try {
				const records = this._getMatchingRecords(query);
				if (records.length === 0) request._resolve(null);
				else {
					const cursor = new IDBCursorWithValue(this, direction ?? "next", records, request);
					request._resolve(cursor);
				}
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Open a key cursor over the index.
		*/
		openKeyCursor(query, direction) {
			return this.openCursor(query, direction);
		}
		/** @internal - Get records that match a query via this index */
		_getMatchingRecords(query) {
			const keyPath = this.keyPath;
			const indexedRecords = this._objectStore._getAllRecords().map((r) => ({
				key: extractKeyPath(r.value, keyPath),
				primaryKey: r.key,
				value: r.value
			}));
			if (query === void 0 || query === null) return indexedRecords;
			const range = query instanceof IDBKeyRange ? query : IDBKeyRange.only(query);
			return indexedRecords.filter((r) => r.key !== void 0 && range.includes(r.key));
		}
	};
	/**
	* Extract a value from an object using a key path.
	*/
	function extractKeyPath(obj, keyPath) {
		if (obj === null || obj === void 0) return void 0;
		if (Array.isArray(keyPath)) return keyPath.map((kp) => extractSingleKeyPath(obj, kp));
		return extractSingleKeyPath(obj, keyPath);
	}
	function extractSingleKeyPath(obj, keyPath) {
		const parts = keyPath.split(".");
		let current = obj;
		for (const part of parts) {
			if (current === null || current === void 0) return void 0;
			current = current[part];
		}
		return current;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBObjectStore.ts
	/**
	* IDBObjectStore - IndexedDB Object Store
	*
	* Represents an object store in the database. Backed by a SQLite table.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/IDBObjectStore
	*/
	init_defineProperty();
	var IDBObjectStore = class {
		constructor(name, options, transaction, db) {
			_defineProperty(this, "name", void 0);
			_defineProperty(this, "keyPath", void 0);
			_defineProperty(this, "autoIncrement", void 0);
			_defineProperty(
				this,
				/** @internal */
				"_transaction",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_db",
				void 0
			);
			_defineProperty(
				this,
				/** @internal */
				"_indexes",
				/* @__PURE__ */ new Map()
			);
			_defineProperty(
				this,
				/** @internal */
				"_autoIncrementValue",
				0
			);
			_defineProperty(
				this,
				/** @internal */
				"_tableName",
				void 0
			);
			this.name = name;
			this.keyPath = options.keyPath ?? null;
			this.autoIncrement = options.autoIncrement ?? false;
			this._transaction = transaction;
			this._db = db;
			this._tableName = `idb_store_${sanitizeName(name)}`;
			this._ensureTable();
		}
		get indexNames() {
			return Array.from(this._indexes.keys()).sort();
		}
		get transaction() {
			return this._transaction;
		}
		/**
		* Add a record to the store. Fails if a record with the same key exists.
		*/
		add(value, key) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				const resolvedKey = this._resolveKey(value, key);
				if (this._hasRecord(resolvedKey)) throw new DOMException$1(`A record with key ${JSON.stringify(resolvedKey)} already exists`, "ConstraintError");
				this._putRecord(resolvedKey, value);
				request._resolve(resolvedKey);
			} catch (e) {
				request._reject(e instanceof DOMException$1 ? e : new DOMException$1(e.message, "DataError"));
			}
			return request;
		}
		/**
		* Add or update a record in the store.
		*/
		put(value, key) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				const resolvedKey = this._resolveKey(value, key);
				this._putRecord(resolvedKey, value);
				request._resolve(resolvedKey);
			} catch (e) {
				request._reject(e instanceof DOMException$1 ? e : new DOMException$1(e.message, "DataError"));
			}
			return request;
		}
		/**
		* Retrieve a record by key.
		*/
		get(query) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				if (query instanceof IDBKeyRange) {
					const match = this._getAllRecords().find((r) => query.includes(r.key));
					request._resolve(match ? match.value : void 0);
				} else {
					const value = this._getRecord(query);
					request._resolve(value);
				}
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Retrieve the key of the first record matching.
		*/
		getKey(query) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				if (query instanceof IDBKeyRange) {
					const match = this._getAllRecords().find((r) => query.includes(r.key));
					request._resolve(match ? match.key : void 0);
				} else {
					const value = this._getRecord(query);
					request._resolve(value !== void 0 ? query : void 0);
				}
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Retrieve all records, optionally filtered by key range.
		*/
		getAll(query, count) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				let records = this._getAllRecords();
				if (query !== void 0 && query !== null) {
					const range = query instanceof IDBKeyRange ? query : IDBKeyRange.only(query);
					records = records.filter((r) => range.includes(r.key));
				}
				if (count !== void 0 && count >= 0) records = records.slice(0, count);
				request._resolve(records.map((r) => r.value));
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Retrieve all keys, optionally filtered.
		*/
		getAllKeys(query, count) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				let records = this._getAllRecords();
				if (query !== void 0 && query !== null) {
					const range = query instanceof IDBKeyRange ? query : IDBKeyRange.only(query);
					records = records.filter((r) => range.includes(r.key));
				}
				if (count !== void 0 && count >= 0) records = records.slice(0, count);
				request._resolve(records.map((r) => r.key));
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Delete a record by key or key range.
		*/
		delete(query) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				if (query instanceof IDBKeyRange) {
					const records = this._getAllRecords();
					{
						const __exactForOfIterator90 = records[Symbol.iterator]();
						for (;;) {
							const __exactForOfStep90 = __exactForOfIterator90.next();
							if (__exactForOfStep90.done) break;
							const r = __exactForOfStep90.value;
							if (query.includes(r.key)) this._deleteRecord(r.key);
						}
					}
				} else this._deleteRecord(query);
				request._resolve(void 0);
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Delete all records in the store.
		*/
		clear() {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				this._clearRecords();
				request._resolve(void 0);
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Count records, optionally filtered.
		*/
		count(query) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				if (query === void 0 || query === null) request._resolve(this._countRecords());
				else {
					const range = query instanceof IDBKeyRange ? query : IDBKeyRange.only(query);
					const records = this._getAllRecords();
					request._resolve(records.filter((r) => range.includes(r.key)).length);
				}
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Create an index on this object store. Only valid during upgradeneeded.
		*/
		createIndex(name, keyPath, options) {
			if (this._indexes.has(name)) throw new DOMException$1(`Index "${name}" already exists`, "ConstraintError");
			const index = new IDBIndex(name, keyPath, options ?? {}, this);
			this._indexes.set(name, index);
			this._db._saveIndexMeta(this.name, name, keyPath, options ?? {});
			return index;
		}
		/**
		* Delete an index from this object store. Only valid during upgradeneeded.
		*/
		deleteIndex(name) {
			if (!this._indexes.has(name)) throw new DOMException$1(`Index "${name}" does not exist`, "NotFoundError");
			this._indexes.delete(name);
			this._db._deleteIndexMeta(this.name, name);
		}
		/**
		* Get a reference to an index by name.
		*/
		index(name) {
			const idx = this._indexes.get(name);
			if (!idx) throw new DOMException$1(`Index "${name}" does not exist on object store "${this.name}"`, "NotFoundError");
			return idx;
		}
		/**
		* Open a cursor over the object store.
		*/
		openCursor(query, direction) {
			const request = new IDBRequest();
			request.source = this;
			request.transaction = this._transaction;
			try {
				let records = this._getAllRecords();
				if (query !== void 0 && query !== null) {
					const range = query instanceof IDBKeyRange ? query : IDBKeyRange.only(query);
					records = records.filter((r) => range.includes(r.key));
				}
				if (records.length === 0) request._resolve(null);
				else {
					const cursorRecords = records.map((r) => ({
						key: r.key,
						primaryKey: r.key,
						value: r.value
					}));
					const cursor = new IDBCursorWithValue(this, direction ?? "next", cursorRecords, request);
					request._resolve(cursor);
				}
			} catch (e) {
				request._reject(e);
			}
			return request;
		}
		/**
		* Open a key cursor over the object store.
		*/
		openKeyCursor(query, direction) {
			return this.openCursor(query, direction);
		}
		/** @internal */
		_ensureTable() {
			this._db._exec(`CREATE TABLE IF NOT EXISTS "${this._tableName}" (key TEXT PRIMARY KEY, value TEXT)`);
			if (this.autoIncrement) {
				const rows = this._db._all(`SELECT key FROM "${this._tableName}"`);
				let maxKey = 0;
				{
					const __exactForOfIterator91 = rows[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep91 = __exactForOfIterator91.next();
						if (__exactForOfStep91.done) break;
						const row = __exactForOfStep91.value;
						try {
							const parsed = JSON.parse(row.key);
							if (typeof parsed === "number" && !isNaN(parsed) && parsed > maxKey) maxKey = parsed;
						} catch (_) {}
					}
				}
				this._autoIncrementValue = maxKey;
			}
		}
		/** @internal */
		_resolveKey(value, explicitKey) {
			if (explicitKey !== void 0) return explicitKey;
			if (this.keyPath !== null) {
				const key = extractKeyPath(value, this.keyPath);
				if (key !== void 0) return key;
			}
			if (this.autoIncrement) {
				this._autoIncrementValue++;
				const newKey = this._autoIncrementValue;
				if (this.keyPath !== null && typeof this.keyPath === "string" && typeof value === "object" && value !== null) setKeyPath(value, this.keyPath, newKey);
				return newKey;
			}
			throw new DOMException$1("No key provided and no keyPath or autoIncrement configured", "DataError");
		}
		/** @internal */
		_hasRecord(key) {
			return !!this._db._get(`SELECT 1 FROM "${this._tableName}" WHERE key = ?`, [JSON.stringify(key)]);
		}
		/** @internal */
		_putRecord(key, value) {
			const serializedKey = JSON.stringify(key);
			const serializedValue = JSON.stringify(value);
			this._db._exec(`INSERT OR REPLACE INTO "${this._tableName}" (key, value) VALUES (?, ?)`, [serializedKey, serializedValue]);
		}
		/** @internal */
		_getRecord(key) {
			const row = this._db._get(`SELECT value FROM "${this._tableName}" WHERE key = ?`, [JSON.stringify(key)]);
			return row ? JSON.parse(row.value) : void 0;
		}
		/** @internal */
		_deleteRecord(key) {
			this._db._exec(`DELETE FROM "${this._tableName}" WHERE key = ?`, [JSON.stringify(key)]);
		}
		/** @internal */
		_clearRecords() {
			this._db._exec(`DELETE FROM "${this._tableName}"`);
			if (this.autoIncrement) this._autoIncrementValue = 0;
		}
		/** @internal */
		_countRecords() {
			const row = this._db._get(`SELECT COUNT(*) as cnt FROM "${this._tableName}"`);
			return row ? row.cnt : 0;
		}
		/** @internal */
		_getAllRecords() {
			const records = this._db._all(`SELECT key, value FROM "${this._tableName}"`).map((r) => ({
				key: JSON.parse(r.key),
				value: JSON.parse(r.value)
			}));
			records.sort((a, b) => compareKeys(a.key, b.key));
			return records;
		}
	};
	/**
	* Set a value at a key path on an object.
	*/
	function setKeyPath(obj, keyPath, value) {
		const parts = keyPath.split(".");
		let current = obj;
		for (let i = 0; i < parts.length - 1; i++) {
			if (current[parts[i]] === void 0) current[parts[i]] = {};
			current = current[parts[i]];
		}
		current[parts[parts.length - 1]] = value;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBDatabase.ts
	/**
	* IDBDatabase - IndexedDB Database
	*
	* Represents an open connection to a database. Backed by a SQLite database.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/IDBDatabase
	*/
	init_defineProperty();
	var IDBDatabase = class {
		constructor(name, version, sqliteDb) {
			_defineProperty(this, "name", void 0);
			_defineProperty(this, "_version", void 0);
			_defineProperty(this, "_objectStores", /* @__PURE__ */ new Map());
			_defineProperty(this, "_closed", false);
			_defineProperty(
				this,
				/** @internal - SQLite database wrapper */
				"_sqliteDb",
				void 0
			);
			_defineProperty(
				this,
				/** @internal - The active versionchange transaction during upgradeneeded, if any */
				"_upgradeTransaction",
				null
			);
			_defineProperty(this, "_listeners", {});
			_defineProperty(this, "onclose", null);
			_defineProperty(this, "onversionchange", null);
			_defineProperty(this, "onerror", null);
			_defineProperty(this, "onabort", null);
			this.name = name;
			this._version = version;
			this._sqliteDb = sqliteDb;
			this._initMeta();
			this._loadStoreDefinitions();
		}
		get version() {
			return this._version;
		}
		get objectStoreNames() {
			return makeDOMStringList(Array.from(this._objectStores.keys()).sort());
		}
		addEventListener(type, fn) {
			if (!this._listeners[type]) this._listeners[type] = [];
			this._listeners[type].push(fn);
		}
		removeEventListener(type, fn) {
			const list = this._listeners[type];
			if (list) this._listeners[type] = list.filter((f) => f !== fn);
		}
		/**
		* Create a new object store. Only valid during upgradeneeded.
		*/
		createObjectStore(name, options) {
			if (this._upgradeTransaction && this._upgradeTransaction._state !== "active") throw new DOMException$1("Can only be called during upgradeneeded", "InvalidStateError");
			if (this._objectStores.has(name)) throw new DOMException$1(`Object store "${name}" already exists`, "ConstraintError");
			const opts = {
				keyPath: options?.keyPath ?? null,
				autoIncrement: options?.autoIncrement ?? false
			};
			this._objectStores.set(name, {
				options: opts,
				indexes: /* @__PURE__ */ new Map()
			});
			this._saveStoreMeta(name, opts);
			return new IDBObjectStore(name, opts, this._upgradeTransaction ?? new IDBTransaction(this, [name], "versionchange"), this);
		}
		/**
		* Delete an object store. Only valid during upgradeneeded.
		*/
		deleteObjectStore(name) {
			if (this._upgradeTransaction && this._upgradeTransaction._state !== "active") throw new DOMException$1("Can only be called during upgradeneeded", "InvalidStateError");
			if (!this._objectStores.has(name)) throw new DOMException$1(`Object store "${name}" does not exist`, "NotFoundError");
			const tableName = `idb_store_${sanitizeName(name)}`;
			this._exec(`DROP TABLE IF EXISTS "${tableName}"`);
			this._exec(`DELETE FROM _idb_stores WHERE store_name = ?`, [name]);
			this._exec(`DELETE FROM _idb_indexes WHERE store_name = ?`, [name]);
			this._objectStores.delete(name);
		}
		/**
		* Create a transaction for the given object stores.
		*/
		transaction(storeNames, mode) {
			this._checkClosed();
			const names = Array.isArray(storeNames) ? storeNames : [storeNames];
			{
				const __exactForOfIterator92 = names[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep92 = __exactForOfIterator92.next();
					if (__exactForOfStep92.done) break;
					const name = __exactForOfStep92.value;
					if (!this._objectStores.has(name)) throw new DOMException$1(`Object store "${name}" does not exist`, "NotFoundError");
				}
			}
			const txn = new IDBTransaction(this, names, mode ?? "readonly");
			queueMicrotask(() => {
				queueMicrotask(() => {
					txn._autoCommit();
				});
			});
			return txn;
		}
		/**
		* Close the database connection.
		*/
		close() {
			if (this._closed) return;
			this._closed = true;
			if (this._sqliteDb && this._sqliteDb.close) this._sqliteDb.close();
			if (this.onclose) {
				const event = {
					type: "close",
					target: this
				};
				this.onclose(event);
			}
		}
		/** @internal */
		_initMeta() {
			this._exec(`
      CREATE TABLE IF NOT EXISTS _idb_meta (
        key TEXT PRIMARY KEY,
        value TEXT
      )
    `);
			this._exec(`
      CREATE TABLE IF NOT EXISTS _idb_stores (
        store_name TEXT PRIMARY KEY,
        key_path TEXT,
        auto_increment INTEGER DEFAULT 0
      )
    `);
			this._exec(`
      CREATE TABLE IF NOT EXISTS _idb_indexes (
        store_name TEXT,
        index_name TEXT,
        key_path TEXT,
        unique_flag INTEGER DEFAULT 0,
        multi_entry INTEGER DEFAULT 0,
        PRIMARY KEY (store_name, index_name)
      )
    `);
		}
		/** @internal */
		_loadStoreDefinitions() {
			const stores = this._all("SELECT store_name, key_path, auto_increment FROM _idb_stores");
			{
				const __exactForOfIterator93 = stores[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep93 = __exactForOfIterator93.next();
					if (__exactForOfStep93.done) break;
					const row = __exactForOfStep93.value;
					const opts = {
						keyPath: row.key_path ? JSON.parse(row.key_path) : null,
						autoIncrement: !!row.auto_increment
					};
					const indexes = /* @__PURE__ */ new Map();
					const idxRows = this._all("SELECT index_name, key_path, unique_flag, multi_entry FROM _idb_indexes WHERE store_name = ?", [row.store_name]);
					for (const idx of idxRows) indexes.set(idx.index_name, {
						keyPath: JSON.parse(idx.key_path),
						unique: !!idx.unique_flag,
						multiEntry: !!idx.multi_entry
					});
					this._objectStores.set(row.store_name, {
						options: opts,
						indexes
					});
				}
			}
		}
		/** @internal */
		_saveStoreMeta(name, options) {
			this._exec(`INSERT OR REPLACE INTO _idb_stores (store_name, key_path, auto_increment) VALUES (?, ?, ?)`, [
				name,
				JSON.stringify(options.keyPath),
				options.autoIncrement ? 1 : 0
			]);
		}
		/** @internal */
		_saveIndexMeta(storeName, indexName, keyPath, options) {
			this._exec(`INSERT OR REPLACE INTO _idb_indexes (store_name, index_name, key_path, unique_flag, multi_entry) VALUES (?, ?, ?, ?, ?)`, [
				storeName,
				indexName,
				JSON.stringify(keyPath),
				options.unique ? 1 : 0,
				options.multiEntry ? 1 : 0
			]);
			const storeInfo = this._objectStores.get(storeName);
			if (storeInfo) storeInfo.indexes.set(indexName, {
				keyPath,
				unique: options.unique ?? false,
				multiEntry: options.multiEntry ?? false
			});
		}
		/** @internal */
		_deleteIndexMeta(storeName, indexName) {
			this._exec(`DELETE FROM _idb_indexes WHERE store_name = ? AND index_name = ?`, [storeName, indexName]);
			const storeInfo = this._objectStores.get(storeName);
			if (storeInfo) storeInfo.indexes.delete(indexName);
		}
		/** @internal - Get an object store instance for a transaction */
		_getObjectStore(name, transaction) {
			const storeInfo = this._objectStores.get(name);
			if (!storeInfo) return null;
			const store = new IDBObjectStore(name, storeInfo.options, transaction, this);
			{
				const __exactForOfIterator94 = storeInfo.indexes[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep94 = __exactForOfIterator94.next();
					if (__exactForOfStep94.done) break;
					const [indexName, indexDef] = __exactForOfStep94.value;
					const idx = new IDBIndex(indexName, indexDef.keyPath, indexDef, store);
					store._indexes.set(indexName, idx);
				}
			}
			return store;
		}
		/** @internal */
		_checkClosed() {
			if (this._closed) throw new DOMException$1("Database connection is closed", "InvalidStateError");
		}
		/** @internal - Execute a SQL statement */
		_exec(sql, params) {
			if (this._sqliteDb.exec) if (params && params.length > 0) this._sqliteDb.exec(sql, ...params);
			else this._sqliteDb.exec(sql);
			else if (this._sqliteDb.run) if (params && params.length > 0) this._sqliteDb.run(sql, ...params);
			else this._sqliteDb.run(sql);
		}
		/** @internal - Query a single row */
		_get(sql, params) {
			if (this._sqliteDb.query) {
				const stmt = this._sqliteDb.query(sql);
				return params ? stmt.get(...params) : stmt.get();
			}
			if (this._sqliteDb.prepare) {
				const stmt = this._sqliteDb.prepare(sql);
				return params ? stmt.get(...params) : stmt.get();
			}
			return null;
		}
		/** @internal - Query all rows */
		_all(sql, params) {
			if (this._sqliteDb.query) {
				const stmt = this._sqliteDb.query(sql);
				return params ? stmt.all(...params) : stmt.all();
			}
			if (this._sqliteDb.prepare) {
				const stmt = this._sqliteDb.prepare(sql);
				return params ? stmt.all(...params) : stmt.all();
			}
			return [];
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/sqlite/Statement.ts
	var _Symbol$dispose$1, g$2, Statement;
	var init_Statement = __esmMin((() => {
		init_defineProperty();
		g$2 = globalThis;
		_Symbol$dispose$1 = Symbol.dispose;
		Statement = class {
			/** The native handle. */
			get native() {
				return {
					columns: [...this.columnNames],
					columnsCount: this.columnNames.length
				};
			}
			/** Column types based on actual values. */
			get columnTypes() {
				if (!this._readOnly) throw new Error("columnTypes is not available for non-read-only statements");
				return this._columnTypes;
			}
			/** Types from CREATE TABLE schema. */
			get declaredTypes() {
				if (this._readOnly && !this._executed) throw new Error("Statement must be executed before accessing declaredTypes");
				return this._declaredTypes;
			}
			/** @internal */
			constructor(dbHandle, sql) {
				_defineProperty(
					this,
					/** @internal */
					"_handle",
					void 0
				);
				_defineProperty(
					this,
					/** @internal */
					"_db",
					void 0
				);
				_defineProperty(
					this,
					/** @internal */
					"_sql",
					void 0
				);
				_defineProperty(
					this,
					/** @internal */
					"_finalized",
					false
				);
				_defineProperty(
					this,
					/** @internal */
					"_columnTypes",
					[]
				);
				_defineProperty(
					this,
					/** @internal */
					"_declaredTypes",
					[]
				);
				_defineProperty(
					this,
					/** @internal */
					"_readOnly",
					true
				);
				_defineProperty(
					this,
					/** @internal */
					"_executed",
					false
				);
				_defineProperty(
					this,
					/** Column names of the result set. */
					"columnNames",
					[]
				);
				_defineProperty(
					this,
					/** Number of parameters expected by the statement. */
					"paramsCount",
					0
				);
				this._db = dbHandle;
				this._sql = sql;
				if (!g$2.__exactSqlitePrepare) throw new Error("exact:sqlite native bridge not available. The exact:sqlite module requires the Exact runtime with SQLite support.");
				const result = g$2.__exactSqlitePrepare(dbHandle, sql);
				this._handle = result.handle;
				this.columnNames = result.columnNames || [];
				this._declaredTypes = result.declaredTypes || [];
				this.paramsCount = result.paramsCount || 0;
				this._readOnly = result.readOnly !== false;
			}
			/**
			* Execute the statement and return all matching rows as objects.
			*/
			all(...params) {
				this._checkFinalized();
				const bindings = this._normalizeParams(params);
				const result = g$2.__exactSqliteAll(this._handle, bindings);
				this._recordExecution(result);
				return result.rows;
			}
			/**
			* Execute the statement and return the first matching row as an object.
			* Returns null if no rows match.
			*/
			get(...params) {
				this._checkFinalized();
				const bindings = this._normalizeParams(params);
				const result = g$2.__exactSqliteGet(this._handle, bindings);
				this._recordExecution(result);
				return result.row ?? null;
			}
			/**
			* Execute the statement. Returns changes info.
			*/
			run(...params) {
				this._checkFinalized();
				const bindings = this._normalizeParams(params);
				const result = g$2.__exactSqliteRun(this._handle, bindings);
				this._executed = true;
				return result;
			}
			/**
			* Execute the statement and return all rows as arrays of values (no keys).
			*/
			values(...params) {
				this._checkFinalized();
				const bindings = this._normalizeParams(params);
				const result = g$2.__exactSqliteValues(this._handle, bindings);
				this._recordExecution(result);
				return result.rows;
			}
			/**
			* Destroy the statement and free resources.
			* Once finalized, the statement cannot be used again.
			*/
			finalize() {
				if (this._finalized) return;
				this._finalized = true;
				if (g$2.__exactSqliteFinalize) g$2.__exactSqliteFinalize(this._handle);
			}
			/**
			* Return the expanded SQL with most recent bound values.
			*/
			toString() {
				this._checkFinalized();
				if (g$2.__exactSqliteExpandedSql) return g$2.__exactSqliteExpandedSql(this._handle);
				return this._sql;
			}
			/**
			* Map results to class instances.
			* Uses Object.create (NOT new) - constructor is NOT called.
			*/
			as(Class) {
				const wrapped = Object.create(this);
				const origAll = this.all.bind(this);
				const origGet = this.get.bind(this);
				wrapped.all = function(...params) {
					return origAll(...params).map((row) => Object.assign(Object.create(Class.prototype), row));
				};
				wrapped.get = function(...params) {
					const row = origGet(...params);
					if (row === null) return null;
					return Object.assign(Object.create(Class.prototype), row);
				};
				return wrapped;
			}
			[_Symbol$dispose$1]() {
				this.finalize();
			}
			/** @internal */
			_checkFinalized() {
				if (this._finalized) throw new Error("Statement has been finalized and cannot be used");
			}
			/** @internal */
			_normalizeParams(params) {
				if (params.length === 0) return void 0;
				if (params.length === 1 && typeof params[0] === "object" && params[0] !== null && !(params[0] instanceof Uint8Array)) return params[0];
				return params;
			}
			/** @internal */
			_recordExecution(result) {
				this._executed = true;
				this._columnTypes = Array.isArray(result?.columnTypes) ? result.columnTypes : [];
			}
		};
	}));
	//#endregion
	//#region ../ibex-runtime-js/src/sqlite/Database.ts
	var _Symbol$dispose, _Symbol$toStringTag$3, g$1, Database;
	var init_Database = __esmMin((() => {
		init_Statement();
		init_defineProperty();
		g$1 = globalThis;
		_Symbol$dispose = Symbol.dispose;
		_Symbol$toStringTag$3 = Symbol.toStringTag;
		Database = class Database {
			/**
			* Create or open a SQLite database.
			*
			* @param filename Path to the database file.
			*   - `:memory:` or `""` or omitted for in-memory database
			* @param options Database options or raw SQLite open flags
			*/
			constructor(filename, options) {
				_defineProperty(
					this,
					/** @internal */
					"_handle",
					void 0
				);
				_defineProperty(
					this,
					/** @internal */
					"_closed",
					false
				);
				_defineProperty(
					this,
					/** @internal */
					"_queryCache",
					/* @__PURE__ */ new Map()
				);
				_defineProperty(
					this,
					/** @internal */
					"_options",
					void 0
				);
				_defineProperty(
					this,
					/** @internal */
					"_crSqliteLoaded",
					false
				);
				_defineProperty(this, "filename", void 0);
				this.filename = filename ?? ":memory:";
				if (typeof options === "number") this._options = {};
				else this._options = options ?? {};
				if (!g$1.__exactSqliteOpen) throw new Error("exact:sqlite native bridge not available. The exact:sqlite module requires the Exact runtime with SQLite support.");
				const flags = typeof options === "number" ? options : void 0;
				this._handle = g$1.__exactSqliteOpen(this.filename, {
					readonly: this._options.readonly ?? false,
					create: this._options.create ?? true,
					readwrite: this._options.readwrite ?? true,
					safeIntegers: this._options.safeIntegers ?? false,
					flags
				});
				this.run("PRAGMA journal_mode = WAL");
				this.run("PRAGMA foreign_keys = ON");
			}
			/**
			* The native SQLite handle.
			*/
			get handle() {
				return this._handle;
			}
			/**
			* Whether the database is currently in a transaction.
			*/
			get inTransaction() {
				this._checkClosed();
				if (g$1.__exactSqliteInTransaction) return g$1.__exactSqliteInTransaction(this._handle);
				return false;
			}
			/**
			* Prepare and cache a SQL statement.
			* Returns the cached statement if the same SQL was previously queried.
			*/
			query(sql) {
				this._checkClosed();
				let stmt = this._queryCache.get(sql);
				if (!stmt) {
					stmt = new Statement(this._handle, sql);
					this._queryCache.set(sql, stmt);
				}
				return stmt;
			}
			/**
			* Prepare a SQL statement WITHOUT caching.
			* Creates a fresh Statement each time.
			*/
			prepare(sql) {
				this._checkClosed();
				return new Statement(this._handle, sql);
			}
			/**
			* Execute a SQL statement directly.
			* Supports multi-statement SQL (e.g., "SELECT 1; SELECT 2;").
			*
			* @returns Changes info with `changes` and `lastInsertRowid`
			*/
			run(sql, ...bindings) {
				this._checkClosed();
				if (g$1.__exactSqliteExec) {
					const params = bindings.length === 1 && typeof bindings[0] === "object" && bindings[0] !== null && !(bindings[0] instanceof Uint8Array) ? bindings[0] : bindings.length > 0 ? bindings : void 0;
					return g$1.__exactSqliteExec(this._handle, sql, params);
				}
				const stmt = this.prepare(sql);
				try {
					return stmt.run(...bindings);
				} finally {
					stmt.finalize();
				}
			}
			/**
			* Alias for run().
			*/
			exec(sql, ...bindings) {
				return this.run(sql, ...bindings);
			}
			/**
			* Create a transaction wrapper function.
			*
			* @returns A callable function that wraps execution in BEGIN/COMMIT.
			*   If the function throws, the transaction is rolled back.
			*   The returned function has `.deferred()`, `.immediate()`, and `.exclusive()` variants.
			*/
			transaction(fn) {
				this._checkClosed();
				const db = this;
				const wrapTransaction = (beginStatement) => {
					return function(...args) {
						if (db.inTransaction) {
							const savepointName = `_sp_${Date.now()}_${Math.random().toString(36).slice(2)}`;
							db.run(`SAVEPOINT "${savepointName}"`);
							try {
								const result = fn.apply(this, args);
								db.run(`RELEASE "${savepointName}"`);
								return result;
							} catch (e) {
								db.run(`ROLLBACK TO "${savepointName}"`);
								db.run(`RELEASE "${savepointName}"`);
								throw e;
							}
						}
						db.run(beginStatement);
						try {
							const result = fn.apply(this, args);
							db.run("COMMIT");
							return result;
						} catch (e) {
							db.run("ROLLBACK");
							throw e;
						}
					};
				};
				const txnFn = wrapTransaction("BEGIN");
				txnFn.deferred = wrapTransaction("BEGIN DEFERRED");
				txnFn.immediate = wrapTransaction("BEGIN IMMEDIATE");
				txnFn.exclusive = wrapTransaction("BEGIN EXCLUSIVE");
				return txnFn;
			}
			/**
			* Close the database connection.
			* @param throwOnError If true, throws on close errors (default: false)
			*/
			close(throwOnError = false) {
				if (this._closed) return;
				if (this._crSqliteLoaded) try {
					this.run("SELECT crsql_finalize()");
				} catch {}
				this._closed = true;
				{
					const __exactForOfIterator95 = this._queryCache.values()[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep95 = __exactForOfIterator95.next();
						if (__exactForOfStep95.done) break;
						__exactForOfStep95.value.finalize();
					}
				}
				this._queryCache.clear();
				if (g$1.__exactSqliteClose) try {
					g$1.__exactSqliteClose(this._handle);
				} catch (e) {
					if (throwOnError) throw e;
				}
			}
			/**
			* Serialize the database to a Buffer/Uint8Array.
			* @param name Database name (default: "main")
			*/
			serialize(name) {
				this._checkClosed();
				if (!g$1.__exactSqliteSerialize) throw new Error("Database serialization not supported in this build");
				return g$1.__exactSqliteSerialize(this._handle, name ?? "main");
			}
			/**
			* Load a SQLite extension.
			* @param path Path to the extension shared library
			* @param entryPoint Optional entry point function name
			*/
			loadExtension(path, entryPoint) {
				this._checkClosed();
				if (!g$1.__exactSqliteLoadExtension) throw new Error("Extension loading not supported in this build");
				g$1.__exactSqliteLoadExtension(this._handle, path, entryPoint);
			}
			fileControl(...args) {
				this._checkClosed();
				if (!g$1.__exactSqliteFileControl) throw new Error("fileControl not supported in this build");
				return g$1.__exactSqliteFileControl(this._handle, ...args);
			}
			/**
			* Load and initialize the cr-sqlite extension for CRDT-based replication.
			*
			* After calling this, you can use:
			* - `db.run("SELECT crsql_as_crr('table_name')")` to make a table a CRR
			* - `db.query("SELECT * FROM crsql_changes WHERE db_version > ?")` to get changes
			* - `db.run("INSERT INTO crsql_changes ...")` to apply changes from another peer
			*
			* @see https://vlcn.io/docs/cr-sqlite/intro
			*/
			enableCrSqlite() {
				this._checkClosed();
				if (this._crSqliteLoaded) return;
				if (g$1.__exactCrSqlitePath) this.loadExtension(g$1.__exactCrSqlitePath());
				else if (g$1.__exactSqliteLoadCrSqlite) g$1.__exactSqliteLoadCrSqlite(this._handle);
				else throw new Error("cr-sqlite extension not available. The Exact runtime must be built with cr-sqlite support.");
				this._crSqliteLoaded = true;
			}
			/**
			* Get the unique site ID for this database (cr-sqlite).
			* Each database instance has a unique identifier for replication.
			*/
			getSiteId() {
				this._checkClosed();
				this._requireCrSqlite();
				return this.query("SELECT crsql_site_id() as id").get().id;
			}
			/**
			* Get the current database version (cr-sqlite logical clock).
			*/
			getDbVersion() {
				this._checkClosed();
				this._requireCrSqlite();
				return this.query("SELECT crsql_db_version() as version").get().version;
			}
			/**
			* Mark a table as a Conflict-free Replicated Relation (CRR).
			* This adds CRDT metadata and triggers for automatic conflict resolution.
			*
			* @param tableName Name of the existing table to make a CRR
			*/
			markAsCrr(tableName) {
				this._checkClosed();
				this._requireCrSqlite();
				this.run(`SELECT crsql_as_crr('${tableName}')`);
			}
			/**
			* Get changesets since a given database version.
			* Used for syncing with other peers.
			*
			* @param sinceVersion Get changes after this version (0 for all changes)
			* @param excludeSiteId Optional site ID to exclude (optimization for bilateral sync)
			*/
			getChanges(sinceVersion = 0, excludeSiteId) {
				this._checkClosed();
				this._requireCrSqlite();
				if (excludeSiteId) return this.query(`SELECT "table", "pk", "cid", "val", "col_version", "db_version", "site_id", "cl", "seq"
         FROM crsql_changes
         WHERE db_version > ? AND site_id IS NOT ?`).all(sinceVersion, excludeSiteId);
				return this.query(`SELECT "table", "pk", "cid", "val", "col_version", "db_version", "site_id", "cl", "seq"
       FROM crsql_changes
       WHERE db_version > ?`).all(sinceVersion);
			}
			/**
			* Apply a changeset from another peer.
			* The changeset should be the output of getChanges() from another database.
			*
			* @param changes Array of change rows from getChanges()
			*/
			applyChanges(changes) {
				this._checkClosed();
				this._requireCrSqlite();
				const insert = this.prepare(`INSERT INTO crsql_changes ("table", "pk", "cid", "val", "col_version", "db_version", "site_id", "cl", "seq")
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`);
				const applyAll = this.transaction((changes) => {
					{
						const __exactForOfIterator96 = changes[Symbol.iterator]();
						for (;;) {
							const __exactForOfStep96 = __exactForOfIterator96.next();
							if (__exactForOfStep96.done) break;
							const change = __exactForOfStep96.value;
							insert.run(change.table, change.pk, change.cid, change.val, change.col_version, change.db_version, change.site_id, change.cl, change.seq);
						}
					}
				});
				try {
					applyAll(changes);
				} finally {
					insert.finalize();
				}
			}
			/**
			* Create or open a SQLite database (alias for constructor).
			*/
			static open(filename, options) {
				return new Database(filename, options);
			}
			/**
			* Create a database from serialized data.
			*/
			static deserialize(data, options) {
				if (!g$1.__exactSqliteDeserialize) throw new Error("Database deserialization not supported in this build");
				const isReadOnly = typeof options === "boolean" ? options : options?.readonly ?? false;
				const handle = g$1.__exactSqliteDeserialize(data, isReadOnly);
				const db = Object.create(Database.prototype);
				db._handle = handle;
				db._closed = false;
				db._queryCache = /* @__PURE__ */ new Map();
				db._options = typeof options === "object" ? options : { readonly: isReadOnly };
				db.filename = ":memory:";
				db._crSqliteLoaded = false;
				return db;
			}
			[_Symbol$dispose]() {
				this.close();
			}
			get [_Symbol$toStringTag$3]() {
				return "Database";
			}
			/** @internal */
			_checkClosed() {
				if (this._closed) throw new Error("Database is closed");
			}
			/** @internal */
			_requireCrSqlite() {
				if (!this._crSqliteLoaded) throw new Error("cr-sqlite is not loaded. Call db.enableCrSqlite() first.");
			}
		};
	}));
	//#endregion
	//#region ../ibex-runtime-js/src/sqlite/constants.ts
	var constants;
	var init_constants = __esmMin((() => {
		constants = {
			SQLITE_OPEN_READONLY: 1,
			SQLITE_OPEN_READWRITE: 2,
			SQLITE_OPEN_CREATE: 4,
			SQLITE_OPEN_DELETEONCLOSE: 8,
			SQLITE_OPEN_EXCLUSIVE: 16,
			SQLITE_OPEN_AUTOPROXY: 32,
			SQLITE_OPEN_URI: 64,
			SQLITE_OPEN_MEMORY: 128,
			SQLITE_OPEN_MAIN_DB: 256,
			SQLITE_OPEN_TEMP_DB: 512,
			SQLITE_OPEN_TRANSIENT_DB: 1024,
			SQLITE_OPEN_MAIN_JOURNAL: 2048,
			SQLITE_OPEN_TEMP_JOURNAL: 4096,
			SQLITE_OPEN_SUBJOURNAL: 8192,
			SQLITE_OPEN_SUPER_JOURNAL: 16384,
			SQLITE_OPEN_NOMUTEX: 32768,
			SQLITE_OPEN_FULLMUTEX: 65536,
			SQLITE_OPEN_SHAREDCACHE: 131072,
			SQLITE_OPEN_PRIVATECACHE: 262144,
			SQLITE_OPEN_WAL: 524288,
			SQLITE_OPEN_NOFOLLOW: 16777216,
			SQLITE_PREPARE_PERSISTENT: 1,
			SQLITE_PREPARE_NORMALIZE: 2,
			SQLITE_PREPARE_NO_VTAB: 4,
			SQLITE_FCNTL_LOCKSTATE: 1,
			SQLITE_FCNTL_GET_LOCKPROXYFILE: 2,
			SQLITE_FCNTL_SET_LOCKPROXYFILE: 3,
			SQLITE_FCNTL_LAST_ERRNO: 4,
			SQLITE_FCNTL_SIZE_HINT: 5,
			SQLITE_FCNTL_CHUNK_SIZE: 6,
			SQLITE_FCNTL_FILE_POINTER: 7,
			SQLITE_FCNTL_SYNC_OMITTED: 8,
			SQLITE_FCNTL_WIN32_AV_RETRY: 9,
			SQLITE_FCNTL_PERSIST_WAL: 10,
			SQLITE_FCNTL_OVERWRITE: 11,
			SQLITE_FCNTL_VFSNAME: 12,
			SQLITE_FCNTL_POWERSAFE_OVERWRITE: 13,
			SQLITE_FCNTL_PRAGMA: 14,
			SQLITE_FCNTL_BUSYHANDLER: 15,
			SQLITE_FCNTL_TEMPFILENAME: 16,
			SQLITE_FCNTL_MMAP_SIZE: 18,
			SQLITE_FCNTL_TRACE: 19,
			SQLITE_FCNTL_HAS_MOVED: 20,
			SQLITE_FCNTL_SYNC: 21,
			SQLITE_FCNTL_COMMIT_PHASETWO: 22,
			SQLITE_FCNTL_WIN32_SET_HANDLE: 23,
			SQLITE_FCNTL_WAL_BLOCK: 24,
			SQLITE_FCNTL_ZIPVFS: 25,
			SQLITE_FCNTL_RBU: 26,
			SQLITE_FCNTL_VFS_POINTER: 27,
			SQLITE_FCNTL_JOURNAL_POINTER: 28,
			SQLITE_FCNTL_WIN32_GET_HANDLE: 29,
			SQLITE_FCNTL_PDB: 30,
			SQLITE_FCNTL_BEGIN_ATOMIC_WRITE: 31,
			SQLITE_FCNTL_COMMIT_ATOMIC_WRITE: 32,
			SQLITE_FCNTL_ROLLBACK_ATOMIC_WRITE: 33,
			SQLITE_FCNTL_LOCK_TIMEOUT: 34,
			SQLITE_FCNTL_DATA_VERSION: 35,
			SQLITE_FCNTL_SIZE_LIMIT: 36,
			SQLITE_FCNTL_CKPT_DONE: 37,
			SQLITE_FCNTL_RESERVE_BYTES: 38,
			SQLITE_FCNTL_CKPT_START: 39,
			SQLITE_FCNTL_EXTERNAL_READER: 40,
			SQLITE_FCNTL_CKSM_FILE: 41
		};
	}));
	//#endregion
	//#region ../ibex-runtime-js/src/sqlite/errors.ts
	var SQLiteError;
	var init_errors = __esmMin((() => {
		init_defineProperty();
		SQLiteError = class {
			constructor(message, errno = 0, code, byteOffset = 0) {
				_defineProperty(this, "name", "SQLiteError");
				_defineProperty(this, "message", void 0);
				_defineProperty(this, "stack", void 0);
				_defineProperty(this, "errno", void 0);
				_defineProperty(this, "code", void 0);
				_defineProperty(this, "byteOffset", void 0);
				this.message = message;
				this.stack = new Error(this.message).stack;
				this.errno = errno;
				this.code = code;
				this.byteOffset = byteOffset;
			}
		};
		Object.setPrototypeOf(SQLiteError.prototype, Error.prototype);
		Object.setPrototypeOf(SQLiteError, Error);
	}));
	//#endregion
	//#region ../ibex-runtime-js/src/sqlite/index.ts
	var sqlite_exports = /* @__PURE__ */ __exportAll({
		Database: () => Database,
		SQLiteError: () => SQLiteError,
		Statement: () => Statement,
		constants: () => constants,
		default: () => Database
	});
	var init_sqlite = __esmMin((() => {
		init_Database();
		init_Statement();
		init_constants();
		init_errors();
	}));
	//#endregion
	//#region ../ibex-runtime-js/src/indexeddb/IDBFactory.ts
	/**
	* IDBFactory - IndexedDB Factory
	*
	* The main entry point for IndexedDB. Provides open() and deleteDatabase().
	* Backed by SQLite via the exact:sqlite module.
	*
	* @see https://developer.mozilla.org/en-US/docs/Web/API/IDBFactory
	*/
	init_defineProperty();
	/**
	* Default provider that uses exact:sqlite Database.
	*/
	var DefaultSQLiteProvider = class {
		create(name) {
			if (globalThis.__exactSqliteOpen) {
				const { Database } = (init_sqlite(), __toCommonJS(sqlite_exports));
				return new Database(`:memory:`);
			}
			throw new Error("IndexedDB requires SQLite support. The exact:sqlite native bridge is not available.");
		}
	};
	var IDBFactory = class {
		constructor(provider) {
			_defineProperty(this, "_databases", /* @__PURE__ */ new Map());
			_defineProperty(this, "_sqliteProvider", void 0);
			this._sqliteProvider = provider ?? new DefaultSQLiteProvider();
		}
		/**
		* Open (or create) a database with the given name and optional version.
		*
		* Returns an IDBOpenDBRequest. Set onupgradeneeded and onsuccess handlers
		* on the request before the event loop yields.
		*/
		open(name, version) {
			const request = new IDBOpenDBRequest();
			const resolvedVersion = version ?? 1;
			if (resolvedVersion <= 0 || !Number.isFinite(resolvedVersion) || Math.floor(resolvedVersion) !== resolvedVersion) {
				queueMicrotask(() => {
					request._reject(/* @__PURE__ */ new TypeError(`The version provided (${version}) is not a valid positive integer.`));
				});
				return request;
			}
			queueMicrotask(() => {
				try {
					const existing = this._databases.get(name);
					const oldVersion = existing ? existing.version : 0;
					let sqliteDb;
					if (existing) sqliteDb = existing.sqliteDb;
					else sqliteDb = this._sqliteProvider.create(name);
					const db = new IDBDatabase(name, resolvedVersion, sqliteDb);
					request._setResult(db);
					if (resolvedVersion > oldVersion) {
						db._upgradeTransaction = new IDBTransaction(db, Array.from(db._objectStores?.keys?.() ?? []), "versionchange");
						if (request.onupgradeneeded) {
							const event = {
								type: "upgradeneeded",
								target: request,
								oldVersion,
								newVersion: resolvedVersion
							};
							request.onupgradeneeded(event);
						}
						db._upgradeTransaction = null;
					}
					this._databases.set(name, {
						version: resolvedVersion,
						sqliteDb
					});
					request._resolveSync(db);
				} catch (e) {
					request._reject(e);
				}
			});
			return request;
		}
		/**
		* Delete a database.
		*/
		deleteDatabase(name) {
			const request = new IDBOpenDBRequest();
			queueMicrotask(() => {
				try {
					const existing = this._databases.get(name);
					if (existing) {
						if (existing.sqliteDb && existing.sqliteDb.close) existing.sqliteDb.close();
						this._databases.delete(name);
					}
					request._resolveSync(void 0);
				} catch (e) {
					request._reject(e);
				}
			});
			return request;
		}
		/**
		* Compare two keys using IndexedDB key comparison rules.
		*/
		cmp(first, second) {
			const result = compareKeys(first, second);
			return result < 0 ? -1 : result > 0 ? 1 : 0;
		}
		/**
		* List all databases. Returns a promise.
		*/
		databases() {
			const result = Array.from(this._databases.entries()).map(([name, info]) => ({
				name,
				version: info.version
			}));
			return Promise.resolve(result);
		}
	};
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/array.ts
	/**
	* ES2023+ Array polyfills for Hermes engine
	*
	* - Array.prototype.toSorted()
	* - Array.prototype.toReversed()
	* - Array.prototype.with()
	* - Array.prototype.toSpliced()
	* - Array.fromAsync()
	*/
	function installArrayPolyfills() {
		if (typeof Array.prototype.toSorted !== "function") Object.defineProperty(Array.prototype, "toSorted", {
			value: function toSorted(compareFn) {
				if (this == null) throw new TypeError("Array.prototype.toSorted called on null or undefined");
				const o = Object(this);
				const len = o.length >>> 0;
				const copy = new Array(len);
				for (let i = 0; i < len; i++) copy[i] = o[i];
				copy.sort(compareFn);
				return copy;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Array.prototype.toReversed !== "function") Object.defineProperty(Array.prototype, "toReversed", {
			value: function toReversed() {
				if (this == null) throw new TypeError("Array.prototype.toReversed called on null or undefined");
				const o = Object(this);
				const len = o.length >>> 0;
				const result = new Array(len);
				for (let i = 0; i < len; i++) result[i] = o[len - 1 - i];
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Array.prototype.with !== "function") Object.defineProperty(Array.prototype, "with", {
			value: function withMethod(index, value) {
				if (this == null) throw new TypeError("Array.prototype.with called on null or undefined");
				const o = Object(this);
				const len = o.length >>> 0;
				const relativeIndex = Number(index);
				const actualIndex = relativeIndex < 0 ? len + relativeIndex : relativeIndex;
				if (actualIndex < 0 || actualIndex >= len) throw new RangeError(`Invalid index : ${index}`);
				const result = new Array(len);
				for (let i = 0; i < len; i++) result[i] = i === actualIndex ? value : o[i];
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Array.prototype.toSpliced !== "function") Object.defineProperty(Array.prototype, "toSpliced", {
			value: function toSpliced(start, deleteCount, ...items) {
				if (this == null) throw new TypeError("Array.prototype.toSpliced called on null or undefined");
				const o = Object(this);
				const len = o.length >>> 0;
				const relativeStart = Number(start) || 0;
				let actualStart;
				if (relativeStart < 0) actualStart = Math.max(len + relativeStart, 0);
				else actualStart = Math.min(relativeStart, len);
				let actualDeleteCount;
				if (deleteCount === void 0 && items.length === 0) actualDeleteCount = len - actualStart;
				else actualDeleteCount = Math.min(Math.max(Number(deleteCount) || 0, 0), len - actualStart);
				const newLen = len - actualDeleteCount + items.length;
				const result = new Array(newLen);
				let r = 0;
				for (let i = 0; i < actualStart; i++) result[r++] = o[i];
				for (let i = 0; i < items.length; i++) result[r++] = items[i];
				for (let i = actualStart + actualDeleteCount; i < len; i++) result[r++] = o[i];
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Array.fromAsync !== "function") Object.defineProperty(Array, "fromAsync", {
			value: async function fromAsync(arrayLike, mapFn, thisArg) {
				if (mapFn !== void 0 && typeof mapFn !== "function") throw new TypeError("mapFn must be a function");
				const result = [];
				let index = 0;
				if (arrayLike != null && typeof arrayLike[Symbol.asyncIterator] === "function") {
					const iterator = arrayLike[Symbol.asyncIterator]();
					let shouldCloseIterator = false;
					try {
						while (true) {
							const step = await iterator.next();
							if (step.done) return result;
							shouldCloseIterator = true;
							const value = mapFn ? await mapFn.call(thisArg, step.value, index) : step.value;
							result.push(value);
							index++;
							shouldCloseIterator = false;
						}
					} catch (error) {
						if (shouldCloseIterator && typeof iterator.return === "function") try {
							await iterator.return();
						} catch {}
						throw error;
					}
				}
				if (arrayLike != null && typeof arrayLike[Symbol.iterator] === "function") {
					for (const item of arrayLike) {
						const awaited = await item;
						const value = mapFn ? await mapFn.call(thisArg, awaited, index) : awaited;
						result.push(value);
						index++;
					}
					return result;
				}
				const len = (arrayLike && arrayLike.length) >>> 0;
				for (let i = 0; i < len; i++) {
					const awaited = await arrayLike[i];
					const value = mapFn ? await mapFn.call(thisArg, awaited, i) : awaited;
					result.push(value);
				}
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/groupby.ts
	/**
	* ES2024 Object.groupBy and Map.groupBy polyfills for Hermes engine
	*
	* - Object.groupBy()
	* - Map.groupBy()
	*/
	function installGroupByPolyfills() {
		if (typeof Object.groupBy !== "function") Object.defineProperty(Object, "groupBy", {
			value: function groupBy(items, callbackFn) {
				if (typeof callbackFn !== "function") throw new TypeError("callbackFn must be a function");
				if (items == null) throw new TypeError("items is null or undefined");
				const result = Object.create(null);
				let index = 0;
				{
					const __exactForOfIterator97 = items[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep97 = __exactForOfIterator97.next();
						if (__exactForOfStep97.done) break;
						const item = __exactForOfStep97.value;
						const key = callbackFn(item, index++);
						if (key in result) result[key].push(item);
						else result[key] = [item];
					}
				}
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Map.groupBy !== "function") Object.defineProperty(Map, "groupBy", {
			value: function groupBy(items, callbackFn) {
				if (typeof callbackFn !== "function") throw new TypeError("callbackFn must be a function");
				if (items == null) throw new TypeError("items is null or undefined");
				const result = /* @__PURE__ */ new Map();
				let index = 0;
				{
					const __exactForOfIterator98 = items[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep98 = __exactForOfIterator98.next();
						if (__exactForOfStep98.done) break;
						const item = __exactForOfStep98.value;
						const key = callbackFn(item, index++);
						const group = result.get(key);
						if (group !== void 0) group.push(item);
						else result.set(key, [item]);
					}
				}
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/set.ts
	/**
	* ES2025 Set methods polyfills for Hermes engine
	*
	* - Set.prototype.union()
	* - Set.prototype.intersection()
	* - Set.prototype.difference()
	* - Set.prototype.symmetricDifference()
	* - Set.prototype.isSubsetOf()
	* - Set.prototype.isSupersetOf()
	* - Set.prototype.isDisjointFrom()
	*/
	/**
	* Helper: get the size of a Set-like object.
	*/
	function getSetLikeSize(other) {
		if (typeof other.size === "number") return other.size;
		if (typeof other.size === "function") return other.size();
		throw new TypeError("Set-like object must have a size property");
	}
	/**
	* Helper: ensure a Set-like has the minimum required interface.
	*/
	function requireSetLike(other) {
		if (other == null || typeof other !== "object") throw new TypeError("The argument must be a Set-like object");
		if (typeof other.has !== "function") throw new TypeError("Set-like object must have a has method");
	}
	function installSetPolyfills() {
		if (typeof Set.prototype.union !== "function") Object.defineProperty(Set.prototype, "union", {
			value: function union(other) {
				requireSetLike(other);
				const result = new Set(this);
				if (typeof other[Symbol.iterator] === "function") {
					const __exactForOfIterator99 = other[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep99 = __exactForOfIterator99.next();
						if (__exactForOfStep99.done) break;
						const value = __exactForOfStep99.value;
						result.add(value);
					}
				} else if (typeof other.forEach === "function") other.forEach((value) => result.add(value));
				else throw new TypeError("Set-like object must be iterable or have forEach");
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Set.prototype.intersection !== "function") Object.defineProperty(Set.prototype, "intersection", {
			value: function intersection(other) {
				requireSetLike(other);
				const result = /* @__PURE__ */ new Set();
				const otherSize = getSetLikeSize(other);
				if (this.size <= otherSize) {
					const __exactForOfIterator100 = this[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep100 = __exactForOfIterator100.next();
						if (__exactForOfStep100.done) break;
						const value = __exactForOfStep100.value;
						if (other.has(value)) result.add(value);
					}
				} else if (typeof other[Symbol.iterator] === "function") {
					const __exactForOfIterator101 = other[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep101 = __exactForOfIterator101.next();
						if (__exactForOfStep101.done) break;
						const value = __exactForOfStep101.value;
						if (this.has(value)) result.add(value);
					}
				} else if (typeof other.forEach === "function") other.forEach((value) => {
					if (this.has(value)) result.add(value);
				});
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Set.prototype.difference !== "function") Object.defineProperty(Set.prototype, "difference", {
			value: function difference(other) {
				requireSetLike(other);
				const result = /* @__PURE__ */ new Set();
				{
					const __exactForOfIterator102 = this[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep102 = __exactForOfIterator102.next();
						if (__exactForOfStep102.done) break;
						const value = __exactForOfStep102.value;
						if (!other.has(value)) result.add(value);
					}
				}
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Set.prototype.symmetricDifference !== "function") Object.defineProperty(Set.prototype, "symmetricDifference", {
			value: function symmetricDifference(other) {
				requireSetLike(other);
				const result = new Set(this);
				if (typeof other[Symbol.iterator] === "function") {
					const __exactForOfIterator103 = other[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep103 = __exactForOfIterator103.next();
						if (__exactForOfStep103.done) break;
						const value = __exactForOfStep103.value;
						if (result.has(value)) result.delete(value);
						else result.add(value);
					}
				} else if (typeof other.forEach === "function") other.forEach((value) => {
					if (result.has(value)) result.delete(value);
					else result.add(value);
				});
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Set.prototype.isSubsetOf !== "function") Object.defineProperty(Set.prototype, "isSubsetOf", {
			value: function isSubsetOf(other) {
				requireSetLike(other);
				const otherSize = getSetLikeSize(other);
				if (this.size > otherSize) return false;
				for (const value of this) if (!other.has(value)) return false;
				return true;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Set.prototype.isSupersetOf !== "function") Object.defineProperty(Set.prototype, "isSupersetOf", {
			value: function isSupersetOf(other) {
				requireSetLike(other);
				const otherSize = getSetLikeSize(other);
				if (this.size < otherSize) return false;
				if (typeof other[Symbol.iterator] === "function") {
					for (const value of other) if (!this.has(value)) return false;
				} else if (typeof other.forEach === "function") {
					let allPresent = true;
					other.forEach((value) => {
						if (!this.has(value)) allPresent = false;
					});
					return allPresent;
				}
				return true;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Set.prototype.isDisjointFrom !== "function") Object.defineProperty(Set.prototype, "isDisjointFrom", {
			value: function isDisjointFrom(other) {
				requireSetLike(other);
				const otherSize = getSetLikeSize(other);
				if (this.size <= otherSize) {
					for (const value of this) if (other.has(value)) return false;
				} else if (typeof other[Symbol.iterator] === "function") {
					for (const value of other) if (this.has(value)) return false;
				} else if (typeof other.forEach === "function") {
					let disjoint = true;
					other.forEach((value) => {
						if (this.has(value)) disjoint = false;
					});
					return disjoint;
				}
				return true;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/promise.ts
	/**
	* ES2024 Promise.withResolvers polyfill for Hermes engine
	*
	* - Promise.withResolvers()
	*/
	function installPromisePolyfills() {
		if (typeof Promise.withResolvers !== "function") Object.defineProperty(Promise, "withResolvers", {
			value: function withResolvers() {
				let resolve;
				let reject;
				return {
					promise: new Promise((res, rej) => {
						resolve = res;
						reject = rej;
					}),
					resolve,
					reject
				};
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/string.ts
	/**
	* ES2024 String well-formed polyfills for Hermes engine
	*
	* - String.prototype.isWellFormed()
	* - String.prototype.toWellFormed()
	*/
	function installStringPolyfills() {
		if (typeof String.prototype.isWellFormed !== "function") Object.defineProperty(String.prototype, "isWellFormed", {
			value: function isWellFormed() {
				if (this == null) throw new TypeError("String.prototype.isWellFormed called on null or undefined");
				const str = String(this);
				for (let i = 0; i < str.length; i++) {
					const code = str.charCodeAt(i);
					if (code >= 55296 && code <= 56319) {
						const next = str.charCodeAt(i + 1);
						if (next < 56320 || next > 57343 || i + 1 >= str.length) return false;
						i++;
					} else if (code >= 56320 && code <= 57343) return false;
				}
				return true;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof String.prototype.toWellFormed !== "function") Object.defineProperty(String.prototype, "toWellFormed", {
			value: function toWellFormed() {
				if (this == null) throw new TypeError("String.prototype.toWellFormed called on null or undefined");
				const str = String(this);
				const result = [];
				for (let i = 0; i < str.length; i++) {
					const code = str.charCodeAt(i);
					if (code >= 55296 && code <= 56319) {
						const next = str.charCodeAt(i + 1);
						if (next >= 56320 && next <= 57343 && i + 1 < str.length) {
							result.push(str[i], str[i + 1]);
							i++;
						} else result.push("�");
					} else if (code >= 56320 && code <= 57343) result.push("�");
					else result.push(str[i]);
				}
				return result.join("");
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/arraybuffer.ts
	/**
	* ES2024 ArrayBuffer transfer polyfills for Hermes engine
	*
	* - ArrayBuffer.prototype.transfer()
	* - ArrayBuffer.prototype.transferToFixedLength()
	*/
	function installArrayBufferPolyfills() {
		const NativeArrayBuffer = ArrayBuffer;
		const resizableBuffers = /* @__PURE__ */ new WeakSet();
		const maxByteLengthByBuffer = /* @__PURE__ */ new WeakMap();
		if (!NativeArrayBuffer.__exactResizableWrapped) {
			const WrappedArrayBuffer = function ArrayBuffer(length, options) {
				const buffer = new NativeArrayBuffer(length);
				if (options && typeof options === "object" && options.maxByteLength !== void 0) {
					const maxByteLength = Number(options.maxByteLength);
					if (!Number.isFinite(maxByteLength) || maxByteLength < buffer.byteLength) throw new RangeError("Invalid array buffer max length");
					resizableBuffers.add(buffer);
					maxByteLengthByBuffer.set(buffer, maxByteLength);
				}
				return buffer;
			};
			WrappedArrayBuffer.prototype = NativeArrayBuffer.prototype;
			Object.setPrototypeOf(WrappedArrayBuffer, NativeArrayBuffer);
			Object.defineProperty(WrappedArrayBuffer, "__exactResizableWrapped", {
				value: true,
				configurable: true
			});
			globalThis.ArrayBuffer = WrappedArrayBuffer;
		}
		if (!Object.getOwnPropertyDescriptor(ArrayBuffer.prototype, "resizable")) Object.defineProperty(ArrayBuffer.prototype, "resizable", {
			get: function resizable() {
				if (!(this instanceof ArrayBuffer)) throw new TypeError("resizable called on non-ArrayBuffer");
				return resizableBuffers.has(this);
			},
			enumerable: false,
			configurable: true
		});
		if (!Object.getOwnPropertyDescriptor(ArrayBuffer.prototype, "maxByteLength")) Object.defineProperty(ArrayBuffer.prototype, "maxByteLength", {
			get: function maxByteLength() {
				if (!(this instanceof ArrayBuffer)) throw new TypeError("maxByteLength called on non-ArrayBuffer");
				return maxByteLengthByBuffer.get(this) ?? this.byteLength;
			},
			enumerable: false,
			configurable: true
		});
		if (typeof ArrayBuffer.prototype.resize !== "function") Object.defineProperty(ArrayBuffer.prototype, "resize", {
			value: function resize(newLength) {
				if (!(this instanceof ArrayBuffer)) throw new TypeError("resize called on non-ArrayBuffer");
				if (!resizableBuffers.has(this)) throw new TypeError("Cannot resize a fixed-length ArrayBuffer");
				const targetLength = Number(newLength);
				if (!Number.isFinite(targetLength) || targetLength < 0) throw new RangeError("Invalid array buffer length");
				if (targetLength > (maxByteLengthByBuffer.get(this) ?? this.byteLength)) throw new RangeError("Invalid array buffer length");
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof ArrayBuffer.prototype.transfer !== "function") Object.defineProperty(ArrayBuffer.prototype, "transfer", {
			value: function transfer(newLength) {
				if (!(this instanceof ArrayBuffer)) throw new TypeError("transfer called on non-ArrayBuffer");
				const oldLength = this.byteLength;
				const targetLength = newLength !== void 0 ? Number(newLength) : oldLength;
				if (targetLength < 0 || !Number.isFinite(targetLength)) throw new RangeError("Invalid array buffer length");
				const newBuffer = new ArrayBuffer(targetLength);
				const copyLength = Math.min(oldLength, targetLength);
				if (copyLength > 0) {
					const source = new Uint8Array(this, 0, copyLength);
					new Uint8Array(newBuffer, 0, copyLength).set(source);
				}
				markDetachedArrayBuffer(this);
				return newBuffer;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof ArrayBuffer.prototype.transferToFixedLength !== "function") Object.defineProperty(ArrayBuffer.prototype, "transferToFixedLength", {
			value: function transferToFixedLength(newLength) {
				if (!(this instanceof ArrayBuffer)) throw new TypeError("transferToFixedLength called on non-ArrayBuffer");
				const oldLength = this.byteLength;
				const targetLength = newLength !== void 0 ? Number(newLength) : oldLength;
				if (targetLength < 0 || !Number.isFinite(targetLength)) throw new RangeError("Invalid array buffer length");
				const newBuffer = new ArrayBuffer(targetLength);
				const copyLength = Math.min(oldLength, targetLength);
				if (copyLength > 0) {
					const source = new Uint8Array(this, 0, copyLength);
					new Uint8Array(newBuffer, 0, copyLength).set(source);
				}
				markDetachedArrayBuffer(this);
				return newBuffer;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/typedarray.ts
	function normalizeTypedArrayIndex(index, length) {
		if (index === void 0) return 0;
		let value = Number(index);
		if (!Number.isFinite(value) || value === 0) return 0;
		value = value < 0 ? Math.ceil(value) : Math.floor(value);
		if (value < 0) return Math.max(length + value, 0);
		return Math.min(value, length);
	}
	function installTypedArrayPolyfills() {
		const typedArrayNames = [
			"Int8Array",
			"Uint8Array",
			"Uint8ClampedArray",
			"Int16Array",
			"Uint16Array",
			"Int32Array",
			"Uint32Array",
			"Float32Array",
			"Float64Array"
		];
		if (typeof globalThis.BigInt64Array === "function") typedArrayNames.push("BigInt64Array");
		if (typeof globalThis.BigUint64Array === "function") typedArrayNames.push("BigUint64Array");
		for (const ctorName of typedArrayNames) {
			const TypedArrayCtor = globalThis[ctorName];
			if (!TypedArrayCtor || typeof TypedArrayCtor.prototype?.subarray !== "function") continue;
			const originalSubarray = TypedArrayCtor.prototype.subarray;
			if (originalSubarray.__exactZeroLengthWrapped) continue;
			const bytesPerElement = TypedArrayCtor.BYTES_PER_ELEMENT ?? TypedArrayCtor.prototype.BYTES_PER_ELEMENT ?? 1;
			const patchedSubarray = function(begin, end) {
				const result = originalSubarray.call(this, begin, end);
				if (result.byteLength !== 0 || result.buffer !== this.buffer || !(this.buffer instanceof ArrayBuffer)) return result;
				const length = typeof this.length === "number" ? this.length : Math.floor(this.byteLength / bytesPerElement);
				const start = normalizeTypedArrayIndex(begin, length);
				if ((end === void 0 ? length : normalizeTypedArrayIndex(end, length)) !== start) return result;
				const correctedByteOffset = this.byteOffset + start * bytesPerElement;
				try {
					Object.defineProperty(result, "byteOffset", {
						value: correctedByteOffset,
						configurable: true
					});
					return result;
				} catch (_error) {
					return new TypedArrayCtor(this.buffer, correctedByteOffset, 0);
				}
			};
			Object.defineProperty(patchedSubarray, "__exactZeroLengthWrapped", {
				value: true,
				configurable: true
			});
			Object.defineProperty(TypedArrayCtor.prototype, "subarray", {
				value: patchedSubarray,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/iterator.ts
	/**
	* ES2025 Iterator Helpers polyfills for Hermes engine
	*
	* - Iterator.prototype.map()
	* - Iterator.prototype.filter()
	* - Iterator.prototype.take()
	* - Iterator.prototype.drop()
	* - Iterator.prototype.flatMap()
	* - Iterator.prototype.reduce()
	* - Iterator.prototype.toArray()
	* - Iterator.prototype.forEach()
	* - Iterator.prototype.some()
	* - Iterator.prototype.every()
	* - Iterator.prototype.find()
	* - Iterator.from()
	*/
	function installIteratorPolyfills() {
		const g = globalThis;
		let IteratorConstructor = g.Iterator;
		if (typeof IteratorConstructor !== "function") {
			IteratorConstructor = function Iterator() {
				if (new.target === IteratorConstructor) throw new TypeError("Iterator is not directly constructible");
			};
			IteratorConstructor.prototype = Object.create(null);
			Object.defineProperty(IteratorConstructor.prototype, Symbol.iterator, {
				value: function() {
					return this;
				},
				writable: true,
				enumerable: false,
				configurable: true
			});
			Object.defineProperty(IteratorConstructor.prototype, "constructor", {
				value: IteratorConstructor,
				writable: true,
				enumerable: false,
				configurable: true
			});
			Object.defineProperty(IteratorConstructor.prototype, Symbol.toStringTag, {
				value: "Iterator",
				writable: false,
				enumerable: false,
				configurable: true
			});
			Object.defineProperty(g, "Iterator", {
				value: IteratorConstructor,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		const IteratorProto = IteratorConstructor.prototype;
		/**
		* Helper to create a wrapper iterator that inherits from Iterator.prototype.
		*/
		function createIteratorHelper(nextFn, returnFn) {
			const iter = Object.create(IteratorProto);
			Object.defineProperty(iter, "next", {
				value: nextFn,
				writable: true,
				enumerable: false,
				configurable: true
			});
			if (returnFn) Object.defineProperty(iter, "return", {
				value: returnFn,
				writable: true,
				enumerable: false,
				configurable: true
			});
			return iter;
		}
		if (typeof IteratorProto.map !== "function") Object.defineProperty(IteratorProto, "map", {
			value: function map(mapper) {
				if (typeof mapper !== "function") throw new TypeError("mapper must be a function");
				const source = this;
				let index = 0;
				return createIteratorHelper(function next() {
					const result = source.next();
					if (result.done) return result;
					return {
						value: mapper(result.value, index++),
						done: false
					};
				}, function returnFn() {
					if (typeof source.return === "function") return source.return();
					return {
						value: void 0,
						done: true
					};
				});
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.filter !== "function") Object.defineProperty(IteratorProto, "filter", {
			value: function filter(predicate) {
				if (typeof predicate !== "function") throw new TypeError("predicate must be a function");
				const source = this;
				let index = 0;
				return createIteratorHelper(function next() {
					while (true) {
						const result = source.next();
						if (result.done) return result;
						if (predicate(result.value, index++)) return {
							value: result.value,
							done: false
						};
					}
				}, function returnFn() {
					if (typeof source.return === "function") return source.return();
					return {
						value: void 0,
						done: true
					};
				});
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.take !== "function") Object.defineProperty(IteratorProto, "take", {
			value: function take(limit) {
				const n = Number(limit);
				if (!Number.isInteger(n) || n < 0) throw new RangeError("take argument must be a non-negative integer");
				const source = this;
				let remaining = n;
				return createIteratorHelper(function next() {
					if (remaining <= 0) return {
						value: void 0,
						done: true
					};
					remaining--;
					const result = source.next();
					if (result.done) remaining = 0;
					return result;
				}, function returnFn() {
					if (typeof source.return === "function") return source.return();
					return {
						value: void 0,
						done: true
					};
				});
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.drop !== "function") Object.defineProperty(IteratorProto, "drop", {
			value: function drop(count) {
				const n = Number(count);
				if (!Number.isInteger(n) || n < 0) throw new RangeError("drop argument must be a non-negative integer");
				const source = this;
				let dropped = false;
				return createIteratorHelper(function next() {
					if (!dropped) {
						dropped = true;
						for (let i = 0; i < n; i++) {
							const result = source.next();
							if (result.done) return result;
						}
					}
					return source.next();
				}, function returnFn() {
					if (typeof source.return === "function") return source.return();
					return {
						value: void 0,
						done: true
					};
				});
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.flatMap !== "function") Object.defineProperty(IteratorProto, "flatMap", {
			value: function flatMap(mapper) {
				if (typeof mapper !== "function") throw new TypeError("mapper must be a function");
				const source = this;
				let index = 0;
				let innerIterator = null;
				return createIteratorHelper(function next() {
					while (true) {
						if (innerIterator !== null) {
							const innerResult = innerIterator.next();
							if (!innerResult.done) return {
								value: innerResult.value,
								done: false
							};
							innerIterator = null;
						}
						const result = source.next();
						if (result.done) return result;
						const mapped = mapper(result.value, index++);
						if (mapped == null || typeof mapped !== "object") throw new TypeError("GetIteratorFlattenable expects its first argument to be an object");
						if (typeof mapped[Symbol.iterator] === "function") innerIterator = mapped[Symbol.iterator]();
						else if (typeof mapped.next === "function") innerIterator = mapped;
						else throw new TypeError("GetIteratorFlattenable expects its first argument to be an object");
					}
				}, function returnFn() {
					if (innerIterator && typeof innerIterator.return === "function") innerIterator.return();
					if (typeof source.return === "function") return source.return();
					return {
						value: void 0,
						done: true
					};
				});
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.reduce !== "function") Object.defineProperty(IteratorProto, "reduce", {
			value: function reduce(reducer, ...args) {
				if (typeof reducer !== "function") throw new TypeError("reducer must be a function");
				const source = this;
				let accumulator;
				let index = 0;
				if (args.length > 0) accumulator = args[0];
				else {
					const first = source.next();
					if (first.done) throw new TypeError("Reduce of empty iterator with no initial value");
					accumulator = first.value;
					index = 1;
				}
				while (true) {
					const result = source.next();
					if (result.done) break;
					accumulator = reducer(accumulator, result.value, index++);
				}
				return accumulator;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.toArray !== "function") Object.defineProperty(IteratorProto, "toArray", {
			value: function toArray() {
				const result = [];
				while (true) {
					const item = this.next();
					if (item.done) break;
					result.push(item.value);
				}
				return result;
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.forEach !== "function") Object.defineProperty(IteratorProto, "forEach", {
			value: function forEach(fn) {
				if (typeof fn !== "function") throw new TypeError("fn must be a function");
				let index = 0;
				while (true) {
					const result = this.next();
					if (result.done) break;
					fn(result.value, index++);
				}
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.some !== "function") Object.defineProperty(IteratorProto, "some", {
			value: function some(predicate) {
				if (typeof predicate !== "function") throw new TypeError("predicate must be a function");
				let index = 0;
				while (true) {
					const result = this.next();
					if (result.done) return false;
					if (predicate(result.value, index++)) return true;
				}
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.every !== "function") Object.defineProperty(IteratorProto, "every", {
			value: function every(predicate) {
				if (typeof predicate !== "function") throw new TypeError("predicate must be a function");
				let index = 0;
				while (true) {
					const result = this.next();
					if (result.done) return true;
					if (!predicate(result.value, index++)) return false;
				}
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorProto.find !== "function") Object.defineProperty(IteratorProto, "find", {
			value: function find(predicate) {
				if (typeof predicate !== "function") throw new TypeError("predicate must be a function");
				let index = 0;
				while (true) {
					const result = this.next();
					if (result.done) return void 0;
					if (predicate(result.value, index++)) return result.value;
				}
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof IteratorConstructor.from !== "function") Object.defineProperty(IteratorConstructor, "from", {
			value: function from(value) {
				if (value == null) throw new TypeError("Iterator.from requires a non-null value");
				if (value instanceof IteratorConstructor) return value;
				let iterator;
				if (typeof value[Symbol.iterator] === "function") iterator = value[Symbol.iterator]();
				else if (typeof value.next === "function") iterator = value;
				else throw new TypeError("Value is not iterable or iterator-like");
				if (iterator instanceof IteratorConstructor) return iterator;
				return createIteratorHelper(function next() {
					return iterator.next();
				}, function returnFn() {
					if (typeof iterator.return === "function") return iterator.return();
					return {
						value: void 0,
						done: true
					};
				});
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/intl.ts
	/**
	* Intl polyfills for Hermes engine
	*
	* Hermes has limited Intl support. It provides basic Intl.DateTimeFormat and
	* Intl.NumberFormat, but may lack newer methods (formatRange, formatToParts)
	* and entirely missing APIs like:
	* - Intl.PluralRules
	* - Intl.ListFormat
	* - Intl.RelativeTimeFormat
	* - Intl.Segmenter
	* - Intl.DisplayNames
	* - Intl.Collator
	*
	* These polyfills cover the API surface with locale-aware plural rules for
	* the top 14 locales, proper formatToParts decomposition for NumberFormat
	* and DateTimeFormat, improved grapheme segmentation for emoji, and more.
	* They will be superseded by native implementations when available.
	*/
	init_defineProperty();
	function parseUnicodeExtensionKeyword(localeTag, keyword) {
		const extensions = localeTag.split("-u-")[1];
		if (!extensions) return;
		const tokens = extensions.split("-").filter(Boolean);
		for (let index = 0; index < tokens.length; index += 1) {
			if (tokens[index] !== keyword) continue;
			const value = tokens[index + 1];
			return value && value.length > 0 ? value : void 0;
		}
	}
	function installIntlPolyfills() {
		const g = globalThis;
		if (typeof g.Intl === "undefined") g.Intl = {};
		const Intl = g.Intl;
		if (typeof Intl.getCanonicalLocales !== "function") Object.defineProperty(Intl, "getCanonicalLocales", {
			value: function getCanonicalLocales(locales) {
				if (!locales) return [];
				return (typeof locales === "string" ? [locales] : Array.from(locales)).map((tag) => {
					const str = String(tag);
					if (str.length === 0) throw new RangeError("invalid language tag: \"\"");
					const parts = str.split("-");
					if (parts.length >= 1) parts[0] = parts[0].toLowerCase();
					if (parts.length >= 2) {
						if (parts[1].length === 4) parts[1] = parts[1][0].toUpperCase() + parts[1].slice(1).toLowerCase();
						else if (parts[1].length === 2) parts[1] = parts[1].toUpperCase();
					}
					if (parts.length >= 3 && parts[2].length === 2) parts[2] = parts[2].toUpperCase();
					return parts.join("-");
				});
			},
			writable: true,
			enumerable: false,
			configurable: true
		});
		if (typeof Intl.Locale !== "function") {
			class LocalePolyfill {
				constructor(tag) {
					_defineProperty(this, "_tag", void 0);
					_defineProperty(this, "_baseName", void 0);
					_defineProperty(this, "_parsed", void 0);
					if (typeof tag !== "string" || tag.trim().length === 0) throw new TypeError("Intl.Locale requires a non-empty locale tag");
					this._tag = canonicalizeLocaleTag(tag);
					this._baseName = this._tag.split("-u-")[0] ?? this._tag;
					this._parsed = parseLocaleTag(this._tag);
				}
				get baseName() {
					return this._baseName;
				}
				get language() {
					return this._parsed.language;
				}
				get region() {
					return this._parsed.region;
				}
				get script() {
					return this._parsed.script;
				}
				get calendar() {
					return parseUnicodeExtensionKeyword(this._tag, "ca");
				}
				get numberingSystem() {
					return parseUnicodeExtensionKeyword(this._tag, "nu");
				}
				get textInfo() {
					return { direction: directionForLocaleTag(this._tag) };
				}
				maximize() {
					console.warn("Intl.Locale.prototype.maximize() is not implemented in Exact core.");
					return this;
				}
				minimize() {
					console.warn("Intl.Locale.prototype.minimize() is not implemented in Exact core.");
					return this;
				}
				toString() {
					return this._tag;
				}
				toJSON() {
					return this._tag;
				}
			}
			Object.defineProperty(Intl, "Locale", {
				value: LocalePolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		} else if (Intl.Locale?.prototype && !("textInfo" in Intl.Locale.prototype)) Object.defineProperty(Intl.Locale.prototype, "textInfo", {
			get() {
				return { direction: directionForLocaleTag(String(this)) };
			},
			enumerable: false,
			configurable: true
		});
		if (typeof Intl.DateTimeFormat === "function") {
			const DTFProto = Intl.DateTimeFormat.prototype;
			if (typeof DTFProto.formatToParts !== "function") {
				Object.defineProperty(DTFProto, "formatToParts", {
					value: function formatToParts(date) {
						const d = date === void 0 ? /* @__PURE__ */ new Date() : new Date(date);
						const formatted = this.format(d);
						const opts = this.resolvedOptions ? this.resolvedOptions() : {};
						const locale = opts.locale || "en";
						const parts = [];
						const components = [];
						const tryFormat = (type, fmtOpts, priority) => {
							try {
								const val = new Intl.DateTimeFormat(locale, fmtOpts).format(d);
								if (val && val.length > 0) components.push({
									type,
									value: val,
									priority
								});
							} catch (_e) {}
						};
						if (opts.era) tryFormat("era", { era: opts.era }, 10);
						if (opts.year) tryFormat("year", { year: opts.year }, 20);
						if (opts.month) tryFormat("month", { month: opts.month }, 15);
						if (opts.day) tryFormat("day", { day: opts.day }, 30);
						if (opts.weekday) tryFormat("weekday", { weekday: opts.weekday }, 5);
						if (opts.hour) tryFormat("hour", {
							hour: opts.hour,
							hour12: opts.hour12
						}, 40);
						if (opts.minute) tryFormat("minute", { minute: opts.minute }, 50);
						if (opts.second) tryFormat("second", { second: opts.second }, 60);
						if (opts.dayPeriod || opts.hour12) tryFormat("dayPeriod", {
							hour: "numeric",
							hour12: true
						}, 45);
						if (opts.timeZoneName) tryFormat("timeZoneName", { timeZoneName: opts.timeZoneName }, 70);
						if (components.length === 0) {
							tryFormat("month", { month: "numeric" }, 15);
							tryFormat("day", { day: "numeric" }, 30);
							tryFormat("year", { year: "numeric" }, 20);
						}
						components.sort((a, b) => b.value.length - a.value.length || a.priority - b.priority);
						const assigned = new Array(formatted.length).fill(false);
						const charType = new Array(formatted.length).fill("");
						{
							const __exactForOfIterator104 = components[Symbol.iterator]();
							for (;;) {
								const __exactForOfStep104 = __exactForOfIterator104.next();
								if (__exactForOfStep104.done) break;
								const comp = __exactForOfStep104.value;
								const idx = findUnassigned(formatted, comp.value, assigned);
								if (idx >= 0) for (let ci = idx; ci < idx + comp.value.length; ci++) {
									assigned[ci] = true;
									charType[ci] = comp.type;
								}
							}
						}
						let currentType = "";
						let currentValue = "";
						for (let ci = 0; ci < formatted.length; ci++) {
							const t = charType[ci] || "literal";
							if (t !== currentType) {
								if (currentValue) parts.push({
									type: currentType || "literal",
									value: currentValue
								});
								currentType = t;
								currentValue = formatted[ci];
							} else currentValue += formatted[ci];
						}
						if (currentValue) parts.push({
							type: currentType || "literal",
							value: currentValue
						});
						if (parts.length === 0) return [{
							type: "literal",
							value: formatted
						}];
						return parts;
					},
					writable: true,
					enumerable: false,
					configurable: true
				});
				function findUnassigned(str, substr, assigned) {
					let startIdx = 0;
					while (startIdx <= str.length - substr.length) {
						const idx = str.indexOf(substr, startIdx);
						if (idx < 0) return -1;
						let allFree = true;
						for (let i = idx; i < idx + substr.length; i++) if (assigned[i]) {
							allFree = false;
							break;
						}
						if (allFree) return idx;
						startIdx = idx + 1;
					}
					return -1;
				}
			}
			if (typeof DTFProto.formatRange !== "function") Object.defineProperty(DTFProto, "formatRange", {
				value: function formatRange(startDate, endDate) {
					if (startDate === void 0 || endDate === void 0) throw new TypeError("formatRange requires two dates");
					const start = this.format(new Date(startDate));
					const end = this.format(new Date(endDate));
					if (start === end) return start;
					return `${start} \u2013 ${end}`;
				},
				writable: true,
				enumerable: false,
				configurable: true
			});
			if (typeof DTFProto.formatRangeToParts !== "function") Object.defineProperty(DTFProto, "formatRangeToParts", {
				value: function formatRangeToParts(startDate, endDate) {
					if (startDate === void 0 || endDate === void 0) throw new TypeError("formatRangeToParts requires two dates");
					const start = this.format(new Date(startDate));
					const end = this.format(new Date(endDate));
					if (start === end) return [{
						type: "literal",
						value: start,
						source: "shared"
					}];
					return [
						{
							type: "literal",
							value: start,
							source: "startRange"
						},
						{
							type: "literal",
							value: " – ",
							source: "shared"
						},
						{
							type: "literal",
							value: end,
							source: "endRange"
						}
					];
				},
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.NumberFormat === "function") {
			const NFProto = Intl.NumberFormat.prototype;
			if (typeof NFProto.formatToParts !== "function") {
				Object.defineProperty(NFProto, "formatToParts", {
					value: function formatToParts(value) {
						const num = value === void 0 ? NaN : Number(value);
						const formatted = this.format(num);
						if (isNaN(num)) return [{
							type: "nan",
							value: formatted
						}];
						if (!isFinite(num)) return [{
							type: "infinity",
							value: formatted
						}];
						const opts = this.resolvedOptions ? this.resolvedOptions() : {};
						const parts = [];
						const isNeg = num < 0 || Object.is && Object.is(num, -0);
						const absNum = Math.abs(num);
						let absFormatted;
						try {
							const absOpts = {};
							if (opts.minimumIntegerDigits) absOpts.minimumIntegerDigits = opts.minimumIntegerDigits;
							if (opts.minimumFractionDigits != null) absOpts.minimumFractionDigits = opts.minimumFractionDigits;
							if (opts.maximumFractionDigits != null) absOpts.maximumFractionDigits = opts.maximumFractionDigits;
							if (opts.minimumSignificantDigits) absOpts.minimumSignificantDigits = opts.minimumSignificantDigits;
							if (opts.maximumSignificantDigits) absOpts.maximumSignificantDigits = opts.maximumSignificantDigits;
							if (opts.useGrouping != null) absOpts.useGrouping = opts.useGrouping;
							absFormatted = new Intl.NumberFormat(opts.locale || "en", absOpts).format(absNum);
						} catch (_e) {
							absFormatted = String(absNum);
						}
						const style = opts.style || "decimal";
						if (style === "percent") return _parsePercentParts(formatted, isNeg);
						if (style === "currency") return _parseCurrencyParts(formatted, absFormatted, isNeg, opts);
						if (isNeg) {
							const minusIdx = formatted.indexOf("-") >= 0 ? formatted.indexOf("-") : formatted.indexOf("−");
							if (minusIdx >= 0) {
								const minusChar = formatted[minusIdx];
								const before = formatted.substring(0, minusIdx);
								const after = formatted.substring(minusIdx + minusChar.length);
								if (before.length > 0) parts.push({
									type: "literal",
									value: before
								});
								parts.push({
									type: "minusSign",
									value: minusChar
								});
								const numberStr = after.trim() || after;
								parts.push(..._parseDecimalParts(numberStr.length > 0 ? numberStr : absFormatted));
								return parts;
							}
						}
						return _parseDecimalParts(formatted);
					},
					writable: true,
					enumerable: false,
					configurable: true
				});
				/**
				* Parse a decimal number string (potentially with grouping) into parts.
				* Handles patterns like: "12,345.67" or "12.345,67" (European)
				*
				* Known edge case: For numbers with exactly 3 digits after the last
				* separator (e.g., "1.234"), the last separator is ambiguous -- it could
				* be a group separator (1234) or a decimal point (1.234). We treat it
				* as a decimal point (last separator = decimal), which is correct for
				* most locales when a single separator is present, but may be incorrect
				* for locales like German where "1.234" means one thousand two hundred
				* thirty-four. This is inherent to string-based decomposition without
				* knowing the locale's separator convention.
				*/
				function _parseDecimalParts(str) {
					const parts = [];
					const trimmed = str.trim();
					if (trimmed.length === 0) return [{
						type: "integer",
						value: "0"
					}];
					let decimalPos = -1;
					let decimalChar = "";
					for (let i = trimmed.length - 1; i >= 0; i--) {
						const ch = trimmed[i];
						if (ch === "." || ch === "," || ch === "·") {
							const afterSep = trimmed.substring(i + 1);
							if (/^\d+$/.test(afterSep)) {
								decimalPos = i;
								decimalChar = ch;
								break;
							}
						}
					}
					let integerPart;
					let fractionPart = null;
					if (decimalPos >= 0) {
						integerPart = trimmed.substring(0, decimalPos);
						fractionPart = trimmed.substring(decimalPos + 1);
					} else integerPart = trimmed;
					if (integerPart.length > 0) _parseIntegerWithGroups(integerPart, parts);
					else parts.push({
						type: "integer",
						value: "0"
					});
					if (fractionPart !== null) {
						parts.push({
							type: "decimal",
							value: decimalChar
						});
						parts.push({
							type: "fraction",
							value: fractionPart
						});
					}
					return parts;
				}
				/**
				* Parse integer part, identifying group separators.
				*/
				function _parseIntegerWithGroups(str, parts) {
					let current = "";
					for (let i = 0; i < str.length; i++) {
						const ch = str[i];
						if (/\d/.test(ch)) current += ch;
						else {
							if (current.length > 0) {
								parts.push({
									type: "integer",
									value: current
								});
								current = "";
							}
							parts.push({
								type: "group",
								value: ch
							});
						}
					}
					if (current.length > 0) parts.push({
						type: "integer",
						value: current
					});
				}
				/**
				* Parse a percent-formatted number string into parts.
				*/
				function _parsePercentParts(formatted, isNeg) {
					const parts = [];
					const percentIdx = formatted.indexOf("%");
					if (percentIdx < 0) return [{
						type: "literal",
						value: formatted
					}];
					let beforePercent = formatted.substring(0, percentIdx);
					let afterPercent = formatted.substring(percentIdx + 1);
					if (isNeg) {
						const minusIdx = beforePercent.indexOf("-") >= 0 ? beforePercent.indexOf("-") : beforePercent.indexOf("−");
						if (minusIdx >= 0) {
							const minusChar = beforePercent[minusIdx];
							const preSign = beforePercent.substring(0, minusIdx);
							const postSign = beforePercent.substring(minusIdx + 1);
							if (preSign.length > 0) parts.push({
								type: "literal",
								value: preSign
							});
							parts.push({
								type: "minusSign",
								value: minusChar
							});
							beforePercent = postSign;
						}
					}
					const numStr = beforePercent.trim();
					if (numStr.length > 0) parts.push(..._parseDecimalParts(numStr));
					if (beforePercent.length - beforePercent.trimEnd().length > 0) parts.push({
						type: "literal",
						value: beforePercent.substring(beforePercent.trimEnd().length)
					});
					parts.push({
						type: "percentSign",
						value: "%"
					});
					if (afterPercent.length > 0) parts.push({
						type: "literal",
						value: afterPercent
					});
					return parts;
				}
				/**
				* Parse a currency-formatted number string into parts.
				*/
				function _parseCurrencyParts(formatted, absFormatted, isNeg, opts) {
					const parts = [];
					const currencyCode = opts.currency || "USD";
					const currencyDisplay = opts.currencyDisplay || "symbol";
					const currencySymbols = {
						USD: "$",
						EUR: "€",
						GBP: "£",
						JPY: "¥",
						CNY: "¥",
						KRW: "₩",
						INR: "₹",
						BRL: "R$",
						CAD: "CA$",
						AUD: "A$",
						CHF: "CHF",
						MXN: "MX$",
						SEK: "kr",
						NOK: "kr",
						DKK: "kr",
						PLN: "zł",
						TRY: "₺",
						RUB: "₽",
						THB: "฿"
					};
					let currencyStr = "";
					if (currencyDisplay === "code") currencyStr = currencyCode;
					else if (currencyDisplay === "name") currencyStr = "";
					else currencyStr = currencySymbols[currencyCode.toUpperCase()] || currencyCode;
					let workStr = formatted;
					if (isNeg) {
						const minusIdx = workStr.indexOf("-") >= 0 ? workStr.indexOf("-") : workStr.indexOf("−");
						if (minusIdx >= 0) {
							const minusChar = workStr[minusIdx];
							if (minusIdx > 0) {
								const pre = workStr.substring(0, minusIdx);
								if (pre.trim() === currencyStr && currencyStr.length > 0) {
									parts.push({
										type: "currency",
										value: currencyStr
									});
									if (pre.length > currencyStr.length) parts.push({
										type: "literal",
										value: pre.substring(currencyStr.length)
									});
								} else if (pre.trim().length > 0) parts.push({
									type: "literal",
									value: pre
								});
							}
							parts.push({
								type: "minusSign",
								value: minusChar
							});
							workStr = workStr.substring(minusIdx + 1);
						}
					}
					if (currencyStr.length > 0) {
						const curIdx = workStr.indexOf(currencyStr);
						if (curIdx >= 0) {
							const before = workStr.substring(0, curIdx);
							const after = workStr.substring(curIdx + currencyStr.length);
							if (curIdx === 0) {
								parts.push({
									type: "currency",
									value: currencyStr
								});
								if (after.length > 0 && /^[\s\u00A0\u202F]/.test(after)) {
									parts.push({
										type: "literal",
										value: after[0]
									});
									const numStr = after.substring(1).trim();
									if (numStr.length > 0) parts.push(..._parseDecimalParts(numStr));
								} else {
									const numStr = after.trim();
									if (numStr.length > 0) parts.push(..._parseDecimalParts(numStr));
								}
							} else {
								const numStr = before.trim();
								if (numStr.length > 0) parts.push(..._parseDecimalParts(numStr));
								if (before.length > before.trimEnd().length) parts.push({
									type: "literal",
									value: before.substring(before.trimEnd().length)
								});
								parts.push({
									type: "currency",
									value: currencyStr
								});
								if (after.trim().length > 0) parts.push({
									type: "literal",
									value: after
								});
							}
						} else parts.push(..._parseDecimalParts(workStr.trim()));
					} else parts.push(..._parseDecimalParts(workStr.trim()));
					return parts.length > 0 ? parts : [{
						type: "literal",
						value: formatted
					}];
				}
			}
			if (typeof NFProto.formatRange !== "function") Object.defineProperty(NFProto, "formatRange", {
				value: function formatRange(start, end) {
					if (start === void 0 || end === void 0) throw new TypeError("formatRange requires two numbers");
					const startStr = this.format(Number(start));
					const endStr = this.format(Number(end));
					if (startStr === endStr) return startStr;
					return `${startStr}\u2013${endStr}`;
				},
				writable: true,
				enumerable: false,
				configurable: true
			});
			if (typeof NFProto.formatRangeToParts !== "function") Object.defineProperty(NFProto, "formatRangeToParts", {
				value: function formatRangeToParts(start, end) {
					if (start === void 0 || end === void 0) throw new TypeError("formatRangeToParts requires two numbers");
					const startStr = this.format(Number(start));
					const endStr = this.format(Number(end));
					if (startStr === endStr) return [{
						type: "literal",
						value: startStr,
						source: "shared"
					}];
					return [
						{
							type: "literal",
							value: startStr,
							source: "startRange"
						},
						{
							type: "literal",
							value: "–",
							source: "shared"
						},
						{
							type: "literal",
							value: endStr,
							source: "endRange"
						}
					];
				},
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.PluralRules !== "function") {
			/**
			* CLDR plural rule functions per locale.
			* Each function receives the absolute number and the type ('cardinal' | 'ordinal').
			* Returns the plural category: 'zero' | 'one' | 'two' | 'few' | 'many' | 'other'.
			*
			* Sources: https://www.unicode.org/cldr/charts/latest/supplemental/language_plural_rules.html
			*/
			const pluralRules = {
				en: (n, type) => {
					const i = Math.floor(Math.abs(n));
					if (type === "ordinal") {
						const mod10 = i % 10;
						const mod100 = i % 100;
						if (mod10 === 1 && mod100 !== 11) return "one";
						if (mod10 === 2 && mod100 !== 12) return "two";
						if (mod10 === 3 && mod100 !== 13) return "few";
						return "other";
					}
					if (i === 1 && n === 1) return "one";
					return "other";
				},
				fr: (n, type) => {
					const i = Math.floor(Math.abs(n));
					if (type === "ordinal") return i === 1 ? "one" : "other";
					return i === 0 || i === 1 ? "one" : "other";
				},
				de: (n, type) => {
					if (type === "ordinal") return "other";
					if (Math.floor(Math.abs(n)) === 1 && n === 1) return "one";
					return "other";
				},
				es: (n, type) => {
					if (type === "ordinal") return "other";
					return n === 1 ? "one" : "other";
				},
				pt: (n, type) => {
					if (type === "ordinal") return "other";
					const i = Math.floor(Math.abs(n));
					return i === 0 || i === 1 ? "one" : "other";
				},
				it: (n, type) => {
					const i = Math.floor(Math.abs(n));
					if (type === "ordinal") {
						if (i === 8 || i === 11 || i === 80 || i === 800) return "many";
						return "other";
					}
					if (i === 1 && n === 1) return "one";
					return "other";
				},
				ru: (n, type) => {
					if (type === "ordinal") return "other";
					const i = Math.floor(Math.abs(n));
					const mod10 = i % 10;
					const mod100 = i % 100;
					if (mod10 === 1 && mod100 !== 11) return "one";
					if (mod10 >= 2 && mod10 <= 4 && !(mod100 >= 12 && mod100 <= 14)) return "few";
					if (mod10 === 0 || mod10 >= 5 && mod10 <= 9 || mod100 >= 11 && mod100 <= 14) return "many";
					return "other";
				},
				pl: (n, type) => {
					if (type === "ordinal") return "other";
					const i = Math.floor(Math.abs(n));
					if (i === 1) return "one";
					const mod10 = i % 10;
					const mod100 = i % 100;
					if (mod10 >= 2 && mod10 <= 4 && !(mod100 >= 12 && mod100 <= 14)) return "few";
					if (mod10 === 0 || mod10 === 1 || mod10 >= 5 && mod10 <= 9 || mod100 >= 12 && mod100 <= 14) return "many";
					return "other";
				},
				ar: (n, type) => {
					if (type === "ordinal") return "other";
					const i = Math.floor(Math.abs(n));
					if (i === 0) return "zero";
					if (i === 1) return "one";
					if (i === 2) return "two";
					const mod100 = i % 100;
					if (mod100 >= 3 && mod100 <= 10) return "few";
					if (mod100 >= 11 && mod100 <= 99) return "many";
					return "other";
				},
				ja: (_n, _type) => "other",
				ko: (_n, _type) => "other",
				zh: (_n, _type) => "other",
				tr: (n, type) => {
					if (type === "ordinal") return "other";
					return n === 1 ? "one" : "other";
				},
				nl: (n, type) => {
					if (type === "ordinal") return "other";
					if (Math.floor(Math.abs(n)) === 1 && n === 1) return "one";
					return "other";
				},
				sv: (n, type) => {
					const i = Math.floor(Math.abs(n));
					if (type === "ordinal") {
						const mod10 = i % 10;
						const mod100 = i % 100;
						if ((mod10 === 1 || mod10 === 2) && mod100 !== 11 && mod100 !== 12) return "one";
						return "other";
					}
					if (i === 1 && n === 1) return "one";
					return "other";
				}
			};
			const pluralCategories = {
				en: {
					cardinal: ["one", "other"],
					ordinal: [
						"one",
						"two",
						"few",
						"other"
					]
				},
				fr: {
					cardinal: ["one", "other"],
					ordinal: ["one", "other"]
				},
				de: {
					cardinal: ["one", "other"],
					ordinal: ["other"]
				},
				es: {
					cardinal: ["one", "other"],
					ordinal: ["other"]
				},
				pt: {
					cardinal: ["one", "other"],
					ordinal: ["other"]
				},
				it: {
					cardinal: ["one", "other"],
					ordinal: ["many", "other"]
				},
				ru: {
					cardinal: [
						"one",
						"few",
						"many",
						"other"
					],
					ordinal: ["other"]
				},
				pl: {
					cardinal: [
						"one",
						"few",
						"many",
						"other"
					],
					ordinal: ["other"]
				},
				ar: {
					cardinal: [
						"zero",
						"one",
						"two",
						"few",
						"many",
						"other"
					],
					ordinal: ["other"]
				},
				ja: {
					cardinal: ["other"],
					ordinal: ["other"]
				},
				ko: {
					cardinal: ["other"],
					ordinal: ["other"]
				},
				zh: {
					cardinal: ["other"],
					ordinal: ["other"]
				},
				tr: {
					cardinal: ["one", "other"],
					ordinal: ["other"]
				},
				nl: {
					cardinal: ["one", "other"],
					ordinal: ["other"]
				},
				sv: {
					cardinal: ["one", "other"],
					ordinal: ["one", "other"]
				}
			};
			/**
			* Resolve the plural rule function for a given locale.
			* Falls back to the language subtag, then to English.
			*/
			function getPluralRule(locale) {
				const key = locale.toLowerCase();
				if (pluralRules[key]) return pluralRules[key];
				const lang = key.split("-")[0];
				if (pluralRules[lang]) return pluralRules[lang];
				return pluralRules.en;
			}
			function getPluralCategories(locale, type) {
				const key = locale.toLowerCase();
				const lang = key.split("-")[0];
				const catMap = pluralCategories[key] || pluralCategories[lang] || pluralCategories.en;
				return catMap[type] || catMap.cardinal || ["one", "other"];
			}
			class PluralRulesPolyfill {
				constructor(locales, options) {
					_defineProperty(this, "_locale", void 0);
					_defineProperty(this, "_type", void 0);
					_defineProperty(this, "_minimumIntegerDigits", void 0);
					_defineProperty(this, "_minimumFractionDigits", void 0);
					_defineProperty(this, "_maximumFractionDigits", void 0);
					_defineProperty(this, "_rule", void 0);
					const opts = options || {};
					this._locale = Array.isArray(locales) ? locales[0] || "en" : locales || "en";
					this._type = opts.type || "cardinal";
					this._minimumIntegerDigits = opts.minimumIntegerDigits || 1;
					this._minimumFractionDigits = opts.minimumFractionDigits || 0;
					this._maximumFractionDigits = opts.maximumFractionDigits || 3;
					this._rule = getPluralRule(this._locale);
				}
				select(n) {
					return this._rule(Number(n), this._type);
				}
				selectRange(start, end) {
					return this._rule(Number(end), this._type);
				}
				resolvedOptions() {
					return {
						locale: this._locale,
						type: this._type,
						minimumIntegerDigits: this._minimumIntegerDigits,
						minimumFractionDigits: this._minimumFractionDigits,
						maximumFractionDigits: this._maximumFractionDigits,
						pluralCategories: getPluralCategories(this._locale, this._type)
					};
				}
				static supportedLocalesOf(locales) {
					if (!locales) return [];
					return (Array.isArray(locales) ? locales : [locales]).filter((l) => typeof l === "string");
				}
			}
			Object.defineProperty(Intl, "PluralRules", {
				value: PluralRulesPolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.ListFormat !== "function") {
			class ListFormatPolyfill {
				constructor(locales, options) {
					_defineProperty(this, "_locale", void 0);
					_defineProperty(this, "_type", void 0);
					_defineProperty(this, "_style", void 0);
					const opts = options || {};
					this._locale = Array.isArray(locales) ? locales[0] || "en" : locales || "en";
					this._type = opts.type || "conjunction";
					this._style = opts.style || "long";
				}
				format(list) {
					const items = Array.from(list);
					if (items.length === 0) return "";
					if (items.length === 1) return String(items[0]);
					const conjunction = this._type === "disjunction" ? "or" : "and";
					if (this._type === "unit") {
						if (this._style === "narrow") return items.join(" ");
						return items.join(", ");
					}
					if (items.length === 2) {
						if (this._style === "narrow") return `${items[0]}, ${items[1]}`;
						return `${items[0]} ${conjunction} ${items[1]}`;
					}
					const last = items[items.length - 1];
					const rest = items.slice(0, -1);
					if (this._style === "narrow") return `${rest.join(", ")}, ${last}`;
					return `${rest.join(", ")}, ${conjunction} ${last}`;
				}
				formatToParts(list) {
					const items = Array.from(list);
					if (items.length === 0) return [];
					if (items.length === 1) return [{
						type: "element",
						value: String(items[0])
					}];
					const conjunction = this._type === "disjunction" ? "or" : "and";
					const parts = [];
					for (let i = 0; i < items.length; i++) {
						if (i > 0) if (i === items.length - 1) if (this._type === "unit") parts.push({
							type: "literal",
							value: this._style === "narrow" ? " " : ", "
						});
						else if (this._style === "narrow") parts.push({
							type: "literal",
							value: ", "
						});
						else if (items.length === 2) parts.push({
							type: "literal",
							value: ` ${conjunction} `
						});
						else parts.push({
							type: "literal",
							value: `, ${conjunction} `
						});
						else parts.push({
							type: "literal",
							value: ", "
						});
						parts.push({
							type: "element",
							value: String(items[i])
						});
					}
					return parts;
				}
				resolvedOptions() {
					return {
						locale: this._locale,
						type: this._type,
						style: this._style
					};
				}
				static supportedLocalesOf(locales) {
					if (!locales) return [];
					return (Array.isArray(locales) ? locales : [locales]).filter((l) => typeof l === "string");
				}
			}
			Object.defineProperty(Intl, "ListFormat", {
				value: ListFormatPolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.RelativeTimeFormat !== "function") {
			const UNITS_LONG = {
				year: ["year", "years"],
				quarter: ["quarter", "quarters"],
				month: ["month", "months"],
				week: ["week", "weeks"],
				day: ["day", "days"],
				hour: ["hour", "hours"],
				minute: ["minute", "minutes"],
				second: ["second", "seconds"]
			};
			const UNITS_SHORT = {
				year: "yr.",
				quarter: "qtr.",
				month: "mo.",
				week: "wk.",
				day: "day",
				hour: "hr.",
				minute: "min.",
				second: "sec."
			};
			const UNITS_NARROW = {
				year: "y",
				quarter: "q",
				month: "m",
				week: "w",
				day: "d",
				hour: "h",
				minute: "m",
				second: "s"
			};
			const VALID_UNITS = new Set([
				"year",
				"years",
				"quarter",
				"quarters",
				"month",
				"months",
				"week",
				"weeks",
				"day",
				"days",
				"hour",
				"hours",
				"minute",
				"minutes",
				"second",
				"seconds"
			]);
			function normalizeUnit(unit) {
				if (!VALID_UNITS.has(unit)) throw new RangeError(`Invalid unit argument for RelativeTimeFormat: ${unit}`);
				return unit.replace(/s$/, "");
			}
			class RelativeTimeFormatPolyfill {
				constructor(locales, options) {
					_defineProperty(this, "_locale", void 0);
					_defineProperty(this, "_style", void 0);
					_defineProperty(this, "_numeric", void 0);
					const opts = options || {};
					this._locale = Array.isArray(locales) ? locales[0] || "en" : locales || "en";
					this._style = opts.style || "long";
					this._numeric = opts.numeric || "always";
				}
				format(value, unit) {
					const normalizedUnit = normalizeUnit(unit);
					const n = Number(value);
					if (this._numeric === "auto") {
						if (normalizedUnit === "day") {
							if (n === -1) return "yesterday";
							if (n === 0) return "today";
							if (n === 1) return "tomorrow";
						}
						if (n === 0) return `this ${normalizedUnit}`;
						if (n === -1) return `last ${normalizedUnit}`;
						if (n === 1) return `next ${normalizedUnit}`;
					}
					const abs = Math.abs(n);
					let unitStr;
					if (this._style === "narrow") {
						unitStr = UNITS_NARROW[normalizedUnit] || normalizedUnit;
						if (n < 0) return `${abs}${unitStr} ago`;
						return `in ${abs}${unitStr}`;
					}
					if (this._style === "short") unitStr = UNITS_SHORT[normalizedUnit] || normalizedUnit;
					else {
						const names = UNITS_LONG[normalizedUnit];
						unitStr = names ? abs === 1 ? names[0] : names[1] : normalizedUnit;
					}
					if (n < 0) return `${abs} ${unitStr} ago`;
					return `in ${abs} ${unitStr}`;
				}
				formatToParts(value, unit) {
					const formatted = this.format(value, unit);
					const normalizedUnit = normalizeUnit(unit);
					const n = Number(value);
					if (n < 0) return [
						{
							type: "integer",
							value: String(Math.abs(n)),
							unit: normalizedUnit
						},
						{
							type: "literal",
							value: " "
						},
						{
							type: "literal",
							value: formatted.replace(/^\d+\s*/, "").replace(/^\s+/, "")
						}
					];
					return [{
						type: "literal",
						value: formatted
					}];
				}
				resolvedOptions() {
					return {
						locale: this._locale,
						style: this._style,
						numeric: this._numeric,
						numberingSystem: "latn"
					};
				}
				static supportedLocalesOf(locales) {
					if (!locales) return [];
					return (Array.isArray(locales) ? locales : [locales]).filter((l) => typeof l === "string");
				}
			}
			Object.defineProperty(Intl, "RelativeTimeFormat", {
				value: RelativeTimeFormatPolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.Segmenter !== "function") {
			let _Symbol$iterator;
			const WORD_REGEX = /[^\s]+|\s+/g;
			const SENTENCE_REGEX = /[^.!?]+[.!?]+\s*|[^.!?]+$/g;
			const ZWJ = 8205;
			const RI_START = 127462;
			const RI_END = 127487;
			const SKIN_TONE_START = 127995;
			const SKIN_TONE_END = 127999;
			const KEYCAP = 8419;
			const TAG_START = 917536;
			const TAG_END = 917630;
			const CANCEL_TAG = 917631;
			/**
			* Get the code point at position i in the string.
			* Returns [codePoint, charCount] where charCount is 1 for BMP, 2 for surrogate pairs.
			*/
			function codePointAt(str, i) {
				const code = str.charCodeAt(i);
				if (code >= 55296 && code <= 56319 && i + 1 < str.length) {
					const next = str.charCodeAt(i + 1);
					if (next >= 56320 && next <= 57343) return [(code - 55296 << 10) + (next - 56320) + 65536, 2];
				}
				return [code, 1];
			}
			/**
			* Check if a code point is a regional indicator symbol.
			*/
			function isRegionalIndicator(cp) {
				return cp >= RI_START && cp <= RI_END;
			}
			/**
			* Check if a code point is a combining mark (Unicode categories Mn, Mc, Me).
			* This is a simplified check covering the most common ranges.
			*/
			function isCombiningMark(cp) {
				if (cp >= 768 && cp <= 879) return true;
				if (cp >= 6832 && cp <= 6911) return true;
				if (cp >= 7616 && cp <= 7679) return true;
				if (cp >= 8400 && cp <= 8447) return true;
				if (cp >= 65056 && cp <= 65071) return true;
				if (cp >= 1155 && cp <= 1161) return true;
				if (cp >= 1425 && cp <= 1469) return true;
				if (cp === 1471 || cp === 1473 || cp === 1474 || cp === 1476 || cp === 1477 || cp === 1479) return true;
				if (cp >= 1552 && cp <= 1562) return true;
				if (cp >= 1611 && cp <= 1631) return true;
				if (cp === 1648) return true;
				if (cp >= 1750 && cp <= 1756) return true;
				if (cp >= 1759 && cp <= 1764) return true;
				if (cp >= 1767 && cp <= 1768) return true;
				if (cp >= 1770 && cp <= 1773) return true;
				if (cp >= 2304 && cp <= 2307) return true;
				if (cp >= 2362 && cp <= 2383) return true;
				if (cp >= 2385 && cp <= 2391) return true;
				if (cp >= 2402 && cp <= 2403) return true;
				return false;
			}
			/**
			* Check if a code point is a skin tone modifier.
			*/
			function isSkinToneModifier(cp) {
				return cp >= SKIN_TONE_START && cp <= SKIN_TONE_END;
			}
			/**
			* Check if a code point is a variation selector (VS1-VS16 or VS17-VS256).
			*/
			function isVariationSelector(cp) {
				if (cp >= 65024 && cp <= 65039) return true;
				if (cp >= 917760 && cp <= 917999) return true;
				return false;
			}
			/**
			* Check if a code point is a tag character.
			*/
			function isTagCharacter(cp) {
				return cp >= TAG_START && cp <= TAG_END || cp === CANCEL_TAG;
			}
			class SegmenterPolyfill {
				constructor(locales, options) {
					_defineProperty(this, "_locale", void 0);
					_defineProperty(this, "_granularity", void 0);
					const opts = options || {};
					this._locale = Array.isArray(locales) ? locales[0] || "en" : locales || "en";
					this._granularity = opts.granularity || "grapheme";
					if (![
						"grapheme",
						"word",
						"sentence"
					].includes(this._granularity)) throw new RangeError(`Invalid granularity: ${this._granularity}`);
				}
				segment(input) {
					return new SegmentsPolyfill(String(input), this._granularity);
				}
				resolvedOptions() {
					return {
						locale: this._locale,
						granularity: this._granularity
					};
				}
				static supportedLocalesOf(locales) {
					if (!locales) return [];
					return (Array.isArray(locales) ? locales : [locales]).filter((l) => typeof l === "string");
				}
			}
			_Symbol$iterator = Symbol.iterator;
			class SegmentsPolyfill {
				constructor(input, granularity) {
					_defineProperty(this, "_input", void 0);
					_defineProperty(this, "_granularity", void 0);
					this._input = input;
					this._granularity = granularity;
				}
				containing(index) {
					if (index < 0 || index >= this._input.length) return void 0;
					const segments = this._getSegments();
					for (const seg of segments) if (index >= seg.index && index < seg.index + seg.segment.length) return seg;
				}
				[_Symbol$iterator]() {
					const segments = this._getSegments();
					let i = 0;
					return { next() {
						if (i >= segments.length) return {
							value: void 0,
							done: true
						};
						return {
							value: segments[i++],
							done: false
						};
					} };
				}
				_getSegments() {
					const input = this._input;
					const results = [];
					if (this._granularity === "grapheme") {
						let i = 0;
						while (i < input.length) {
							const startIdx = i;
							const [cp, cpLen] = codePointAt(input, i);
							i += cpLen;
							if (isRegionalIndicator(cp)) {
								if (i < input.length) {
									const [nextCp, nextLen] = codePointAt(input, i);
									if (isRegionalIndicator(nextCp)) i += nextLen;
								}
								while (i < input.length) {
									const [nextCp, nextLen] = codePointAt(input, i);
									if (isVariationSelector(nextCp)) i += nextLen;
									else break;
								}
								results.push({
									segment: input.substring(startIdx, i),
									index: startIdx,
									input
								});
								continue;
							}
							while (i < input.length) {
								const [nextCp, nextLen] = codePointAt(input, i);
								if (isCombiningMark(nextCp)) {
									i += nextLen;
									continue;
								}
								if (isVariationSelector(nextCp)) {
									i += nextLen;
									continue;
								}
								if (isSkinToneModifier(nextCp)) {
									i += nextLen;
									continue;
								}
								if (nextCp === KEYCAP) {
									i += nextLen;
									continue;
								}
								if (isTagCharacter(nextCp)) {
									i += nextLen;
									while (i < input.length) {
										const [tagCp, tagLen] = codePointAt(input, i);
										if (isTagCharacter(tagCp)) {
											i += tagLen;
											if (tagCp === CANCEL_TAG) break;
										} else break;
									}
									continue;
								}
								if (nextCp === ZWJ) {
									i += nextLen;
									if (i < input.length) {
										const [joinedCp, joinedLen] = codePointAt(input, i);
										i += joinedLen;
									}
									continue;
								}
								break;
							}
							if (cp === 13 && i < input.length && input.charCodeAt(i) === 10) {}
							results.push({
								segment: input.substring(startIdx, i),
								index: startIdx,
								input
							});
						}
					} else if (this._granularity === "word") {
						let match;
						const regex = new RegExp(WORD_REGEX.source, "g");
						while ((match = regex.exec(input)) !== null) {
							const segment = match[0];
							const isWordLike = !/^\s+$/.test(segment);
							results.push({
								segment,
								index: match.index,
								input,
								isWordLike
							});
						}
					} else if (this._granularity === "sentence") {
						if (input.length === 0) return results;
						let match;
						const regex = new RegExp(SENTENCE_REGEX.source, "g");
						while ((match = regex.exec(input)) !== null) results.push({
							segment: match[0],
							index: match.index,
							input
						});
						if (results.length === 0) results.push({
							segment: input,
							index: 0,
							input
						});
					}
					return results;
				}
			}
			Object.defineProperty(Intl, "Segmenter", {
				value: SegmenterPolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.DisplayNames !== "function") {
			const LANGUAGE_NAMES = {
				en: "English",
				es: "Spanish",
				fr: "French",
				de: "German",
				it: "Italian",
				pt: "Portuguese",
				ja: "Japanese",
				ko: "Korean",
				zh: "Chinese",
				ar: "Arabic",
				hi: "Hindi",
				ru: "Russian",
				nl: "Dutch",
				sv: "Swedish",
				pl: "Polish",
				tr: "Turkish",
				th: "Thai",
				vi: "Vietnamese"
			};
			const REGION_NAMES = {
				US: "United States",
				GB: "United Kingdom",
				CA: "Canada",
				AU: "Australia",
				DE: "Germany",
				FR: "France",
				JP: "Japan",
				CN: "China",
				IN: "India",
				BR: "Brazil",
				MX: "Mexico",
				ES: "Spain",
				IT: "Italy",
				KR: "South Korea",
				RU: "Russia",
				NL: "Netherlands",
				SE: "Sweden",
				NO: "Norway",
				DK: "Denmark"
			};
			const CURRENCY_NAMES = {
				USD: "US Dollar",
				EUR: "Euro",
				GBP: "British Pound",
				JPY: "Japanese Yen",
				CNY: "Chinese Yuan",
				KRW: "South Korean Won",
				INR: "Indian Rupee",
				BRL: "Brazilian Real",
				CAD: "Canadian Dollar",
				AUD: "Australian Dollar",
				CHF: "Swiss Franc",
				MXN: "Mexican Peso",
				SEK: "Swedish Krona"
			};
			class DisplayNamesPolyfill {
				constructor(locales, options) {
					_defineProperty(this, "_locale", void 0);
					_defineProperty(this, "_type", void 0);
					_defineProperty(this, "_style", void 0);
					_defineProperty(this, "_fallback", void 0);
					const opts = options || {};
					if (!opts.type) throw new TypeError("DisplayNames requires a \"type\" option");
					this._locale = Array.isArray(locales) ? locales[0] || "en" : locales || "en";
					this._type = opts.type;
					this._style = opts.style || "long";
					this._fallback = opts.fallback || "code";
					if (![
						"language",
						"region",
						"script",
						"currency",
						"calendar",
						"dateTimeField"
					].includes(this._type)) throw new RangeError(`Invalid type: ${this._type}`);
				}
				of(code) {
					if (typeof code !== "string" || code.length === 0) throw new RangeError("Invalid code for DisplayNames");
					let result;
					switch (this._type) {
						case "language":
							result = LANGUAGE_NAMES[code.toLowerCase()] || LANGUAGE_NAMES[code.split("-")[0].toLowerCase()];
							break;
						case "region":
							result = REGION_NAMES[code.toUpperCase()];
							break;
						case "currency":
							result = CURRENCY_NAMES[code.toUpperCase()];
							break;
						default: result = void 0;
					}
					if (result === void 0) {
						if (this._fallback === "code") return code;
						return;
					}
					return result;
				}
				resolvedOptions() {
					return {
						locale: this._locale,
						type: this._type,
						style: this._style,
						fallback: this._fallback
					};
				}
				static supportedLocalesOf(locales) {
					if (!locales) return [];
					return (Array.isArray(locales) ? locales : [locales]).filter((l) => typeof l === "string");
				}
			}
			Object.defineProperty(Intl, "DisplayNames", {
				value: DisplayNamesPolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.Collator !== "function") {
			class CollatorPolyfill {
				constructor(locales, options) {
					_defineProperty(this, "_locale", void 0);
					_defineProperty(this, "_usage", void 0);
					_defineProperty(this, "_sensitivity", void 0);
					_defineProperty(this, "_ignorePunctuation", void 0);
					_defineProperty(this, "_numeric", void 0);
					_defineProperty(this, "_caseFirst", void 0);
					_defineProperty(this, "_collation", void 0);
					const opts = options || {};
					this._locale = Array.isArray(locales) ? locales[0] || "en" : locales || "en";
					this._usage = opts.usage || "sort";
					this._sensitivity = opts.sensitivity || "variant";
					this._ignorePunctuation = opts.ignorePunctuation || false;
					this._numeric = opts.numeric || false;
					this._caseFirst = opts.caseFirst || "false";
					this._collation = opts.collation || "default";
				}
				compare(a, b) {
					try {
						return String(a).localeCompare(String(b), this._locale, {
							sensitivity: this._sensitivity,
							ignorePunctuation: this._ignorePunctuation,
							numeric: this._numeric,
							caseFirst: this._caseFirst
						});
					} catch (_e) {
						return String(a).localeCompare(String(b));
					}
				}
				resolvedOptions() {
					return {
						locale: this._locale,
						usage: this._usage,
						sensitivity: this._sensitivity,
						ignorePunctuation: this._ignorePunctuation,
						numeric: this._numeric,
						caseFirst: this._caseFirst,
						collation: this._collation
					};
				}
				static supportedLocalesOf(locales) {
					if (!locales) return [];
					return (Array.isArray(locales) ? locales : [locales]).filter((l) => typeof l === "string");
				}
			}
			Object.defineProperty(Intl, "Collator", {
				value: CollatorPolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
		if (typeof Intl.DurationFormat !== "function") {
			const DURATION_FIELDS = [
				{
					field: "years",
					long: ["year", "years"],
					short: "yr",
					narrow: "y"
				},
				{
					field: "months",
					long: ["month", "months"],
					short: "mo",
					narrow: "mo"
				},
				{
					field: "weeks",
					long: ["week", "weeks"],
					short: "wk",
					narrow: "w"
				},
				{
					field: "days",
					long: ["day", "days"],
					short: "day",
					narrow: "d"
				},
				{
					field: "hours",
					long: ["hour", "hours"],
					short: "hr",
					narrow: "h"
				},
				{
					field: "minutes",
					long: ["minute", "minutes"],
					short: "min",
					narrow: "m"
				},
				{
					field: "seconds",
					long: ["second", "seconds"],
					short: "sec",
					narrow: "s"
				},
				{
					field: "milliseconds",
					long: ["millisecond", "milliseconds"],
					short: "ms",
					narrow: "ms"
				},
				{
					field: "microseconds",
					long: ["microsecond", "microseconds"],
					short: "µs",
					narrow: "µs"
				},
				{
					field: "nanoseconds",
					long: ["nanosecond", "nanoseconds"],
					short: "ns",
					narrow: "ns"
				}
			];
			const TIME_FIELDS = new Set([
				"hours",
				"minutes",
				"seconds"
			]);
			const SUB_SECOND_FIELDS = new Set([
				"milliseconds",
				"microseconds",
				"nanoseconds"
			]);
			class DurationFormatPolyfill {
				constructor(locales, options) {
					_defineProperty(this, "_locale", void 0);
					_defineProperty(this, "_style", void 0);
					const opts = options || {};
					this._locale = Array.isArray(locales) ? locales[0] || "en" : locales || "en";
					this._style = opts.style || "short";
					if (![
						"long",
						"short",
						"narrow",
						"digital"
					].includes(this._style)) throw new RangeError(`Invalid style: ${this._style}`);
				}
				format(duration) {
					if (duration === null || duration === void 0 || typeof duration !== "object") throw new TypeError("Duration must be an object");
					return this._buildParts(duration).map((p) => p.value).join("");
				}
				formatToParts(duration) {
					if (duration === null || duration === void 0 || typeof duration !== "object") throw new TypeError("Duration must be an object");
					return this._buildParts(duration);
				}
				resolvedOptions() {
					return {
						locale: this._locale,
						style: this._style,
						numberingSystem: "latn"
					};
				}
				static supportedLocalesOf(locales) {
					if (!locales) return [];
					return (Array.isArray(locales) ? locales : [locales]).filter((l) => typeof l === "string");
				}
				_buildParts(duration) {
					if (this._style === "digital") return this._buildDigitalParts(duration);
					return this._buildTextParts(duration);
				}
				/**
				* Build parts for text styles: long, short, narrow.
				*/
				_buildTextParts(duration) {
					const parts = [];
					const segments = [];
					for (const def of DURATION_FIELDS) {
						const val = Number(duration[def.field]) || 0;
						if (val === 0) continue;
						let unitStr;
						if (this._style === "long") unitStr = val === 1 ? def.long[0] : def.long[1];
						else if (this._style === "short") unitStr = def.short;
						else unitStr = def.narrow;
						let formatted;
						if (this._style === "narrow") formatted = `${val}${unitStr}`;
						else formatted = `${val} ${unitStr}`;
						segments.push({
							type: def.field,
							value: val,
							formatted
						});
					}
					for (let i = 0; i < segments.length; i++) {
						if (i > 0) if (this._style === "narrow") parts.push({
							type: "literal",
							value: " "
						});
						else parts.push({
							type: "literal",
							value: ", "
						});
						const seg = segments[i];
						parts.push({
							type: "integer",
							value: String(seg.value)
						});
						if (this._style === "narrow") parts.push({
							type: "unit",
							value: seg.formatted.substring(String(seg.value).length)
						});
						else {
							parts.push({
								type: "literal",
								value: " "
							});
							parts.push({
								type: "unit",
								value: seg.formatted.split(" ").slice(1).join(" ")
							});
						}
					}
					return parts;
				}
				/**
				* Build parts for digital style: "1:02:03"
				* Date fields (years, months, weeks, days) are formatted as text (short).
				* Time fields (hours, minutes, seconds) are formatted as HH:MM:SS.
				* Sub-second fields are appended after seconds with a decimal point.
				*/
				_buildDigitalParts(duration) {
					const parts = [];
					const dateSegments = [];
					for (const def of DURATION_FIELDS) {
						if (TIME_FIELDS.has(def.field) || SUB_SECOND_FIELDS.has(def.field)) continue;
						const val = Number(duration[def.field]) || 0;
						if (val === 0) continue;
						dateSegments.push(`${val} ${def.short}`);
					}
					if (dateSegments.length > 0) parts.push({
						type: "literal",
						value: dateSegments.join(", ")
					});
					const hours = Number(duration.hours) || 0;
					const minutes = Number(duration.minutes) || 0;
					const seconds = Number(duration.seconds) || 0;
					const milliseconds = Number(duration.milliseconds) || 0;
					const microseconds = Number(duration.microseconds) || 0;
					const nanoseconds = Number(duration.nanoseconds) || 0;
					const hasTimePart = hours > 0 || minutes > 0 || seconds > 0 || milliseconds > 0 || microseconds > 0 || nanoseconds > 0;
					if (!hasTimePart && parts.length > 0) return parts;
					if (!hasTimePart && parts.length === 0) {
						parts.push({
							type: "integer",
							value: "0"
						});
						parts.push({
							type: "literal",
							value: ":"
						});
						parts.push({
							type: "integer",
							value: "00"
						});
						parts.push({
							type: "literal",
							value: ":"
						});
						parts.push({
							type: "integer",
							value: "00"
						});
						return parts;
					}
					if (dateSegments.length > 0) parts.push({
						type: "literal",
						value: ", "
					});
					const pad2 = (n) => n < 10 ? `0${n}` : String(n);
					parts.push({
						type: "integer",
						value: String(hours)
					});
					parts.push({
						type: "literal",
						value: ":"
					});
					parts.push({
						type: "integer",
						value: pad2(minutes)
					});
					parts.push({
						type: "literal",
						value: ":"
					});
					parts.push({
						type: "integer",
						value: pad2(seconds)
					});
					if (milliseconds > 0 || microseconds > 0 || nanoseconds > 0) {
						const totalNanos = milliseconds * 1e6 + microseconds * 1e3 + nanoseconds;
						const fracStr = String(totalNanos).padStart(9, "0").replace(/0+$/, "");
						parts.push({
							type: "literal",
							value: "."
						});
						parts.push({
							type: "fraction",
							value: fracStr
						});
					}
					return parts;
				}
			}
			Object.defineProperty(Intl, "DurationFormat", {
				value: DurationFormatPolyfill,
				writable: true,
				enumerable: false,
				configurable: true
			});
		}
	}
	//#endregion
	//#region ../ibex-runtime-js/src/polyfills/index.ts
	/**
	* ES2023+ Polyfills for Hermes Engine
	*
	* Installs all modern JavaScript polyfills that Hermes doesn't natively support.
	* Each polyfill checks for existing implementations before installing.
	*
	* Covers:
	* - Array: toSorted, toReversed, with, toSpliced, fromAsync
	* - Object/Map: groupBy
	* - Set: union, intersection, difference, symmetricDifference, isSubsetOf, isSupersetOf, isDisjointFrom
	* - Promise: withResolvers
	* - String: isWellFormed, toWellFormed
	* - ArrayBuffer: transfer, transferToFixedLength
	* - Iterator: map, filter, take, drop, flatMap, reduce, toArray, forEach, some, every, find, from
	* - Intl: PluralRules, ListFormat, RelativeTimeFormat, Segmenter, DisplayNames,
	*         DateTimeFormat.formatRange/formatToParts, NumberFormat.formatRange/formatToParts
	*/
	/**
	* Install all ES2023+ polyfills.
	* Safe to call multiple times; each polyfill checks before installing.
	*/
	function installPolyfills() {
		installArrayPolyfills();
		installGroupByPolyfills();
		installSetPolyfills();
		installPromisePolyfills();
		installStringPolyfills();
		installArrayBufferPolyfills();
		installTypedArrayPolyfills();
		installIteratorPolyfills();
		const g = globalThis;
		if (typeof g.Intl === "object" && g.Intl !== null) {
			const realIntl = g.Intl;
			let installed = false;
			function ensureIntl() {
				if (installed) return;
				installed = true;
				g.Intl = realIntl;
				installIntlPolyfills();
			}
			g.Intl = new Proxy(realIntl, {
				get(target, prop, receiver) {
					ensureIntl();
					return Reflect.get(g.Intl, prop, receiver);
				},
				has(target, prop) {
					ensureIntl();
					return Reflect.has(g.Intl, prop);
				}
			});
		} else installIntlPolyfills();
	}
	//#endregion
	//#region ../ibex-runtime-js/src/fs/ExactFile.ts
	init_defineProperty();
	const MIME_TYPES = {
		".txt": "text/plain;charset=utf-8",
		".html": "text/html;charset=utf-8",
		".htm": "text/html;charset=utf-8",
		".css": "text/css;charset=utf-8",
		".js": "text/javascript;charset=utf-8",
		".mjs": "text/javascript;charset=utf-8",
		".cjs": "text/javascript;charset=utf-8",
		".ts": "text/typescript;charset=utf-8",
		".tsx": "text/typescript;charset=utf-8",
		".jsx": "text/javascript;charset=utf-8",
		".json": "application/json;charset=utf-8",
		".jsonl": "application/x-ndjson;charset=utf-8",
		".xml": "application/xml;charset=utf-8",
		".svg": "image/svg+xml;charset=utf-8",
		".png": "image/png",
		".jpg": "image/jpeg",
		".jpeg": "image/jpeg",
		".gif": "image/gif",
		".webp": "image/webp",
		".avif": "image/avif",
		".ico": "image/x-icon",
		".bmp": "image/bmp",
		".tiff": "image/tiff",
		".tif": "image/tiff",
		".pdf": "application/pdf",
		".zip": "application/zip",
		".gz": "application/gzip",
		".tar": "application/x-tar",
		".wasm": "application/wasm",
		".mp3": "audio/mpeg",
		".mp4": "video/mp4",
		".webm": "video/webm",
		".ogg": "audio/ogg",
		".wav": "audio/wav",
		".woff": "font/woff",
		".woff2": "font/woff2",
		".ttf": "font/ttf",
		".otf": "font/otf",
		".md": "text/markdown;charset=utf-8",
		".yaml": "text/yaml;charset=utf-8",
		".yml": "text/yaml;charset=utf-8",
		".toml": "text/toml;charset=utf-8",
		".csv": "text/csv;charset=utf-8",
		".sh": "application/x-sh",
		".sql": "application/sql",
		".wgsl": "text/wgsl;charset=utf-8",
		".glsl": "text/plain;charset=utf-8"
	};
	function getMimeType(path) {
		const dot = path.lastIndexOf(".");
		if (dot === -1) return "application/octet-stream";
		return MIME_TYPES[path.slice(dot).toLowerCase()] || "application/octet-stream";
	}
	function decodeBytes(bytes) {
		if (typeof TextDecoder !== "undefined") {
			const decoded = new TextDecoder("utf-8").decode(bytes);
			if (bytes.length >= 3 && bytes[0] === 239 && bytes[1] === 187 && bytes[2] === 191 && decoded.charCodeAt(0) !== 65279) return `\uFEFF${decoded}`;
			return decoded;
		}
		let result = "";
		for (let i = 0; i < bytes.length; i++) result += String.fromCharCode(bytes[i]);
		return result;
	}
	function toUint8Array(data) {
		if (typeof data === "string") {
			if (typeof TextEncoder !== "undefined") return new TextEncoder().encode(data);
			const buf = new Uint8Array(data.length * 3);
			let offset = 0;
			for (let i = 0; i < data.length; i++) {
				const code = data.charCodeAt(i);
				if (code < 128) buf[offset++] = code;
				else if (code < 2048) {
					buf[offset++] = 192 | code >> 6;
					buf[offset++] = 128 | code & 63;
				} else {
					buf[offset++] = 224 | code >> 12;
					buf[offset++] = 128 | code >> 6 & 63;
					buf[offset++] = 128 | code & 63;
				}
			}
			return buf.slice(0, offset);
		}
		if (data instanceof Uint8Array) return data;
		if (data instanceof ArrayBuffer) return new Uint8Array(data);
		if (ArrayBuffer.isView(data)) return new Uint8Array(data.buffer, data.byteOffset, data.byteLength);
		return new Uint8Array(data);
	}
	let _exactFsInitialized = false;
	function ensureExactFs() {
		if (_exactFsInitialized) return;
		const g = globalThis;
		if (!(typeof g.__exactReadFile === "function" && typeof g.__exactWriteFile === "function" && typeof g.__exactStat === "function" && typeof g.__exactAccess === "function") && typeof g.__exactEnsureFs === "function") g.__exactEnsureFs();
		_exactFsInitialized = typeof g.__exactReadFile === "function" || typeof g.__exactWriteFile === "function" || typeof g.__exactStat === "function" || typeof g.__exactAccess === "function";
	}
	function normalizeExactFilePath(path) {
		if (typeof path === "string") return path;
		if (path && typeof path === "object") {
			if (typeof path.pathname === "string" && path.pathname) return decodeURIComponent(path.pathname);
			if (typeof path.href === "string" && path.href.startsWith("file:")) return decodeURIComponent(path.href.replace(/^file:\/\//, ""));
		}
		return String(path);
	}
	/**
	* Lazy file reference compatible with Bun.file().
	*
	* Properties:
	* - name: string (the file path)
	* - size: number (file size in bytes, 0 if not found)
	* - type: string (MIME type inferred from extension)
	* - lastModified: number (mtime in ms since epoch)
	*
	* Methods:
	* - text(): Promise<string>
	* - json(): Promise<any>
	* - arrayBuffer(): Promise<ArrayBuffer>
	* - bytes(): Promise<Uint8Array>
	* - slice(begin?, end?): Blob
	* - exists(): Promise<boolean>
	* - stat(): Promise<{size, mtime_ms, ...} | null>
	*/
	var ExactFile = class {
		constructor(path, options) {
			_defineProperty(this, "name", void 0);
			_defineProperty(this, "type", void 0);
			const normalizedPath = normalizeExactFilePath(path);
			this.name = normalizedPath;
			this.type = options?.type || getMimeType(normalizedPath);
		}
		get size() {
			ensureExactFs();
			try {
				const json = __exactStat(this.name);
				return JSON.parse(json).size;
			} catch {
				return 0;
			}
		}
		get lastModified() {
			ensureExactFs();
			try {
				const json = __exactStat(this.name);
				return JSON.parse(json).mtime_ms;
			} catch {
				return 0;
			}
		}
		async text() {
			ensureExactFs();
			return decodeBytes(__exactReadFile(this.name));
		}
		async json() {
			const text = await this.text();
			return JSON.parse(text);
		}
		async arrayBuffer() {
			ensureExactFs();
			const bytes = __exactReadFile(this.name);
			return bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
		}
		async bytes() {
			ensureExactFs();
			return __exactReadFile(this.name);
		}
		_getBytes() {
			ensureExactFs();
			return __exactReadFile(this.name);
		}
		async exists() {
			ensureExactFs();
			try {
				__exactAccess(this.name, 0);
				return true;
			} catch {
				return false;
			}
		}
		async stat() {
			ensureExactFs();
			try {
				const json = __exactStat(this.name);
				return JSON.parse(json);
			} catch {
				return null;
			}
		}
		slice(begin, end, contentType) {
			ensureExactFs();
			const bytes = __exactReadFile(this.name);
			const sliced = bytes.slice(begin ?? 0, end ?? bytes.length);
			return new Blob([sliced], { type: contentType || this.type });
		}
		stream() {
			ensureExactFs();
			const name = this.name;
			return new ReadableStream({ start(controller) {
				try {
					const bytes = __exactReadFile(name);
					controller.enqueue(bytes);
					controller.close();
				} catch (err) {
					controller.error(err);
				}
			} });
		}
		writer() {
			const name = this.name;
			let started = false;
			return {
				write(data) {
					ensureExactFs();
					const bytes = toUint8Array(data);
					if (!started) {
						__exactWriteFile(name, bytes);
						started = true;
					} else {
						const g = globalThis;
						if (typeof g.__exactAppendFile === "function") g.__exactAppendFile(name, bytes);
						else {
							const existing = __exactReadFile(name);
							const combined = new Uint8Array(existing.length + bytes.length);
							combined.set(existing);
							combined.set(bytes, existing.length);
							__exactWriteFile(name, combined);
						}
					}
					return bytes.length;
				},
				end() {},
				flush() {}
			};
		}
		toString() {
			return `ExactFile("${this.name}")`;
		}
		toJSON() {
			return {
				name: this.name,
				type: this.type,
				size: this.size,
				lastModified: this.lastModified
			};
		}
	};
	/**
	* Exact.write(dest, data) - Write data to a file.
	* Compatible with Bun.write().
	*
	* @param dest - File path or ExactFile object
	* @param data - String, Uint8Array, ArrayBuffer, Blob, or Response
	* @returns Promise<number> - bytes written
	*/
	async function exactWrite(dest, data) {
		ensureExactFs();
		const path = typeof dest === "string" ? dest : dest.name;
		let bytes;
		if (typeof data === "string") bytes = toUint8Array(data);
		else if (data instanceof Uint8Array) bytes = data;
		else if (data instanceof ArrayBuffer) bytes = new Uint8Array(data);
		else if (ArrayBuffer.isView(data)) bytes = new Uint8Array(data.buffer, data.byteOffset, data.byteLength);
		else if (typeof Blob !== "undefined" && data instanceof Blob) {
			const ab = await data.arrayBuffer();
			bytes = new Uint8Array(ab);
		} else if (typeof Response !== "undefined" && data instanceof Response) {
			const ab = await data.arrayBuffer();
			bytes = new Uint8Array(ab);
		} else bytes = toUint8Array(String(data));
		__exactWriteFile(path, bytes);
		return bytes.length;
	}
	function defineGlobalValue(target, key, value) {
		Object.defineProperty(target, key, {
			value,
			configurable: true,
			enumerable: true,
			writable: true
		});
	}
	/**
	* Install Exact and Bun globals with file() and write() methods.
	*/
	function installExactGlobal() {
		const g = globalThis;
		if (!g.Exact) g.Exact = {};
		const file = (path, options) => new ExactFile(path, options);
		const write = exactWrite;
		defineGlobalValue(g.Exact, "file", file);
		defineGlobalValue(g.Exact, "write", write);
		if (!g.Exact.version) g.Exact.version = "0.1.0";
		if (!g.Exact.platform) g.Exact.platform = "cli";
		try {
			if (!g.Bun) g.Bun = g.Exact;
			else {
				const bunFileDescriptor = Object.getOwnPropertyDescriptor(g.Bun, "file");
				if (!bunFileDescriptor || bunFileDescriptor.get || bunFileDescriptor.value == null) defineGlobalValue(g.Bun, "file", file);
				const bunWriteDescriptor = Object.getOwnPropertyDescriptor(g.Bun, "write");
				if (!bunWriteDescriptor || bunWriteDescriptor.get || bunWriteDescriptor.value == null) defineGlobalValue(g.Bun, "write", write);
			}
		} catch (_e) {}
		return {
			file,
			write
		};
	}
	//#endregion
	//#region ../ibex-runtime-js/src/core/host-call-bridge.ts
	/**
	* Shared helper for looking up the host bridge function exposed on
	* `globalThis.__hostCall`.
	*/
	function hostCallBridge() {
		const hostCall = globalThis.__hostCall;
		return typeof hostCall === "function" ? hostCall : null;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/core/accessibility-state.ts
	function freezeSnapshot(snapshot) {
		return Object.freeze({ ...snapshot });
	}
	function normalizeFontScale(value) {
		return typeof value === "number" && Number.isFinite(value) && value > 0 ? value : 1;
	}
	function readNativeAccessibilitySnapshot() {
		const nativeSnapshot = globalThis.__exactAccessibilitySnapshot;
		if (nativeSnapshot && typeof nativeSnapshot === "object") return nativeSnapshot;
		const appearanceState = globalThis.__exactAppearanceState;
		if (appearanceState && typeof appearanceState === "object") return {
			colorScheme: appearanceState.colorScheme === "dark" ? "dark" : "light",
			prefersReducedMotion: appearanceState.reducedMotion === true
		};
		return null;
	}
	function buildAccessibilitySnapshot(value) {
		return freezeSnapshot({
			prefersReducedMotion: value?.prefersReducedMotion === true,
			isBoldTextEnabled: value?.isBoldTextEnabled === true,
			prefersHighContrast: value?.prefersHighContrast === true,
			prefersReducedTransparency: value?.prefersReducedTransparency === true,
			fontScale: normalizeFontScale(value?.fontScale),
			isScreenReaderEnabled: value?.isScreenReaderEnabled === true,
			colorScheme: value?.colorScheme === "dark" ? "dark" : "light",
			isInvertColorsEnabled: value?.isInvertColorsEnabled === true,
			isGrayscaleEnabled: value?.isGrayscaleEnabled === true,
			dynamicTypeSize: typeof value?.dynamicTypeSize === "string" && value.dynamicTypeSize.length > 0 ? value.dynamicTypeSize : null
		});
	}
	function createDefaultState() {
		return {
			snapshot: buildAccessibilitySnapshot(readNativeAccessibilitySnapshot()),
			listeners: /* @__PURE__ */ new Set(),
			eventListeners: /* @__PURE__ */ new Map(),
			changeTimer: null
		};
	}
	function getAccessibilityState() {
		if (!globalThis.__exactAccessibilityState) globalThis.__exactAccessibilityState = createDefaultState();
		return globalThis.__exactAccessibilityState;
	}
	function dispatchAccessibilityChange(state) {
		const snapshot = state.snapshot;
		{
			const __exactForOfIterator105 = state.listeners[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep105 = __exactForOfIterator105.next();
				if (__exactForOfStep105.done) break;
				const listener = __exactForOfStep105.value;
				try {
					listener(snapshot);
				} catch {}
			}
		}
	}
	function emitNamedEvent(state, event, value) {
		const listeners = state.eventListeners.get(event);
		if (!listeners) return;
		{
			const __exactForOfIterator106 = listeners[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep106 = __exactForOfIterator106.next();
				if (__exactForOfStep106.done) break;
				const listener = __exactForOfStep106.value;
				try {
					listener(value);
				} catch {}
			}
		}
	}
	function scheduleAccessibilityChange(state) {
		if (state.changeTimer != null) clearTimeout(state.changeTimer);
		state.changeTimer = setTimeout(() => {
			state.changeTimer = null;
			dispatchAccessibilityChange(state);
		}, 100);
	}
	function updateAppearanceMirror(snapshot) {
		globalThis.__exactAppearanceState = {
			colorScheme: snapshot.colorScheme,
			reducedMotion: snapshot.prefersReducedMotion
		};
		globalThis.__exactWindowNotifyMediaChange?.({
			colorScheme: snapshot.colorScheme,
			reducedMotion: snapshot.prefersReducedMotion
		});
	}
	function updateAccessibilitySnapshot(value, notify) {
		const state = getAccessibilityState();
		const previous = state.snapshot;
		const next = buildAccessibilitySnapshot(value);
		state.snapshot = next;
		updateAppearanceMirror(next);
		const changedEntries = [
			["prefersReducedMotion", "reduceMotionChanged"],
			["isBoldTextEnabled", "boldTextChanged"],
			["prefersHighContrast", "highContrastChanged"],
			["prefersReducedTransparency", "reducedTransparencyChanged"],
			["fontScale", "fontScaleChanged"],
			["isScreenReaderEnabled", "screenReaderChanged"],
			["colorScheme", "colorSchemeChanged"],
			["isInvertColorsEnabled", "invertColorsChanged"],
			["isGrayscaleEnabled", "grayscaleChanged"],
			["dynamicTypeSize", "dynamicTypeSizeChanged"]
		].filter(([key]) => previous[key] !== next[key]);
		if (notify && changedEntries.length > 0) {
			scheduleAccessibilityChange(state);
			{
				const __exactForOfIterator107 = changedEntries[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep107 = __exactForOfIterator107.next();
					if (__exactForOfStep107.done) break;
					const [key, event] = __exactForOfStep107.value;
					emitNamedEvent(state, event, next[key]);
				}
			}
			emitNamedEvent(state, "change", next);
		}
		return next;
	}
	function addExactAccessibilityEventListener(event, listener) {
		const state = getAccessibilityState();
		const listeners = state.eventListeners.get(event) ?? /* @__PURE__ */ new Set();
		listeners.add(listener);
		state.eventListeners.set(event, listeners);
		return () => {
			listeners.delete(listener);
			if (listeners.size === 0) state.eventListeners.delete(event);
		};
	}
	function getExactAccessibilitySnapshot() {
		return getAccessibilityState().snapshot;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/core/accessibility.ts
	function createAccessibilityNamespace() {
		return {
			get prefersReducedMotion() {
				return getExactAccessibilitySnapshot().prefersReducedMotion;
			},
			get isBoldTextEnabled() {
				return getExactAccessibilitySnapshot().isBoldTextEnabled;
			},
			get prefersHighContrast() {
				return getExactAccessibilitySnapshot().prefersHighContrast;
			},
			get prefersReducedTransparency() {
				return getExactAccessibilitySnapshot().prefersReducedTransparency;
			},
			get fontScale() {
				return getExactAccessibilitySnapshot().fontScale;
			},
			get isScreenReaderEnabled() {
				return getExactAccessibilitySnapshot().isScreenReaderEnabled;
			},
			get colorScheme() {
				return getExactAccessibilitySnapshot().colorScheme;
			},
			get isInvertColorsEnabled() {
				return getExactAccessibilitySnapshot().isInvertColorsEnabled;
			},
			get isGrayscaleEnabled() {
				return getExactAccessibilitySnapshot().isGrayscaleEnabled;
			},
			get dynamicTypeSize() {
				return getExactAccessibilitySnapshot().dynamicTypeSize;
			},
			get(key) {
				return getExactAccessibilitySnapshot()[key];
			},
			addEventListener(event, listener) {
				return addExactAccessibilityEventListener(event, listener);
			},
			announce(message, options) {
				return announceForAccessibility(message, options);
			}
		};
	}
	function installExactAccessibilityGlobal() {
		const g = globalThis;
		if (typeof g.Exact !== "object" || g.Exact === null) g.Exact = {};
		const exact = g.Exact;
		const existing = exact.accessibility;
		if (existing && typeof existing === "object" && typeof existing.addEventListener === "function") return existing;
		updateAccessibilitySnapshot(readNativeAccessibilitySnapshot(), false);
		const accessibility = createAccessibilityNamespace();
		Object.defineProperty(exact, "accessibility", {
			value: accessibility,
			writable: true,
			configurable: true,
			enumerable: true
		});
		globalThis.__exactAccessibilityChanged = (snapshot) => {
			if (snapshot && typeof snapshot === "object") globalThis.__exactAccessibilitySnapshot = snapshot;
			updateAccessibilitySnapshot(snapshot ?? readNativeAccessibilitySnapshot(), true);
		};
		return accessibility;
	}
	function announceForAccessibility(message, options = {}) {
		if (typeof message !== "string" || message.trim().length === 0) return false;
		const hostCall = hostCallBridge();
		if (!hostCall) return false;
		try {
			return hostCall("accessibility.announce", JSON.stringify({
				message,
				queue: options.queue !== false
			})) !== false;
		} catch {
			return false;
		}
	}
	//#endregion
	//#region ../ibex-runtime-js/src/timers/Timers.ts
	init_defineProperty();
	let _Symbol$toPrimitive;
	const symbolDispose = typeof Symbol === "function" ? Symbol.dispose ?? (typeof Symbol.for === "function" ? Symbol.for("nodejs.dispose") : void 0) : void 0;
	function validateTimerCallback(callback) {
		if (typeof callback === "function") return callback;
		const err = /* @__PURE__ */ new TypeError(`The "callback" argument must be of type function. Received type ${callback === null ? "null" : typeof callback}`);
		err.code = "ERR_INVALID_ARG_TYPE";
		throw err;
	}
	_Symbol$toPrimitive = Symbol.toPrimitive;
	var ImmediateHandle = class {
		constructor(handle) {
			_defineProperty(this, "_handle", void 0);
			_defineProperty(this, "_destroyed", false);
			_defineProperty(this, "_refed", true);
			this._handle = handle;
		}
		ref() {
			this._refed = true;
			const handle = this._handle;
			if (handle && typeof handle === "object" && typeof handle.ref === "function") handle.ref();
			else if (typeof globalThis.__exactTimerRef === "function" && handle != null) globalThis.__exactTimerRef(handle);
			return this;
		}
		unref() {
			this._refed = false;
			const handle = this._handle;
			if (handle && typeof handle === "object" && typeof handle.unref === "function") handle.unref();
			else if (typeof globalThis.__exactTimerUnref === "function" && handle != null) globalThis.__exactTimerUnref(handle);
			return this;
		}
		hasRef() {
			return this._refed;
		}
		close() {
			if (!this._destroyed) {
				this._destroyed = true;
				globalThis.clearTimeout(this._handle);
			}
			return this;
		}
		[_Symbol$toPrimitive]() {
			if (typeof this._handle === "number") return this._handle;
			return 0;
		}
	};
	if (symbolDispose) ImmediateHandle.prototype[symbolDispose] = function dispose() {
		this.close();
	};
	let nextId = 1;
	const timeoutMap = /* @__PURE__ */ new Map();
	const intervalMap = /* @__PURE__ */ new Map();
	const fallbackModule = {
		setTimeout(callback, delay) {
			const id = nextId++;
			const handle = globalThis.setTimeout(() => {
				timeoutMap.delete(id);
				callback();
			}, delay);
			timeoutMap.set(id, handle);
			return id;
		},
		clearTimeout(id) {
			const handle = timeoutMap.get(id);
			if (handle !== void 0) {
				globalThis.clearTimeout(handle);
				timeoutMap.delete(id);
			}
		},
		setInterval(callback, delay) {
			const id = nextId++;
			const handle = globalThis.setInterval(callback, delay);
			intervalMap.set(id, handle);
			return id;
		},
		clearInterval(id) {
			const handle = intervalMap.get(id);
			if (handle !== void 0) {
				globalThis.clearInterval(handle);
				intervalMap.delete(id);
			}
		}
	};
	function getModule() {
		return fallbackModule;
	}
	function reportTimerError(error) {
		const processObj = globalThis.process;
		if (processObj && typeof processObj.emit === "function" && processObj.emit("uncaughtException", error)) return;
		queueMicrotask$1(() => {
			throw error;
		});
	}
	/**
	* clearTimeout - Cancel a scheduled timeout
	*
	* @param id Timer ID returned by setTimeout
	*/
	function clearTimeout$1(id) {
		if (id !== void 0 && id !== null) getModule().clearTimeout(id);
	}
	/**
	* queueMicrotask - Schedule a microtask
	*
	* Microtasks run before the next macrotask (timer, I/O, etc.)
	*
	* @param callback Function to call
	*/
	function queueMicrotask$1(callback) {
		if (typeof globalThis.queueMicrotask === "function") globalThis.queueMicrotask(() => {
			try {
				callback();
			} catch (error) {
				console.error("Error in queueMicrotask callback:", error);
			}
		});
		else Promise.resolve().then(() => {
			try {
				callback();
			} catch (error) {
				console.error("Error in queueMicrotask callback:", error);
			}
		});
	}
	/**
	* setImmediate - Schedule a macrotask (Node.js compatibility)
	*
	* In Exact, this is equivalent to setTimeout(fn, 0)
	*
	* @param callback Function to call
	* @param args Additional arguments to pass to callback
	* @returns Timer ID
	*/
	function setImmediate(callback, ...args) {
		callback = validateTimerCallback(callback);
		let immediate;
		const wrappedCallback = () => {
			immediate._destroyed = true;
			try {
				callback(...args);
			} catch (error) {
				reportTimerError(error);
			}
		};
		immediate = new ImmediateHandle(globalThis.setTimeout(wrappedCallback, 0));
		return immediate;
	}
	/**
	* clearImmediate - Cancel a scheduled immediate (Node.js compatibility)
	*
	* @param id Timer ID returned by setImmediate
	*/
	function clearImmediate(id) {
		if (id && typeof id === "object" && typeof id.close === "function") {
			id.close();
			return;
		}
		clearTimeout$1(id);
	}
	//#endregion
	//#region ../ibex-runtime-js/src/node/Buffer.ts
	init_defineProperty();
	let _Symbol$iterator, _Symbol$for, _Symbol$toStringTag$2;
	_Symbol$iterator = Symbol.iterator;
	_Symbol$for = Symbol.for("nodejs.util.inspect.custom");
	_Symbol$toStringTag$2 = Symbol.toStringTag;
	/**
	* Buffer class for Node.js compatibility.
	* Extends Uint8Array with additional methods.
	*/
	var Buffer$1 = class Buffer$1 extends Uint8Array {
		/**
		* Allocates a new Buffer of size bytes.
		* The buffer is initialized with zeros.
		*/
		static alloc(size, fill, encoding) {
			size = validateBufferSize(size);
			const buffer = new Buffer$1(size);
			if (fill !== void 0) buffer.fill(fill, 0, size, encoding);
			return buffer;
		}
		/**
		* Allocates a new Buffer of size bytes.
		* The buffer is NOT initialized (contains garbage data).
		* 
		* WARNING: This returns uninitialized memory for performance.
		* Use Buffer.alloc() for security-sensitive code.
		*/
		static allocUnsafe(size) {
			size = validateBufferSize(size);
			return new Buffer$1(size);
		}
		/**
		* Same as allocUnsafe but guaranteed to never use pooled memory.
		*/
		static allocUnsafeSlow(size) {
			return Buffer$1.allocUnsafe(size);
		}
		static from(value, encodingOrOffset, length) {
			if (value === null || value === void 0) throw makeFirstArgumentError(value);
			if (typeof value === "string") return Buffer$1.fromString(value, encodingOrOffset);
			const arrayBufferByteLength = getArrayBufferByteLength(value);
			if (arrayBufferByteLength !== null) {
				const byteOffset = normalizeArrayBufferOffset(encodingOrOffset, arrayBufferByteLength);
				const viewLength = normalizeArrayBufferLength(length, arrayBufferByteLength, byteOffset);
				const copy = new Uint8Array(viewLength);
				copy.set(new Uint8Array(value, byteOffset, viewLength));
				return Object.setPrototypeOf(copy, Buffer$1.prototype);
			}
			if (value instanceof Buffer$1) {
				const buffer = new Buffer$1(value.length);
				buffer.set(value);
				return buffer;
			}
			if (typeof value === "object" && value !== null && value.type === "Buffer" && "data" in value) return Buffer$1.from(value.data);
			if (Array.isArray(value) || ArrayBuffer.isView(value)) {
				const buffer = new Buffer$1(toArrayLikeLength(value));
				for (let i = 0; i < buffer.length; i++) buffer[i] = value[i] & 255;
				return buffer;
			}
			if (typeof value === "object" && "length" in value) {
				const arrayLike = value;
				const buffer = new Buffer$1(toArrayLikeLength(value));
				for (let i = 0; i < buffer.length; i++) buffer[i] = (arrayLike[i] ?? 0) & 255;
				return buffer;
			}
			if (typeof value === "object" && Symbol.iterator in value) {
				const arr = [...value];
				return Buffer$1.from(arr);
			}
			throw makeFirstArgumentError(value);
		}
		/**
		* Creates a Buffer from a string with specified encoding.
		*/
		static fromString(str, encoding = "utf8") {
			const normalizedEncoding = normalizeEncoding(encoding);
			switch (normalizedEncoding) {
				case "utf8": {
					const bytes = new TextEncoder().encode(str);
					const buffer = new Buffer$1(bytes.length);
					buffer.set(bytes);
					return buffer;
				}
				case "ascii":
				case "latin1": {
					const buffer = new Buffer$1(str.length);
					for (let i = 0; i < str.length; i++) buffer[i] = str.charCodeAt(i) & 255;
					return buffer;
				}
				case "hex": {
					const bytes = [];
					for (let i = 0; i < str.length - 1; i += 2) {
						const byte = parseInt(str.slice(i, i + 2), 16);
						if (Number.isNaN(byte)) break;
						bytes.push(byte);
					}
					const buffer = new Buffer$1(bytes.length);
					for (let i = 0; i < bytes.length; i++) buffer[i] = bytes[i];
					return buffer;
				}
				case "base64":
				case "base64url": {
					const bytes = decodeBase64Bytes(normalizedEncoding === "base64url" ? str.replace(/-/g, "+").replace(/_/g, "/") : str);
					const buffer = new Buffer$1(bytes.length);
					buffer.set(bytes);
					return buffer;
				}
				case "utf16le": {
					const buffer = new Buffer$1(str.length * 2);
					for (let i = 0; i < str.length; i++) {
						const code = str.charCodeAt(i);
						buffer[i * 2] = code & 255;
						buffer[i * 2 + 1] = code >> 8 & 255;
					}
					return buffer;
				}
				default: throw new TypeError(`Unknown encoding: ${encoding}`);
			}
		}
		/**
		* Returns the byte length of a string when encoded.
		*/
		static byteLength(string, encoding) {
			if (typeof string !== "string") {
				if (ArrayBuffer.isView(string)) return string.byteLength;
				if (isArrayBufferLike(string)) return string.byteLength;
				throw makeInvalidArgTypeError("string", "of type string or an instance of Buffer or ArrayBuffer", string);
			}
			const normalizedEncoding = normalizeEncodingOrUtf8(encoding);
			switch (normalizedEncoding) {
				case "ascii":
				case "latin1": return string.length;
				case "hex": return Math.floor(string.length / 2);
				case "base64":
				case "base64url": return Buffer$1.from(string, normalizedEncoding).length;
				case "utf16le": return string.length * 2;
				default: return utf8ByteLength(string);
			}
		}
		/**
		* Concatenate multiple Buffers into one.
		*/
		static concat(list, totalLength) {
			if (!Array.isArray(list)) throw makeInvalidArgTypeError("list", "an instance of Array", list);
			if (list.length === 0) return new Buffer$1(0);
			let len;
			if (totalLength === void 0) {
				len = 0;
				for (let i = 0; i < list.length; i++) {
					const item = list[i];
					if (!isUint8ArrayLike(item)) throw makeInvalidArgTypeError(`list[${i}]`, "an instance of Buffer or Uint8Array", item);
					len += item.length;
				}
			} else {
				if (typeof totalLength !== "number") throw makeInvalidArgTypeError("length", "of type number", totalLength);
				if (!Number.isInteger(totalLength)) throw createOutOfRangeError("length", "an integer", totalLength);
				if (totalLength < 0 || totalLength > Number.MAX_SAFE_INTEGER) throw createOutOfRangeError("length", ">= 0 && <= 9007199254740991", totalLength);
				len = totalLength;
				for (let i = 0; i < list.length; i++) {
					const item = list[i];
					if (!isUint8ArrayLike(item)) throw makeInvalidArgTypeError(`list[${i}]`, "an instance of Buffer or Uint8Array", item);
				}
			}
			const result = new Buffer$1(len);
			let offset = 0;
			for (const buf of list) {
				if (offset >= len) break;
				const copyLen = Math.min(buf.length, len - offset);
				result.set(buf.subarray(0, copyLen), offset);
				offset += copyLen;
			}
			return result;
		}
		/**
		* Compares two Buffers.
		*/
		static compare(buf1, buf2) {
			if (!isUint8ArrayLike(buf1)) throw makeInvalidArgTypeError("buf1", "an instance of Buffer or Uint8Array", buf1, true);
			if (!isUint8ArrayLike(buf2)) throw makeInvalidArgTypeError("buf2", "an instance of Buffer or Uint8Array", buf2, true);
			return compareByteRanges(buf1, buf2, 0, buf1.length, 0, buf2.length);
		}
		/**
		* Check if value is a Buffer.
		*/
		static isBuffer(obj) {
			return !!(obj && typeof obj === "object" && ArrayBuffer.isView(obj) && (obj[Symbol.toStringTag] === "Buffer" || obj instanceof Buffer$1));
		}
		/**
		* Check if encoding is valid.
		*/
		static isEncoding(encoding) {
			if (typeof encoding !== "string" || encoding === "") return false;
			try {
				normalizeEncoding(encoding);
				return true;
			} catch {
				return false;
			}
		}
		/**
		* Compare this buffer with another.
		*/
		compare(target, targetStart = 0, targetEnd, sourceStart = 0, sourceEnd) {
			if (!isUint8ArrayLike(target)) throw makeInvalidArgTypeError("target", "an instance of Buffer or Uint8Array", target, true);
			const actualTargetStart = normalizeCompareIndex(targetStart, "targetStart", 0);
			const actualTargetEnd = normalizeCompareIndex(targetEnd, "targetEnd", target.length);
			const actualSourceStart = normalizeCompareIndex(sourceStart, "sourceStart", 0);
			const actualSourceEnd = normalizeCompareIndex(sourceEnd, "sourceEnd", this.length);
			if (actualTargetStart < 0) throw createOutOfRangeError("targetStart", ">= 0", actualTargetStart);
			if (actualTargetEnd < 0) throw createOutOfRangeError("targetEnd", ">= 0", actualTargetEnd);
			if (actualSourceStart < 0) throw createOutOfRangeError("sourceStart", ">= 0", actualSourceStart);
			if (actualSourceEnd < 0) throw createOutOfRangeError("sourceEnd", ">= 0", actualSourceEnd);
			if (actualTargetEnd > target.length) throw createOutOfRangeError("targetEnd", `>= 0 && <= ${target.length}`, actualTargetEnd);
			if (actualSourceEnd > this.length) throw createOutOfRangeError("sourceEnd", `>= 0 && <= ${this.length}`, actualSourceEnd);
			if (actualSourceStart >= actualSourceEnd) return actualTargetStart >= actualTargetEnd ? 0 : -1;
			if (actualTargetStart >= actualTargetEnd) return 1;
			return compareByteRanges(this, target, actualSourceStart, actualSourceEnd, actualTargetStart, actualTargetEnd);
		}
		/**
		* Copy data from this buffer to target.
		*/
		copy(target, targetStart = 0, sourceStart = 0, sourceEnd) {
			const sourceBytes = getWritableByteView(this);
			if (!sourceBytes) throw makeInvalidArgTypeError("this", "an instance of Buffer or Uint8Array", this);
			const targetBytes = getWritableByteView(target);
			if (!targetBytes) throw makeInvalidArgTypeError("target", "an instance of Buffer or Uint8Array", target);
			const actualTargetStart = normalizeCopyIndex(targetStart, "targetStart", 0);
			const actualSourceStart = normalizeCopyIndex(sourceStart, "sourceStart", 0);
			let actualSourceEnd = sourceEnd == null ? sourceBytes.length : normalizeCopyIndex(sourceEnd, "sourceEnd", sourceBytes.length);
			if (actualTargetStart < 0) throw createOutOfRangeError("targetStart", ">= 0", actualTargetStart);
			if (actualSourceStart < 0) throw createOutOfRangeError("sourceStart", ">= 0", actualSourceStart);
			if (actualSourceEnd < 0) throw createOutOfRangeError("sourceEnd", ">= 0", actualSourceEnd);
			if (actualSourceStart > sourceBytes.length) throw createOutOfRangeError("sourceStart", `>= 0 && <= ${sourceBytes.length}`, actualSourceStart);
			if (actualSourceEnd > sourceBytes.length) actualSourceEnd = sourceBytes.length;
			if (actualSourceEnd <= actualSourceStart || actualTargetStart >= targetBytes.length) return 0;
			const length = Math.min(actualSourceEnd - actualSourceStart, targetBytes.length - actualTargetStart);
			if (sourceBytes.buffer === targetBytes.buffer && actualSourceStart < actualTargetStart && actualTargetStart < actualSourceStart + length) {
				for (let i = length - 1; i >= 0; i--) targetBytes[actualTargetStart + i] = sourceBytes[actualSourceStart + i];
				return length;
			}
			for (let i = 0; i < length; i++) targetBytes[actualTargetStart + i] = sourceBytes[actualSourceStart + i];
			return length;
		}
		/**
		* Check if this buffer equals another.
		*/
		equals(other) {
			if (!isUint8ArrayLike(other)) throw makeInvalidArgTypeError("otherBuffer", "an instance of Buffer or Uint8Array", other, true);
			return compareByteRanges(this, other, 0, this.length, 0, other.length) === 0;
		}
		/**
		* Fill buffer with a value.
		*/
		fill(value, offset = 0, end, encoding) {
			if (typeof offset === "string") {
				encoding = offset;
				offset = 0;
			}
			if (typeof end === "string") {
				encoding = end;
				end = void 0;
			}
			const fillEnd = end ?? this.length;
			const clampedOffset = Math.max(0, Math.min(offset, this.length));
			const clampedEnd = Math.max(0, Math.min(fillEnd, this.length));
			if (clampedOffset >= clampedEnd) return this;
			if (typeof value === "number") for (let i = clampedOffset; i < clampedEnd; i++) this[i] = value & 255;
			else if (typeof value === "string") if (value.length === 0) for (let i = clampedOffset; i < clampedEnd; i++) this[i] = 0;
			else {
				const fillBuffer = Buffer$1.from(value, encoding);
				if (fillBuffer.length === 0) for (let i = clampedOffset; i < clampedEnd; i++) this[i] = 0;
				else {
					let j = 0;
					for (let i = clampedOffset; i < clampedEnd; i++) {
						this[i] = fillBuffer[j % fillBuffer.length];
						j++;
					}
				}
			}
			else {
				if (value.length === 0) return this;
				let j = 0;
				for (let i = clampedOffset; i < clampedEnd; i++) {
					this[i] = value[j % value.length];
					j++;
				}
			}
			return this;
		}
		/**
		* Check if buffer includes a value.
		*/
		includes(value, byteOffset, encoding) {
			if (typeof byteOffset === "string") return this.indexOf(value, 0, byteOffset) !== -1;
			return this.indexOf(value, byteOffset, encoding) !== -1;
		}
		/**
		* Find index of value in buffer.
		*/
		indexOf(value, byteOffset, encoding) {
			if (typeof byteOffset === "string") {
				encoding = byteOffset;
				byteOffset = 0;
			}
			let offset = byteOffset ?? 0;
			if (offset < 0) offset = Math.max(0, this.length + offset);
			if (offset >= this.length) {
				if (typeof value === "number") return -1;
				return (typeof value === "string" ? Buffer$1.from(value, encoding) : value).length === 0 ? this.length : -1;
			}
			if (typeof value === "number") {
				for (let i = offset; i < this.length; i++) if (this[i] === (value & 255)) return i;
				return -1;
			}
			const searchBuffer = typeof value === "string" ? Buffer$1.from(value, encoding) : value;
			if (searchBuffer.length === 0) return Math.min(offset, this.length);
			if (searchBuffer.length > this.length - offset) return -1;
			outer: for (let i = offset; i <= this.length - searchBuffer.length; i++) {
				for (let j = 0; j < searchBuffer.length; j++) if (this[i + j] !== searchBuffer[j]) continue outer;
				return i;
			}
			return -1;
		}
		/**
		* Find last index of value in buffer.
		*/
		lastIndexOf(value, byteOffset, encoding) {
			if (typeof byteOffset === "string") {
				encoding = byteOffset;
				byteOffset = void 0;
			}
			let startOffset = byteOffset ?? this.length;
			if (startOffset < 0) {
				startOffset = this.length + startOffset;
				if (startOffset < 0) {
					if (typeof value === "number") return -1;
					return (typeof value === "string" ? Buffer$1.from(value, encoding) : value).length === 0 ? 0 : -1;
				}
			}
			if (typeof value === "number") {
				for (let i = Math.min(startOffset, this.length - 1); i >= 0; i--) if (this[i] === (value & 255)) return i;
				return -1;
			}
			const searchBuffer = typeof value === "string" ? Buffer$1.from(value, encoding) : value;
			if (searchBuffer.length === 0) return Math.min(startOffset, this.length);
			outer: for (let i = Math.min(startOffset, this.length - searchBuffer.length); i >= 0; i--) {
				for (let j = 0; j < searchBuffer.length; j++) if (this[i + j] !== searchBuffer[j]) continue outer;
				return i;
			}
			return -1;
		}
		/**
		* Create a new Buffer that shares memory with this one.
		*/
		slice(start, end) {
			return this.subarray(start, end);
		}
		/**
		* Create a subarray (shares memory).
		*/
		subarray(start, end) {
			const sub = super.subarray(start, end);
			return Object.setPrototypeOf(sub, Buffer$1.prototype);
		}
		/**
		* Swap byte order (16-bit).
		*/
		swap16() {
			if (this.length % 2 !== 0) throw new RangeError("Buffer size must be a multiple of 16-bits");
			for (let i = 0; i < this.length; i += 2) {
				const tmp = this[i];
				this[i] = this[i + 1];
				this[i + 1] = tmp;
			}
			return this;
		}
		/**
		* Swap byte order (32-bit).
		*/
		swap32() {
			if (this.length % 4 !== 0) throw new RangeError("Buffer size must be a multiple of 32-bits");
			for (let i = 0; i < this.length; i += 4) {
				const tmp0 = this[i];
				const tmp1 = this[i + 1];
				this[i] = this[i + 3];
				this[i + 1] = this[i + 2];
				this[i + 2] = tmp1;
				this[i + 3] = tmp0;
			}
			return this;
		}
		/**
		* Swap byte order (64-bit).
		*/
		swap64() {
			if (this.length % 8 !== 0) throw new RangeError("Buffer size must be a multiple of 64-bits");
			for (let i = 0; i < this.length; i += 8) {
				const tmp0 = this[i];
				const tmp1 = this[i + 1];
				const tmp2 = this[i + 2];
				const tmp3 = this[i + 3];
				this[i] = this[i + 7];
				this[i + 1] = this[i + 6];
				this[i + 2] = this[i + 5];
				this[i + 3] = this[i + 4];
				this[i + 4] = tmp3;
				this[i + 5] = tmp2;
				this[i + 6] = tmp1;
				this[i + 7] = tmp0;
			}
			return this;
		}
		/**
		* Convert buffer to JSON.
		*/
		toJSON() {
			return {
				type: "Buffer",
				data: [...this]
			};
		}
		/**
		* Convert buffer to string.
		*/
		toString(encoding = "utf8", start = 0, end) {
			const normalizedEncoding = normalizeEncoding(encoding);
			const actualStart = normalizeToStringRangeIndex(start, this.length, 0);
			const actualEnd = end === void 0 ? this.length : normalizeToStringRangeIndex(end, this.length, 0);
			if (actualEnd <= actualStart) return "";
			const slice = this.subarray(actualStart, actualEnd);
			switch (normalizedEncoding) {
				case "utf8": {
					const decoded = new TextDecoder("utf-8").decode(slice);
					if (slice.length >= 3 && slice[0] === 239 && slice[1] === 187 && slice[2] === 191 && decoded.charCodeAt(0) !== 65279) return `\uFEFF${decoded}`;
					return decoded;
				}
				case "ascii": {
					let result = "";
					for (let i = 0; i < slice.length; i++) result += String.fromCharCode(slice[i] & 127);
					return result;
				}
				case "latin1": {
					let result = "";
					for (let i = 0; i < slice.length; i++) result += String.fromCharCode(slice[i]);
					return result;
				}
				case "hex": {
					let result = "";
					for (let i = 0; i < slice.length; i++) result += slice[i].toString(16).padStart(2, "0");
					return result;
				}
				case "base64": {
					let binary = "";
					for (let i = 0; i < slice.length; i++) binary += String.fromCharCode(slice[i]);
					return btoa(binary);
				}
				case "base64url": {
					let binary = "";
					for (let i = 0; i < slice.length; i++) binary += String.fromCharCode(slice[i]);
					return btoa(binary).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
				}
				case "utf16le": {
					let result = "";
					for (let i = 0; i < slice.length - 1; i += 2) result += String.fromCharCode(slice[i] | slice[i + 1] << 8);
					return result;
				}
			}
		}
		/**
		* Write string to buffer.
		*/
		write(string, offset = 0, length, encoding) {
			let writeEncoding = encoding;
			let writeLength = length;
			if (typeof length === "string") {
				writeEncoding = length;
				writeLength = void 0;
			} else if (typeof offset === "string") {
				writeEncoding = offset;
				offset = 0;
				writeLength = void 0;
			}
			const data = Buffer$1.from(string, writeEncoding);
			const bytesToWrite = Math.min(writeLength ?? data.length, this.length - offset, data.length);
			if (bytesToWrite <= 0) return 0;
			this.set(data.subarray(0, bytesToWrite), offset);
			return bytesToWrite;
		}
		readBigInt64BE(offset = 0) {
			offset = validateOffset(offset, 8, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getBigInt64(0, false);
		}
		readBigInt64LE(offset = 0) {
			offset = validateOffset(offset, 8, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getBigInt64(0, true);
		}
		readBigUInt64BE(offset = 0) {
			offset = validateOffset(offset, 8, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getBigUint64(0, false);
		}
		readBigUInt64LE(offset = 0) {
			offset = validateOffset(offset, 8, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getBigUint64(0, true);
		}
		readDoubleBE(offset = 0) {
			offset = validateOffset(offset, 8, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getFloat64(0, false);
		}
		readDoubleLE(offset = 0) {
			offset = validateOffset(offset, 8, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getFloat64(0, true);
		}
		readFloatBE(offset = 0) {
			offset = validateOffset(offset, 4, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getFloat32(0, false);
		}
		readFloatLE(offset = 0) {
			offset = validateOffset(offset, 4, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getFloat32(0, true);
		}
		readInt8(offset = 0) {
			offset = validateOffset(offset, 1, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getInt8(0);
		}
		readInt16BE(offset = 0) {
			offset = validateOffset(offset, 2, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getInt16(0, false);
		}
		readInt16LE(offset = 0) {
			offset = validateOffset(offset, 2, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getInt16(0, true);
		}
		readInt32BE(offset = 0) {
			offset = validateOffset(offset, 4, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getInt32(0, false);
		}
		readInt32LE(offset = 0) {
			offset = validateOffset(offset, 4, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getInt32(0, true);
		}
		readUInt8(offset = 0) {
			offset = validateOffset(offset, 1, this.length);
			return this[offset];
		}
		readUInt16BE(offset = 0) {
			offset = validateOffset(offset, 2, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getUint16(0, false);
		}
		readUInt16LE(offset = 0) {
			offset = validateOffset(offset, 2, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getUint16(0, true);
		}
		readUInt32BE(offset = 0) {
			offset = validateOffset(offset, 4, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getUint32(0, false);
		}
		readUInt32LE(offset = 0) {
			offset = validateOffset(offset, 4, this.length);
			return new DataView(this.buffer, this.byteOffset + offset).getUint32(0, true);
		}
		readUIntBE(offset, byteLength) {
			return readVariableUint(this, offset, byteLength, false);
		}
		readUIntLE(offset, byteLength) {
			return readVariableUint(this, offset, byteLength, true);
		}
		readIntBE(offset, byteLength) {
			return readVariableInt(this, offset, byteLength, false);
		}
		readIntLE(offset, byteLength) {
			return readVariableInt(this, offset, byteLength, true);
		}
		writeBigInt64BE(value, offset = 0) {
			validateBigIntWrite(value, true);
			validateFixedWidthOffset(offset, 8, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setBigInt64(0, value, false);
			return offset + 8;
		}
		writeBigInt64LE(value, offset = 0) {
			validateBigIntWrite(value, true);
			validateFixedWidthOffset(offset, 8, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setBigInt64(0, value, true);
			return offset + 8;
		}
		writeBigUInt64BE(value, offset = 0) {
			validateBigIntWrite(value, false);
			validateFixedWidthOffset(offset, 8, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setBigUint64(0, value, false);
			return offset + 8;
		}
		writeBigUInt64LE(value, offset = 0) {
			validateBigIntWrite(value, false);
			validateFixedWidthOffset(offset, 8, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setBigUint64(0, value, true);
			return offset + 8;
		}
		writeDoubleBE(value, offset = 0) {
			new DataView(this.buffer, this.byteOffset + offset).setFloat64(0, value, false);
			return offset + 8;
		}
		writeDoubleLE(value, offset = 0) {
			new DataView(this.buffer, this.byteOffset + offset).setFloat64(0, value, true);
			return offset + 8;
		}
		writeFloatBE(value, offset = 0) {
			new DataView(this.buffer, this.byteOffset + offset).setFloat32(0, value, false);
			return offset + 4;
		}
		writeFloatLE(value, offset = 0) {
			new DataView(this.buffer, this.byteOffset + offset).setFloat32(0, value, true);
			return offset + 4;
		}
		writeInt8(value, offset = 0) {
			validateIntegerWriteValue(value, -128, 127, ">= -128 and <= 127");
			offset = validateOffset(offset, 1, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setInt8(0, value);
			return offset + 1;
		}
		writeInt16BE(value, offset = 0) {
			validateIntegerWriteValue(value, -32768, 32767, ">= -32768 and <= 32767");
			offset = validateOffset(offset, 2, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setInt16(0, value, false);
			return offset + 2;
		}
		writeInt16LE(value, offset = 0) {
			validateIntegerWriteValue(value, -32768, 32767, ">= -32768 and <= 32767");
			offset = validateOffset(offset, 2, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setInt16(0, value, true);
			return offset + 2;
		}
		writeInt32BE(value, offset = 0) {
			validateIntegerWriteValue(value, -2147483648, 2147483647, ">= -2147483648 and <= 2147483647");
			offset = validateOffset(offset, 4, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setInt32(0, value, false);
			return offset + 4;
		}
		writeInt32LE(value, offset = 0) {
			validateIntegerWriteValue(value, -2147483648, 2147483647, ">= -2147483648 and <= 2147483647");
			offset = validateOffset(offset, 4, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setInt32(0, value, true);
			return offset + 4;
		}
		writeUInt8(value, offset = 0) {
			validateIntegerWriteValue(value, 0, 255, ">= 0 and <= 255");
			offset = validateOffset(offset, 1, this.length);
			this[offset] = value & 255;
			return offset + 1;
		}
		writeUInt16BE(value, offset = 0) {
			validateIntegerWriteValue(value, 0, 65535, ">= 0 and <= 65535");
			offset = validateOffset(offset, 2, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setUint16(0, value, false);
			return offset + 2;
		}
		writeUInt16LE(value, offset = 0) {
			validateIntegerWriteValue(value, 0, 65535, ">= 0 and <= 65535");
			offset = validateOffset(offset, 2, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setUint16(0, value, true);
			return offset + 2;
		}
		writeUInt32BE(value, offset = 0) {
			validateIntegerWriteValue(value, 0, 4294967295, ">= 0 and <= 4294967295");
			offset = validateOffset(offset, 4, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setUint32(0, value, false);
			return offset + 4;
		}
		writeUInt32LE(value, offset = 0) {
			validateIntegerWriteValue(value, 0, 4294967295, ">= 0 and <= 4294967295");
			offset = validateOffset(offset, 4, this.length);
			new DataView(this.buffer, this.byteOffset + offset).setUint32(0, value, true);
			return offset + 4;
		}
		writeUIntBE(value, offset, byteLength) {
			return writeVariableUint(this, value, offset, byteLength, false);
		}
		writeUIntLE(value, offset, byteLength) {
			return writeVariableUint(this, value, offset, byteLength, true);
		}
		writeIntBE(value, offset, byteLength) {
			return writeVariableInt(this, value, offset, byteLength, false);
		}
		writeIntLE(value, offset, byteLength) {
			return writeVariableInt(this, value, offset, byteLength, true);
		}
		readUint8(offset = 0) {
			return this.readUInt8(offset);
		}
		readUint16BE(offset = 0) {
			return this.readUInt16BE(offset);
		}
		readUint16LE(offset = 0) {
			return this.readUInt16LE(offset);
		}
		readUint32BE(offset = 0) {
			return this.readUInt32BE(offset);
		}
		readUint32LE(offset = 0) {
			return this.readUInt32LE(offset);
		}
		readUintBE(offset, byteLength) {
			return this.readUIntBE(offset, byteLength);
		}
		readUintLE(offset, byteLength) {
			return this.readUIntLE(offset, byteLength);
		}
		readBigUint64BE(offset = 0) {
			return this.readBigUInt64BE(offset);
		}
		readBigUint64LE(offset = 0) {
			return this.readBigUInt64LE(offset);
		}
		writeUint8(value, offset = 0) {
			return this.writeUInt8(value, offset);
		}
		writeUint16BE(value, offset = 0) {
			return this.writeUInt16BE(value, offset);
		}
		writeUint16LE(value, offset = 0) {
			return this.writeUInt16LE(value, offset);
		}
		writeUint32BE(value, offset = 0) {
			return this.writeUInt32BE(value, offset);
		}
		writeUint32LE(value, offset = 0) {
			return this.writeUInt32LE(value, offset);
		}
		writeUintBE(value, offset, byteLength) {
			return this.writeUIntBE(value, offset, byteLength);
		}
		writeUintLE(value, offset, byteLength) {
			return this.writeUIntLE(value, offset, byteLength);
		}
		writeBigUint64BE(value, offset = 0) {
			return this.writeBigUInt64BE(value, offset);
		}
		writeBigUint64LE(value, offset = 0) {
			return this.writeBigUInt64LE(value, offset);
		}
		[_Symbol$iterator]() {
			return super.values();
		}
		entries() {
			return super.entries();
		}
		keys() {
			return super.keys();
		}
		values() {
			return super.values();
		}
		[_Symbol$for](_depth, _opts) {
			if (this.length === 0) return "<Buffer >";
			const hexBytes = [];
			const limit = Math.min(this.length, 50);
			for (let i = 0; i < limit; i++) hexBytes.push(this[i].toString(16).padStart(2, "0"));
			const suffix = this.length > 50 ? ` ... ${this.length - 50} more bytes` : "";
			return `<Buffer ${hexBytes.join(" ")}${suffix}>`;
		}
		get parent() {
			return this.buffer;
		}
		get offset() {
			return this.byteOffset;
		}
		get [_Symbol$toStringTag$2]() {
			return "Buffer";
		}
	};
	_defineProperty(Buffer$1, "poolSize", 8192);
	_defineProperty(Buffer$1, "constants", Object.freeze({
		MAX_LENGTH: 2 ** 31 - 1,
		MAX_STRING_LENGTH: 2 ** 28 - 16
	}));
	/**
	* Normalize encoding name to standard form.
	*/
	function normalizeEncoding(encoding) {
		const value = typeof encoding === "string" ? encoding : String(encoding);
		switch (value.toLowerCase()) {
			case "utf8":
			case "utf-8": return "utf8";
			case "ascii": return "ascii";
			case "latin1":
			case "binary": return "latin1";
			case "hex": return "hex";
			case "base64": return "base64";
			case "base64url": return "base64url";
			case "ucs2":
			case "ucs-2":
			case "utf16le":
			case "utf-16le": return "utf16le";
			default: throw makeUnknownEncodingError(value);
		}
	}
	function normalizeEncodingOrUtf8(encoding) {
		if (typeof encoding !== "string" || encoding === "") return "utf8";
		try {
			return normalizeEncoding(encoding);
		} catch {
			return "utf8";
		}
	}
	function utf8ByteLength(value) {
		let total = 0;
		for (let index = 0; index < value.length; index++) {
			const code = value.charCodeAt(index);
			if (code <= 127) {
				total += 1;
				continue;
			}
			if (code <= 2047) {
				total += 2;
				continue;
			}
			if (code >= 55296 && code <= 56319 && index + 1 < value.length) {
				const next = value.charCodeAt(index + 1);
				if (next >= 56320 && next <= 57343) {
					total += 4;
					index += 1;
					continue;
				}
			}
			total += 3;
		}
		return total;
	}
	function decodeBase64Char(code) {
		if (code >= 65 && code <= 90) return code - 65;
		if (code >= 97 && code <= 122) return code - 71;
		if (code >= 48 && code <= 57) return code + 4;
		if (code === 43 || code === 45) return 62;
		if (code === 47 || code === 95) return 63;
		return -1;
	}
	function decodeBase64Bytes(value) {
		const sextets = [];
		for (let i = 0; i < value.length; i++) {
			const code = value.charCodeAt(i);
			if (code === 9 || code === 10 || code === 12 || code === 13 || code === 32) continue;
			if (code === 61) break;
			const sextet = decodeBase64Char(code);
			if (sextet >= 0) sextets.push(sextet);
		}
		const fullGroups = Math.floor(sextets.length / 4);
		const remainder = sextets.length % 4;
		let byteLength = fullGroups * 3;
		if (remainder === 2) byteLength += 1;
		else if (remainder === 3) byteLength += 2;
		const bytes = new Uint8Array(byteLength);
		let outIndex = 0;
		let sextetIndex = 0;
		for (let group = 0; group < fullGroups; group++) {
			const a = sextets[sextetIndex++];
			const b = sextets[sextetIndex++];
			const c = sextets[sextetIndex++];
			const d = sextets[sextetIndex++];
			bytes[outIndex++] = a << 2 | b >> 4;
			bytes[outIndex++] = (b & 15) << 4 | c >> 2;
			bytes[outIndex++] = (c & 3) << 6 | d;
		}
		if (remainder === 2) {
			const a = sextets[sextetIndex++];
			const b = sextets[sextetIndex++];
			bytes[outIndex++] = a << 2 | b >> 4;
		} else if (remainder === 3) {
			const a = sextets[sextetIndex++];
			const b = sextets[sextetIndex++];
			const c = sextets[sextetIndex++];
			bytes[outIndex++] = a << 2 | b >> 4;
			bytes[outIndex++] = (b & 15) << 4 | c >> 2;
		}
		return bytes;
	}
	function validateBufferSize(size) {
		if (typeof size !== "number") throw makeInvalidArgTypeError("size", "of type number", size);
		if (Number.isNaN(size)) throw createOutOfRangeError("size", "a non-negative integer", "NaN");
		if (!Number.isFinite(size) || size < 0 || size > Buffer$1.constants.MAX_LENGTH) throw createOutOfRangeError("size", `>= 0 && <= ${Buffer$1.constants.MAX_LENGTH}`, size);
		return normalizeInteger(size);
	}
	function isArrayBufferLike(value) {
		return getArrayBufferByteLength(value) !== null;
	}
	function getArrayBufferByteLength(value) {
		if (!value || typeof value !== "object") return null;
		const tag = Object.prototype.toString.call(value);
		if (tag !== "[object ArrayBuffer]" && tag !== "[object SharedArrayBuffer]") return null;
		try {
			return typeof value.byteLength === "number" ? value.byteLength : null;
		} catch {
			return null;
		}
	}
	function isUint8ArrayLike(value) {
		return !!(value && typeof value === "object" && (value instanceof Buffer$1 || Object.prototype.toString.call(value) === "[object Uint8Array]"));
	}
	function toArrayLikeLength(value) {
		const raw = Number(value?.length);
		if (!Number.isFinite(raw) || raw <= 0) return 0;
		return Math.min(Math.floor(raw), 2147483647);
	}
	function formatReceivedValue$1(value, quoteStrings = false) {
		if (value === null) return "null";
		if (value === void 0) return "undefined";
		if (typeof value === "string") return quoteStrings ? `type string ('${value}')` : `type string (${value})`;
		if (typeof value === "function") return `function ${value.name || "<anonymous>"}`;
		if (typeof value !== "object") return `type ${typeof value} (${String(value)})`;
		const ctorName = value.constructor?.name;
		if (ctorName) return `an instance of ${ctorName}`;
		return String(value);
	}
	function makeInvalidArgTypeError(name, expected, value, quoteStrings = false) {
		const err = /* @__PURE__ */ new TypeError(`The "${name}" argument must be ${expected}. Received ${formatReceivedValue$1(value, quoteStrings)}`);
		err.code = "ERR_INVALID_ARG_TYPE";
		return err;
	}
	function makeFirstArgumentError(value) {
		const err = /* @__PURE__ */ new TypeError(`The first argument must be of type string or an instance of Buffer, ArrayBuffer, or Array or an Array-like Object. Received ${formatReceivedValue$1(value)}`);
		err.code = "ERR_INVALID_ARG_TYPE";
		return err;
	}
	function makeUnknownEncodingError(encoding) {
		const err = /* @__PURE__ */ new TypeError(`Unknown encoding: ${encoding}`);
		err.code = "ERR_UNKNOWN_ENCODING";
		return err;
	}
	function createOutOfRangeError(name, expectation, received) {
		const err = /* @__PURE__ */ new RangeError(`The value of "${name}" is out of range. It must be ${expectation}. Received ${String(received)}`);
		err.code = "ERR_OUT_OF_RANGE";
		return err;
	}
	function makeBufferBoundsError(which) {
		const err = /* @__PURE__ */ new RangeError(`"${which}" is outside of buffer bounds`);
		err.code = "ERR_BUFFER_OUT_OF_BOUNDS";
		return err;
	}
	function normalizeInteger(value) {
		return value < 0 ? Math.ceil(value) : Math.floor(value);
	}
	function normalizeToStringRangeIndex(value, bufferLength, fallback) {
		if (value === void 0) return fallback;
		let numeric = Number(value);
		if (Number.isNaN(numeric)) numeric = 0;
		if (!Number.isFinite(numeric)) return numeric < 0 ? 0 : bufferLength;
		numeric = normalizeInteger(numeric);
		if (numeric < 0) return 0;
		if (numeric > bufferLength) return bufferLength;
		return numeric;
	}
	function normalizeArrayBufferOffset(value, totalLength) {
		if (value === void 0) return 0;
		const offset = Number(value);
		if (Number.isNaN(offset)) return 0;
		if (!Number.isFinite(offset)) throw makeBufferBoundsError("offset");
		const normalized = normalizeInteger(offset);
		if (normalized < 0 || normalized > totalLength) throw makeBufferBoundsError("offset");
		return normalized;
	}
	function normalizeArrayBufferLength(value, totalLength, offset) {
		if (value === void 0) return totalLength - offset;
		const length = Number(value);
		if (Number.isNaN(length)) return 0;
		if (!Number.isFinite(length)) throw makeBufferBoundsError("length");
		const normalized = normalizeInteger(length);
		if (normalized < 0 || offset + normalized > totalLength) throw makeBufferBoundsError("length");
		return normalized;
	}
	function compareByteRanges(left, right, leftStart, leftEnd, rightStart, rightEnd) {
		const leftLength = leftEnd - leftStart;
		const rightLength = rightEnd - rightStart;
		const length = Math.min(leftLength, rightLength);
		for (let i = 0; i < length; i++) {
			const leftValue = left[leftStart + i];
			const rightValue = right[rightStart + i];
			if (leftValue !== rightValue) return leftValue < rightValue ? -1 : 1;
		}
		if (leftLength === rightLength) return 0;
		return leftLength < rightLength ? -1 : 1;
	}
	function getWritableByteView(value) {
		if (isUint8ArrayLike(value)) return value;
		if (ArrayBuffer.isView(value)) return new Uint8Array(value.buffer, value.byteOffset, value.byteLength);
		return null;
	}
	function normalizeCompareIndex(value, name, fallback) {
		if (value === void 0) return fallback;
		if (typeof value !== "number") throw makeInvalidArgTypeError(name, "of type number", value);
		if (Number.isNaN(value) || !Number.isInteger(value)) throw createOutOfRangeError(name, "an integer", value);
		if (!Number.isFinite(value)) throw createOutOfRangeError(name, ">= 0 && <= 2147483647", value);
		return normalizeInteger(value);
	}
	function normalizeCopyIndex(value, name, fallback) {
		if (value === void 0) return fallback;
		const numeric = Number(value);
		if (Number.isNaN(numeric)) return fallback;
		if (!Number.isFinite(numeric)) throw createOutOfRangeError(name, ">= 0", value);
		return normalizeInteger(numeric);
	}
	function validateBigIntWrite(value, signed) {
		if (typeof value !== "bigint") throw makeInvalidArgTypeError("value", "of type bigint", value);
		if (value < (signed ? -(1n << 63n) : 0n) || value > (signed ? (1n << 63n) - 1n : (1n << 64n) - 1n)) {
			const err = /* @__PURE__ */ new RangeError(`The value of "value" is out of range. It must be ${signed ? ">= -(2n ** 63n) and < 2n ** 63n" : ">= 0n and < 2n ** 64n"}. Received ${formatBigInt(value)}`);
			err.code = "ERR_OUT_OF_RANGE";
			throw err;
		}
	}
	function formatBigInt(value) {
		const negative = value < 0n;
		const digits = (negative ? -value : value).toString();
		let formatted = "";
		const prefixLength = digits.length % 3;
		if (prefixLength > 0) formatted = digits.slice(0, prefixLength);
		for (let index = prefixLength; index < digits.length; index += 3) {
			if (formatted) formatted += "_";
			formatted += digits.slice(index, index + 3);
		}
		return `${negative ? "-" : ""}${formatted || "0"}n`;
	}
	function formatIntegerForRangeError(value) {
		if (!Number.isFinite(value) || Number.isNaN(value)) return String(value);
		const negative = value < 0;
		const digits = String(Math.trunc(Math.abs(value)));
		let formatted = "";
		const prefixLength = digits.length % 3;
		if (prefixLength > 0) formatted = digits.slice(0, prefixLength);
		for (let index = prefixLength; index < digits.length; index += 3) {
			if (formatted) formatted += "_";
			formatted += digits.slice(index, index + 3);
		}
		return `${negative ? "-" : ""}${formatted || "0"}`;
	}
	function validateIntegerWriteValue(value, min, max, expectation, formatReceived = false) {
		if (typeof value !== "number") throw makeInvalidArgTypeError("value", "of type number", value);
		if (!Number.isFinite(value) || !Number.isInteger(value) || value < min || value > max) throw createOutOfRangeError("value", expectation, formatReceived ? formatIntegerForRangeError(value) : value);
		return value;
	}
	function validateFixedWidthOffset(offset, byteLength, bufferLength) {
		if (!Number.isInteger(offset) || offset < 0 || offset + byteLength > bufferLength) throw createOutOfRangeError("offset", `>= 0 and <= ${Math.max(bufferLength - byteLength, 0)}`, offset);
	}
	function makeOutOfBoundsReadError() {
		const err = /* @__PURE__ */ new RangeError("Attempt to access memory outside buffer bounds");
		err.code = "ERR_BUFFER_OUT_OF_BOUNDS";
		return err;
	}
	function validateOffset(offset, byteLength, bufferLength) {
		if (offset === void 0) return 0;
		if (typeof offset !== "number") throw makeInvalidArgTypeError("offset", "of type number", offset);
		if (Number.isNaN(offset) || Number.isFinite(offset) && !Number.isInteger(offset)) throw createOutOfRangeError("offset", "an integer", offset);
		const maxOffset = Math.max(bufferLength - byteLength, 0);
		if (!Number.isFinite(offset) || offset < 0 || offset > maxOffset) {
			if (bufferLength < byteLength && offset >= 0 && Number.isFinite(offset)) throw makeOutOfBoundsReadError();
			throw createOutOfRangeError("offset", `>= 0 and <= ${maxOffset}`, offset);
		}
		if (offset + byteLength > bufferLength) throw makeOutOfBoundsReadError();
		return offset;
	}
	function validateRequiredOffset(offset, byteLength, bufferLength) {
		if (offset === void 0) throw makeInvalidArgTypeError("offset", "of type number", offset);
		return validateOffset(offset, byteLength, bufferLength);
	}
	function validateByteLength(byteLength) {
		if (typeof byteLength !== "number") throw makeInvalidArgTypeError("byteLength", "of type number", byteLength);
		if (Number.isNaN(byteLength) || Number.isFinite(byteLength) && !Number.isInteger(byteLength)) throw createOutOfRangeError("byteLength", "an integer", byteLength);
		if (!Number.isFinite(byteLength) || byteLength < 1 || byteLength > 6) throw createOutOfRangeError("byteLength", ">= 1 and <= 6", byteLength);
		return byteLength;
	}
	function readVariableUint(buffer, offset, byteLength, littleEndian) {
		const actualByteLength = validateByteLength(byteLength);
		const actualOffset = validateRequiredOffset(offset, actualByteLength, buffer.length);
		let value = 0;
		if (littleEndian) {
			for (let index = actualByteLength - 1; index >= 0; index--) value = value * 256 + buffer[actualOffset + index];
			return value;
		}
		for (let index = 0; index < actualByteLength; index++) value = value * 256 + buffer[actualOffset + index];
		return value;
	}
	function readVariableInt(buffer, offset, byteLength, littleEndian) {
		const actualByteLength = validateByteLength(byteLength);
		const unsigned = readVariableUint(buffer, offset, actualByteLength, littleEndian);
		const bits = actualByteLength * 8;
		const signBit = 2 ** (bits - 1);
		const fullRange = 2 ** bits;
		return unsigned >= signBit ? unsigned - fullRange : unsigned;
	}
	function writeVariableUint(buffer, value, offset, byteLength, littleEndian) {
		const actualByteLength = validateByteLength(byteLength);
		const actualOffset = validateRequiredOffset(offset, actualByteLength, buffer.length);
		const max = 2 ** (actualByteLength * 8) - 1;
		let remaining = validateIntegerWriteValue(value, 0, max, actualByteLength > 4 ? `>= 0 and < 2 ** ${actualByteLength * 8}` : `>= 0 and <= ${max}`, actualByteLength > 4);
		if (littleEndian) for (let i = 0; i < actualByteLength; i++) {
			buffer[actualOffset + i] = remaining & 255;
			remaining = Math.floor(remaining / 256);
		}
		else for (let i = actualByteLength - 1; i >= 0; i--) {
			buffer[actualOffset + i] = remaining & 255;
			remaining = Math.floor(remaining / 256);
		}
		return actualOffset + actualByteLength;
	}
	function writeVariableInt(buffer, value, offset, byteLength, littleEndian) {
		const actualByteLength = validateByteLength(byteLength);
		const min = -(2 ** (actualByteLength * 8 - 1));
		const max = 2 ** (actualByteLength * 8 - 1) - 1;
		const actualValue = validateIntegerWriteValue(value, min, max, actualByteLength > 4 ? `>= -(2 ** ${actualByteLength * 8 - 1}) and < 2 ** ${actualByteLength * 8 - 1}` : `>= ${min} and <= ${max}`, actualByteLength > 4);
		const fullRange = 2 ** (actualByteLength * 8);
		return writeVariableUint(buffer, actualValue < 0 ? actualValue + fullRange : actualValue, offset, actualByteLength, littleEndian);
	}
	Buffer$1.allocUnsafeSlow.bind(Buffer$1);
	const bufferPrototype = Buffer$1.prototype;
	{
		const __exactForOfIterator108 = [
			["readUint8", "readUInt8"],
			["readUint16LE", "readUInt16LE"],
			["readUint16BE", "readUInt16BE"],
			["readUint32LE", "readUInt32LE"],
			["readUint32BE", "readUInt32BE"],
			["readUintLE", "readUIntLE"],
			["readUintBE", "readUIntBE"],
			["readBigUint64LE", "readBigUInt64LE"],
			["readBigUint64BE", "readBigUInt64BE"],
			["writeUint8", "writeUInt8"],
			["writeUint16LE", "writeUInt16LE"],
			["writeUint16BE", "writeUInt16BE"],
			["writeUint32LE", "writeUInt32LE"],
			["writeUint32BE", "writeUInt32BE"],
			["writeUintLE", "writeUIntLE"],
			["writeUintBE", "writeUIntBE"],
			["writeBigUint64LE", "writeBigUInt64LE"],
			["writeBigUint64BE", "writeBigUInt64BE"]
		][Symbol.iterator]();
		for (;;) {
			const __exactForOfStep108 = __exactForOfIterator108.next();
			if (__exactForOfStep108.done) break;
			const [alias, original] = __exactForOfStep108.value;
			Object.defineProperty(bufferPrototype, alias, {
				value: bufferPrototype[original],
				writable: true,
				configurable: true
			});
		}
	}
	//#endregion
	//#region ../ibex-runtime-js/src/window/index.ts
	/**
	* Window API Implementation for Exact Runtime
	*
	* Provides browser-compatible window object for library compatibility.
	* Many npm packages check for `typeof window !== 'undefined'` to detect
	* browser environments.
	*
	* Note: This is NOT a DOM window - it's a compatibility shim that provides
	* commonly-used window properties that make sense in a native app context.
	*/
	init_defineProperty();
	let _Symbol$toStringTag$1;
	/**
	* Get screen dimensions from native or use defaults
	*/
	function getScreenDimensions() {
		if (typeof __exactGetScreenInfo === "function") {
			const info = __exactGetScreenInfo();
			return {
				width: info.width,
				height: info.height,
				scale: info.scale
			};
		}
		return {
			width: 393,
			height: 852,
			scale: 3
		};
	}
	/**
	* Create the screen object
	*/
	function createScreen() {
		getScreenDimensions();
		return {
			get width() {
				return getScreenDimensions().width;
			},
			get height() {
				return getScreenDimensions().height;
			},
			get availWidth() {
				return getScreenDimensions().width;
			},
			get availHeight() {
				return getScreenDimensions().height - 47;
			},
			colorDepth: 24,
			pixelDepth: 24,
			orientation: {
				type: "portrait-primary",
				angle: 0
			}
		};
	}
	/**
	* Create location stub
	*/
	function createLocation() {
		let currentHref = "exact://app/";
		const parseUrl = (href) => {
			try {
				const url = new URL(href);
				return {
					href: url.href,
					protocol: url.protocol,
					host: url.host,
					hostname: url.hostname,
					port: url.port,
					pathname: url.pathname,
					search: url.search,
					hash: url.hash,
					origin: url.origin
				};
			} catch {
				return {
					href,
					protocol: "exact:",
					host: "app",
					hostname: "app",
					port: "",
					pathname: "/",
					search: "",
					hash: "",
					origin: "exact://app"
				};
			}
		};
		return {
			get href() {
				return parseUrl(currentHref).href;
			},
			get protocol() {
				return parseUrl(currentHref).protocol;
			},
			get host() {
				return parseUrl(currentHref).host;
			},
			get hostname() {
				return parseUrl(currentHref).hostname;
			},
			get port() {
				return parseUrl(currentHref).port;
			},
			get pathname() {
				return parseUrl(currentHref).pathname;
			},
			get search() {
				return parseUrl(currentHref).search;
			},
			get hash() {
				return parseUrl(currentHref).hash;
			},
			get origin() {
				return parseUrl(currentHref).origin;
			},
			toString() {
				return parseUrl(currentHref).href;
			},
			reload: () => {
				console.warn("location.reload() is not supported in Exact runtime");
			},
			replace: (url) => {
				currentHref = url;
			},
			assign: (url) => {
				currentHref = url;
			}
		};
	}
	const mediaQueryLists = /* @__PURE__ */ new Set();
	function normalizeAppearanceState(value) {
		const candidate = typeof value === "object" && value !== null ? value : null;
		return {
			colorScheme: candidate?.colorScheme === "dark" ? "dark" : "light",
			reducedMotion: candidate?.reducedMotion === true
		};
	}
	let appearanceState = normalizeAppearanceState(globalThis.__exactAppearanceState);
	function updateAppearanceState(next) {
		appearanceState = normalizeAppearanceState(next);
		globalThis.__exactAppearanceState = { ...appearanceState };
		{
			const __exactForOfIterator109 = mediaQueryLists[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep109 = __exactForOfIterator109.next();
				if (__exactForOfStep109.done) break;
				__exactForOfStep109.value._syncFromAppearance();
			}
		}
	}
	/**
	* MediaQueryList for matchMedia support
	*/
	var MediaQueryList = class extends EventTarget {
		constructor(media) {
			super();
			_defineProperty(this, "media", void 0);
			_defineProperty(this, "_matches", void 0);
			_defineProperty(
				this,
				/** @deprecated Use addEventListener instead */
				"onchange",
				null
			);
			this.media = media;
			this._matches = this._evaluate(media);
			mediaQueryLists.add(this);
		}
		get matches() {
			return this._matches;
		}
		_evaluate(query) {
			const dims = getScreenDimensions();
			const minWidthMatch = query.match(/\(min-width:\s*(\d+)px\)/);
			if (minWidthMatch) return dims.width >= parseInt(minWidthMatch[1], 10);
			const maxWidthMatch = query.match(/\(max-width:\s*(\d+)px\)/);
			if (maxWidthMatch) return dims.width <= parseInt(maxWidthMatch[1], 10);
			const minHeightMatch = query.match(/\(min-height:\s*(\d+)px\)/);
			if (minHeightMatch) return dims.height >= parseInt(minHeightMatch[1], 10);
			const maxHeightMatch = query.match(/\(max-height:\s*(\d+)px\)/);
			if (maxHeightMatch) return dims.height <= parseInt(maxHeightMatch[1], 10);
			const orientationMatch = query.match(/\(orientation:\s*(portrait|landscape)\)/);
			if (orientationMatch) {
				const isPortrait = dims.height > dims.width;
				return orientationMatch[1] === "portrait" ? isPortrait : !isPortrait;
			}
			const colorSchemeMatch = query.match(/\(prefers-color-scheme:\s*(dark|light)\)/);
			if (colorSchemeMatch) return appearanceState.colorScheme === colorSchemeMatch[1];
			const reducedMotionMatch = query.match(/\(prefers-reduced-motion:\s*(reduce|no-preference)\)/);
			if (reducedMotionMatch) return reducedMotionMatch[1] === "reduce" ? appearanceState.reducedMotion : !appearanceState.reducedMotion;
			return false;
		}
		/** @deprecated Use addEventListener('change', ...) instead */
		addListener(callback) {
			this.addEventListener("change", callback);
		}
		/** @deprecated Use removeEventListener('change', ...) instead */
		removeListener(callback) {
			this.removeEventListener("change", callback);
		}
		_syncFromAppearance() {
			const nextMatches = this._evaluate(this.media);
			if (nextMatches === this._matches) return;
			this._matches = nextMatches;
			const event = new MediaQueryListEvent("change", {
				matches: nextMatches,
				media: this.media
			});
			this.dispatchEvent(event);
			this.onchange?.call(this, event);
		}
	};
	/**
	* MediaQueryListEvent for matchMedia change events
	*/
	var MediaQueryListEvent = class extends Event {
		constructor(type, init) {
			super(type);
			_defineProperty(this, "matches", void 0);
			_defineProperty(this, "media", void 0);
			this.matches = init?.matches ?? false;
			this.media = init?.media ?? "";
		}
	};
	_Symbol$toStringTag$1 = Symbol.toStringTag;
	/**
	* Window object implementation
	*/
	var Window = class extends EventTarget {
		constructor(..._args2) {
			super(..._args2);
			_defineProperty(this, "length", 0);
			_defineProperty(this, "name", "");
			_defineProperty(this, "opener", null);
			_defineProperty(this, "frameElement", null);
			_defineProperty(this, "closed", false);
			_defineProperty(this, "screen", createScreen());
			_defineProperty(this, "location", createLocation());
		}
		get window() {
			return this;
		}
		get self() {
			return this;
		}
		get globalThis() {
			return globalThis;
		}
		get frames() {
			return this;
		}
		get parent() {
			return this;
		}
		get top() {
			return this;
		}
		get devicePixelRatio() {
			return getScreenDimensions().scale;
		}
		get innerWidth() {
			return getScreenDimensions().width;
		}
		get innerHeight() {
			return getScreenDimensions().height;
		}
		get outerWidth() {
			return getScreenDimensions().width;
		}
		get outerHeight() {
			return getScreenDimensions().height;
		}
		get scrollX() {
			return 0;
		}
		get scrollY() {
			return 0;
		}
		get pageXOffset() {
			return 0;
		}
		get pageYOffset() {
			return 0;
		}
		get screenX() {
			return 0;
		}
		get screenY() {
			return 0;
		}
		get screenLeft() {
			return 0;
		}
		get screenTop() {
			return 0;
		}
		get visualViewport() {
			const dims = getScreenDimensions();
			return {
				width: dims.width,
				height: dims.height,
				scale: 1,
				offsetLeft: 0,
				offsetTop: 0
			};
		}
		get isSecureContext() {
			return true;
		}
		get origin() {
			return "exact://app";
		}
		/**
		* Match media query
		*/
		matchMedia(query) {
			return new MediaQueryList(query);
		}
		/**
		* Get computed style (stub - no DOM)
		*/
		getComputedStyle(_element, _pseudoElt) {
			console.warn("getComputedStyle() is not supported in Exact runtime (no DOM)");
			return new Proxy({}, { get: () => "" });
		}
		/**
		* Get selection (stub - no DOM)
		*/
		getSelection() {
			return null;
		}
		/**
		* Scroll methods (no-op in native)
		*/
		scroll(_x, _y) {}
		scrollTo(_x, _y) {}
		scrollBy(_x, _y) {}
		/**
		* Focus/blur (no-op in native)
		*/
		focus() {}
		blur() {}
		/**
		* Print (not supported)
		*/
		print() {
			console.warn("print() is not supported in Exact runtime");
		}
		/**
		* Stop (no-op)
		*/
		stop() {}
		/**
		* Open (not supported - no popup windows)
		*/
		open(_url, _target, _features) {
			console.warn("window.open() is not supported in Exact runtime");
			return null;
		}
		/**
		* Close (not supported)
		*/
		close() {
			console.warn("window.close() is not supported in Exact runtime");
		}
		/**
		* Alert dialog
		* TODO: Implement native alert
		*/
		alert(message) {
			console.log("[Alert]", String(message ?? ""));
		}
		/**
		* Confirm dialog
		* TODO: Implement native confirm - for now always returns false
		*/
		confirm(_message) {
			console.warn("confirm() is not fully supported in Exact runtime");
			return false;
		}
		/**
		* Prompt dialog
		* TODO: Implement native prompt - for now always returns null
		*/
		prompt(_message, _defaultValue) {
			console.warn("prompt() is not fully supported in Exact runtime");
			return null;
		}
		/**
		* Post message (for compatibility)
		*/
		postMessage(message, _targetOrigin, _transfer) {
			const event = new MessageEvent("message", {
				data: message,
				origin: this.origin,
				source: this
			});
			this.dispatchEvent(event);
		}
		/**
		* Request animation frame - forward to global
		*/
		requestAnimationFrame(callback) {
			return globalThis.requestAnimationFrame(callback);
		}
		/**
		* Cancel animation frame - forward to global
		*/
		cancelAnimationFrame(handle) {
			return globalThis.cancelAnimationFrame(handle);
		}
		/**
		* Request idle callback - forward to global
		*/
		requestIdleCallback(callback, options) {
			return globalThis.requestIdleCallback(callback, options);
		}
		/**
		* Cancel idle callback - forward to global
		*/
		cancelIdleCallback(handle) {
			return globalThis.cancelIdleCallback(handle);
		}
		/**
		* Set timeout - forward to global
		*/
		setTimeout(handler, timeout, ...args) {
			return globalThis.setTimeout(handler, timeout, ...args);
		}
		/**
		* Clear timeout - forward to global
		*/
		clearTimeout(id) {
			return globalThis.clearTimeout(id);
		}
		/**
		* Set interval - forward to global
		*/
		setInterval(handler, timeout, ...args) {
			return globalThis.setInterval(handler, timeout, ...args);
		}
		/**
		* Clear interval - forward to global
		*/
		clearInterval(id) {
			return globalThis.clearInterval(id);
		}
		/**
		* Queue microtask - forward to global
		*/
		queueMicrotask(callback) {
			return globalThis.queueMicrotask(callback);
		}
		/**
		* Create image bitmap (not supported - no DOM)
		*/
		createImageBitmap(..._args) {
			return Promise.reject(/* @__PURE__ */ new Error("createImageBitmap() is not supported in Exact runtime"));
		}
		/**
		* Fetch - forward to global
		*/
		fetch(input, init) {
			return globalThis.fetch(input, init);
		}
		/**
		* btoa - forward to global
		*/
		btoa(data) {
			return globalThis.btoa(data);
		}
		/**
		* atob - forward to global
		*/
		atob(data) {
			return globalThis.atob(data);
		}
		/**
		* structuredClone - forward to global
		*/
		structuredClone(value, options) {
			return globalThis.structuredClone(value, options);
		}
		get [_Symbol$toStringTag$1]() {
			return "Window";
		}
	};
	const window$1 = new Window();
	globalThis.__exactWindowNotifyMediaChange = (next) => {
		updateAppearanceState(next);
	};
	//#endregion
	//#region ../ibex-runtime-js/src/node/process.ts
	/**
	* process object implementation for Exact runtime (Node.js compatibility)
	* 
	* Provides a subset of Node.js process API for compatibility with
	* libraries that check for Node.js environment.
	*/
	init_defineProperty();
	let _Symbol$toStringTag;
	var _processCwd = "/";
	const _nextTickQueue = [];
	let _nextTickScheduled = false;
	let _nextTickHooksInstalled = false;
	const _nativeGlobalNextTick = typeof globalThis.nextTick === "function" ? globalThis.nextTick.bind(globalThis) : void 0;
	const _nativeProcessNextTick = typeof globalThis.process?.nextTick === "function" ? globalThis.process.nextTick.bind(globalThis.process) : void 0;
	const _nativeProcessKill = typeof globalThis.process?.kill === "function" ? globalThis.process.kill.bind(globalThis.process) : void 0;
	const _nativeArch = typeof globalThis.process?.arch === "string" ? globalThis.process.arch : void 0;
	const _nativeGetuid = typeof globalThis.process?.getuid === "function" ? globalThis.process.getuid.bind(globalThis.process) : void 0;
	const _nativeGetgid = typeof globalThis.process?.getgid === "function" ? globalThis.process.getgid.bind(globalThis.process) : void 0;
	const _nativeGeteuid = typeof globalThis.process?.geteuid === "function" ? globalThis.process.geteuid.bind(globalThis.process) : void 0;
	const _nativeGetegid = typeof globalThis.process?.getegid === "function" ? globalThis.process.getegid.bind(globalThis.process) : void 0;
	const _nativePromiseThen = Promise.prototype.then;
	const _nativeQueueMicrotask = typeof queueMicrotask === "function" ? queueMicrotask.bind(globalThis) : void 0;
	const SIGNAL_NAME_TO_NUMBER = {
		SIGHUP: 1,
		SIGINT: 2,
		SIGQUIT: 3,
		SIGILL: 4,
		SIGTRAP: 5,
		SIGABRT: 6,
		SIGIOT: 6,
		SIGBUS: 7,
		SIGFPE: 8,
		SIGKILL: 9,
		SIGUSR1: 10,
		SIGSEGV: 11,
		SIGUSR2: 12,
		SIGPIPE: 13,
		SIGALRM: 14,
		SIGTERM: 15,
		SIGCHLD: 17,
		SIGCONT: 18,
		SIGSTOP: 19,
		SIGTSTP: 20,
		SIGTTIN: 21,
		SIGTTOU: 22,
		SIGURG: 23,
		SIGXCPU: 24,
		SIGXFSZ: 25,
		SIGVTALRM: 26,
		SIGPROF: 27,
		SIGWINCH: 28,
		SIGIO: 29,
		SIGPOLL: 29,
		SIGPWR: 30,
		SIGSYS: 31
	};
	const USER_CREDENTIALS = {
		root: 0,
		nobody: 65534
	};
	const GROUP_CREDENTIALS = {
		root: 0,
		nobody: 65534,
		nogroup: 65534
	};
	const CANONICAL_ALLOWED_NODE_ENVIRONMENT_FLAGS = [
		"--perf-basic-prof",
		"--stack-trace-limit",
		"-r",
		"--debug-arraybuffer-allocations",
		"--no-debug-arraybuffer-allocations",
		"--es-module-specifier-resolution",
		"--experimental-fetch",
		"--experimental-global-customevent",
		"--experimental-global-webcrypto",
		"--experimental-quic",
		"--experimental-report",
		"--experimental-wasm-modules",
		"--experimental-worker",
		"--loader",
		"--no-node-snapshot",
		"--node-snapshot",
		"--no-trace-promises",
		"--no-verify-base-objects",
		"--trace-promises",
		"--verify-base-objects"
	];
	const _nativeSetAdd = Set.prototype.add;
	const _nativeSetClear = Set.prototype.clear;
	const _nativeSetDelete = Set.prototype.delete;
	const _nativeSetHas = Set.prototype.has;
	let _immutableAllowedNodeEnvironmentFlags = null;
	let _dateToStringPatched = false;
	const _nativeDateToString = Date.prototype.toString;
	function makeInvalidObjectDefinePropertyError(message) {
		const err = new TypeError(message);
		err.code = "ERR_INVALID_OBJECT_DEFINE_PROPERTY";
		return err;
	}
	function normalizeAllowedNodeEnvironmentFlag(flag) {
		if (typeof flag !== "string" || flag.length === 0) return null;
		let dashCount = 0;
		while (dashCount < flag.length && flag.charCodeAt(dashCount) === 45) dashCount += 1;
		let name = flag.slice(dashCount);
		if (name.length === 0) return null;
		const equalsIndex = name.indexOf("=");
		if (equalsIndex !== -1) name = name.slice(0, equalsIndex);
		name = name.replace(/_/g, "-");
		if (dashCount === 0) return name.length === 1 ? `-${name}` : `--${name}`;
		if (dashCount === 1) return `-${name}`;
		if (dashCount === 2) return `--${name}`;
		return `${"-".repeat(dashCount)}${name}`;
	}
	function createAllowedNodeEnvironmentFlags() {
		if (!_immutableAllowedNodeEnvironmentFlags) {
			const allowedFlags = new Set(CANONICAL_ALLOWED_NODE_ENVIRONMENT_FLAGS);
			if (!Set.prototype.__exactImmutableAllowedFlagsPatched) {
				Object.defineProperty(Set.prototype, "add", {
					value(value) {
						if (this === _immutableAllowedNodeEnvironmentFlags) return this;
						return _nativeSetAdd.call(this, value);
					},
					writable: true,
					configurable: true
				});
				Object.defineProperty(Set.prototype, "delete", {
					value(value) {
						if (this === _immutableAllowedNodeEnvironmentFlags) return false;
						return _nativeSetDelete.call(this, value);
					},
					writable: true,
					configurable: true
				});
				Object.defineProperty(Set.prototype, "clear", {
					value() {
						if (this === _immutableAllowedNodeEnvironmentFlags) return;
						return _nativeSetClear.call(this);
					},
					writable: true,
					configurable: true
				});
				Object.defineProperty(Set.prototype, "__exactImmutableAllowedFlagsPatched", {
					value: true,
					writable: false,
					configurable: true
				});
			}
			Object.defineProperty(allowedFlags, "has", {
				value(value) {
					const normalized = normalizeAllowedNodeEnvironmentFlag(value);
					if (normalized === null) return false;
					return _nativeSetHas.call(this, normalized);
				},
				writable: false,
				configurable: false
			});
			_immutableAllowedNodeEnvironmentFlags = Object.freeze(allowedFlags);
		}
		return _immutableAllowedNodeEnvironmentFlags;
	}
	function _normalizeResolvedPath(path) {
		let rest = path;
		let prefix = "";
		let absolute = false;
		if (/^[A-Za-z]:[\\/]/.test(rest)) {
			prefix = rest.slice(0, 2);
			rest = rest.slice(2);
			absolute = true;
		} else if (rest.startsWith("/")) {
			rest = rest.slice(1);
			absolute = true;
		}
		const parts = rest.split(/[\\/]+/);
		const normalized = [];
		for (const part of parts) {
			if (!part || part === ".") continue;
			if (part === "..") {
				if (normalized.length > 0) normalized.pop();
				else if (!absolute) normalized.push(part);
				continue;
			}
			normalized.push(part);
		}
		const joined = normalized.join("/");
		if (prefix) return joined ? `${prefix}/${joined}` : `${prefix}/`;
		if (absolute) return joined ? `/${joined}` : "/";
		return joined || ".";
	}
	function _drainNextTickQueue() {
		if (_nextTickQueue.length === 0) {
			_nextTickScheduled = false;
			return;
		}
		_nextTickScheduled = false;
		while (_nextTickQueue.length > 0) {
			const batch = _nextTickQueue.splice(0, _nextTickQueue.length);
			{
				const __exactForOfIterator110 = batch[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep110 = __exactForOfIterator110.next();
					if (__exactForOfStep110.done) break;
					const entry = __exactForOfStep110.value;
					entry.callback(...entry.args);
				}
			}
		}
	}
	function _wrapNextTickAwareCallback(callback) {
		if (typeof callback !== "function") return;
		return function(...args) {
			_drainNextTickQueue();
			return callback.apply(this, args);
		};
	}
	function _installNextTickHooks() {
		if (_nextTickHooksInstalled || _nativeProcessNextTick || _nativeGlobalNextTick) return;
		_nextTickHooksInstalled = true;
		Promise.prototype.then = function then(onFulfilled, onRejected) {
			return _nativePromiseThen.call(this, _wrapNextTickAwareCallback(onFulfilled), _wrapNextTickAwareCallback(onRejected));
		};
		if (_nativeQueueMicrotask) try {
			globalThis.queueMicrotask = (callback) => {
				_nativeQueueMicrotask(() => {
					_drainNextTickQueue();
					callback();
				});
			};
		} catch (_err) {}
	}
	function _scheduleNextTickDrain() {
		if (_nextTickScheduled) return;
		_nextTickScheduled = true;
		_installNextTickHooks();
		if (_nativeQueueMicrotask) {
			_nativeQueueMicrotask(_drainNextTickQueue);
			return;
		}
		Promise.resolve().then(_drainNextTickQueue);
	}
	function _monotonicTimeNs() {
		const perf = globalThis.performance;
		if (perf && typeof perf.now === "function") {
			const originMs = typeof perf.timeOrigin === "number" && Number.isFinite(perf.timeOrigin) ? perf.timeOrigin : Date.now();
			return BigInt(Math.floor((originMs + perf.now()) * 1e6));
		}
		return BigInt(Date.now()) * 1000000n;
	}
	function _stringifyPathPart(path) {
		if (typeof path === "string") return path;
		if (typeof Buffer !== "undefined" && Buffer.isBuffer?.(path)) return path.toString();
		return String(path);
	}
	function _normalizeCwdPath(value) {
		const path = _stringifyPathPart(value);
		if (path.length === 0) return "/";
		if (path.charAt(0) === "/" || /^[A-Za-z]:[\\/]/.test(path)) return _normalizeResolvedPath(path);
		return _normalizeResolvedPath(`/${path}`);
	}
	function _resolveCwd(path) {
		if (path.charAt(0) === "/" || /^[A-Za-z]:[\\/]/.test(path)) return _normalizeResolvedPath(path);
		const cwd = typeof process$1 === "object" && process$1 && typeof process$1.cwd === "function" ? process$1.cwd() : "/";
		if (cwd.charAt(cwd.length - 1) !== "/") return _normalizeResolvedPath(`${cwd}/${path}`);
		return _normalizeResolvedPath(`${cwd}${path}`);
	}
	function _readNativeCwd() {
		if (typeof __exactGetCwd !== "function") return null;
		try {
			const value = __exactGetCwd();
			if (typeof value === "string" && value.length > 0) return _normalizeCwdPath(value);
		} catch (_err) {}
		return null;
	}
	function _coerceChdirError(err, origArg) {
		const fallbackErrorMap = {
			EACCES: 13,
			EINVAL: 22,
			ENOENT: 2,
			ENOTDIR: 20
		};
		const currentCwd = (typeof __exactGetCwd === "function" ? __exactGetCwd() : null) || _processCwd;
		if (err && typeof err === "object" && typeof err.message === "string") {
			const message = err.message;
			const lower = message.toLowerCase();
			let code;
			if (lower.includes("no such file") || lower.includes("does not exist")) code = "ENOENT";
			else if (lower.includes("permission denied")) code = "EACCES";
			else if (lower.includes("not a directory")) code = "ENOTDIR";
			if (code) {
				const errMsg = `${code}: ${{
					ENOENT: "no such file or directory",
					EACCES: "permission denied",
					ENOTDIR: "not a directory"
				}[code] || message}, chdir '${currentCwd}' -> '${origArg}'`;
				const mapped = new Error(errMsg);
				mapped.code = code;
				const errno = fallbackErrorMap[code];
				if (errno !== void 0) mapped.errno = -errno;
				mapped.syscall = "chdir";
				mapped.path = currentCwd;
				mapped.dest = origArg;
				return mapped;
			}
		}
		const fallback = /* @__PURE__ */ new Error("process.chdir failed");
		fallback.code = "EINVAL";
		fallback.syscall = "chdir";
		fallback.path = currentCwd;
		fallback.dest = origArg;
		return fallback;
	}
	function _deepFreeze(value) {
		if (!value || typeof value !== "object" && typeof value !== "function" || Object.isFrozen(value)) return value;
		Object.freeze(value);
		{
			const __exactForOfIterator111 = Object.getOwnPropertyNames(value)[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep111 = __exactForOfIterator111.next();
				if (__exactForOfStep111.done) break;
				const key = __exactForOfStep111.value;
				_deepFreeze(value[key]);
			}
		}
		return value;
	}
	function _getConfiguredTimeZone() {
		const timeZone = (globalThis.process?.env)?.TZ;
		return typeof timeZone === "string" && timeZone.length > 0 ? timeZone : void 0;
	}
	function _getTimeZoneOffsetMinutes(date, timeZone) {
		const parts = new Intl.DateTimeFormat("en-CA", {
			timeZone,
			year: "numeric",
			month: "2-digit",
			day: "2-digit",
			hour: "2-digit",
			minute: "2-digit",
			second: "2-digit",
			hourCycle: "h23"
		}).formatToParts(date);
		const values = {};
		for (const part of parts) {
			if (part.type === "literal") continue;
			values[part.type] = Number(part.value);
		}
		const zonedAsUtc = Date.UTC(values.year, (values.month ?? 1) - 1, values.day ?? 1, values.hour ?? 0, values.minute ?? 0, values.second ?? 0);
		return Math.round((zonedAsUtc - date.getTime()) / 6e4);
	}
	function _formatDateForConfiguredTimeZone(date, timeZone) {
		if (Number.isNaN(date.getTime()) || typeof Intl === "undefined" || typeof Intl.DateTimeFormat !== "function") return null;
		try {
			const match = new Intl.DateTimeFormat("en-US", {
				timeZone,
				weekday: "short",
				month: "short",
				day: "2-digit",
				year: "numeric",
				hour: "2-digit",
				minute: "2-digit",
				second: "2-digit",
				hourCycle: "h23"
			}).format(date).match(/^([A-Za-z]{3}), ([A-Za-z]{3}) (\d{2}), (\d{4}) at (\d{2}:\d{2}:\d{2})$/);
			if (!match) return null;
			const timeZoneName = new Intl.DateTimeFormat("en-US", {
				timeZone,
				timeZoneName: "long"
			}).format(date).split(", ").slice(1).join(", ") || timeZone;
			const totalOffsetMinutes = _getTimeZoneOffsetMinutes(date, timeZone);
			const absoluteOffsetMinutes = Math.abs(totalOffsetMinutes);
			const offsetHours = Math.floor(absoluteOffsetMinutes / 60).toString().padStart(2, "0");
			const offsetMinutes = (absoluteOffsetMinutes % 60).toString().padStart(2, "0");
			const sign = totalOffsetMinutes >= 0 ? "+" : "-";
			return `${match[1]} ${match[2]} ${match[3]} ${match[4]} ${match[5]} GMT${sign}${offsetHours}${offsetMinutes} (${timeZoneName})`;
		} catch {
			return null;
		}
	}
	function _installTimeZoneAwareDateToString() {
		if (_dateToStringPatched) return;
		_dateToStringPatched = true;
		Date.prototype.toString = function exactToString() {
			const timeZone = _getConfiguredTimeZone();
			if (!timeZone) return _nativeDateToString.call(this);
			return _formatDateForConfiguredTimeZone(this, timeZone) ?? _nativeDateToString.call(this);
		};
	}
	const PROCESS_DEFAULT_CONFIG = _deepFreeze({
		target_defaults: {
			cflags: [],
			default_configuration: "Release",
			defines: [],
			include_dirs: [],
			libraries: []
		},
		variables: {
			asan: 0,
			coverage: false,
			debug_nghttp2: false,
			enable_lto: false,
			enable_pgo_generate: false,
			enable_pgo_use: false,
			error_on_warn: false,
			force_dynamic_crt: 0,
			host_arch: "arm64",
			icu_data_in: "../../deps/icu-tmp/icudt76l.dat",
			icu_endianness: "l",
			icu_gyp_path: "tools/icu/icu-generic.gyp",
			icu_path: "deps/icu-small",
			icu_small: false,
			icu_ver_major: "76",
			is_debug: 0,
			llvm_version: "0.0",
			napi_build_version: "9",
			node_builtin_shareable_builtins: [],
			node_byteorder: "little",
			node_debug_lib: false,
			node_enable_d8: false,
			node_enable_v8_vtunejit: false,
			node_fipsinstall: false,
			node_install_corepack: true,
			node_install_npm: true,
			node_library_files: [],
			node_module_version: 131,
			node_no_browser_globals: false,
			node_prefix: "/",
			node_release_urlbase: "",
			node_section_ordering_info: "",
			node_shared: false,
			node_shared_brotli: false,
			node_shared_cares: false,
			node_shared_http_parser: false,
			node_shared_libuv: false,
			node_shared_nghttp2: false,
			node_shared_openssl: false,
			node_shared_zlib: false,
			node_tag: "",
			node_target_type: "executable",
			node_use_bundled_v8: true,
			node_use_node_code_cache: true,
			node_use_node_snapshot: true,
			node_use_openssl: true,
			node_use_v8_platform: true,
			node_with_ltcg: false,
			node_without_node_options: false,
			openssl_is_fips: false,
			openssl_quic: true,
			ossfuzz: false,
			shlib_suffix: "so.131",
			target_arch: "arm64",
			v8_enable_31bit_smis_on_64bit_arch: 0,
			v8_enable_gdbjit: 0,
			v8_enable_hugepage: 0,
			v8_enable_i18n_support: 1,
			v8_enable_inspector: 1,
			v8_enable_javascript_promise_hooks: 1,
			v8_enable_lite_mode: 0,
			v8_enable_object_print: 1,
			v8_enable_pointer_compression: 0,
			v8_enable_shared_ro_heap: 1,
			v8_enable_short_builtin_calls: 1,
			v8_enable_webassembly: 1,
			v8_no_strict_aliasing: 1,
			v8_optimized_debug: 1,
			v8_promise_internal_field_count: 1,
			v8_random_seed: 0,
			v8_trace_maps: 0,
			v8_use_siphash: 1,
			want_separate_host_toolset: 0
		}
	});
	const PROCESS_VERSIONS = createProcessVersions();
	const PROCESS_RELEASE = createProcessRelease(PROCESS_VERSIONS.node ?? RUNTIME_VERSIONS.find(([key]) => key === "node")?.[1] ?? "0.0.0");
	function createProcessVersions() {
		const versions = {};
		const entries = [...RUNTIME_VERSIONS];
		if (isCompatModeEnabled("bun")) entries.push(["bun", BUN_COMPAT_VERSION]);
		if (isCompatFixtureMode()) entries.push(["acorn", "8.15.0"], ["ada", "2.9.2"], ["ares", "1.34.4"], ["brotli", "1.1.0"], ["cjs_module_lexer", "2.1.0"], ["cldr", "46.0"], ["icu", "76.1"], ["llhttp", "9.3.0"], ["modules", "131"], ["napi", "9"], ["nbytes", "0.1.1"], ["ncrypto", "0.0.1"], ["nghttp2", "1.64.0"], ["nghttp3", "1.6.0"], ["ngtcp2", "1.9.1"], ["openssl", "3.4.1"], ["simdjson", "3.13.0"], ["simdutf", "6.4.2"], ["tz", "2025a"], ["unicode", "16.0"], ["uv", "1.50.0"], ["uvwasi", "0.0.21"], ["v8", "13.6.233.8-node.26"], ["zlib", "1.3.1.1-motley-82a5fec"], ["zstd", "1.5.7"]);
		{
			const __exactForOfIterator112 = entries[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep112 = __exactForOfIterator112.next();
				if (__exactForOfStep112.done) break;
				const [key, value] = __exactForOfStep112.value;
				Object.defineProperty(versions, key, {
					value,
					writable: false,
					enumerable: true,
					configurable: true
				});
			}
		}
		return versions;
	}
	function isCompatModeEnabled(mode) {
		try {
			const g = globalThis;
			const modes = g.__exactCompatModes;
			if (Array.isArray(modes) && modes.indexOf(mode) !== -1) return true;
			if (mode === "bun") {
				const env = g.__exactHostEnv ?? g.process?.env;
				if (env && env.EXACT_COMPAT_BUN === "1") return true;
			}
		} catch (_) {}
		return false;
	}
	function isCompatFixtureMode() {
		try {
			const g = globalThis;
			const env = g.__exactHostEnv ?? g.process?.env;
			return !!(env && env.EXACT_COMPAT_TEST);
		} catch (_) {
			return false;
		}
	}
	function createProcessRelease(nodeVersion) {
		const release = { name: RELEASE_NAME };
		const [majorText = "", minorText = ""] = String(nodeVersion).split(".");
		const major = Number.parseInt(majorText, 10);
		const minor = Number.parseInt(minorText, 10);
		for (const [ltsMajor, ltsMinor, ltsName] of [
			[
				4,
				2,
				"Argon"
			],
			[
				6,
				9,
				"Boron"
			],
			[
				8,
				9,
				"Carbon"
			],
			[
				10,
				13,
				"Dubnium"
			],
			[
				12,
				13,
				"Erbium"
			],
			[
				14,
				15,
				"Fermium"
			],
			[
				16,
				13,
				"Gallium"
			],
			[
				18,
				12,
				"Hydrogen"
			],
			[
				20,
				9,
				"Iron"
			],
			[
				22,
				11,
				"Jod"
			],
			[
				24,
				11,
				"Krypton"
			]
		]) if (major === ltsMajor && minor >= ltsMinor) {
			release.lts = ltsName;
			break;
		}
		return release;
	}
	/**
	* Create a process stream (stdout/stderr/stdin) with basic EventEmitter support.
	* This ensures pipe() and other stream operations work even before stream-enhance runs.
	*/
	function _makeProcessStream(fd, isTTY, writeFn) {
		const listeners = {};
		function coerceChunk(chunk) {
			if (chunk == null) return "";
			if (typeof chunk === "string") return chunk;
			if (typeof Buffer !== "undefined" && Buffer.isBuffer && Buffer.isBuffer(chunk)) return chunk.toString();
			if (chunk instanceof Uint8Array) {
				if (typeof TextDecoder !== "undefined") return new TextDecoder().decode(chunk);
				let out = "";
				for (let i = 0; i < chunk.length; i++) out += String.fromCharCode(chunk[i]);
				return out;
			}
			return String(chunk);
		}
		const stream = {
			fd,
			isTTY,
			readable: fd === 0,
			readableEnded: false,
			readableFlowing: null,
			readableLength: 0,
			writable: fd > 0,
			writableEnded: false,
			writableFinished: false,
			destroyed: false,
			_encoding: null,
			_paused: true,
			_ended: false,
			_pollTimer: 0,
			on(event, fn) {
				if (!listeners[event]) listeners[event] = [];
				listeners[event].push({
					fn,
					once: false
				});
				return stream;
			},
			addListener(event, fn) {
				return stream.on(event, fn);
			},
			once(event, fn) {
				if (!listeners[event]) listeners[event] = [];
				listeners[event].push({
					fn,
					once: true
				});
				return stream;
			},
			emit(event, ...args) {
				const list = listeners[event];
				if (!list) return false;
				const keep = [];
				for (let i = 0; i < list.length; i++) {
					if (typeof list[i].fn === "function") list[i].fn.apply(stream, args);
					if (!list[i].once) keep.push(list[i]);
				}
				listeners[event] = keep;
				return true;
			},
			removeListener(event, fn) {
				const list = listeners[event];
				if (list) listeners[event] = list.filter((l) => l.fn !== fn);
				return stream;
			},
			off(event, fn) {
				return stream.removeListener(event, fn);
			},
			removeAllListeners(event) {
				if (event) listeners[event] = [];
				else for (const k in listeners) listeners[k] = [];
				return stream;
			},
			setMaxListeners() {
				return stream;
			},
			getMaxListeners() {
				return 10;
			},
			listenerCount(event) {
				return listeners[event] ? listeners[event].length : 0;
			},
			listeners(event) {
				return (listeners[event] || []).map((l) => l.fn);
			},
			prependListener(event, fn) {
				if (!listeners[event]) listeners[event] = [];
				listeners[event].unshift({
					fn,
					once: false
				});
				return stream;
			},
			prependOnceListener(event, fn) {
				if (!listeners[event]) listeners[event] = [];
				listeners[event].unshift({
					fn,
					once: true
				});
				return stream;
			},
			rawListeners(event) {
				return stream.listeners(event);
			},
			eventNames() {
				return Object.keys(listeners).filter((k) => listeners[k] && listeners[k].length > 0);
			},
			destroy(err) {
				stream.destroyed = true;
				if (fd === 0) {
					stream.readable = false;
					stream.readableEnded = true;
					stream.readableFlowing = false;
				} else {
					stream.writableEnded = true;
					stream.writableFinished = true;
				}
				if (err) stream.emit("error", err);
				stream.emit("close");
				return stream;
			}
		};
		if (fd === 0) {
			stream.setEncoding = function(enc) {
				stream._encoding = enc;
				return stream;
			};
			stream.resume = function() {
				stream.readable = true;
				stream._paused = false;
				stream.readableFlowing = true;
				if (!stream._ended && !stream._pollTimer && typeof globalThis.__exactStdinRead === "function") (function pollStdin() {
					if (stream._paused || stream._ended || stream.destroyed) return;
					const stdinRead = globalThis.__exactStdinRead;
					const data = stdinRead(4096);
					if (data === null) {
						stream._pollTimer = setTimeout(pollStdin, 25);
						return;
					}
					if (data === "") {
						stream._ended = true;
						stream._pollTimer = 0;
						stream.readableEnded = true;
						stream.emit("end");
						stream.emit("close");
						return;
					}
					stream._pollTimer = 0;
					let chunk = data;
					if (stream._encoding && typeof Buffer !== "undefined" && Buffer.from) chunk = Buffer.from(data, "binary").toString(stream._encoding);
					stream.readableLength += chunk.length || 0;
					stream.emit("data", chunk);
					stream.readableLength = 0;
					if (!stream._paused && !stream._ended) stream._pollTimer = setTimeout(pollStdin, 10);
				})();
				return stream;
			};
			stream.pause = function() {
				stream._paused = true;
				stream.readableFlowing = false;
				if (stream._pollTimer) {
					clearTimeout(stream._pollTimer);
					stream._pollTimer = 0;
				}
				return stream;
			};
			stream.read = function(size) {
				const stdinRead = globalThis.__exactStdinRead;
				if (typeof stdinRead !== "function") return null;
				const data = stdinRead(size || 4096);
				if (data === "") return null;
				if (stream._encoding && data && typeof Buffer !== "undefined" && Buffer.from) return Buffer.from(data, "binary").toString(stream._encoding);
				return data;
			};
			stream.pipe = function(destination) {
				stream.on("data", (chunk) => {
					destination.write(chunk);
				});
				stream.once("end", () => {
					destination.end?.();
				});
				stream.once("error", (error) => {
					if (typeof destination.emit === "function") destination.emit("error", error);
					else destination.destroy?.(error);
				});
				stream.resume();
				return destination;
			};
			const originalOn = stream.on;
			stream.on = function(event, fn) {
				const result = originalOn.call(stream, event, fn);
				if (event === "data" && !stream._ended && stream._paused) stream.resume();
				return result;
			};
			stream.addListener = stream.on;
			const originalOnce = stream.once;
			stream.once = function(event, fn) {
				const result = originalOnce.call(stream, event, fn);
				if (event === "data" && !stream._ended && stream._paused) stream.resume();
				return result;
			};
		}
		if (writeFn) {
			const _exactWriteImpl = function(chunk, encoding, callback) {
				if (typeof encoding === "function") {
					callback = encoding;
					encoding = void 0;
				}
				const result = writeFn(coerceChunk(chunk));
				if (typeof callback === "function") callback();
				return result;
			};
			_exactWriteImpl.__exactNativeWrite = true;
			stream.write = _exactWriteImpl;
			stream.end = function(chunk, encoding, callback) {
				if (typeof chunk === "function") {
					callback = chunk;
					chunk = void 0;
				}
				if (typeof encoding === "function") {
					callback = encoding;
					encoding = void 0;
				}
				if (chunk != null) stream.write(chunk, encoding);
				stream.writableEnded = true;
				stream.writableFinished = true;
				stream.emit("finish");
				stream.emit("close");
				if (typeof callback === "function") callback();
			};
		}
		return stream;
	}
	/**
	* Create a Proxy-based env object that reads from native
	*/
	function createEnvProxy() {
		const jsEnv = { NODE_ENV: "development" };
		let nativeCache = null;
		function refreshNativeCache() {
			if (typeof __exactGetAllEnv === "function") nativeCache = { ...__exactGetAllEnv() };
			else if (nativeCache === null) nativeCache = {};
			return nativeCache ?? {};
		}
		function getNativeValue(key) {
			if (nativeCache && Object.prototype.hasOwnProperty.call(nativeCache, key)) return nativeCache[key];
			if (typeof __exactGetEnv === "function") {
				const value = __exactGetEnv(key);
				if (value !== void 0) refreshNativeCache()[key] = value;
				return value;
			}
			return refreshNativeCache()[key];
		}
		return new Proxy(jsEnv, {
			get(target, prop) {
				if (typeof prop === "symbol") return;
				if (prop === "toJSON") return () => {
					const all = { ...target };
					Object.assign(all, refreshNativeCache());
					return all;
				};
				const nativeValue = getNativeValue(prop);
				if (nativeValue !== void 0) return nativeValue;
				return target[prop];
			},
			set(target, prop, value) {
				if (typeof prop === "symbol") throw new TypeError("Cannot convert a Symbol value to a string");
				if (typeof value === "symbol") throw new TypeError("Cannot convert a Symbol value to a string");
				const key = prop;
				if (key.length === 0) return true;
				const normalized = String(value);
				target[key] = normalized;
				if (nativeCache) nativeCache[key] = normalized;
				return true;
			},
			has(target, prop) {
				if (typeof prop === "symbol") return false;
				if (getNativeValue(prop) !== void 0) return true;
				return prop in target;
			},
			deleteProperty(target, prop) {
				if (typeof prop === "symbol") return true;
				const key = prop;
				delete target[key];
				if (nativeCache) delete nativeCache[key];
				return true;
			},
			ownKeys(target) {
				const keys = new Set(Object.keys(target));
				Object.keys(refreshNativeCache()).forEach((k) => keys.add(k));
				return Array.from(keys);
			},
			defineProperty(target, prop, descriptor) {
				if (typeof prop === "symbol") throw makeInvalidObjectDefinePropertyError("'process.env' only accepts a configurable, writable, and enumerable data descriptor");
				if (typeof descriptor.get === "function" || typeof descriptor.set === "function") throw makeInvalidObjectDefinePropertyError("'process.env' does not accept an accessor(getter/setter) descriptor");
				if (descriptor.configurable !== true || descriptor.writable !== true || descriptor.enumerable !== true) throw makeInvalidObjectDefinePropertyError("'process.env' only accepts a configurable, writable, and enumerable data descriptor");
				this.set(target, prop, descriptor.value, target);
				return true;
			},
			getOwnPropertyDescriptor(target, prop) {
				if (typeof prop === "symbol") return;
				const key = prop;
				if (Object.prototype.hasOwnProperty.call(target, key)) return {
					value: target[key],
					writable: true,
					enumerable: true,
					configurable: true
				};
				const nativeValue = getNativeValue(key);
				if (nativeValue !== void 0) return {
					value: nativeValue,
					writable: true,
					enumerable: true,
					configurable: true
				};
			}
		});
	}
	_Symbol$toStringTag = Symbol.toStringTag;
	/**
	* Process object providing Node.js-like environment info.
	*/
	var Process = class {
		constructor() {
			_defineProperty(this, "_maxListeners", 10);
			_defineProperty(this, "env", createEnvProxy());
			_defineProperty(this, "config", PROCESS_DEFAULT_CONFIG);
			_defineProperty(this, "_exitCode", void 0);
			_defineProperty(this, "_uid", readNativeProcessId(_nativeGetuid, 0));
			_defineProperty(this, "_gid", readNativeProcessId(_nativeGetgid, 0));
			_defineProperty(this, "_euid", readNativeProcessId(_nativeGeteuid, this._uid));
			_defineProperty(this, "_egid", readNativeProcessId(_nativeGetegid, this._gid));
			_defineProperty(
				this,
				/**
				* Whether deprecation warnings should be suppressed.
				*/
				"noDeprecation",
				false
			);
			_defineProperty(
				this,
				/**
				* Whether to throw on deprecation warnings instead of logging.
				*/
				"throwDeprecation",
				false
			);
			_defineProperty(
				this,
				/**
				* Whether to show stack traces for deprecation warnings.
				*/
				"traceDeprecation",
				false
			);
			_defineProperty(
				this,
				/**
				* Whether the process is currently exiting.
				*/
				"_exactExiting",
				false
			);
			_defineProperty(
				this,
				/**
				* High-resolution time.
				* Returns [seconds, nanoseconds] tuple.
				* Also has hrtime.bigint() method.
				*/
				"hrtime",
				Object.assign((time) => {
					if (time !== void 0) {
						if (!Array.isArray(time)) {
							const e = /* @__PURE__ */ new TypeError(`The "time" argument must be an instance of Array. Received type ${typeof time} (${String(time)})`);
							e.code = "ERR_INVALID_ARG_TYPE";
							throw e;
						}
						if (time.length !== 2) {
							const e = /* @__PURE__ */ new RangeError(`The value of "time" is out of range. It must be 2. Received ${time.length}`);
							e.code = "ERR_OUT_OF_RANGE";
							throw e;
						}
					}
					const nowNs = _monotonicTimeNs();
					const seconds = Number(nowNs / 1000000000n);
					const nanoseconds = Number(nowNs % 1000000000n);
					if (time) {
						let diffSeconds = seconds - time[0];
						let diffNanos = nanoseconds - time[1];
						if (diffNanos < 0) {
							diffSeconds -= 1;
							diffNanos += 1e9;
						}
						return [diffSeconds, diffNanos];
					}
					return [seconds, nanoseconds];
				}, { bigint: () => _monotonicTimeNs() })
			);
			_defineProperty(
				this,
				/**
				* Process features flags.
				* Matches Node.js process.features structure.
				*/
				"features",
				{
					inspector: false,
					debug: false,
					uv: true,
					ipv6: true,
					tls_alpn: false,
					tls_sni: false,
					tls_ocsp: false,
					tls: false,
					cached_builtins: true,
					typescript: false,
					require_module: false
				}
			);
			_defineProperty(this, "debugPort", 9229);
			_defineProperty(this, "_umask", 18);
			_defineProperty(this, "umask", (mask) => {
				if (mask === void 0) return this._umask;
				const old = this._umask;
				if (typeof mask !== "string" && typeof mask !== "number") {
					const v = mask;
					const received = v == null ? ` Received ${v}` : typeof v === "function" ? ` Received function ${v.name}` : typeof v === "object" ? v.constructor && v.constructor.name ? ` Received an instance of ${v.constructor.name}` : ` Received ${String(v)}` : ` Received type ${typeof v} (${String(v)})`;
					const e = /* @__PURE__ */ new TypeError(`The "mask" argument must be one of type string or number.${received}`);
					e.code = "ERR_INVALID_ARG_TYPE";
					throw e;
				}
				if (typeof mask === "string") {
					if (!/^(0o?)?[0-7]+$/.test(mask)) {
						const e = /* @__PURE__ */ new RangeError(`The property 'mask' is invalid. Received '${mask}'`);
						e.code = "ERR_INVALID_ARG_VALUE";
						throw e;
					}
					const stripped = mask.replace(/^0o/i, "").replace(/^0+(?=\d)/, "");
					mask = parseInt(stripped || "0", 8);
				}
				if (typeof mask === "number" && mask === (mask | 0)) this._umask = mask & 511;
				return old;
			});
			_defineProperty(this, "_uncaughtCaptureCb", null);
			_defineProperty(this, "report", {
				writeReport: () => "",
				getReport: () => ({}),
				directory: "",
				filename: "",
				compact: false,
				signal: "SIGUSR2",
				reportOnFatalError: false,
				reportOnSignal: false,
				reportOnUncaughtException: false
			});
			_defineProperty(this, "allowedNodeEnvironmentFlags", createAllowedNodeEnvironmentFlags());
			_defineProperty(this, "_stdin", void 0);
			_defineProperty(this, "_stdout", void 0);
			_defineProperty(this, "_stderr", void 0);
			_defineProperty(this, "_events", /* @__PURE__ */ new Map());
			_defineProperty(this, "_uncaughtExceptionHooked", false);
			_defineProperty(this, "_unhandledRejectionHooked", false);
		}
		/**
		* Platform identifier.
		* Returns host platform when available (e.g. "linux", "darwin", "win32"),
		* otherwise falls back to runtime detection.
		*/
		get platform() {
			const g = globalThis;
			if (typeof g.__exactPlatform === "string" && g.__exactPlatform.length > 0) return g.__exactPlatform;
			return runtimeInfo.platform;
		}
		/**
		* Process architecture.
		* Returns host arch when available (e.g. "x64", "arm64").
		*/
		get arch() {
			const g = globalThis;
			if (typeof g.__exactArch === "string" && g.__exactArch.length > 0) return g.__exactArch;
			if (_nativeArch) return _nativeArch;
			return "arm64";
		}
		/**
		* Node.js-compatible version string.
		* Format: "v{major}.{minor}.{patch}"
		*/
		get version() {
			return `v${PROCESS_VERSIONS.node ?? getRuntimeVersion()}`;
		}
		/**
		* Version strings for various components.
		* Includes all fields that Node.js tests expect on `process.versions`.
		*/
		get versions() {
			return PROCESS_VERSIONS;
		}
		/**
		* Release information.
		*/
		get release() {
			return PROCESS_RELEASE;
		}
		/**
		* Process title (runtime name).
		*/
		get title() {
			return "ibex";
		}
		/**
		* Process ID (not meaningful in mobile context, always returns 1).
		*/
		get pid() {
			return 1;
		}
		/**
		* Parent process ID (always 0 in mobile context).
		*/
		get ppid() {
			return 0;
		}
		binding(name) {
			if (name === "util") {
				const types = globalThis.require?.("util")?.types ?? {};
				const binding = {};
				{
					const __exactForOfIterator113 = [
						"isAnyArrayBuffer",
						"isArrayBuffer",
						"isArrayBufferView",
						"isAsyncFunction",
						"isDataView",
						"isDate",
						"isExternal",
						"isMap",
						"isMapIterator",
						"isNativeError",
						"isPromise",
						"isRegExp",
						"isSet",
						"isSetIterator",
						"isTypedArray",
						"isUint8Array"
					][Symbol.iterator]();
					for (;;) {
						const __exactForOfStep113 = __exactForOfIterator113.next();
						if (__exactForOfStep113.done) break;
						const key = __exactForOfStep113.value;
						binding[key] = types[key];
					}
				}
				return binding;
			}
			if (name === "constants") return {
				os: {
					errno: {
						E2BIG: 7,
						EACCES: 13,
						EADDRINUSE: 48,
						EADDRNOTAVAIL: 49,
						EAFNOSUPPORT: 47,
						EAGAIN: 35,
						EALREADY: 37,
						EBADF: 9,
						EBUSY: 16,
						ECANCELED: 89,
						ECHILD: 10,
						ECONNABORTED: 53,
						ECONNREFUSED: 61,
						ECONNRESET: 54,
						EDEADLK: 11,
						EDESTADDRREQ: 39,
						EDOM: 33,
						EEXIST: 17,
						EFAULT: 14,
						EFBIG: 27,
						EHOSTUNREACH: 65,
						EINPROGRESS: 36,
						EINTR: 4,
						EINVAL: 22,
						EIO: 5,
						EISCONN: 56,
						EISDIR: 21,
						ELOOP: 62,
						EMFILE: 24,
						EMLINK: 31,
						EMSGSIZE: 40,
						ENAMETOOLONG: 63,
						ENETDOWN: 50,
						ENETRESET: 52,
						ENETUNREACH: 51,
						ENFILE: 23,
						ENOBUFS: 55,
						ENODEV: 19,
						ENOENT: 2,
						ENOEXEC: 8,
						ENOLCK: 77,
						ENOMEM: 12,
						ENOPROTOOPT: 42,
						ENOSPC: 28,
						ENOSYS: 78,
						ENOTCONN: 57,
						ENOTDIR: 20,
						ENOTEMPTY: 66,
						ENOTSOCK: 38,
						ENOTSUP: 45,
						ENOTTY: 25,
						ENXIO: 6,
						EOPNOTSUPP: 102,
						EOVERFLOW: 84,
						EPERM: 1,
						EPIPE: 32,
						EPROTONOSUPPORT: 43,
						EPROTOTYPE: 41,
						ERANGE: 34,
						EROFS: 30,
						ESPIPE: 29,
						ESRCH: 3,
						ETIMEDOUT: 60,
						ETXTBSY: 26,
						EWOULDBLOCK: 35,
						EXDEV: 18
					},
					signals: {
						SIGHUP: 1,
						SIGINT: 2,
						SIGQUIT: 3,
						SIGILL: 4,
						SIGTRAP: 5,
						SIGABRT: 6,
						SIGFPE: 8,
						SIGKILL: 9,
						SIGBUS: 10,
						SIGSEGV: 11,
						SIGSYS: 12,
						SIGPIPE: 13,
						SIGALRM: 14,
						SIGTERM: 15,
						SIGURG: 16,
						SIGSTOP: 17,
						SIGTSTP: 18,
						SIGCONT: 19,
						SIGCHLD: 20,
						SIGTTIN: 21,
						SIGTTOU: 22,
						SIGIO: 23,
						SIGXCPU: 24,
						SIGXFSZ: 25,
						SIGVTALRM: 26,
						SIGPROF: 27,
						SIGINFO: 29,
						SIGUSR1: 30,
						SIGUSR2: 31
					}
				},
				fs: {
					O_RDONLY: 0,
					O_WRONLY: 1,
					O_RDWR: 2,
					O_CREAT: 512,
					O_EXCL: 2048,
					O_TRUNC: 1024,
					O_APPEND: 8,
					O_DIRECTORY: 1048576,
					O_SYMLINK: 2097152,
					S_IFMT: 61440,
					S_IFREG: 32768,
					S_IFDIR: 16384,
					S_IFLNK: 40960,
					S_IFCHR: 8192,
					S_IFBLK: 24576,
					S_IFIFO: 4096,
					S_IFSOCK: 49152
				},
				zlib: {
					Z_NO_FLUSH: 0,
					Z_PARTIAL_FLUSH: 1,
					Z_SYNC_FLUSH: 2,
					Z_FULL_FLUSH: 3,
					Z_FINISH: 4,
					Z_BLOCK: 5,
					Z_OK: 0,
					Z_STREAM_END: 1,
					Z_NEED_DICT: 2,
					Z_ERRNO: -1,
					Z_STREAM_ERROR: -2,
					Z_DATA_ERROR: -3,
					Z_MEM_ERROR: -4,
					Z_BUF_ERROR: -5,
					Z_VERSION_ERROR: -6,
					Z_NO_COMPRESSION: 0,
					Z_BEST_SPEED: 1,
					Z_BEST_COMPRESSION: 9,
					Z_DEFAULT_COMPRESSION: -1,
					Z_FILTERED: 1,
					Z_HUFFMAN_ONLY: 2,
					Z_RLE: 3,
					Z_FIXED: 4,
					Z_DEFAULT_STRATEGY: 0,
					Z_MIN_WINDOWBITS: 8,
					Z_MAX_WINDOWBITS: 15,
					Z_DEFAULT_WINDOWBITS: 15,
					Z_MIN_CHUNK: 64,
					Z_MAX_CHUNK: null,
					Z_DEFAULT_CHUNK: 16384,
					Z_MIN_MEMLEVEL: 1,
					Z_MAX_MEMLEVEL: 9,
					Z_DEFAULT_MEMLEVEL: 8,
					Z_MIN_LEVEL: -1,
					Z_MAX_LEVEL: 9,
					Z_DEFAULT_LEVEL: -1,
					DEFLATE: 1,
					INFLATE: 2,
					GZIP: 3,
					GUNZIP: 4,
					DEFLATERAW: 5,
					INFLATERAW: 6,
					UNZIP: 7,
					BROTLI_DECODE: 8,
					BROTLI_ENCODE: 9,
					BROTLI_OPERATION_PROCESS: 0,
					BROTLI_OPERATION_FLUSH: 1,
					BROTLI_OPERATION_FINISH: 2,
					BROTLI_OPERATION_EMIT_METADATA: 3,
					BROTLI_PARAM_MODE: 0,
					BROTLI_MODE_GENERIC: 0,
					BROTLI_MODE_TEXT: 1,
					BROTLI_MODE_FONT: 2,
					BROTLI_DEFAULT_MODE: 0,
					BROTLI_PARAM_QUALITY: 1,
					BROTLI_MIN_QUALITY: 0,
					BROTLI_MAX_QUALITY: 11,
					BROTLI_DEFAULT_QUALITY: 11,
					BROTLI_PARAM_LGWIN: 2,
					BROTLI_MIN_WINDOW_BITS: 10,
					BROTLI_MAX_WINDOW_BITS: 24,
					BROTLI_LARGE_MAX_WINDOW_BITS: 30,
					BROTLI_DEFAULT_WINDOW: 22,
					BROTLI_PARAM_LGBLOCK: 3,
					BROTLI_MIN_INPUT_BLOCK_BITS: 16,
					BROTLI_MAX_INPUT_BLOCK_BITS: 24,
					BROTLI_PARAM_DISABLE_LITERAL_CONTEXT_MODELING: 4,
					BROTLI_PARAM_SIZE_HINT: 5,
					BROTLI_PARAM_LARGE_WINDOW: 6,
					BROTLI_PARAM_NPOSTFIX: 7,
					BROTLI_PARAM_NDIRECT: 8,
					BROTLI_DECODER_RESULT_ERROR: 0,
					BROTLI_DECODER_RESULT_SUCCESS: 1,
					BROTLI_DECODER_RESULT_NEEDS_MORE_INPUT: 2,
					BROTLI_DECODER_RESULT_NEEDS_MORE_OUTPUT: 3,
					BROTLI_DECODER_PARAM_DISABLE_RING_BUFFER_REALLOCATION: 0,
					BROTLI_DECODER_PARAM_LARGE_WINDOW: 1,
					BROTLI_DECODER_NO_ERROR: 0,
					BROTLI_DECODER_SUCCESS: 1,
					BROTLI_DECODER_NEEDS_MORE_INPUT: 2,
					BROTLI_DECODER_NEEDS_MORE_OUTPUT: 3
				}
			};
			if (name === "uv" || name === "http_parser") return {};
			throw new Error(`No such module: ${name}`);
		}
		/**
		* IPC channel (not available in Exact runtime).
		*/
		get channel() {}
		execve(execPath, args, envObj) {
			validateExecveArguments(execPath, args, envObj, arguments.length >= 3);
			if (isChildProcessPermissionDenied(this.execArgv)) {
				const err = /* @__PURE__ */ new Error("Access to this API has been restricted");
				err.code = "ERR_ACCESS_DENIED";
				err.permission = "ChildProcess";
				err.resource = execPath;
				throw err;
			}
			const requireFn = globalThis.require;
			if (typeof requireFn !== "function") {
				const err = /* @__PURE__ */ new TypeError("process.execve is not supported in this runtime");
				err.code = "ERR_FEATURE_UNAVAILABLE_ON_PLATFORM";
				throw err;
			}
			const childProcess = requireFn("child_process");
			if (!childProcess || typeof childProcess.spawnSync !== "function") {
				const err = /* @__PURE__ */ new TypeError("process.execve is not supported in this runtime");
				err.code = "ERR_FEATURE_UNAVAILABLE_ON_PLATFORM";
				throw err;
			}
			const result = childProcess.spawnSync(execPath, args.slice(1), {
				env: envObj ?? { ...this.env },
				stdio: "inherit"
			});
			if (result?.error) throw result.error;
			this.exit(typeof result?.status === "number" ? result.status : 0);
		}
		get exitCode() {
			return this._exitCode;
		}
		set exitCode(value) {
			if (value === null || value === void 0) {
				this._exitCode = void 0;
				return;
			}
			if (typeof value === "string") {
				if (value.length === 0) {
					const e = /* @__PURE__ */ new TypeError(`The "code" argument must be of type number. Received type string ('')`);
					e.code = "ERR_INVALID_ARG_TYPE";
					throw e;
				}
				const num = Number(value);
				if (Number.isNaN(num) || !Number.isInteger(num) || !Number.isFinite(num)) {
					const e = /* @__PURE__ */ new TypeError(`The "code" argument must be of type number. Received type string ('${value}')`);
					e.code = "ERR_INVALID_ARG_TYPE";
					throw e;
				}
				this._exitCode = num;
				return;
			}
			if (typeof value === "bigint") {
				const e = /* @__PURE__ */ new TypeError(`The "code" argument must be of type number. Received type bigint (${value}n)`);
				e.code = "ERR_INVALID_ARG_TYPE";
				throw e;
			}
			if (typeof value !== "number") {
				const e = /* @__PURE__ */ new TypeError(`The "code" argument must be of type number. Received type ${typeof value}`);
				e.code = "ERR_INVALID_ARG_TYPE";
				throw e;
			}
			if (!Number.isInteger(value) || !Number.isFinite(value)) {
				const e = /* @__PURE__ */ new RangeError(`The value of "code" is out of range. It must be an integer. Received ${value}`);
				e.code = "ERR_OUT_OF_RANGE";
				throw e;
			}
			this._exitCode = value;
		}
		/**
		* Get the user ID of the process.
		*/
		getuid() {
			return this._uid;
		}
		/**
		* Get the group ID of the process.
		*/
		getgid() {
			return this._gid;
		}
		/**
		* Get the effective user ID of the process.
		*/
		geteuid() {
			return this._euid;
		}
		/**
		* Get the effective group ID of the process.
		*/
		getegid() {
			return this._egid;
		}
		/**
		* Set the user ID (throws - not supported).
		*/
		setuid(id) {
			const resolved = resolveCredentialId(id, "user");
			if (this._uid !== 0 && this._euid !== 0) throw makeCredentialPermissionError();
			this._uid = resolved;
			this._euid = resolved;
		}
		/**
		* Set the group ID (throws - not supported).
		*/
		setgid(id) {
			const resolved = resolveCredentialId(id, "group");
			if (this._uid !== 0 && this._euid !== 0) throw makeCredentialPermissionError();
			this._gid = resolved;
			this._egid = resolved;
		}
		/**
		* Set the effective user ID (throws - not supported).
		*/
		seteuid(id) {
			const resolved = resolveCredentialId(id, "user");
			if (this._uid !== 0 && this._euid !== 0) throw makeCredentialPermissionError();
			this._euid = resolved;
		}
		/**
		* Set the effective group ID (throws - not supported).
		*/
		setegid(id) {
			const resolved = resolveCredentialId(id, "group");
			if (this._uid !== 0 && this._euid !== 0) throw makeCredentialPermissionError();
			this._egid = resolved;
		}
		/**
		* Get supplementary group IDs.
		*/
		getgroups() {
			return [this._gid];
		}
		/**
		* Send a signal to a process.
		* Validates signal names/numbers per Node.js behavior.
		*/
		kill(pid, signal) {
			const normalizedPid = normalizeProcessId(pid);
			const normalizedSignal = normalizeSignal(signal);
			this._kill.call(this, normalizedPid, normalizedSignal);
			return true;
		}
		_kill(pid, signalNumber) {
			if (_nativeProcessKill) try {
				_nativeProcessKill(pid, signalNumber);
				return true;
			} catch {}
			if (pid === this.pid) return true;
			const e = /* @__PURE__ */ new Error("kill ESRCH");
			e.code = "ESRCH";
			e.errno = -3;
			e.syscall = "kill";
			throw e;
		}
		/**
		* Returns the current working directory.
		* In mobile context, this is the app's documents directory.
		*/
		cwd() {
			if (_processCwd && _processCwd !== "/") return _processCwd;
			const nativeCwd = _readNativeCwd();
			if (nativeCwd) {
				_processCwd = nativeCwd;
				return _processCwd;
			}
			if (!_processCwd || _processCwd === "/") _processCwd = "/";
			return _processCwd;
		}
		/**
		* Change working directory.
		*/
		chdir(_directory) {
			if (typeof _directory !== "string") {
				const err = /* @__PURE__ */ new TypeError(`The "directory" argument must be of type string. Received type ${typeof _directory}`);
				err.code = "ERR_INVALID_ARG_TYPE";
				throw err;
			}
			const resolvedPath = _resolveCwd(_directory);
			if (typeof __exactSetCwd !== "function") {
				if (typeof globalThis.__exactAccess === "function") try {
					globalThis.__exactAccess(resolvedPath, 0);
				} catch (e) {
					throw _coerceChdirError(e, _directory);
				}
				_processCwd = resolvedPath;
				return;
			}
			try {
				__exactSetCwd(resolvedPath);
				_processCwd = resolvedPath;
			} catch (e) {
				throw _coerceChdirError(e, _directory);
			}
		}
		/**
		* Exit the process.
		* Sets exitCode first, fires exit handlers, then terminates if native exit is available.
		*/
		exit(code) {
			const requestedExitCode = code ?? this.exitCode ?? 0;
			this.exitCode = requestedExitCode;
			if (this._exactExiting) return;
			this._exactExiting = true;
			try {
				const listeners = [...this._events.get("exit") || []];
				{
					const __exactForOfIterator114 = listeners[Symbol.iterator]();
					for (;;) {
						const __exactForOfStep114 = __exactForOfIterator114.next();
						if (__exactForOfStep114.done) break;
						const entry = __exactForOfStep114.value;
						try {
							entry.fn(requestedExitCode);
						} catch (_e) {}
					}
				}
			} catch (_e) {} finally {
				if (typeof this._events.clear === "function") this._events.clear();
			}
			const finalExitCode = this.exitCode ?? 0;
			const g = globalThis;
			if (typeof g.__exactExit === "function") try {
				g.__exactExit(finalExitCode);
			} catch (_e) {}
		}
		/**
		* Abort the process.
		*/
		abort() {
			throw new Error("process.abort() is not supported in Exact runtime");
		}
		/**
		* Schedule a callback to run on the next tick of the event loop.
		* This runs before any I/O events but after the current operation.
		*/
		nextTick(callback, ...args) {
			if (_nativeProcessNextTick) {
				_nativeProcessNextTick(callback, ...args);
				return;
			}
			if (_nativeGlobalNextTick) {
				_nativeGlobalNextTick(callback, ...args);
				return;
			}
			_nextTickQueue.push({
				callback,
				args
			});
			_scheduleNextTickDrain();
		}
		/**
		* Memory usage (approximate, not accurate on mobile).
		*/
		memoryUsage() {
			const exactGlobal = globalThis;
			const nativeHeapInfo = typeof exactGlobal.__exactGetHeapInfo === "function" ? exactGlobal.__exactGetHeapInfo(false) : null;
			if (nativeHeapInfo && typeof nativeHeapInfo === "object") {
				const heapTotal = typeof nativeHeapInfo.hermes_heapSize === "number" ? nativeHeapInfo.hermes_heapSize : typeof nativeHeapInfo.heapSize === "number" ? nativeHeapInfo.heapSize : 0;
				const heapUsed = typeof nativeHeapInfo.hermes_allocatedBytes === "number" ? nativeHeapInfo.hermes_allocatedBytes : typeof nativeHeapInfo.allocatedBytes === "number" ? nativeHeapInfo.allocatedBytes : 0;
				const external = typeof nativeHeapInfo.hermes_externalBytes === "number" ? nativeHeapInfo.hermes_externalBytes : typeof nativeHeapInfo.externalBytes === "number" ? nativeHeapInfo.externalBytes : 0;
				const arrayBuffers = typeof nativeHeapInfo.hermes_arrayBufferBytes === "number" ? nativeHeapInfo.hermes_arrayBufferBytes : typeof nativeHeapInfo.arrayBufferBytes === "number" ? nativeHeapInfo.arrayBufferBytes : 0;
				const nativeRss = typeof exactGlobal.__exactGetProcessRSS === "function" ? exactGlobal.__exactGetProcessRSS() : null;
				return {
					rss: typeof nativeRss === "number" && nativeRss > 0 ? nativeRss : Math.max(heapTotal, heapUsed + external, 1024 * 1024),
					heapTotal,
					heapUsed,
					external,
					arrayBuffers
				};
			}
			const perfMemory = globalThis.performance?.memory;
			const heapTotal = typeof perfMemory?.totalJSHeapSize === "number" ? perfMemory.totalJSHeapSize : 0;
			const heapUsed = typeof perfMemory?.usedJSHeapSize === "number" ? perfMemory.usedJSHeapSize : 0;
			const arrayBuffers = typeof perfMemory?.arrayBuffers === "number" ? perfMemory.arrayBuffers : 0;
			return {
				rss: typeof perfMemory?.jsHeapSizeLimit === "number" ? Math.max(perfMemory.jsHeapSizeLimit, heapTotal, heapUsed) : Math.max(heapTotal, heapUsed, 1024 * 1024),
				heapTotal,
				heapUsed,
				external: 0,
				arrayBuffers
			};
		}
		/**
		* CPU usage (not available on mobile, returns zeros).
		*/
		cpuUsage(prevValue) {
			if (prevValue !== void 0) {
				if (typeof prevValue !== "object" || prevValue === null || Array.isArray(prevValue)) {
					const e = /* @__PURE__ */ new TypeError(`The "prevValue" argument must be of type object. Received type ${typeof prevValue} (${String(prevValue)})`);
					e.code = "ERR_INVALID_ARG_TYPE";
					throw e;
				}
				if (typeof prevValue.user !== "number") {
					const v = prevValue.user;
					const received = v == null ? ` Received ${v}` : typeof v === "function" ? ` Received function ${v.name}` : typeof v === "object" ? v.constructor && v.constructor.name ? ` Received an instance of ${v.constructor.name}` : ` Received ${String(v)}` : ` Received type ${typeof v} (${String(v)})`;
					const e = /* @__PURE__ */ new TypeError(`The "prevValue.user" property must be of type number.${received}`);
					e.code = "ERR_INVALID_ARG_TYPE";
					throw e;
				}
				if (typeof prevValue.system !== "number") {
					const v = prevValue.system;
					const received = v == null ? ` Received ${v}` : typeof v === "function" ? ` Received function ${v.name}` : typeof v === "object" ? v.constructor && v.constructor.name ? ` Received an instance of ${v.constructor.name}` : ` Received ${String(v)}` : ` Received type ${typeof v} (${String(v)})`;
					const e = /* @__PURE__ */ new TypeError(`The "prevValue.system" property must be of type number.${received}`);
					e.code = "ERR_INVALID_ARG_TYPE";
					throw e;
				}
				if (prevValue.user < 0 || !Number.isFinite(prevValue.user)) {
					const e = /* @__PURE__ */ new RangeError(`The property 'prevValue.user' is invalid. Received ${prevValue.user}`);
					e.code = "ERR_INVALID_ARG_VALUE";
					throw e;
				}
				if (prevValue.system < 0 || !Number.isFinite(prevValue.system)) {
					const e = /* @__PURE__ */ new RangeError(`The property 'prevValue.system' is invalid. Received ${prevValue.system}`);
					e.code = "ERR_INVALID_ARG_VALUE";
					throw e;
				}
			}
			return {
				user: 0,
				system: 0
			};
		}
		/**
		* Emit a warning (async like Node.js).
		*/
		emitWarning(warning, type, code, ctor) {
			let detail;
			if (typeof type === "object" && type !== null && !Array.isArray(type)) {
				code = type.code;
				ctor = type.ctor;
				detail = type.detail;
				type = type.type || "Warning";
			}
			if (typeof code === "function") {
				ctor = code;
				code = void 0;
			}
			if (typeof type === "function") {
				ctor = type;
				type = "Warning";
				code = void 0;
			}
			if (warning === void 0 || typeof warning !== "string" && !(warning instanceof Error)) {
				const e = /* @__PURE__ */ new TypeError(`The "warning" argument must be of type string or an instance of Error. Received ${warning === void 0 ? "undefined" : "type " + typeof warning}`);
				e.code = "ERR_INVALID_ARG_TYPE";
				throw e;
			}
			if (type !== void 0 && typeof type !== "string") {
				const e = /* @__PURE__ */ new TypeError(`The "type" argument must be of type string. Received type ${typeof type}`);
				e.code = "ERR_INVALID_ARG_TYPE";
				throw e;
			}
			if (code !== void 0 && typeof code !== "string") {
				const e = /* @__PURE__ */ new TypeError(`The "code" argument must be of type string. Received type ${typeof code}`);
				e.code = "ERR_INVALID_ARG_TYPE";
				throw e;
			}
			if ((type === "DeprecationWarning" || typeof type === "undefined" && typeof warning === "string" && warning.startsWith("DEP")) && this.noDeprecation) return;
			let warningObj;
			if (typeof warning === "string") {
				warningObj = new Error(warning);
				warningObj.name = type || "Warning";
			} else warningObj = warning;
			if (code) warningObj.code = code;
			if (type && warningObj.name !== type) warningObj.name = type;
			if (typeof detail === "string") warningObj.detail = detail;
			this.nextTick(() => {
				this.emit("warning", warningObj);
			});
		}
		getActiveResourcesInfo() {
			return [];
		}
		_getActiveRequests() {
			return [];
		}
		_getActiveHandles() {
			return [];
		}
		resourceUsage() {
			return {
				userCPUTime: 0,
				systemCPUTime: 0,
				maxRSS: 0,
				sharedMemorySize: 0,
				unsharedDataSize: 0,
				unsharedStackSize: 0,
				minorPageFault: 0,
				majorPageFault: 0,
				swappedOut: 0,
				fsRead: 0,
				fsWrite: 0,
				ipcSent: 0,
				ipcReceived: 0,
				signalsCount: 0,
				voluntaryContextSwitches: 0,
				involuntaryContextSwitches: 0
			};
		}
		setUncaughtExceptionCaptureCallback(fn) {
			if (fn !== null && typeof fn !== "function") {
				const e = /* @__PURE__ */ new TypeError("The \"fn\" argument must be of type function or null. Received type " + typeof fn + " (" + String(fn) + ")");
				e.code = "ERR_INVALID_ARG_TYPE";
				throw e;
			}
			if (fn !== null && this._uncaughtCaptureCb !== null) {
				const e = /* @__PURE__ */ new Error("process.setupUncaughtExceptionCapture() was called while a capture callback was already active");
				e.code = "ERR_UNCAUGHT_EXCEPTION_CAPTURE_ALREADY_SET";
				throw e;
			}
			this._uncaughtCaptureCb = fn;
		}
		hasUncaughtExceptionCaptureCallback() {
			return this._uncaughtCaptureCb !== null;
		}
		/**
		* Uptime in seconds.
		*/
		uptime() {
			const timeOrigin = performance?.timeOrigin ?? Date.now();
			return (Date.now() - timeOrigin) / 1e3;
		}
		get stdin() {
			if (!this._stdin) this._stdin = _makeProcessStream(0, false);
			return this._stdin;
		}
		get stdout() {
			if (!this._stdout) this._stdout = _makeProcessStream(1, false, (data) => {
				const g = globalThis;
				if (typeof g.__exactFsWrite !== "function" && typeof g.__exactEnsureFs === "function") g.__exactEnsureFs();
				if (typeof g.__exactFsWrite === "function") {
					g.__exactFsWrite(1, data, -1);
					return true;
				}
				console.log(data);
				return true;
			});
			return this._stdout;
		}
		get stderr() {
			if (!this._stderr) this._stderr = _makeProcessStream(2, false, (data) => {
				const g = globalThis;
				if (typeof g.__exactFsWrite !== "function" && typeof g.__exactEnsureFs === "function") g.__exactEnsureFs();
				if (typeof g.__exactFsWrite === "function") {
					g.__exactFsWrite(2, data, -1);
					return true;
				}
				console.error(data);
				return true;
			});
			return this._stderr;
		}
		/**
		* Execution arguments (empty in mobile context).
		*/
		get argv() {
			return getRuntimeArgv();
		}
		/**
		* Execution arguments including node path.
		*/
		get argv0() {
			return getRuntimeArgv0();
		}
		/**
		* Executable path.
		*/
		get execPath() {
			return typeof globalThis !== "undefined" && globalThis.__exactExecPath || "/usr/bin/ibex";
		}
		set execPath(path) {
			if (typeof path === "string") {
				if (typeof globalThis !== "undefined") globalThis.__exactExecPath = path;
			}
		}
		/**
		* Node-specific execution arguments (empty).
		*/
		get execArgv() {
			if (typeof globalThis !== "undefined" && Array.isArray(globalThis.__exactExecArgv)) return globalThis.__exactExecArgv;
			return [];
		}
		/**
		* Whether running in browser (always false).
		*/
		get browser() {
			return false;
		}
		on(event, listener) {
			const listeners = this._events.get(event) || [];
			listeners.push({
				fn: listener,
				once: false
			});
			this._events.set(event, listeners);
			if (event === "uncaughtException") this._hookUncaughtException();
			if (event === "unhandledRejection") this._hookUnhandledRejection();
			return this;
		}
		off(event, listener) {
			return this.removeListener(event, listener);
		}
		addListener(event, listener) {
			return this.on(event, listener);
		}
		once(event, listener) {
			const listeners = this._events.get(event) || [];
			listeners.push({
				fn: listener,
				once: true
			});
			this._events.set(event, listeners);
			if (event === "uncaughtException") this._hookUncaughtException();
			if (event === "unhandledRejection") this._hookUnhandledRejection();
			return this;
		}
		prependListener(event, listener) {
			const listeners = this._events.get(event) || [];
			listeners.unshift({
				fn: listener,
				once: false
			});
			this._events.set(event, listeners);
			return this;
		}
		prependOnceListener(event, listener) {
			const listeners = this._events.get(event) || [];
			listeners.unshift({
				fn: listener,
				once: true
			});
			this._events.set(event, listeners);
			return this;
		}
		emit(event, ...args) {
			const listeners = this._events.get(event);
			if (!listeners || listeners.length === 0) return false;
			const remaining = [];
			{
				const __exactForOfIterator115 = listeners[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep115 = __exactForOfIterator115.next();
					if (__exactForOfStep115.done) break;
					const entry = __exactForOfStep115.value;
					entry.fn(...args);
					if (!entry.once) remaining.push(entry);
				}
			}
			this._events.set(event, remaining);
			return true;
		}
		removeListener(event, listener) {
			const listeners = this._events.get(event);
			if (listeners) this._events.set(event, listeners.filter((e) => e.fn !== listener));
			return this;
		}
		removeAllListeners(event) {
			if (event) this._events.delete(event);
			else this._events.clear();
			return this;
		}
		listeners(event) {
			return (this._events.get(event) || []).map((e) => e.fn);
		}
		rawListeners(event) {
			return this.listeners(event);
		}
		listenerCount(event) {
			return (this._events.get(event) || []).length;
		}
		eventNames() {
			return Array.from(this._events.keys()).filter((event) => this.listenerCount(event) > 0);
		}
		setMaxListeners(count) {
			this._maxListeners = count;
			return this;
		}
		getMaxListeners() {
			return this._maxListeners;
		}
		_hookUncaughtException() {
			if (this._uncaughtExceptionHooked) return;
			this._uncaughtExceptionHooked = true;
			const g = globalThis;
			const self = this;
			const prev = g.onerror;
			g.onerror = function(msg, _src, _line, _col, err) {
				const error = err instanceof Error ? err : new Error(String(msg));
				if (self.listenerCount("uncaughtException") > 0) {
					self.emit("uncaughtException", error);
					return true;
				}
				if (typeof prev === "function") return prev(msg, _src, _line, _col, err);
				return false;
			};
		}
		_hookUnhandledRejection() {
			if (this._unhandledRejectionHooked) return;
			this._unhandledRejectionHooked = true;
			const g = globalThis;
			const self = this;
			const addFn = g.addEventListener;
			if (typeof addFn === "function") addFn.call(g, "unhandledrejection", (event) => {
				if (self.listenerCount("unhandledRejection") > 0) {
					self.emit("unhandledRejection", event.reason, event.promise);
					event.preventDefault?.();
				}
			});
		}
		get [_Symbol$toStringTag]() {
			return "process";
		}
	};
	_installTimeZoneAwareDateToString();
	const process$1 = new Process();
	const exitCodeDescriptor = Object.getOwnPropertyDescriptor(Process.prototype, "exitCode");
	if (exitCodeDescriptor?.get && exitCodeDescriptor?.set) Object.defineProperty(process$1, "exitCode", {
		get: exitCodeDescriptor.get.bind(process$1),
		set: exitCodeDescriptor.set.bind(process$1),
		enumerable: true,
		configurable: false
	});
	function getRuntimeArgv() {
		if (typeof __exactArgv === "object" && Array.isArray(__exactArgv) && __exactArgv.length > 0) return __exactArgv;
		return ["ibex"];
	}
	function getRuntimeArgv0() {
		return getRuntimeArgv()[0] ?? "ibex";
	}
	function readNativeProcessId(reader, fallback) {
		if (typeof reader !== "function") return fallback;
		try {
			const value = reader();
			return typeof value === "number" && Number.isInteger(value) ? value : fallback;
		} catch {
			return fallback;
		}
	}
	function formatReceivedValue(value) {
		if (value === null) return "null";
		if (value === void 0) return "undefined";
		if (typeof value === "string") return `type string ('${value}')`;
		if (typeof value === "bigint") return `type bigint (${value}n)`;
		if (typeof value === "function") return `function ${value.name || "<anonymous>"}`;
		if (typeof value !== "object") return `type ${typeof value} (${String(value)})`;
		const ctorName = value.constructor?.name;
		return ctorName ? `an instance of ${ctorName}` : String(value);
	}
	function makeCredentialTypeError(value) {
		const err = /* @__PURE__ */ new TypeError(`The "id" argument must be one of type number or string. Received ${formatReceivedValue(value)}`);
		err.code = "ERR_INVALID_ARG_TYPE";
		return err;
	}
	function makeUnknownCredentialError(kind, value) {
		const err = /* @__PURE__ */ new Error(`${kind === "user" ? "User" : "Group"} identifier does not exist: ${value}`);
		err.code = "ERR_UNKNOWN_CREDENTIAL";
		return err;
	}
	function makeCredentialPermissionError() {
		const err = /* @__PURE__ */ new Error("EPERM, Operation not permitted");
		err.code = "EPERM";
		return err;
	}
	function resolveCredentialId(id, kind) {
		if (typeof id === "number") {
			if (!Number.isFinite(id) || !Number.isInteger(id)) throw makeCredentialTypeError(id);
			return Object.is(id, -0) ? 0 : id;
		}
		if (typeof id !== "string") throw makeCredentialTypeError(id);
		const table = kind === "user" ? USER_CREDENTIALS : GROUP_CREDENTIALS;
		if (Object.prototype.hasOwnProperty.call(table, id)) return table[id];
		const numeric = Number(id);
		if (id.length > 0 && Number.isFinite(numeric) && Number.isInteger(numeric)) return Object.is(numeric, -0) ? 0 : numeric;
		throw makeUnknownCredentialError(kind, id);
	}
	function makeInvalidPidError(value) {
		const err = /* @__PURE__ */ new TypeError(`The "pid" argument must be of type number. Received ${formatReceivedValue(value)}`);
		err.code = "ERR_INVALID_ARG_TYPE";
		return err;
	}
	function normalizeProcessId(pid) {
		if (typeof pid === "string") {
			const numeric = Number(pid);
			if (!Number.isFinite(numeric) || !Number.isInteger(numeric)) throw makeInvalidPidError(pid);
			return Object.is(numeric, -0) ? 0 : numeric;
		}
		if (typeof pid !== "number" || !Number.isFinite(pid) || !Number.isInteger(pid)) throw makeInvalidPidError(pid);
		return Object.is(pid, -0) ? 0 : pid;
	}
	function normalizeSignal(signal) {
		if (signal === void 0) return SIGNAL_NAME_TO_NUMBER.SIGTERM;
		if (typeof signal === "string") {
			if (signal === "0") return 0;
			const numeric = SIGNAL_NAME_TO_NUMBER[signal];
			if (numeric === void 0) {
				const err = /* @__PURE__ */ new TypeError(`Unknown signal: ${signal}`);
				err.code = "ERR_UNKNOWN_SIGNAL";
				throw err;
			}
			return numeric;
		}
		if (typeof signal !== "number" || !Number.isFinite(signal) || !Number.isInteger(signal)) {
			const err = /* @__PURE__ */ new Error("kill EINVAL");
			err.code = "EINVAL";
			throw err;
		}
		if (signal < 0 || signal > 31) {
			const err = /* @__PURE__ */ new Error("kill EINVAL");
			err.code = "EINVAL";
			throw err;
		}
		return signal;
	}
	function inspectExecveValue(value) {
		if (typeof value === "string") return `'${value.replace(/\0/g, "\\x00")}'`;
		return String(value);
	}
	function validateExecveArguments(execPath, args, envObj, hasEnvArgument) {
		if (typeof execPath !== "string") {
			const err = /* @__PURE__ */ new TypeError(`The "execPath" argument must be of type string. Received type ${typeof execPath} (${String(execPath)})`);
			err.code = "ERR_INVALID_ARG_TYPE";
			throw err;
		}
		if (!Array.isArray(args)) {
			const err = /* @__PURE__ */ new TypeError(`The "args" argument must be an instance of Array. Received type ${typeof args} (${inspectExecveValue(args)})`);
			err.code = "ERR_INVALID_ARG_TYPE";
			throw err;
		}
		for (let index = 0; index < args.length; index++) {
			const value = args[index];
			if (typeof value !== "string" || value.includes("\0")) {
				const err = /* @__PURE__ */ new TypeError(`The argument 'args[${index}]' must be a string without null bytes. Received ${inspectExecveValue(value)}`);
				err.code = "ERR_INVALID_ARG_VALUE";
				throw err;
			}
		}
		if (!hasEnvArgument) return;
		if (typeof envObj !== "object" || envObj === null || Array.isArray(envObj)) {
			const err = /* @__PURE__ */ new TypeError(`The "env" argument must be of type object. Received type ${typeof envObj} (${inspectExecveValue(envObj)})`);
			err.code = "ERR_INVALID_ARG_TYPE";
			throw err;
		}
		const entries = Object.keys(envObj);
		{
			const __exactForOfIterator116 = entries[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep116 = __exactForOfIterator116.next();
				if (__exactForOfStep116.done) break;
				const value = envObj[__exactForOfStep116.value];
				if (typeof value !== "string" || value.includes("\0")) {
					const pairs = entries.map((entryKey) => `${entryKey}: ${inspectExecveValue(envObj[entryKey])}`);
					const err = /* @__PURE__ */ new TypeError(`The argument 'env' must be an object with string keys and values without null bytes. Received { ${pairs.join(", ")} }`);
					err.code = "ERR_INVALID_ARG_VALUE";
					throw err;
				}
			}
		}
	}
	function isChildProcessPermissionDenied(execArgv) {
		let permissionMode = false;
		let allowChildProcess = false;
		for (const rawArg of execArgv) {
			const arg = String(rawArg);
			if (arg === "--permission" || arg.startsWith("--permission=")) {
				permissionMode = true;
				continue;
			}
			if (arg === "--allow-child-process" || arg.startsWith("--allow-child-process=")) allowChildProcess = true;
		}
		return permissionMode && !allowChildProcess;
	}
	//#endregion
	//#region ../ibex-runtime-js/src/bootstrap.ts
	/**
	* Exact Runtime Bootstrap
	*
	* Installs web-standard globals on demand using lazy loading.
	* Essential APIs (console, timers, process, Buffer, require) load eagerly.
	* Non-essential APIs (streams, crypto, fetch, inspect) use lazy getters
	* that self-replace on first access, reducing startup GC pressure.
	*/
	init_defineProperty();
	function createGlobalBufferConstructor(BufferCtor) {
		function Buffer(value, encodingOrOffset, length) {
			if (typeof value === "number") {
				if (arguments.length > 1) {
					const err = /* @__PURE__ */ new TypeError(`The "string" argument must be of type string. Received type number (${value})`);
					err.code = "ERR_INVALID_ARG_TYPE";
					throw err;
				}
				return BufferCtor.allocUnsafe(value);
			}
			return BufferCtor.from(value, encodingOrOffset, length);
		}
		Object.setPrototypeOf(Buffer, BufferCtor);
		Object.defineProperty(Buffer, "prototype", {
			value: BufferCtor.prototype,
			writable: false,
			configurable: false
		});
		return Buffer;
	}
	/**
	* Define a lazy global property that self-replaces with the real value on first access.
	* This avoids eagerly assigning heavy objects to globalThis during bootstrap,
	* reducing GC registration and startup cost for unused APIs.
	*/
	function defineLazyGlobal(g, name, factory) {
		if (typeof g[name] !== "undefined") return;
		Object.defineProperty(g, name, {
			configurable: true,
			enumerable: true,
			get() {
				const value = factory();
				Object.defineProperty(g, name, {
					value,
					writable: true,
					configurable: true,
					enumerable: true
				});
				return value;
			}
		});
	}
	function preserveConstructorName(value, expectedName) {
		if (typeof value === "function" && value.name !== expectedName) try {
			Object.defineProperty(value, "name", {
				value: expectedName,
				writable: false,
				configurable: true,
				enumerable: false
			});
		} catch (_) {}
		return value;
	}
	let _fetchInitialized = false;
	let _fsModuleInitialized = false;
	/**
	* Ensure fetch globals are initialized (called lazily on first access)
	*/
	function ensureFetchInitialized() {
		if (_fetchInitialized) return;
		_fetchInitialized = true;
		const g = globalThis;
		if (typeof g.__nativeFetch === "function") {
			initializeNativeFetch();
			installFetchGlobals(g);
		}
	}
	let _cryptoInitialized = false;
	/**
	* Get the crypto object, initializing native crypto module if needed.
	* Called lazily on first access of globalThis.crypto.
	*/
	function ensureCryptoInitialized() {
		if (_cryptoInitialized) return crypto$1;
		_cryptoInitialized = true;
		return crypto$1;
	}
	/**
	* Install runtime globals with lazy loading for non-essential APIs.
	*
	* Essential (immediate): process, __exactRuntimeLoaded, capabilities, fs bridge
	* Lazy (on first access): crypto, streams, fetch, inspect
	*/
	function installGlobals() {
		const g = globalThis;
		if (g.__exactLoadTimings) g.__exactLoadTimings.installGlobalsStart = Date.now();
		if (typeof g.__exactHostNavigator === "undefined" && typeof g.navigator === "object" && g.navigator) g.__exactHostNavigator = g.navigator;
		installExactLocaleGlobal();
		installExactAccessibilityGlobal();
		g.__exactRuntimeLoaded = true;
		const _prevEnv = {};
		const _oldProcess = g.process;
		if (g.__exactRuntimeContext !== "shell" && g.process?.env && typeof g.process.env === "object") {
			const hostEnv = g.process.env;
			{
				const __exactForOfIterator117 = [
					"NODE_ENV",
					"EXACT_ALLOW_INSECURE_CRYPTO",
					"CI",
					"TEST"
				][Symbol.iterator]();
				for (;;) {
					const __exactForOfStep117 = __exactForOfIterator117.next();
					if (__exactForOfStep117.done) break;
					const key = __exactForOfStep117.value;
					const val = hostEnv[key];
					if (typeof val === "string") _prevEnv[key] = val;
				}
			}
		}
		g.process = process$1;
		if (_oldProcess && typeof _oldProcess === "object") {
			const skip = new Set([
				"env",
				"version",
				"versions",
				"platform",
				"arch",
				"pid",
				"ppid",
				"argv",
				"argv0",
				"execPath",
				"execArgv",
				"title",
				"stdin",
				"stdout",
				"stderr",
				"config",
				"release",
				"browser",
				"on",
				"off",
				"once",
				"emit",
				"removeListener",
				"removeAllListeners",
				"listeners",
				"listenerCount",
				"addListener",
				"_events"
			]);
			{
				const __exactForOfIterator118 = Object.getOwnPropertyNames(_oldProcess)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep118 = __exactForOfIterator118.next();
					if (__exactForOfStep118.done) break;
					const key = __exactForOfStep118.value;
					if (!skip.has(key) && process$1[key] === void 0) try {
						process$1[key] = _oldProcess[key];
					} catch (_e) {}
				}
			}
			const proto = Object.getPrototypeOf(_oldProcess);
			if (proto && proto !== Object.prototype) {
				const __exactForOfIterator119 = Object.getOwnPropertyNames(proto)[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep119 = __exactForOfIterator119.next();
					if (__exactForOfStep119.done) break;
					const key = __exactForOfStep119.value;
					if (!skip.has(key) && process$1[key] === void 0) try {
						process$1[key] = proto[key];
					} catch (_e) {}
				}
			}
		}
		{
			const __exactForOfIterator120 = Object.entries(_prevEnv)[Symbol.iterator]();
			for (;;) {
				const __exactForOfStep120 = __exactForOfIterator120.next();
				if (__exactForOfStep120.done) break;
				const [key, val] = __exactForOfStep120.value;
				if (process$1.env[key] === void 0 || process$1.env[key] === "development") process$1.env[key] = val;
			}
		}
		if (typeof g.Buffer === "undefined") Object.defineProperty(g, "Buffer", {
			value: createGlobalBufferConstructor(Buffer$1),
			writable: true,
			configurable: true,
			enumerable: true
		});
		Object.defineProperty(g, "setImmediate", {
			value: setImmediate,
			writable: true,
			configurable: true,
			enumerable: true
		});
		Object.defineProperty(g, "clearImmediate", {
			value: clearImmediate,
			writable: true,
			configurable: true,
			enumerable: true
		});
		const exactFormatBytes = (value) => {
			let bytes = typeof value === "number" ? value : Number(value);
			if (!Number.isFinite(bytes) || bytes <= 0) return "0B";
			const units = [
				"B",
				"KB",
				"MB",
				"GB",
				"TB"
			];
			let unitIndex = 0;
			while (bytes >= 1024 && unitIndex < units.length - 1) {
				bytes /= 1024;
				unitIndex += 1;
			}
			return `${bytes >= 100 || unitIndex === 0 ? bytes.toFixed(0) : bytes.toFixed(1).replace(/\.0$/, "")}${units[unitIndex]}`;
		};
		const exactEstimateStringBytes = (value) => {
			if (typeof value !== "string" || value.length === 0) return 0;
			if (typeof TextEncoder$1 === "function") try {
				return new TextEncoder$1().encode(value).length;
			} catch {
				return value.length * 2;
			}
			return value.length * 2;
		};
		const exactNormalizeMemoryOptions = (input) => {
			if (input && typeof input === "object") {
				const candidate = input;
				return {
					includeExpensive: candidate.includeExpensive === true,
					includeGCStats: candidate.includeGCStats === true
				};
			}
			return {
				includeExpensive: input === true,
				includeGCStats: false
			};
		};
		const exactGetDebugModuleSourcesSummary = () => {
			const entries = Array.isArray(g.__exactDebugModuleSources) ? g.__exactDebugModuleSources : [];
			let totalBytes = 0;
			{
				const __exactForOfIterator121 = entries[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep121 = __exactForOfIterator121.next();
					if (__exactForOfStep121.done) break;
					const entry = __exactForOfStep121.value;
					if (entry && typeof entry.source === "string") totalBytes += exactEstimateStringBytes(entry.source);
				}
			}
			const configuredLimit = Number(g.__exactDebugModuleSourceLimit);
			return {
				count: entries.length,
				totalBytes,
				currentSourceBytes: exactEstimateStringBytes(typeof g.__exactDebugModuleSource === "string" ? g.__exactDebugModuleSource : ""),
				limit: Number.isFinite(configuredLimit) && configuredLimit > 0 ? Math.floor(configuredLimit) : 256
			};
		};
		const exactGetPerformanceTimelineSummary = () => {
			const performanceObject = typeof g.performance === "object" && g.performance !== null ? g.performance : null;
			if (!performanceObject) return {
				entries: 0,
				markNames: 0,
				measureNames: 0
			};
			return {
				entries: Array.isArray(performanceObject._entries) ? performanceObject._entries.length : 0,
				markNames: typeof performanceObject._marks?.size === "number" ? performanceObject._marks.size : 0,
				measureNames: typeof performanceObject._measures?.size === "number" ? performanceObject._measures.size : 0
			};
		};
		const exactMemorySnapshot = (input) => {
			const options = exactNormalizeMemoryOptions(input);
			let usage = null;
			if (typeof process$1.memoryUsage === "function") try {
				usage = process$1.memoryUsage();
			} catch {
				usage = null;
			}
			let heapInfo = null;
			if (typeof g.__exactGetHeapInfo === "function") try {
				heapInfo = g.__exactGetHeapInfo(options.includeExpensive);
			} catch {
				heapInfo = null;
			}
			let gcStats = null;
			if (options.includeGCStats && typeof g.__exactGetGCStats === "function") try {
				const rawGcStats = g.__exactGetGCStats();
				gcStats = typeof rawGcStats === "string" ? JSON.parse(rawGcStats) : null;
			} catch {
				gcStats = null;
			}
			let sourceCache = null;
			if (typeof g.__exactGetSourceCacheStats === "function") try {
				sourceCache = g.__exactGetSourceCacheStats();
			} catch {
				sourceCache = null;
			}
			const loader = typeof g.__exactRequire === "function" ? g.__exactRequire : g.require;
			const requireCacheCount = loader && loader.cache && typeof loader.cache === "object" ? Object.keys(loader.cache).length : 0;
			return {
				timestamp: Date.now(),
				usage,
				heapInfo,
				gcStats,
				sourceCache,
				performanceTimeline: exactGetPerformanceTimelineSummary(),
				requireCacheCount,
				debugModuleSources: exactGetDebugModuleSourcesSummary()
			};
		};
		const exactMemorySummary = (snapshotInput) => {
			const snapshot = snapshotInput && typeof snapshotInput === "object" ? snapshotInput : exactMemorySnapshot(snapshotInput);
			const usage = snapshot.usage && typeof snapshot.usage === "object" ? snapshot.usage : {};
			const heapInfo = snapshot.heapInfo && typeof snapshot.heapInfo === "object" ? snapshot.heapInfo : {};
			const sourceCache = snapshot.sourceCache && typeof snapshot.sourceCache === "object" ? snapshot.sourceCache : {};
			const performanceTimeline = snapshot.performanceTimeline && typeof snapshot.performanceTimeline === "object" ? snapshot.performanceTimeline : {};
			const debugModuleSources = snapshot.debugModuleSources && typeof snapshot.debugModuleSources === "object" ? snapshot.debugModuleSources : {};
			const heapUsed = typeof usage.heapUsed === "number" ? usage.heapUsed : typeof heapInfo.hermes_allocatedBytes === "number" ? heapInfo.hermes_allocatedBytes : 0;
			const heapTotal = typeof usage.heapTotal === "number" ? usage.heapTotal : typeof heapInfo.hermes_heapSize === "number" ? heapInfo.hermes_heapSize : 0;
			return {
				timestamp: typeof snapshot.timestamp === "number" ? snapshot.timestamp : Date.now(),
				heapUsed,
				heapTotal,
				rss: typeof usage.rss === "number" ? usage.rss : 0,
				external: typeof usage.external === "number" ? usage.external : typeof heapInfo.hermes_externalBytes === "number" ? heapInfo.hermes_externalBytes : 0,
				requireCacheCount: typeof snapshot.requireCacheCount === "number" ? snapshot.requireCacheCount : 0,
				performanceEntriesCount: typeof performanceTimeline.entries === "number" ? performanceTimeline.entries : 0,
				debugModuleSourcesCount: typeof debugModuleSources.count === "number" ? debugModuleSources.count : 0,
				debugModuleSourcesBytes: typeof debugModuleSources.totalBytes === "number" ? debugModuleSources.totalBytes : 0,
				sourceCacheCount: typeof sourceCache.count === "number" ? sourceCache.count : 0,
				sourceCacheBytes: typeof sourceCache.totalBytes === "number" ? sourceCache.totalBytes : 0,
				numCollections: typeof heapInfo.hermes_numCollections === "number" ? heapInfo.hermes_numCollections : typeof heapInfo.numCollections === "number" ? heapInfo.numCollections : 0
			};
		};
		const exactLogMemorySample = (snapshot) => {
			const summary = exactMemorySummary(snapshot);
			console.info("[exact:memory]", `heap=${exactFormatBytes(summary.heapUsed)}/${exactFormatBytes(summary.heapTotal)}`, `rss=${exactFormatBytes(summary.rss)}`, `external=${exactFormatBytes(summary.external)}`, `require=${summary.requireCacheCount}`, `perf=${summary.performanceEntriesCount}`, `debugSources=${summary.debugModuleSourcesCount}/${exactFormatBytes(summary.debugModuleSourcesBytes)}`, `sourceCache=${summary.sourceCacheCount}/${exactFormatBytes(summary.sourceCacheBytes)}`, `gc=${summary.numCollections}`);
		};
		if (!g.__exactMemoryDebugState || typeof g.__exactMemoryDebugState !== "object") g.__exactMemoryDebugState = {
			timer: null,
			samples: [],
			nextSampleId: 0,
			options: null,
			sampleCount: 0,
			lastLoggedHeapUsed: 0
		};
		const exactMemoryDebugState = g.__exactMemoryDebugState;
		const exactStopMemorySampler = () => {
			if (exactMemoryDebugState.timer != null) {
				clearInterval(exactMemoryDebugState.timer);
				exactMemoryDebugState.timer = null;
			}
			return exactMemoryDebugState.samples.slice();
		};
		g.__exactMemoryDebug = {
			snapshot: (options) => exactMemorySnapshot(options),
			summary: (options) => exactMemorySummary(exactMemorySnapshot(options)),
			samples: () => exactMemoryDebugState.samples.slice(),
			state: () => ({
				running: exactMemoryDebugState.timer != null,
				options: exactMemoryDebugState.options,
				sampleCount: exactMemoryDebugState.sampleCount,
				retainedSamples: exactMemoryDebugState.samples.length
			}),
			clearModuleDebugSources: () => {
				if (Array.isArray(g.__exactDebugModuleSources)) g.__exactDebugModuleSources.length = 0;
				g.__exactDebugModuleSource = void 0;
				return exactGetDebugModuleSourcesSummary();
			},
			stop: () => exactStopMemorySampler(),
			start: (options) => {
				exactStopMemorySampler();
				const normalizedOptions = {
					intervalMs: Math.max(200, Number(options?.intervalMs) || 1e3),
					maxSamples: Math.max(10, Number(options?.maxSamples) || 240),
					logEvery: Math.max(0, Math.floor(Number(options?.logEvery) || 0)),
					logOnGrowthBytes: Math.max(0, Math.floor(Number(options?.logOnGrowthBytes) || 32 * 1024 * 1024)),
					includeExpensive: options?.includeExpensive === true,
					includeGCStats: options?.includeGCStats === true
				};
				exactMemoryDebugState.samples = [];
				exactMemoryDebugState.sampleCount = 0;
				exactMemoryDebugState.lastLoggedHeapUsed = 0;
				exactMemoryDebugState.options = normalizedOptions;
				const takeSample = () => {
					const sample = exactMemorySnapshot(normalizedOptions);
					exactMemoryDebugState.nextSampleId += 1;
					sample.sampleId = exactMemoryDebugState.nextSampleId;
					exactMemoryDebugState.samples.push(sample);
					if (exactMemoryDebugState.samples.length > normalizedOptions.maxSamples) exactMemoryDebugState.samples.splice(0, exactMemoryDebugState.samples.length - normalizedOptions.maxSamples);
					exactMemoryDebugState.sampleCount += 1;
					const summary = exactMemorySummary(sample);
					if (exactMemoryDebugState.sampleCount === 1 || normalizedOptions.logEvery > 0 && exactMemoryDebugState.sampleCount % normalizedOptions.logEvery === 0 || normalizedOptions.logOnGrowthBytes > 0 && summary.heapUsed - exactMemoryDebugState.lastLoggedHeapUsed >= normalizedOptions.logOnGrowthBytes) {
						exactMemoryDebugState.lastLoggedHeapUsed = summary.heapUsed;
						exactLogMemorySample(sample);
					}
					return sample;
				};
				const first = takeSample();
				exactMemoryDebugState.timer = setInterval(takeSample, normalizedOptions.intervalMs);
				if (typeof exactMemoryDebugState.timer?.unref === "function") exactMemoryDebugState.timer.unref();
				return first;
			},
			formatBytes: exactFormatBytes
		};
		if (typeof g.__exactCapabilityCheck === "function") {
			setNativeCapabilityModule({
				checkCapability: (capability, moduleId) => !!g.__exactCapabilityCheck(capability, moduleId),
				requestCapability: async (capability) => !!g.__exactCapabilityCheck(capability),
				getGrantedCapabilities: () => []
			});
			enableStrictMode();
		}
		if (typeof g.__exactRandomBytes === "function") setNativeCryptoModule({
			getRandomValues: (array) => {
				const bytes = g.__exactRandomBytes(array.byteLength);
				array.set(bytes);
				return array;
			},
			randomUUID: () => {
				const bytes = g.__exactRandomBytes(16);
				bytes[6] = bytes[6] & 15 | 64;
				bytes[8] = bytes[8] & 63 | 128;
				const hex = Array.from(bytes, (b) => b.toString(16).padStart(2, "0"));
				return hex.slice(0, 4).join("") + "-" + hex.slice(4, 6).join("") + "-" + hex.slice(6, 8).join("") + "-" + hex.slice(8, 10).join("") + "-" + hex.slice(10).join("");
			},
			sha256: async () => {
				throw new DOMException$6("sha256 not available", "NotSupportedError");
			},
			sha384: async () => {
				throw new DOMException$6("sha384 not available", "NotSupportedError");
			},
			sha512: async () => {
				throw new DOMException$6("sha512 not available", "NotSupportedError");
			},
			sha1: async () => {
				throw new DOMException$6("sha1 not available", "NotSupportedError");
			},
			digest: typeof g.__exactCryptoSubtleDigest === "function" ? async (hash, data) => Promise.resolve(g.__exactCryptoSubtleDigest(hash, data)) : void 0,
			hmacSign: typeof g.__exactCryptoSubtleSign === "function" ? async (hash, key, data) => Promise.resolve(g.__exactCryptoSubtleSign(hash, key, data)) : void 0,
			hmacVerify: typeof g.__exactCryptoSubtleVerify === "function" ? async (hash, key, signature, data) => Promise.resolve(g.__exactCryptoSubtleVerify(hash, key, signature, data)) : void 0
		});
		if (!g.crypto) Object.defineProperty(g, "crypto", {
			configurable: true,
			enumerable: true,
			get() {
				const value = ensureCryptoInitialized();
				Object.defineProperty(g, "crypto", {
					value,
					writable: true,
					configurable: true,
					enumerable: true
				});
				return value;
			}
		});
		else {
			const fullSubtle = crypto$1.subtle;
			const subtleDescriptor = Object.getOwnPropertyDescriptor(g.crypto, "subtle");
			if (!subtleDescriptor || subtleDescriptor.configurable) Object.defineProperty(g.crypto, "subtle", {
				get() {
					return fullSubtle;
				},
				set() {},
				enumerable: true,
				configurable: true
			});
		}
		if (g.crypto && g.crypto.subtle) try {
			Object.defineProperty(g.crypto.subtle, "__exactFullSubtle", {
				value: true,
				writable: false,
				enumerable: false,
				configurable: false
			});
		} catch (_) {}
		if (typeof g.Crypto === "undefined") Object.defineProperty(g, "Crypto", {
			value: Crypto,
			writable: true,
			configurable: true,
			enumerable: true
		});
		if (typeof g.SubtleCrypto === "undefined") Object.defineProperty(g, "SubtleCrypto", {
			value: SubtleCrypto,
			writable: true,
			configurable: true,
			enumerable: true
		});
		if (typeof g.CryptoKey === "undefined") Object.defineProperty(g, "CryptoKey", {
			value: ExactCryptoKey,
			writable: true,
			configurable: true,
			enumerable: true
		});
		if (typeof g.__exactReadFile === "function" || typeof g.__exactEnsureFs === "function") g.__exactEnsureFilesystemModule = () => {
			if (_fsModuleInitialized) return;
			if (typeof g.__exactReadFile !== "function" && typeof g.__exactEnsureFs === "function") g.__exactEnsureFs();
			if (typeof g.__exactReadFile !== "function") return;
			_fsModuleInitialized = true;
			const toBytes = (data) => {
				if (data instanceof Uint8Array) return data;
				return new Uint8Array(data);
			};
			setNativeFileSystemModule({
				readFile: async (path) => g.__exactReadFile(path),
				writeFile: async (path, data) => g.__exactWriteFile(path, toBytes(data)),
				appendFile: async (path, data) => g.__exactAppendFile(path, toBytes(data)),
				exists: async (path) => {
					try {
						g.__exactAccess(path, 0);
						return true;
					} catch {
						return false;
					}
				},
				unlink: async (path) => g.__exactUnlink(path),
				stat: async (path) => JSON.parse(g.__exactStat(path)),
				lstat: async (path) => JSON.parse(g.__exactLstat(path)),
				readdir: async (path) => JSON.parse(g.__exactReaddir(path)),
				mkdir: async (path, options) => g.__exactMkdir(path, !!(options && options.recursive)),
				rmdir: async (path) => g.__exactRmdir(path),
				rename: async (oldPath, newPath) => g.__exactRename(oldPath, newPath),
				copyFile: async (src, dest) => g.__exactCopyFile(src, dest),
				access: async (path, mode) => g.__exactAccess(path, mode ?? 0),
				chmod: async (path, mode) => g.__exactChmod(path, mode),
				realpath: async (path) => g.__exactRealpath(path),
				mkdtemp: async (prefix) => g.__exactMkdtemp(prefix),
				getDocumentsDir: () => "/",
				getCacheDir: () => "/",
				getTempDir: () => "/"
			});
		};
		Object.defineProperty(g, "Headers", {
			value: Headers,
			writable: true,
			configurable: true,
			enumerable: true
		});
		Object.defineProperty(g, "Request", {
			value: Request,
			writable: true,
			configurable: true,
			enumerable: true
		});
		Object.defineProperty(g, "Response", {
			value: Response$1,
			writable: true,
			configurable: true,
			enumerable: true
		});
		if (typeof g.__nativeFetch === "function") {
			if (typeof g.fetch === "undefined") Object.defineProperty(g, "fetch", {
				configurable: true,
				enumerable: true,
				get() {
					delete g.fetch;
					ensureFetchInitialized();
					return g.fetch;
				}
			});
		}
		if (typeof g.__exactWsConnect === "function") setNativeWebSocketModule({
			create(url, protocols, instance) {
				const protocolStr = (protocols || []).join(",");
				return g.__exactWsConnect(url, protocolStr, instance);
			},
			send(id, data) {
				g.__exactWsSend(id, data);
			},
			close(id, code, reason) {
				g.__exactWsClose(id, code ?? 1005, reason ?? "");
			},
			pause(id) {
				if (typeof g.__exactWsPause === "function") g.__exactWsPause(id);
			},
			resume(id) {
				if (typeof g.__exactWsResume === "function") g.__exactWsResume(id);
			},
			setFlowControlled(id, enabled) {
				if (typeof g.__exactWsSetFlowControlled === "function") g.__exactWsSetFlowControlled(id, !!enabled);
			}
		});
		g.Exact = g.Exact || {};
		Object.defineProperty(g.Exact, "inspect", {
			configurable: true,
			enumerable: true,
			get() {
				Object.defineProperty(g.Exact, "inspect", {
					value: inspect,
					writable: true,
					configurable: true,
					enumerable: true
				});
				return inspect;
			}
		});
		g.Exact.setModuleCapabilities = function(moduleId, capabilities) {
			const grantFn = g.__exactGrantCapability;
			if (typeof grantFn === "function") for (let i = 0; i < capabilities.length; i++) grantFn(moduleId, capabilities[i]);
		};
		defineLazyGlobal(g, "ReadableStream", () => preserveConstructorName(ReadableStream$1, "ReadableStream"));
		defineLazyGlobal(g, "ReadableStreamDefaultReader", () => preserveConstructorName(ReadableStreamDefaultReader, "ReadableStreamDefaultReader"));
		defineLazyGlobal(g, "ReadableStreamDefaultController", () => preserveConstructorName(ReadableStreamDefaultController, "ReadableStreamDefaultController"));
		defineLazyGlobal(g, "ReadableByteStreamController", () => preserveConstructorName(ReadableByteStreamController, "ReadableByteStreamController"));
		defineLazyGlobal(g, "ReadableStreamBYOBReader", () => preserveConstructorName(ReadableStreamBYOBReader, "ReadableStreamBYOBReader"));
		defineLazyGlobal(g, "ReadableStreamBYOBRequest", () => preserveConstructorName(ReadableStreamBYOBRequest, "ReadableStreamBYOBRequest"));
		defineLazyGlobal(g, "WritableStream", () => preserveConstructorName(WritableStream$1, "WritableStream"));
		defineLazyGlobal(g, "WritableStreamDefaultWriter", () => preserveConstructorName(WritableStreamDefaultWriter, "WritableStreamDefaultWriter"));
		defineLazyGlobal(g, "WritableStreamDefaultController", () => preserveConstructorName(WritableStreamDefaultController, "WritableStreamDefaultController"));
		defineLazyGlobal(g, "TransformStream", () => preserveConstructorName(TransformStream, "TransformStream"));
		defineLazyGlobal(g, "TransformStreamDefaultController", () => preserveConstructorName(TransformStreamDefaultController, "TransformStreamDefaultController"));
		defineLazyGlobal(g, "ByteLengthQueuingStrategy", () => preserveConstructorName(ByteLengthQueuingStrategy, "ByteLengthQueuingStrategy"));
		defineLazyGlobal(g, "CountQueuingStrategy", () => preserveConstructorName(CountQueuingStrategy, "CountQueuingStrategy"));
		if (typeof g.__exactIsReadableStream !== "function") g.__exactIsReadableStream = isReadableStream;
		defineLazyGlobal(g, "Blob", () => Blob$1);
		defineLazyGlobal(g, "File", () => File$1);
		defineLazyGlobal(g, "FormData", () => FormData$1);
		defineLazyGlobal(g, "Event", () => Event);
		defineLazyGlobal(g, "CustomEvent", () => CustomEvent);
		defineLazyGlobal(g, "EventTarget", () => EventTarget);
		defineLazyGlobal(g, "DOMException", () => DOMException$6);
		defineLazyGlobal(g, "MessageEvent", () => MessageEvent$1);
		defineLazyGlobal(g, "CloseEvent", () => {
			const globalEvent = g.Event || Event;
			if (globalEvent && Object.getPrototypeOf(CloseEvent) !== globalEvent) Object.setPrototypeOf(CloseEvent, globalEvent);
			if (globalEvent && globalEvent.prototype && Object.getPrototypeOf(CloseEvent.prototype) !== globalEvent.prototype) Object.setPrototypeOf(CloseEvent.prototype, globalEvent.prototype);
			return CloseEvent;
		});
		defineLazyGlobal(g, "ErrorEvent", () => ErrorEvent);
		defineLazyGlobal(g, "ProgressEvent", () => ProgressEvent);
		defineLazyGlobal(g, "KeyboardEvent", () => KeyboardEvent);
		defineLazyGlobal(g, "FocusEvent", () => FocusEvent);
		defineLazyGlobal(g, "PromiseRejectionEvent", () => PromiseRejectionEvent);
		defineLazyGlobal(g, "AbortController", () => AbortController$1);
		defineLazyGlobal(g, "AbortSignal", () => AbortSignal);
		defineLazyGlobal(g, "TextEncoder", () => TextEncoder$1);
		defineLazyGlobal(g, "TextDecoder", () => TextDecoder$1);
		defineLazyGlobal(g, "TextEncoderStream", () => TextEncoderStream);
		defineLazyGlobal(g, "TextDecoderStream", () => TextDecoderStream);
		if (typeof g.URL === "undefined") Object.defineProperty(g, "URL", {
			value: URL$1,
			writable: true,
			configurable: true,
			enumerable: true
		});
		if (typeof g.URLSearchParams === "undefined") Object.defineProperty(g, "URLSearchParams", {
			value: URLSearchParams$1,
			writable: true,
			configurable: true,
			enumerable: true
		});
		if (typeof g.URL === "function") {
			const urlCtor = g.URL;
			if (typeof urlCtor.createObjectURL !== "function") Object.defineProperty(urlCtor, "createObjectURL", {
				value: URL$1.createObjectURL,
				writable: true,
				configurable: true,
				enumerable: true
			});
			if (typeof urlCtor.revokeObjectURL !== "function") Object.defineProperty(urlCtor, "revokeObjectURL", {
				value: URL$1.revokeObjectURL,
				writable: true,
				configurable: true,
				enumerable: true
			});
		}
		defineLazyGlobal(g, "URLPattern", () => URLPattern);
		defineLazyGlobal(g, "MessageChannel", () => MessageChannel);
		defineLazyGlobal(g, "MessagePort", () => MessagePort);
		defineLazyGlobal(g, "VideoFrame", () => VideoFrame);
		defineLazyGlobal(g, "BroadcastChannel", () => BroadcastChannel);
		defineLazyGlobal(g, "WebSocket", () => {
			const globalEventTarget = g.EventTarget || EventTarget;
			if (globalEventTarget && Object.getPrototypeOf(WebSocket) !== globalEventTarget) Object.setPrototypeOf(WebSocket, globalEventTarget);
			if (globalEventTarget && globalEventTarget.prototype && Object.getPrototypeOf(WebSocket.prototype) !== globalEventTarget.prototype) Object.setPrototypeOf(WebSocket.prototype, globalEventTarget.prototype);
			return WebSocket;
		});
		defineLazyGlobal(g, "WebSocketError", () => WebSocketError);
		defineLazyGlobal(g, "WebSocketStream", () => WebSocketStream);
		defineLazyGlobal(g, "EventSource", () => EventSource);
		defineLazyGlobal(g, "FileReader", () => FileReader);
		Object.defineProperty(g, "navigator", {
			value: navigator$1,
			writable: true,
			configurable: true,
			enumerable: true
		});
		defineLazyGlobal(g, "localStorage", () => localStorage);
		defineLazyGlobal(g, "sessionStorage", () => sessionStorage);
		Object.defineProperty(g, "performance", {
			value: performance$1,
			writable: true,
			configurable: true,
			enumerable: true
		});
		defineLazyGlobal(g, "PerformanceObserver", () => PerformanceObserver);
		defineLazyGlobal(g, "PerformanceEntry", () => PerformanceEntry);
		defineLazyGlobal(g, "PerformanceMark", () => PerformanceMark);
		defineLazyGlobal(g, "PerformanceMeasure", () => PerformanceMeasure);
		Object.defineProperty(g, "structuredClone", {
			value: structuredClone,
			writable: true,
			configurable: true,
			enumerable: true
		});
		defineLazyGlobal(g, "atob", () => atob$1);
		defineLazyGlobal(g, "btoa", () => btoa$1);
		defineLazyGlobal(g, "requestAnimationFrame", () => requestAnimationFrame$1);
		defineLazyGlobal(g, "cancelAnimationFrame", () => cancelAnimationFrame$1);
		defineLazyGlobal(g, "requestIdleCallback", () => requestIdleCallback);
		defineLazyGlobal(g, "cancelIdleCallback", () => cancelIdleCallback);
		defineLazyGlobal(g, "CompressionStream", () => CompressionStream);
		defineLazyGlobal(g, "DecompressionStream", () => DecompressionStream);
		defineLazyGlobal(g, "caches", () => new CacheStorage());
		defineLazyGlobal(g, "ClipboardItem", () => ClipboardItem);
		defineLazyGlobal(g, "indexedDB", () => new IDBFactory());
		defineLazyGlobal(g, "IDBKeyRange", () => IDBKeyRange);
		defineLazyGlobal(g, "IDBDatabase", () => IDBDatabase);
		defineLazyGlobal(g, "IDBObjectStore", () => IDBObjectStore);
		defineLazyGlobal(g, "IDBTransaction", () => IDBTransaction);
		defineLazyGlobal(g, "IDBRequest", () => IDBRequest);
		defineLazyGlobal(g, "IDBOpenDBRequest", () => IDBOpenDBRequest);
		defineLazyGlobal(g, "IDBCursor", () => IDBCursor);
		defineLazyGlobal(g, "IDBCursorWithValue", () => IDBCursorWithValue);
		defineLazyGlobal(g, "IDBIndex", () => IDBIndex);
		if (typeof g.window === "undefined") g.window = g;
		if (typeof g.self === "undefined") g.self = g;
		if (typeof g.global === "undefined") g.global = g;
		defineLazyGlobal(g, "matchMedia", () => window$1.matchMedia.bind(window$1));
		defineLazyGlobal(g, "MediaQueryList", () => MediaQueryList);
		defineLazyGlobal(g, "MediaQueryListEvent", () => MediaQueryListEvent);
		g.Exact = g.Exact || {};
		if (typeof g.__exactReadFile === "function" || typeof g.__exactEnsureFs === "function") {
			const exactFileDescriptor = Object.getOwnPropertyDescriptor(g.Exact, "file");
			const exactWriteDescriptor = Object.getOwnPropertyDescriptor(g.Exact, "write");
			const hasConcreteExactFile = !!exactFileDescriptor && !exactFileDescriptor.get && typeof exactFileDescriptor.value === "function";
			const hasConcreteExactWrite = !!exactWriteDescriptor && !exactWriteDescriptor.get && typeof exactWriteDescriptor.value === "function";
			if (!hasConcreteExactFile || !hasConcreteExactWrite) {
				let _exactGlobalInstalled = false;
				let _exactFileFactory;
				let _exactWriteFactory;
				const ensureExactGlobal = () => {
					if (_exactGlobalInstalled) return {
						file: _exactFileFactory,
						write: _exactWriteFactory
					};
					_exactGlobalInstalled = true;
					const installed = installExactGlobal();
					_exactFileFactory = installed.file;
					_exactWriteFactory = installed.write;
					return installed;
				};
				Object.defineProperty(g.Exact, "file", {
					configurable: true,
					enumerable: true,
					get() {
						return ensureExactGlobal().file;
					}
				});
				Object.defineProperty(g.Exact, "write", {
					configurable: true,
					enumerable: true,
					get() {
						return ensureExactGlobal().write;
					}
				});
			}
		}
		const compatModes = g.__exactCompatModes;
		if ((Array.isArray(compatModes) && compatModes.indexOf("bun") !== -1 || g.process?.env?.EXACT_COMPAT_BUN === "1") && !g.Bun) g.Bun = g.Exact;
		if (g.Exact && !g.Exact.env) Object.defineProperty(g.Exact, "env", {
			get() {
				return g.process?.env;
			},
			configurable: true,
			enumerable: true
		});
		if (g.Exact && !g.Exact.sleep) {
			g.Exact.sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
			g.Exact.sleepSync = (ms) => {
				const end = Date.now() + ms;
				while (Date.now() < end);
			};
		}
		if (g.Exact && !g.Exact.which) g.Exact.which = (_cmd) => null;
		if (g.Exact) {
			const bunUnsafe = g.Exact.unsafe && typeof g.Exact.unsafe === "object" ? g.Exact.unsafe : {};
			if (typeof bunUnsafe.gcAggressionLevel !== "function") bunUnsafe.gcAggressionLevel = (level) => level !== void 0 ? level : 0;
			if (typeof bunUnsafe.arrayBufferToString !== "function") bunUnsafe.arrayBufferToString = (buf) => {
				const bytes = buf instanceof ArrayBuffer ? new Uint8Array(buf) : buf instanceof Uint8Array ? buf : new Uint8Array(buf.buffer, buf.byteOffset, buf.byteLength);
				return new TextDecoder$1().decode(bytes);
			};
			if (typeof bunUnsafe.segfault !== "function") bunUnsafe.segfault = () => {
				throw new Error("Bun.unsafe.segfault is not implemented");
			};
			g.Exact.unsafe = bunUnsafe;
		}
		if (g.Exact) {
			if (typeof g.Exact.peek !== "function") g.Exact.peek = (value) => value;
			if (g.Exact.peek && typeof g.Exact.peek.status !== "function") g.Exact.peek.status = (_promise) => "pending";
		}
		if (g.Exact && !g.Exact.deepMatch) g.Exact.deepMatch = function deepMatch(obj, subset) {
			if (subset === obj) return true;
			if (subset === null || typeof subset !== "object") return subset === obj;
			if (obj === null || typeof obj !== "object") return false;
			for (const key of Object.keys(subset)) if (!deepMatch(obj[key], subset[key])) return false;
			return true;
		};
		if (g.Exact && !g.Exact.password) g.Exact.password = {
			hash: () => Promise.reject(/* @__PURE__ */ new Error("Bun.password not available in Exact runtime")),
			verify: () => Promise.reject(/* @__PURE__ */ new Error("Bun.password not available in Exact runtime")),
			hashSync: () => {
				throw new Error("Bun.password not available in Exact runtime");
			},
			verifySync: () => {
				throw new Error("Bun.password not available in Exact runtime");
			}
		};
		if (g.Exact && !g.Exact.semver) g.Exact.semver = {
			satisfies: (_version, _range) => false,
			order: (a, b) => {
				const parse = (v) => v.replace(/^[^0-9]*/, "").split(".").map(Number);
				const [aMajor = 0, aMinor = 0, aPatch = 0] = parse(a);
				const [bMajor = 0, bMinor = 0, bPatch = 0] = parse(b);
				if (aMajor !== bMajor) return aMajor - bMajor;
				if (aMinor !== bMinor) return aMinor - bMinor;
				return aPatch - bPatch;
			}
		};
		if (g.Exact && !g.Exact.CryptoHasher) {
			var _CryptoHasher;
			g.Exact.CryptoHasher = (_CryptoHasher = class CryptoHasher {
				constructor(algorithm) {
					_defineProperty(this, "_algo", void 0);
					_defineProperty(this, "_chunks", []);
					this._algo = algorithm.toLowerCase().replace(/-/g, "");
				}
				update(data) {
					if (typeof data === "string") this._chunks.push(data);
					else {
						const bytes = data instanceof Uint8Array ? data : new Uint8Array(data);
						let s = "";
						for (let i = 0; i < bytes.length; i++) s += String.fromCharCode(bytes[i]);
						this._chunks.push(s);
					}
					return this;
				}
				digest(encoding) {
					const joined = this._chunks.join("");
					const gx = globalThis;
					if (typeof gx.__exactHashSync === "function") {
						const hex = gx.__exactHashSync(this._algo, joined);
						if (!encoding || encoding === "hex") return hex;
						if (encoding === "base64" || encoding === "base64url") {
							const bytes = [];
							for (let i = 0; i < hex.length; i += 2) bytes.push(parseInt(hex.substr(i, 2), 16));
							const b64 = btoa(bytes.map((b) => String.fromCharCode(b)).join(""));
							return encoding === "base64url" ? b64.replace(/\+/g, "-").replace(/\//g, "_").replace(/=/g, "") : b64;
						}
					}
					throw new Error("CryptoHasher: native hash not available");
				}
			}, _defineProperty(_CryptoHasher, "algorithms", [
				"md5",
				"sha1",
				"sha256",
				"sha384",
				"sha512",
				"sha224"
			]), _CryptoHasher);
		}
		if (typeof g.SharedArrayBuffer === "undefined") {
			let _Symbol$toStringTag;
			g.SharedArrayBuffer = (_Symbol$toStringTag = Symbol.toStringTag, class SharedArrayBuffer {
				constructor(byteLength) {
					_defineProperty(this, "_buffer", void 0);
					const length = Math.floor(Number(byteLength));
					if (!Number.isFinite(length) || length < 0) throw new RangeError("Invalid SharedArrayBuffer length");
					this._buffer = new ArrayBuffer(length);
				}
				get byteLength() {
					return this._buffer.byteLength;
				}
				slice(begin, end) {
					const sliced = this._buffer.slice(begin, end);
					const result = new g.SharedArrayBuffer(sliced.byteLength);
					new Uint8Array(result._buffer).set(new Uint8Array(sliced));
					return result;
				}
				get [_Symbol$toStringTag]() {
					return "SharedArrayBuffer";
				}
			});
		}
		const patchBrokenSharedArrayBufferViews = () => {
			if (typeof g.SharedArrayBuffer !== "function" || typeof g.Uint8Array !== "function") return;
			let needsViewPatch = false;
			try {
				const probeBuffer = new g.SharedArrayBuffer(1);
				const probeView = new g.Uint8Array(probeBuffer);
				needsViewPatch = probeView.length === 0 || probeView.byteLength === 0 || Object.prototype.toString.call(probeView.buffer) !== "[object SharedArrayBuffer]";
			} catch {
				return;
			}
			if (!needsViewPatch) return;
			const getSharedArrayBufferBacking = (buffer) => {
				if (!buffer || typeof buffer !== "object") return null;
				if (Object.prototype.toString.call(buffer) !== "[object SharedArrayBuffer]") return null;
				if (!buffer._buffer || Object.prototype.toString.call(buffer._buffer) !== "[object ArrayBuffer]") return null;
				return buffer._buffer;
			};
			const exposeSharedArrayBufferView = (view, originalBuffer) => {
				try {
					Object.defineProperty(view, "buffer", {
						configurable: true,
						enumerable: false,
						get() {
							return originalBuffer;
						}
					});
				} catch {}
				try {
					Object.defineProperty(view, "__exactSharedArrayBuffer", {
						value: originalBuffer,
						writable: false,
						configurable: true,
						enumerable: false
					});
				} catch {}
				return view;
			};
			const wrapSharedArrayBufferViewCtor = (name) => {
				const NativeCtor = g[name];
				if (typeof NativeCtor !== "function" || NativeCtor.__exactSharedArrayBufferWrapped) return;
				const WrappedCtor = function(buffer, byteOffset, length) {
					if (!(this instanceof WrappedCtor)) throw new TypeError(`Constructor ${name} requires "new"`);
					const backing = getSharedArrayBufferBacking(buffer);
					if (backing) {
						let view;
						if (arguments.length <= 1) view = new NativeCtor(backing);
						else if (arguments.length === 2) view = new NativeCtor(backing, byteOffset);
						else view = new NativeCtor(backing, byteOffset, length);
						return exposeSharedArrayBufferView(view, buffer);
					}
					if (arguments.length === 0) return new NativeCtor();
					if (arguments.length === 1) return new NativeCtor(buffer);
					if (arguments.length === 2) return new NativeCtor(buffer, byteOffset);
					return new NativeCtor(buffer, byteOffset, length);
				};
				WrappedCtor.prototype = NativeCtor.prototype;
				try {
					Object.defineProperty(WrappedCtor.prototype, "constructor", {
						value: WrappedCtor,
						writable: true,
						configurable: true,
						enumerable: false
					});
				} catch {}
				try {
					Object.setPrototypeOf(WrappedCtor, NativeCtor);
				} catch {}
				try {
					Object.defineProperty(WrappedCtor, "name", {
						value: name,
						configurable: true
					});
				} catch {}
				try {
					Object.defineProperty(WrappedCtor, "__exactSharedArrayBufferWrapped", {
						value: true,
						writable: false,
						configurable: true,
						enumerable: false
					});
				} catch {}
				try {
					Object.defineProperty(g, name, {
						value: WrappedCtor,
						writable: true,
						configurable: true,
						enumerable: false
					});
				} catch {
					g[name] = WrappedCtor;
				}
			};
			const typedArrayNames = [
				"Int8Array",
				"Uint8Array",
				"Uint8ClampedArray",
				"Int16Array",
				"Uint16Array",
				"Int32Array",
				"Uint32Array",
				"Float32Array",
				"Float64Array"
			];
			if (typeof g.Float16Array === "function") typedArrayNames.push("Float16Array");
			if (typeof g.BigInt64Array === "function") typedArrayNames.push("BigInt64Array");
			if (typeof g.BigUint64Array === "function") typedArrayNames.push("BigUint64Array");
			{
				const __exactForOfIterator122 = typedArrayNames[Symbol.iterator]();
				for (;;) {
					const __exactForOfStep122 = __exactForOfIterator122.next();
					if (__exactForOfStep122.done) break;
					const name = __exactForOfStep122.value;
					wrapSharedArrayBufferViewCtor(name);
				}
			}
			wrapSharedArrayBufferViewCtor("DataView");
		};
		patchBrokenSharedArrayBufferViews();
		const ensureSharedArrayBufferUint8ArrayWorks = () => {
			if (typeof g.SharedArrayBuffer !== "function" || typeof g.Uint8Array !== "function") return;
			const exposeUint8ArrayBuffer = (view, originalBuffer) => {
				try {
					Object.defineProperty(view, "buffer", {
						configurable: true,
						enumerable: false,
						get() {
							return originalBuffer;
						}
					});
				} catch {}
				try {
					Object.defineProperty(view, "__exactSharedArrayBuffer", {
						value: originalBuffer,
						writable: false,
						configurable: true,
						enumerable: false
					});
				} catch {}
				return view;
			};
			try {
				const probeBuffer = new g.SharedArrayBuffer(1);
				const probeView = new g.Uint8Array(probeBuffer);
				if (probeView.length === 1 && probeView.byteLength === 1 && Object.prototype.toString.call(probeView.buffer) === "[object SharedArrayBuffer]") return;
			} catch {}
			const NativeUint8Array = g.Uint8Array;
			const WrappedUint8Array = function Uint8Array(buffer, byteOffset, length) {
				if (!(this instanceof WrappedUint8Array)) throw new TypeError("Constructor Uint8Array requires \"new\"");
				const backing = buffer && buffer._buffer;
				if (backing && Object.prototype.toString.call(backing) === "[object ArrayBuffer]") {
					let view;
					if (arguments.length <= 1) view = new NativeUint8Array(backing);
					else if (arguments.length === 2) view = new NativeUint8Array(backing, byteOffset);
					else view = new NativeUint8Array(backing, byteOffset, length);
					return exposeUint8ArrayBuffer(view, buffer);
				}
				if (arguments.length === 0) return new NativeUint8Array();
				if (arguments.length === 1) return new NativeUint8Array(buffer);
				if (arguments.length === 2) return new NativeUint8Array(buffer, byteOffset);
				return new NativeUint8Array(buffer, byteOffset, length);
			};
			WrappedUint8Array.prototype = NativeUint8Array.prototype;
			try {
				Object.setPrototypeOf(WrappedUint8Array, NativeUint8Array);
			} catch {}
			try {
				Object.defineProperty(WrappedUint8Array.prototype, "constructor", {
					value: WrappedUint8Array,
					writable: true,
					configurable: true
				});
			} catch {}
			try {
				Object.defineProperty(WrappedUint8Array, "__exactSharedArrayBufferWrapped", {
					value: true,
					writable: false,
					configurable: true
				});
			} catch {}
			try {
				Object.defineProperty(g, "Uint8Array", {
					value: WrappedUint8Array,
					writable: true,
					configurable: true,
					enumerable: false
				});
			} catch {
				g.Uint8Array = WrappedUint8Array;
			}
		};
		ensureSharedArrayBufferUint8ArrayWorks();
		if (typeof g.Atomics === "undefined") g.Atomics = {
			add(ta, index, value) {
				const old = ta[index];
				ta[index] += value;
				return old;
			},
			and(ta, index, value) {
				const old = ta[index];
				ta[index] &= value;
				return old;
			},
			compareExchange(ta, index, expected, replacement) {
				const old = ta[index];
				if (old === expected) ta[index] = replacement;
				return old;
			},
			exchange(ta, index, value) {
				const old = ta[index];
				ta[index] = value;
				return old;
			},
			isLockFree(size) {
				return size === 1 || size === 2 || size === 4;
			},
			load(ta, index) {
				return ta[index];
			},
			or(ta, index, value) {
				const old = ta[index];
				ta[index] |= value;
				return old;
			},
			store(ta, index, value) {
				ta[index] = value;
				return value;
			},
			sub(ta, index, value) {
				const old = ta[index];
				ta[index] -= value;
				return old;
			},
			wait() {
				return "not-equal";
			},
			notify() {
				return 0;
			},
			xor(ta, index, value) {
				const old = ta[index];
				ta[index] ^= value;
				return old;
			},
			[Symbol.toStringTag]: "Atomics"
		};
		if (typeof g.Float16Array === "undefined") g.Float16Array = class Float16Array extends Uint16Array {
			static get BYTES_PER_ELEMENT() {
				return 2;
			}
			get [Symbol.toStringTag]() {
				return "Float16Array";
			}
		};
		installPolyfills();
		installPromiseRejectionTracking();
		if (g.__exactLoadTimings) g.__exactLoadTimings.installGlobalsEnd = Date.now();
	}
	function getRuntimeVersion() {
		return "0.1.0";
	}
	function detectEngine() {
		return "hermes";
	}
	function detectPlatform() {
		const g = globalThis;
		if (typeof g.__exactPlatform === "string" && g.__exactPlatform.length > 0) return g.__exactPlatform;
		const hostProcess = g.process;
		if (hostProcess) {
			let isExactProcess = false;
			try {
				const versions = hostProcess.versions;
				isExactProcess = !!(versions && typeof versions === "object" && (typeof versions.ibex === "string" || typeof versions.exact === "string"));
			} catch {
				isExactProcess = false;
			}
			if (!isExactProcess) try {
				const platform = hostProcess.platform;
				if (typeof platform === "string" && platform.length > 0) return platform;
			} catch {}
		}
		return "ios";
	}
	const runtimeInfo = {
		version: "0.1.0",
		engine: "hermes",
		get platform() {
			return detectPlatform();
		}
	};
	function areGlobalsInstalled() {
		return globalThis.__exactRuntimeLoaded === true;
	}
	const runtimeBundle = {
		installGlobals,
		areGlobalsInstalled,
		getRuntimeVersion,
		detectEngine,
		detectPlatform,
		runtimeInfo
	};
	globalThis.ExactBundle = globalThis.ExactBundle || runtimeBundle;
	if (globalThis.ExactBundle && globalThis.ExactBundle !== runtimeBundle) Object.assign(globalThis.ExactBundle, runtimeBundle);
	//#endregion
	//#region ../ibex-runtime-js/src/runtime-entry.ts
	/**
	* Exact Runtime Entry Point
	*
	* This file is the entry point for building the standalone runtime bundle.
	* It installs all web-standard globals and exposes runtime utilities.
	*
	* Build: vite build --config vite.config.runtime.ts
	* Output: dist/runtime.js -> hermesc -> dist/runtime.hbc
	*/
	installGlobals();
	globalThis.__exactRuntime = {
		version: getRuntimeVersion(),
		installed: true,
		installedAt: Date.now(),
		engine: detectEngine(),
		platform: detectPlatform()
	};
	const g = globalThis;
	if (!g.exact) g.exact = {};
	g.exact.runtime = {
		version: getRuntimeVersion(),
		info: runtimeInfo,
		isInstalled: areGlobalsInstalled,
		detectEngine,
		detectPlatform
	};
	try {
		if (!globalThis.__exactSuppressRuntimeBanner) console.log(`[Exact Runtime v${getRuntimeVersion()}] Installed (${detectEngine()})`);
	} catch {}
	//#endregion
})();
