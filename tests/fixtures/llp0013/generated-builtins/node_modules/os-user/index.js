// Statically imports the `os` builtin. The generator observes this specifier
// and emits `builtins: ["node:os"]`, so the import is allowed under enforce.
var os = require("os");
module.exports = function osUser() {
  try {
    return "OK:" + typeof os.hostname;
  } catch (e) {
    return "IMPORT-DENIED";
  }
};
