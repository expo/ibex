// shared-pkg @2.0.0 — the top-level version the app imports directly.
var fs = require("fs");
module.exports.v = "v2";
module.exports.read = function read(p) {
  try {
    return "READ:" + String(fs.readFileSync(p)).trim();
  } catch (e) {
    return "DENIED";
  }
};
