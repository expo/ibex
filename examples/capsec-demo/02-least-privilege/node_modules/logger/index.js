module.exports.inspectAmbientEnvironment = function inspectAmbientEnvironment() {
  try {
    return "UNEXPECTED process.env.APP_MODE=" + process.env.APP_MODE;
  } catch (error) {
    return "WITHHELD (" + (error && error.name) + ")";
  }
};
