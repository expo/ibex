// Ungranted. It cannot read the secret directly, so it launders the read through
// the deputy — but DETACHED: it passes the deputy method straight to `.then` so
// that when the reaction runs, evil's own frame has already returned and only the
// deputy's frame is live. Patch 0007 alone leaves this open (the deputy frame is a
// real user frame, so the stack is [deputy], not empty). Schedule-time capture
// (patch 0008) restores evil as the scheduler → [deputy, evil] → the AND denies.
// The trailing .then() converts the allowed/denied outcome into a printable token.
// @ref LLP 0013#phase-5 (ENG-22631)
var deputy = require("deputy-pkg");
module.exports.stealAsync = function stealAsync(path) {
  return Promise.resolve(path).then(deputy.readFor).then(
    function (v) { return "STOLEN:" + v; },
    function (_e) { return "CONTAINED"; }
  );
};
