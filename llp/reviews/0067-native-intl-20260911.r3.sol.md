# Native Intl correction-delta code review — r3.sol

**Date:** 2026-09-11
**Model / effort:** gpt-5.6-sol / xhigh
**Session:** `01a09227-cc82-7f61-a189-738bf5455d59`
**Interval:** 2026-09-11T20:27:56Z–20:34:52Z
**Exit:** 0
**Target:** `12dfbf3fd20fe0986768a3a5443972d38e69566c`
**Base:** `02cf5b12dc2f3ebe2b64327fd57457eb0ae68485`
**Candidate archive SHA-256:** `132ee9bce899d38bbe3503c32d113ee9f4d145f1557eb6c77aa4579c2d83539a`
**Correction patch SHA-256:** `bad4f0f7b62cd8acd9d8bc30ae7219d64a7a2039a9198632f9318ce989c0f315`
**Reference:** exact upstream es2020 negotiation source, SHA-256
`a7e43d1fbaaaafc8b6d2c6addeeb1f9250d533e4f6481e7a39643c35b6b059f6`.
**Method:** Mutually blind correction-only review, one reviewer per family.
No peer artifacts/findings, subagents, clones, worktrees, remotes, web, edits,
builds or tests. Exact selected-spec source was supplied to both reviewers.
Only public response text follows; escaped newlines rendered for readability.
The TimeClip child is a separate addendum, not covered by this target review.
Historical verdicts are not rewritten after root disposition; see release record.

## Received review text

NOT READY

Material finding:

- **High — Unicode option aliases are not canonicalized before locale-data membership.** [intl_datetime.rs:328](/Users/ccheever/release-inputs/snapback2-intl-20260911/r3/candidate/crates/ibex2/src/stdlib/intl_datetime.rs:328) and [intl.rs:329](/Users/ccheever/release-inputs/snapback2-intl-20260911/r3/candidate/crates/ibex2/src/stdlib/intl.rs:329) only ASCII-lowercase explicit types. ES2020 instead requires canonical-syntax and alias replacement before membership ([reference:240](/Users/ccheever/release-inputs/snapback2-intl-20260911/r3/ecma402-es2020-negotiation.html:240)). The changed final-value retention at [intl_datetime.rs:693](/Users/ccheever/release-inputs/snapback2-intl-20260911/r3/candidate/crates/ibex2/src/stdlib/intl_datetime.rs:693) then compounds the error.

  Reproducible scenario:

  ```js
  new Intl.DateTimeFormat("en-u-ca-buddhist", {
    calendar: "islamicc",
    timeZone: "UTC",
    year: "numeric"
  }).resolvedOptions()
  ```

  ES2020 canonicalizes `islamicc` to supported `islamic-civil`, selects it, and removes the differing requested `ca-buddhist` extension. Static tracing shows the candidate treats `islamicc` as unsupported, selects `buddhist`, and retains `en-u-ca-buddhist`. The complete-type alias `ethiopic-amete-alem` → `ethioaa` exposes the same omission.

  Minimal correction: perform full key-aware Unicode type canonicalization—not merely lowercase—before support lookup and comparison for `ca` and `nu`, preserving complete multi-subtag values. Add differing-extension regressions for `islamicc` and `ethiopic-amete-alem`.

Optional test suggestion: the new `Reflect.construct(fn, [])` assertions can mistake a constructable function’s receiver-brand `TypeError` for nonconstructability. Use each function as `newTarget`, such as `Reflect.construct(function () {}, [], fn)`, to test `[[Construct]]` without executing its body.

Inherited limitations remain unchanged: primitive-`newTarget.prototype` fallback, DateTimeFormat basic-matcher behavior, and the documented platform/constructor gaps. Artifact qualification remains release work, not this finding.

Inspection limits: read `correction.patch` first; inspected only the seven directly changed candidate source/test files and the supplied reference, whose SHA-256 matched. The workspace had no Git metadata, so commit identities could not be independently verified. No builds, tests, edits, web access, parent-directory inspection, peer outputs, or subagents were used.

