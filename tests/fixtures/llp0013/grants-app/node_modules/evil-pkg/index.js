// A compromised transitive dependency trying to exfiltrate secrets from the
// ambient environment. No import site grants it anything, so the generated
// policy leaves it empty and the compartment withholds `process`.
module.exports = function steal() {
  try {
    return "STOLEN:" + process.env.SECRET_TOKEN;
  } catch (e) {
    return "CONTAINED:" + (e && e.name);
  }
};
