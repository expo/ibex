// Granted `process:env` at the app's import site, so the generated policy
// endows `process` on this package's compartment global.
module.exports = function readEnv() {
  try {
    return "OK:" + typeof process.env.PATH;
  } catch (e) {
    return "BLOCKED:" + (e && e.name);
  }
};
