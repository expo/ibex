// A compromised dependency probing runtime capability-escape channels that must
// all be closed under --capsec enforce (evil-pkg is granted nothing; builtins:[]).

// ENG-22695 — self-grant: call Exact.setModuleCapabilities for our own package
// id, then attempt a read the policy denies. The self-grant must not stick.
module.exports.selfGrant = function selfGrant(secretPath) {
  var present = "absent";
  try {
    if (typeof Exact === "object" && typeof Exact.setModuleCapabilities === "function") {
      present = "present";
      Exact.setModuleCapabilities(module.__exactPackageId, ["fs:read"]);
    }
  } catch (e) { present = "threw"; }
  var read;
  try { require("fs").readFileSync(secretPath); read = "STOLEN"; }
  catch (e) { read = "DENIED"; }
  return "setModuleCapabilities=" + present + " read=" + read;
};

// ENG-22697 — builtin aliases: exact:/bun: namespaces must be gated like node:.
module.exports.aliases = function aliases() {
  return ["exact:sqlite", "bun:sqlite", "bun:fs"].map(function (a) {
    try { require(a); return a + "=IMPORTED"; }
    catch (e) { return a + "=DENIED"; }
  }).join(" ");
};

// ENG-22696 — detached require: a bare globalThis.require callback runs with no
// package frame and must fail closed, not inherit trusted/root.
module.exports.detachedRequire = function detachedRequire() {
  return Promise.resolve("fs").then(globalThis.require).then(
    function (fs) { return "IMPORTED:" + typeof fs.readFileSync; },
    function (e) { return "DENIED"; }
  );
};
