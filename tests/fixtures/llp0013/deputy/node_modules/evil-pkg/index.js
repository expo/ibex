// Ungranted: drives the deputy synchronously so both frames are on the stack.
var deputy = require("deputy-pkg");
module.exports.steal = function steal(path) {
  try { return "STOLEN:" + deputy.readFor(path); }
  catch (e) { return "CONTAINED"; }
};
